---
task_id: task_8e58168d2194
title: 'تلفیق: mechanical:title (2 تسک)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-18T04:27:44.357447+00:00'
updated_at: '2026-05-29T20:23:52.624210+00:00'
tags:
- consolidated
- post_verify_merge
---

# تلفیق: mechanical:title (2 تسک)

## Raw Idea

🧬 این یک تسک تلفیقی است — از 2 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): شباهت عنوان (trigram Jaccard ≥ 0.7)
🎯 theme: mechanical:title
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 2
  id: d866cc4e-7bbe-420b-a849-5dd8249a95b2
  عنوان اصلی: [منطق] عدم وجود hallucination guards در سطح pipeline
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: -

📋 acceptance_criteria کامل:
  - هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد [verify_method=static] [verify_plan={"grep_patterns": ["oversight_strong_prompt", "ai_manager", "hallucination", "consistency", "validation"], "files_hint": ["backend/app/pipelines/ai_llm.py", "backend/app/components/oversight_strong_pr]
  - ground truth تعیین شد و طرف دیگر align شد [verify_method=static] [verify_plan={"grep_patterns": ["ground.truth", "align", "hallucination.guard", "consistency.check"], "files_hint": ["backend/app/pipelines/ai_llm.py", "backend/app/components/oversight_strong_prompt.py", "backend]
  - integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_pipelines/test_ai_llm.py::test_integration", "timeout_seconds": 120}]
  - PR description توضیح می‌دهد چرا این تصمیم گرفته شد [verify_method=static] [verify_plan={"grep_patterns": ["PR description", "decision", "why"], "files_hint": [".github/pull_request_template.md"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guards در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.

## 💥 پیامد (impact)
AI ممکن است اطلاعات نادرست، مسیرهای فایل غیرواقعی، یا کدهای غیرقابل اجرا تولید کند که مستقیماً به کاربر نهایی تحویل داده شود.

## 🛠 پیشنهاد رفع اولیه
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  - بررسی وضعیت فعلی pipeline ai_llm و شناسایی فایل‌های مرتبط — بررسی کامل فایل‌های oversight_strong_prompt و ai_manager و شناسایی ناسازگاری
  - طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل — طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل
  - پیاده‌سازی ماژول SyntaxChecker برای اعتبارسنجی syntax کدهای تولید شده — پیاده‌سازی ماژول SyntaxChecker برای بررسی syntax کدها
  - پیاده‌سازی ماژول FactChecker با استفاده از LLM دوم برای اعتبارسنجی محتوا — پیاده‌سازی ماژول FactChecker با LLM دوم
  - یکپارچه‌سازی PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm — یکپارچه‌سازی سه ماژول در pipeline ai_llm
  - نوشتن تست‌های واحد برای PostProcessor، SyntaxChecker و FactChecker — نوشتن unit tests برای PostProcessor, SyntaxChecker, FactChecker
  - نوشتن تست‌های یکپارچه‌سازی برای pipeline جدید — نوشتن integration tests برای pipeline جدید
  - به‌روزرسانی مستندات و اضافه کردن کامنت‌های توضیحی در کد — به‌روزرسانی مستندات و افزودن کامنت‌های توضیحی

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 2
  id: 3f80ca64-610f-4a2e-848c-9fb93ee9963e
  عنوان اصلی: [منطق] عدم وجود hallucination guard در سطح pipeline
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: -

📋 acceptance_criteria کامل:
  - هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد [verify_method=static] [verify_plan={"grep_patterns": ["hallucination", "fact.check", "source.ground", "confidence.score"], "files_hint": ["backend/app/pipeline/ai_llm.py", "backend/app/pipeline/components/*.py"]}]
  - ground truth تعیین شد و طرف دیگر align شد [verify_method=static] [verify_plan={"grep_patterns": ["ground.truth", "align", "target_locations", "related_files"], "files_hint": ["backend/app/pipeline/ai_llm.py", "backend/app/pipeline/hallucination_guard.py"]}]
  - integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_pipeline_ai_llm.py", "timeout_seconds": 120}]
  - PR description توضیح می‌دهد چرا این تصمیم گرفته شد [verify_method=manual_only] [verify_plan={"reason": "نیاز به بازبینی دستی"}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guard در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها مکانیزمی برای تشخیص یا کاهش توهم (hallucination) مانند fact-checking, source grounding, یا confidence scoring ندارند. این در oversight که نیازمند دقت بالاست حیاتی است.

## 💥 پیامد (impact)
احتمال تولید پاسخ‌های نادرست یا گمراه‌کننده در تسک‌های حساس oversight

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  - بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard — بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard
  - طراحی و پیاده‌سازی ماژول HallucinationGuard با قابلیت fact-checking و source grounding — طراحی و پیاده‌سازی ماژول HallucinationGuard با fact-checking و source grounding
  - یکپارچه‌سازی HallucinationGuard در pipeline ai_llm به عنوان یک stage پس از تولید خروجی LLM — یکپارچه‌سازی HallucinationGuard در pipeline ai_llm به عنوان stage پس از تولید
  - طراحی و پیاده‌سازی endpoint API برای دریافت گزارش HallucinationGuard به همراه خروجی LLM — طراحی و پیاده‌سازی endpoint API برای دریافت گزارش HallucinationGuard
  - اصلاح UI (NextJS) برای نمایش گزارش HallucinationGuard در کنار خروجی LLM — اصلاح UI برای نمایش گزارش HallucinationGuard در کنار خروجی LLM
  - نوشتن unit tests برای ماژول HallucinationGuard — نوشتن unit tests برای ماژول HallucinationGuard
  - نوشتن integration tests برای pipeline ai_llm با HallucinationGuard یکپارچه‌شده — نوشتن integration tests برای pipeline ai_llm با HallucinationGuard
  - نوشتن PR description جامع با توضیح ناسازگاری، راه‌حل، و checklist کامیت‌ها — نوشتن PR description جامع با توضیح ناسازگاری و checklist کامیت‌ها

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: d866cc4e-7bbe-420b-a849-5dd8249a95b2, 3f80ca64-610f-4a2e-848c-9fb93ee9963e`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Prompt

🧬 این یک تسک تلفیقی است — از 2 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): شباهت عنوان (trigram Jaccard ≥ 0.7)
🎯 theme: mechanical:title
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 2
  id: d866cc4e-7bbe-420b-a849-5dd8249a95b2
  عنوان اصلی: [منطق] عدم وجود hallucination guards در سطح pipeline
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: -

📋 acceptance_criteria کامل:
  - هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد [verify_method=static] [verify_plan={"grep_patterns": ["oversight_strong_prompt", "ai_manager", "hallucination", "consistency", "validation"], "files_hint": ["backend/app/pipelines/ai_llm.py", "backend/app/components/oversight_strong_pr]
  - ground truth تعیین شد و طرف دیگر align شد [verify_method=static] [verify_plan={"grep_patterns": ["ground.truth", "align", "hallucination.guard", "consistency.check"], "files_hint": ["backend/app/pipelines/ai_llm.py", "backend/app/components/oversight_strong_prompt.py", "backend]
  - integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_pipelines/test_ai_llm.py::test_integration", "timeout_seconds": 120}]
  - PR description توضیح می‌دهد چرا این تصمیم گرفته شد [verify_method=static] [verify_plan={"grep_patterns": ["PR description", "decision", "why"], "files_hint": [".github/pull_request_template.md"]}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guards در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.

## 💥 پیامد (impact)
AI ممکن است اطلاعات نادرست، مسیرهای فایل غیرواقعی، یا کدهای غیرقابل اجرا تولید کند که مستقیماً به کاربر نهایی تحویل داده شود.

## 🛠 پیشنهاد رفع اولیه
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  - بررسی وضعیت فعلی pipeline ai_llm و شناسایی فایل‌های مرتبط — بررسی کامل فایل‌های oversight_strong_prompt و ai_manager و شناسایی ناسازگاری
  - طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل — طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل
  - پیاده‌سازی ماژول SyntaxChecker برای اعتبارسنجی syntax کدهای تولید شده — پیاده‌سازی ماژول SyntaxChecker برای بررسی syntax کدها
  - پیاده‌سازی ماژول FactChecker با استفاده از LLM دوم برای اعتبارسنجی محتوا — پیاده‌سازی ماژول FactChecker با LLM دوم
  - یکپارچه‌سازی PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm — یکپارچه‌سازی سه ماژول در pipeline ai_llm
  - نوشتن تست‌های واحد برای PostProcessor، SyntaxChecker و FactChecker — نوشتن unit tests برای PostProcessor, SyntaxChecker, FactChecker
  - نوشتن تست‌های یکپارچه‌سازی برای pipeline جدید — نوشتن integration tests برای pipeline جدید
  - به‌روزرسانی مستندات و اضافه کردن کامنت‌های توضیحی در کد — به‌روزرسانی مستندات و افزودن کامنت‌های توضیحی

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 2
  id: 3f80ca64-610f-4a2e-848c-9fb93ee9963e
  عنوان اصلی: [منطق] عدم وجود hallucination guard در سطح pipeline
  اولویت اصلی: high
  وضعیت verify قبلی: pending
  فایل‌های دخیل: -

📋 acceptance_criteria کامل:
  - هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد [verify_method=static] [verify_plan={"grep_patterns": ["hallucination", "fact.check", "source.ground", "confidence.score"], "files_hint": ["backend/app/pipeline/ai_llm.py", "backend/app/pipeline/components/*.py"]}]
  - ground truth تعیین شد و طرف دیگر align شد [verify_method=static] [verify_plan={"grep_patterns": ["ground.truth", "align", "target_locations", "related_files"], "files_hint": ["backend/app/pipeline/ai_llm.py", "backend/app/pipeline/hallucination_guard.py"]}]
  - integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_pipeline_ai_llm.py", "timeout_seconds": 120}]
  - PR description توضیح می‌دهد چرا این تصمیم گرفته شد [verify_method=manual_only] [verify_plan={"reason": "نیاز به بازبینی دستی"}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guard در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها مکانیزمی برای تشخیص یا کاهش توهم (hallucination) مانند fact-checking, source grounding, یا confidence scoring ندارند. این در oversight که نیازمند دقت بالاست حیاتی است.

## 💥 پیامد (impact)
احتمال تولید پاسخ‌های نادرست یا گمراه‌کننده در تسک‌های حساس oversight

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  - بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard — بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard
  - طراحی و پیاده‌سازی ماژول HallucinationGuard با قابلیت fact-checking و source grounding — طراحی و پیاده‌سازی ماژول HallucinationGuard با fact-checking و source grounding
  - یکپارچه‌سازی HallucinationGuard در pipeline ai_llm به عنوان یک stage پس از تولید خروجی LLM — یکپارچه‌سازی HallucinationGuard در pipeline ai_llm به عنوان stage پس از تولید
  - طراحی و پیاده‌سازی endpoint API برای دریافت گزارش HallucinationGuard به همراه خروجی LLM — طراحی و پیاده‌سازی endpoint API برای دریافت گزارش HallucinationGuard
  - اصلاح UI (NextJS) برای نمایش گزارش HallucinationGuard در کنار خروجی LLM — اصلاح UI برای نمایش گزارش HallucinationGuard در کنار خروجی LLM
  - نوشتن unit tests برای ماژول HallucinationGuard — نوشتن unit tests برای ماژول HallucinationGuard
  - نوشتن integration tests برای pipeline ai_llm با HallucinationGuard یکپارچه‌شده — نوشتن integration tests برای pipeline ai_llm با HallucinationGuard
  - نوشتن PR description جامع با توضیح ناسازگاری، راه‌حل، و checklist کامیت‌ها — نوشتن PR description جامع با توضیح ناسازگاری و checklist کامیت‌ها

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: d866cc4e-7bbe-420b-a849-5dd8249a95b2, 3f80ca64-610f-4a2e-848c-9fb93ee9963e`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند


## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی کامل فایل‌های مرتبط با pipeline ai_llm (backend/app/pipelines/ai_llm.py، backend/app/components/oversight_strong_prompt.py، backend/app/components/ai_manager.py) برای شناسایی ناسازگاری منطقی و مستندسازی فرض‌های هر کامپوننت است. خارج از این مرحله: پیاده‌سازی هرگونه کد جدید یا اصلاح کد موجود. نکته حیاتی: باید با grep/search و خواندن فایل‌ها بررسی شود که آیا بخشی از این کار قبلاً انجام شده است.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد: هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.

🔧 مراحل remaining که در super-task باید انجام شوند:
  - بررسی وضعیت فعلی pipeline ai_llm و شناسایی فایل‌های مرتبط — بررسی کامل فایل‌های oversight_strong_prompt و ai_manager و شناسایی ناسازگاری
```

### Step 2: طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی کلاس PostProcessor است که مسیرهای فایل ذکر شده در خروجی LLM را با target_locations تطبیق می‌دهد. خارج از این مرحله: پیاده‌سازی SyntaxChecker یا FactChecker. نکته حیاتی: باید از قبل بررسی شود که آیا کلاس مشابهی وجود دارد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

🔧 مراحل remaining که در super-task باید انجام شوند:
  - طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل — طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل
```

### Step 3: پیاده‌سازی ماژول SyntaxChecker برای اعتبارسنجی syntax کدهای تولید شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی ماژول SyntaxChecker است که کدهای تولید شده توسط LLM را از نظر syntax بررسی می‌کند. خارج از این مرحله: پیاده‌سازی PostProcessor یا FactChecker. نکته حیاتی: باید زبان‌های برنامه‌نویسی مختلف (Python, JavaScript, TypeScript) را پشتیبانی کند.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

🔧 مراحل remaining که در super-task باید انجام شوند:
  - پیاده‌سازی ماژول SyntaxChecker برای اعتبارسنجی syntax کدهای تولید شده — پیاده‌سازی ماژول SyntaxChecker برای بررسی syntax کدها
```

### Step 4: پیاده‌سازی ماژول FactChecker با استفاده از LLM دوم برای اعتبارسنجی محتوا
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی ماژول FactChecker است که از یک LLM دوم برای fact-checking خروجی LLM اول استفاده می‌کند. خارج از این مرحله: پیاده‌سازی PostProcessor یا SyntaxChecker. نکته حیاتی: باید از LLM دوم برای بررسی consistency با context اصلی استفاده کند.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

🔧 مراحل remaining که در super-task باید انجام شوند:
  - پیاده‌سازی ماژول FactChecker با استفاده از LLM دوم برای اعتبارسنجی محتوا — پیاده‌سازی ماژول FactChecker با LLM دوم
```

### Step 5: یکپارچه‌سازی PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm
**Status:** `pending` (0%)
**Scope:** این مرحله شامل یکپارچه‌سازی سه ماژول PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm به عنوان stageهای پس از تولید خروجی LLM است. خارج از این مرحله: نوشتن تست‌ها. نکته حیاتی: ترتیب اجرا باید PostProcessor → SyntaxChecker → FactChecker باشد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

🔧 مراحل remaining که در super-task باید انجام شوند:
  - یکپارچه‌سازی PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm — یکپارچه‌سازی سه ماژول در pipeline ai_llm
```

### Step 6: نوشتن تست‌های واحد و یکپارچه‌سازی برای PostProcessor، SyntaxChecker، FactChecker و pipeline جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای PostProcessor، SyntaxChecker و FactChecker و integration tests برای pipeline ai_llm با ماژول‌های جدید است. خارج از این مرحله: به‌روزرسانی مستندات. نکته حیاتی: تست‌ها باید تمام ACها را پوشش دهند و با pytest اجرا شوند.
**Excerpt:**
```
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_pipelines/test_ai_llm.py::test_integration", "timeout_seconds": 120}]

🔧 مراحل remaining که در super-task باید انجام شوند:
  - نوشتن تست‌های واحد برای PostProcessor، SyntaxChecker و FactChecker — نوشتن unit tests برای PostProcessor, SyntaxChecker, FactChecker
  - نوشتن تست‌های یکپارچه‌سازی برای pipeline جدید — نوشتن integration tests برای pipeline جدید
```
