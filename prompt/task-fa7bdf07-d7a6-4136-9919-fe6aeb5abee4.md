---
task_id: fa7bdf07-d7a6-4136-9919-fe6aeb5abee4
title: '[بصری] قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص'
type: bug
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: needs_clarification
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-11T17:10:36.204649+00:00'
updated_at: '2026-06-03T18:07:30.680475+00:00'
target_files:
- backend/app/api/routes/debate.py
- backend/app/models/debate.py
- backend/app/main.py
- frontend/src/app/debate/page.tsx
- frontend/src/hooks/use
- backend/app/services/debate_service.py
- backend/app/services/oversight_strong_prompt.py
- backend/app/services/notification_service.py
- backend/app/api/routes/oversight.py
---

# [بصری] قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص

## Raw Idea

قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه
یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد و مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
این قسمت هم باید در نوتیفیکشین ریدف خاص خودش رو داشته باشه و همینطور از طریق تلگرام هم بشه محتواش رو مشاهده کرد و صداش زد

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه
یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد و مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
این قسمت هم باید در نوتیفیکشین ریدف خاص خودش رو داشته باشه و همینطور از طریق تلگرام هم بشه محتواش رو مشاهده کرد و صداش زد
```

## 📋 چک‌لیست مراحل (5 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: تعریف مدل داده و API برای اتصال صفحه/پروژه به مناظره** — ایجاد مدل‌های داده (مثلاً DebateAttachment) و endpoint‌های CRUD برای ذخیره و بازیابی اتصال یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه به یک مناظره. خارج از این مرحله: UI فرانت‌اند و نوتیفیکیشن.
- [ ] **مرحله 2: اضافه کردن UI انتخاب و اتصال محتوا در صفحه مناظره** — تغییر فرانت‌اند صفحه مناظره برای نمایش یک دکمه/پنل «اتصال محتوا» که کاربر بتواند از طریق آن یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه را انتخاب و متصل کند. خارج از این مرحله: نوتیفیکیشن و تلگرام.
- [ ] **مرحله 3: ارسال محتوای کامل متصل‌شده به مدل‌های زبانی در حین مناظره** — هنگام ارسال درخواست به مدل‌های زبانی (LLM)، محتوای کامل صفحه/پروژه متصل‌شده را به عنوان context به prompt اضافه کنید. خارج از این مرحله: نوتیفیکیشن و تلگرام.
- [ ] **مرحله 4: پیاده‌سازی نوتیفیکیشن داخلی برای رویداد اتصال محتوا** — اضافه کردن یک نوع نوتیفیکیشن جدید (مثلاً debate_attached) در سیستم نوتیفیکیشن موجود، شامل ذخیره و نمایش در فرانت‌اند. خارج از این مرحله: تلگرام.
- [ ] **مرحله 5: یکپارچه‌سازی با تلگرام برای مشاهده و تعامل با مناظره متصل‌شده** — در ربات تلگرام، قابلیت نمایش محتوای متصل‌شده (صفحه/پروژه) و امکان شروع/ادامه مناظره از طریق تلگرام را اضافه کنید. خارج از این مرحله: نوتیفیکیشن داخلی.

---

# 🔹 مرحله 1: تعریف مدل داده و API برای اتصال صفحه/پروژه به مناظره

**Scope:** ایجاد مدل‌های داده (مثلاً DebateAttachment) و endpoint‌های CRUD برای ذخیره و بازیابی اتصال یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه به یک مناظره. خارج از این مرحله: UI فرانت‌اند و نوتیفیکیشن.
**Key terms:** DebateAttachment, endpoint, API, مدل داده

**بخش مربوط از متن کاربر:**
```
قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد و مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد مدل داده DebateAttachment و APIهای CRUD برای اتصال صفحه/پروژه به مناظره

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/debate.py:1-10` — `router` — فایل deep-read نشده — بر اساس ساختار سطحی. اینجا باید endpoint جدید اضافه شود یا router جدید ایجاد گردد.
  ```python
  from fastapi import APIRouter, HTTPException, Depends
  from pydantic import BaseModel
  from typing import List, Optional
  from sqlalchemy.orm import Session
  
  from ...core.database import get_db
  from ...models.debate import Debate
  from ...services.debate_service import get_debate_service
  
  logger = logging.getLogger(__name__)
  router = APIRouter(prefix="/debate", tags=["Debate"])
  ```
- `backend/app/models/debate.py:1-10` — `Debate model` — مدل DebateAttachment باید به این مدل FK داشته باشد (debate_id).
  ```python
  فایل deep-read نشده — مدل موجود Debate احتمالاً شامل فیلدهای id, title, models, status, created_at است.
  ```
- `backend/app/main.py:1-30` — `app.include_router` — باید router جدید debate_attachment در اینجا ثبت شود.
  ```python
  فایل deep-read نشده — احتمالاً شامل import و include_router برای همه routeهاست.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/app/debate/page.tsx` — صفحه اصلی مناظره که UI اتصال در آن

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک قابلیت جدید برای اتصال یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه به یک مناظره را دارد. این قابلیت باید از طریق مدل داده‌ای به نام DebateAttachment و endpointهای CRUD پیاده‌سازی شود. هدف این است که در صفحه مناظره (frontend/src/app/debate/page.tsx) بتوان یک صفحه، زیرصفحه، پروژه لود شده یا بخشی از پروژه را انتخاب کرد و به مناظره متصل نمود تا مدل‌های AI بتوانند به محتوای کامل آن بخش دسترسی داشته باشند و حول آن به طور دقیق مناظره کنند. کاربر تأکید کرده که UI فرانت‌اند و نوتیفیکیشن خارج از این مرحله است. کلیدواژه‌های اصلی: DebateAttachment, endpoint, API, مدل داده. در کد فعلی، endpoint مناظره در backend/app/api/routes/debate.py (که deep-read نشده) و مدل debate در backend/app/models/debate.py وجود دارد. همچنین frontend/src/app/debate/page.tsx صفحه اصلی مناظره است. برای پیاده‌سازی، نیاز به یک مدل جدید DebateAttachment در backend/app/models/ (مثلاً debate_attachment.py) و یک router جدید یا افزودن endpoint به debate.py داریم.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل داده DebateAttachment در فایل جدید backend/app/models/debate_attachment.py با فیلدهای: id (UUID), debate_id (FK به Debate), entity_type (enum: 'page', 'subpage', 'project', 'project_section'), entity_id (string), entity_path (string, مسیر نسبی), content_snapshot (text, snapshot از محتوا در زمان اتصال), created_at, updated_at. 2. ایجاد router جدید backend/app/api/routes/debate_attachment.py با prefix='/debate-attachments' و endpointهای: POST /attach (ایجاد اتصال), GET /list?debate_id=... (لیست اتصالات یک مناظره), GET /{attachment_id} (جزئیات), DELETE /{attachment_id} (حذف). 3. ثبت router در backend/app/main.py. 4. ایجاد سرویس debate_attachment_service.py در backend/app/services/ برای منطق business (validation, snapshot گرفتن از entity). 5. اضافه کردن مدل به __init__.py مدل‌ها و سرویس به __init__.py سرویس‌ها.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: اضافه کردن UI انتخاب و اتصال محتوا در صفحه مناظره

**Scope:** تغییر فرانت‌اند صفحه مناظره برای نمایش یک دکمه/پنل «اتصال محتوا» که کاربر بتواند از طریق آن یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه را انتخاب و متصل کند. خارج از این مرحله: نوتیفیکیشن و تلگرام.
**Key terms:** UI, فرانت‌اند, صفحه مناظره, اتصال محتوا

**بخش مربوط از متن کاربر:**
```
قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن UI انتخاب و اتصال محتوا در صفحه مناظره

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/debate/page.tsx:1-50` — `DebatePage` — این فایل deep-read شده است. خطوط دقیق مشخص نیستند، اما اینجا نقطه اصلی اضافه کردن UI است.
  ```tsx
  // frontend/src/app/debate/page.tsx (فایل deep-read شده)
  // این صفحه اصلی مناظره است که باید دکمه/پنل اتصال محتوا به آن اضافه شود.
  // ساختار فعلی: یک صفحه Next.js که از useDebate hook و api service استفاده می‌کند.
  // نیاز به اضافه کردن یک state برای محتوای انتخاب شده و یک UI برای انتخاب دارد.
  ```
- `frontend/src/hooks/use`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن یک UI جدید در صفحه مناظره (frontend/src/app/debate/page.tsx) برای اتصال محتوا داده است. این قابلیت باید به کاربر اجازه دهد که از طریق یک دکمه یا پنل «اتصال محتوا»، یک صفحه، زیرصفحه، پروژه لود شده یا بخشی از پروژه را انتخاب و به مناظره متصل کند تا مشخص شود مناظره حول چه موضوعی است. کاربر تأکید کرده که این قابلیت باید در خود صفحه مناظره (فرانت‌اند) پیاده‌سازی شود و خارج از این مرحله، نوتیفیکیشن و تلگرام است. کلیدواژه‌های اصلی: UI, فرانت‌اند, صفحه مناظره, اتصال محتوا. در کد فعلی، صفحه مناظره در frontend/src/app/debate/page.tsx قرار دارد و از هوک useDebate (frontend/src/hooks/useDebate.ts) و سرویس api (frontend/src/services/api.ts) استفاده می‌کند. backend/app/api/routes/debate.py مسیرهای API مربوط به مناظره را مدیریت می‌کند. backend/app/services/debate_service.py سرویس اصلی مناظره است. backend/app/models/debate.py مدل دیتابیس مناظره را تعریف می‌کند. backend/app/services/project_service.py و backend/app/services/smart_orchestrator.py می‌توانند برای دریافت اطلاعات پروژه‌ها و صفحات استفاده شوند. backend/app/api/routes/projects.py و backend/app/api/routes/simple_projects.py مسیرهای API برای پروژه‌ها را ارائه می‌دهند. backend/app/api/routes/project_structure.py می‌تواند ساختار پروژه را برگرداند. frontend/src/types/index.ts تایپ‌های TypeScript را تعریف می‌کند. frontend/src/lib/api.ts توابع API پایه را دارد.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در frontend/src/app/debate/page.tsx، یک دکمه/پنل «اتصال محتوا» اضافه کن. 2. یک کامپوننت جدید مثلاً ContentSelector در frontend/src/components/ContentSelector.tsx بساز که یک dropdown/modal برای انتخاب نوع محتوا (صفحه، زیرصفحه، پروژه، بخشی از پروژه) داشته باشد. 3. از backend/app/api/routes/projects.py (GET /projects) برای لیست پروژه‌ها و backend/app/api/routes/project_structure.py (GET /projects/{id}/structure) برای دریافت ساختار پروژه استفاده کن. 4. state انتخاب شده را در useDebate hook (frontend/src/hooks/useDebate.ts) ذخیره کن. 5. هنگام ارسال درخواست مناظره به backend/app/api/routes/debate.py (POST /debate/start)، فیلد context_id و context_type را به body اضافه کن. 6. در backend/app/services/debate_service.py، تابع start_debate را به‌روز کن تا context را از دیتابیس (backend/app/models/project.py, backend/app/models/project.py ProjectFile) بارگذاری کند و به پرامپت AI اضافه کند. 7. backend/app/models/debate.py مدل Debate را با فیلدهای context_id, context_type, context_content به‌روز کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: ارسال محتوای کامل متصل‌شده به مدل‌های زبانی در حین مناظره

**Scope:** هنگام ارسال درخواست به مدل‌های زبانی (LLM)، محتوای کامل صفحه/پروژه متصل‌شده را به عنوان context به prompt اضافه کنید. خارج از این مرحله: نوتیفیکیشن و تلگرام.
**Key terms:** LLM, context, prompt, مدل زبانی

**بخش مربوط از متن کاربر:**
```
مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
```

## 🎯 هدف (خلاصه ساختاریافته)
ارسال محتوای کامل پروژه متصل‌شده به عنوان context به LLM در حین مناظره

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/debate_service.py:نامشخص (فایل deep-read نشده)` — `build_debate_prompt (فرضی)` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً تابع سازنده prompt مناظره در اینجاست.
- `backend/app/api/routes/debate.py:نامشخص (فایل deep-read نشده)` — `start_debate (فرضی)` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. endpoint شروع مناظره احتمالاً در اینجاست.
- `backend/app/services/oversight_strong_prompt.py:149-333` — `build_strong_prompt`
  ```python
  def build_strong_prompt(
      *,
      title: str,
      user_goal: str = "",
      raw_user_request: str = "",
      description: str = "",
      proposed_action: str = "",
      context_snippet: str = "",
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که هنگام ارسال درخواست به مدل‌های زبانی (LLM) در بخش مناظره، محتوای کامل صفحه/پروژه متصل‌شده به عنوان context به prompt اضافه شود تا مدل‌ها بتوانند دسترسی کامل داشته باشند و آن قسمت را به طور کامل ببینند و حول آن دقیق مناظره کنند. کلیدواژه‌های اصلی: LLM, context, prompt, مدل زبانی. خارج از این مرحله: نوتیفیکیشن و تلگرام.

شواهد در کد: در فایل `backend/app/services/debate_service.py` (که در deep context موجود نیست ولی در ساختار پروژه وجود دارد) احتمالاً منطق اصلی ارسال prompt به LLM‌ها پیاده‌سازی شده است. همچنین در `backend/app/api/routes/debate.py` endpointهای مربوط به مناظره تعریف شده‌اند. در `backend/app/services/ai_manager.py` مدیریت مرکزی AI و ارسال درخواست به مدل‌ها انجام می‌شود. فایل `backend/app/services/oversight_strong_prompt.py` (خطوط 149-333) تابع `build_strong_prompt` را دارد که ساختار استاندارد پرامپت اجرایی را می‌سازد و شامل پارامتر `raw_user_request` برای حفظ متن خام کاربر است. همچنین `backend/app/services/prompt_helper.py` برای مدیریت پرامپت‌های سیستمی استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `backend/app/services/debate_service.py`، تابعی که prompt نهایی را برای ارسال به LLM می‌سازد (مثلاً `build_debate_prompt`) را پیدا کن.
2. یک پارامتر جدید به نام `attached_content` یا `project_context` به این تابع اضافه کن.
3. در endpoint مربوط به شروع مناظره در `backend/app/api/routes/debate.py`، هنگام دریافت درخواست، محتوای کامل پروژه/صفحه متصل‌شده را از دیتابیس یا فایل بخوان.
4. این محتوا را به عنوان context به تابع سازنده prompt پاس بده.
5. در تابع `build_debate_prompt`، محتوای دریافتی را به صورت یک بخش مجزا (مثلاً با عنوان "## 📄 محتوای پروژه متصل‌شده") به prompt اصلی اضافه کن.
6. اطمینان حاصل کن که این context قبل از ارسال به همه مدل‌های شرکت‌کننده در مناظره اضافه می‌شود.
7. برای نوتیفیکیشن و تلگرام (که خارج از این مرحله است) تغییری ایجاد نکن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی نوتیفیکیشن داخلی برای رویداد اتصال محتوا

**Scope:** اضافه کردن یک نوع نوتیفیکیشن جدید (مثلاً debate_attached) در سیستم نوتیفیکیشن موجود، شامل ذخیره و نمایش در فرانت‌اند. خارج از این مرحله: تلگرام.
**Key terms:** نوتیفیکیشن, debate_attached, ریدف

**بخش مربوط از متن کاربر:**
```
این قسمت هم باید در نوتیفیکیشن ریدف خاص خودش رو داشته باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن نوع نوتیفیکیشن debate_attached به سیستم نوتیفیکیشن داخلی (ذخیره و نمایش در فرانت‌اند)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/notification_service.py:1-50 (تخمینی)` — `کلاس NotificationService`
  ```python
  فایل deep
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی نوتیفیکیشن داخلی برای رویداد اتصال محتوا را داده است. به‌طور مشخص، یک نوع نوتیفیکیشن جدید با نام `debate_attached` باید به سیستم نوتیفیکیشن موجود اضافه شود. این شامل دو بخش اصلی است: (1) ذخیره‌سازی این نوع نوتیفیکیشن در بک‌اند و (2) نمایش آن در فرانت‌اند. کاربر تأکید کرده که ارسال نوتیفیکیشن از طریق تلگرام خارج از این مرحله است. کلیدواژه‌های اصلی: `debate_attached`, `نوتیفیکیشن`, `ریدف`. در کد فعلی، سیستم نوتیفیکیشن در فایل `backend/app/services/notification_service.py` پیاده‌سازی شده است. این سرویس احتمالاً متدهایی برای ایجاد و ذخیره نوتیفیکیشن‌ها دارد. همچنین، در فایل `backend/app/api/routes/notifications.py`، API endpoints مربوط به نوتیفیکیشن‌ها تعریف شده‌اند. در فرانت‌اند، کامپوننت `frontend/src/components/NotificationSettingsPanel.tsx` برای مدیریت تنظیمات نوتیفیکیشن و احتمالاً نمایش آن‌ها وجود دارد. برای افزودن نوع جدید `debate_attached`، باید مراحل زیر طی شود: (1) تعریف ثابت یا enum برای نوع نوتیفیکیشن جدید در بک‌اند، (2) افزودن منطق ایجاد نوتیفیکیشن از نوع `debate_attached` در سرویس مربوطه (احتمالاً `notification_service.py` یا سرویس مناظره `debate_service.py`)، (3) اطمینان از ذخیره‌سازی صحیح در دیتابیس، (4) به‌روزرسانی API برای بازگرداندن این نوع نوتیفیکیشن، (5) نمایش نوتیفیکیشن جدید در فرانت‌اند با آیکون و متن مناسب.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `backend/app/services/notification_service.py`، یک ثابت یا enum جدید برای نوع نوتیفیکیشن `debate_attached` تعریف کن (مثلاً `NOTIFICATION_TYPE_DEBATE_ATTACHED = 'debate_attached'`).
2. در همان فایل، متدی مانند `create_debate_attached_notification(debate_id: str, user_id: str, attached_content_id: str, attached_content_type: str)` ایجاد کن که یک رکورد نوتیفیکیشن با نوع `debate_attached` و داده‌های مرتبط (مانند `debate_id`, `attached_content_id`, `attached_content_type`) در دیتابیس ذخیره کند.
3. در فایل `backend/app/services/debate_service.py`، بعد از عملیات اتصال محتوا به یک مناظره، متد ایجاد نوتیفیکیشن را فراخوانی کن.
4. در فایل `backend/app/api/routes/notifications.py`، اطمینان حاصل کن که endpoint دریافت نوتیفیکیشن‌ها (مثلاً `GET /notifications`) نوع `debate_attached` را نیز بازمی‌گرداند.
5. در فرانت‌اند، فایل `frontend/src/components/NotificationSettingsPanel.tsx` را به‌روزرسانی کن تا نوتیفیکیشن‌های نوع `debate_attached` را با آیکون و متن مناسب (مثلاً "محتوای جدید به مناظره متصل شد") نمایش دهد.
6. در صورت وجود، فایل `frontend/src/services/api.ts` را بررسی کن تا از صحت type definition برای نوتیفیکیشن جدید اطمینان حاصل شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: یکپارچه‌سازی با تلگرام برای مشاهده و تعامل با مناظره متصل‌شده

**Scope:** در ربات تلگرام، قابلیت نمایش محتوای متصل‌شده (صفحه/پروژه) و امکان شروع/ادامه مناظره از طریق تلگرام را اضافه کنید. خارج از این مرحله: نوتیفیکیشن داخلی.
**Key terms:** تلگرام, ربات, مشاهده, صدا زدن

**بخش مربوط از متن کاربر:**
```
و همینطور از طریق تلگرام هم بشه محتواش رو مشاهده کرد و صداش زد
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن قابلیت مشاهده و شروع/ادامه مناظره از طریق ربات تلگرام

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py:1-50` — `router`
  ```python
  router = APIRouter(prefix="/oversight", tags=["Oversight
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی با تلگرام برای مشاهده و تعامل با مناظره متصل‌شده را دارد. به‌طور خاص، در ربات تلگرام، قابلیت نمایش محتوای متصل‌شده (صفحه/پروژه) و امکان شروع/ادامه مناظره از طریق تلگرام باید اضافه شود. خارج از این مرحله: نوتیفیکیشن داخلی. بخش مربوط از درخواست اصلی کاربر: «و همینطور از طریق تلگرام هم بشه محتواش رو مشاهده کرد و صداش زد». کلیدواژه‌ها: تلگرام, ربات, مشاهده, صدا زدن. در کد فعلی، فایل `backend/app/services/notification_service.py` (که deep-read نشده) احتمالاً مسئول ارسال نوتیفیکیشن‌هاست و می‌تواند نقطه شروع برای افزودن قابلیت تلگرام باشد. همچنین فایل `backend/app/api/routes/debate.py` (که deep-read نشده) احتمالاً endpointهای مربوط به مناظره را دارد. فایل `backend/app/services/debate_service.py` (که deep-read نشده) سرویس اصلی مناظره است. برای تلگرام، نیاز به یک سرویس جدید یا توسعه سرویس موجود است. در `backend/app/services/notification_service.py` احتمالاً متدهایی برای ارسال پیام به کانال‌های مختلف وجود دارد. همچنین فایل `backend/app/core/config.py` (که deep-read نشده) ممکن است تنظیمات مربوط به توکن ربات تلگرام را داشته باشد. در `backend/app/services/ai_manager.py` (که deep-read نشده) مدیریت مدل‌های AI انجام می‌شود که برای شروع مناظره لازم است. فایل `backend/app/services/oversight_service.py` (که deep-read نشده) ممکن است برای مدیریت پروژه‌های watched استفاده شود. فایل `backend/app/api/routes/oversight.py` (که deep-read شده) شامل endpointهای مربوط به oversight است که می‌تواند برای دریافت اطلاعات پروژه استفاده شود. فایل `backend/app/api/routes/projects.py` (که deep-read نشده) شامل endpointهای مدیریت پروژه است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/telegram_bot_service.py` که مسئول ارتباط با API تلگرام (با استفاده از کتابخانه `python-telegram-bot` یا `httpx` مستقیم) باشد. 2. افزودن متدهای `send_debate_content` و `start_debate` و `continue_debate` به این سرویس. 3. در `backend/app/services/notification_service.py` (که deep-read نشده) یک متد جدید `send_telegram_notification` اضافه شود که از سرویس تلگرام استفاده کند. 4. در `backend/app/api/routes/debate.py` (که deep-read نشده) یک endpoint جدید مانند `POST /debate/telegram/start` و `POST /debate/telegram/continue` اضافه شود که توسط ربات تلگرام فراخوانی شود. 5. در `backend/app/core/config.py` (که deep-read نشده) متغیرهای محیطی `TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID` اضافه شود. 6. در `backend/app/services/debate_service.py` (که deep-read نشده) متدهای `get_debate_content_for_telegram` و `initiate_debate_via_telegram` اضافه شود. 7. در `backend/app/services/ai_manager.py` (که deep-read نشده) اطمینان حاصل شود که مدل‌های AI برای شروع مناظره از طریق تلگرام در دسترس هستند. 8. در `backend/app/api/routes/oversight.py` (که deep-read شده) از endpointهای موجود برای دریافت اطلاعات پروژه استفاده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: backend_test)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: backend_test)_

## Task Steps

### Step 1: تعریف مدل داده و API برای اتصال صفحه/پروژه به مناظره
**Status:** `not_done` (0%)
**Scope:** ایجاد مدل‌های داده (مثلاً DebateAttachment) و endpoint‌های CRUD برای ذخیره و بازیابی اتصال یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه به یک مناظره. خارج از این مرحله: UI فرانت‌اند و نوتیفیکیشن.
**Excerpt:**
```
قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد و مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
```

### Step 2: اضافه کردن UI انتخاب و اتصال محتوا در صفحه مناظره
**Status:** `not_done` (0%)
**Scope:** تغییر فرانت‌اند صفحه مناظره برای نمایش یک دکمه/پنل «اتصال محتوا» که کاربر بتواند از طریق آن یک صفحه، زیرصفحه، پروژه یا بخشی از پروژه را انتخاب و متصل کند. خارج از این مرحله: نوتیفیکیشن و تلگرام.
**Excerpt:**
```
قابلیت اتصال صفحه مناظره، از طریق همان فرانت در خود صفحه مناظره، برای اینکه مشخص کنیم منظاره حول چه موضوعی باشه یعنی با اتصال و انتخاب صفحه یا یک زیر صفحه یا یک پروژه لود شده و یا صفحه و بخشی از پروژه لود شده، بتوان حول اون قسمت و محتواش و سایر چیزهاش سوال کرد
```

### Step 3: ارسال محتوای کامل متصل‌شده به مدل‌های زبانی در حین مناظره
**Status:** `not_done` (0%)
**Scope:** هنگام ارسال درخواست به مدل‌های زبانی (LLM)، محتوای کامل صفحه/پروژه متصل‌شده را به عنوان context به prompt اضافه کنید. خارج از این مرحله: نوتیفیکیشن و تلگرام.
**Excerpt:**
```
مدل ها وقتی اون قسمت رو متصل میکنیم بتونن دسترسی کامل داشته باشن و اون قسمت رو به طور کاملل ببینن و بتونن حولش دقیق مناظره کنن.
```

### Step 4: پیاده‌سازی نوتیفیکیشن داخلی برای رویداد اتصال محتوا
**Status:** `not_done` (0%)
**Scope:** اضافه کردن یک نوع نوتیفیکیشن جدید (مثلاً debate_attached) در سیستم نوتیفیکیشن موجود، شامل ذخیره و نمایش در فرانت‌اند. خارج از این مرحله: تلگرام.
**Excerpt:**
```
این قسمت هم باید در نوتیفیکیشن ریدف خاص خودش رو داشته باشه
```

### Step 5: یکپارچه‌سازی با تلگرام برای مشاهده و تعامل با مناظره متصل‌شده
**Status:** `not_done` (0%)
**Scope:** در ربات تلگرام، قابلیت نمایش محتوای متصل‌شده (صفحه/پروژه) و امکان شروع/ادامه مناظره از طریق تلگرام را اضافه کنید. خارج از این مرحله: نوتیفیکیشن داخلی.
**Excerpt:**
```
و همینطور از طریق تلگرام هم بشه محتواش رو مشاهده کرد و صداش زد
```
