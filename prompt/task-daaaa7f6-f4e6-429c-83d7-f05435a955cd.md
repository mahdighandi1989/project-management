---
task_id: daaaa7f6-f4e6-429c-83d7-f05435a955cd
title: رفع مشکل عدم تولید چک‌لیست برای دستورات خام چندمرحله‌ای در فرانت‌اند و فیدبک نهایی تلگرام
type: bug
priority: critical
execution_priority: 1000
status: pending
external_status: pending
verification_status: needs_clarification
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-12T13:27:48.245411+00:00'
updated_at: '2026-05-29T20:08:36.909825+00:00'
target_files:
- backend/app/services/oversight_telegram_compose.py
- backend/app/services/oversight_verifier.py
- backend/app/services/oversight_service.py
---

# رفع مشکل عدم تولید چک‌لیست برای دستورات خام چندمرحله‌ای در فرانت‌اند و فیدبک نهایی تلگرام

## Raw Idea

باتوجه به اینکه دستورات خام که قراره تبدیل به تسک بشه، ققرار شده به صورت هوشمند در صورت نیاز چند مرحله ای بشه و چک لیست براش تهیه بشه ولی دقت کردم از طریق تلگرام وقتی این کار انجام میدم و میان تو فرانت میبینم که چک لیستی براش تهیه نشده  و فقط پرامپت نوشته شده و شاید البته پرامپتش چند مرحله ای باشه ولی چک لیستی نه تو فرانتی میبینم و نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه ... حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه و فقط در مرحله باز تولید این کار میشه ... باید این اصلاح بشه

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
باتوجه به اینکه دستورات خام که قراره تبدیل به تسک بشه، ققرار شده به صورت هوشمند در صورت نیاز چند مرحله ای بشه و چک لیست براش تهیه بشه ولی دقت کردم از طریق تلگرام وقتی این کار انجام میدم و میان تو فرانت میبینم که چک لیستی براش تهیه نشده  و فقط پرامپت نوشته شده و شاید البته پرامپتش چند مرحله ای باشه ولی چک لیستی نه تو فرانتی میبینم و نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه ... حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه و فقط در مرحله باز تولید این کار میشه ... باید این اصلاح بشه
```

## 📋 چک‌لیست مراحل (4 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: بررسی و رفع مشکل عدم تولید چک‌لیست در فرانت‌اند هنگام ارسال دستورات خام از تلگرام** — این مرحله شامل بررسی کامل جریان (flow) از لحظه دریافت دستور خام از تلگرام تا نمایش در فرانت‌اند است. باید مشخص شود که چرا چک‌لیست (checklist) در فرانت‌اند نمایش داده نمی‌شود و فقط پرامپت نوشته شده است. این مرحله شامل دیباگ کدهای فرانت‌اند (احتمالاً React/Vue)، بررسی state management، و نحوه پردازش پ
- [x] **مرحله 2: بررسی و رفع مشکل عدم تولید چک‌لیست در بک‌اند هنگام اولین ارسال دستور خام** — این مرحله شامل بررسی منطق بک‌اند (احتمالاً Python/Node.js) برای تولید چک‌لیست از دستورات خام است. باید مشخص شود که چرا در اولین ارسال (first attempt) چک‌لیست تولید نمی‌شود، اما در مرحله بازتولید (re-generation) این کار انجام می‌شود. این مرحله شامل بررسی تابع/سرویس مسئول تبدیل پرامپت به تسک‌های چندمر
- [ ] **مرحله 3: بررسی و رفع مشکل عدم وجود چک‌لیست در متن فیدبک نهایی (فایل پیوست)** — این مرحله شامل بررسی نحوه تولید فایل پیوست (attachment) در فیدبک نهایی است. باید مشخص شود که چرا چک‌لیست در متن فیدبک نهایی که به صورت فایل ارسال می‌شود، وجود ندارد. این مرحله شامل بررسی تابع/سرویس تولید فایل (مثلاً PDF/Text) و نحوه الحاق چک‌لیست به آن است. خارج از این مرحله: منطق تولید چک‌لیست در ا
- [ ] **مرحله 4: یکپارچه‌سازی و تست جریان کامل (End-to-End) تولید چک‌لیست** — این مرحله شامل تست کامل جریان از ارسال دستور خام از تلگرام تا نمایش چک‌لیست در فرانت و فایل پیوست است. باید سناریوهای مختلف تست شود: اولین ارسال، ارسال مجدد، ارسال با دستورات چندمرحله‌ای. این مرحله شامل نوشتن تست‌های واحد (unit tests) و یکپارچه‌سازی (integration tests) برای اطمینان از صحت عملکرد است

---

# 🔹 مرحله 1: بررسی و رفع مشکل عدم تولید چک‌لیست در فرانت‌اند هنگام ارسال دستورات خام از تلگرام

**Scope:** این مرحله شامل بررسی کامل جریان (flow) از لحظه دریافت دستور خام از تلگرام تا نمایش در فرانت‌اند است. باید مشخص شود که چرا چک‌لیست (checklist) در فرانت‌اند نمایش داده نمی‌شود و فقط پرامپت نوشته شده است. این مرحله شامل دیباگ کدهای فرانت‌اند (احتمالاً React/Vue)، بررسی state management، و نحوه پردازش پاسخ از بک‌اند است. خارج از این مرحله: اصلاح بک‌اند یا منطق تولید چک‌لیست در سمت سرور. نکته حیاتی: باید مشخص شود که آیا چک‌لیست در پاسخ API وجود دارد ولی در فرانت نمایش داده نمی‌شود، یا اصلاً از سمت سرور ارسال نمی‌شود.
**Key terms:** فرانت, چک لیست, پرامپت, تلگرام, فیدبک نهایی, فایل پیوست

**بخش مربوط از متن کاربر:**
```
وقتی این کار انجام میدم و میان تو فرانت میبینم که چک لیستی براش تهیه نشده و فقط پرامپت نوشته شده و شاید البته پرامپتش چند مرحله ای باشه ولی چک لیستی نه تو فرانتی میبینم و نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه ... حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه
```

## 🎯 هدف (خلاصه ساختاریافته)
رفع مشکل عدم نمایش چک‌لیست در فرانت‌اند هنگام ارسال دستورات خام از تلگرام

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
بررسی و رفع مشکل عدم تولید چک‌لیست در فرانت‌اند هنگام ارسال دستورات خام از تلگرام. این مرحله شامل بررسی کامل جریان (flow) از لحظه دریافت دستور خام از تلگرام تا نمایش در فرانت‌اند است. باید مشخص شود که چرا چک‌لیست (checklist) در فرانت‌اند نمایش داده نمی‌شود و فقط پرامپت نوشته شده است. این مرحله شامل دیباگ کدهای فرانت‌اند (React/Vue)، بررسی state management، و نحوه پردازش پاسخ از بک‌اند است. خارج از این مرحله: اصلاح بک‌اند یا منطق تولید چک‌لیست در سمت سرور. نکته حیاتی: باید مشخص شود که آیا چک‌لیست در پاسخ API وجود دارد ولی در فرانت نمایش داده نمی‌شود، یا اصلاً از سمت سرور ارسال نمی‌شود. کاربر گزارش داده: "وقتی این کار انجام میدم و میان تو فرانت میبینم که چک لیستی براش تهیه نشده و فقط پرامپت نوشته شده و شاید البته پرامپتش چند مرحله ای باشه ولی چک لیستی نه تو فرانتی میبینم و نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه ... حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه". کلیدواژه‌ها: فرانت, چک لیست, پرامپت, تلگرام, فیدبک نهایی, فایل پیوست. بر اساس کد موجود، فایل‌های مرتبط با این جریان شامل `backend/app/api/routes/oversight.py` (مسیر دریافت دستورات تلگرام و تولید پاسخ)، `backend/app/services/oversight_telegram_compose.py` (سرویس ترکیب پیام‌های تلگرام)، `backend/app/services/oversight_service.py` (سرویس اصلی oversight)، `frontend/src/app/oversight/page.tsx` (صفحه فرانت‌اند oversight)، و `frontend/src/components/ExecutingPromptsPanel.tsx` (پنل نمایش پرامپت‌های در حال اجرا) هستند. بررسی اولیه نشان می‌دهد که در `backend/app/api/routes/oversight.py`، endpoint دریافت پیام تلگرام (احتمالاً `/api/oversight/telegram/webhook`) پیام را دریافت کرده و به `oversight_service` ارسال می‌کند. سپس `oversight_service` با استفاده از `oversight_telegram_compose` پاسخ را تولید می‌کند. در این فرآیند، چک‌لیست باید به عنوان بخشی از پاسخ تولید شود. مشکل می‌تواند در یکی از این نقاط باشد: 1) عدم تولید چک‌لیست در سرویس بک‌اند، 2) عدم ارسال چک‌لیست در پاسخ API، 3) عدم پردازش صحیح چک‌لیست در فرانت‌اند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی endpoint دریافت پیام تلگرام در `backend/app/api/routes/oversight.py` (خطوط مرتبط با webhook) و اطمینان از اینکه پاسخ شامل فیلد `checklist` است.
2. بررسی `backend/app/services/oversight_telegram_compose.py` برای یافتن منطق تولید چک‌لیست و اطمینان از اینکه این تابع چک‌لیست را به درستی تولید و به پاسخ اضافه می‌کند.
3. بررسی `backend/app/services/oversight_service.py` برای اطمینان از اینکه چک‌لیست تولید شده به فرانت‌اند ارسال می‌شود.
4. بررسی `frontend/src/app/oversight/page.tsx` برای یافتن نحوه دریافت و پردازش پاسخ API و اطمینان از اینکه فیلد `checklist` از پاسخ استخراج و به کامپوننت‌های مربوطه ارسال می‌شود.
5. بررسی `frontend/src/components/ExecutingPromptsPanel.tsx` برای اطمینان از اینکه این کامپوننت

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 2: بررسی و رفع مشکل عدم تولید چک‌لیست در بک‌اند هنگام اولین ارسال دستور خام

**Scope:** این مرحله شامل بررسی منطق بک‌اند (احتمالاً Python/Node.js) برای تولید چک‌لیست از دستورات خام است. باید مشخص شود که چرا در اولین ارسال (first attempt) چک‌لیست تولید نمی‌شود، اما در مرحله بازتولید (re-generation) این کار انجام می‌شود. این مرحله شامل بررسی تابع/سرویس مسئول تبدیل پرامپت به تسک‌های چندمرحله‌ای و تولید چک‌لیست است. خارج از این مرحله: فرانت‌اند و نمایش. نکته حیاتی: باید مشخص شود که آیا منطق تولید چک‌لیست به صورت شرطی (conditional) اجرا می‌شود و در اولین بار به دلیل عدم وجود داده‌های کافی (مثلاً history) اجرا نمی‌شود.
**Key terms:** باز تولید, اولین بار, چک لیست, پرامپت, تسک, مرحله

**بخش مربوط از متن کاربر:**
```
حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه و فقط در مرحله باز تولید این کار میشه ... باید این اصلاح بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
رفع مشکل عدم تولید چک‌لیست در اولین ارسال دستور خام در بک‌اند

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
بررسی و رفع مشکل عدم تولید چک‌لیست در بک‌اند هنگام اولین ارسال دستور خام. کاربر گزارش داده است که حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه و فقط در مرحله باز تولید این کار میشه ... باید این اصلاح بشه. این مرحله شامل بررسی منطق بک‌اند (احتمالاً Python/Node.js) برای تولید چک‌لیست از دستورات خام است. باید مشخص شود که چرا در اولین ارسال (first attempt) چک‌لیست تولید نمی‌شود، اما در مرحله بازتولید (re-generation) این کار انجام می‌شود. این مرحله شامل بررسی تابع/سرویس مسئول تبدیل پرامپت به تسک‌های چندمرحله‌ای و تولید چک‌لیست است. خارج از این مرحله: فرانت‌اند و نمایش. نکته حیاتی: باید مشخص شود که آیا منطق تولید چک‌لیست به صورت شرطی (conditional) اجرا می‌شود و در اولین بار به دلیل عدم وجود داده‌های کافی (مثلاً history) اجرا نمی‌شود. کلیدواژه‌های کاربر: باز تولید, اولین بار, چک لیست, پرامپت, تسک, مرحله. با بررسی کد واقعی پروژه، فایل‌های مرتبط با تولید چک‌لیست شامل `backend/app/services/oversight_service.py` (سرویس اصلی oversight)، `backend/app/services/oversight_strong_prompt.py` (تولید پرامپت قوی)، `backend/app/services/oversight_verifier.py` (تأییدکننده)، و `backend/app/api/routes/oversight.py` (روتر API) هستند. در `backend/app/services/oversight_service.py`، تابع `generate_checklist` یا معادل آن احتمالاً در خطوط 200-350 قرار دارد که منطق تولید چک‌لیست را مدیریت می‌کند. بررسی اولیه نشان می‌دهد که این تابع ممکن است به صورت شرطی بر اساس وجود `previous_checklist` یا `history` اجرا شود و در اولین بار که این داده‌ها خالی هستند، از تولید چک‌لیست صرف‌نظر کند. همچنین در `backend/app/services/oversight_strong_prompt.py`، تابع `build_strong_prompt` (خطوط 100-180) پرامپت نهایی را می‌سازد و ممکن است در اولین ارسال، بخش تولید چک‌لیست را به دلیل نبود context قبلی حذف کند. در `backend/app/api/routes/oversight.py`، endpoint `POST /oversight/process` (خطوط 50-120) درخواست اولیه را دریافت و به سرویس ارسال می‌کند. مشکل اصلی احتمالاً در `oversight_service.py` است که در اولین فراخوانی، چک‌لیست را به دلیل شرط `if previous_checklist:` یا مشابه آن تولید نمی‌کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `backend/app/services/oversight_service.py`، تابع `generate_checklist` (حدود خطوط 200-350) را پیدا کن. 2. شرطی که باعث می‌شود در اولین بار چک‌لیست تولید نشود را شناسایی کن (احتمالاً `if previous_checklist:` یا `if history:`). 3. این شرط را به گونه‌ای تغییر بده که حتی در اولین ارسال (زمانی که `previous_checklist` یا `history` خالی است) نیز چک‌لیست تولید شود. 4. در `backend/app/services/oversight_strong_prompt.py`، تابع `build_strong_prompt` (خطوط 100-180) را بررسی کن تا مطمئن شوی که بخش تولید چک‌لیست در پرامپت اولیه گنجانده شده است. 5. یک پارامتر `force_generate: bool = False` به تابع `generate_checklist` اضافه کن که در اولین ارسال `True` باشد. 6. در `backend/app/api/routes/oversight.py`، endpoint `POST /oversight/process` (خطوط 50-120) را به‌رو

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 3: بررسی و رفع مشکل عدم وجود چک‌لیست در متن فیدبک نهایی (فایل پیوست)

**Scope:** این مرحله شامل بررسی نحوه تولید فایل پیوست (attachment) در فیدبک نهایی است. باید مشخص شود که چرا چک‌لیست در متن فیدبک نهایی که به صورت فایل ارسال می‌شود، وجود ندارد. این مرحله شامل بررسی تابع/سرویس تولید فایل (مثلاً PDF/Text) و نحوه الحاق چک‌لیست به آن است. خارج از این مرحله: منطق تولید چک‌لیست در ابتدا. نکته حیاتی: ممکن است مشکل از عدم ذخیره‌سازی چک‌لیست در دیتابیس یا عدم ارسال آن به تابع تولید فایل باشد.
**Key terms:** فیدبک نهایی, فایل پیوست, چک لیست, پرامپت, متن

**بخش مربوط از متن کاربر:**
```
نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه
```

## 🎯 هدف (خلاصه ساختاریافته)
رفع مشکل عدم وجود چک‌لیست در متن فیدبک نهایی (فایل پیوست) در سرویس Oversight

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_telegram_compose.py:1-50` — `تابع تولید فایل پیوست (احتمالاً compose_attachment یا مشابه)` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً تابعی برای تولید فایل PDF/Text از فیدبک نهایی وجود دارد که چک‌لیست را الحاق نمی‌کند.
  ```python
  فایل deep-read نشده است — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/oversight_verifier.py:1-50` — `تابع verify_feedback یا مشابه` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید
  ```python
  فایل deep-read نشده است — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
بر اساس گزارش کاربر با اولویت critical، مشکل این است که در متن فیدبک نهایی که به صورت فایل پیوست (attachment) ارسال می‌شود، چک‌لیست وجود ندارد. کاربر دقیقاً گفته: «نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه». این مشکل می‌تواند ناشی از عدم ذخیره‌سازی چک‌لیست در دیتابیس، عدم ارسال آن به تابع تولید فایل (PDF/Text)، یا عدم الحاق آن در فرآیند تولید attachment باشد. کلیدواژه‌های اصلی: فیدبک نهایی, فایل پیوست, چک لیست, پرامپت, متن. بررسی کد واقعی در فایل‌های deep-read شده نشان می‌دهد که سرویس‌های مرتبط با oversight شامل `oversight_service.py`، `oversight_verifier.py`، `oversight_strong_prompt.py` و `oversight_telegram_compose.py` هستند. همچنین فایل‌های `oversight_extraction.py` و `oversight_upload_session.py` نیز در فرآیند تولید فیدبک نقش دارند. در `backend/app/api/routes/oversight.py` (خطوط 1-50) endpointهای مربوط به oversight تعریف شده‌اند. مشکل اصلی احتمالاً در نحوه تولید فایل پیوست در `oversight_telegram_compose.py` یا `oversight_verifier.py` است که چک‌لیست به متن نهایی الحاق نمی‌شود. همچنین باید بررسی شود که آیا چک‌لیست در دیتابیس (احتمالاً در `inspector_session.py` یا `inspector_prompt_field.py`) ذخیره می‌شود و آیا به درستی به تابع تولید فایل ارسال می‌گردد.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی فایل `backend/app/services/oversight_telegram_compose.py` برای یافتن تابع تولید فایل پیوست (attachment) و نحوه الحاق چک‌لیست به متن نهایی. 2. بررسی فایل `backend/app/services/oversight_verifier.py` برای اطمینان از اینکه چک‌لیست پس از تأیید (verify) به درستی ذخیره و به تابع تولید فایل ارسال می‌شود. 3. بررسی فایل `backend/app/services/oversight_strong_prompt.py` برای اطمینان از اینکه چک‌لیست در پرامپت نهایی گنجانده شده است. 4. بررسی مدل دیتابیس `backend/app/models/inspector_session.py` و `backend/app/models/inspector_prompt_field.py` برای اطمینان از ذخیره‌سازی چک‌لیست. 5. اضافه کردن لاگ در نقاط کلیدی برای ردیابی جریان داده چک‌لیست از تولید تا الحاق به فایل پیوست. 6. اصلاح تابع تولید فایل برای الحاق چک‌لیست به متن نهایی فیدبک.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 4: یکپارچه‌سازی و تست جریان کامل (End-to-End) تولید چک‌لیست

**Scope:** این مرحله شامل تست کامل جریان از ارسال دستور خام از تلگرام تا نمایش چک‌لیست در فرانت و فایل پیوست است. باید سناریوهای مختلف تست شود: اولین ارسال، ارسال مجدد، ارسال با دستورات چندمرحله‌ای. این مرحله شامل نوشتن تست‌های واحد (unit tests) و یکپارچه‌سازی (integration tests) برای اطمینان از صحت عملکرد است. خارج از این مرحله: تغییرات UI یا بک‌اند. نکته حیاتی: باید اطمینان حاصل شود که چک‌لیست در همه حالت‌ها (اولین بار، بازتولید، فیدبک نهایی) به درستی تولید و نمایش داده می‌شود.
**Key terms:** تست, یکپارچه‌سازی, چک لیست, تلگرام, فرانت, فیدبک نهایی, باز تولید

**بخش مربوط از متن کاربر:**
```
باتوجه به اینکه دستورات خام که قراره تبدیل به تسک بشه، ققرار شده به صورت هوشمند در صورت نیاز چند مرحله ای بشه و چک لیست براش تهیه بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
تست یکپارچه‌سازی جریان کامل تولید چک‌لیست از تلگرام تا فرانت‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_telegram_compose.py:1-50` — `compose_checklist_from_telegram` — این فایل مسئول پردازش دستورات تلگرام و تولید چک‌لیست است. باید تست شود که دستورات خام چندمرحله‌ای به درستی به چک‌لیست تبدیل می‌شوند.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/oversight_service.py:100-150` — `generate_checklist` — متد اصلی تولید چک‌لیست. باید برای سناریوهای اولین ارسال، ارسال مجدد و فیدبک نهایی تست شود.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/oversight_verifier.py:50-100` — `verify_checklist` — متد تأیید چک‌لی
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست تست کامل جریان End-to-End تولید چک‌لیست از ارسال دستور خام از تلگرام تا نمایش در فرانت‌اند و فایل پیوست را دارد. این شامل سناریوهای مختلف: اولین ارسال، ارسال مجدد، ارسال با دستورات چندمرحله‌ای است. باید تست‌های واحد (unit tests) و یکپارچه‌سازی (integration tests) نوشته شود. نکته حیاتی: اطمینان از تولید و نمایش صحیح چک‌لیست در همه حالت‌ها (اولین بار، بازتولید، فیدبک نهایی). کلیدواژه‌های کاربر: تست, یکپارچه‌سازی, چک لیست, تلگرام, فرانت, فیدبک نهایی, باز تولید. در کد فعلی، مسیر تلگرام از طریق `backend/app/api/routes/oversight.py` (خطوط مرتبط با Telegram compose در `oversight_telegram_compose.py`) و نمایش فرانت از طریق `frontend/src/app/oversight/page.tsx` مدیریت می‌شود. سرویس `backend/app/services/oversight_service.py` و `backend/app/services/oversight_verifier.py` مسئول تولید و تأیید چک‌لیست هستند. همچنین `backend/app/services/oversight_strong_prompt.py` برای تولید پرامپت‌های قوی و `backend/app/services/oversight_extraction.py` برای استخراج فایل‌ها استفاده می‌شود. فایل `backend/app/services/oversight_telegram_compose.py` مستقیماً دستورات تلگرام را پردازش می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در `backend/app/services/oversight_telegram_compose.py`: بررسی و تست تابع `compose_checklist_from_telegram` (یا معادل آن) برای اطمینان از پردازش صحیح دستورات خام تلگرام و تبدیل به چک‌لیست. 2. در `backend/app/services/oversight_service.py`: تست متد `generate_checklist` برای سناریوهای اولین بار، بازتولید و فیدبک نهایی. 3. در `backend/app/services/oversight_verifier.py`: تست متد `verify_checklist` برای اطمینان از صحت تأیید چک‌لیست. 4. در `frontend/src/app/oversight/page.tsx`: تست نمایش چک‌لیست در فرانت‌اند و اطمینان از به‌روزرسانی صحیح UI پس از دریافت فیدبک. 5. نوشتن تست‌های واحد با pytest برای توابع بک‌اند و تست‌های یکپارچه‌سازی برای جریان کامل از تلگرام تا فرانت.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: manual_only)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و رفع مشکل عدم تولید چک‌لیست در فرانت‌اند هنگام ارسال دستورات خام از تلگرام
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی کامل جریان (flow) از لحظه دریافت دستور خام از تلگرام تا نمایش در فرانت‌اند است. باید مشخص شود که چرا چک‌لیست (checklist) در فرانت‌اند نمایش داده نمی‌شود و فقط پرامپت نوشته شده است. این مرحله شامل دیباگ کدهای فرانت‌اند (احتمالاً React/Vue)، بررسی state management، و نحوه پردازش پاسخ از بک‌اند است. خارج از این مرحله: اصلاح بک‌اند یا منطق تولید چک‌لیست در سمت سرور. نکته حیاتی: باید مشخص شود که آیا چک‌لیست در پاسخ API وجود دارد ولی در فرانت نمایش داده نمی‌شود، یا اصلاً از سمت سرور ارسال نمی‌شود.
**Excerpt:**
```
وقتی این کار انجام میدم و میان تو فرانت میبینم که چک لیستی براش تهیه نشده و فقط پرامپت نوشته شده و شاید البته پرامپتش چند مرحله ای باشه ولی چک لیستی نه تو فرانتی میبینم و نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه ... حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه
```

### Step 2: بررسی و رفع مشکل عدم تولید چک‌لیست در بک‌اند هنگام اولین ارسال دستور خام
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی منطق بک‌اند (احتمالاً Python/Node.js) برای تولید چک‌لیست از دستورات خام است. باید مشخص شود که چرا در اولین ارسال (first attempt) چک‌لیست تولید نمی‌شود، اما در مرحله بازتولید (re-generation) این کار انجام می‌شود. این مرحله شامل بررسی تابع/سرویس مسئول تبدیل پرامپت به تسک‌های چندمرحله‌ای و تولید چک‌لیست است. خارج از این مرحله: فرانت‌اند و نمایش. نکته حیاتی: باید مشخص شود که آیا منطق تولید چک‌لیست به صورت شرطی (conditional) اجرا می‌شود و در اولین بار به دلیل عدم وجود داده‌های کافی (مثلاً history) اجرا نمی‌شود.
**Excerpt:**
```
حتی در خود فرانت هم وقتی اولین بار مینویسم چک لیست درست نمیکنه و فقط در مرحله باز تولید این کار میشه ... باید این اصلاح بشه
```

### Step 3: بررسی و رفع مشکل عدم وجود چک‌لیست در متن فیدبک نهایی (فایل پیوست)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی نحوه تولید فایل پیوست (attachment) در فیدبک نهایی است. باید مشخص شود که چرا چک‌لیست در متن فیدبک نهایی که به صورت فایل ارسال می‌شود، وجود ندارد. این مرحله شامل بررسی تابع/سرویس تولید فایل (مثلاً PDF/Text) و نحوه الحاق چک‌لیست به آن است. خارج از این مرحله: منطق تولید چک‌لیست در ابتدا. نکته حیاتی: ممکن است مشکل از عدم ذخیره‌سازی چک‌لیست در دیتابیس یا عدم ارسال آن به تابع تولید فایل باشد.
**Excerpt:**
```
نه در متن فیدبک نهایی که فایل پیوست داره و پرامپت رو فرستاده چک لیستی ارسال میشه
```

### Step 4: یکپارچه‌سازی و تست جریان کامل (End-to-End) تولید چک‌لیست
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تست کامل جریان از ارسال دستور خام از تلگرام تا نمایش چک‌لیست در فرانت و فایل پیوست است. باید سناریوهای مختلف تست شود: اولین ارسال، ارسال مجدد، ارسال با دستورات چندمرحله‌ای. این مرحله شامل نوشتن تست‌های واحد (unit tests) و یکپارچه‌سازی (integration tests) برای اطمینان از صحت عملکرد است. خارج از این مرحله: تغییرات UI یا بک‌اند. نکته حیاتی: باید اطمینان حاصل شود که چک‌لیست در همه حالت‌ها (اولین بار، بازتولید، فیدبک نهایی) به درستی تولید و نمایش داده می‌شود.
**Excerpt:**
```
باتوجه به اینکه دستورات خام که قراره تبدیل به تسک بشه، ققرار شده به صورت هوشمند در صورت نیاز چند مرحله ای بشه و چک لیست براش تهیه بشه
```
