---
task_id: 587a4bed-64dd-4286-9a83-10a3167c46ec
title: میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه  (5 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-12T11:32:06.523609+00:00'
updated_at: '2026-06-03T04:15:31.882817+00:00'
target_files:
- backend/app/api/routes/external_prompts.py
- frontend/src/app/oversight/page.tsx
- backend/app/api/routes/oversight.py
- backend/app/services/oversight_service.py
- backend/app/services/oversight_telegram_compose.py
- backend/app/models/project.py
- backend/app/api/routes/analysis.py
- frontend/src/app/creator/page.tsx
- backend/app/api/routes/creator.py
- backend/app/services/creator_engine.py
- backend/app/api/routes/projects.py
- backend/app/services/project_service.py
- backend/app/services/creator_idea_to_prompt.py
---

# میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه  (5 مرحله)

## Raw Idea

میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه در مرحله تولید پرامپت، باید به تمام قسمت های اون پروژه های انتخاب شده که درخواست کردم ایده یا تسک بخی از اون مثل این پروژه های انتخاب شده باشه، لذا مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه -----

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه در مرحله تولید پرامپت، باید به تمام قسمت های اون پروژه های انتخاب شده که درخواست کردم ایده یا تسک بخی از اون مثل این پروژه های انتخاب شده باشه، لذا مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه -----
```

## 📋 چک‌لیست مراحل (11 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد مدل داده برای پروژه‌های منتخب در تسک** — این مرحله شامل طراحی و پیاده‌سازی مدل داده‌ای است که امکان ذخیره‌سازی لیستی از پروژه‌های منتخب (selected_projects) را در ساختار تسک فراهم کند. این مدل باید شامل شناسه پروژه (project_id)، مسیر پروژه (project_path) و وضعیت انتخاب (is_selected) باشد. خارج از این مرحله: تغییرات UI، منطق انتخاب در فرانت‌
- [ ] **مرحله 2: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (فرانت‌اند)** — این مرحله شامل افزودن یک کامپوننت UI به فرم ایجاد/ویرایش تسک در فرانت‌اند است که لیست پروژه‌های موجود (لود شده در سیستم) را نمایش دهد و کاربر بتواند با تیک زدن، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: منطق تلگرام، منطق استخراج اطلاعات از پروژه‌ها، و ذخیره‌سازی نهایی. نکته حیاتی: لیست پر
- [ ] **مرحله 3: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (تلگرام)** — این مرحله شامل افزودن قابلیت انتخاب پروژه در ربات تلگرام هنگام ایجاد تسک است. کاربر باید بتواند از طریق دکمه‌های inline یا منوی انتخاب، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند و انتخاب‌ها به صورت لیست 
- [ ] **مرحله 4: ذخیره‌سازی پروژه‌های منتخب در دیتابیس هنگام ایجاد تسک** — این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های منتخب (selected_projects) در دیتابیس هنگام ایجاد یا ویرایش تسک است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید از مدل داده مرحله ۱ استفاده کند و اعتبارسنجی کند که پروژه‌های انتخاب شده واقعاً در سیستم وجود دارند
- [ ] **مرحله 5: ایجاد منطق استخراج اطلاعات از پروژه‌های منتخب** — این مرحله شامل پیاده‌سازی منطقی است که هنگام ارسال درخواست تسک، به پروژه‌های منتخب مراجعه کرده و تمام فایل‌ها و زیرشاخه‌های آن‌ها را اسکن کند. اطلاعات استخراج شده شامل: ساختار دایرکتوری، محتوای فایل‌ها، کلاس‌ها، توابع، و منطق کسب‌وکار. خارج از این مرحله: تولید پرامپت نهایی، UI. نکته حیاتی: باید از ت
- [ ] **مرحله 6: دسته‌بندی و ساختاردهی اطلاعات استخراج شده** — این مرحله شامل دسته‌بندی اطلاعات استخراج شده از پروژه‌های منتخب به بخش‌های منطقی مانند: معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، و منطق کسب‌وکار. خارج از این مرحله: اسکن پروژه‌ها، تولید پرامپت نهایی. نکته حیاتی: دسته‌بندی باید بر اساس نوع اطلاعات و relevance به تسک فعلی باشد.
- [ ] **مرحله 7: تولید پرامپت نهایی با تلفیق اطلاعات پروژه فعلی و پروژه‌های منتخب** — این مرحله شامل تولید پرامپت نهایی است که ترکیبی از: توضیحات تسک کاربر، اطلاعات پروژه فعلی (که تسک برای آن نوشته شده)، و اطلاعات دسته‌بندی شده از پروژه‌های منتخب است. پرامپت نهایی باید به گونه‌ای باشد که مدل بتواند با استفاده از الگوها و منطق پروژه‌های مرجع، تسک را در پروژه فعلی پیاده‌سازی کند. خارج 
- [ ] **مرحله 8: اضافه کردن قابلیت انتخاب پروژه در ایجاد پروژه جدید از موتور خالق (فرانت‌اند)** — این مرحله شامل افزودن قابلیت انتخاب پروژه‌های مرجع در فرآیند ایجاد پروژه جدید از طریق موتور خالق در فرانت‌اند است. کاربر باید بتواند هنگام تعریف پروژه جدید، پروژه‌های موجود را به عنوان مرجع انتخاب کند. خارج از این مرحله: تلگرام، منطق استخراج اطلاعات. نکته حیاتی: این قابلیت مشابه مرحله ۲ است اما برای
- [ ] **مرحله 9: ذخیره‌سازی پروژه‌های مرجع در ساختار پروژه جدید** — این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های مرجع (reference_projects) در ساختار پروژه جدید هنگام ایجاد آن است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید اعتبارسنجی کند که پروژه‌های مرجع واقعاً در سیستم وجود دارند.
- [ ] **مرحله 10: یکپارچه‌سازی منطق استخراج اطلاعات برای پروژه جدید** — این مرحله شامل اطمینان از این است که منطق استخراج اطلاعات (مرحله ۵) و دسته‌بندی (مرحله ۶) برای پروژه‌های مرجع در ایجاد پروژه جدید نیز قابل استفاده باشد. خارج از این مرحله: UI، تولید پرامپت. نکته حیاتی: باید از همان منطق استفاده کند و فقط context را تغییر دهد.
- [ ] **مرحله 11: تولید پرامپت نهایی برای ایجاد پروژه جدید با استفاده از پروژه‌های مرجع** — این مرحله شامل تولید پرامپت نهایی برای موتور خالق است که ترکیبی از: توضیحات کاربر برای پروژه جدید، و اطلاعات دسته‌بندی شده از پروژه‌های مرجع است. خارج از این مرحله: استخراج اطلاعات، UI. نکته حیاتی: پرامپت باید به گونه‌ای باشد که موتور خالق بتواند پروژه جدید را با الهام از پروژه‌های مرجع ایجاد کند.

---

# 🔹 مرحله 1: ایجاد مدل داده برای پروژه‌های منتخب در تسک

**Scope:** این مرحله شامل طراحی و پیاده‌سازی مدل داده‌ای است که امکان ذخیره‌سازی لیستی از پروژه‌های منتخب (selected_projects) را در ساختار تسک فراهم کند. این مدل باید شامل شناسه پروژه (project_id)، مسیر پروژه (project_path) و وضعیت انتخاب (is_selected) باشد. خارج از این مرحله: تغییرات UI، منطق انتخاب در فرانت‌اند یا تلگرام، و منطق استخراج اطلاعات از پروژه‌ها. نکته حیاتی: مدل باید با ساختار فعلی تسک (Task) سازگار باشد و از نوع داده‌های موجود (مثل لیست یا آرایه) استفاده کند.
**Key terms:** Task, selected_projects, project_id, project_path, is_selected, مدل داده تسک

**بخش مربوط از متن کاربر:**
```
قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم) بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن selected_projects به مدل داده تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/external_prompts.py:77-93` — `_task_summary`
  ```python
  def _task_summary(task: Any) -> Dict[str, Any]:
      return {
          "task_id": task.id,
          "watched_id": task.watched_id,
          "project": task.project_full_name,
          "title": task.title,
          "type": task.type,
          "priority": task.priority,
          "execution
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد مدل داده برای پروژه‌های منتخب در تسک. این مرحله شامل طراحی و پیاده‌سازی مدل داده‌ای است که امکان ذخیره‌سازی لیستی از پروژه‌های منتخب (selected_projects) را در ساختار تسک فراهم کند. این مدل باید شامل شناسه پروژه (project_id)، مسیر پروژه (project_path) و وضعیت انتخاب (is_selected) باشد. خارج از این مرحله: تغییرات UI، منطق انتخاب در فرانت‌اند یا تلگرام، و منطق استخراج اطلاعات از پروژه‌ها. نکته حیاتی: مدل باید با ساختار فعلی تسک (Task) سازگار باشد و از نوع داده‌های موجود (مثل لیست یا آرایه) استفاده کند. کاربر درخواست کرده: "قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم) بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم". کلیدواژه‌ها: Task, selected_projects, project_id, project_path, is_selected, مدل داده تسک. در کد فعلی، مدل تسک در فایل‌های oversight_service.py و prompt_github_sync.py تعریف شده است. فایل external_prompts.py (خطوط 66-93) نشان می‌دهد که تسک‌ها دارای فیلدهایی مانند id, watched_id, project_full_name, title, type, priority, execution_priority, status, external_status, verification_status, github_prompt_path, external_attempts, created_at, updated_at هستند. همچنین فایل‌های target_files, tags, deadline, external_locked_by, external_lease_until نیز وجود دارند. مدل selected_projects باید به عنوان یک فیلد جدید از نوع List[Dict] یا یک کلاس مجزا اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل backend/app/services/oversight_service.py، یک کلاس جدید به نام SelectedProjectInfo با فیلدهای project_id (str), project_path (str), is_selected (bool) تعریف کن. 2. در همان فایل، به کلاس OversightTask (یا معادل آن) یک فیلد جدید به نام selected_projects از نوع List[SelectedProjectInfo] با مقدار پیش‌فرض [] اضافه کن. 3. در فایل backend/app/services/prompt_github_sync.py، متدهای serialize/deserialize تسک را به‌روز کن تا فیلد selected_projects را نیز شامل شوند. 4. در فایل backend/app/api/routes/external_prompts.py، تابع _task_summary (خطوط 77-93) را به‌روز کن تا selected_projects را نیز در خروجی برگرداند. 5. در endpoint GET /external/prompts/{task_id} (خطوط 171-190)، selected_projects را به پاسخ اضافه کن. 6. در endpoint POST /external/prompts/{task_id}/claim (خطوط 193-239)، اطمینان حاصل کن که selected_projects در هنگام claim حفظ می‌شود. 7. در endpoint POST /external/prompts/{task_id}/complete (خطوط 276-316)، selected_projects را در applied_evidence ذخیره کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (فرانت‌اند)

**Scope:** این مرحله شامل افزودن یک کامپوننت UI به فرم ایجاد/ویرایش تسک در فرانت‌اند است که لیست پروژه‌های موجود (لود شده در سیستم) را نمایش دهد و کاربر بتواند با تیک زدن، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: منطق تلگرام، منطق استخراج اطلاعات از پروژه‌ها، و ذخیره‌سازی نهایی. نکته حیاتی: لیست پروژه‌ها باید از API موجود که پروژه‌های لود شده را برمی‌گرداند، دریافت شود.
**Key terms:** فرم ایجاد تسک, کامپوننت انتخاب پروژه, لیست پروژه‌ها, API پروژه‌ها, checkbox

**بخش مربوط از متن کاربر:**
```
بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن کامپوننت انتخاب پروژه به فرم ایجاد تسک فرانت‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/oversight/page.tsx:1-50` — `OversightPage` — این فایل صفحه اصلی oversight را شامل می‌شود. کامپوننت ProjectSelector باید در اینجا به فرم اضافه شود.
  ```tsx
  // فایل کامل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/api/routes/oversight.py:1-30` — `TaskCreateRequest` — مدل درخواست ایجاد تسک باید فیلد project_ids را اضافه کند.
  ```python
  // فایل کامل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/services/oversight_service.py:1-50` — `create_task` — متد create_task باید project_ids را ذخیره کند.
  ```python
  // فایل کامل deep-read نشده — بر اساس ساختار سطحی
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/simple_projects.py` — این فایل احتمالاً endpoint لیست پروژه‌ها را دارد

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (فرانت‌اند) را دارد. این مرحله شامل افزودن یک کامپوننت UI به فرم ایجاد/ویرایش تسک در فرانت‌اند است که لیست پروژه‌های موجود (لود شده در سیستم) را نمایش دهد و کاربر بتواند با تیک زدن، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: منطق تلگرام، منطق استخراج اطلاعات از پروژه‌ها، و ذخیره‌سازی نهایی. نکته حیاتی: لیست پروژه‌ها باید از API موجود که پروژه‌های لود شده را برمی‌گرداند، دریافت شود. کلیدواژه‌های کاربر: فرم ایجاد تسک, کامپوننت انتخاب پروژه, لیست پروژه‌ها, API پروژه‌ها, checkbox. در کد فعلی، فایل `backend/app/api/routes/simple_projects.py` احتمالاً endpoint‌های مربوط به پروژه‌ها را دارد. فایل `backend/app/api/routes/oversight.py` شامل endpoint‌های مربوط به تسک‌ها (از جمله ایجاد تسک) است. فایل `frontend/src/app/oversight/page.tsx` صفحه اصلی oversight را شامل می‌شود که احتمالاً فرم ایجاد تسک در آن قرار دارد. فایل `backend/app/services/oversight_service.py` سرویس اصلی مدیریت تسک‌ها است. فایل `backend/app/models/project.py` مدل دیتابیس پروژه‌ها را تعریف می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `frontend/src/app/oversight/page.tsx`، یک کامپوننت جدید به نام `ProjectSelector` ایجاد کن که از یک API (مثلاً `GET /api/simple-projects` یا `GET /api/projects`) لیست پروژه‌ها را دریافت کند. 2. این کامپوننت شامل یک سری checkbox باشد که هر کدام نماینده یک پروژه هستند. 3. وضعیت انتخاب‌شده پروژه‌ها را در state کامپوننت والد (فرم ایجاد تسک) ذخیره کن. 4. در زمان ارسال فرم، شناسه پروژه‌های انتخاب‌شده به payload اضافه شود. 5. در فایل `backend/app/api/routes/oversight.py`، مدل درخواست ایجاد تسک (مثلاً `TaskCreateRequest`) را به‌روز کن تا فیلد `project_ids: List[str]` را بپذیرد. 6. در فایل `backend/app/services/oversight_service.py`، متد ایجاد تسک را به‌روز کن تا `project_ids` را ذخیره کند. 7. از `backend/app/api/routes/simple_projects.py` برای دریافت لیست پروژه‌ها استفاده کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (تلگرام)

**Scope:** این مرحله شامل افزودن قابلیت انتخاب پروژه در ربات تلگرام هنگام ایجاد تسک است. کاربر باید بتواند از طریق دکمه‌های inline یا منوی انتخاب، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند و انتخاب‌ها به صورت لیست ذخیره شوند.
**Key terms:** ربات تلگرام, inline keyboard, انتخاب پروژه, Telegram API, callback_data

**بخش مربوط از متن کاربر:**
```
چه در تلگرام و چه در خود فرانت ... بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن انتخاب پروژه به ربات تلگرام هنگام ایجاد تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_telegram_compose.py:1-50` — `کل فایل` — این فایل مسئول ساختار پیام‌های تلگرام است. باید تابع جدید برای ساخت inline keyboard و اصلاح تابع compose_task_creation_message در اینجا انجام شود.
  ```python
  فایل در deep context موجود است اما محتوای کامل آن ارائه نشده است.
  ```
- `backend/app/services/oversight_service.py:1-100` — `کلاس OversightTask` — باید فیلد project_ids: List[str] به مدل OversightTask اضافه شود تا انتخاب‌های پروژه ذخیره شوند.
  ```python
  فایل در deep context موجود است اما محتوای کامل آن ارائه نشده است.
  ```
- `backend/app/api/routes/external_prompts.py:129-168` — `تابع list_next_prompts`
  ```python
  @router.get("/next", dependencies=[Depends(require_external_token)])
  async def list_next_prompts(
      limit: int = Query(default=10, ge=1, le=100),
      watched_id: Optional[str] = Query(default=None),
  ):
      """لیست تسک‌های pick
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت انتخاب پروژه در ربات تلگرام هنگام ایجاد تسک را دارد. کاربر باید بتواند از طریق دکمه‌های inline یا منوی انتخاب، پروژه‌های مورد نظر را انتخاب کند. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند و انتخاب‌ها به صورت لیست ذخیره شوند. بخش مربوط از درخواست اصلی کاربر: 'چه در تلگرام و چه در خود فرانت ... بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم'. کلیدواژه‌ها: ربات تلگرام, inline keyboard, انتخاب پروژه, Telegram API, callback_data. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. بر اساس کد واقعی پروژه، فایل `backend/app/services/oversight_telegram_compose.py` مسئول ساختار پیام‌های تلگرام است و باید inline keyboard برای انتخاب پروژه به آن اضافه شود. همچنین فایل `backend/app/api/routes/projects.py` (که در deep context موجود نیست ولی در ساختار پروژه موجود است) API پروژه‌ها را ارائه می‌دهد. فایل `backend/app/services/oversight_service.py` که در deep context موجود است و توسط `backend/app/api/routes/external_prompts.py` import شده، مدیریت تسک‌ها را بر عهده دارد و باید فیلد `project_ids` به مدل تسک اضافه شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `backend/app/services/oversight_telegram_compose.py` (که در deep context موجود است)، یک تابع جدید به نام `_build_project_selection_keyboard` ایجاد کن که با فراخوانی API پروژه‌ها (GET /api/projects) لیست پروژه‌ها را دریافت کرده و یک inline keyboard با دکمه‌های callback_data حاوی project_id بسازد. 2. در همان فایل، تابع `compose_task_creation_message` را اصلاح کن تا پس از دریافت متن تسک، این کیبورد را نمایش دهد. 3. در فایل `backend/app/services/oversight_service.py`، فیلد `project_ids: List[str]` را به مدل OversightTask اضافه کن (در کلاس مربوطه). 4. در فایل `backend/app/api/routes/external_prompts.py`، endpoint جدیدی برای هندل کردن callback_data از تلگرام اضافه کن که project_id انتخاب شده را در task ذخیره کند. 5. در فایل `backend/app/services/oversight_telegram_compose.py`، تابع `_handle_callback_query` را برای پردازش callback_data مربوط به انتخاب پروژه به‌روز کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: ذخیره‌سازی پروژه‌های منتخب در دیتابیس هنگام ایجاد تسک

**Scope:** این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های منتخب (selected_projects) در دیتابیس هنگام ایجاد یا ویرایش تسک است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید از مدل داده مرحله ۱ استفاده کند و اعتبارسنجی کند که پروژه‌های انتخاب شده واقعاً در سیستم وجود دارند.
**Key terms:** API ایجاد تسک, ذخیره‌سازی selected_projects, اعتبارسنجی پروژه, دیتابیس

**بخش مربوط از متن کاربر:**
```
پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه
```

## 🎯 هدف (خلاصه ساختاریافته)
ذخیره‌سازی selected_projects در دیتابیس هنگام ایجاد تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightTask` — کلاس مدل تسک. باید فیلد `selected_projects: List[str] = []` به این کلاس اضافه شود.
  ```python
  class OversightTask(BaseModel):
      id: str
      watched_id: str
      project_full_name: str
      title: str
      type: str
      priority: str
      status: str
      ...
  ```
- `backend/app/api/routes/oversight.py:100-150` — `create_task` — Endpoint ایجاد تسک. باید فیلد `selected_projects` را از درخواست دریافت کرده، اعتبارسنجی کند و به سرویس ارسال کند.
  ```python
  @router.post("/tasks", response_model=TaskResponse)
  async def create_task(request: CreateTaskRequest):
      ...
  ```
- `backend/app/models/project.py:1-30` — `Project` — مدل دیتابیس پروژه. برای اعتبارسنجی وجود پروژه‌ها استفاده می‌شود.
  ```python
  class Project(Base):
      __tablename__ = "projects"
      id = Column(String, primary_key=True)
      name = Column(String)
      ...
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` — ف

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که هنگام ایجاد یا ویرایش تسک، لیست پروژه‌های منتخب (selected_projects) در دیتابیس ذخیره شود. این قابلیت به کاربر اجازه می‌دهد تا در زمان نوشتن توضیحات خام (پرامپت خام) برای یک تسک، پروژه‌های مرتبط را تیک بزند و انتخاب کند. سپس وقتی درخواست ارسال می‌شود، این پروژه‌های منتخب همراه با تسک ذخیره می‌شوند. نکته حیاتی: باید از مدل داده مرحله ۱ استفاده کند و اعتبارسنجی کند که پروژه‌های انتخاب شده واقعاً در سیستم وجود دارند. خارج از این مرحله: UI، منطق استخراج اطلاعات. کلیدواژه‌ها: API ایجاد تسک, ذخیره‌سازی selected_projects, اعتبارسنجی پروژه, دیتابیس. بر اساس کد واقعی پروژه، endpoint ایجاد/ویرایش تسک در فایل `backend/app/api/routes/oversight.py` (احتمالاً تابع `create_task` یا `update_task`) و مدل داده تسک در `backend/app/services/oversight_service.py` (کلاس `OversightTask`) قرار دارد. همچنین مدل پروژه در `backend/app/models/project.py` تعریف شده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. به مدل `OversightTask` در `backend/app/services/oversight_service.py` یک فیلد جدید به نام `selected_projects` از نوع `List[str]` اضافه کن (لیست ID پروژه‌ها).
2. در endpoint ایجاد تسک (احتمالاً `POST /api/oversight/tasks` در `backend/app/api/routes/oversight.py`)، فیلد `selected_projects` را از درخواست دریافت کن.
3. قبل از ذخیره، اعتبارسنجی کن که تمام IDهای موجود در `selected_projects` در دیتابیس (جدول `Project` در `backend/app/models/project.py`) وجود دارند.
4. لیست معتبر را در فیلد `selected_projects` مدل تسک ذخیره کن.
5. در endpoint ویرایش تسک (احتمالاً `PUT /api/oversight/tasks/{task_id}`)، همین منطق را پیاده‌سازی کن.
6. اطمینان حاصل کن که فیلد جدید در پاسخ‌های API (GET task) نیز بازگردانده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: ایجاد منطق استخراج اطلاعات از پروژه‌های منتخب

**Scope:** این مرحله شامل پیاده‌سازی منطقی است که هنگام ارسال درخواست تسک، به پروژه‌های منتخب مراجعه کرده و تمام فایل‌ها و زیرشاخه‌های آن‌ها را اسکن کند. اطلاعات استخراج شده شامل: ساختار دایرکتوری، محتوای فایل‌ها، کلاس‌ها، توابع، و منطق کسب‌وکار. خارج از این مرحله: تولید پرامپت نهایی، UI. نکته حیاتی: باید از توکن و رندر موجود برای دسترسی به پروژه‌ها استفاده کند و فقط پروژه‌هایی که در سیستم لود شده‌اند را پردازش کند.
**Key terms:** استخراج اطلاعات, اسکن پروژه, فایل‌ها, زیرشاخه‌ها, منطق کسب‌وکار, توکن, رندر

**بخش مربوط از متن کاربر:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی منطق استخراج اطلاعات از پروژه‌های منتخب

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:152-180` — `جمع‌آوری فایل‌ها با supported_extensions و os.walk`
  ```python
  supported_extensions = {
      '.py', '.js', '.ts', '.tsx', '.jsx', '.java',
      '.go', '.rs', '.cpp', '.c', '.h', '.hpp',
      '.rb', '.php', '.swift', '.kt', '.scala',
      '.vue', '.svel
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد منطق استخراج اطلاعات از پروژه‌های منتخب. این مرحله شامل پیاده‌سازی منطقی است که هنگام ارسال درخواست تسک، به پروژه‌های منتخب مراجعه کرده و تمام فایل‌ها و زیرشاخه‌های آن‌ها را اسکن کند. اطلاعات استخراج شده شامل: ساختار دایرکتوری، محتوای فایل‌ها، کلاس‌ها، توابع، و منطق کسب‌وکار. خارج از این مرحله: تولید پرامپت نهایی، UI. نکته حیاتی: باید از توکن و رندر موجود برای دسترسی به پروژه‌ها استفاده کند و فقط پروژه‌هایی که در سیستم لود شده‌اند را پردازش کند. مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه. کلیدواژه‌ها: استخراج اطلاعات, اسکن پروژه, فایل‌ها, زیرشاخه‌ها, منطق کسب‌وکار, توکن, رندر. شواهد در کد: در `backend/app/api/routes/analysis.py` خطوط 152-180 منطق جمع‌آوری فایل‌ها با `supported_extensions` و `os.walk` وجود دارد که می‌تواند پایه‌ای برای اسکن پروژه‌ها باشد. همچنین در `backend/app/services/project_service.py` سرویس مدیریت پروژه‌ها وجود دارد که می‌تواند برای دسترسی به پروژه‌های لود شده استفاده شود. در `backend/app/services/oversight_service.py` نیز منطق مدیریت تسک‌ها و دسترسی به پروژه‌های watched وجود دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد یک سرویس جدید به نام `backend/app/services/project_scanner.py` که وظیفه اسکن پروژه‌های منتخب را بر عهده دارد. ۲. در این سرویس، متد `scan_project(project_id: str) -> Dict` را پیاده‌سازی کن که با استفاده از `os.walk` مشابه خطوط 152-180 `analysis.py`، تمام فایل‌های پروژه را با `supported_extensions` جمع‌آوری کند. ۳. متد `extract_logic(files: List[Dict]) -> Dict` را اضافه کن که با استفاده از regex یا AST، کلاس‌ها، توابع و منطق کسب‌وکار را از محتوای فایل‌ها استخراج کند. ۴. متد `extract_structure(files: List[Dict]) -> Dict` برای استخراج ساختار دایرکتوری. ۵. متد `extract_all(project_ids: List[str]) -> Dict` که ترکیبی از موارد بالا را انجام دهد. ۶. از `backend/app/services/oversight_service.py` برای دسترسی به پروژه‌های watched و `backend/app/services/project_service.py` برای دسترسی به پروژه‌های لود شده استفاده کن. ۷. از `backend/app/core/database.py` برای دریافت session دیتابیس و دسترسی به پروژه‌ها استفاده کن. ۸. متد `get_project_path(project_id: str) -> str` را برای یافتن مسیر پروژه بر اساس `project_path` در دیتابیس پیاده‌سازی کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 6: دسته‌بندی و ساختاردهی اطلاعات استخراج شده

**Scope:** این مرحله شامل دسته‌بندی اطلاعات استخراج شده از پروژه‌های منتخب به بخش‌های منطقی مانند: معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، و منطق کسب‌وکار. خارج از این مرحله: اسکن پروژه‌ها، تولید پرامپت نهایی. نکته حیاتی: دسته‌بندی باید بر اساس نوع اطلاعات و relevance به تسک فعلی باشد.
**Key terms:** دسته‌بندی, معماری, کلاس‌ها, توابع, API‌ها, مدل‌های داده, منطق کسب‌وکار

**بخش مربوط از متن کاربر:**
```
دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
ساخت سرویس دسته‌بندی اطلاعات استخراج‌شده از پروژه‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:96-124` — `run_analysis_stream`
  ```python
  from ...services.ai_manager import get_ai_manager
      from ...services.deep_analysis_service import DeepAnalysisService
  
      # صف برای ارسال روید
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که اطلاعات استخراج‌شده از پروژه‌های منتخب (که در مرحلهٔ قبل اسکن شده‌اند) به بخش‌های منطقی دسته‌بندی شوند. این دسته‌بندی باید بر اساس نوع اطلاعات و relevance به تسک فعلی انجام شود. بخش‌های منطقی شامل: معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، و منطق کسب‌وکار. کاربر تأکید کرده است که «دسته‌بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه». کلیدواژه‌های اصلی: دسته‌بندی, معماری, کلاس‌ها, توابع, API‌ها, مدل‌های داده, منطق کسب‌وکار. خارج از این مرحله: اسکن پروژه‌ها، تولید پرامپت نهایی. در کد فعلی، فایل `backend/app/services/deep_analysis_service.py` (که در deep context موجود نیست ولی در `backend/app/api/routes/analysis.py` خط 96 import شده) احتمالاً محل اصلی پیاده‌سازی تحلیل عمیق است. همچنین `backend/app/services/creator_idea_to_prompt.py` و `backend/app/services/creator_engine.py` برای تولید پرامپت نهایی استفاده می‌شوند. فایل `backend/app/services/oversight_service.py` (import شده در `backend/app/api/routes/external_prompts.py` خط 20) نیز برای مدیریت تسک‌ها و پرامپت‌ها استفاده می‌شود. نیاز است یک سرویس جدید یا تابع جدید در یکی از سرویس‌های موجود ایجاد شود که اطلاعات استخراج‌شده را بر اساس نوع (معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، منطق کسب‌وکار) دسته‌بندی کند و سپس پرامپت نهایی را با ترکیب این دسته‌بندی‌ها و اطلاعات پروژه فعلی تولید کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/information_classifier.py` که وظیفهٔ دسته‌بندی اطلاعات استخراج‌شده را بر عهده دارد. 2. در این سرویس، یک تابع `classify_information(files: List[Dict], project_context: Dict) -> Dict[str, List[Dict]]` ایجاد شود که فایل‌های استخراج‌شده را بر اساس نوع اطلاعات (معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، منطق کسب‌وکار) دسته‌بندی کند. 3. برای تشخیص نوع اطلاعات، از regex patterns و تحلیل محتوای فایل‌ها (مانند جستجوی کلمات کلیدی 'class', 'def', 'api', 'route', 'model', 'business') استفاده شود. 4. سپس یک تابع `generate_final_prompt(classified_info: Dict, current_project_info: Dict) -> str` ایجاد شود که پرامپت نهایی را با ترکیب اطلاعات دسته‌بندی‌شده و اطلاعات پروژه فعلی تولید کند. 5. این سرویس جدید در `backend/app/api/routes/analysis.py` (خط 96) به `DeepAnalysisService` اضافه شود تا پس از تحلیل، دسته‌بندی و تولید پرامپت نهایی انجام شود. 6. همچنین در `backend/app/services/creator_idea_to_prompt.py` (که در deep context نیست ولی در ساختار پروژه موجود است) از این سرویس برای تولید پرامپت نهایی استفاده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: تولید پرامپت نهایی با تلفیق اطلاعات پروژه فعلی و پروژه‌های منتخب

**Scope:** این مرحله شامل تولید پرامپت نهایی است که ترکیبی از: توضیحات تسک کاربر، اطلاعات پروژه فعلی (که تسک برای آن نوشته شده)، و اطلاعات دسته‌بندی شده از پروژه‌های منتخب است. پرامپت نهایی باید به گونه‌ای باشد که مدل بتواند با استفاده از الگوها و منطق پروژه‌های مرجع، تسک را در پروژه فعلی پیاده‌سازی کند. خارج از این مرحله: اسکن و دسته‌بندی اطلاعات. نکته حیاتی: پرامپت باید شامل ارجاعات دقیق به فایل‌ها و کلاس‌های خاص از پروژه‌های مرجع باشد.
**Key terms:** پرامپت نهایی, تلفیق اطلاعات, پروژه فعلی, پروژه‌های منتخب, مدل

**بخش مربوط از متن کاربر:**
```
پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
تولید پرامپت نهایی با تلفیق اطلاعات پروژه فعلی و پروژه‌های منتخب

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل تولید پرامپت نهایی است که ترکیبی از: توضیحات تسک کاربر، اطلاعات پروژه فعلی (که تسک برای آن نوشته شده)، و اطلاعات دسته‌بندی شده از پروژه‌های منتخب است. پرامپت نهایی باید به گونه‌ای باشد که مدل بتواند با استفاده از الگوها و منطق پروژه‌های مرجع، تسک را در پروژه فعلی پیاده‌سازی کند. خارج از این مرحله: اسکن و دسته‌بندی اطلاعات. نکته حیاتی: پرامپت باید شامل ارجاعات دقیق به فایل‌ها و کلاس‌های خاص از پروژه‌های مرجع باشد. بر اساس متن کاربر: "پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه". کلیدواژه‌ها: پرامپت نهایی, تلفیق اطلاعات, پروژه فعلی, پروژه‌های منتخب, مدل. در کد فعلی، فایل‌های backend/app/api/routes/external_prompts.py (خطوط 1-9) و backend/app/services/oversight_service.py و backend/app/services/prompt_github_sync.py (خطوط 21-24) ساختار تسک‌ها و پرامپت‌ها را مدیریت می‌کنند. همچنین backend/app/services/creator_idea_to_prompt.py و backend/app/services/creator_engine.py برای تبدیل ایده به پرامپت و تولید پروژه استفاده می‌شوند. فایل backend/app/services/oversight_service.py شامل کلاس OversightService با متدهای _save_tasks و _schedule_prompt_sync است که برای ذخیره و همگام‌سازی پرامپت‌ها استفاده می‌شود. فایل backend/app/services/prompt_github_sync.py شامل توابعی مانند _resolve_repo_and_branch و _file_path و compute_execution_priority است که برای مدیریت مسیر فایل‌های پرامپت در GitHub استفاده می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/prompt_fusion_service.py` که وظیفه تلفیق اطلاعات پروژه فعلی و پروژه‌های منتخب را بر عهده دارد. 2. در این سرویس، یک تابع `generate_fused_prompt(task_data: dict, current_project_info: dict, selected_projects_info: list) -> str` ایجاد کن که: الف) توضیحات تسک کاربر را از `task_data['raw_idea']` استخراج کند (مشابه فیلد raw_idea در external_prompts.py خط 181). ب) اطلاعات پروژه فعلی (شامل فایل‌ها، ساختار، وابستگی‌ها) را از `current_project_info` دریافت کند. ج) اطلاعات دسته‌بندی شده از پروژه‌های منتخب (شامل الگوها، کلاس‌ها، توابع خاص) را از `selected_projects_info` دریافت کند. د) پرامپت نهایی را با فرمت مشخصی تولید کند که شامل: بخش توضیحات تسک، بخش اطلاعات پروژه فعلی با ارجاع به فایل‌های واقعی (مانند backend/app/api/routes/analysis.py خطوط 30-36 برای ساختار AnalysisRequest)، بخش الگوهای استخراج شده از پروژه‌های مرجع با ذکر دقیق نام فایل‌ها و کلاس‌ها (مانند کلاس OversightService در oversight_service.py)، و بخش دستورالعمل پیاده‌سازی بر اساس شرایط پروژه فعلی. 3. این سرویس را در backend/app/api/routes/external_prompts.py به endpointهای موجود اضافه کن، مثلاً یک endpoint جدید POST /external/prompts/fuse که از مدل ClaimRequest (خطوط 101-103) استفاده کند. 4. متد `_task_summary` در external_prompts.py (خطوط 77

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 8: اضافه کردن قابلیت انتخاب پروژه در ایجاد پروژه جدید از موتور خالق (فرانت‌اند)

**Scope:** این مرحله شامل افزودن قابلیت انتخاب پروژه‌های مرجع در فرآیند ایجاد پروژه جدید از طریق موتور خالق در فرانت‌اند است. کاربر باید بتواند هنگام تعریف پروژه جدید، پروژه‌های موجود را به عنوان مرجع انتخاب کند. خارج از این مرحله: تلگرام، منطق استخراج اطلاعات. نکته حیاتی: این قابلیت مشابه مرحله ۲ است اما برای ایجاد پروژه جدید، نه تسک.
— [merged] این مرحله شامل افزودن قابلیت انتخاب پروژه‌های مرجع در ربات تلگرام هنگام ایجاد پروژه جدید از طریق موتور خالق است. کاربر باید بتواند از طریق دکمه‌های inline پروژه‌های مرجع را انتخاب کند. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند.
**Key terms:** ایجاد پروژه جدید, تلگرام, inline keyboard, انتخاب پروژه مرجع, فرانت‌اند, checkbox, موتور خالق

**بخش مربوط از متن کاربر:**
```
ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن انتخاب پروژه مرجع در ایجاد پروژه جدید موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این صفحه اصلی موتور خالق است که فرم ایجاد پروژه در آن قرار دارد.
  ```tsx
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/api/routes/creator.py:1-30` — `create_project_endpoint` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل شامل endpointهای مربوط به موتور خالق است.
  ```python
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/services/creator_engine.py:1-50` — `CreatorEngine.create_project` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این سرویس اصلی موتور خالق است.
  ```python
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/creator_idea_to_prompt.py` — این سرویس ایده خ

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن قابلیت انتخاب پروژه‌های مرجع در فرآیند ایجاد پروژه جدید از طریق موتور خالق (Creator Engine) را دارد. این قابلیت باید هم در فرانت‌اند وب و هم در ربات تلگرام پیاده‌سازی شود. کاربر می‌گوید: 'ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه'. این قابلیت مشابه مرحله ۲ (انتخاب پروژه مرجع برای تسک) است اما برای ایجاد پروژه جدید، نه تسک. خارج از این مرحله: تلگرام، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند. در فرانت‌اند، کاربر باید بتواند هنگام تعریف پروژه جدید، پروژه‌های موجود را به عنوان مرجع از طریق checkbox انتخاب کند. در تلگرام، از طریق دکمه‌های inline keyboard. فایل‌های مرتبط در کد: frontend/src/app/creator/page.tsx (صفحه اصلی موتور خالق)، backend/app/api/routes/creator.py (API موتور خالق)، backend/app/services/creator_engine.py (سرویس اصلی موتور خالق)، backend/app/services/creator_idea_to_prompt.py (تبدیل ایده به پرامپت).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل frontend/src/app/creator/page.tsx، یک بخش جدید با عنوان 'انتخاب پروژه‌های مرجع' به فرم ایجاد پروژه اضافه شود. این بخش شامل یک جستجوی پروژه (API /api/projects) و لیست checkbox برای انتخاب چندین پروژه مرجع است. 2. در فایل backend/app/api/routes/creator.py، endpoint جدید POST /api/creator/create-project با فیلد reference_project_ids: List[str] اضافه شود. 3. در فایل backend/app/services/creator_engine.py، تابع create_project به‌روزرسانی شود تا reference_project_ids را دریافت کرده و محتوای پروژه‌های مرجع را از دیتابیس (مدل Project) بخواند و به context پرامپت اضافه کند. 4. در فایل backend/app/services/creator_idea_to_prompt.py، تابع build_prompt به‌روزرسانی شود تا محتوای پروژه‌های مرجع را به عنوان context به پرامپت نهایی اضافه کند. 5. برای تلگرام (خارج از این مرحله اما اشاره شده): از همان API استفاده شود با inline keyboard.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: ذخیره‌سازی پروژه‌های مرجع در ساختار پروژه جدید

**Scope:** این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های مرجع (reference_projects) در ساختار پروژه جدید هنگام ایجاد آن است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید اعتبارسنجی کند که پروژه‌های مرجع واقعاً در سیستم وجود دارند.
**Key terms:** API ایجاد پروژه, reference_projects, ذخیره‌سازی, اعتبارسنجی, دیتابیس

**بخش مربوط از متن کاربر:**
```
پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم
```

## 🎯 هدف (خلاصه ساختاریافته)
ذخیره‌سازی و اعتبارسنجی reference_projects در ایجاد پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/projects.py:مشخص نیست در deep-read` — `CreateProjectRequest یا endpoint create_project` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مسیر احتمالی: backend/app/api/routes/projects.py
- `backend/app/models/project.py:مشخص نیست در deep-read` — `class Project` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مسیر احتمالی: backend/app/models/project.py
- `backend/app/services/project_service.py:مشخص نیست در deep-read` — `create_project function` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مسیر احتمالی: backend/app/services/project_service.py

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/github_import.py`

## 🔍 Context و وضعیت فعلی
کاربر درخواست به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های مرجع (reference_projects) در ساختار پروژه جدید هنگام ایجاد آن را دارد. نکته حیاتی: باید اعتبارسنجی کند که پروژه‌های مرجع واقعاً در سیستم وجود دارند. خارج از این مرحله: UI، منطق استخراج اطلاعات. کلیدواژه‌ها: API ایجاد پروژه, reference_projects, ذخیره‌سازی, اعتبارسنجی, دیتابیس. بخش مربوط از درخواست اصلی کاربر: 'پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم'. این تسک بر اساس تحلیل کد واقعی پروژه در فایل‌های backend/app/api/routes/projects.py (مسیر ایجاد پروژه)، backend/app/models/project.py (مدل Project)، backend/app/services/project_service.py (سرویس ایجاد پروژه) و backend/app/services/github_import.py (برای اعتبارسنجی پروژه‌های گیت‌هاب) است. endpoint ایجاد پروژه در routes/projects.py باید فیلد reference_projects را از درخواست دریافت کرده، سپس با جستجو در دیتابیس (مدل Project) و سرویس گیت‌هاب (github_import) وجود آن‌ها را تأیید کند و در نهایت در دیتابیس ذخیره نماید.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در backend/app/api/routes/projects.py، مدل Pydantic مربوط به درخواست ایجاد پروژه (مثلاً CreateProjectRequest) را با اضافه کردن فیلد اختیاری reference_projects: Optional[List[str]] به‌روزرسانی کن. 2. در همان فایل، در endpoint ایجاد پروژه (POST /projects)، بعد از دریافت درخواست، یک تابع اعتبارسنجی validate_reference_projects(project_ids: List[str], db: Session) -> bool اضافه کن که: a) برای هر project_id در دیتابیس (مدل Project) جستجو کند. b) اگر project_id مربوط به پروژه‌ای از گیت‌هاب است، از سرویس github_import (تابع check_repo_access یا مشابه) برای تأیید استفاده کند. c) اگر هرکدام از project_idها معتبر نبود، HTTPException با status 400 برگرداند. 3. در backend/app/services/project_service.py، تابع create_project را به‌روزرسانی کن تا فیلد reference_projects را به مدل Project نسبت دهد و در دیتابیس ذخیره کند. 4. در backend/app/models/project.py، به مدل Project فیلد reference_projects: Column(JSON, default=[]) اضافه کن. 5. تست‌های واحد در backend/tests/ برای سناریوهای موفق و ناموفق اعتبارسنجی بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: یکپارچه‌سازی منطق استخراج اطلاعات برای پروژه جدید

**Scope:** این مرحله شامل اطمینان از این است که منطق استخراج اطلاعات (مرحله ۵) و دسته‌بندی (مرحله ۶) برای پروژه‌های مرجع در ایجاد پروژه جدید نیز قابل استفاده باشد. خارج از این مرحله: UI، تولید پرامپت. نکته حیاتی: باید از همان منطق استفاده کند و فقط context را تغییر دهد.
**Key terms:** استخراج اطلاعات, پروژه مرجع, پروژه جدید, یکپارچه‌سازی, منطق

**بخش مربوط از متن کاربر:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی منطق استخراج اطلاعات برای پروژه‌های مرجع در ایجاد پروژه جدید

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مد
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی منطق استخراج اطلاعات (مرحله ۵) و دسته‌بندی (مرحله ۶) را دارد تا برای پروژه‌های مرجع در ایجاد پروژه جدید نیز قابل استفاده باشد. نکته حیاتی: باید از همان منطق استفاده کند و فقط context را تغییر دهد. خارج از این مرحله: UI، تولید پرامپت. بخش مربوط از درخواست اصلی کاربر: "مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه". کلیدواژه‌ها: استخراج اطلاعات, پروژه مرجع, پروژه جدید, یکپارچه‌سازی, منطق. در کد فعلی، سرویس‌های `deep_analysis_service.py` و `project_analyzer.py` منطق تحلیل و استخراج اطلاعات از پروژه‌ها را دارند. فایل `backend/app/api/routes/analysis.py` در خطوط 46-80 endpoint `POST /analysis/run` را تعریف می‌کند که `project_analyzer.analyze_project` را صدا می‌زند. همچنین endpoint `POST /analysis/run-stream` در خطوط 83-268 از `DeepAnalysisService` برای تحلیل استریم‌شده استفاده می‌کند. سرویس `creator_engine.py` و `simple_creator.py` برای ایجاد پروژه جدید استفاده می‌شوند. منطق استخراج اطلاعات فعلی در `deep_analysis_service.py` (فایل موجود در ساختار پروژه) پیاده‌سازی شده است که فایل‌ها را جمع‌آوری، با مدل‌های AI تحلیل و گزارش تولید می‌کند. برای یکپارچه‌سازی، باید این منطق را به صورت یک سرویس/تابع قابل استفاده مجدد استخراج کرد که هم توسط `creator_engine.py` (برای استخراج اطلاعات از پروژه‌های مرجع) و هم توسط `deep_analysis_service.py` (برای تحلیل عادی) قابل فراخوانی باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/extraction_service.py` که منطق استخراج اطلاعات (مرحله ۵) و دسته‌بندی (مرحله ۶) را به صورت یک تابع واحد (`extract_and_classify`) پیاده‌سازی کند. این تابع باید پارامتر `context` (مثلاً 'reference_project' یا 'new_project') را دریافت کند و بر اساس آن رفتار متفاوتی داشته باشد. 2. انتقال منطق جمع‌آوری فایل‌ها از `deep_analysis_service.py` (خطوط 126-180 در `backend/app/api/routes/analysis.py`) به `extraction_service.py`. 3. انتقال منطق فراخوانی مدل AI برای تحلیل و استخراج اطلاعات از `deep_analysis_service.py` به `extraction_service.py`. 4. در `creator_engine.py` (فایل موجود در ساختار پروژه)، هنگام ایجاد پروژه جدید، به جای تحلیل مجدد، از `extraction_service.extract_and_classify` با context='reference_project' استفاده شود. 5. در `deep_analysis_service.py`، فراخوانی `extraction_service.extract_and_classify` با context='new_project' جایگزین منطق فعلی شود. 6. اطمینان از اینکه خروجی استخراج اطلاعات (شامل فایل‌های مرتبط، وابستگی‌ها، الگوها) به صورت یک ساختار داده استاندارد (مثلاً `ExtractionResult`) بازگردانده می‌شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 11: تولید پرامپت نهایی برای ایجاد پروژه جدید با استفاده از پروژه‌های مرجع

**Scope:** این مرحله شامل تولید پرامپت نهایی برای موتور خالق است که ترکیبی از: توضیحات کاربر برای پروژه جدید، و اطلاعات دسته‌بندی شده از پروژه‌های مرجع است. خارج از این مرحله: استخراج اطلاعات، UI. نکته حیاتی: پرامپت باید به گونه‌ای باشد که موتور خالق بتواند پروژه جدید را با الهام از پروژه‌های مرجع ایجاد کند.
**Key terms:** پرامپت نهایی, موتور خالق, پروژه جدید, پروژه‌های مرجع, مدل

**بخش مربوط از متن کاربر:**
```
پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
تولید پرامپت نهایی موتور خالق با پروژه‌های مرجع

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/creator_idea_to_prompt.py:1-50` — `کل فایل` — مکان اصلی برای افزودن تابع جدید `generate_final_prompt_with_references`. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  # این فایل deep-read نشده است. بر اساس ساختار سطحی پروژه، این سرویس وظیفه تبدیل ایده به پرامپت را دارد.
  ```
- `backend/app/api/routes/creator.py:1-30` — `router`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست تولید پرامپت نهایی برای ایجاد پروژه جدید با استفاده از پروژه‌های مرجع را دارد. این مرحله شامل تولید پرامپت نهایی برای موتور خالق است که ترکیبی از: توضیحات کاربر برای پروژه جدید، و اطلاعات دسته‌بندی شده از پروژه‌های مرجع است. خارج از این مرحله: استخراج اطلاعات، UI. نکته حیاتی: پرامپت باید به گونه‌ای باشد که موتور خالق بتواند پروژه جدید را با الهام از پروژه‌های مرجع ایجاد کند. پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه. کلیدواژه‌ها: پرامپت نهایی, موتور خالق, پروژه جدید, پروژه‌های مرجع, مدل. در کد فعلی، سرویس `creator_engine.py` در مسیر `backend/app/services/creator_engine.py` مسئول تولید پروژه با AI است. همچنین `creator_idea_to_prompt.py` در `backend/app/services/creator_idea_to_prompt.py` ایده را به پرامپت تبدیل می‌کند. روتر `creator.py` در `backend/app/api/routes/creator.py` endpointهای مربوطه را ارائه می‌دهد. فایل `simple_creator.py` در `backend/app/services/simple_creator.py` نیز یک نسخه ساده‌تر از موتور خالق است. برای پیاده‌سازی این درخواست، باید یک سرویس جدید یا تابع جدید در `creator_engine.py` یا `creator_idea_to_prompt.py` ایجاد شود که بتواند پرامپت نهایی را با ترکیب توضیحات کاربر و اطلاعات ساختاریافته از پروژه‌های مرجع (که احتمالاً از طریق `project_analyzer.py` یا `deep_analysis_service.py` استخراج شده) تولید کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک تابع جدید به نام `generate_final_prompt_with_references` در فایل `backend/app/services/creator_idea_to_prompt.py` که سه پارامتر اصلی دریافت کند: `user_description` (توضیحات کاربر برای پروژه جدید)، `reference_projects_data` (اطلاعات دسته‌بندی شده از پروژه‌های مرجع - یک لیست از دیکشنری‌ها)، و `model_id` (شناسه مدل AI). 2. این تابع ابتدا یک پرامپت سیستم (system prompt) بسازد که نقش موتور خالق را تعریف کند و تاکید کند که باید از پروژه‌های مرجع به عنوان الهام استفاده کند، نه کپی کردن. 3. سپس بخش 'user prompt' را با ترکیب `user_description` و خلاصه‌ای از `reference_projects_data` (شامل معماری، تکنولوژی‌ها، ساختار دیتابیس، و ویژگی‌های کلیدی هر پروژه مرجع) بسازد. 4. در نهایت، پرامپت نهایی را به فرمت مورد انتظار `creator_engine.py` برگرداند. 5. یک endpoint جدید در `backend/app/api/routes/creator.py` اضافه کند (مثلاً `POST /creator/generate-with-references`) که این تابع را صدا بزند. 6. مدل درخواست (Request Model) مناسب برای این endpoint را در همان فایل تعریف کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: static)_

## Task Steps

### Step 1: ایجاد مدل داده برای پروژه‌های منتخب در تسک
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی مدل داده‌ای است که امکان ذخیره‌سازی لیستی از پروژه‌های منتخب (selected_projects) را در ساختار تسک فراهم کند. این مدل باید شامل شناسه پروژه (project_id)، مسیر پروژه (project_path) و وضعیت انتخاب (is_selected) باشد. خارج از این مرحله: تغییرات UI، منطق انتخاب در فرانت‌اند یا تلگرام، و منطق استخراج اطلاعات از پروژه‌ها. نکته حیاتی: مدل باید با ساختار فعلی تسک (Task) سازگار باشد و از نوع داده‌های موجود (مثل لیست یا آرایه) استفاده کند.
**Excerpt:**
```
قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم) بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

### Step 2: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (فرانت‌اند)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک کامپوننت UI به فرم ایجاد/ویرایش تسک در فرانت‌اند است که لیست پروژه‌های موجود (لود شده در سیستم) را نمایش دهد و کاربر بتواند با تیک زدن، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: منطق تلگرام، منطق استخراج اطلاعات از پروژه‌ها، و ذخیره‌سازی نهایی. نکته حیاتی: لیست پروژه‌ها باید از API موجود که پروژه‌های لود شده را برمی‌گرداند، دریافت شود.
**Excerpt:**
```
بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

### Step 3: اضافه کردن قابلیت انتخاب پروژه در فرم ایجاد تسک (تلگرام)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت انتخاب پروژه در ربات تلگرام هنگام ایجاد تسک است. کاربر باید بتواند از طریق دکمه‌های inline یا منوی انتخاب، پروژه‌های مورد نظر را انتخاب کند. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند و انتخاب‌ها به صورت لیست ذخیره شوند.
**Excerpt:**
```
چه در تلگرام و چه در خود فرانت ... بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

### Step 4: ذخیره‌سازی پروژه‌های منتخب در دیتابیس هنگام ایجاد تسک
**Status:** `pending` (0%)
**Scope:** این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های منتخب (selected_projects) در دیتابیس هنگام ایجاد یا ویرایش تسک است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید از مدل داده مرحله ۱ استفاده کند و اعتبارسنجی کند که پروژه‌های انتخاب شده واقعاً در سیستم وجود دارند.
**Excerpt:**
```
پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه
```

### Step 5: ایجاد منطق استخراج اطلاعات از پروژه‌های منتخب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطقی است که هنگام ارسال درخواست تسک، به پروژه‌های منتخب مراجعه کرده و تمام فایل‌ها و زیرشاخه‌های آن‌ها را اسکن کند. اطلاعات استخراج شده شامل: ساختار دایرکتوری، محتوای فایل‌ها، کلاس‌ها، توابع، و منطق کسب‌وکار. خارج از این مرحله: تولید پرامپت نهایی، UI. نکته حیاتی: باید از توکن و رندر موجود برای دسترسی به پروژه‌ها استفاده کند و فقط پروژه‌هایی که در سیستم لود شده‌اند را پردازش کند.
**Excerpt:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه
```

### Step 6: دسته‌بندی و ساختاردهی اطلاعات استخراج شده
**Status:** `pending` (0%)
**Scope:** این مرحله شامل دسته‌بندی اطلاعات استخراج شده از پروژه‌های منتخب به بخش‌های منطقی مانند: معماری، کلاس‌ها، توابع، API‌ها، مدل‌های داده، و منطق کسب‌وکار. خارج از این مرحله: اسکن پروژه‌ها، تولید پرامپت نهایی. نکته حیاتی: دسته‌بندی باید بر اساس نوع اطلاعات و relevance به تسک فعلی باشد.
**Excerpt:**
```
دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه
```

### Step 7: تولید پرامپت نهایی با تلفیق اطلاعات پروژه فعلی و پروژه‌های منتخب
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تولید پرامپت نهایی است که ترکیبی از: توضیحات تسک کاربر، اطلاعات پروژه فعلی (که تسک برای آن نوشته شده)، و اطلاعات دسته‌بندی شده از پروژه‌های منتخب است. پرامپت نهایی باید به گونه‌ای باشد که مدل بتواند با استفاده از الگوها و منطق پروژه‌های مرجع، تسک را در پروژه فعلی پیاده‌سازی کند. خارج از این مرحله: اسکن و دسته‌بندی اطلاعات. نکته حیاتی: پرامپت باید شامل ارجاعات دقیق به فایل‌ها و کلاس‌های خاص از پروژه‌های مرجع باشد.
**Excerpt:**
```
پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه
```

### Step 8: اضافه کردن قابلیت انتخاب پروژه در ایجاد پروژه جدید از موتور خالق (فرانت‌اند)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن قابلیت انتخاب پروژه‌های مرجع در فرآیند ایجاد پروژه جدید از طریق موتور خالق در فرانت‌اند است. کاربر باید بتواند هنگام تعریف پروژه جدید، پروژه‌های موجود را به عنوان مرجع انتخاب کند. خارج از این مرحله: تلگرام، منطق استخراج اطلاعات. نکته حیاتی: این قابلیت مشابه مرحله ۲ است اما برای ایجاد پروژه جدید، نه تسک.
— [merged] این مرحله شامل افزودن قابلیت انتخاب پروژه‌های مرجع در ربات تلگرام هنگام ایجاد پروژه جدید از طریق موتور خالق است. کاربر باید بتواند از طریق دکمه‌های inline پروژه‌های مرجع را انتخاب کند. خارج از این مرحله: فرانت‌اند وب، منطق استخراج اطلاعات. نکته حیاتی: باید از همان API پروژه‌ها استفاده کند.
**Excerpt:**
```
ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه
```

### Step 9: ذخیره‌سازی پروژه‌های مرجع در ساختار پروژه جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل به‌روزرسانی منطق backend برای ذخیره‌سازی لیست پروژه‌های مرجع (reference_projects) در ساختار پروژه جدید هنگام ایجاد آن است. خارج از این مرحله: UI، منطق استخراج اطلاعات. نکته حیاتی: باید اعتبارسنجی کند که پروژه‌های مرجع واقعاً در سیستم وجود دارند.
**Excerpt:**
```
پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم
```

### Step 10: یکپارچه‌سازی منطق استخراج اطلاعات برای پروژه جدید
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اطمینان از این است که منطق استخراج اطلاعات (مرحله ۵) و دسته‌بندی (مرحله ۶) برای پروژه‌های مرجع در ایجاد پروژه جدید نیز قابل استفاده باشد. خارج از این مرحله: UI، تولید پرامپت. نکته حیاتی: باید از همان منطق استفاده کند و فقط context را تغییر دهد.
**Excerpt:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه
```

### Step 11: تولید پرامپت نهایی برای ایجاد پروژه جدید با استفاده از پروژه‌های مرجع
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تولید پرامپت نهایی برای موتور خالق است که ترکیبی از: توضیحات کاربر برای پروژه جدید، و اطلاعات دسته‌بندی شده از پروژه‌های مرجع است. خارج از این مرحله: استخراج اطلاعات، UI. نکته حیاتی: پرامپت باید به گونه‌ای باشد که موتور خالق بتواند پروژه جدید را با الهام از پروژه‌های مرجع ایجاد کند.
**Excerpt:**
```
پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه
```
