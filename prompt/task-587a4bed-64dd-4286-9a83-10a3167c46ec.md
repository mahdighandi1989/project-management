---
task_id: 587a4bed-64dd-4286-9a83-10a3167c46ec
title: میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه  (5 مرحله)
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-12T11:32:06.523609+00:00'
updated_at: '2026-05-20T04:22:11.731230+00:00'
target_files:
- backend/app/api/routes/analysis.py
---

# میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه  (5 مرحله)

## Raw Idea

میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه در مرحله تولید پرامپت، باید به تمام قسمت های اون پروژه های انتخاب شده که درخواست کردم ایده یا تسک بخی از اون مثل این پروژه های انتخاب شده باشه، لذا مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه -----

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن وقتی ارسالل میشه در مرحله تولید پرامپت، باید به تمام قسمت های اون پروژه های انتخاب شده که درخواست کردم ایده یا تسک بخی از اون مثل این پروژه های انتخاب شده باشه، لذا مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه و در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه -----
```

## 📋 چک‌لیست مراحل (6 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: طراحی ساختار داده و مدل برای پروژه‌های منتخب و ارجاع به آن‌ها در تسک‌ها** — ایجاد مدل‌های دیتابیس یا ساختارهای داده برای ذخیره لیست پروژه‌های لود شده (گیت‌هاب/محلی) و امکان انتخاب چند پروژه در زمان ایجاد تسک. این مرحله شامل منطق فرانت و بک‌اند برای ذخیره و بازیابی پروژه‌های منتخب است.
- [ ] **مرحله 2: اضافه کردن UI انتخاب پروژه در فرانت و تلگرام** — پیاده‌سازی کامپوننت‌های UI در فرانت و بات تلگرام برای نمایش لیست پروژه‌های لود شده و امکان تیک زدن چند پروژه در زمان ایجاد تسک یا پروژه جدید. این مرحله فقط UI است و منطق بک‌اند را شامل نمی‌شود.
- [ ] **مرحله 3: توسعه منطق استخراج اطلاعات از پروژه‌های منتخب** — پیاده‌سازی سرویسی که پس از ارسال درخواست، تمام فایل‌ها و زیرشاخه‌های پروژه‌های منتخب را اسکن کرده، منطق‌ها و ساختارهای مرتبط را استخراج و دسته‌بندی می‌کند. این مرحله شامل خواندن فایل‌ها، پارس کردن کد و استخراج متادیتا است.
- [ ] **مرحله 4: تولید پرامپت نهایی با تلفیق اطلاعات پروژه‌های منتخب و پروژه فعلی** — ایجاد موتور پرامپت‌سازی که اطلاعات استخراج شده از پروژه‌های منتخب را با ساختار و شرایط پروژه فعلی ترکیب کرده و یک پرامپت نهایی منسجم و متناسب با تسک تولید می‌کند. این مرحله شامل منطق تلفیق و اولویت‌بندی است.
- [ ] **مرحله 5: یکپارچه‌سازی با APIهای تلگرام و فرانت برای ارسال و دریافت** — اتصال منطق استخراج و تولید پرامپت به APIهای موجود در تلگرام و فرانت. این مرحله شامل endpointهای جدید برای دریافت پروژه‌های منتخب، ارسال درخواست و بازگرداندن پرامپت نهایی است.
- [ ] **مرحله 6: نوشتن تست‌های واحد و یکپارچه‌سازی برای کل جریان** — نوشتن تست برای مدل‌ها، UI، منطق استخراج، موتور پرامپت و APIها. تست‌ها باید سناریوهای مختلف انتخاب پروژه و استخراج اطلاعات را پوشش دهند.

---

# 🔹 مرحله 1: طراحی ساختار داده و مدل برای پروژه‌های منتخب و ارجاع به آن‌ها در تسک‌ها

**Scope:** ایجاد مدل‌های دیتابیس یا ساختارهای داده برای ذخیره لیست پروژه‌های لود شده (گیت‌هاب/محلی) و امکان انتخاب چند پروژه در زمان ایجاد تسک. این مرحله شامل منطق فرانت و بک‌اند برای ذخیره و بازیابی پروژه‌های منتخب است.
**Key terms:** تسک, پروژه, پروژه های گیت هاب, پروژه های محلی, لیست, تیک بزنم, پرامپت خام

**بخش مربوط از متن کاربر:**
```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی ساختار داده و مدل برای پروژه‌های منتخب و ارجاع به آن‌ها در تسک‌ها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که برای قسمت تسک‌ها، چه در تلگرام و چه در خود فرانت، و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشد که بتواند وقتی به آن در پرامپت خام می‌گوید مثلاً «یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)» بتواند از طریق لیست در زمان نوشتن تسک پروژه و پروژه‌های منتخب را تیک بزند. کلیدواژه‌های کاربر: تسک, پروژه, پروژه های گیت هاب, پروژه های محلی, لیست, تیک بزنم, پرامپت خام. در کد فعلی، مدل `Project` در `backend/app/models/project.py` وجود دارد که فیلدهایی مانند `id`, `name`, `project_type` (مقادیر: `github_import`, `local`, `creator` و غیره) دارد. همچنین در `backend/app/api/routes/github_import.py` خط ۱۹۳-۱۹۵ پروژه‌های `github_import` کوئری می‌شوند. در `backend/app/api/routes/simple_projects.py` احتمالاً پروژه‌های محلی مدیریت می‌شوند. اما هیچ مدل یا جدول واسطی برای ذخیره «پروژه‌های منتخب» در یک تسک وجود ندارد. همچنین در فرانت‌اند، صفحه `frontend/src/app/projects/[id]/page.tsx` جزئیات یک پروژه را نمایش می‌دهد ولی قابلیت انتخاب چند پروژه و ارجاع به آن‌ها در تسک وجود ندارد. نیاز به ایجاد یک مدل جدید `TaskProjectReference` یا فیلد جدید در مدل تسک (اگر وجود دارد) و همچنین API endpoint برای دریافت لیست پروژه‌های لود شده (گیت‌هاب/محلی) و UI برای انتخاب چندتایی آن‌ها در زمان ایجاد تسک است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل دیتابیس `TaskProjectReference` در `backend/app/models/` با فیلدهای: `id`, `task_id` (FK به جدول تسک‌ها - اگر جدول تسک وجود دارد، مثلاً `OversightTask` در `backend/app/models/oversight_session.py`)، `project_id` (FK به `Project.id`)، `reference_type` (مثلاً 'source', 'example', 'dependency')، `notes` (متن دلخواه کاربر). 2. ایجاد API endpoint جدید در `backend/app/api/routes/tasks.py` (یا اضافه کردن به `backend/app/api/routes/oversight.py`) برای: `POST /tasks/{task_id}/projects` (افزودن پروژه منتخب), `GET /tasks/{task_id}/projects` (دریافت لیست پروژه‌های منتخب), `DELETE /tasks/{task_id}/projects/{project_id}` (حذف). 3. ایجاد API endpoint برای دریافت لیست پروژه‌های لود شده (گیت‌هاب و محلی) با فیلتر: `GET /projects/loaded?type=github_import,local`. 4. در فرانت‌اند، افزودن کامپوننت `ProjectSelector` در `frontend/src/components/` که از API لیست پروژه‌ها را گرفته و با چک‌باکس نمایش دهد. 5. ادغام این کامپوننت در صفحه ایجاد تسک (مثلاً `frontend/src/app/oversight/page.tsx` یا `frontend/src/app/creator/page.tsx`) و همچنین در فرم‌های تلگرام (از طریق API). 6. به‌روزرسانی پرامپت خام برای شامل شدن پروژه‌های منتخب در context ارسالی به AI.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 2: اضافه کردن UI انتخاب پروژه در فرانت و تلگرام

**Scope:** پیاده‌سازی کامپوننت‌های UI در فرانت و بات تلگرام برای نمایش لیست پروژه‌های لود شده و امکان تیک زدن چند پروژه در زمان ایجاد تسک یا پروژه جدید. این مرحله فقط UI است و منطق بک‌اند را شامل نمی‌شود.
**Key terms:** فرانت, تلگرام, لیست, تیک زدن, UI, کامپوننت

**بخش مربوط از متن کاربر:**
```
بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن
```

## 🎯 هدف (خلاصه ساختاریافته)
اضافه کردن UI انتخاب پروژه در فرانت و تلگرام برای تیک زدن پروژه‌های منتخب در زمان ایجاد تسک

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن کامپوننت‌های UI در فرانت (Next.js) و بات تلگرام برای نمایش لیست پروژه‌های لود شده و امکان تیک زدن چند پروژه در زمان ایجاد تسک یا پروژه جدید را دارد. این مرحله فقط UI است و منطق بک‌اند را شامل نمی‌شود. کاربر می‌گوید: «بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن». کلیدواژه‌ها: فرانت, تلگرام, لیست, تیک زدن, UI, کامپوننت. در کد فعلی، فایل‌های frontend/src/app/creator/page.tsx (صفحه ایجاد پروژه) و frontend/src/app/projects/[id]/page.tsx (جزئیات پروژه) وجود دارند. همچنین frontend/src/components/TaskFilePicker.tsx و frontend/src/components/ExtractionDefaultPicker.tsx کامپوننت‌های موجود برای انتخاب فایل هستند. backend/app/api/routes/creator.py و backend/app/api/routes/projects.py مسیرهای API برای پروژه‌ها را ارائه می‌دهند. backend/app/services/creator_engine.py و backend/app/services/creator_idea_to_prompt.py سرویس‌های مربوط به ایجاد پروژه را شامل می‌شوند. backend/app/services/oversight_service.py و backend/app/services/oversight_extraction.py سرویس‌های نظارت و استخراج را دارند. frontend/src/services/api.ts سرویس API فرانت را مدیریت می‌کند. backend/app/models/project.py مدل پروژه را تعریف می‌کند. backend/app/core/database.py دیتابیس را مدیریت می‌کند. backend/app/services/ai_manager.py مدیریت AI را بر عهده دارد. backend/app/services/notification_service.py سرویس اعلان‌ها را شامل می‌شود. backend/app/services/telegram_bot.py (در ساختار موجود نیست اما احتمالاً وجود دارد) بات تلگرام را مدیریت می‌کند. frontend/src/app/layout.tsx لایه اصلی فرانت است. frontend/src/components/Layout.tsx کامپوننت لایه را شامل می‌شود. frontend/src/hooks/useDebate.ts هوک مربوط به مناظره را دارد. frontend/src/lib/api.ts کتابخانه API را شامل می‌شود. frontend/src/types/index.ts تایپ‌های فرانت را تعریف می‌کند. backend/app/services/telegram_bot.py (در ساختار موجود نیست) بات تلگرام را مدیریت می‌کند. backend/app/services/telegram_handlers.py (در ساختار موجود نیست) هندلرهای تلگرام را شامل می‌شود. backend/app/services/telegram_ui.py (در ساختار موجود نیست) UI تلگرام را مدیریت می‌کند. backend/app/services/telegram_utils.py (در ساختار موجود نیست) ابزارهای تلگرام را شامل می‌شود. backend/app/services/telegram_views.py (در ساختار موجود نیست) ویوهای تلگرام را مدیریت می‌کند. backend/app/services/telegram_menus.py (در ساختار موجود نیست) منوهای تلگرام را شامل می‌شود. backend/app/services/telegram_keyboards.py (در ساختار موجود نیست) کیبوردهای تلگرام را مدیریت می‌کند. backend/app/services/telegram_messages.py (در ساختار موجود نیست) پیام‌های تلگرام را شامل می‌شود. backend/app/services/telegram_commands.py (در ساختار موجود نیست) دستورات تلگرام را مدیریت می‌کند. backend/app/services/telegram_handlers.py (در ساختار موجود نیست) هندلرهای تلگرام را شامل می‌شود. backend/app/services/telegram_ui.py (در ساختار موجود نیست) UI تلگرام را مدیریت می‌کند. backend/app/services/telegram_utils.py (در ساختار موجود نیست) ابزارهای تلگرام را شامل می‌شود. backend

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: توسعه منطق استخراج اطلاعات از پروژه‌های منتخب

**Scope:** پیاده‌سازی سرویسی که پس از ارسال درخواست، تمام فایل‌ها و زیرشاخه‌های پروژه‌های منتخب را اسکن کرده، منطق‌ها و ساختارهای مرتبط را استخراج و دسته‌بندی می‌کند. این مرحله شامل خواندن فایل‌ها، پارس کردن کد و استخراج متادیتا است.
**Key terms:** استخراج, فایل ها, زیر شاخه ها, منطق, دسته بندی, اسکن

**بخش مربوط از متن کاربر:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه سرویس استخراج اطلاعات از پروژه‌های منتخب (Extraction Service)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:152-180` — `run_analysis_stream`
  ```python
  for root
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه منطق استخراج اطلاعات از پروژه‌های منتخب را دارد. به‌طور مشخص، پس از ارسال درخواست، سرویسی باید تمام فایل‌ها و زیرشاخه‌های پروژه‌های منتخب را اسکن کرده، منطق‌ها و ساختارهای مرتبط را استخراج و دسته‌بندی کند. این مرحله شامل خواندن فایل‌ها، پارس کردن کد و استخراج متادیتا است. کاربر تأکید دارد: "مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه". کلیدواژه‌های اصلی: استخراج, فایل ها, زیر شاخه ها, منطق, دسته بندی, اسکن.

شواهد در کد واقعی: در فایل `backend/app/api/routes/analysis.py` (خطوط 152-180) یک منطق مشابه برای جمع‌آوری فایل‌ها با `os.walk` و فیلتر کردن دایرکتوری‌های غیرضروری (`node_modules`, `.git`, `__pycache__`, `venv`, `.venv`, `env`, `dist`, `build`, `.next`) و پسوندهای پشتیبانی‌شده (`.py`, `.js`, `.ts`, `.tsx`, `.jsx`, `.java`, `.go`, `.rs`, `.cpp`, `.c`, `.h`, `.hpp`, `.rb`, `.php`, `.swift`, `.kt`, `.scala`, `.vue`, `.svelte`, `.html`, `.css`, `.scss`) وجود دارد. همچنین در `backend/app/services/oversight_extraction.py` (که در deep context موجود نیست ولی در ساختار پروژه دیده می‌شود) احتمالاً منطق استخراج وجود دارد. کاربر نیاز به یک سرویس جدید یا توسعه سرویس موجود دارد که بتواند پروژه‌های منتخب (با شناسه project_id) را اسکن کرده و خروجی ساختاریافته از منطق‌ها و ساختارها ارائه دهد.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/project_extraction_service.py` که وظیفه اسکن پروژه‌های منتخب و استخراج اطلاعات ساختاریافته را بر عهده دارد.
2. این سرویس باید از منطق مشابه `analysis.py` (خطوط 152-180) برای جمع‌آوری فایل‌ها استفاده کند، اما با قابلیت دریافت لیست پروژه‌های منتخب (project_ids) و اسکن همزمان آن‌ها.
3. پیاده‌سازی متد `extract_project_logic(project_id: str, project_path: str) -> Dict` که:
   - فایل‌ها را با `os.walk` اسکن کند
   - محتوای فایل‌ها را بخواند
   - متادیتا (اندازه، نوع، تاریخ) را استخراج کند
   - منطق‌های اصلی (کلاس‌ها، توابع، importها) را با regex یا AST پارس کند
   - خروجی را به صورت دسته‌بندی‌شده (بر اساس نوع فایل، دایرکتوری، وابستگی) برگرداند
4. ایجاد endpoint جدید در `backend/app/api/routes/analysis.py` با مسیر `/analysis/extract` که درخواست استخراج را دریافت کرده و نتیجه را برگرداند.
5. استفاده از `BackgroundTasks` برای اجرای طولانی‌مدت و بازگرداندن سریع response.
6. اضافه کردن مدل Pydantic جدید برای درخواست و پاسخ استخراج.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: تولید پرامپت نهایی با تلفیق اطلاعات پروژه‌های منتخب و پروژه فعلی

**Scope:** ایجاد موتور پرامپت‌سازی که اطلاعات استخراج شده از پروژه‌های منتخب را با ساختار و شرایط پروژه فعلی ترکیب کرده و یک پرامپت نهایی منسجم و متناسب با تسک تولید می‌کند. این مرحله شامل منطق تلفیق و اولویت‌بندی است.
**Key terms:** پرامپت نهایی, پروژه فعلی, تلفیق, اطلاعات استخراج شده, موتور پرامپت

**بخش مربوط از متن کاربر:**
```
در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی و پیاده‌سازی موتور پرامپت‌سازی نهایی با تلفیق اطلاعات پروژه‌های منتخب و پروژه فعلی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک موتور پرامپت‌سازی (Prompt Engine) را دارد که اطلاعات استخراج‌شده از پروژه‌های منتخب (که قبلاً در مراحل قبلی Oversight Extraction پردازش شده‌اند) را با ساختار و شرایط پروژه فعلی (همان پروژه‌ای که کاربر برای آن تسک نوشته) ترکیب کرده و یک پرامپت نهایی منسجم و متناسب با تسک تولید کند. کلیدواژه‌های اصلی کاربر: «پرامپت نهایی», «پروژه فعلی», «تلفیق», «اطلاعات استخراج شده», «موتور پرامپت». کاربر تأکید دارد: «در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه». این یعنی منطق تلفیق باید اولویت را به شرایط پروژه فعلی بدهد اما تمام قابلیت‌ها و منطق‌های استخراج‌شده از پروژه‌های منتخب را نیز در نظر بگیرد. در کد فعلی پروژه، سرویس‌های مرتبط با Oversight Extraction در فایل‌های `backend/app/services/oversight_extraction.py` و `backend/app/services/oversight_strong_prompt.py` و `backend/app/services/oversight_verifier.py` وجود دارند. همچنین فایل `backend/app/services/creator_idea_to_prompt.py` برای تبدیل ایده به پرامپت اولیه استفاده می‌شود. اما هیچ سرویس مشخصی برای تلفیق اطلاعات چند پروژه و تولید پرامپت نهایی وجود ندارد. مسیر API مرتبط با Oversight در `backend/app/api/routes/oversight.py` (خطوط 1-350) تعریف شده است. همچنین فایل `backend/app/services/oversight_inspector_bridge.py` برای ارتباط با Inspector Bridge استفاده می‌شود. برای پیاده‌سازی این موتور، باید یک سرویس جدید به نام `PromptFusionService` ایجاد شود که از `DeepAnalysisService` (در `backend/app/services/deep_analysis_service.py`) و `ai_manager` (در `backend/app/services/ai_manager.py`) برای تولید پرامپت نهایی استفاده کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `backend/app/services/prompt_fusion_service.py` شامل کلاس `PromptFusionService` با متد `generate_final_prompt(project_id: str, selected_project_ids: List[str], task_description: str, db_session: Session) -> str`. 2. این سرویس باید: (الف) اطلاعات استخراج‌شده از پروژه‌های منتخب را از دیتابیس (جدول `InspectorSession` یا `AnalysisReport`) بخواند. (ب) ساختار و شرایط پروژه فعلی را از `ProjectService` (در `backend/app/services/project_service.py`) دریافت کند. (ج) با استفاده از `ai_manager` (خط 113 در `analysis.py`) یک پرامپت نهایی با رعایت اولویت پروژه فعلی و تلفیق منطق‌های استخراج‌شده تولید کند. 3. اضافه کردن endpoint جدید در `backend/app/api/routes/oversight.py` با مسیر `POST /oversight/fuse-prompt` که `PromptFusionRequest` (شامل `project_id`, `selected_project_ids: List[str]`, `task_description: str`) را دریافت کرده و پرامپت نهایی را برمی‌گرداند. 4. به‌روزرسانی `backend/app/services/oversight_extraction.py` برای ذخیره‌سازی ساختاریافته‌تر اطلاعات استخراج‌شده (مانند `extracted_logic`, `capability_patterns`, `priority_rules`) در دیتابیس. 5. اضافه کردن تست‌های واحد در `backend/tests/test_prompt_fusion.py`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: یکپارچه‌سازی با APIهای تلگرام و فرانت برای ارسال و دریافت

**Scope:** اتصال منطق استخراج و تولید پرامپت به APIهای موجود در تلگرام و فرانت. این مرحله شامل endpointهای جدید برای دریافت پروژه‌های منتخب، ارسال درخواست و بازگرداندن پرامپت نهایی است.
**Key terms:** API, endpoint, تلگرام, فرانت, ارسال, دریافت

**بخش مربوط از متن کاربر:**
```
چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی APIهای تلگرام و فرانت برای ارسال و دریافت پروژه‌های منتخب و پرامپت نهایی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی با APIهای تلگرام و فرانت برای ارسال و دریافت داده‌ها را دارد. این درخواست شامل اتصال منطق استخراج و تولید پرامپت به APIهای موجود در تلگرام و فرانت است. کاربر به صراحت به 'endpointهای جدید برای دریافت پروژه‌های منتخب، ارسال درخواست و بازگرداندن پرامپت نهایی' اشاره کرده است. همچنین بخش مربوط از درخواست اصلی کاربر: 'چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام'. کلیدواژه‌های اصلی: API, endpoint, تلگرام, فرانت, ارسال, دریافت. در کد فعلی پروژه، endpointهای متعددی برای مدیریت پروژه‌ها و تعامل با AI وجود دارد. فایل `backend/app/api/routes/creator.py` (deep-read نشده) احتمالاً منطق موتور خالق را مدیریت می‌کند. فایل `backend/app/api/routes/github_import.py` (deep-read شده) شامل endpointهای `/github/import` (خط 94) و `/github/check` (خط 55) است که الگوی مناسبی برای ساخت endpointهای جدید ارائه می‌دهد. فایل `backend/app/api/routes/analysis.py` (deep-read شده) شامل endpointهای `/analysis/run` (خط 46) و `/analysis/run-stream` (خط 83) است که از StreamingResponse برای بازگرداندن نتایج استفاده می‌کند. فایل `backend/app/api/routes/models.py` (deep-read شده) شامل endpointهای `/models` (خط 78) و `/models/providers` (خط 162) است. فایل `backend/app/api/routes/orchestrator.py` (deep-read شده) شامل endpointهای `/orchestrator` (خط 15) است. فایل `backend/app/services/creator_engine.py` و `backend/app/services/creator_idea_to_prompt.py` (deep-read نشده) احتمالاً منطق اصلی تولید پرامپت را دارند. فایل `frontend/src/app/creator/page.tsx` (deep-read شده) صفحه فرانت creator را نشان می‌دهد. فایل `frontend/src/services/api.ts` (deep-read نشده) سرویس API فرانت را مدیریت می‌کند. فایل `frontend/src/lib/api.ts` (deep-read نشده) توابع API را دارد. برای تلگرام، فایل‌های مرتبط در backend وجود دارد اما deep-read نشده‌اند. نیاز به endpointهای جدید برای دریافت پروژه‌های منتخب از تلگرام/فرانت، ارسال درخواست به موتور خالق و بازگرداندن پرامپت نهایی است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد endpoint جدید در `backend/app/api/routes/creator.py` با مسیر `/creator/generate-prompt` که یک `PromptGenerationRequest` شامل `project_id` (از پروژه‌های منتخب)، `user_prompt` (درخواست کاربر)، `source` (تلگرام یا فرانت) و `options` (تنظیمات اضافی) دریافت کند. 2. ایجاد سرویس جدید `backend/app/services/prompt_generation_service.py` که منطق استخراج و تولید پرامپت را از `creator_engine.py` و `creator_idea_to_prompt.py` فراخوانی کند. 3. ایجاد endpoint جدید در `backend/app/api/routes/creator.py` با مسیر `/creator/selected-projects` که لیست پروژه‌های منتخب را از تلگرام/فرانت دریافت و ذخیره کند. 4. به‌روزرسانی `frontend/src/app/creator/page.tsx` برای ارسال درخواست به endpoint جدید و نمایش پرامپت نهایی. 5. به‌روزرسانی `frontend/src/services/api.ts` برای اضافه کردن توابع `generatePrompt` و `submitSelectedProjects`. 6. ایجاد webhook handler در backend برای تلگرام (احتمالاً در `backend/app/services/telegram_service.py` یا فایل مشابه) که پیام‌های تلگرام را دریافت و به endpoint جدید هدایت کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: نوشتن تست‌های واحد و یکپارچه‌سازی برای کل جریان

**Scope:** نوشتن تست برای مدل‌ها، UI، منطق استخراج، موتور پرامپت و APIها. تست‌ها باید سناریوهای مختلف انتخاب پروژه و استخراج اطلاعات را پوشش دهند.
**Key terms:** تست, تست واحد, تست یکپارچه, سناریو

**بخش مربوط از متن کاربر:**
```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد و یکپارچه‌سازی برای کل جریان تسک‌ها (تلگرام و فرانت) و ایجاد پروژه از موتور خالق

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد و یکپارچه‌سازی برای کل جریان تسک‌ها، چه در تلگرام و چه در خود فرانت، و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام را دارد. کلیدواژه‌های اصلی: تست, تست واحد, تست یکپارچه, سناریو. این درخواست شامل پوشش سناریوهای مختلف انتخاب پروژه و استخراج اطلاعات است. در کد واقعی پروژه، فایل‌های متعددی برای مدیریت تسک‌ها و ایجاد پروژه وجود دارند:
- backend/app/api/routes/creator.py: مسیرهای API برای موتور خالق (ایجاد پروژه جدید)
- backend/app/services/creator_engine.py: سرویس اصلی موتور خالق
- backend/app/services/creator_idea_to_prompt.py: تبدیل ایده به پرامپت
- backend/app/services/simple_creator.py: ایجاد پروژه ساده
- frontend/src/app/creator/page.tsx: صفحه فرانت برای ایجاد پروژه
- backend/app/api/routes/simple_projects.py: مسیرهای API برای پروژه‌های ساده
- backend/app/services/task_merge_service.py: سرویس ادغام تسک‌ها
- backend/app/services/quick_approval_service.py: سرویس تأیید سریع
- backend/app/services/oversight_service.py: سرویس نظارت (شامل auto_register_watched)
- backend/app/api/routes/github_import.py: خطوط 132-181 (راه‌اندازی خودکار حافظه و auto-register به watched)
- backend/app/services/project_auto_setup.py: راه‌اندازی خودکار پروژه
- backend/app/services/ai_manager.py: مدیریت مرکزی AI
- backend/app/services/ai_base.py: پایه سرویس‌های AI
- backend/app/services/db_service.py: سرویس دیتابیس
- backend/app/models/project.py: مدل پروژه
- backend/app/models/ai_log.py: مدل لاگ AI
- backend/app/models/debate.py: مدل مناظره
- backend/app/models/setting.py: مدل تنظیمات
- frontend/src/services/api.ts: سرویس API فرانت
- frontend/src/lib/api.ts: کتابخانه API
- frontend/src/hooks/useDebate.ts: هوک مناظره
- frontend/src/types/index.ts: تایپ‌های فرانت
- backend/app/core/database.py: هسته دیتابیس
- backend/app/core/config.py: تنظیمات
- backend/app/core/models_registry.py: رجیستری مدل‌ها
- backend/requirements.txt: وابستگی‌های پایتون
- frontend/package.json: وابستگی‌های فرانت

تست‌ها باید سناریوهای مختلف انتخاب پروژه و استخراج اطلاعات را پوشش دهند. کاربر به طور خاص به "تسک‌ها" اشاره دارد که شامل جریان‌های تلگرام و فرانت می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست backend/tests/test_creator_engine.py برای تست creator_engine.py با mock کردن ai_manager و db_session. 2. ایجاد backend/tests/test_simple_creator.py برای تست simple_creator.py با سناریوهای مختلف ورودی. 3. ایجاد backend/tests/test_task_merge_service.py برای تست task_merge_service.py با mock کردن دیتابیس. 4. ایجاد backend/tests/test_quick_approval_service.py برای تست quick_approval_service.py. 5. ایجاد backend/tests/test_oversight_service.py برای تست oversight_service.py (به خصوص auto_register_watched). 6. ایجاد backend/tests/test_project_auto_setup.py برای تست auto_setup_project_memory. 7. ایجاد backend/tests/test_github_import.py برای تست import_repository و auto_setup. 8. ایجاد backend/tests/test_ai_manager.py برای تست get_available_models و fallback. 9. ایجاد backend/tests/test_db_service.py برای تست عملیات CRUD. 10. ایجاد frontend/__tests__/api.test.ts برای تست سرویس API فرانت با mock

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: static)_

## Task Steps

### Step 1: طراحی ساختار داده و مدل برای پروژه‌های منتخب و ارجاع به آن‌ها در تسک‌ها
**Status:** `pending` (0%)
**Scope:** ایجاد مدل‌های دیتابیس یا ساختارهای داده برای ذخیره لیست پروژه‌های لود شده (گیت‌هاب/محلی) و امکان انتخاب چند پروژه در زمان ایجاد تسک. این مرحله شامل منطق فرانت و بک‌اند برای ذخیره و بازیابی پروژه‌های منتخب است.
**Excerpt:**
```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام، قسمتی باشه که بتونم وقتی بهش در پرامپت خام میگم مثلا یه بخش از برنامه یا فلان قسمت برنامم میخوام مثل فلان قسمت از اون یه پروژه باشه (پروژه باید از قبل تو این سیستم لود شده باشه و جزو پروژه های گیت هاب یا محلی باشه که در این پروژه فعلی لود کردم و توکن و رندر هم موجوده که بهش دسترسی داشته باشم)  بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم
```

### Step 2: اضافه کردن UI انتخاب پروژه در فرانت و تلگرام
**Status:** `pending` (0%)
**Scope:** پیاده‌سازی کامپوننت‌های UI در فرانت و بات تلگرام برای نمایش لیست پروژه‌های لود شده و امکان تیک زدن چند پروژه در زمان ایجاد تسک یا پروژه جدید. این مرحله فقط UI است و منطق بک‌اند را شامل نمی‌شود.
**Excerpt:**
```
بتونم از طریق لیست در زمان نوشتن تسک پروژه و پروژه های منتخب رو تیک بزنم تا وقتی در پرامپت خام توضیحات مینویسم که میخوام چی مثل چی باشه، در زمان که درخواست رو میفرستم و پروژه ها هم انتخاب شدن
```

### Step 3: توسعه منطق استخراج اطلاعات از پروژه‌های منتخب
**Status:** `pending` (0%)
**Scope:** پیاده‌سازی سرویسی که پس از ارسال درخواست، تمام فایل‌ها و زیرشاخه‌های پروژه‌های منتخب را اسکن کرده، منطق‌ها و ساختارهای مرتبط را استخراج و دسته‌بندی می‌کند. این مرحله شامل خواندن فایل‌ها، پارس کردن کد و استخراج متادیتا است.
**Excerpt:**
```
مدل باید با دقت تمام به اون پروژه ها مراجعه کنه و اطلاعات لازمی که برا پیاده سازی تسکم نیاز هست رو از تمام فایل ها و زیر شاخه های مربوطه اش استراج کنه و تمام منطق مورد نیاز برای پیاده سازی تسک رو استخراج کنه و دسته بندی کنه
```

### Step 4: تولید پرامپت نهایی با تلفیق اطلاعات پروژه‌های منتخب و پروژه فعلی
**Status:** `pending` (0%)
**Scope:** ایجاد موتور پرامپت‌سازی که اطلاعات استخراج شده از پروژه‌های منتخب را با ساختار و شرایط پروژه فعلی ترکیب کرده و یک پرامپت نهایی منسجم و متناسب با تسک تولید می‌کند. این مرحله شامل منطق تلفیق و اولویت‌بندی است.
**Excerpt:**
```
در نهایت پرامپت نهایی باید حسب همه اطلاعات استخراج شده از اون پروژه ها و همینطور پروژه فعلی که براش تسک نوشتم باشه و موقع تبدیلش به پرامپت نهایی تمام منطق های استخراج شده از اون قابلیت پروژه های انتخاب شده باید و حتما بر اساس پروژه ای که براش تسک نوشتم و حسب شرایطش نوشته و بشه
```

### Step 5: یکپارچه‌سازی با APIهای تلگرام و فرانت برای ارسال و دریافت
**Status:** `pending` (0%)
**Scope:** اتصال منطق استخراج و تولید پرامپت به APIهای موجود در تلگرام و فرانت. این مرحله شامل endpointهای جدید برای دریافت پروژه‌های منتخب، ارسال درخواست و بازگرداندن پرامپت نهایی است.
**Excerpt:**
```
چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام
```

### Step 6: نوشتن تست‌های واحد و یکپارچه‌سازی برای کل جریان
**Status:** `pending` (0%)
**Scope:** نوشتن تست برای مدل‌ها، UI، منطق استخراج، موتور پرامپت و APIها. تست‌ها باید سناریوهای مختلف انتخاب پروژه و استخراج اطلاعات را پوشش دهند.
**Excerpt:**
```
میخوام برای قسمت تسک ها، چه در تلگرام و چه در خود فرانت , و همینطور ایجاد پروژه جدید از موتور خالق چه از طریق فرانت و چه از طریق تلگرام
```
