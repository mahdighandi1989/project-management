---
task_id: 0dc65e56-af37-45dc-8d6c-90560f7cacb4
title: '[Effectiveness] عدم پایش کیفیت نوتیفیکیشن‌های تلگرام'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T10:34:16.778479+00:00'
updated_at: '2026-05-29T20:13:37.381686+00:00'
---

# [Effectiveness] عدم پایش کیفیت نوتیفیکیشن‌های تلگرام

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
پس از پیاده‌سازی، 98% از نوتیفیکیشن‌های ارسالی باید در کمتر از 1 ثانیه به کاربر تحویل داده شوند و نرخ کلیک روی inline keyboards حداقل 40% باشد

## 📊 وضعیت فعلی
outcome data هیچ معیاری برای delivery rate، latency تحویل، یا engagement کاربران با نوتیفیکیشن‌ها ندارد. سیستم ممکن است نوتیفیکیشن‌ها را با تأخیر ارسال کند یا کاربران با آن‌ها تعامل نداشته باشند بدون اینکه خطایی ثبت شود.

## 🛠 اقدام پیشنهادی
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] عدم پایش کیفیت نوتیفیکیشن‌های تلگرام

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
پس از پیاده‌سازی، 98% از نوتیفیکیشن‌های ارسالی باید در کمتر از 1 ثانیه به کاربر تحویل داده شوند و نرخ کلیک روی inline keyboards حداقل 40% باشد

## 📊 وضعیت فعلی
outcome data هیچ معیاری برای delivery rate، latency تحویل، یا engagement کاربران با نوتیفیکیشن‌ها ندارد. سیستم ممکن است نوتیفیکیشن‌ها را با تأخیر ارسال کند یا کاربران با آن‌ها تعامل نداشته باشند بدون اینکه خطایی ثبت شود.

## 🛠 اقدام پیشنهادی
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: static)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: static)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: static)_

## Task Steps

### Step 1: اضافه کردن مدل‌های داده برای metrics نوتیفیکیشن
**Status:** `not_done` (0%)
**Scope:** ایجاد dataclass یا Pydantic model برای ذخیره metrics مربوط به delivery rate، latency تحویل، و click-through rate روی inline keyboards. این مدل‌ها باید شامل فیلدهای timestamp، notification_id، delivery_status، latency_ms، و click_status باشند. خارج از این مرحله: پیاده‌سازی منطق جمع‌آوری metrics، ذخیره‌سازی در دیتابیس، یا نمایش UI. نکته حیاتی: مدل‌ها باید با ساختار outcome data موجود هماهنگ باشند و قابلیت سریال‌سازی به JSON را داشته باشند.
**Excerpt:**
```
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data
```

### Step 2: اضافه کردن endpoint برای ثبت delivery status نوتیفیکیشن
**Status:** `not_done` (0%)
**Scope:** ایجاد یک API endpoint جدید (مثلاً POST /api/notifications/delivery-status) که اطلاعات delivery rate و latency تحویل را از سرویس تلگرام دریافت کرده و در outcome data ذخیره کند. این endpoint باید timestamp، notification_id، و latency_ms را دریافت کند. خارج از این مرحله: پیاده‌سازی سمت تلگرام برای ارسال این اطلاعات، UI نمایش metrics. نکته حیاتی: endpoint باید احراز هویت داشته باشد و فقط از سمت trusted sources قابل دسترسی باشد.
**Excerpt:**
```
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data
```

### Step 3: اضافه کردن endpoint برای ثبت click-through rate روی inline keyboards
**Status:** `not_done` (0%)
**Scope:** ایجاد یک API endpoint جدید (مثلاً POST /api/notifications/click-status) که اطلاعات کلیک روی inline keyboards را از سرویس تلگرام دریافت کرده و در outcome data ذخیره کند. این endpoint باید notification_id، button_id، و timestamp را دریافت کند. خارج از این مرحله: پیاده‌سازی سمت تلگرام برای ارسال callback data، UI نمایش metrics. نکته حیاتی: باید با callback data از تلگرام هماهنگ باشد و از CSRF protection استفاده کند.
**Excerpt:**
```
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data
```

### Step 4: اضافه کردن منطق جمع‌آوری metrics در سرویس تلگرام (سمت backend)
**Status:** `not_done` (0%)
**Scope:** در سرویس ارسال نوتیفیکیشن تلگرام (احتمالاً در فایل‌های مربوط به TelegramService یا NotificationService)، منطق اندازه‌گیری latency تحویل (زمان بین ارسال و دریافت delivery confirmation) و ثبت delivery status را اضافه کنید. همچنین برای inline keyboards، منطق ثبت callback data و تبدیل آن به click metric را پیاده‌سازی کنید. خارج از این مرحله: تغییر در UI یا frontend. نکته حیاتی: باید از timing دقیق استفاده شود و خطاهای network در نظر گرفته شوند.
**Excerpt:**
```
سیستم ممکن است نوتیفیکیشن‌ها را با تأخیر ارسال کند یا کاربران با آن‌ها تعامل نداشته باشند بدون اینکه خطایی ثبت شود.
```

### Step 5: اضافه کردن دیتابیس یا storage برای ذخیره metrics نوتیفیکیشن
**Status:** `not_done` (0%)
**Scope:** ایجاد جدول یا collection جدید در دیتابیس (بسته به stack: PostgreSQL table یا MongoDB collection) برای ذخیره metrics نوتیفیکیشن شامل delivery_rate، latency_ms، click_through_rate، و timestamp. همچنین migration scripts برای ایجاد این ساختار. خارج از این مرحله: پیاده‌سازی queryهای تحلیلی یا aggregation. نکته حیاتی: باید با مدل‌های داده مرحله 1 هماهنگ باشد و indexing مناسب برای queryهای زمانی داشته باشد.
**Excerpt:**
```
اضافه کردن metrics برای delivery rate، latency تحویل، و click-through rate روی inline keyboards به outcome data
```

### Step 6: اضافه کردن endpoint برای query metrics نوتیفیکیشن (API خواندن)
**Status:** `not_done` (0%)
**Scope:** ایجاد یک API endpoint خواندنی (مثلاً GET /api/notifications/metrics) که امکان query metrics ذخیره‌شده را با پارامترهای time range، notification_id، و aggregation level فراهم کند. این endpoint باید delivery rate، average latency، و click-through rate را برگرداند. خارج از این مرحله: UI نمایش metrics، real-time dashboard. نکته حیاتی: باید از pagination و caching استفاده کند تا performance بالا باشد.
**Excerpt:**
```
پس از پیاده‌سازی، 98% از نوتیفیکیشن‌های ارسالی باید در کمتر از 1 ثانیه به کاربر تحویل داده شوند و نرخ کلیک روی inline keyboards حداقل 40% باشد
```

### Step 7: اضافه کردن UI dashboard برای نمایش metrics نوتیفیکیشن (سمت frontend)
**Status:** `not_done` (0%)
**Scope:** در بخش frontend (Next.js)، یک صفحه یا کامپوننت جدید برای نمایش metrics نوتیفیکیشن اضافه کنید. این dashboard باید delivery rate، average latency، و click-through rate را به‌صورت نمودارهای زمانی (line chart) و کارت‌های summary نمایش دهد. خارج از این مرحله: real-time updates، alerting. نکته حیاتی: باید responsive باشد و با دیتای واقعی از endpoint مرحله 6 کار کند.
**Excerpt:**
```
پس از پیاده‌سازی، 98% از نوتیفیکیشن‌های ارسالی باید در کمتر از 1 ثانیه به کاربر تحویل داده شوند و نرخ کلیک روی inline keyboards حداقل 40% باشد
```

### Step 8: اضافه کردن E2E test برای اندازه‌گیری outcome metrics
**Status:** `not_done` (0%)
**Scope:** نوشتن یک تست E2E (با ابزارهایی مثل Playwright یا Cypress) که یک نوتیفیکیشن تلگرام ارسال می‌کند، delivery status و click را شبیه‌سازی می‌کند، و سپس از endpoint metrics query می‌کند تا مطمئن شود delivery rate و latency در محدوده acceptable هستند. خارج از این مرحله: unit tests برای توابع داخلی. نکته حیاتی: تست باید مستقل از سرویس واقعی تلگرام باشد و از mock/stub استفاده کند.
**Excerpt:**
```
test E2E که outcome را اندازه می‌گیرد عبور می‌کند
```

### Step 9: اضافه کردن unit tests برای مدل‌ها و منطق metrics
**Status:** `not_done` (0%)
**Scope:** نوشتن unit tests برای مدل‌های داده metrics (مرحله 1)، منطق جمع‌آوری metrics در سرویس تلگرام (مرحله 4)، و endpointهای ثبت و query metrics (مراحل 2، 3، 6). تست‌ها باید validation، edge cases، و error handling را پوشش دهند. خارج از این مرحله: integration tests با دیتابیس واقعی. نکته حیاتی: تست‌ها باید سریع و بدون وابستگی خارجی باشند.
**Excerpt:**
```
هیچ تستی fail نمی‌شود (`npm run te
```

### Step 10: اضافه کردن logging و alerting برای metrics خارج از محدوده
**Status:** `not_done` (0%)
**Scope:** اضافه کردن logging ساختاریافته برای metrics نوتیفیکیشن در production. همچنین پیاده‌سازی alerting ساده (مثلاً از طریق email یا Slack webhook) زمانی که delivery rate کمتر از 98% یا average latency بیشتر از 1 ثانیه باشد. خارج از این مرحله: پیاده‌سازی dashboard real-time. نکته حیاتی: alerting باید threshold قابل تنظیم داشته باشد و از spam protection استفاده کند.
**Excerpt:**
```
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```
