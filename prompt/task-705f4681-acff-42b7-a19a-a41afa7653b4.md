---
task_id: 705f4681-acff-42b7-a19a-a41afa7653b4
title: '[منطق] عدم مدیریت empty result در streaming خروجی ai_base.py'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-17T07:18:45.432499+00:00'
updated_at: '2026-05-29T20:16:45.547601+00:00'
---

# [منطق] عدم مدیریت empty result در streaming خروجی ai_base.py

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

خروجی streaming به صورت `AsyncIterator[str]` تعریف شده است، اما مشخص نیست که اگر مدل هیچ توکنی تولید نکند (مثلاً empty response)، iterator خالی برگردانده می‌شود یا خطا. همچنین در حالت غیر-streaming، `content` می‌تواند خالی باشد و `finish_reason` ممکن است 'stop' نباشد.

## 💥 پیامد (impact)
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند.

## 🛠 پیشنهاد رفع اولیه
در مستندات و کد مشخص کنید که iterator خالی مجاز است و در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-17T08:08:35.554463+00:00]
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در متد streaming، اگر مدل هیچ خروجی تولید نکند (empty result)، ممکن است iterator خالی برگرداند. اما در expected_outputs اشاره‌ای به handling این حالت نشده است. همچنین در صورت خطا در حین streaming، نحوه بازگشت خطا مشخص نیست.

## 💥 پیا

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم مدیریت empty result در streaming خروجی ai_base.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

خروجی streaming به صورت `AsyncIterator[str]` تعریف شده است، اما مشخص نیست که اگر مدل هیچ توکنی تولید نکند (مثلاً empty response)، iterator خالی برگردانده می‌شود یا خطا. همچنین در حالت غیر-streaming، `content` می‌تواند خالی باشد و `finish_reason` ممکن است 'stop' نباشد.

## 💥 پیامد (impact)
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند.

## 🛠 پیشنهاد رفع اولیه
در مستندات و کد مشخص کنید که iterator خالی مجاز است و در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی فرض‌های فعلی در pipeline data برای streaming و non-streaming
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن تمام فایل‌های مرتبط با pipeline data (به خصوص ai_base.py و هر فایلی که AsyncIterator[str] را تعریف یا استفاده می‌کند) است. هدف شناسایی و مستندسازی فرض‌های فعلی کد درباره empty response در هر دو حالت streaming و non-streaming است. این مرحله شامل تغییر کد نمی‌شود، فقط تحلیل و مستندسازی است. نکته حیاتی: باید تمام نقاطی که خروجی streaming مصرف می‌شود (caller services) نیز بررسی شوند تا فرض‌های آن‌ها هم مشخص شود.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: خروجی streaming به صورت `AsyncIterator[str]` تعریف شده است، اما مشخص نیست که اگر مدل هیچ توکنی تولید نکند (مثلاً empty response)، iterator خالی برگردانده می‌شود یا خطا. همچنین در حالت غیر-streaming، `content` می‌تواند خالی باشد و `finish_reason` ممکن است 'stop' نباشد. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 2: تعیین ground truth برای رفتار empty response در streaming
**Status:** `not_done` (0%)
**Scope:** بر اساس تحلیل مرحله قبل، تصمیم‌گیری کنید که رفتار صحیح (ground truth) برای empty response در حالت streaming چیست: آیا iterator خالی مجاز است یا باید خطا برگرداند؟ این تصمیم باید با توجه به نیاز caller services و best practices گرفته شود. نتیجه این مرحله یک تصمیم مستند است که مبنای تغییرات بعدی خواهد بود. نکته حیاتی: این تصمیم باید با stakeholders (اگر موجود باشند) یا بر اساس منطق قوی گرفته شود.
**Excerpt:**
```
در مستندات و کد مشخص کنید که iterator خالی مجاز است و در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود. ground truth تعیین شد و طرف دیگر align شد.
```

### Step 3: تعیین ground truth برای رفتار empty response در non-streaming
**Status:** `not_done` (0%)
**Scope:** بر اساس تحلیل مرحله 1، تصمیم‌گیری کنید که رفتار صحیح برای empty response در حالت non-streaming چیست: آیا content خالی با finish_reason='empty' برگردانده شود یا رفتار دیگری. این تصمیم باید با ground truth streaming هماهنگ باشد. نکته حیاتی: finish_reason='empty' یک مقدار جدید است که باید در سیستم تعریف شود.
**Excerpt:**
```
در مستندات و کد مشخص کنید که iterator خالی مجاز است و در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود. ground truth تعیین شد و طرف دیگر align شد.
```

### Step 4: اضافه کردن finish_reason='empty' به enum یا type definitions موجود
**Status:** `not_done` (0%)
**Scope:** مقدار جدید 'empty' را به هر enum یا type definition که finish_reason را تعریف می‌کند اضافه کنید. این ممکن است شامل مدل‌های Pydantic، dataclassها، یا type aliases باشد. نکته حیاتی: باید تمام مکان‌هایی که finish_reason تعریف شده پیدا شود و مقدار جدید به همه اضافه شود. این مرحله شامل تغییر logic مصرف‌کننده نیست.
**Excerpt:**
```
در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود.
```

### Step 5: اصلاح تولیدکننده streaming برای برگرداندن iterator خالی در صورت empty response
**Status:** `not_done` (0%)
**Scope:** در کدی که خروجی streaming را تولید می‌کند (احتمالاً در ai_base.py یا فایل‌های مرتبط)، logic را طوری تغییر دهید که اگر مدل هیچ توکنی تولید نکرد، یک AsyncIterator خالی (بدون هیچ آیتم) برگرداند، نه اینکه خطا بدهد یا منتظر بماند. نکته حیاتی: باید مطمئن شوید که iterator خالی به درستی بسته می‌شود و منبعی leak نمی‌کند.
**Excerpt:**
```
در مستندات و کد مشخص کنید که iterator خالی مجاز است. خروجی streaming به صورت AsyncIterator[str] تعریف شده است، اما مشخص نیست که اگر مدل هیچ توکنی تولید نکند (مثلاً empty response)، iterator خالی برگردانده می‌شود یا خطا.
```

### Step 6: اصلاح مصرف‌کننده streaming برای handle کردن iterator خالی
**Status:** `not_done` (0%)
**Scope:** تمام مکان‌هایی که AsyncIterator[str] مصرف می‌شود (caller services) را پیدا کنید و logic را طوری تغییر دهید که iterator خالی را به درستی handle کنند: بدون خطا، بدون انتظار بی‌مورد، و با رفتار مناسب (مثلاً برگرداندن پاسخ خالی به کاربر نهایی). نکته حیاتی: این مرحله وابسته به مرحله 5 است و باید بعد از آن انجام شود.
**Excerpt:**
```
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند.
```

### Step 7: اصلاح تولیدکننده non-streaming برای برگرداندن content خالی با finish_reason='empty'
**Status:** `not_done` (0%)
**Scope:** در کدی که خروجی non-streaming را تولید می‌کند، logic را طوری تغییر دهید که اگر مدل هیچ محتوایی تولید نکرد، یک پاسخ با content='' و finish_reason='empty' برگرداند. نکته حیاتی: باید مطمئن شوید که finish_reason='empty' در type system تعریف شده است (مرحله 4).
**Excerpt:**
```
در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود.
```

### Step 8: اصلاح مصرف‌کننده non-streaming برای handle کردن content خالی با finish_reason='empty'
**Status:** `not_done` (0%)
**Scope:** تمام مکان‌هایی که پاسخ non-streaming مصرف می‌شود را پیدا کنید و logic را طوری تغییر دهید که content خالی با finish_reason='empty' را به درستی handle کنند: بدون خطا، و با رفتار مناسب (مثلاً نمایش پیام 'no response' به کاربر). نکته حیاتی: این مرحله وابسته به مرحله 7 است.
**Excerpt:**
```
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند. (این مورد برای non-streaming هم صادق است)
```

### Step 9: به‌روزرسانی مستندات API برای رفتار empty response
**Status:** `not_done` (0%)
**Scope:** مستندات API (مانند docstring توابع، فایل‌های OpenAPI/Swagger، یا مستندات جداگانه) را به‌روزرسانی کنید تا رفتار empty response را در هر دو حالت streaming و non-streaming به وضوح توضیح دهد. نکته حیاتی: مستندات باید دقیقاً منعکس‌کننده ground truth تعیین شده در مراحل 2 و 3 باشد.
**Excerpt:**
```
در مستندات و کد مشخص کنید که iterator خالی مجاز است و در non-streaming حالت، content خالی با finish_reason='empty' برگردانده شود.
```

### Step 10: نوشتن تست‌های واحد برای تولیدکننده streaming با empty response
**Status:** `not_done` (0%)
**Scope:** تست‌های واحد بنویسید که سناریوی empty response را برای تولیدکننده streaming پوشش دهند: بررسی کنید که iterator خالی برگردانده می‌شود، نه خطا. نکته حیاتی: تست‌ها باید مستقل از مصرف‌کننده باشند و فقط تولیدکننده را test کنند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند. (نیاز به تست‌های خاص برای این سناریو)
```

### Step 11: نوشتن تست‌های واحد برای تولیدکننده non-streaming با empty response
**Status:** `not_done` (0%)
**Scope:** تست‌های واحد بنویسید که سناریوی empty response را برای تولیدکننده non-streaming پوشش دهند: بررسی کنید که پاسخ شامل content='' و finish_reason='empty' است. نکته حیاتی: تست‌ها باید مستقل از مصرف‌کننده باشند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند. (نیاز به تست‌های خاص برای این سناریو)
```

### Step 12: نوشتن تست‌های integration برای سناریوی empty response در streaming
**Status:** `not_done` (0%)
**Scope:** تست‌های integration بنویسید که کل pipeline را از تولیدکننده تا مصرف‌کننده در سناریوی empty response streaming test کنند. بررسی کنید که هیچ crash یا hang رخ نمی‌دهد و رفتار نهایی مناسب است. نکته حیاتی: این تست‌ها باید real یا mock نزدیک به real را استفاده کنند.
**Excerpt:**
```
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 13: نوشتن تست‌های integration برای سناریوی empty response در non-streaming
**Status:** `not_done` (0%)
**Scope:** تست‌های integration بنویسید که کل pipeline را از تولیدکننده تا مصرف‌کننده در سناریوی empty response non-streaming test کنند. بررسی کنید که هیچ crash رخ نمی‌دهد و رفتار نهایی مناسب است. نکته حیاتی: این تست‌ها باید real یا mock نزدیک به real را استفاده کنند.
**Excerpt:**
```
کالر سرویس ممکن است منتظر داده بماند یا crash کند اگر empty iterator را handle نکند. (این مورد برای non-streaming هم صادق است)
```

### Step 14: بررسی نهایی و commit نهایی با checklist
**Status:** `not_done` (0%)
**Scope:** یک بررسی نهایی انجام دهید تا مطمئن شوید تمام مراحل به درستی پیاده‌سازی شده‌اند. یک checklist از تمام مراحل در commit message یا PR description بنویسید. اگر همه چیز درست است، یک commit نهایی بزنید. اگر چیزی کم است، آن را تکمیل کنید. نکته حیاتی: این مرحله شامل تغییر کد جدید نیست، فقط بررسی و مستندسازی است.
**Excerpt:**
```
در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.
```
