---
task_id: 57fae7f8-ea66-4edc-9b70-c8b9b268eb3a
title: '[منطق] عدم مدیریت orphan files در صورت crash پیش از cleanup'
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-17T07:14:11.576665+00:00'
updated_at: '2026-05-29T20:16:41.473508+00:00'
---

# [منطق] عدم مدیریت orphan files در صورت crash پیش از cleanup

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در oversight_upload_session اشاره به `پاکسازی خودکار فایل‌های temp orphan` شده است، اما مکانیزم مشخصی (مثلاً background task یا TTL) ذکر نشده است. اگر سرویس crash کند، فایل‌های temp باقی می‌مانند.

## 💥 پیامد (impact)
فضای دیسک پر می‌شود و ممکن است فایل‌های حساس لو بروند.

## 🛠 پیشنهاد رفع اولیه
یک cron job یا background task برای پاکسازی فایل‌های temp older than X hours اضافه کنید، یا از temp directory سیستمی با auto-cleanup استفاده کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-17T07:17:52.263797+00:00]
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

اگرچه به پاکسازی خودکار فایل‌های temp orphan اشاره شده، اما مکانیزم مشخصی برای cleanup در صورت crash یا timeout وجود ندارد. همچنین race condition بین چند request همزمان ممکن است.

## 💥 پیامد (impact)
انباشت فایل‌های temp روی دیسک، مص

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم مدیریت orphan files در صورت crash پیش از cleanup

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در oversight_upload_session اشاره به `پاکسازی خودکار فایل‌های temp orphan` شده است، اما مکانیزم مشخصی (مثلاً background task یا TTL) ذکر نشده است. اگر سرویس crash کند، فایل‌های temp باقی می‌مانند.

## 💥 پیامد (impact)
فضای دیسک پر می‌شود و ممکن است فایل‌های حساس لو بروند.

## 🛠 پیشنهاد رفع اولیه
یک cron job یا background task برای پاکسازی فایل‌های temp older than X hours اضافه کنید، یا از temp directory سیستمی با auto-cleanup استفاده کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی ناسازگاری در pipeline data
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و شناسایی دقیق تمام بخش‌های کد در pipeline `data` است که به پاکسازی خودکار فایل‌های temp orphan اشاره دارند. باید مشخص شود که کدام فایل‌ها، توابع و کامنت‌ها به این موضوع اشاره کرده‌اند و فرض‌های ناسازگار آن‌ها چیست. این مرحله شامل پیاده‌سازی هیچ راه‌حلی نیست، فقط مستندسازی وضعیت موجود است. خروجی این مرحله یک گزارش متنی (مثلاً در فایل ISSUE.md یا کامیت message) است که دو طرف ناسازگاری و فرض‌هایشان را شرح می‌دهد.
**Excerpt:**
```
در oversight_upload_session اشاره به `پاکسازی خودکار فایل‌های temp orphan` شده است، اما مکانیزم مشخصی (مثلاً background task یا TTL) ذکر نشده است. اگر سرویس crash کند، فایل‌های temp باقی می‌مانند. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 2: تعیین ground truth و تصمیم‌گیری برای مکانیزم پاکسازی
**Status:** `not_done` (0%)
**Scope:** بر اساس مستندسازی مرحله قبل، باید تصمیم گرفته شود که کدام رویکرد (cron job، background task، temp directory سیستمی با auto-cleanup) به عنوان ground truth انتخاب می‌شود. این تصمیم باید با توجه به معماری پروژه (FastAPI, Next.js) و نیازمندی‌های non-functional گرفته شود. خروجی این مرحله یک تصمیم مستند (در کامیت message یا فایل تصمیم) است که مشخص می‌کند کدام مکانیزم پیاده‌سازی خواهد شد و چرا.
**Excerpt:**
```
یک cron job یا background task برای پاکسازی فایل‌های temp older than X hours اضافه کنید، یا از temp directory سیستمی با auto-cleanup استفاده کنید. ground truth تعیین شد و طرف دیگر align شد.
```

### Step 3: پیاده‌سازی مکانیزم پاکسازی temp files (background task در FastAPI)
**Status:** `not_done` (0%)
**Scope:** بر اساس تصمیم مرحله قبل، مکانیزم پاکسازی را پیاده‌سازی کنید. اگر تصمیم بر background task است، یک تابع async در FastAPI بنویسید که به صورت دوره‌ای (مثلاً هر 1 ساعت) فایل‌های temp older than X hours را پاک کند. اگر تصمیم بر cron job است، یک اسکریپت جداگانه یا task scheduler (مثلاً APScheduler) اضافه کنید. این مرحله شامل نوشتن تست برای این مکانیزم نیست (مرحله جداگانه). همچنین شامل تغییر oversight_upload_session برای align با این مکانیزم است.
**Excerpt:**
```
یک cron job یا background task برای پاکسازی فایل‌های temp older than X hours اضافه کنید. ground truth تعیین شد و طرف دیگر align شد.
```

### Step 4: align کردن oversight_upload_session با مکانیزم جدید
**Status:** `not_done` (0%)
**Scope:** بخش‌هایی از کد که در oversight_upload_session به پاکسازی خودکار اشاره داشتند (کامنت‌ها، توابع، یا منطق) باید به‌روزرسانی شوند تا با مکانیزم جدید هماهنگ باشند. اگر کامنتی وجود داشت که به مکانیزم قدیمی اشاره می‌کرد، باید اصلاح شود. اگر تابعی وجود داشت که فرض می‌کرد پاکسازی خودکار وجود دارد، باید بررسی شود که آیا با مکانیزم جدید کار می‌کند یا نیاز به تغییر دارد.
**Excerpt:**
```
در oversight_upload_session اشاره به `پاکسازی خودکار فایل‌های temp orphan` شده است. ground truth تعیین شد و طرف دیگر align شد.
```

### Step 5: نوشتن integration test برای pipeline data
**Status:** `not_done` (0%)
**Scope:** یک integration test بنویسید که سناریوی crash سرویس و باقی ماندن فایل‌های temp را شبیه‌سازی کند و سپس بررسی کند که مکانیزم پاکسازی آن‌ها را حذف می‌کند. این تست باید در pipeline CI/CD اجرا شود و از شکست آن جلوگیری کند. تست باید مستقل از محیط اجرا باشد (از temp directory ایزوله استفاده کند).
**Excerpt:**
```
integration test برای pipeline `data` بدون شکست عبور می‌کند. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 6: نوشتن PR description با توضیح کامل تغییرات
**Status:** `not_done` (0%)
**Scope:** یک PR description جامع بنویسید که شامل: ۱) شرح مشکل (ناسازگاری منطقی)، ۲) راه‌حل انتخاب‌شده و دلیل آن، ۳) لیست تمام فایل‌های تغییر یافته، ۴) نحوه تست (دستی و خودکار)، ۵) checklist از همه کامیت‌ها. این description باید به‌اندازه کافی دقیق باشد که یک reviewer بتواند بدون مراجعه به کد، متوجه تغییرات شود.
**Excerpt:**
```
PR description توضیح می‌دهد چرا این تغییرات لازم بودند و چگونه coherence issue را حل می‌کنند.
```
