---
task_id: 1c7feb42-f95f-4a8d-ab33-476a0a3cd8ec
title: '[منطق] عدم مدیریت empty chunk data در oversight_upload_session.py'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-17T07:19:18.607203+00:00'
updated_at: '2026-06-03T18:16:23.543341+00:00'
---

# [منطق] عدم مدیریت empty chunk data در oversight_upload_session.py

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

ورودی `chunk data (binary stream)` می‌تواند خالی باشد (مثلاً در انتهای آپلود یا در صورت خطای شبکه). اگر chunk خالی دریافت شود، ممکن است فایل temp خراب شود یا state به اشتباه به `completed` برود.

## 💥 پیامد (impact)
فایل ناقص ذخیره می‌شود و session ممکن است به اشتباه کامل تلقی شود و باعث خطا در مرحله extract شود.

## 🛠 پیشنهاد رفع اولیه
قبل از ذخیره chunk، بررسی کنید که داده غیرخالی است. اگر chunk خالی است، آن را نادیده بگیرید یا خطا برگردانید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-17T08:12:59.396322+00:00]
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در بخش chunk data، اشاره‌ای به handling chunk خالی یا صفر بایت نشده است. همچنین در صورت دریافت chunk با size نامتناسب با total_size، رفتار مشخص نیست.

## 💥 پیامد (impact)
chunk خالی می‌تواند باعث corruption فایل نهایی شود. همچنین اگر
---
[scan #3 at 2026-05-17T08:45:18.077503+00:00]
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در expected_inputs برای oversight_upload_session.py، 'chunkهای داده (از طریق API)' ذکر شده است، اما هیچ اشاره‌ای به مدیریت chunkهای خالی یا ناقص نشده است. اگر یک chunk خالی ارسال شود، ممکن است سرویس خطا دهد یا فایل ناقص ذخیره شود.

#

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم مدیریت empty chunk data در oversight_upload_session.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

ورودی `chunk data (binary stream)` می‌تواند خالی باشد (مثلاً در انتهای آپلود یا در صورت خطای شبکه). اگر chunk خالی دریافت شود، ممکن است فایل temp خراب شود یا state به اشتباه به `completed` برود.

## 💥 پیامد (impact)
فایل ناقص ذخیره می‌شود و session ممکن است به اشتباه کامل تلقی شود و باعث خطا در مرحله extract شود.

## 🛠 پیشنهاد رفع اولیه
قبل از ذخیره chunk، بررسی کنید که داده غیرخالی است. اگر chunk خالی است، آن را نادیده بگیرید یا خطا برگردانید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و شناسایی فایل‌های مرتبط با pipeline داده و مدیریت chunk در oversight_upload_session
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و شناسایی تمام فایل‌های مرتبط با pipeline داده، به‌ویژه فایل‌های مربوط به مدیریت session آپلود و پردازش chunk در پروژه است. باید فایل‌هایی مانند oversight_upload_session.py، هرگونه هندلر یا endpoint مرتبط با آپلود فایل، و فایل‌های مربوط به state management session را پیدا کنید. خارج از این مرحله: تغییر کد، نوشتن تست، یا اصلاح منطق. نکته حیاتی: از grep و جستجوی ساختاریافته برای یافتن تمام ارجاعات به 'chunk'، 'upload_session'، 'binary stream' و 'completed' state استفاده کنید.
**Excerpt:**
```
📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 2: بررسی کد موجود برای مدیریت chunk خالی و وضعیت فعلی oversight_upload_session
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل خواندن دقیق فایل‌های شناسایی‌شده در مرحله قبل، به‌ویژه oversight_upload_session.py، برای درک منطق فعلی پردازش chunk و مدیریت state session است. باید مشخص شود که آیا هم‌اکنون هیچ بررسی برای chunk خالی وجود دارد، state چگونه به 'completed' تغییر می‌کند، و چه فرضیاتی در کد وجود دارد. خارج از این مرحله: تغییر کد یا نوشتن تست. نکته حیاتی: به دنبال تابع‌هایی بگردید که chunk را دریافت و ذخیره می‌کنند، و نحوه مدیریت state session را بررسی کنید.
**Excerpt:**
```
ورودی `chunk data (binary stream)` می‌تواند خالی باشد (مثلاً در انتهای آپلود یا در صورت خطای شبکه). اگر chunk خالی دریافت شود، ممکن است فایل temp خراب شود یا state به اشتباه به `completed` برود.

💥 پیامد (impact)
فایل ناقص ذخیره می‌شود و session ممکن است به اشتباه کامل تلقی شود و باعث خطا در مرحله extract شود.
```

### Step 3: اضافه کردن بررسی empty chunk در endpoint دریافت chunk (backend)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اصلاح endpoint یا تابعی است که chunk data را دریافت می‌کند تا قبل از هرگونه پردازش، بررسی کند که داده دریافتی خالی نیست. اگر chunk خالی باشد، باید یا نادیده گرفته شود (و یک پاسخ مناسب مانند 204 No Content برگرداند) یا یک خطای 400 Bad Request برگرداند. خارج از این مرحله: تغییر state session، نوشتن تست، یا اصلاح frontend. نکته حیاتی: این بررسی باید در اولین نقطه ورود داده (قبل از ذخیره در فایل temp) انجام شود.
**Excerpt:**
```
🛠 پیشنهاد رفع اولیه
قبل از ذخیره chunk، بررسی کنید که داده غیرخالی است. اگر chunk خالی است، آن را نادیده بگیرید یا خطا برگردانید.
```

### Step 4: اصلاح منطق state management session برای جلوگیری از transition به completed در صورت دریافت chunk خالی
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اصلاح منطق تغییر state session است تا اطمینان حاصل شود که session تنها زمانی به state 'completed' تغییر وضعیت می‌دهد که تمام chunk‌های مورد انتظار (غیرخالی) دریافت شده باشند. باید بررسی شود که آیا state 'completed' به اشتباه پس از دریافت یک chunk خالی (که نادیده گرفته شده) ست می‌شود یا خیر. خارج از این مرحله: نوشتن تست، اصلاح endpoint دریافت chunk. نکته حیاتی: منطق transition state باید با دقت بررسی شود تا از ایجاد state‌های متناقض جلوگیری شود.
**Excerpt:**
```
اگر chunk خالی دریافت شود، ممکن است فایل temp خراب شود یا state به اشتباه به `completed` برود.

💥 پیامد (impact)
فایل ناقص ذخیره می‌شود و session ممکن است به اشتباه کامل تلقی شود و باعث خطا در مرحله extract شود.
```

### Step 5: نوشتن integration test برای سناریوی دریافت chunk خالی در pipeline داده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست integration است که سناریوی ارسال یک chunk خالی به pipeline داده را شبیه‌سازی می‌کند. تست باید بررسی کند که: (1) chunk خالی نادیده گرفته می‌شود یا خطا برمی‌گرداند، (2) فایل temp خراب نمی‌شود، (3) state session به اشتباه به 'completed' تغییر نمی‌کند. خارج از این مرحله: نوشتن unit test برای توابع جداگانه، اصلاح کد تولیدی. نکته حیاتی: تست باید کل pipeline را از دریافت chunk تا ذخیره نهایی و تغییر state پوشش دهد.
**Excerpt:**
```
✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توض
```

### Step 6: مستندسازی فرضیات ناسازگار و ground truth تعیین‌شده در PR description
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک PR description جامع است که: (1) ناسازگاری منطقی شناسایی‌شده را توضیح می‌دهد (عدم مدیریت chunk خالی)، (2) فرضیات دو طرف کد (دریافت chunk و مدیریت state) را مستند می‌کند، (3) ground truth تعیین‌شده (chunk خالی باید نادیده گرفته شود و state را تغییر ندهد) را مشخص می‌کند، (4) تغییرات انجام‌شده را لیست می‌کند. خارج از این مرحله: تغییر کد یا نوشتن تست. نکته حیاتی: PR description باید شفاف و کامل باشد تا reviewers بتوانند به راحتی تغییرات را درک کنند.
**Excerpt:**
```
✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توض
```
