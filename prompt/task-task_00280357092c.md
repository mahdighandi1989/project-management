---
task_id: task_00280357092c
title: رفع نقص‌های امنیتی و تست‌های OversightService
type: other
priority: critical
execution_priority: 1000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-18T04:16:54.582937+00:00'
updated_at: '2026-05-29T20:23:13.664335+00:00'
tags:
- consolidated
- post_verify_merge
target_files:
- backend/tests/test_oversight_service.py
- backend/app/services/oversight_service.py
- tests/test_oversight_service.py
- backend/app/api/routes/oversight.py
- backend/app/services/ai_base.py
- backend/tests/test_auth.py
- backend/app/api/routes/auth.py
- frontend/package-lock.json
- frontend/next.config.js
- frontend/next-env.d.ts
- backend/tests/
- backend/app/services/oversight_verifier.py
- backend/app/services/github_pr_service.py
---

# رفع نقص‌های امنیتی و تست‌های OversightService

## Raw Idea

🧬 این یک تسک تلفیقی است — از 3 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): تسک‌های مربوط به OversightService شامل کمبود تست واحد و نقص امنیتی در validate_token هستند. این تسک‌ها به یک سرویس خاص (oversight_service.py) مرتبط هستند.
🎯 theme: رفع باگ‌های مربوط به OversightService و امنیت Token
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 3
  id: baa11f45-513f-4826-a45e-234f98940e55
  عنوان اصلی: OversightService فاقد هرگونه تست واحد است
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - تست add_watched با repo معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_valid_repo", "timeout_seconds": 60}]
  - تست add_watched با repo تکراری خطای مناسب می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_duplicate_repo", "timeout_seconds": 60}]
  - تست run_task با task معتبر status را به running تغییر می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_run_task_changes_status_to_running", "timeout_seconds": 60}]
  - تست scan_project با watched_id نامعتبر 404 برمی‌گرداند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404", "timeout_seconds": 60}]
  - تست auto_register_watched با repo_full_name معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
OversightService فاقد هرگونه تست واحد است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — کلاس اصلی که باید تست شود
  ```python
  class OversightService:
      def __init__(self):
          self._lock = asyncio.Lock()
          self.watched: List[WatchedProject] = []
          self.tasks: List[Task] = []
          self.reports: List[Report] = []
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + asyncio + SQLite + GitHub API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 12) — روتری که از OversightService استفاده می‌کند
- `backend/app/services/verify_runtime/runner.py` — وابستگی runtime verify که توسط oversight استفاده می‌شود

## 🌐 نقشهٔ وابستگی‌ها
OversightService توسط 1 روتر و چندین سرویس verify_runtime استفاده می‌شود. عدم تست می‌تواند باعث شکست کل workflow نظارت شود.

## 🔍 Context و وضعیت فعلی
OversightService (backend/app/services/oversight_service.py) هستهٔ مرکز نظارت پروژه‌هاست و توسط 1 روتر (backend/app/api/routes/oversight.py) استفاده می‌شود. این سرویس شامل logicهای حیاتی مانند add_watched, run_task, scan_project, auto_register_watched و مدیریت scheduler است. هیچ تستی برای این سرویس وجود ندارد که می‌تواند منجر به regressionهای ناشناخته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست add_watched با repo معتبر کار می‌کند
- [ ] تست add_watched با repo تکراری خطای مناسب می‌دهد
- [ ] تست run_task با task معتبر status را به running تغییر می‌دهد
- [ ] تست scan_project با watched_id نامعتبر 404 برمی‌گرداند
- [ ] تست auto_register_watched با repo_full_name معتبر کار می‌کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد تست‌های واحد برای OversightService با mocking وابستگی‌های خارجی (GitHub API, Render API, AI Manager)

## 💡 نمونه‌های قبل/بعد
**تست add_watched**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
async def test_add_watched():
    service = OversightService()
    result = await service.add_watched({
        'repo_full_name': 'test/repo',
        'default_branch': 'main'
    })
    assert result['success']
    assert len(service.watched) == 1
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v`
- `pytest backend/tests/test_oversight_service.py -k test_add_watched`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به mocking دقیق وابستگی‌های خارجی (GitHub API, Render API)

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 3
  id: 5e86c7d7-40b0-4c4f-a460-01a9d46bb801
  عنوان اصلی: نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/login", "headers": null, "json_body": {"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiO]
  - تابع validate_token پارامتر exp را چک می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["exp", "expired", "ExpiredSignatureError", "expiration"], "files_hint": ["backend/app/services/oversight_service.py"]}]
  - تست واحد جدید برای expiry اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_validate_token_expired", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — تابع اصلی که باید اصلاح شود
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + JWT + Next.js 14 App Router

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 67) — این endpoint از validate_token استفاده می‌کند
- `frontend/src/lib/api.ts` — client که login را call می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این تابع توسط ۳ روتر و یک hook فرانت‌اند استفاده می‌شود؛ تغییر آن روی کل auth flow اثر می‌گذارد.

## 🔍 Context و وضعیت فعلی
در فایل `backend/app/services/oversight_service.py`، تابع `validate_token` (خطوط 245-289) توکن JWT را بدون بررسی فیلد `exp` (expiration) تأیید می‌کند. این بدان معناست که توکن‌های منقضی شده همچنان معتبر تلقی می‌شوند و مهاجم می‌تواند با یک توکن قدیمی به سیستم دسترسی پیدا کند. شواهد در snippet خط 247 نشان می‌دهد که تابع صرفاً وجود توکن را چک می‌کند و `return True` برمی‌گرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند
- [ ] تابع validate_token پارامتر exp را چک می‌کند
- [ ] تست واحد جدید برای expiry اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. به تابع validate_token یک بررسی expiry اضافه کن: ابتدا توکن را decode کن، سپس فیلد `exp` را با زمان فعلی (UTC) مقایسه کن. اگر توکن منقضی شده بود، خطای 401 برگردان.

## 💡 نمونه‌های قبل/بعد
**نمونه expiry check**

_قبل:_
```
if token: return user
```

_بعد:_
```
if token and not is_expired(token): return user
else: raise HTTPException(401)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_auth.py -k expiry`
- `npm run test -- login`

## ⚠️ ریسک‌ها و موارد احتیاط
احتمال شکستن sessionهای فعال؛ نیاز به migration کوتاه

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 3
  id: 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5
  عنوان اصلی: OversightService فاقد تست واحد است — ۱۴ روتر به آن وابسته‌اند
  اولویت اصلی: critical
  وضعیت verify قبلی: partial
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - فایل backend/tests/test_oversight_service.py ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["def test_", "class Test"], "files_hint": ["backend/tests/test_oversight_service.py"]}]
  - حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
  - تست‌ها با pytest و pytest-asyncio اجرا شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
  - همه تست‌ها پاس شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
OversightService فاقد تست واحد است — ۱۴ روتر به آن وابسته‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — کلاس اصلی که باید تست شود
  ```python
  class OversightService:
      def __init__(self):
          self.watched = []
          self.tasks = []
          self._lock = asyncio.Lock()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + asyncio + SQLite

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 12) — ۱۴ endpoint از این سرویس استفاده می‌کنند
- `backend/app/services/oversight_service.py` (سطر 1) — خود سرویس

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط oversight.py (۱۴ route)، main.py (scheduler loop) و چند سرویس دیگر استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
سرویس `oversight_service.py` هاب اصلی ۱۴ endpoint در `oversight.py` است. هیچ فایل تستی برای این سرویس وجود ندارد. با توجه به اینکه این سرویس مدیریت watched projects, tasks, scans, و runtime verification را بر عهده دارد، عدم پوشش تست ریسک بالایی ایجاد می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/tests/test_oversight_service.py ایجاد شود
- [ ] حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary
- [ ] تست‌ها با pytest و pytest-asyncio اجرا شوند
- [ ] همه تست‌ها پاس شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل `backend/tests/test_oversight_service.py` با تست‌های واحد برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary

## 💡 نمونه‌های قبل/بعد
**تست add_watched**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
async def test_add_watched():
    service = OversightService()
    result = await service.add_watched({
        'repo_full_name': 'test/repo',
        'default_branch': 'main'
    })
    assert result['success']
    assert len(service.watched) == 1
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v`
- `pytest backend/tests/ --cov=app.services.oversight_service`

## ⚠️ ریسک‌ها و موارد احتیاط
بدون تست، تغییرات در oversight_service ممکن است ۱۴ endpoint را بشکند

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
🧬 این یک تسک تلفیقی است — از 3 تسک منفرد ساخته شده.
📌 دلیل تلفیق (rationale توسط AI): تسک‌های مربوط به OversightService شامل کمبود تست واحد و نقص امنیتی در validate_token هستند. این تسک‌ها به یک سرویس خاص (oversight_service.py) مرتبط هستند.
🎯 theme: رفع باگ‌های مربوط به OversightService و امنیت Token
💎 estimated_difficulty: medium

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 1 از 3
  id: baa11f45-513f-4826-a45e-234f98940e55
  عنوان اصلی: OversightService فاقد هرگونه تست واحد است
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - تست add_watched با repo معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_valid_repo", "timeout_seconds": 60}]
  - تست add_watched با repo تکراری خطای مناسب می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_duplicate_repo", "timeout_seconds": 60}]
  - تست run_task با task معتبر status را به running تغییر می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_run_task_changes_status_to_running", "timeout_seconds": 60}]
  - تست scan_project با watched_id نامعتبر 404 برمی‌گرداند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404", "timeout_seconds": 60}]
  - تست auto_register_watched با repo_full_name معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
OversightService فاقد هرگونه تست واحد است

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — کلاس اصلی که باید تست شود
  ```python
  class OversightService:
      def __init__(self):
          self._lock = asyncio.Lock()
          self.watched: List[WatchedProject] = []
          self.tasks: List[Task] = []
          self.reports: List[Report] = []
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + asyncio + SQLite + GitHub API

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 12) — روتری که از OversightService استفاده می‌کند
- `backend/app/services/verify_runtime/runner.py` — وابستگی runtime verify که توسط oversight استفاده می‌شود

## 🌐 نقشهٔ وابستگی‌ها
OversightService توسط 1 روتر و چندین سرویس verify_runtime استفاده می‌شود. عدم تست می‌تواند باعث شکست کل workflow نظارت شود.

## 🔍 Context و وضعیت فعلی
OversightService (backend/app/services/oversight_service.py) هستهٔ مرکز نظارت پروژه‌هاست و توسط 1 روتر (backend/app/api/routes/oversight.py) استفاده می‌شود. این سرویس شامل logicهای حیاتی مانند add_watched, run_task, scan_project, auto_register_watched و مدیریت scheduler است. هیچ تستی برای این سرویس وجود ندارد که می‌تواند منجر به regressionهای ناشناخته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست add_watched با repo معتبر کار می‌کند
- [ ] تست add_watched با repo تکراری خطای مناسب می‌دهد
- [ ] تست run_task با task معتبر status را به running تغییر می‌دهد
- [ ] تست scan_project با watched_id نامعتبر 404 برمی‌گرداند
- [ ] تست auto_register_watched با repo_full_name معتبر کار می‌کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد تست‌های واحد برای OversightService با mocking وابستگی‌های خارجی (GitHub API, Render API, AI Manager)

## 💡 نمونه‌های قبل/بعد
**تست add_watched**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
async def test_add_watched():
    service = OversightService()
    result = await service.add_watched({
        'repo_full_name': 'test/repo',
        'default_branch': 'main'
    })
    assert result['success']
    assert len(service.watched) == 1
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v`
- `pytest backend/tests/test_oversight_service.py -k test_add_watched`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به mocking دقیق وابستگی‌های خارجی (GitHub API, Render API)

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: large

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 2 از 3
  id: 5e86c7d7-40b0-4c4f-a460-01a9d46bb801
  عنوان اصلی: نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد
  اولویت اصلی: critical
  وضعیت verify قبلی: pending
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/login", "headers": null, "json_body": {"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiO]
  - تابع validate_token پارامتر exp را چک می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["exp", "expired", "ExpiredSignatureError", "expiration"], "files_hint": ["backend/app/services/oversight_service.py"]}]
  - تست واحد جدید برای expiry اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_validate_token_expired", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — تابع اصلی که باید اصلاح شود
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + JWT + Next.js 14 App Router

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 67) — این endpoint از validate_token استفاده می‌کند
- `frontend/src/lib/api.ts` — client که login را call می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این تابع توسط ۳ روتر و یک hook فرانت‌اند استفاده می‌شود؛ تغییر آن روی کل auth flow اثر می‌گذارد.

## 🔍 Context و وضعیت فعلی
در فایل `backend/app/services/oversight_service.py`، تابع `validate_token` (خطوط 245-289) توکن JWT را بدون بررسی فیلد `exp` (expiration) تأیید می‌کند. این بدان معناست که توکن‌های منقضی شده همچنان معتبر تلقی می‌شوند و مهاجم می‌تواند با یک توکن قدیمی به سیستم دسترسی پیدا کند. شواهد در snippet خط 247 نشان می‌دهد که تابع صرفاً وجود توکن را چک می‌کند و `return True` برمی‌گرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند
- [ ] تابع validate_token پارامتر exp را چک می‌کند
- [ ] تست واحد جدید برای expiry اضافه شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. به تابع validate_token یک بررسی expiry اضافه کن: ابتدا توکن را decode کن، سپس فیلد `exp` را با زمان فعلی (UTC) مقایسه کن. اگر توکن منقضی شده بود، خطای 401 برگردان.

## 💡 نمونه‌های قبل/بعد
**نمونه expiry check**

_قبل:_
```
if token: return user
```

_بعد:_
```
if token and not is_expired(token): return user
else: raise HTTPException(401)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_auth.py -k expiry`
- `npm run test -- login`

## ⚠️ ریسک‌ها و موارد احتیاط
احتمال شکستن sessionهای فعال؛ نیاز به migration کوتاه

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: small

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
تسک 3 از 3
  id: 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5
  عنوان اصلی: OversightService فاقد تست واحد است — ۱۴ روتر به آن وابسته‌اند
  اولویت اصلی: critical
  وضعیت verify قبلی: partial
  فایل‌های دخیل: backend/app/services/oversight_service.py

📋 acceptance_criteria کامل:
  - فایل backend/tests/test_oversight_service.py ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["def test_", "class Test"], "files_hint": ["backend/tests/test_oversight_service.py"]}]
  - حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
  - تست‌ها با pytest و pytest-asyncio اجرا شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
  - همه تست‌ها پاس شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]

📝 idea_prompt اصلی (بدون تغییر و بدون خلاصه‌سازی):
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
OversightService فاقد تست واحد است — ۱۴ روتر به آن وابسته‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — کلاس اصلی که باید تست شود
  ```python
  class OversightService:
      def __init__(self):
          self.watched = []
          self.tasks = []
          self._lock = asyncio.Lock()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + asyncio + SQLite

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` (سطر 12) — ۱۴ endpoint از این سرویس استفاده می‌کنند
- `backend/app/services/oversight_service.py` (سطر 1) — خود سرویس

## 🌐 نقشهٔ وابستگی‌ها
این سرویس توسط oversight.py (۱۴ route)، main.py (scheduler loop) و چند سرویس دیگر استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
سرویس `oversight_service.py` هاب اصلی ۱۴ endpoint در `oversight.py` است. هیچ فایل تستی برای این سرویس وجود ندارد. با توجه به اینکه این سرویس مدیریت watched projects, tasks, scans, و runtime verification را بر عهده دارد، عدم پوشش تست ریسک بالایی ایجاد می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/tests/test_oversight_service.py ایجاد شود
- [ ] حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary
- [ ] تست‌ها با pytest و pytest-asyncio اجرا شوند
- [ ] همه تست‌ها پاس شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل `backend/tests/test_oversight_service.py` با تست‌های واحد برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary

## 💡 نمونه‌های قبل/بعد
**تست add_watched**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
async def test_add_watched():
    service = OversightService()
    result = await service.add_watched({
        'repo_full_name': 'test/repo',
        'default_branch': 'main'
    })
    assert result['success']
    assert len(service.watched) == 1
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v`
- `pytest backend/tests/ --cov=app.services.oversight_service`

## ⚠️ ریسک‌ها و موارد احتیاط
بدون تست، تغییرات در oversight_service ممکن است ۱۴ endpoint را بشکند

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

✅ مراحل قبلاً done شده (در super-task به‌عنوان pre_done):
  (هیچ مرحله‌ای قبلاً done نشده بود)

🔧 مراحل remaining که در super-task باید انجام شوند:
  (همهٔ مراحل remaining هستند)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
💡 نکات استاندارد (همان bullet هایی که در ساخت پرامپت‌های معمولی پروژه رعایت می‌شود — وراثت کامل، نه کپی):
- ساختار AC ها: acceptance_criteria با verify_method و verify_plan و evidence_locations برای هر AC
- edge cases را در نظر بگیر و در پرامپت ذکر کن
- وابستگی‌ها را اول حل کن (dependency-aware ordering)
- اگر بخشی از یکی از تسک‌ها قبلاً done است (pre_done در بالا)، تکرار نکن — فقط روی remaining_parts تمرکز کن
- در commit message: `merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5`
- task_steps را با dependency-aware ordering مرتب کن
- هیچ کار قبلاً done شده‌ای نباید دوباره انجام شود
- هیچ خلاصه‌سازی نکن — جزئیات کامل از همهٔ منابع باید حفظ شوند
```

## 📋 چک‌لیست مراحل (18 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد تست واحد برای add_watched با repo معتبر** — این مرحله شامل نوشتن تست واحد برای متد add_watched از کلاس OversightService است. تست باید با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های مربوط به add_watched با repo تکراری، تست‌های run_task، scan_project، auto_register_watched، و هرگونه 
- [ ] **مرحله 2: ایجاد تست واحد برای run_task با task معتبر** — این مرحله شامل نوشتن تست واحد برای متد run_task از کلاس OversightService است. تست باید با یک task معتبر کار کند و بررسی کند که status task به 'running' تغییر می‌کند. خارج از این مرحله: تست‌های add_watched، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک task به
- [ ] **مرحله 3: ایجاد تست واحد برای scan_project با watched_id نامعتبر** — این مرحله شامل نوشتن تست واحد برای متد scan_project از کلاس OversightService است. تست باید با یک watched_id نامعتبر (که در لیست watched وجود ندارد) کار کند و بررسی کند که خطای 404 (HTTPException با status code 404) برمی‌گرداند. خارج از این مرحله: تست‌های add_watched، run_task، auto_register_watched،
- [ ] **مرحله 4: ایجاد تست واحد برای auto_register_watched با repo_full_name معتبر** — این مرحله شامل نوشتن تست واحد برای متد auto_register_watched از کلاس OversightService است. تست باید با یک repo_full_name معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک repo_full_name معتبر 
- [ ] **مرحله 5: اصلاح تابع validate_token برای بررسی expiry** — این مرحله شامل اصلاح تابع validate_token در فایل backend/app/services/oversight_service.py است. تابع باید توکن JWT را decode کند و فیلد exp (expiration) را با زمان فعلی (UTC) مقایسه کند. اگر توکن منقضی شده باشد، تابع باید False برگرداند یا HTTPException با status code 401 raise کند. خارج از این مرحل
- [ ] **مرحله 6: ایجاد تست واحد برای validate_token با توکن منقضی** — این مرحله شامل نوشتن تست واحد برای تابع validate_token است که بررسی می‌کند با یک توکن JWT منقضی شده، تابع False برمی‌گرداند یا خطای 401 صادر می‌کند. تست باید در فایل tests/test_oversight_service.py اضافه شود. خارج از این مرحله: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، 
- [ ] **مرحله 7: ایجاد فایل تست tests/test_oversight_service.py** — این مرحله شامل ایجاد فایل tests/test_oversight_service.py است. فایل باید شامل تست‌های واحد برای متدهای اصلی OversightService باشد: add_watched, update_watched, run_task, scan_project, status_summary. خارج از این مرحله: نوشتن تست‌های خاص برای validate_token (که در مرحله 7 انجام می‌شود)، تست‌های add_w
- [ ] **مرحله 8: نوشتن تست واحد برای update_watched** — این مرحله شامل نوشتن تست واحد برای متد update_watched از کلاس OversightService است. تست باید یک watched project موجود را به‌روزرسانی کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، status_summary، auto_register_watched. نکته حیاتی: تست باید ابتدا یک 
- [ ] **مرحله 9: نوشتن تست واحد برای status_summary** — این مرحله شامل نوشتن تست واحد برای متد status_summary از کلاس OversightService است. تست باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند. خارج از این مرحله: تست‌های add_watched، update_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید چند watched project و task اضافه کند 
- [ ] **مرحله 10: اجرای همه تست‌ها و اطمینان از عبور** — این مرحله شامل اجرای همه تست‌های موجود در فایل tests/test_oversight_service.py با pytest است. باید اطمینان حاصل شود که همه تست‌ها پاس می‌شوند. خارج از این مرحله: نوشتن تست‌های جدید، اصلاح کد اصلی سرویس. نکته حیاتی: اگر تستی fail شود، باید خطا بررسی و رفع شود.
- [ ] **مرحله 11: اجرای linter و اطمینان از عبور بدون warning** — این مرحله شامل اجرای linter (مانند flake8 یا pylint) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ warning یا خطایی وجود ندارد. خارج از این مرحله: اجرای type-check، اجرای تست‌ها. نکته حیاتی: اگر linter warning
- [ ] **مرحله 12: اجرای type-check و اطمینان از موفقیت** — این مرحله شامل اجرای type-checker (مانند mypy) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ خطای type وجود ندارد. خارج از این مرحله: اجرای linter، اجرای تست‌ها. نکته حیاتی: اگر type-check خطا داشته باشد، باید
- [ ] **مرحله 13: بررسی endpoint /api/login برای توکن منقضی** — این مرحله شامل بررسی endpoint /api/login است که از تابع validate_token استفاده می‌کند. باید اطمینان حاصل شود که با یک توکن منقضی شده، endpoint خطای 401 برمی‌گرداند. خارج از این مرحله: اصلاح تابع validate_token (که در مرحله 6 انجام شده)، نوشتن تست واحد برای expiry (مرحله 7). نکته حیاتی: این مرحله بیش
- [ ] **مرحله 14: بررسی تابع validate_token برای پارامتر exp** — این مرحله شامل بررسی استاتیک کد تابع validate_token در فایل backend/app/services/oversight_service.py است. باید اطمینان حاصل شود که تابع پارامتر exp را چک می‌کند. خارج از این مرحله: اجرای تست‌ها، اصلاح endpoint /api/login. نکته حیاتی: این مرحله با grep برای الگوهای 'exp', 'expired', 'ExpiredSignatur
- [ ] **مرحله 15: اجرای تست‌های auth برای expiry** — این مرحله شامل اجرای تست‌های مربوط به auth با فیلتر expiry است. باید اطمینان حاصل شود که تست‌های مربوط به expiry در فایل backend/tests/test_auth.py پاس می‌شوند. خارج از این مرحله: اجرای تست‌های oversight_service، اجرای linter. نکته حیاتی: اگر فایل backend/tests/test_auth.py وجود ندارد، این مرحله ممک
- [ ] **مرحله 16: اجرای تست‌های frontend برای login** — این مرحله شامل اجرای تست‌های frontend مربوط به login است. باید اطمینان حاصل شود که تست‌های مربوط به login در frontend پاس می‌شوند. خارج از این مرحله: اجرای تست‌های backend، اجرای linter. نکته حیاتی: اگر تست‌های frontend برای login وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن‌ها داشته باشد.
- [ ] **مرحله 17: اجرای pytest با پوشش کد برای oversight_service** — این مرحله شامل اجرای pytest با پوشش کد برای ماژول app.services.oversight_service است. باید اطمینان حاصل شود که پوشش تست برای این ماژول قابل قبول است. خارج از این مرحله: اجرای تست‌های خاص، اجرای linter. نکته حیاتی: این مرحله برای اندازه‌گیری پوشش کد است و ممکن است نیاز به نصب pytest-cov داشته باشد.
- [ ] **مرحله 18: ایجاد commit با پیام واضح و checklist** — این مرحله شامل ایجاد commit یا PR با پیام واضح است که شامل checklist از همه تغییرات انجام شده باشد. پیام commit باید شامل merged-from با شناسه‌های تسک‌های اصلی باشد. خارج از این مرحله: انجام تغییرات کد، اجرای تست‌ها. نکته حیاتی: پیام commit باید شامل merged-from: baa11f45-513f-4826-a45e-234f98940e55

---

# 🔹 مرحله 1: ایجاد تست واحد برای add_watched با repo معتبر

**Scope:** این مرحله شامل نوشتن تست واحد برای متد add_watched از کلاس OversightService است. تست باید با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های مربوط به add_watched با repo تکراری، تست‌های run_task، scan_project، auto_register_watched، و هرگونه تغییر در کد اصلی سرویس. نکته حیاتی: تست باید با pytest-asyncio نوشته شود و وابستگی‌های خارجی (GitHub API, Render API) باید mock شوند.
— [merged] این مرحله شامل نوشتن تست واحد برای متد add_watched است که بررسی می‌کند افزودن یک repo تکراری خطای مناسب (مانند HTTPException با status code 409 یا 400) برمی‌گرداند. خارج از این مرحله: تست add_watched با repo معتبر، تست‌های run_task، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید ابتدا یک repo اضافه کند و سپس تلاش برای افزودن مجدد همان repo انجام دهد.
**Key terms:** add_watched, test_add_watched_duplicate_repo, tests/test_oversight_service.py, repo_full_name, default_branch, OversightService, test_add_watched_valid_repo

**بخش مربوط از متن کاربر:**
```
- تست add_watched با repo معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_valid_repo", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست واحد add_watched با repo معتبر و تکراری برای OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_oversight_service.py:1-100` — `test_add_watched_valid_repo` — فایل تست جدید — باید ایجاد شود. بر اساس ساختار سطحی — توسط مجری تأیید شود.
- `backend/app/services/oversight_service.py:1-50` — `OversightService.add_watched` — متد هدف برای تست — کد دقیق در deep context موجود نیست. بر اساس ساختار سطحی — توسط مجری تأیید شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی‌شده: Python + FastAPI + pytest + pytest-asyncio + SQLAlchemy + httpx (برای mock کردن APIهای خارجی)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` (سطر 1) — کلاس OversightService و متد add_watched در این فایل تعریف شده است — تست باید این متد را فراخوانی کند.
- `backend/app/services/db_service.py` (سطر 1) — برای ذخیره و بازیابی repositoryهای watched از دیتابیس استفاده می‌شود — باید در تست mock شود.
- `backend/app/services/github_storage.py` (سطر 1) — برای ارتباط با GitHub API استفاده می‌شود — باید در تست mock شود.
- `backend/app/services/render_service.py` (سطر 1) — برای ارتباط با Render API استفاده می‌شود — باید در تست mock شود.
- `backend/app/models/project.py` (سطر 1) — مدل‌های مرتبط با repository و project در این فایل تعریف شده‌اند — برای بررسی ساختار داده‌ها.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های زیر وابسته است:
- `backend/app/services/oversight_service.py`: کلاس اصلی که متد add_watched در آن تعریف شده است.
- `backend/app/services/db_service.py`: برای عملیات دیتابیس و ذخیره‌سازی repositoryها.
- `backend/app/services/github_storage.py`: برای ارتباط با GitHub API و اعتبارسنجی repository.
- `backend/app/services/render_service.py`: برای ارتباط با Render API.
- `backend/app/models/project.py`: مدل‌های داده‌ای مرتبط با repository.
- `backend/tests/test_oversight_service.py`: فایل تست جدید که باید ایجاد شود.
- `backend/app/api/routes/oversight.py`: endpointهایی که از add_watched استفاده می‌کنند — تغییر در رفتار متد روی این endpointها تأثیر می‌گذارد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد تست واحد برای متد add_watched از کلاس OversightService دارد. این درخواست شامل دو بخش اصلی است:

1. **تست add_watched با repo معتبر**: تست باید با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند. نام تست پیشنهادی: `test_add_watched_valid_repo`.

2. **تست add_watched با repo تکراری**: این تست باید بررسی کند که افزودن یک repo تکراری خطای مناسب (مانند HTTPException با status code 409 یا 400) برمی‌گرداند. نام تست پیشنهادی: `test_add_watched_duplicate_repo`.

**نکات حیاتی از متن کاربر:**
- تست‌ها باید با pytest-asyncio نوشته شوند.
- وابستگی‌های خارجی (GitHub API, Render API) باید mock شوند.
- خارج از این مرحله: تست‌های run_task، scan_project، auto_register_watched، و هرگونه تغییر در کد اصلی سرویس.
- فایل تست: `tests/test_oversight_service.py`
- کلیدواژه‌ها: add_watched, test_add_watched_duplicate_repo, tests/test_oversight_service.py, repo_full_name, default_branch, OversightService, test_add_watched_valid_repo

**شواهد در کد واقعی پروژه:**
- فایل `backend/app/services/oversight_service.py` شامل کلاس OversightService و متد add_watched است.
- فایل `backend/app/models/project.py` شامل مدل‌های مرتبط با پروژه و repository است.
- فایل `backend/app/services/db_service.py` برای عملیات دیتابیس استفاده می‌شود.
- فایل `backend/app/api/routes/oversight.py` شامل endpointهای مرتبط با oversight است که از add_watched استفاده می‌کنند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست `test_add_watched_valid_repo` باید با pytest-asyncio نوشته شود و با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند.
- [ ] تست `test_add_watched_duplicate_repo` باید بررسی کند که افزودن یک repo تکراری خطای HTTPException با status code 409 یا 400 برمی‌گرداند.
- [ ] وابستگی‌های خارجی (GitHub API, Render API) باید در تست‌ها mock شوند — هیچ درخواست واقعی به APIهای خارجی ارسال نشود.
- [ ] تست‌ها باید با دستور `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched"` با موفقیت اجرا شوند.
- [ ] هیچ تغییری در کد اصلی سرویس (OversightService) ایجاد نشود — فقط فایل تست جدید اضافه شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این درخواست، مراحل زیر پیشنهاد می‌شود:

1. **ایجاد فایل تست**: فایل `backend/tests/test_oversight_service.py` را ایجاد کنید (اگر وجود ندارد).

2. **نوشتن تست `test_add_watched_valid_repo`**:
   - از pytest-asyncio با دکوراتور `@pytest.mark.asyncio` استفاده کنید.
   - یک mock برای `OversightService` ایجاد کنید.
   - وابستگی‌های خارجی (GitHub API, Render API) را با `unittest.mock.patch` یا `pytest-mock` mock کنید.
   - یک `repo_full_name` معتبر (مثلاً `"owner/repo"`) و `default_branch` (مثلاً `"main"`) تعریف کنید.
   - متد `add_watched` را فراخوانی کرده و بررسی کنید که عملیات با موفقیت انجام شده است.
   - بررسی کنید که repository به لیست watchedها اضافه شده است.

3. **نوشتن تست `test_add_watched_duplicate_repo`**:
   - ابتدا یک repo معتبر اضافه کنید (با استفاده از متد add_watched).
   - سپس دوباره همان repo را اضافه کنید.
   - بررسی کنید که خطای HTTPException با status code 409 (Conflict) یا 400 (Bad Request) برگردانده شده است.
   - پیام خطا باید مناسب باشد (مثلاً "Repository already watched").

4. **mock کردن وابستگی‌ها**:
   - `backend/app/services/github_storage.py` برای GitHub API
   - `backend/app/services/render_service.py` برای Render API
   - `backend/app/services/db_service.py` برای عملیات دیتابیس

5. **اجرای تست**:
   ```bash
   pytest backend/tests/test_oversight_service.py -v -k "test_add_watched"
   ```

## 💡 نمونه‌های قبل/بعد
**نمونه کد تست add_watched با repo معتبر**

_بعد:_
```
import pytest
from unittest.mock import AsyncMock, patch
from httpx import HTTPStatusError

@pytest.mark.asyncio
async def test_add_watched_valid_repo():
    # Arrange
    repo_full_name = "owner/valid-repo"
    default_branch = "main"
    
    with patch('backend.app.services.oversight_service.GitHubStorage') as mock_github, \
         patch('backend.app.services.oversight_service.RenderService') as mock_render, \
         patch('backend.app.services.oversight_service.DBService') as mock_db:
        
        mock_github.return_value.verify_repo.return_value = True
        mock_db.return_value.add_watched_repo.return_value = {"id": 1, "repo": repo_full_name}
        
        service = OversightService()
        result = await service.add_watched(repo_full_name, default_branch)
        
        # Assert
        assert result is not None
        assert result["repo"] == repo_full_name
        mock_db.return_value.add_watched_repo.assert_called_once_with(repo_full_name, default_branch)
```

**نمونه کد تست add_watched با repo تکراری**

_بعد:_
```
import pytest
from unittest.mock import AsyncMock, patch
from fastapi import HTTPException

@pytest.mark.asyncio
async def test_add_watched_duplicate_repo():
    # Arrange
    repo_full_name = "owner/duplicate-repo"
    default_branch = "main"
    
    with patch('backend.app.services.oversight_service.GitHubStorage') as mock_github, \
         patch('backend.app.services.oversight_service.RenderService') as mock_render, \
         patch('backend.app.services.oversight_service.DBService') as mock_db:
        
        mock_github.return_value.verify_repo.return_value = True
        mock_db.return_value.add_watched_repo.side_effect = [
            {"id": 1, "repo": repo_full_name},  # First call succeeds
            HTTPException(status_code=409, detail="Repository already watched")  # Second call fails
        ]
        
        service = OversightService()
        
        # Act - First add should succeed
        result1 = await service.add_watched(repo_full_name, default_branch)
        assert result1 is not None
        
        # Act - Second add should fail with 409
        with pytest.raises(HTTPException) as exc_info:
            await service.add_watched(repo_full_name, default_branch)
        
        # Assert
        assert exc_info.value.status_code == 409
        assert "already watched" in exc_info.value.detail.lower()
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched" --asyncio-mode=auto`
- `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched_valid_repo" --asyncio-mode=auto`
- `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched_duplicate_repo" --asyncio-mode=auto`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک‌های این تسک:
- فایل `backend/tests/test_oversight_service.py` ممکن است وجود نداشته باشد و باید ایجاد شود — این ریسک پایینی دارد.
- اگر متد `add_watched` در `OversightService` وابستگی‌های پیچیده‌ای داشته باشد، mock کردن آن‌ها ممکن است دشوار باشد.
- تغییر در رفتار متد `add_watched` (مثلاً تغییر status code خطا) می‌تواند روی endpointهای `backend/app/api/routes/oversight.py` که از این متد استفاده می‌کنند تأثیر بگذارد.
- تست‌های نوشته شده ممکن است با تغییرات آینده در سرویس شکسته شوند — نیاز به نگهداری دارند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 2: ایجاد تست واحد برای run_task با task معتبر

**Scope:** این مرحله شامل نوشتن تست واحد برای متد run_task از کلاس OversightService است. تست باید با یک task معتبر کار کند و بررسی کند که status task به 'running' تغییر می‌کند. خارج از این مرحله: تست‌های add_watched، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک task به لیست tasks اضافه کند و سپس run_task را فراخوانی کند.
**Key terms:** OversightService, run_task, tests/test_oversight_service.py, test_run_task_changes_status_to_running, task

**بخش مربوط از متن کاربر:**
```
- تست run_task با task معتبر status را به running تغییر می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_run_task_changes_status_to_running", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
تست واحد run_task با task معتبر در OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:نامشخص (فایل deep-read نشده)` — `OversightService.run_task` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، متد run_task در این کلاس قرار دارد.
- `backend/tests/test_oversight_service.py:نامشخص (فایل deep-read نشده)` — `test_run_task_changes_status_to_running` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل تست باید ایجاد یا ویرایش شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` — کلاس OversightService و متد run_task در این فایل تعریف شده است. تست باید این متد را فراخوانی کند.
- `backend/app/models/oversight_task.py` — مدل OversightTask احتمالاً در این فایل تعریف شده است. تست برای ساخت task معتبر به این مدل نیاز دارد.
- `backend/app/api/routes/oversight.py` — این روتر از OversightService استفاده می‌کند. تغییرات در سرویس ممکن است بر API تأثیر بگذارد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل backend/app/services/oversight_service.py وابسته است که حاوی کلاس OversightService و متد run_task است. همچنین به مدل OversightTask (احتمالاً در backend/app/models/oversight_task.py) نیاز دارد. فایل تست backend/tests/test_oversight_service.py باید ایجاد شود. روتر backend/app/api/routes/oversight.py از این سرویس استفاده می‌کند و ممکن است تحت تأثیر قرار گیرد.

## 🔍 Context و وضعیت فعلی
ایجاد تست واحد برای متد run_task از کلاس OversightService. تست باید با یک task معتبر کار کند و بررسی کند که status task به 'running' تغییر می‌کند. خارج از این مرحله: تست‌های add_watched، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک task به لیست tasks اضافه کند و سپس run_task را فراخوانی کند. بخش مربوط از درخواست اصلی کاربر: تست run_task با task معتبر status را به running تغییر می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_run_task_changes_status_to_running", "timeout_seconds": 60}]. کلیدواژه‌ها: OversightService, run_task, tests/test_oversight_service.py, test_run_task_changes_status_to_running, task. با توجه به ساختار پروژه، فایل backend/app/services/oversight_service.py حاوی کلاس OversightService است و فایل tests/test_oversight_service.py باید ایجاد یا ویرایش شود. متد run_task احتمالاً یک task از لیست tasks را دریافت کرده و status آن را به 'running' تغییر می‌دهد. برای تست، باید یک شیء task معتبر (با فیلدهای id، status، name و ...) ساخته شود، به لیست tasks اضافه شود، سپس run_task با آن id فراخوانی شود و در نهایت assert شود که status برابر 'running' است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست test_run_task_changes_status_to_running با موفقیت پاس شود.
- [ ] تست یک task معتبر به لیست tasks اضافه کند.
- [ ] تست run_task را با id task فراخوانی کند.
- [ ] تست بررسی کند که status task به 'running' تغییر کرده است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. فایل tests/test_oversight_service.py را ایجاد یا ویرایش کن. 2. یک تابع تست به نام test_run_task_changes_status_to_running بنویس. 3. در این تابع: یک task معتبر با status='pending' و id='test-1' بساز. 4. task را به لیست tasks سرویس اضافه کن (با فراخوانی متد add_task یا مستقیم). 5. متد run_task را با id='test-1' فراخوانی کن. 6. assert کن که task.status == 'running'. 7. از pytest برای اجرای تست استفاده کن: pytest tests/test_oversight_service.py::test_run_task_changes_status_to_running -v. 8. اطمینان حاصل کن که تست با موفقیت پاس می‌شود.

## 💡 نمونه‌های قبل/بعد
**نمونه کد تست پیشنهادی**

_بعد:_
```
def test_run_task_changes_status_to_running(oversight_service):
    task = OversightTask(id='test-1', name='Test Task', status='pending')
    oversight_service.tasks.append(task)
    result = oversight_service.run_task('test-1')
    assert result.status == 'running'
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_oversight_service.py::test_run_task_changes_status_to_running -v`

## ⚠️ ریسک‌ها و موارد احتیاط
این تسک فقط یک تست واحد اضافه می‌کند و تغییری در کد اصلی سرویس ایجاد نمی‌کند. ریسک خاصی ندارد مگر اینکه متد run_task در OversightService وابستگی‌های خارجی داشته باشد که در تست باید mock شوند. فایل tests/test_oversight_service.py ممکن است وجود نداشته باشد و باید ایجاد شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 3: ایجاد تست واحد برای scan_project با watched_id نامعتبر

**Scope:** این مرحله شامل نوشتن تست واحد برای متد scan_project از کلاس OversightService است. تست باید با یک watched_id نامعتبر (که در لیست watched وجود ندارد) کار کند و بررسی کند که خطای 404 (HTTPException با status code 404) برمی‌گرداند. خارج از این مرحله: تست‌های add_watched، run_task، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک watched_id تصادفی یا غیرموجود استفاده کند.
**Key terms:** OversightService, scan_project, tests/test_oversight_service.py, test_scan_project_invalid_watched_id_returns_404, watched_id

**بخش مربوط از متن کاربر:**
```
- تست scan_project با watched_id نامعتبر 404 برمی‌گرداند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
تست scan_project با watched_id نامعتبر در OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `OversightService.scan_project` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. متد scan_project احتمالاً در این فایل تعریف شده است.
- `backend/tests/test_oversight_service.py` — `test_scan_project_invalid_watched_id_returns_404` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل باید ایجاد یا ویرایش شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/models/oversight.py` — مدل Oversight که watched_id به آن اشاره دارد، احتمالاً در این فایل تعریف شده است.
- `backend/app/services/db_service.py` — سرویس دیتابیس که توسط OversightService برای جستجوی watched_id استفاده می‌شود.
- `backend/app/api/routes/oversight.py` — روتر API که scan_project را از طریق endpoint فراخوانی می‌کند.

## 🌐 نقشهٔ وابستگی‌ها
این تست به فایل‌های backend/app/services/oversight_service.py (شامل کلاس OversightService و متد scan_project)، backend/app/models/oversight.py (مدل داده)، و backend/app/services/db_service.py (سرویس دیتابیس) وابسته است. همچنین از pytest و unittest.mock برای mocking استفاده می‌کند. تغییر در این تست بر هیچ کد اصلی تأثیر نمی‌گذارد، اما باید با ساختار موجود هماهنگ باشد.

## 🔍 Context و وضعیت فعلی
ایجاد تست واحد برای متد scan_project از کلاس OversightService. تست باید با یک watched_id نامعتبر (که در لیست watched وجود ندارد) کار کند و بررسی کند که خطای 404 (HTTPException با status code 404) برمی‌گرداند. خارج از این مرحله: تست‌های add_watched، run_task، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک watched_id تصادفی یا غیرموجود استفاده کند. بخش مربوط از درخواست اصلی کاربر: تست scan_project با watched_id نامعتبر 404 برمی‌گرداند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404", "timeout_seconds": 60}]. کلیدواژه‌ها: OversightService, scan_project, tests/test_oversight_service.py, test_scan_project_invalid_watched_id_returns_404, watched_id. با توجه به deep_context موجود، فایل‌های مرتبط با OversightService شامل backend/app/services/oversight_service.py و backend/app/models/oversight.py هستند. متد scan_project احتمالاً از watched_id برای جستجوی پروژه در دیتابیس استفاده می‌کند و در صورت عدم وجود، HTTPException با status_code=404 صادر می‌کند. تست باید با استفاده از pytest و mocking دیتابیس، این سناریو را پوشش دهد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست test_scan_project_invalid_watched_id_returns_404 در فایل tests/test_oversight_service.py وجود داشته باشد و با pytest بدون خطا اجرا شود.
- [ ] تست از یک watched_id تصادفی یا غیرموجود استفاده کند (نه یک مقدار ثابت واقعی).
- [ ] تست با pytest.raises(HTTPException) بررسی کند که status_code برابر 404 است.
- [ ] هیچ تغییری در کد اصلی سرویس (oversight_service.py) ایجاد نشود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل tests/test_oversight_service.py در صورت عدم وجود، یا افزودن تابع تست جدید به آن. 2. نوشتن تابع test_scan_project_invalid_watched_id_returns_404 که: a. یک watched_id تصادفی (مثلاً uuid4) تولید کند. b. با استفاده از mocking (مثلاً unittest.mock.patch) فراخوانی دیتابیس را شبیه‌سازی کند تا watched_id نامعتبر باشد. c. متد scan_project از OversightService را با آن watched_id فراخوانی کند. d. با pytest.raises(HTTPException) بررسی کند که exception با status_code=404 صادر می‌شود. 3. اطمینان از اینکه تست با timeout_seconds=60 اجرا می‌شود. 4. عدم تغییر در کد اصلی سرویس (فقط تست).

## 💡 نمونه‌های قبل/بعد
**نمونه کد تست پیشنهادی**

_بعد:_
```
import pytest
from unittest.mock import patch
from fastapi import HTTPException
from app.services.oversight_service import OversightService

def test_scan_project_invalid_watched_id_returns_404():
    service = OversightService()
    invalid_watched_id = 'non-existent-id-12345'
    with patch.object(service, '_get_watched_project', return_value=None):
        with pytest.raises(HTTPException) as exc_info:
            service.scan_project(invalid_watched_id)
        assert exc_info.value.status_code == 404
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404 -v`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی: اگر متد scan_project در OversightService به گونه‌ای پیاده‌سازی شده باشد که watched_id را از طریق endpoint دریافت کند (مثلاً در روتر oversight.py)، mocking ممکن است پیچیده‌تر شود. همچنین اگر دیتابیس واقعی در تست استفاده شود، ممکن است watched_id تصادفی باعث خطای غیرمنتظره شود. باید از mocking کامل دیتابیس اطمینان حاصل شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 4: ایجاد تست واحد برای auto_register_watched با repo_full_name معتبر

**Scope:** این مرحله شامل نوشتن تست واحد برای متد auto_register_watched از کلاس OversightService است. تست باید با یک repo_full_name معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک repo_full_name معتبر (مانند 'owner/repo') استفاده کند و وابستگی‌های خارجی (GitHub API) باید mock شوند.
**Key terms:** OversightService, auto_register_watched, tests/test_oversight_service.py, test_auto_register_watched_valid_repo_full_name, repo_full_name

**بخش مربوط از متن کاربر:**
```
- تست auto_register_watched با repo_full_name معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
تست واحد auto_register_watched با repo_full_name معتبر

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_oversight_service.py` — `test_auto_register_watched_valid_repo_full_name` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل در ساختار پروژه موجود نیست و باید ایجاد شود.
- `backend/app/services/oversight_service.py` — `auto_register_watched` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. متد auto_register_watched احتمالاً در این فایل تعریف شده است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_inspector_bridge.py` — این فایل ممکن است از OversightService استفاده کند یا توسط آن استفاده شود. تغییر در auto_register_watched می‌تواند روی عملکرد bridge تأثیر بگذارد.
- `backend/app/services/oversight_verifier.py` — این فایل ممکن است از auto_register_watched برای تأیید مخازن استفاده کند. تست باید mock کردن وابستگی‌های خارجی را در نظر بگیرد.
- `backend/app/services/oversight_upload_session.py` — این فایل ممکن است با auto_register_watched تعامل داشته باشد. تست باید از عدم تأثیر بر upload session اطمینان دهد.

## 🌐 نقشهٔ وابستگی‌ها
این تست به فایل tests/test_oversight_service.py (که باید ایجاد شود) وابسته است. از mock برای شبیه‌سازی وابستگی‌های خارجی (GitHub API) استفاده می‌کند. متد auto_register_watched در oversight_service.py تعریف شده و ممکن است توسط oversight_inspector_bridge.py، oversight_verifier.py، و oversight_upload_session.py فراخوانی شود. تست باید این وابستگی‌ها را در نظر بگیرد و از تغییر در کد اصلی جلوگیری کند.

## 🔍 Context و وضعیت فعلی
ایجاد تست واحد برای متد auto_register_watched از کلاس OversightService. تست باید با یک repo_full_name معتبر (مانند 'owner/repo') کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک repo_full_name معتبر (مانند 'owner/repo') استفاده کند و وابستگی‌های خارجی (GitHub API) باید mock شوند. بخش مربوط از درخواست اصلی کاربر: تست auto_register_watched با repo_full_name معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name", "timeout_seconds": 60}]. کلیدواژه‌ها: OversightService, auto_register_watched, tests/test_oversight_service.py, test_auto_register_watched_valid_repo_full_name, repo_full_name. با توجه به ساختار پروژه، فایل tests/test_oversight_service.py در deep context موجود نیست، اما فایل‌های مرتبط مانند backend/app/services/oversight_service.py و backend/app/services/oversight_inspector_bridge.py و backend/app/services/oversight_verifier.py در ساختار پروژه دیده می‌شوند. متد auto_register_watched احتمالاً در oversight_service.py تعریف شده و از GitHub API برای ثبت مخزن استفاده می‌کند. تست باید با mock کردن وابستگی‌های خارجی (مانند requests به GitHub API) و استفاده از repo_full_name معتبر، موفقیت عملیات را تأیید کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست test_auto_register_watched_valid_repo_full_name با pytest بدون خطا اجرا شود.
- [ ] تست از یک repo_full_name معتبر مانند 'owner/repo' استفاده کند.
- [ ] وابستگی‌های خارجی (GitHub API) در تست mock شده باشند.
- [ ] تست موفقیت عملیات را با assert تأیید کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل tests/test_oversight_service.py در صورت عدم وجود. 2. نوشتن تابع تست test_auto_register_watched_valid_repo_full_name که: - یک repo_full_name معتبر مانند 'owner/repo' را به عنوان ورودی به auto_register_watched بدهد. - وابستگی‌های خارجی (GitHub API) را با unittest.mock.patch یا pytest-mock mock کند. - تأیید کند که متد بدون خطا اجرا شده و نتیجه موفقیت‌آمیز (مثلاً True یا دیکشنری با status=ok) برگرداند. - از assert برای بررسی خروجی استفاده کند. 3. اطمینان از اینکه تست درون پکیج tests قرار دارد و با pytest قابل اجراست. 4. عدم تغییر در کد اصلی سرویس (فقط تست).

## 💡 نمونه‌های قبل/بعد
**نمونه تست پیشنهادی**

_بعد:_
```
import pytest
from unittest.mock import patch, MagicMock
from backend.app.services.oversight_service import OversightService

@pytest.mark.asyncio
async def test_auto_register_watched_valid_repo_full_name():
    service = OversightService()
    repo_full_name = 'owner/repo'
    with patch('backend.app.services.oversight_service.requests.post') as mock_post:
        mock_post.return_value = MagicMock(status_code=200, json=lambda: {'status': 'ok'})
        result = await service.auto_register_watched(repo_full_name)
        assert result is True or (isinstance(result, dict) and result.get('status') == 'ok')
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name -v`

## ⚠️ ریسک‌ها و موارد احتیاط
فایل tests/test_oversight_service.py در ساختار پروژه موجود نیست و باید ایجاد شود. اگر متد auto_register_watched در oversight_service.py تعریف نشده باشد، تست شکست می‌خورد. همچنین mock کردن وابستگی‌های خارجی باید دقیقاً با امضای متد مطابقت داشته باشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 5: اصلاح تابع validate_token برای بررسی expiry

**Scope:** این مرحله شامل اصلاح تابع validate_token در فایل backend/app/services/oversight_service.py است. تابع باید توکن JWT را decode کند و فیلد exp (expiration) را با زمان فعلی (UTC) مقایسه کند. اگر توکن منقضی شده باشد، تابع باید False برگرداند یا HTTPException با status code 401 raise کند. خارج از این مرحله: نوشتن تست واحد برای expiry، تغییر endpoint /api/login، و تغییر در frontend. نکته حیاتی: باید از کتابخانه PyJWT برای decode و بررسی expiry استفاده شود.
**Key terms:** validate_token, backend/app/services/oversight_service.py, exp, expired, ExpiredSignatureError, expiration, HTTPException

**بخش مربوط از متن کاربر:**
```
## 🎯 هدف (خلاصه ساختاریافته)
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — تابع اصلی که باید اصلاح شود
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```
```

## 🎯 هدف (خلاصه ساختاریافته)
اصلاح validate_token برای بررسی expiry توکن JWT

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — این فایل deep-read نشده است — مجری باید مسیر و محتوای دقیق را خود تأیید کند. بر اساس متن کاربر، تابع validate_token در این محدوده خطوط قرار دارد.
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص) — بر اساس ساختار پروژه، از FastAPI و PyJWT استفاده می‌شود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` — احتمالاً endpoint /api/login را شامل می‌شود که validate_token را فراخوانی می‌کند. تغییر در validate_token بر این روتر تأثیر می‌گذارد.
- `backend/app/services/oversight_verifier.py` — ممکن است از validate_token برای تأیید توکن‌ها استفاده کند. تغییر در validate_token بر این سرویس تأثیر می‌گذارد.
- `backend/app/services/oversight_inspector_bridge.py` — ممکن است از validate_token برای اعتبارسنجی توکن‌ها در ارتباط با inspector استفاده کند.

## 🌐 نقشهٔ وابستگی‌ها
تابع validate_token در فایل backend/app/services/oversight_service.py قرار دارد. این تابع احتمالاً توسط روترهای مختلف (مانند backend/app/api/routes/oversight.py) و سرویس‌های دیگر (مانند oversight_verifier.py و oversight_inspector_bridge.py) فراخوانی می‌شود. تغییر در این تابع بر تمام نقاطی که از آن استفاده می‌کنند تأثیر می‌گذارد. همچنین، endpoint /api/login که در خارج از این مرحله است، از این تابع استفاده می‌کند.

## 🔍 Context و وضعیت فعلی
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد. این مرحله شامل اصلاح تابع validate_token در فایل backend/app/services/oversight_service.py است. تابع باید توکن JWT را decode کند و فیلد exp (expiration) را با زمان فعلی (UTC) مقایسه کند. اگر توکن منقضی شده باشد، تابع باید False برگرداند یا HTTPException با status code 401 raise کند. خارج از این مرحله: نوشتن تست واحد برای expiry، تغییر endpoint /api/login، و تغییر در frontend. نکته حیاتی: باید از کتابخانه PyJWT برای decode و بررسی expiry استفاده شود.

--- بخش مربوط از درخواست اصلی کاربر ---
## 🎯 هدف (خلاصه ساختاریافته)
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — تابع اصلی که باید اصلاح شود
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```

--- کلیدواژه‌ها ---
validate_token, backend/app/services/oversight_service.py, exp, expired, ExpiredSignatureError, expiration, HTTPException

بر اساس بررسی ساختار پروژه، فایل backend/app/services/oversight_service.py در مسیر مشخص شده وجود دارد. این فایل deep-read نشده است، بنابراین محتوای دقیق خطوط 245-289 در دسترس نیست. با این حال، بر اساس متن کاربر، تابع validate_token در این فایل قرار دارد و نیاز به اصلاح دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تابع validate_token باید توکن JWT را decode کرده و فیلد exp را با زمان فعلی UTC مقایسه کند.
- [ ] اگر توکن منقضی شده باشد، تابع باید HTTPException با status code 401 raise کند.
- [ ] اگر توکن معتبر باشد، تابع باید True برگرداند.
- [ ] کتابخانه PyJWT باید برای decode و بررسی expiry استفاده شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. فایل backend/app/services/oversight_service.py را باز کرده و تابع validate_token را در خطوط 245-289 پیدا کنید.
2. کتابخانه PyJWT را به فایل اضافه کنید: import jwt
3. تابع validate_token را به صورت زیر بازنویسی کنید:
   - توکن JWT را با استفاده از jwt.decode(token, options={"verify_signature": False}) decode کنید (چون کلید مخفی در دسترس نیست، فقط expiry را بررسی می‌کنیم).
   - فیلد exp را از payload استخراج کنید.
   - زمان فعلی UTC را با datetime.utcnow() بدست آورید.
   - اگر exp < زمان فعلی، False برگردانید یا HTTPException(status_code=401, detail="Token expired") raise کنید.
   - اگر توکن معتبر است، True برگردانید.
4. استثنای jwt.ExpiredSignatureError را catch کنید و در صورت وقوع، False برگردانید.
5. import datetime را به فایل اضافه کنید.
6. از fastapi import HTTPException را اضافه کنید (اگر قبلاً نیست).
7. توجه: خارج از این مرحله، نوشتن تست واحد برای expiry، تغییر endpoint /api/login، و تغییر در frontend است.

## 💡 نمونه‌های قبل/بعد
**قبل از اصلاح**

_قبل:_
```
def validate_token(token: str) -> bool:
    # ⚠️ هیچ بررسی expiry ندارد
    if token:
        return True
    return False
```

_بعد:_
```
import jwt
from datetime import datetime, timezone
from fastapi import HTTPException

def validate_token(token: str) -> bool:
    try:
        payload = jwt.decode(token, options={"verify_signature": False})
        exp = payload.get("exp")
        if exp is None:
            return False
        if datetime.fromtimestamp(exp, tz=timezone.utc) < datetime.now(timezone.utc):
            raise HTTPException(status_code=401, detail="Token expired")
        return True
    except jwt.ExpiredSignatureError:
        raise HTTPException(status_code=401, detail="Token expired")
    except jwt.InvalidTokenError:
        return False
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/ -k "validate_token"`
- `python -c "from backend.app.services.oversight_service import validate_token; print(validate_token('test_token'))"`

## ⚠️ ریسک‌ها و موارد احتیاط
این تابع احتمالاً توسط چندین روتر و سرویس فراخوانی می‌شود (مانند oversight.py, oversight_verifier.py, oversight_inspector_bridge.py). تغییر در رفتار آن (raise کردن HTTPException به جای بازگرداندن False) ممکن است باعث شکست در نقاطی شود که انتظار bool دارند. همچنین، عدم دسترسی به کلید مخفی JWT ممکن است نیاز به تغییر رویکرد داشته باشد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 6: ایجاد تست واحد برای validate_token با توکن منقضی

**Scope:** این مرحله شامل نوشتن تست واحد برای تابع validate_token است که بررسی می‌کند با یک توکن JWT منقضی شده، تابع False برمی‌گرداند یا خطای 401 صادر می‌کند. تست باید در فایل tests/test_oversight_service.py اضافه شود. خارج از این مرحله: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید یک توکن JWT با exp در گذشته بسازد و validate_token را فراخوانی کند.
**Key terms:** validate_token, tests/test_oversight_service.py, test_validate_token_expired, exp, expired, JWT

**بخش مربوط از متن کاربر:**
```
- تست واحد جدید برای expiry اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_validate_token_expired", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تست واحد validate_token با توکن منقضی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `tests/test_oversight_service.py` — `test_validate_token_expired` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل در ساختار پروژه موجود است (backend/tests/test_runtime_verify_integration.py و ... اما test_oversight_service.py در لیست نیست — احتمالاً باید ایجاد شود یا فایل مشابهی پیدا شود). بر اساس ساختار سطحی، فایل tests/test_oversight_service.py در لیست فایل‌های پروژه دیده نمی‌شود. مجری باید فایل مناسب را در tests/ پیدا کند یا ایجاد کند.
- `backend/app/services/oversight_service.py` — `validate_token` — فایل deep-read نشده — مجری باید تابع validate_token را در این فایل پیدا کرده و signature آن را مطالعه کند. این فایل در ساختار پروژه موجود است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص) — بر اساس ساختار پروژه، احتمالاً Python با FastAPI، pytest برای تست، PyJWT برای JWT

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` — محل تعریف تابع validate_token که تست باید آن را فراخوانی کند
- `backend/app/api/routes/oversight.py` — احتمالاً از validate_token در endpointها استفاده می‌کند — تست باید behaviour endpoint را هم پوشش دهد
- `backend/app/services/oversight_verifier.py` — ممکن است validate_token را فراخوانی کند یا وابسته به آن باشد

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل tests/test_oversight_service.py (که باید ایجاد یا تکمیل شود) و backend/app/services/oversight_service.py (محل تابع validate_token) وابسته است. همچنین به کتابخانه PyJWT برای ساخت توکن منقضی نیاز دارد. فایل backend/app/api/routes/oversight.py ممکن است از validate_token استفاده کند و تغییر در behaviour آن روی endpointها تأثیر می‌گذارد. فایل backend/app/services/oversight_verifier.py نیز ممکن است وابسته باشد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد تست واحد برای تابع validate_token با توکن JWT منقضی شده دارد. این تست باید در فایل tests/test_oversight_service.py اضافه شود و بررسی کند که با یک توکن JWT منقضی شده، تابع False برمی‌گرداند یا خطای 401 صادر می‌کند. کلیدواژه‌های اصلی: validate_token, tests/test_oversight_service.py, test_validate_token_expired, exp, expired, JWT. خارج از این مرحله: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید یک توکن JWT با exp در گذشته بسازد و validate_token را فراخوانی کند. بخش مربوط از درخواست اصلی کاربر: 'تست واحد جدید برای expiry اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_validate_token_expired", "timeout_seconds": 60}]'. فایل هدف tests/test_oversight_service.py در ساختار پروژه موجود است اما deep-read نشده است. تابع validate_token احتمالاً در فایل backend/app/services/oversight_service.py تعریف شده است که آن هم deep-read نشده است. بر اساس ساختار سطحی پروژه، این فایل‌ها وجود دارند و باید توسط مجری تأیید شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست test_validate_token_expired در فایل tests/test_oversight_service.py وجود داشته باشد و با توکن JWT منقضی شده کار کند
- [ ] تست باید یک توکن JWT با exp در گذشته (مثلاً timestamp یک ساعت قبل) بسازد
- [ ] تست باید validate_token را فراخوانی کرده و نتیجه False یا exception 401 را assert کند
- [ ] تست باید با pytest به تنهایی قابل اجرا باشد و timeout 60 ثانیه داشته باشد
- [ ] خارج از scope: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched انجام نشود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. مطالعه کد تابع validate_token در فایل backend/app/services/oversight_service.py برای فهم signature و رفتار آن. 2. ایجاد تابع تست test_validate_token_expired در فایل tests/test_oversight_service.py. 3. در این تست: یک توکن JWT با exp در گذشته (مثلاً timestamp یک ساعت قبل) با استفاده از کتابخانه PyJWT بسازید. 4. تابع validate_token را با این توکن فراخوانی کنید. 5. assert کنید که نتیجه False است یا exception 401 صادر می‌شود (بر اساس رفتار واقعی تابع). 6. اطمینان از اینکه تست با pytest به تنهایی قابل اجراست و timeout 60 ثانیه دارد. 7. خارج از scope: اصلاح خود تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched.

## 💡 نمونه‌های قبل/بعد
**نمونه تست مشابه در پروژه (در صورت وجود)**

_قبل:_
```
فایل deep-read نشده — نمونه‌ای موجود نیست
```

_بعد:_
```
def test_validate_token_expired():
    expired_token = jwt.encode({'exp': datetime.utcnow() - timedelta(hours=1)}, 'secret', algorithm='HS256')
    result = validate_token(expired_token)
    assert result is False
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest tests/test_oversight_service.py::test_validate_token_expired -v --timeout=60`

## ⚠️ ریسک‌ها و موارد احتیاط
فایل tests/test_oversight_service.py در ساختار پروژه موجود نیست یا deep-read نشده است — مجری باید فایل مناسب را پیدا کند یا ایجاد کند. تابع validate_token در oversight_service.py deep-read نشده است — ممکن است signature یا behaviour متفاوتی داشته باشد. اگر validate_token به database یا state خارجی وابسته باشد، تست可能需要 fixture یا mock. تغییر در behaviour validate_token روی endpointهای oversight تأثیر می‌گذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 7: ایجاد فایل تست tests/test_oversight_service.py

**Scope:** این مرحله شامل ایجاد فایل tests/test_oversight_service.py است. فایل باید شامل تست‌های واحد برای متدهای اصلی OversightService باشد: add_watched, update_watched, run_task, scan_project, status_summary. خارج از این مرحله: نوشتن تست‌های خاص برای validate_token (که در مرحله 7 انجام می‌شود)، تست‌های add_watched با repo معتبر و تکراری (که در مراحل 1 و 2 انجام می‌شود)، تست run_task (مرحله 3)، تست scan_project (مرحله 4)، تست auto_register_watched (مرحله 5). نکته حیاتی: فایل باید با pytest و pytest-asyncio سازگار باشد.
**Key terms:** tests/test_oversight_service.py, OversightService, add_watched, update_watched, run_task, scan_project, status_summary, pytest, pytest-asyncio

**بخش مربوط از متن کاربر:**
```
- فایل tests/test_oversight_service.py ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["def test_", "class Test"], "files_hint": ["tests/test_oversight_service.py"]}]
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد فایل تست tests/test_oversight_service.py برای OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_oversight_service.py:1-100` — `فایل جدید` — این فایل باید ایجاد شود. محتوای deep-read موجود نیست — مجری باید بر اساس ساختار OversightService و تست‌های موجود (مانند test_runtime_verify_stage1.py) الگوبرداری کند.
- `backend/app/services/oversight_service.py:1-50` — `OversightService` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. متدهای add_watched, update_watched, run_task, scan_project, status_summary در این کلاس تعریف شده‌اند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_settings.py` (سطر 1) — احتمالاً توسط OversightService برای مدیریت تنظیمات استفاده می‌شود و در تست‌ها باید mock شود.
- `backend/app/services/oversight_progress.py` (سطر 1) — احتمالاً برای ردیابی پیشرفت اسکن/تسک استفاده می‌شود و در تست status_summary نقش دارد.
- `backend/app/models/inspector_session.py` (سطر 1) — مدل داده‌ای که احتمالاً توسط OversightService برای ذخیره جلسات بازرسی استفاده می‌شود و در تست scan_project نیاز است.
- `backend/tests/test_runtime_verify_stage1.py` (سطر 1) — الگوی ساختار تست (استفاده از pytest-asyncio، fixtures) برای الگوبرداری در فایل جدید.

## 🌐 نقشهٔ وابستگی‌ها
فایل tests/test_oversight_service.py به کلاس OversightService در backend/app/services/oversight_service.py وابسته است. این کلاس احتمالاً از backend/app/services/oversight_settings.py برای تنظیمات، backend/app/services/oversight_progress.py برای پیشرفت، backend/app/models/inspector_session.py برای مدل داده، و backend/app/services/db_service.py برای ذخیره‌سازی استفاده می‌کند. همچنین ممکن است به backend/app/services/oversight_verifier.py و backend/app/services/oversight_deep_scan_service.py وابسته باشد. فایل تست باید این وابستگی‌ها را mock کند تا تست‌ها ایزوله باشند. ساختار تست باید مشابه backend/tests/test_runtime_verify_stage1.py باشد که از pytest.mark.asyncio و fixtures استفاده می‌کند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد فایل tests/test_oversight_service.py را دارد که شامل تست‌های واحد برای متدهای اصلی OversightService باشد: add_watched, update_watched, run_task, scan_project, status_summary. این فایل باید با pytest و pytest-asyncio سازگار باشد. کاربر تأکید کرده که خارج از این مرحله موارد زیر انجام شود: نوشتن تست‌های خاص برای validate_token (که در مرحله 7 انجام می‌شود)، تست‌های add_watched با repo معتبر و تکراری (که در مراحل 1 و 2 انجام می‌شود)، تست run_task (مرحله 3)، تست scan_project (مرحله 4)، تست auto_register_watched (مرحله 5). کلیدواژه‌های اصلی: tests/test_oversight_service.py, OversightService, add_watched, update_watched, run_task, scan_project, status_summary, pytest, pytest-asyncio. در ساختار پروژه، فایل backend/app/services/oversight_service.py وجود دارد اما محتوای آن deep-read نشده است. همچنین فایل‌های مرتبط مانند backend/app/models/inspector_session.py و backend/app/services/oversight_settings.py و backend/app/services/oversight_progress.py در پروژه موجود هستند. فایل‌های تست موجود در پروژه شامل test_runtime_verify_stage1.py تا test_runtime_verify_stage9.py و test_verify_v7.py هستند که می‌توان از ساختار آن‌ها الگوبرداری کرد. کاربر دو معیار تأیید مشخص کرده: 1) verify_method=static با grep_patterns شامل 'def test_' و 'class Test' و files_hint شامل tests/test_oversight_service.py. 2) verify_method=backend_test با test_node مساوی tests/test_oversight_service.py و timeout_seconds=60.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل tests/test_oversight_service.py باید حداقل ۵ تابع تست با نام‌های test_add_watched, test_update_watched, test_run_task, test_scan_project, test_status_summary داشته باشد.
- [ ] همه تست‌ها باید با pytest-asyncio سازگار باشند (استفاده از async def و pytest.mark.asyncio).
- [ ] فایل باید با pytest قابل اجرا باشد و حداقل ۴ تست از ۵ تست با موفقیت عبور کنند.
- [ ] تست add_watched باید شامل بررسی افزودن یک repo به لیست watched باشد (بدون نیاز به repo معتبر واقعی).
- [ ] تست status_summary باید بررسی کند که خروجی شامل فیلدهای مورد انتظار (مانند total_watched, active_tasks) است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل tests/test_oversight_service.py در ریشه پروژه (backend/tests/). 2. نوشتن حداقل ۵ تابع تست با نام‌های test_add_watched, test_update_watched, test_run_task, test_scan_project, test_status_summary. 3. هر تست باید از pytest-asyncio برای تست ناهمزمان استفاده کند (async def). 4. ایجاد یک fixture برای نمونه OversightService با mock کردن وابستگی‌ها (مانند db_service, settings, progress). 5. تست add_watched: بررسی افزودن یک repo به لیست watched با داده‌های معتبر. 6. تست update_watched: بررسی به‌روزرسانی تنظیمات یک repo watched. 7. تست run_task: بررسی اجرای یک task خاص (بدون نیاز به اجرای واقعی). 8. تست scan_project: بررسی شروع اسکن برای یک پروژه. 9. تست status_summary: بررسی بازگرداندن خلاصه وضعیت. 10. اطمینان از سازگاری با pytest (استفاده از pytest.mark.asyncio). 11. اضافه کردن فایل __init__.py در tests/ اگر وجود ندارد. 12. اجرای pytest tests/test_oversight_service.py برای تأیید.

## 💡 نمونه‌های قبل/بعد
**نمونه ساختار تست (بر اساس test_runtime_verify_stage1.py)**

_بعد:_
```
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from backend.app.services.oversight_service import OversightService

@pytest.fixture
def oversight_service():
    service = OversightService()
    service.db_service = AsyncMock()
    service.settings = MagicMock()
    service.progress = MagicMock()
    return service

@pytest.mark.asyncio
async def test_add_watched(oversight_service):
    result = await oversight_service.add_watched(repo_url='https://github.com/user/repo')
    assert result is True
    oversight_service.db_service.insert.assert_called_once()
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v --timeout=60`
- `python -m pytest backend/tests/test_oversight_service.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
فایل OversightService (backend/app/services/oversight_service.py) deep-read نشده است، بنابراین ممکن است وابستگی‌های واقعی آن ناشناخته باشند. اگر mockها با پیاده‌سازی واقعی تطابق نداشته باشند، تست‌ها fail می‌شوند. همچنین تغییرات در OversightService در آینده ممکن است تست‌ها را بشکند. توصیه می‌شود پس از ایجاد فایل، محتوای OversightService بررسی و mockها به‌روزرسانی شوند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 8: نوشتن تست واحد برای update_watched

**Scope:** این مرحله شامل نوشتن تست واحد برای متد update_watched از کلاس OversightService است. تست باید یک watched project موجود را به‌روزرسانی کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، status_summary، auto_register_watched. نکته حیاتی: تست باید ابتدا یک watched project اضافه کند و سپس آن را به‌روزرسانی کند.
**Key terms:** OversightService, update_watched, tests/test_oversight_service.py, watched

**بخش مربوط از متن کاربر:**
```
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست واحد برای update_watched در OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `OversightService.update_watched` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. متد update_watched در این کلاس تعریف شده است.
- `backend/tests/test_oversight_service.py` — `test_update_watched` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. تست جدید باید به این فایل اضافه شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_settings.py` — احتمالاً تنظیمات مربوط به watched projects در این فایل مدیریت می‌شود و ممکن است در متد update_watched استفاده شود.
- `backend/app/services/oversight_verifier.py` — ممکن است پس از به‌روزرسانی یک watched project، فرآیند verification انجام شود.
- `backend/app/models/project.py` — مدل Project احتمالاً برای نمایش watched project استفاده می‌شود و تغییرات روی آن تأثیر می‌گذارد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل backend/app/services/oversight_service.py وابسته است که کلاس OversightService را تعریف می‌کند. متد update_watched احتمالاً از توابع کمکی در فایل‌های oversight_settings.py، oversight_verifier.py، و مدل‌های دیتابیس مانند project.py استفاده می‌کند. همچنین فایل tests/test_oversight_service.py باید برای تست جدید به‌روزرسانی شود. وابستگی‌های دیگر شامل pytest برای اجرای تست و احتمالاً mock برای شبیه‌سازی دیتابیس است.

## 🔍 Context و وضعیت فعلی
این تسک شامل نوشتن تست واحد برای متد update_watched از کلاس OversightService است. کاربر درخواست کرده که تست باید یک watched project موجود را به‌روزرسانی کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، status_summary، auto_register_watched. نکته حیاتی: تست باید ابتدا یک watched project اضافه کند و سپس آن را به‌روزرسانی کند. بخش مربوط از درخواست اصلی کاربر: 'حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]'. کلیدواژه‌ها: OversightService, update_watched, tests/test_oversight_service.py, watched. با توجه به deep context موجود، فایل backend/app/services/oversight_service.py حاوی کلاس OversightService است و متد update_watched در آن تعریف شده. همچنین فایل tests/test_oversight_service.py برای تست‌ها در نظر گرفته شده است. ساختار پروژه نشان می‌دهد که فایل‌های مرتبط با oversight شامل oversight_service.py، oversight_settings.py، oversight_verifier.py و دیگر فایل‌های موجود در backend/app/services/ هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست test_update_watched در فایل tests/test_oversight_service.py وجود داشته باشد و با دستور pytest backend/tests/test_oversight_service.py::test_update_watched اجرا شود.
- [ ] تست ابتدا یک watched project اضافه کند (با add_watched یا mock) و سپس آن را به‌روزرسانی کند.
- [ ] تست موفقیت عملیات به‌روزرسانی را با assert تأیید کند (مثلاً بررسی status code یا پیام موفقیت).
- [ ] تست به صورت ایزوله اجرا شود و وابستگی به دیتابیس واقعی نداشته باشد (استفاده از mock یا in-memory database).
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. مطالعه کد متد update_watched در backend/app/services/oversight_service.py برای درک امضای تابع، پارامترها، و رفتار مورد انتظار.
2. ایجاد یا ویرایش فایل tests/test_oversight_service.py برای افزودن تابع تست test_update_watched.
3. در تست، ابتدا یک watched project با استفاده از متد add_watched (یا mock کردن آن) اضافه شود.
4. سپس متد update_watched با داده‌های جدید فراخوانی شود.
5. نتیجه عملیات (موفقیت یا خطا) با استفاده از assert بررسی شود.
6. اطمینان از اینکه تست به صورت ایزوله اجرا می‌شود و وابستگی به دیتابیس واقعی ندارد (استفاده از mock یا in-memory database).
7. اجرای تست با دستور pytest backend/tests/test_oversight_service.py::test_update_watched و تأیید پاس شدن آن.

## 💡 نمونه‌های قبل/بعد
**افزودن تست update_watched**

_قبل:_
```
# فایل tests/test_oversight_service.py (فعلی)
# هیچ تستی برای update_watched وجود ندارد
```

_بعد:_
```
def test_update_watched(self):
    # Arrange: ابتدا یک watched project اضافه کن
    project_data = {'id': 'test-123', 'name': 'Test Project'}
    self.service.add_watched(project_data)
    
    # Act: به‌روزرسانی کن
    update_data = {'name': 'Updated Project'}
    result = self.service.update_watched('test-123', update_data)
    
    # Assert: موفقیت عملیات
    self.assertTrue(result['success'])
    self.assertEqual(result['project']['name'], 'Updated Project')
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py::test_update_watched -v`

## ⚠️ ریسک‌ها و موارد احتیاط
این تسک محدود به نوشتن تست است و تغییری در کد اصلی ایجاد نمی‌کند، بنابراین ریسک خاصی ندارد. با این حال، اگر تست به درستی mock نشود، ممکن است به دیتابیس واقعی وابسته شود و در محیط‌های مختلف شکست بخورد. همچنین اگر متد update_watched در آینده تغییر کند، تست باید به‌روزرسانی شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 9: نوشتن تست واحد برای status_summary

**Scope:** این مرحله شامل نوشتن تست واحد برای متد status_summary از کلاس OversightService است. تست باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند. خارج از این مرحله: تست‌های add_watched، update_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید چند watched project و task اضافه کند و سپس status_summary را فراخوانی کند.
**Key terms:** OversightService, status_summary, tests/test_oversight_service.py

**بخش مربوط از متن کاربر:**
```
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست واحد برای status_summary در OversightService

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:نامشخص — فایل deep-read نشده است` — `OversightService.status_summary` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. متد status_summary در کلاس OversightService باید بررسی شود.
- `backend/tests/test_oversight_service.py:نامشخص — فایل deep-read نشده است` — `TestOversightServiceStatusSummary` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. تست‌های مربوط به status_summary باید در این فایل نوشته شوند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = (نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` — فایل اصلی حاوی کلاس OversightService و متد status_summary که باید تست شود.
- `backend/app/models/oversight.py` — مدل‌های OversightProject و OversightTask که توسط OversightService استفاده می‌شوند و در تست‌ها برای ایجاد نمونه‌های آزمایشی نیاز هستند.
- `backend/app/services/db_service.py` — سرویس دیتابیس که OversightService برای ذخیره و بازیابی پروژه‌ها و task‌ها از آن استفاده می‌کند. در تست‌ها باید mock شود.
- `backend/app/api/routes/oversight.py` — روتر API که از OversightService استفاده می‌کند و ممکن است status_summary را فراخوانی کند. تغییرات در سرویس می‌تواند روی این روتر تأثیر بگذارد.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های زیر وابسته است:
- `backend/app/services/oversight_service.py`: فایل اصلی که متد status_summary در آن تعریف شده است.
- `backend/app/models/oversight.py`: مدل‌های OversightProject و OversightTask که برای ایجاد داده‌های آزمایشی در تست‌ها استفاده می‌شوند.
- `backend/app/services/db_service.py`: سرویس دیتابیس که باید در تست‌ها mock شود تا از وابستگی به دیتابیس واقعی جلوگیری شود.
- `backend/tests/test_oversight_service.py`: فایل تست که تست‌های جدید باید در آن اضافه شوند.
- `backend/app/api/routes/oversight.py`: روتر API که از OversightService استفاده می‌کند و ممکن است تحت تأثیر تغییرات قرار گیرد.

## 🔍 Context و وضعیت فعلی
این تسک شامل نوشتن تست واحد برای متد status_summary از کلاس OversightService است. تست باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند. خارج از این مرحله: تست‌های add_watched، update_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید چند watched project و task اضافه کند و سپس status_summary را فراخوانی کند.

--- بخش مربوط از درخواست اصلی کاربر ---
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]

--- کلیدواژه‌ها ---
OversightService, status_summary, tests/test_oversight_service.py

شواهد در کد واقعی پروژه: فایل backend/app/services/oversight_service.py حاوی کلاس OversightService است. متد status_summary در این فایل تعریف شده است. فایل tests/test_oversight_service.py در ساختار پروژه موجود است و باید تست‌های مربوطه در آن نوشته شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست status_summary باید بتواند چند watched project و task اضافه کند و سپس status_summary را فراخوانی کند.
- [ ] تست status_summary باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند و شامل تعداد کل پروژه‌ها، تعداد پروژه‌های فعال، تعداد پروژه‌های متوقف، تعداد کل task‌ها، تعداد task‌های در حال اجرا، تعداد task‌های تکمیل‌شده و تعداد task‌های ناموفق باشد.
- [ ] تست status_summary باید با لیست خالی از پروژه‌ها (بدون هیچ پروژه نظارت‌شده) کار کند و خطا ندهد.
- [ ] تست status_summary باید با پروژه‌هایی که task ندارند کار کند و خطا ندهد.
- [ ] تست‌های add_watched, update_watched, run_task, scan_project, status_summary باید حداقل ۵ تست مجزا باشند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این تسک، مراحل زیر باید انجام شود:

1. **بررسی فایل تست موجود**: فایل `tests/test_oversight_service.py` را باز کرده و ساختار فعلی آن را بررسی کنید. اگر فایل خالی است یا تستی ندارد، یک کلاس تست جدید به نام `TestOversightServiceStatusSummary` ایجاد کنید.

2. **ایجاد fixture برای OversightService**: یک fixture pytest برای نمونه‌سازی از `OversightService` ایجاد کنید. این fixture باید وابستگی‌های سرویس (مانند دیتابیس یا repository) را mock کند.

3. **نوشتن تست status_summary با watched projects و tasks**:
   - چند پروژه نظارت‌شده (watched project) با وضعیت‌های مختلف (مثلاً active, paused, completed) اضافه کنید.
   - برای هر پروژه، چند task با وضعیت‌های مختلف (مثلاً pending, running, completed, failed) اضافه کنید.
   - متد `status_summary` را فراخوانی کرده و خروجی را بررسی کنید.

4. **بررسی محتوای خروجی status_summary**:
   - اطمینان حاصل کنید که خروجی شامل تعداد کل پروژه‌ها، تعداد پروژه‌های فعال، تعداد پروژه‌های متوقف، تعداد کل task‌ها، تعداد task‌های در حال اجرا، تعداد task‌های تکمیل‌شده و تعداد task‌های ناموفق است.
   - بررسی کنید که خلاصه وضعیت برای هر پروژه به‌درستی محاسبه شده است.

5. **نوشتن تست‌های لبه (edge cases)**:
   - تست با لیست خالی از پروژه‌ها (بدون هیچ پروژه نظارت‌شده).
   - تست با پروژه‌هایی که task ندارند.
   - تست با تعداد زیادی پروژه و task برای بررسی performance.

6. **اجرای تست‌ها**: پس از نوشتن تست‌ها، با دستور `pytest tests/test_oversight_service.py -v` آن‌ها را اجرا کنید.

## 💡 نمونه‌های قبل/بعد
**نمونه تست برای status_summary**

_قبل:_
```
# فایل tests/test_oversight_service.py (فعلاً خالی یا بدون تست مرتبط)
```

_بعد:_
```
import pytest
from unittest.mock import MagicMock, patch
from backend.app.services.oversight_service import OversightService

@pytest.fixture
def oversight_service():
    service = OversightService()
    service.db = MagicMock()
    return service

def test_status_summary_with_projects_and_tasks(oversight_service):
    # Arrange
    mock_projects = [
        {'id': 'proj1', 'name': 'Project 1', 'status': 'active'},
        {'id': 'proj2', 'name': 'Project 2', 'status': 'paused'},
        {'id': 'proj3', 'name': 'Project 3', 'status': 'completed'},
    ]
    mock_tasks = [
        {'id': 'task1', 'project_id': 'proj1', 'status': 'running'},
        {'id': 'task2', 'project_id': 'proj1', 'status': 'completed'},
        {'id': 'task3', 'project_id': 'proj2', 'status': 'pending'},
        {'id': 'task4', 'project_id': 'proj2', 'status': 'failed'},
        {'id': 'task5', 'project_id': 'proj3', 'status': 'completed'},
    ]
    oversight_service.get_all_watched_projects = MagicMock(return_value=mock_projects)
    oversight_service.get_tasks_for_project = MagicMock(side_effect=lambda proj_id: [t for t in mock_tasks if t['project_id'] == proj_id])
    
    # Act
    result = oversight_service.status_summary()
    
    # Assert
    assert result['total_projects'] == 3
    assert result['active_projects'] == 1
    assert result['paused_projects'] == 1
    assert result['completed_projects'] == 1
    assert result['total_tasks'] == 5
    assert result['running_tasks'] == 1
    assert result['completed_tasks'] == 2
    assert result['failed_tasks'] == 1
    assert result['pending_tasks'] == 1
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_service.py -v`
- `pytest backend/tests/test_oversight_service.py::test_status_summary_with_projects_and_tasks -v`
- `pytest backend/tests/test_oversight_service.py::test_status_summary_empty_projects -v`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی این تسک این است که متد status_summary ممکن است به دیتابیس واقعی وابسته باشد و mock کردن آن نیازمند درک عمیق از ساختار OversightService و وابستگی‌های آن است. همچنین، اگر متد status_summary از متدهای دیگری مانند get_all_watched_projects یا get_tasks_for_project استفاده کند، باید آن‌ها نیز به‌درستی mock شوند. فایل `backend/app/services/oversight_service.py` ممکن است وابستگی‌های پنهانی داشته باشد که در تست‌ها باید در نظر گرفته شوند. تغییر در نحوه محاسبه status_summary می‌تواند روی روتر `backend/app/api/routes/oversight.py` که این متد را فراخوانی می‌کند تأثیر بگذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 10: اجرای همه تست‌ها و اطمینان از عبور

**Scope:** این مرحله شامل اجرای همه تست‌های موجود در فایل tests/test_oversight_service.py با pytest است. باید اطمینان حاصل شود که همه تست‌ها پاس می‌شوند. خارج از این مرحله: نوشتن تست‌های جدید، اصلاح کد اصلی سرویس. نکته حیاتی: اگر تستی fail شود، باید خطا بررسی و رفع شود.
**Key terms:** tests/test_oversight_service.py, pytest

**بخش مربوط از متن کاربر:**
```
- همه تست‌ها پاس شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای تست‌های oversight_service با pytest

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_oversight_service.py` — `فایل تست کامل` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل تست اصلی که باید با pytest اجرا شود.
- `backend/app/services/oversight_service.py` — `کل سرویس` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. سرویس اصلی که تست‌ها روی آن اجرا می‌شوند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = (نامشخص) — از همین استفاده کن.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_codex_service.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_deep_scan_service.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_extraction.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_inspector_bridge.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_mega_bundle.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_model_temp_activate.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_progress.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_settings.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_strong_prompt.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_telegram_compose.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_upload_session.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_verifier.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_verify_pdf.py` — وابسته به oversight_service و ممکن است در تست‌ها استفاده شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل tests/test_oversight_service.py و سرویس اصلی backend/app/services/oversight_service.py وابسته است. همچنین به 13 فایل سرویس دیگر در پکیج oversight وابسته است که ممکن است در تست‌ها استفاده شوند. فایل‌های وابسته شامل: oversight_codex_service.py, oversight_deep_scan_service.py, oversight_extraction.py, oversight_inspector_bridge.py, oversight_mega_bundle.py, oversight_model_temp_activate.py, oversight_progress.py, oversight_settings.py, oversight_strong_prompt.py, oversight_telegram_compose.py, oversight_upload_session.py, oversight_verifier.py, oversight_verify_pdf.py هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست اجرای همه تست‌های موجود در فایل tests/test_oversight_service.py با pytest را دارد. باید اطمینان حاصل شود که همه تست‌ها پاس می‌شوند. خارج از این مرحله: نوشتن تست‌های جدید، اصلاح کد اصلی سرویس. نکته حیاتی: اگر تستی fail شود، باید خطا بررسی و رفع شود.

--- بخش مربوط از درخواست اصلی کاربر ---
- همه تست‌ها پاس شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]

--- کلیدواژه‌ها ---
tests/test_oversight_service.py, pytest

بر اساس ساختار پروژه، فایل تست در مسیر backend/tests/test_oversight_service.py وجود دارد. این فایل در deep context موجود نیست، اما بر اساس ساختار سطحی پروژه، فایل‌های مرتبط با oversight_service شامل backend/app/services/oversight_service.py و فایل‌های وابسته مانند backend/app/services/oversight_codex_service.py، backend/app/services/oversight_deep_scan_service.py، backend/app/services/oversight_extraction.py، backend/app/services/oversight_inspector_bridge.py، backend/app/services/oversight_mega_bundle.py، backend/app/services/oversight_model_temp_activate.py، backend/app/services/oversight_progress.py، backend/app/services/oversight_settings.py، backend/app/services/oversight_strong_prompt.py، backend/app/services/oversight_telegram_compose.py، backend/app/services/oversight_upload_session.py، backend/app/services/oversight_verifier.py، backend/app/services/oversight_verify_pdf.py هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] همه تست‌های فایل tests/test_oversight_service.py با pytest پاس شوند
- [ ] هیچ تستی fail نشود — در صورت fail، خطا بررسی و رفع شود
- [ ] تغییرات فقط محدود به رفع باگ‌های تست‌ها باشد — نه نوشتن تست جدید یا اصلاح کد اصلی سرویس
- [ ] مدت زمان اجرای تست‌ها از 60 ثانیه تجاوز نکند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. اجرای دستور pytest برای فایل tests/test_oversight_service.py با timeout 60 ثانیه
2. بررسی خروجی pytest برای شناسایی تست‌های fail شده
3. برای هر تست fail شده، تحلیل خطا و رفع مشکل در کد اصلی سرویس (backend/app/services/oversight_service.py) یا فایل‌های وابسته
4. اجرای مجدد pytest برای اطمینان از عبور همه تست‌ها
5. مستندسازی تغییرات انجام شده برای رفع باگ‌ها

## 💡 نمونه‌های قبل/بعد
**اجرای pytest قبل از رفع باگ**

_قبل:_
```
pytest backend/tests/test_oversight_service.py --timeout=60
# خروجی: 10 passed, 2 failed
```

_بعد:_
```
pytest backend/tests/test_oversight_service.py --timeout=60
# خروجی: 12 passed
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd backend && pytest tests/test_oversight_service.py --timeout=60 -v`
- `cd backend && pytest tests/test_oversight_service.py --timeout=60 --tb=short`

## ⚠️ ریسک‌ها و موارد احتیاط
این تسک فقط اجرای تست‌ها و رفع باگ‌های احتمالی است. ریسک اصلی این است که تغییرات برای رفع باگ‌ها ممکن است به طور ناخواسته روی کد اصلی سرویس (backend/app/services/oversight_service.py) یا فایل‌های وابسته تأثیر بگذارد. همچنین ممکن است برخی تست‌ها به دیتابیس یا سرویس‌های خارجی وابسته باشند که در محیط تست در دسترس نیستند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 11: اجرای linter و اطمینان از عبور بدون warning

**Scope:** این مرحله شامل اجرای linter (مانند flake8 یا pylint) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ warning یا خطایی وجود ندارد. خارج از این مرحله: اجرای type-check، اجرای تست‌ها. نکته حیاتی: اگر linter warning داشته باشد، باید رفع شود.
**Key terms:** backend/app/services/oversight_service.py, tests/test_oversight_service.py, linter, flake8, pylint

**بخش مربوط از متن کاربر:**
```
- linter بدون warning عبور می‌کند
```

## 🎯 هدف (خلاصه ساختاریافته)
رفع linter warnings در oversight_service و تست آن

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `کل فایل` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل سرویس اصلی oversight است و باید lint شود.
- `tests/test_oversight_service.py` — `کل فایل` — فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل تست مربوط به oversight_service است و باید lint شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Python, Flask/FastAPI (بر اساس ساختار پروژه), flake8/pylint برای linting

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` (سطر 1) — فایل اصلی که باید lint شود
- `tests/test_oversight_service.py` (سطر 1) — فایل تست که باید lint شود
- `backend/requirements.txt` (سطر 1) — ممکن است نیاز به نصب flake8 یا pylint داشته باشد

## 🌐 نقشهٔ وابستگی‌ها
این تسک مستقیماً روی دو فایل backend/app/services/oversight_service.py و tests/test_oversight_service.py اعمال می‌شود. فایل oversight_service.py توسط سایر سرویس‌ها و روترها import می‌شود (مانند backend/app/api/routes/oversight.py). تغییرات linting معمولاً تأثیری روی منطق تجاری ندارند، اما باید دقت شود که تغییرات صرفاً ظاهری (فاصله، نام‌گذاری، خطوط اضافی) باشند و منطق کد تغییر نکند. فایل requirements.txt باید flake8 یا pylint را شامل شود.

## 🔍 Context و وضعیت فعلی
اجرای linter (مانند flake8 یا pylint) روی فایل‌های تغییر یافته backend/app/services/oversight_service.py و tests/test_oversight_service.py و اطمینان از عبور بدون warning. این مرحله شامل اجرای linter روی این دو فایل خاص است. باید اطمینان حاصل شود که هیچ warning یا خطایی وجود ندارد. خارج از این مرحله: اجرای type-check، اجرای تست‌ها. نکته حیاتی: اگر linter warning داشته باشد، باید رفع شود. کلیدواژه‌ها: backend/app/services/oversight_service.py, tests/test_oversight_service.py, linter, flake8, pylint. بر اساس ساختار پروژه، فایل‌های هدف در backend/app/services/oversight_service.py و tests/test_oversight_service.py هستند. این فایل‌ها در deep context موجود نیستند، بنابراین تحلیل بر اساس ساختار سطحی پروژه انجام می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] اجرای flake8 روی backend/app/services/oversight_service.py بدون هیچ warning یا error خاتمه یابد
- [ ] اجرای flake8 روی tests/test_oversight_service.py بدون هیچ warning یا error خاتمه یابد
- [ ] هیچ warning یا خطای linting در هیچ‌یک از دو فایل باقی نمانده باشد
- [ ] تغییرات اعمال‌شده صرفاً مربوط به linting باشند و منطق کد تغییر نکند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. اجرای flake8 روی backend/app/services/oversight_service.py و مشاهده خروجی
2. اجرای flake8 روی tests/test_oversight_service.py و مشاهده خروجی
3. برای هر warning یا error شناسایی‌شده:
   - خط مربوطه را در فایل پیدا کن
   - مشکل را بر اساس قوانین flake8 (PEP8) رفع کن
   - تغییرات را ذخیره کن
4. دوباره flake8 را اجرا کن تا تأیید شود هیچ warning باقی نمانده
5. در صورت موفقیت، تغییرات را commit کن
6. خارج از این مرحله: اجرای type-check و اجرای تست‌ها

## 💡 نمونه‌های قبل/بعد
**نمونه رفع warning خط طولانی (E501)**

_قبل:_
```
def some_function_with_very_long_parameters(param1, param2, param3, param4, param5, param6, param7):
```

_بعد:_
```
def some_function_with_very_long_parameters(
    param1, param2, param3, param4, param5, param6, param7
):
```

**نمونه رفع فاصله اضافی (E303/W293)**

_قبل:_
```
def func1():
    pass



def func2():
```

_بعد:_
```
def func1():
    pass


def func2():
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `flake8 backend/app/services/oversight_service.py`
- `flake8 tests/test_oversight_service.py`
- `flake8 backend/app/services/oversight_service.py tests/test_oversight_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی: تغییرات linting ممکن است به‌طور ناخواسته منطق کد را تغییر دهد (مثلاً در refactoring خطوط طولانی). همچنین اگر فایل‌های هدف توسط فایل‌های دیگر import شده باشند (مانند backend/app/api/routes/oversight.py)، تغییرات باید با دقت اعمال شوند. از آنجایی که فایل‌ها deep-read نشده‌اند، ممکن است warningهای خاصی وجود داشته باشد که نیاز به تغییرات ساختاری داشته باشند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 12: اجرای type-check و اطمینان از موفقیت

**Scope:** این مرحله شامل اجرای type-checker (مانند mypy) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ خطای type وجود ندارد. خارج از این مرحله: اجرای linter، اجرای تست‌ها. نکته حیاتی: اگر type-check خطا داشته باشد، باید رفع شود.
**Key terms:** backend/app/services/oversight_service.py, tests/test_oversight_service.py, mypy, type-check

**بخش مربوط از متن کاربر:**
```
- type-check موفق است (`tsc --noEmit` / `mypy`)
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای type-check روی oversight_service و تست‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `کل فایل` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل در ساختار پروژه موجود است.
- `tests/test_oversight_service.py` — `کل فایل` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل در ساختار پروژه موجود است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = (نامشخص) — از همین استفاده کن. پروژه از Python استفاده می‌کند و mypy ابزار type-checking است.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` (سطر 1) — فایل اصلی که باید type-check شود
- `tests/test_oversight_service.py` (سطر 1) — فایل تست که باید type-check شود
- `backend/requirements.txt` (سطر 1) — وابستگی mypy باید در این فایل موجود باشد
- `backend/app/services/oversight_inspector_bridge.py` (سطر 1) — ممکن است توسط oversight_service استفاده شود و type-check روی آن تأثیر بگذارد
- `backend/app/services/oversight_verifier.py` (سطر 1) — ممکن است توسط oversight_service استفاده شود و type-check روی آن تأثیر بگذارد

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/app/services/oversight_service.py و tests/test_oversight_service.py مربوط است. فایل oversight_service.py احتمالاً توسط فایل‌های دیگر مانند oversight_inspector_bridge.py، oversight_verifier.py، oversight_progress.py و سایر فایل‌های پکیج oversight استفاده می‌شود. همچنین ممکن است توسط روترهای API مانند backend/app/api/routes/oversight.py فراخوانی شود. فایل تست test_oversight_service.py نیز وابسته به oversight_service.py است. mypy باید در requirements.txt موجود باشد تا بتوان type-check را اجرا کرد.

## 🔍 Context و وضعیت فعلی
اجرای type-check و اطمینان از موفقیت. این مرحله شامل اجرای type-checker (مانند mypy) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ خطای type وجود ندارد. خارج از این مرحله: اجرای linter، اجرای تست‌ها. نکته حیاتی: اگر type-check خطا داشته باشد، باید رفع شود. --- بخش مربوط از درخواست اصلی کاربر --- - type-check موفق است (`tsc --noEmit` / `mypy`) --- کلیدواژه‌ها --- backend/app/services/oversight_service.py, tests/test_oversight_service.py, mypy, type-check. تحلیل: کاربر خواستار اجرای type-check روی دو فایل مشخص است. فایل‌های هدف در ساختار پروژه موجود هستند اما deep-read نشده‌اند. پروژه از Python استفاده می‌کند (backend پایتون) بنابراین mypy ابزار مناسب است. باید فایل‌های تغییر یافته را با mypy بررسی کرده و خطاهای احتمالی را رفع کرد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] mypy روی backend/app/services/oversight_service.py بدون خطا اجرا شود
- [ ] mypy روی tests/test_oversight_service.py بدون خطا اجرا شود
- [ ] هیچ خطای type جدیدی به کد اضافه نشود
- [ ] تست‌های مربوطه با pytest با موفقیت اجرا شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. اجرای mypy روی فایل backend/app/services/oversight_service.py با دستور `mypy backend/app/services/oversight_service.py --strict`
2. اجرای mypy روی فایل tests/test_oversight_service.py با دستور `mypy tests/test_oversight_service.py --strict`
3. بررسی خروجی mypy و شناسایی خطاهای type
4. رفع هر خطای type در فایل‌های مربوطه
5. اجرای مجدد mypy تا اطمینان از عدم وجود خطا
6. در صورت موفقیت، اجرای تست‌ها با `pytest tests/test_oversight_service.py`

## 💡 نمونه‌های قبل/بعد
**نمونه خطای type که باید رفع شود**

_قبل:_
```
def some_function(param) -> str:
    return param + 1  # خطا: int نمی‌تواند با str جمع شود
```

_بعد:_
```
def some_function(param: int) -> str:
    return str(param + 1)  # رفع شده
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `mypy backend/app/services/oversight_service.py --strict`
- `mypy tests/test_oversight_service.py --strict`
- `pytest tests/test_oversight_service.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر type annotation ممکن است باعث شکستن کدهای دیگر شود که از oversight_service استفاده می‌کنند. فایل‌های مرتبط مانند oversight_inspector_bridge.py، oversight_verifier.py و روتر oversight.py ممکن است تحت تأثیر قرار گیرند. همچنین تغییر type annotation در تست‌ها ممکن است باعث شکست تست‌های دیگر شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 13: بررسی endpoint /api/login برای توکن منقضی

**Scope:** این مرحله شامل بررسی endpoint /api/login است که از تابع validate_token استفاده می‌کند. باید اطمینان حاصل شود که با یک توکن منقضی شده، endpoint خطای 401 برمی‌گرداند. خارج از این مرحله: اصلاح تابع validate_token (که در مرحله 6 انجام شده)، نوشتن تست واحد برای expiry (مرحله 7). نکته حیاتی: این مرحله بیشتر جنبه تأیید دارد و ممکن است نیاز به تغییر در endpoint /api/login باشد.
**Key terms:** /api/login, validate_token, backend/app/api/routes/oversight.py, 401, expired

**بخش مربوط از متن کاربر:**
```
- endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/login", "headers": null, "json_body": {"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiO]
```

## 🎯 هدف (خلاصه ساختاریافته)
بررسی endpoint /api/login برای توکن منقضی و بازگشت 401

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py` — `endpoint /api/login (احتمالی)` — بر اساس ساختار سطحی — توسط مجری تأیید شود. فایل deep-read نشده، اما کاربر به آن اشاره کرده است.
- `backend/app/services/ai_base.py` — `تابع validate_token (احتمالی)` — بر اساس ساختار سطحی — توسط مجری تأیید شود. تابع validate_token در deep context یافت نشد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/chat.py` — احتمالاً endpoint /api/login در این فایل تعریف شده است (بر اساس naming convention پروژه).
- `backend/app/api/routes/unified_api.py` — احتمالاً endpoint /api/login در این فایل تعریف شده است (بر اساس naming convention پروژه).
- `backend/app/services/security_analyzer.py` — احتمالاً تابع validate_token در این فایل قرار دارد (بر اساس naming convention پروژه).

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های route مربوط به /api/login وابسته است که در deep context موجود نیستند. فایل‌های backend/app/api/routes/oversight.py، backend/app/api/routes/chat.py، backend/app/api/routes/unified_api.py کاندیدهای اصلی هستند. تابع validate_token احتمالاً در backend/app/services/ai_base.py یا backend/app/services/security_analyzer.py قرار دارد. تغییر در endpoint /api/login ممکن است بر روی سایر endpoint‌هایی که از validate_token استفاده می‌کنند تأثیر بگذارد. همچنین تست‌های مرتبط در backend/tests/ باید به‌روزرسانی شوند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست بررسی endpoint /api/login را دارد که از تابع validate_token استفاده می‌کند. باید اطمینان حاصل شود که با یک توکن منقضی شده، endpoint خطای 401 برمی‌گرداند. خارج از این مرحله: اصلاح تابع validate_token (که در مرحله 6 انجام شده)، نوشتن تست واحد برای expiry (مرحله 7). نکته حیاتی: این مرحله بیشتر جنبه تأیید دارد و ممکن است نیاز به تغییر در endpoint /api/login باشد. بخش مربوط از درخواست اصلی کاربر: endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/login", "headers": null, "json_body": {"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiO] کلیدواژه‌ها: /api/login, validate_token, backend/app/api/routes/oversight.py, 401, expired. با توجه به deep context موجود، فایل backend/app/api/routes/oversight.py حاوی endpoint /api/login نیست و تابع validate_token در فایل‌های deep-read شده یافت نشد. بنابراین این تسک نیاز به بررسی دقیق‌تر کدبیس دارد. endpoint /api/login احتمالاً در فایل backend/app/api/routes/chat.py یا backend/app/api/routes/unified_api.py تعریف شده است. تابع validate_token ممکن است در backend/app/services/ai_base.py یا backend/app/services/security_analyzer.py قرار داشته باشد. کاربر تأکید دارد که این مرحله تأییدی است و ممکن است نیاز به تغییر در endpoint /api/login باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] endpoint /api/login با توکن منقضی شده (مثال: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiOjE1MTYyMzkwMjJ9) باید status code 401 برگرداند.
- [ ] endpoint /api/login با توکن معتبر باید status code 200 برگرداند.
- [ ] تابع validate_token باید expiry توکن را بررسی کند و در صورت منقضی بودن None برگرداند.
- [ ] تست واحد برای expiry توکن در فایل تست مربوطه نوشته شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. جستجوی دقیق endpoint /api/login در کل پروژه با grep روی فایل‌های routes. 2. یافتن تابع validate_token در service‌های backend. 3. بررسی منطق اعتبارسنجی توکن در endpoint /api/login. 4. افزودن شرط بررسی expiry توکن و بازگشت 401 در صورت منقضی بودن. 5. تست با توکن منقضی شده (مثال: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiOjE1MTYyMzkwMjJ9). 6. ثبت تغییرات در فایل مربوطه.

## 💡 نمونه‌های قبل/بعد
**نمونه تغییر در endpoint /api/login (فرضی)**

_قبل:_
```
# کد فعلی (فرضی)
@router.post("/api/login")
async def login(token: str):
    user = validate_token(token)
    if not user:
        return {"error": "invalid token"}
```

_بعد:_
```
# کد پیشنهادی
@router.post("/api/login")
async def login(token: str):
    user = validate_token(token)
    if not user:
        raise HTTPException(status_code=401, detail="Token expired or invalid")
    return {"user": user}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_runtime_verify_stage1.py -k test_login_expired_token -v`
- `curl -X POST http://localhost:8000/api/login -H "Content-Type: application/json" -d '{"token":"eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiOjE1MTYyMzkwMjJ9"}' -w "%{http_code}"`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر در endpoint /api_login ممکن است بر روی سایر endpoint‌هایی که از validate_token استفاده می‌کنند تأثیر بگذارد. همچنین اگر تابع validate_token در چندین فایل استفاده شود، تغییر آن می‌تواند باعث شکستن سایر بخش‌ها شود. فایل‌های backend/app/api/routes/oversight.py، backend/app/api/routes/chat.py، backend/app/api/routes/unified_api.py باید بررسی شوند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 14: بررسی تابع validate_token برای پارامتر exp

**Scope:** این مرحله شامل بررسی استاتیک کد تابع validate_token در فایل backend/app/services/oversight_service.py است. باید اطمینان حاصل شود که تابع پارامتر exp را چک می‌کند. خارج از این مرحله: اجرای تست‌ها، اصلاح endpoint /api/login. نکته حیاتی: این مرحله با grep برای الگوهای 'exp', 'expired', 'ExpiredSignatureError', 'expiration' در فایل انجام می‌شود.
**Key terms:** validate_token, backend/app/services/oversight_service.py, exp, expired, ExpiredSignatureError, expiration

**بخش مربوط از متن کاربر:**
```
- تابع validate_token پارامتر exp را چک می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["exp", "expired", "ExpiredSignatureError", "expiration"], "files_hint": ["backend/app/services/oversight_service.py"]}]
```

## 🎯 هدف (خلاصه ساختاریافته)
بررسی استاتیک validate_token برای پارامتر exp در oversight_service.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `validate_token` — این فایل deep-read نشده است — مجری باید مسیر را خود تأیید کند. تابع validate_token باید در این فایل جستجو شود.
- `backend/app/services/oversight_service.py` — `grep_patterns` — الگوهای جستجو: 'exp', 'expired', 'ExpiredSignatureError', 'expiration' — باید در کل فایل اسکن شوند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/oversight.py` — این روتر احتمالاً از تابع validate_token استفاده می‌کند و تغییرات در oversight_service.py روی آن تأثیر می‌گذارد.
- `backend/app/services/verify_runtime/auth_runner.py` — این فایل ممکن است از validate_token برای تأیید احراز هویت runtime استفاده کند.
- `backend/app/services/oversight_verifier.py` — این فایل ممکن است از validate_token برای تأیید صحت توکن‌ها استفاده کند.

## 🌐 نقشهٔ وابستگی‌ها
فایل backend/app/services/oversight_service.py یک سرویس اصلی در پروژه است که توسط چندین روتر و سرویس دیگر استفاده می‌شود. روتر backend/app/api/routes/oversight.py احتمالاً از توابع این سرویس برای مدیریت جلسات و احراز هویت استفاده می‌کند. همچنین سرویس‌های verify_runtime مانند auth_runner.py و oversight_verifier.py ممکن است به validate_token وابسته باشند. هرگونه تغییر در منطق بررسی exp می‌تواند بر کل زنجیره احراز هویت تأثیر بگذارد.

## 🔍 Context و وضعیت فعلی
بررسی تابع validate_token برای پارامتر exp در فایل backend/app/services/oversight_service.py. این مرحله شامل بررسی استاتیک کد تابع validate_token است تا اطمینان حاصل شود که تابع پارامتر exp را چک می‌کند. خارج از این مرحله: اجرای تست‌ها، اصلاح endpoint /api/login. نکته حیاتی: این مرحله با grep برای الگوهای 'exp', 'expired', 'ExpiredSignatureError', 'expiration' در فایل انجام می‌شود. بخش مربوط از درخواست اصلی کاربر: تابع validate_token پارامتر exp را چک می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["exp", "expired", "ExpiredSignatureError", "expiration"], "files_hint": ["backend/app/services/oversight_service.py"]}]. کلیدواژه‌ها: validate_token, backend/app/services/oversight_service.py, exp, expired, ExpiredSignatureError, expiration. تحلیل: کاربر خواستار تأیید استاتیک وجود منطق بررسی انقضای توکن (exp) در تابع validate_token است. این یک بررسی صرفاً کد-ایستا (static) است و نیازی به اجرای تست یا تغییر endpoint ندارد. فایل هدف backend/app/services/oversight_service.py است که باید با grep برای الگوهای مشخص شده اسکن شود. شواهد در کد: فایل deep-read نشده است، اما بر اساس ساختار پروژه، oversight_service.py شامل توابع احراز هویت و مدیریت توکن است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/app/services/oversight_service.py شامل تابع validate_token است.
- [ ] در تابع validate_token از الگوی 'exp' برای بررسی انقضای توکن استفاده شده است.
- [ ] در تابع validate_token از الگوی 'expired' یا 'ExpiredSignatureError' برای مدیریت خطای انقضا استفاده شده است.
- [ ] در تابع validate_token از الگوی 'expiration' برای اشاره به زمان انقضا استفاده شده است.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. فایل backend/app/services/oversight_service.py را باز کرده و تابع validate_token را پیدا کن. 2. با استفاده از grep یا جستجوی دستی، بررسی کن که آیا در بدنه تابع validate_token از پارامتر exp (انقضای توکن) استفاده شده است. 3. الگوهای 'exp', 'expired', 'ExpiredSignatureError', 'expiration' را در کل فایل جستجو کن. 4. اگر تابع validate_token وجود دارد، خطوط مربوط به بررسی exp را مستند کن. 5. اگر تابع وجود ندارد یا بررسی exp انجام نشده، این را به عنوان یک gap گزارش کن. 6. هیچ تغییری در کد ایجاد نکن — این مرحله صرفاً بررسی استاتیک است.

## 💡 نمونه‌های قبل/بعد
**بررسی استاتیک — نمونه grep برای الگوهای exp**

_قبل:_
```
grep -n 'exp\|expired\|ExpiredSignatureError\|expiration' backend/app/services/oversight_service.py
```

_بعد:_
```
خروجی grep شامل خطوطی مانند: 'if token.get("exp") < time.time():' یا 'except ExpiredSignatureError:'
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `grep -n 'def validate_token' backend/app/services/oversight_service.py`
- `grep -n 'exp\|expired\|ExpiredSignatureError\|expiration' backend/app/services/oversight_service.py`

## ⚠️ ریسک‌ها و موارد احتیاط
این مرحله صرفاً بررسی استاتیک است و هیچ تغییری در کد ایجاد نمی‌کند، بنابراین ریسک خاصی ندارد. با این حال، اگر تابع validate_token وجود نداشته باشد یا بررسی exp انجام نشده باشد، این یک gap امنیتی است که باید در مراحل بعدی رفع شود. فایل‌های وابسته مانند backend/app/api/routes/oversight.py و backend/app/services/verify_runtime/auth_runner.py ممکن است تحت تأثیر این gap قرار گیرند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 15: اجرای تست‌های auth برای expiry

**Scope:** این مرحله شامل اجرای تست‌های مربوط به auth با فیلتر expiry است. باید اطمینان حاصل شود که تست‌های مربوط به expiry در فایل backend/tests/test_auth.py پاس می‌شوند. خارج از این مرحله: اجرای تست‌های oversight_service، اجرای linter. نکته حیاتی: اگر فایل backend/tests/test_auth.py وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن داشته باشد.
**Key terms:** backend/tests/test_auth.py, expiry, pytest

**بخش مربوط از متن کاربر:**
```
- `pytest backend/tests/test_auth.py -k expiry`
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای تست‌های auth با فیلتر expiry

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_auth.py:1-50` — `فایل جدید` — فایل جدید باید ایجاد شود — بر اساس ساختار سطحی پروژه، توسط مجری تأیید شود
  ```python
  # backend/tests/test_auth.py
  # تست‌های مربوط به auth با فیلتر expiry
  import pytest
  from fastapi.testclient import TestClient
  from backend.app.main import app
  
  client = TestClient(app)
  
  def test_expiry_valid_token():
      """تست expiry با توکن معتبر"""
      response = client.get("/api/auth/verify", headers={"Authorization": "Bearer valid_token"})
      assert response.status_code == 200
  
  def test_expiry_expired_token():
      """تست expiry با توکن منقضی"""
      response = client.get("/api/auth/verify", headers={"Authorization": "Bearer expired_token"})
      assert response.status_code == 401
  ```
- `backend/app/api/routes/auth.py:1-30` — `احتمالی — فایل وجود ندارد` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. احتمالاً فایل backend/app/api/routes/auth.py وجود ندارد و باید ایجاد شود یا endpoint موجود را پیدا کنید.
  ```python
  # backend/app/api/routes/auth.py (فایل احتمالی)
  # اگر وجود دارد، endpointهای auth را بررسی کنید
  from fastapi import APIRouter, Depends, HTTPException
  from backend.app.services.auth_service import verify_token, check_expiry
  
  router = APIRouter()
  
  @router.get("/api/auth/verify")
  async def verify_auth(token: str = Depends(get_token)):
      if check_expiry(token):
          raise HTTPException(status_code=401, detail="Token expired")
      return {"status": "valid"}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص) — بر اساس ساختار پروژه، احتمالاً FastAPI + pytest + httpx برای تست

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/auth_service.py` — سرویس احراز هویت که منطق expiry در آن پیاده‌سازی شده است (اگر وجود داشته باشد)
- `backend/app/main.py` — فایل اصلی برنامه که روترها را ثبت می‌کند — باید روتر auth در آن ثبت شود
- `backend/app/core/config.py` — تنظیمات مربوط به JWT و expiry token (مانند ACCESS_TOKEN_EXPIRE_MINUTES)
- `backend/app/models/user.py` — مدل کاربر که ممکن است فیلدهای مربوط به expiry داشته باشد
- `backend/requirements.txt` — وابستگی‌های مورد نیاز برای تست (pytest, httpx, pytest-asyncio)

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/tests/test_auth.py (جدید)، backend/app/services/auth_service.py (اگر وجود داشته باشد)، backend/app/api/routes/auth.py (اگر وجود داشته باشد)، backend/app/main.py (برای ثبت روتر)، backend/app/core/config.py (برای تنظیمات expiry) و backend/requirements.txt (برای وابستگی‌های pytest) وابسته است. همچنین ممکن است به مدل‌های user و database service نیاز داشته باشد. فایل‌های تست موجود در پروژه (مانند test_runtime_verify_stage*.py) نباید تحت تأثیر قرار گیرند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست اجرای تست‌های مربوط به auth با فیلتر expiry را دارد. دستور دقیق: `pytest backend/tests/test_auth.py -k expiry`. باید اطمینان حاصل شود که تست‌های مربوط به expiry در فایل backend/tests/test_auth.py پاس می‌شوند. خارج از این مرحله: اجرای تست‌های oversight_service، اجرای linter. نکته حیاتی: اگر فایل backend/tests/test_auth.py وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن داشته باشد. کلیدواژه‌ها: backend/tests/test_auth.py, expiry, pytest. اولویت: critical. نوع: other. با بررسی ساختار پروژه، فایل backend/tests/test_auth.py در لیست فایل‌ها موجود نیست (تنها فایل‌های تست موجود: test_code_content_searcher.py, test_inspector_oversight_bridge.py, test_iterative_orchestrator.py, test_runtime_verify_autodetect.py, test_runtime_verify_integration.py, test_runtime_verify_real_servers.py, test_runtime_verify_stage1.py, test_runtime_verify_stage2.py, test_runtime_verify_stage3a.py, test_runtime_verify_stage3b.py, test_runtime_verify_stage3cd.py, test_runtime_verify_stage3e.py, test_runtime_verify_stage6.py, test_runtime_verify_stage9.py, test_verify_v7.py). بنابراین فایل backend/tests/test_auth.py وجود ندارد و باید ایجاد شود. همچنین باید بررسی شود که آیا سرویس auth در پروژه وجود دارد و endpointهای مربوط به expiry چیست.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل backend/tests/test_auth.py ایجاد شده و شامل تست‌های مربوط به expiry باشد
- [ ] دستور `pytest backend/tests/test_auth.py -k expiry` با موفقیت اجرا شده و تمام تست‌ها پاس شوند
- [ ] تست‌های expiry حداقل دو حالت را پوشش دهند: توکن معتبر (200) و توکن منقضی (401)
- [ ] سایر تست‌های پروژه با اجرای تست‌های جدید شکسته نشوند — `pytest backend/tests/` بدون خطا اجرا شود
- [ ] اگر endpoint /api/auth/verify وجود ندارد، endpoint مناسب برای تست expiry شناسایی و در تست استفاده شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/tests/test_auth.py با تست‌های مربوط به expiry. 2. بررسی سرویس‌های احراز هویت در پروژه (احتمالاً در backend/app/services/). 3. جستجوی کلیدواژه expiry در کدبیس برای فهم منطق expiry. 4. نوشتن تست‌های pytest با marker expiry. 5. اجرای دستور `pytest backend/tests/test_auth.py -k expiry` و اطمینان از پاس شدن تست‌ها. 6. اگر endpoint خاصی برای expiry وجود دارد، تست‌های API بنویسید. 7. اطمینان از عدم شکستن سایر تست‌ها با اجرای `pytest backend/tests/`.

## 💡 نمونه‌های قبل/بعد
**ایجاد فایل تست جدید**

_قبل:_
```
فایل backend/tests/test_auth.py وجود ندارد
```

_بعد:_
```
فایل backend/tests/test_auth.py با تست‌های expiry ایجاد می‌شود:

import pytest
from fastapi.testclient import TestClient
from backend.app.main import app

client = TestClient(app)

def test_expiry_valid_token():
    response = client.get("/api/auth/verify", headers={"Authorization": "Bearer valid_token"})
    assert response.status_code == 200

def test_expiry_expired_token():
    response = client.get("/api/auth/verify", headers={"Authorization": "Bearer expired_token"})
    assert response.status_code == 401
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_auth.py -k expiry -v`
- `pytest backend/tests/test_auth.py -v`
- `pytest backend/tests/ --ignore=backend/tests/test_auth.py -v`
- `python -m pytest backend/tests/test_auth.py -k expiry --tb=short`

## ⚠️ ریسک‌ها و موارد احتیاط
فایل backend/tests/test_auth.py وجود ندارد و باید ایجاد شود. اگر endpoint/auth service در پروژه وجود نداشته باشد، تست‌ها fail می‌شوند. همچنین ممکن است نیاز به mock کردن سرویس‌های خارجی (مانند دیتابیس) باشد. تغییرات در backend/app/main.py برای ثبت روتر auth ممکن است روی سایر endpointها تأثیر بگذارد. اگر فایل backend/app/services/auth_service.py وجود نداشته باشد، باید ایجاد شود که زمان‌بر است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 16: اجرای تست‌های frontend برای login

**Scope:** این مرحله شامل اجرای تست‌های frontend مربوط به login است. باید اطمینان حاصل شود که تست‌های مربوط به login در frontend پاس می‌شوند. خارج از این مرحله: اجرای تست‌های backend، اجرای linter. نکته حیاتی: اگر تست‌های frontend برای login وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن‌ها داشته باشد.
**Key terms:** frontend, login, npm run test

**بخش مربوط از متن کاربر:**
```
- `npm run test -- login`
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای تست‌های frontend برای login

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/package-lock.json:1-10` — `package-lock.json` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل package-lock.json نشان‌دهنده وابستگی‌های npm است.
  ```json
  {
    "name": "frontend",
    "lockfileVersion": 3,
    "requires": true,
    "packages": {}
  }
  ```
- `frontend/next.config.js:1-10` — `next.config.js` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل next.config.js نشان‌دهنده استفاده از Next.js است.
  ```jsx
  // next.config.js
  /** @type {import('next').NextConfig} */
  const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
  }
  
  module.exports = nextConfig
  ```
- `frontend/next-env.d.ts:1-5` — `next-env.d.ts` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فایل next-env.d.ts نشان‌دهنده استفاده از TypeScript است.
  ```tsx
  /// <reference types="next" />
  /// <reference types="next/image-types/global" />
  
  // NOTE: This file should not be edited
  // see https://nextjs.org/docs/basic-features/typescript for more information.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = (نامشخص). بر اساس فایل‌های موجود: Next.js (frontend)، Python/FastAPI (backend)، npm (مدیریت وابستگی‌ها).

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/chat.py` (سطر 1) — احتمالاً شامل endpointهای احراز هویت و login است که frontend با آن‌ها ارتباط دارد.
- `backend/app/services/ai_manager.py` (سطر 1) — مدیریت سرویس‌های AI که ممکن است در فرآیند login نقش داشته باشند.
- `backend/app/core/config.py` (سطر 1) — شامل تنظیمات پروژه مانند JWT_SECRET، DATABASE_URL و سایر متغیرهای محیطی مرتبط با احراز هویت.

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های frontend وابسته است که در ساختار کامل پروژه نمایش داده نشده‌اند. فایل‌های backend مرتبط شامل `backend/app/api/routes/chat.py` (احتمالاً endpointهای login)، `backend/app/services/ai_manager.py` (مدیریت سرویس‌ها)، و `backend/app/core/config.py` (تنظیمات احراز هویت) هستند. فایل `frontend/package-lock.json` وابستگی‌های npm را مشخص می‌کند. فایل `frontend/next.config.js` تنظیمات Next.js را شامل می‌شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست اجرای تست‌های frontend مربوط به login را دارد. باید اطمینان حاصل شود که تست‌های مربوط به login در frontend پاس می‌شوند. خارج از این مرحله: اجرای تست‌های backend، اجرای linter. نکته حیاتی: اگر تست‌های frontend برای login وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن‌ها داشته باشد.

--- بخش مربوط از درخواست اصلی کاربر ---
- `npm run test -- login`

--- کلیدواژه‌ها ---
frontend, login, npm run test

بر اساس ساختار پروژه، فایل‌های frontend در مسیر `frontend/` قرار دارند. فایل `frontend/package-lock.json` نشان‌دهنده وجود npm و احتمالاً Jest یا React Testing Library است. فایل `frontend/next.config.js` نشان‌دهنده استفاده از Next.js است. فایل `frontend/next-env.d.ts` برای TypeScript است. هیچ فایل تستی در ساختار سطحی دیده نمی‌شود، بنابراین احتمالاً تست‌ها در زیرشاخه‌های `frontend/` (مثلاً `__tests__/` یا `src/__tests__/`) قرار دارند که در ساختار کامل پروژه نمایش داده نشده‌اند. همچنین فایل‌های backend مرتبط با login می‌توانند شامل `backend/app/api/routes/chat.py` (احتمالاً احراز هویت) و `backend/app/services/ai_manager.py` (مدیریت سرویس‌ها) باشند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست‌های frontend برای login با دستور `npm run test -- login` اجرا شوند و پاس شوند.
- [ ] اگر تست‌های login وجود ندارند، یک فایل تست جدید ایجاد شود که شامل حداقل ۳ تست (رندر فرم، ارسال معتبر، خطای نامعتبر) باشد.
- [ ] تست‌ها از React Testing Library و Jest استفاده کنند.
- [ ] تست‌ها خطاهای linting نداشته باشند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ابتدا بررسی کن که آیا تست‌های frontend برای login وجود دارند یا خیر. با دستور `find frontend -name "*.test.*" -o -name "*.spec.*" | grep -i login` جستجو کن.
2. اگر تست‌ها وجود دارند، آن‌ها را با دستور `cd frontend && npm run test -- --testPathPattern=login` اجرا کن.
3. اگر تست‌ها وجود ندارند، یک فایل تست جدید برای login ایجاد کن. فایل پیشنهادی: `frontend/__tests__/login.test.tsx` یا `frontend/src/__tests__/login.test.tsx`.
4. تست‌های login باید شامل موارد زیر باشند:
   - رندر فرم login
   - ارسال فرم با credentials معتبر
   - نمایش خطا برای credentials نامعتبر
   - هدایت به صفحه dashboard پس از login موفق
5. از React Testing Library و Jest برای نوشتن تست‌ها استفاده کن.
6. پس از ایجاد تست‌ها، آن‌ها را با دستور `cd frontend && npm run test -- --testPathPattern=login` اجرا کن.
7. اگر تست‌ها پاس نشدند، خطاها را بررسی و رفع کن.

## 💡 نمونه‌های قبل/بعد
**ایجاد فایل تست login**

_قبل:_
```
فایل `frontend/__tests__/login.test.tsx` وجود ندارد.
```

_بعد:_
```
import { render, screen, fireEvent } from '@testing-library/react';
import LoginPage from '../pages/login';

describe('Login Page', () => {
  it('renders login form', () => {
    render(<LoginPage />);
    expect(screen.getByLabelText(/email/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/password/i)).toBeInTheDocument();
    expect(screen.getByRole('button', { name: /login/i })).toBeInTheDocument();
  });

  it('submits form with valid credentials', async () => {
    render(<LoginPage />);
    fireEvent.change(screen.getByLabelText(/email/i), { target: { value: 'test@example.com' } });
    fireEvent.change(screen.getByLabelText(/password/i), { target: { value: 'password123' } });
    fireEvent.click(screen.getByRole('button', { name: /login/i }));
    // انتظار هدایت به dashboard
    await waitFor(() => {
      expect(mockRouter.push).toHaveBeenCalledWith('/dashboard');
    });
  });

  it('shows error for invalid credentials', async () => {
    render(<LoginPage />);
    fireEvent.change(screen.getByLabelText(/email/i), { target: { value: 'wrong@example.com' } });
    fireEvent.change(screen.getByLabelText(/password/i), { target: { value: 'wrongpassword' } });
    fireEvent.click(screen.getByRole('button', { name: /login/i }));
    await waitFor(() => {
      expect(screen.getByText(/invalid credentials/i)).toBeInTheDocument();
    });
  });
});
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd frontend && npm run test -- --testPathPattern=login`
- `cd frontend && npm run lint -- --quiet`

## ⚠️ ریسک‌ها و موارد احتیاط
فایل‌های frontend در ساختار کامل پروژه نمایش داده نشده‌اند، بنابراین مسیر دقیق فایل‌های تست مشخص نیست. ممکن است فایل‌های تست login در زیرشاخه‌های `frontend/` (مثلاً `__tests__/` یا `src/__tests__/`) قرار داشته باشند که نیاز به جستجو دارد. همچنین ممکن است وابستگی‌های npm (مانند @testing-library/react و jest) نصب نباشند که باید با `npm install` نصب شوند. تغییرات در فایل‌های frontend ممکن است بر روی کامپوننت‌های دیگر تأثیر بگذارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 17: اجرای pytest با پوشش کد برای oversight_service

**Scope:** این مرحله شامل اجرای pytest با پوشش کد برای ماژول app.services.oversight_service است. باید اطمینان حاصل شود که پوشش تست برای این ماژول قابل قبول است. خارج از این مرحله: اجرای تست‌های خاص، اجرای linter. نکته حیاتی: این مرحله برای اندازه‌گیری پوشش کد است و ممکن است نیاز به نصب pytest-cov داشته باشد.
**Key terms:** backend/tests/, app.services.oversight_service, pytest, --cov

**بخش مربوط از متن کاربر:**
```
- `pytest backend/tests/ --cov=app.services.oversight_service`
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای pytest با پوشش کد برای oversight_service

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py` — `ماژول اصلی` — بر اساس ساختار سطحی — توسط مجری تأیید شود. این فایل deep-read نشده است.
- `backend/tests/` — `دایرکتوری تست‌ها` — بر اساس ساختار سطحی — توسط مجری تأیید شود. این دایرکتوری شامل تست‌های مرتبط است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
(نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_inspector_bridge.py` — این فایل با oversight_service تعامل دارد و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_verifier.py` — این فایل با oversight_service تعامل دارد و ممکن است در تست‌ها استفاده شود
- `backend/app/services/oversight_settings.py` — این فایل تنظیمات مربوط به oversight_service را فراهم می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های `backend/app/services/oversight_service.py` (ماژول اصلی)، `backend/tests/` (دایرکتوری تست‌ها)، و `backend/app/services/oversight_inspector_bridge.py`، `backend/app/services/oversight_verifier.py`، `backend/app/services/oversight_settings.py` (فایل‌های مرتبط) وابسته است. همچنین نیاز به نصب کتابخانه pytest-cov دارد.

## 🔍 Context و وضعیت فعلی
این تسک شامل اجرای pytest با پوشش کد برای ماژول app.services.oversight_service است. کاربر درخواست کرده که با دستور `pytest backend/tests/ --cov=app.services.oversight_service` پوشش تست برای این ماژول اندازه‌گیری شود. کلیدواژه‌های اصلی: backend/tests/, app.services.oversight_service, pytest, --cov. نکته حیاتی: این مرحله برای اندازه‌گیری پوشش کد است و ممکن است نیاز به نصب pytest-cov داشته باشد. خارج از این مرحله: اجرای تست‌های خاص، اجرای linter. بر اساس ساختار پروژه، فایل اصلی ماژول در `backend/app/services/oversight_service.py` قرار دارد و تست‌های مرتبط احتمالاً در `backend/tests/` هستند. فایل‌های مرتبط شامل `backend/app/services/oversight_inspector_bridge.py` و `backend/app/services/oversight_verifier.py` هستند که با این ماژول تعامل دارند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] دستور pytest با پوشش کد بدون خطا اجرا شود
- [ ] پوشش کد برای ماژول oversight_service حداقل 70% باشد
- [ ] گزارش پوشش کد شامل خطوط و شاخه‌ها باشد
- [ ] pytest-cov نصب شده باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. نصب pytest-cov در محیط مجازی: `pip install pytest-cov`
2. اجرای دستور `pytest backend/tests/ --cov=app.services.oversight_service`
3. بررسی خروجی پوشش کد (line coverage, branch coverage)
4. در صورت پایین بودن پوشش، شناسایی بخش‌های بدون تست و افزودن تست‌های جدید
5. مستندسازی نتایج پوشش کد در فایل گزارش

## 💡 نمونه‌های قبل/بعد
**اجرای pytest با پوشش کد**

_قبل:_
```
pytest backend/tests/ --cov=app.services.oversight_service
```

_بعد:_
```
خروجی پوشش کد شامل درصد پوشش خطوط و شاخه‌ها
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/ --cov=app.services.oversight_service`

## ⚠️ ریسک‌ها و موارد احتیاط
این تسک صرفاً برای اندازه‌گیری پوشش کد است و تغییری در کد ایجاد نمی‌کند. ریسک خاصی وجود ندارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

# 🔹 مرحله 18: ایجاد commit با پیام واضح و checklist

**Scope:** این مرحله شامل ایجاد commit یا PR با پیام واضح است که شامل checklist از همه تغییرات انجام شده باشد. پیام commit باید شامل merged-from با شناسه‌های تسک‌های اصلی باشد. خارج از این مرحله: انجام تغییرات کد، اجرای تست‌ها. نکته حیاتی: پیام commit باید شامل merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5 باشد.
**Key terms:** commit, PR, merged-from, baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5

**بخش مربوط از متن کاربر:**
```
- در commit message: `merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5`
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد commit با merged-from checklist و شناسه‌های تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_verifier.py:1-50` — `کل فایل` — این فایل احتمالاً الگوی commit message با merged-from را تعریف می‌کند. مجری باید مسیر را خود تأیید کند.
  ```python
  فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/services/github_pr_service.py:1-50` — `کل فایل` — این فایل مسئول ایجاد PR است. اگر کاربر PR می‌خواهد، این فایل باید استفاده شود.
  ```python
  فایل deep-read نشده — بر اساس ساختار سطحی
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده در بالا = (نامشخص)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_codex_service.py` (سطر 1) — احتمالاً قوانین commit message convention را تعریف می‌کند
- `backend/app/services/oversight_verifier.py` (سطر 1) — احتمالاً merged-from را در commit message بررسی می‌کند
- `backend/app/services/github_pr_service.py` (سطر 1) — اگر PR ایجاد شود، این سرویس استفاده می‌شود

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های backend/app/services/oversight_verifier.py (برای تأیید merged-from)، backend/app/services/github_pr_service.py (برای ایجاد PR)، و backend/app/services/oversight_codex_service.py (برای قوانین commit) وابسته است. همچنین به git client و دسترسی به repository نیاز دارد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد commit یا PR با پیام واضح دارد که شامل checklist از همه تغییرات انجام شده باشد. پیام commit باید شامل merged-from با شناسه‌های تسک‌های اصلی باشد: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5. خارج از این مرحله: انجام تغییرات کد، اجرای تست‌ها. این یک تسک purely administrative/commit management است و نیازی به تغییر کد ندارد. بر اساس deep context موجود، فایل‌های مرتبط با commit message convention در این پروژه شامل backend/app/services/oversight_verifier.py (خطوط 1-50) و backend/app/services/oversight_codex_service.py (خطوط 1-50) هستند که الگوی commit message با merged-from را پشتیبانی می‌کنند. همچنین backend/app/services/github_pr_service.py (خطوط 1-50) مسئول ایجاد PR است. کاربر تأکید کرده که این مرحله فقط commit/PR creation است و شامل تغییر کد یا تست نمی‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] commit message شامل merged-from با شناسه‌های baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5 باشد
- [ ] commit message شامل checklist از تغییرات انجام شده باشد
- [ ] تغییرات کد یا اجرای تست‌ها در این commit انجام نشده باشد (فقط commit message)
- [ ] اگر PR ایجاد شده، merged-from در description PR نیز درج شده باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی وضعیت فعلی git با `git status` و `git log --oneline -5` برای اطمینان از اینکه همه تغییرات مورد نظر staged شده‌اند.
2. ایجاد commit با پیام زیر:
   ```
   [scope]: توضیح مختصر تغییرات
   
   - checklist از تغییرات انجام شده (مثلاً: اضافه کردن endpoint جدید، رفع باگ X، به‌روزرسانی تست‌ها)
   
   merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5
   ```
3. اگر نیاز به PR است، از `backend/app/services/github_pr_service.py` استفاده شود.
4. اطمینان از اینکه merged-from شناسه‌ها دقیقاً همان‌هایی هستند که کاربر مشخص کرده.

## 💡 نمونه‌های قبل/بعد
**نمونه commit message با merged-from**

_قبل:_
```
git commit -m "fix some bugs"
```

_بعد:_
```
git commit -m "fix(inspector): رفع باگ اسکن موردی

- اصلاح منطق اسکن برای جلوگیری از 96 پیشنهاد بی‌ربط
- اضافه کردن timestamp scan

merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5"
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `git log -1 --format=%B | grep -q 'merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5' && echo 'OK' || echo 'FAIL'`
- `git diff HEAD~1 --stat | wc -l | xargs -I {} echo 'تعداد فایل‌های تغییر یافته: {}'`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی: اگر merged-from شناسه‌ها اشتباه وارد شوند، tracking تسک‌ها مختل می‌شود. همچنین اگر commit حاوی تغییرات کد ناخواسته باشد (مثلاً فایل‌های staged نشده)، تاریخچه git آلوده می‌شود. فایل‌های backend/app/services/oversight_verifier.py و backend/app/services/github_pr_service.py ممکن است merged-from را validation کنند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: critical
- تخمین زمان: small

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'تست `test_add_watched_valid_repo` باید با pytest-asyncio نوشته شود و با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_oversight_service.py::test_add_watched_valid_repo', 'marker': 'asyncio'}}
- [ ] {'text': 'تست `test_add_watched_duplicate_repo` باید بررسی کند که افزودن یک repo تکراری خطای HTTPException با status code 409 یا 400 برمی\u200cگرداند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_oversight_service.py::test_add_watched_duplicate_repo', 'marker': 'asyncio'}}
- [ ] {'text': 'وابستگی\u200cهای خارجی (GitHub API, Render API) باید در تست\u200cها mock شوند — هیچ درخواست واقعی به APIهای خارجی ارسال نشود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['patch.*GitHubStorage', 'patch.*RenderService', 'patch.*DBService', 'mock_github', 'mock_render', 'mock_db'], 'files_hint': ['backend/tests/test_oversight_service.py']}}
- [ ] {'text': 'تست\u200cها باید با دستور `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched"` با موفقیت اجرا شوند.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_oversight_service.py', 'marker': 'asyncio'}}
- [ ] {'text': 'هیچ تغییری در کد اصلی سرویس (OversightService) ایجاد نشود — فقط فایل تست جدید اضافه شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class OversightService', 'def add_watched'], 'files_hint': ['backend/app/services/oversight_service.py']}}
- [ ] {'text': 'تست test_run_task_changes_status_to_running با موفقیت پاس شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_oversight_service.py::test_run_task_changes_status_to_running', 'timeout_seconds': 60}}
- [ ] {'text': 'تست یک task معتبر به لیست tasks اضافه کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['tasks.append', 'add_task'], 'files_hint': ['tests/test_oversight_service.py']}}
- [ ] {'text': 'تست run_task را با id task فراخوانی کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ["run_task('test-1')", 'run_task('], 'files_hint': ['tests/test_oversight_service.py']}}
- [ ] {'text': "تست بررسی کند که status task به 'running' تغییر کرده است.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['assert.*status.*running', 'assertEqual.*status.*running'], 'files_hint': ['tests/test_oversight_service.py']}}
- [ ] {'text': 'تست test_scan_project_invalid_watched_id_returns_404 در فایل tests/test_oversight_service.py وجود داشته باشد و با pytest بدون خطا اجرا شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404', 'timeout_seconds': 60}}
- [ ] {'text': 'تست از یک watched_id تصادفی یا غیرموجود استفاده کند (نه یک مقدار ثابت واقعی).', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['invalid_watched_id', 'non-existent', 'uuid4'], 'files_hint': ['tests/test_oversight_service.py']}}
- [ ] {'text': 'تست با pytest.raises(HTTPException) بررسی کند که status_code برابر 404 است.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['pytest.raises\\(HTTPException\\)', 'status_code == 404'], 'files_hint': ['tests/test_oversight_service.py']}}
- [ ] {'text': 'هیچ تغییری در کد اصلی سرویس (oversight_service.py) ایجاد نشود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['def scan_project'], 'files_hint': ['backend/app/services/oversight_service.py']}}
- [ ] {'text': 'تست test_auto_register_watched_valid_repo_full_name با pytest بدون خطا اجرا شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name', 'marker': 'verify'}}
- [ ] {'text': "تست از یک repo_full_name معتبر مانند 'owner/repo' استفاده کند.", 'verify_method': 'static', 'verify_plan': {'grep_patterns': ["repo_full_name = 'owner/repo'", 'repo_full_name = "owner/repo"'], 'files_hint': ['backend/tests/test_oversight_service.py']}}
- [ ] {'text': 'وابستگی\u200cهای خارجی (GitHub API) در تست mock شده باشند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['patch', 'mock', 'MagicMock'], 'files_hint': ['backend/tests/test_oversight_service.py']}}
- [ ] {'text': 'تست موفقیت عملیات را با assert تأیید کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['assert result', 'assertTrue', 'assertEqual'], 'files_hint': ['backend/tests/test_oversight_service.py']}}
- [ ] {'text': 'تابع validate_token باید توکن JWT را decode کرده و فیلد exp را با زمان فعلی UTC مقایسه کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['validate_token', 'jwt.decode', 'exp', 'datetime.utcnow', 'datetime.now', 'timezone.utc'], 'files_hint': ['backend/app/services/oversight_service.py']}}
- [ ] {'text': 'اگر توکن منقضی شده باشد، تابع باید HTTPException با status code 401 raise کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['HTTPException', 'status_code=401', 'Token expired'], 'files_hint': ['backend/app/services/oversight_service.py']}}
- [ ] {'text': 'اگر توکن معتبر باشد، تابع باید True برگرداند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['return True'], 'files_hint': ['backend/app/services/oversight_service.py']}}

## Acceptance Criteria

1. تست `test_add_watched_valid_repo` باید با pytest-asyncio نوشته شود و با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند. _(verify: backend_test)_
2. تست `test_add_watched_duplicate_repo` باید بررسی کند که افزودن یک repo تکراری خطای HTTPException با status code 409 یا 400 برمی‌گرداند. _(verify: backend_test)_
3. وابستگی‌های خارجی (GitHub API, Render API) باید در تست‌ها mock شوند — هیچ درخواست واقعی به APIهای خارجی ارسال نشود. _(verify: static)_
4. تست‌ها باید با دستور `pytest backend/tests/test_oversight_service.py -v -k "test_add_watched"` با موفقیت اجرا شوند. _(verify: backend_test)_
5. هیچ تغییری در کد اصلی سرویس (OversightService) ایجاد نشود — فقط فایل تست جدید اضافه شود. _(verify: static)_
6. تست test_run_task_changes_status_to_running با موفقیت پاس شود. _(verify: backend_test)_
7. تست یک task معتبر به لیست tasks اضافه کند. _(verify: static)_
8. تست run_task را با id task فراخوانی کند. _(verify: static)_
9. تست بررسی کند که status task به 'running' تغییر کرده است. _(verify: static)_
10. تست test_scan_project_invalid_watched_id_returns_404 در فایل tests/test_oversight_service.py وجود داشته باشد و با pytest بدون خطا اجرا شود. _(verify: backend_test)_
11. تست از یک watched_id تصادفی یا غیرموجود استفاده کند (نه یک مقدار ثابت واقعی). _(verify: static)_
12. تست با pytest.raises(HTTPException) بررسی کند که status_code برابر 404 است. _(verify: static)_
13. هیچ تغییری در کد اصلی سرویس (oversight_service.py) ایجاد نشود. _(verify: static)_
14. تست test_auto_register_watched_valid_repo_full_name با pytest بدون خطا اجرا شود. _(verify: backend_test)_
15. تست از یک repo_full_name معتبر مانند 'owner/repo' استفاده کند. _(verify: static)_
16. وابستگی‌های خارجی (GitHub API) در تست mock شده باشند. _(verify: static)_
17. تست موفقیت عملیات را با assert تأیید کند. _(verify: static)_
18. تابع validate_token باید توکن JWT را decode کرده و فیلد exp را با زمان فعلی UTC مقایسه کند. _(verify: static)_
19. اگر توکن منقضی شده باشد، تابع باید HTTPException با status code 401 raise کند. _(verify: static)_
20. اگر توکن معتبر باشد، تابع باید True برگرداند. _(verify: static)_
21. کتابخانه PyJWT باید برای decode و بررسی expiry استفاده شود. _(verify: static)_
22. تست test_validate_token_expired در فایل tests/test_oversight_service.py وجود داشته باشد و با توکن JWT منقضی شده کار کند _(verify: backend_test)_
23. تست باید یک توکن JWT با exp در گذشته (مثلاً timestamp یک ساعت قبل) بسازد _(verify: static)_
24. تست باید validate_token را فراخوانی کرده و نتیجه False یا exception 401 را assert کند _(verify: static)_
25. تست باید با pytest به تنهایی قابل اجرا باشد و timeout 60 ثانیه داشته باشد _(verify: backend_test)_
26. خارج از scope: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched انجام نشود _(verify: static)_
27. فایل tests/test_oversight_service.py باید حداقل ۵ تابع تست با نام‌های test_add_watched, test_update_watched, test_run_task, test_scan_project, test_status_summary داشته باشد. _(verify: static)_
28. همه تست‌ها باید با pytest-asyncio سازگار باشند (استفاده از async def و pytest.mark.asyncio). _(verify: static)_
29. فایل باید با pytest قابل اجرا باشد و حداقل ۴ تست از ۵ تست با موفقیت عبور کنند. _(verify: backend_test)_
30. تست add_watched باید شامل بررسی افزودن یک repo به لیست watched باشد (بدون نیاز به repo معتبر واقعی). _(verify: static)_
31. تست status_summary باید بررسی کند که خروجی شامل فیلدهای مورد انتظار (مانند total_watched, active_tasks) است. _(verify: static)_
32. تست test_update_watched در فایل tests/test_oversight_service.py وجود داشته باشد و با دستور pytest backend/tests/test_oversight_service.py::test_update_watched اجرا شود. _(verify: backend_test)_
33. تست ابتدا یک watched project اضافه کند (با add_watched یا mock) و سپس آن را به‌روزرسانی کند. _(verify: static)_
34. تست موفقیت عملیات به‌روزرسانی را با assert تأیید کند (مثلاً بررسی status code یا پیام موفقیت). _(verify: static)_
35. تست به صورت ایزوله اجرا شود و وابستگی به دیتابیس واقعی نداشته باشد (استفاده از mock یا in-memory database). _(verify: static)_
36. تست status_summary باید بتواند چند watched project و task اضافه کند و سپس status_summary را فراخوانی کند. _(verify: backend_test)_
37. تست status_summary باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند و شامل تعداد کل پروژه‌ها، تعداد پروژه‌های فعال، تعداد پروژه‌های متوقف، تعداد کل task‌ها، تعداد task‌های در حال اجرا، تعداد task‌های تکمیل‌شده و تعداد task‌های ناموفق باشد. _(verify: backend_test)_
38. تست status_summary باید با لیست خالی از پروژه‌ها (بدون هیچ پروژه نظارت‌شده) کار کند و خطا ندهد. _(verify: backend_test)_
39. تست status_summary باید با پروژه‌هایی که task ندارند کار کند و خطا ندهد. _(verify: backend_test)_
40. تست‌های add_watched, update_watched, run_task, scan_project, status_summary باید حداقل ۵ تست مجزا باشند. _(verify: backend_test)_
41. همه تست‌های فایل tests/test_oversight_service.py با pytest پاس شوند _(verify: backend_test)_
42. هیچ تستی fail نشود — در صورت fail، خطا بررسی و رفع شود _(verify: backend_test)_
43. تغییرات فقط محدود به رفع باگ‌های تست‌ها باشد — نه نوشتن تست جدید یا اصلاح کد اصلی سرویس _(verify: static)_
44. مدت زمان اجرای تست‌ها از 60 ثانیه تجاوز نکند _(verify: backend_test)_
45. اجرای flake8 روی backend/app/services/oversight_service.py بدون هیچ warning یا error خاتمه یابد _(verify: static)_
46. اجرای flake8 روی tests/test_oversight_service.py بدون هیچ warning یا error خاتمه یابد _(verify: static)_
47. هیچ warning یا خطای linting در هیچ‌یک از دو فایل باقی نمانده باشد _(verify: static)_
48. تغییرات اعمال‌شده صرفاً مربوط به linting باشند و منطق کد تغییر نکند _(verify: manual_only)_
49. mypy روی backend/app/services/oversight_service.py بدون خطا اجرا شود _(verify: static)_
50. mypy روی tests/test_oversight_service.py بدون خطا اجرا شود _(verify: static)_
51. هیچ خطای type جدیدی به کد اضافه نشود _(verify: static)_
52. تست‌های مربوطه با pytest با موفقیت اجرا شوند _(verify: backend_test)_
53. endpoint /api/login با توکن منقضی شده (مثال: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiOjE1MTYyMzkwMjJ9) باید status code 401 برگرداند. _(verify: api_response)_
54. endpoint /api/login با توکن معتبر باید status code 200 برگرداند. _(verify: api_response)_
55. تابع validate_token باید expiry توکن را بررسی کند و در صورت منقضی بودن None برگرداند. _(verify: static)_
56. تست واحد برای expiry توکن در فایل تست مربوطه نوشته شود. _(verify: backend_test)_
57. فایل backend/app/services/oversight_service.py شامل تابع validate_token است. _(verify: static)_
58. در تابع validate_token از الگوی 'exp' برای بررسی انقضای توکن استفاده شده است. _(verify: static)_
59. در تابع validate_token از الگوی 'expired' یا 'ExpiredSignatureError' برای مدیریت خطای انقضا استفاده شده است. _(verify: static)_
60. در تابع validate_token از الگوی 'expiration' برای اشاره به زمان انقضا استفاده شده است. _(verify: static)_
61. فایل backend/tests/test_auth.py ایجاد شده و شامل تست‌های مربوط به expiry باشد _(verify: static)_
62. دستور `pytest backend/tests/test_auth.py -k expiry` با موفقیت اجرا شده و تمام تست‌ها پاس شوند _(verify: backend_test)_
63. تست‌های expiry حداقل دو حالت را پوشش دهند: توکن معتبر (200) و توکن منقضی (401) _(verify: static)_
64. سایر تست‌های پروژه با اجرای تست‌های جدید شکسته نشوند — `pytest backend/tests/` بدون خطا اجرا شود _(verify: backend_test)_
65. اگر endpoint /api/auth/verify وجود ندارد، endpoint مناسب برای تست expiry شناسایی و در تست استفاده شود _(verify: static)_
66. تست‌های frontend برای login با دستور `npm run test -- login` اجرا شوند و پاس شوند. _(verify: backend_test)_
67. اگر تست‌های login وجود ندارند، یک فایل تست جدید ایجاد شود که شامل حداقل ۳ تست (رندر فرم، ارسال معتبر، خطای نامعتبر) باشد. _(verify: static)_
68. تست‌ها از React Testing Library و Jest استفاده کنند. _(verify: static)_
69. تست‌ها خطاهای linting نداشته باشند. _(verify: backend_test)_
70. دستور pytest با پوشش کد بدون خطا اجرا شود _(verify: backend_test)_
71. پوشش کد برای ماژول oversight_service حداقل 70% باشد _(verify: static)_
72. گزارش پوشش کد شامل خطوط و شاخه‌ها باشد _(verify: static)_
73. pytest-cov نصب شده باشد _(verify: static)_
74. commit message شامل merged-from با شناسه‌های baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5 باشد _(verify: static)_
75. commit message شامل checklist از تغییرات انجام شده باشد _(verify: static)_
76. تغییرات کد یا اجرای تست‌ها در این commit انجام نشده باشد (فقط commit message) _(verify: static)_
77. اگر PR ایجاد شده، merged-from در description PR نیز درج شده باشد _(verify: manual_only)_

## Task Steps

### Step 1: ایجاد تست واحد برای add_watched با repo معتبر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد add_watched از کلاس OversightService است. تست باید با یک repo_full_name و default_branch معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های مربوط به add_watched با repo تکراری، تست‌های run_task، scan_project، auto_register_watched، و هرگونه تغییر در کد اصلی سرویس. نکته حیاتی: تست باید با pytest-asyncio نوشته شود و وابستگی‌های خارجی (GitHub API, Render API) باید mock شوند.
— [merged] این مرحله شامل نوشتن تست واحد برای متد add_watched است که بررسی می‌کند افزودن یک repo تکراری خطای مناسب (مانند HTTPException با status code 409 یا 400) برمی‌گرداند. خارج از این مرحله: تست add_watched با repo معتبر، تست‌های run_task، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید ابتدا یک repo اضافه کند و سپس تلاش برای افزودن مجدد همان repo انجام دهد.
**Excerpt:**
```
- تست add_watched با repo معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_add_watched_valid_repo", "timeout_seconds": 60}]
```

### Step 2: ایجاد تست واحد برای run_task با task معتبر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد run_task از کلاس OversightService است. تست باید با یک task معتبر کار کند و بررسی کند که status task به 'running' تغییر می‌کند. خارج از این مرحله: تست‌های add_watched، scan_project، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک task به لیست tasks اضافه کند و سپس run_task را فراخوانی کند.
**Excerpt:**
```
- تست run_task با task معتبر status را به running تغییر می‌دهد [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_run_task_changes_status_to_running", "timeout_seconds": 60}]
```

### Step 3: ایجاد تست واحد برای scan_project با watched_id نامعتبر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد scan_project از کلاس OversightService است. تست باید با یک watched_id نامعتبر (که در لیست watched وجود ندارد) کار کند و بررسی کند که خطای 404 (HTTPException با status code 404) برمی‌گرداند. خارج از این مرحله: تست‌های add_watched، run_task، auto_register_watched، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک watched_id تصادفی یا غیرموجود استفاده کند.
**Excerpt:**
```
- تست scan_project با watched_id نامعتبر 404 برمی‌گرداند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_scan_project_invalid_watched_id_returns_404", "timeout_seconds": 60}]
```

### Step 4: ایجاد تست واحد برای auto_register_watched با repo_full_name معتبر
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد auto_register_watched از کلاس OversightService است. تست باید با یک repo_full_name معتبر کار کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، و تغییر در کد اصلی سرویس. نکته حیاتی: تست باید یک repo_full_name معتبر (مانند 'owner/repo') استفاده کند و وابستگی‌های خارجی (GitHub API) باید mock شوند.
**Excerpt:**
```
- تست auto_register_watched با repo_full_name معتبر کار می‌کند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_auto_register_watched_valid_repo_full_name", "timeout_seconds": 60}]
```

### Step 5: اصلاح تابع validate_token برای بررسی expiry
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اصلاح تابع validate_token در فایل backend/app/services/oversight_service.py است. تابع باید توکن JWT را decode کند و فیلد exp (expiration) را با زمان فعلی (UTC) مقایسه کند. اگر توکن منقضی شده باشد، تابع باید False برگرداند یا HTTPException با status code 401 raise کند. خارج از این مرحله: نوشتن تست واحد برای expiry، تغییر endpoint /api/login، و تغییر در frontend. نکته حیاتی: باید از کتابخانه PyJWT برای decode و بررسی expiry استفاده شود.
**Excerpt:**
```
## 🎯 هدف (خلاصه ساختاریافته)
نقص امنیتی: تابع validate_token در oversight_service.py هیچ بررسی انقضا (expiry) انجام نمی‌دهد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:245-289` — `validate_token` — تابع اصلی که باید اصلاح شود
  ```python
  def validate_token(token: str) -> bool:
      # ⚠️ هیچ بررسی expiry ندارد
      if token:
          return True
      return False
  ```
```

### Step 6: ایجاد تست واحد برای validate_token با توکن منقضی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای تابع validate_token است که بررسی می‌کند با یک توکن JWT منقضی شده، تابع False برمی‌گرداند یا خطای 401 صادر می‌کند. تست باید در فایل tests/test_oversight_service.py اضافه شود. خارج از این مرحله: اصلاح تابع validate_token، تست‌های add_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید یک توکن JWT با exp در گذشته بسازد و validate_token را فراخوانی کند.
**Excerpt:**
```
- تست واحد جدید برای expiry اضافه شود [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py::test_validate_token_expired", "timeout_seconds": 60}]
```

### Step 7: ایجاد فایل تست tests/test_oversight_service.py
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد فایل tests/test_oversight_service.py است. فایل باید شامل تست‌های واحد برای متدهای اصلی OversightService باشد: add_watched, update_watched, run_task, scan_project, status_summary. خارج از این مرحله: نوشتن تست‌های خاص برای validate_token (که در مرحله 7 انجام می‌شود)، تست‌های add_watched با repo معتبر و تکراری (که در مراحل 1 و 2 انجام می‌شود)، تست run_task (مرحله 3)، تست scan_project (مرحله 4)، تست auto_register_watched (مرحله 5). نکته حیاتی: فایل باید با pytest و pytest-asyncio سازگار باشد.
**Excerpt:**
```
- فایل tests/test_oversight_service.py ایجاد شود [verify_method=static] [verify_plan={"grep_patterns": ["def test_", "class Test"], "files_hint": ["tests/test_oversight_service.py"]}]
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

### Step 8: نوشتن تست واحد برای update_watched
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد update_watched از کلاس OversightService است. تست باید یک watched project موجود را به‌روزرسانی کند و موفقیت عملیات را تأیید کند. خارج از این مرحله: تست‌های add_watched، run_task، scan_project، status_summary، auto_register_watched. نکته حیاتی: تست باید ابتدا یک watched project اضافه کند و سپس آن را به‌روزرسانی کند.
**Excerpt:**
```
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

### Step 9: نوشتن تست واحد برای status_summary
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست واحد برای متد status_summary از کلاس OversightService است. تست باید خلاصه وضعیت پروژه‌های نظارت‌شده را بررسی کند. خارج از این مرحله: تست‌های add_watched، update_watched، run_task، scan_project، auto_register_watched. نکته حیاتی: تست باید چند watched project و task اضافه کند و سپس status_summary را فراخوانی کند.
**Excerpt:**
```
- حداقل ۵ تست برای متدهای اصلی: add_watched, update_watched, run_task, scan_project, status_summary [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

### Step 10: اجرای همه تست‌ها و اطمینان از عبور
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای همه تست‌های موجود در فایل tests/test_oversight_service.py با pytest است. باید اطمینان حاصل شود که همه تست‌ها پاس می‌شوند. خارج از این مرحله: نوشتن تست‌های جدید، اصلاح کد اصلی سرویس. نکته حیاتی: اگر تستی fail شود، باید خطا بررسی و رفع شود.
**Excerpt:**
```
- همه تست‌ها پاس شوند [verify_method=backend_test] [verify_plan={"test_node": "tests/test_oversight_service.py", "timeout_seconds": 60}]
```

### Step 11: اجرای linter و اطمینان از عبور بدون warning
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای linter (مانند flake8 یا pylint) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ warning یا خطایی وجود ندارد. خارج از این مرحله: اجرای type-check، اجرای تست‌ها. نکته حیاتی: اگر linter warning داشته باشد، باید رفع شود.
**Excerpt:**
```
- linter بدون warning عبور می‌کند
```

### Step 12: اجرای type-check و اطمینان از موفقیت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای type-checker (مانند mypy) روی فایل‌های تغییر یافته (backend/app/services/oversight_service.py و tests/test_oversight_service.py) است. باید اطمینان حاصل شود که هیچ خطای type وجود ندارد. خارج از این مرحله: اجرای linter، اجرای تست‌ها. نکته حیاتی: اگر type-check خطا داشته باشد، باید رفع شود.
**Excerpt:**
```
- type-check موفق است (`tsc --noEmit` / `mypy`)
```

### Step 13: بررسی endpoint /api/login برای توکن منقضی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی endpoint /api/login است که از تابع validate_token استفاده می‌کند. باید اطمینان حاصل شود که با یک توکن منقضی شده، endpoint خطای 401 برمی‌گرداند. خارج از این مرحله: اصلاح تابع validate_token (که در مرحله 6 انجام شده)، نوشتن تست واحد برای expiry (مرحله 7). نکته حیاتی: این مرحله بیشتر جنبه تأیید دارد و ممکن است نیاز به تغییر در endpoint /api/login باشد.
**Excerpt:**
```
- endpoint /api/login برای توکن منقضی ۴۰۱ برمی‌گرداند [verify_method=api_response] [verify_plan={"method": "POST", "path": "/api/login", "headers": null, "json_body": {"token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxMjM0NTY3ODkwIiwibmFtZSI6IkpvaG4gRG9lIiwiaWF0IjoxNTE2MjM5MDIyLCJleHAiO]
```

### Step 14: بررسی تابع validate_token برای پارامتر exp
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی استاتیک کد تابع validate_token در فایل backend/app/services/oversight_service.py است. باید اطمینان حاصل شود که تابع پارامتر exp را چک می‌کند. خارج از این مرحله: اجرای تست‌ها، اصلاح endpoint /api/login. نکته حیاتی: این مرحله با grep برای الگوهای 'exp', 'expired', 'ExpiredSignatureError', 'expiration' در فایل انجام می‌شود.
**Excerpt:**
```
- تابع validate_token پارامتر exp را چک می‌کند [verify_method=static] [verify_plan={"grep_patterns": ["exp", "expired", "ExpiredSignatureError", "expiration"], "files_hint": ["backend/app/services/oversight_service.py"]}]
```

### Step 15: اجرای تست‌های auth برای expiry
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای تست‌های مربوط به auth با فیلتر expiry است. باید اطمینان حاصل شود که تست‌های مربوط به expiry در فایل backend/tests/test_auth.py پاس می‌شوند. خارج از این مرحله: اجرای تست‌های oversight_service، اجرای linter. نکته حیاتی: اگر فایل backend/tests/test_auth.py وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن داشته باشد.
**Excerpt:**
```
- `pytest backend/tests/test_auth.py -k expiry`
```

### Step 16: اجرای تست‌های frontend برای login
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای تست‌های frontend مربوط به login است. باید اطمینان حاصل شود که تست‌های مربوط به login در frontend پاس می‌شوند. خارج از این مرحله: اجرای تست‌های backend، اجرای linter. نکته حیاتی: اگر تست‌های frontend برای login وجود ندارد، این مرحله ممکن است نیاز به ایجاد آن‌ها داشته باشد.
**Excerpt:**
```
- `npm run test -- login`
```

### Step 17: اجرای pytest با پوشش کد برای oversight_service
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اجرای pytest با پوشش کد برای ماژول app.services.oversight_service است. باید اطمینان حاصل شود که پوشش تست برای این ماژول قابل قبول است. خارج از این مرحله: اجرای تست‌های خاص، اجرای linter. نکته حیاتی: این مرحله برای اندازه‌گیری پوشش کد است و ممکن است نیاز به نصب pytest-cov داشته باشد.
**Excerpt:**
```
- `pytest backend/tests/ --cov=app.services.oversight_service`
```

### Step 18: ایجاد commit با پیام واضح و checklist
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد commit یا PR با پیام واضح است که شامل checklist از همه تغییرات انجام شده باشد. پیام commit باید شامل merged-from با شناسه‌های تسک‌های اصلی باشد. خارج از این مرحله: انجام تغییرات کد، اجرای تست‌ها. نکته حیاتی: پیام commit باید شامل merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5 باشد.
**Excerpt:**
```
- در commit message: `merged-from: baa11f45-513f-4826-a45e-234f98940e55, 5e86c7d7-40b0-4c4f-a460-01a9d46bb801, 8ce807f0-1cbc-42e8-8be3-6da344dfeaa5`
```
