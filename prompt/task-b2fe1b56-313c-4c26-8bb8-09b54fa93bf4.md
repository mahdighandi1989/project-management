---
task_id: b2fe1b56-313c-4c26-8bb8-09b54fa93bf4
title: پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق در بک‌اند
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-23T08:27:41.145588+00:00'
updated_at: '2026-05-23T09:43:02.831889+00:00'
target_files:
- backend/app/models/project.py
- backend/app/api/routes/creator.py
- frontend/src/app/creator/page.tsx
---

# پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق در بک‌اند

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_695702_AgAD4xwA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد_

## درخواست اصلی کاربر از هوش مصنوعی  _(at: audio/ogg)_

درخواست اصلی کاربر از هوش مصنوعی:

[ 0m0s702ms - 0m17s852ms ] توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق.
[ 0m17s852ms - 0m37s892ms ] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد.
[ 0m37s892ms - 0m44s122ms ] هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## جزئیات و الزامات مربوط به خروجی درخواستی  _(at: audio/ogg)_

توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش، یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## موضوع کلی پیام صوتی  _(at: audio/ogg)_

[00:00:900] توی
[00:01:500] صفحه موتور خالق
[00:02:800] یه پروژه ای قبلاً ایجاد کرده بودم
[00:04:700] که فایل هاشو ساخت و بعدش هم پوش و کامیت کردم به
[00:08:900] گیت هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید میموند اون پروژه و توضیحات اون چیزایی که داده بودم
[00:14:800] به عنوان سابقه کار تو اون صفحه موتور خالق.
[00:17:800] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش.
[00:20:900] فکر می کنم اونم به صورت
[00:23:000] محلی احتمالاً
[00:25:400] لوکال
[00:27:200] توی لوکال هاست احتمالاً ذخیره میشه.
[00:30:800] چون فکر می کنم اون موقع که صفحه رو هارد رفرش می کنم
[00:34:600] اطلاعات رفته در صورتی که باید توی بک اند ذخیره می شد.
[00:37:700] هرچند که اینا رو توی فایل هاشو توی گیت هاب دارم
[00:40:500] ولی باید تو خود اونم به عنوان سابقه کار می موند.

## هدف کاربر از ارسال این فایل صوتی  _(at: audio/ogg)_

توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدش هم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش. یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## نوع و فرمت فایل پیوست  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

[00:00] توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## شناسایی گوینده پیام  _(at: audio/ogg)_

در ادامه رونویسی کامل و دقیق فایل صوتی ارائه شده است:

[ 0m0s969ms - 0m17s799ms ] توی صفحه موتور خالق یه پروژه ای قبلاً ایجاد کرده بودم که فایل هاشو ساخت و بعدش هم پوش و کامیت کردم به گیت هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید میموند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق.
[ 0m17s799ms - 0m37s799ms ] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می کنم اون موقع که صفحه رو هارد رفرش می کنم اطلاعات رفته در صورتی که باید توی بک اند ذخیره می شد.
[ 0m37s799ms - 0m44s199ms ] هرچند که اینا رو توی فایل هاشو توی گیت هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می موند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_695702_AgAD4xwA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد_

## درخواست اصلی کاربر از هوش مصنوعی  _(at: audio/ogg)_

درخواست اصلی کاربر از هوش مصنوعی:

[ 0m0s702ms - 0m17s852ms ] توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق.
[ 0m17s852ms - 0m37s892ms ] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد.
[ 0m37s892ms - 0m44s122ms ] هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## جزئیات و الزامات مربوط به خروجی درخواستی  _(at: audio/ogg)_

توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش، یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## موضوع کلی پیام صوتی  _(at: audio/ogg)_

[00:00:900] توی
[00:01:500] صفحه موتور خالق
[00:02:800] یه پروژه ای قبلاً ایجاد کرده بودم
[00:04:700] که فایل هاشو ساخت و بعدش هم پوش و کامیت کردم به
[00:08:900] گیت هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید میموند اون پروژه و توضیحات اون چیزایی که داده بودم
[00:14:800] به عنوان سابقه کار تو اون صفحه موتور خالق.
[00:17:800] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش.
[00:20:900] فکر می کنم اونم به صورت
[00:23:000] محلی احتمالاً
[00:25:400] لوکال
[00:27:200] توی لوکال هاست احتمالاً ذخیره میشه.
[00:30:800] چون فکر می کنم اون موقع که صفحه رو هارد رفرش می کنم
[00:34:600] اطلاعات رفته در صورتی که باید توی بک اند ذخیره می شد.
[00:37:700] هرچند که اینا رو توی فایل هاشو توی گیت هاب دارم
[00:40:500] ولی باید تو خود اونم به عنوان سابقه کار می موند.

## هدف کاربر از ارسال این فایل صوتی  _(at: audio/ogg)_

توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدش هم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش. یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## نوع و فرمت فایل پیوست  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

[00:00] توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.

## شناسایی گوینده پیام  _(at: audio/ogg)_

در ادامه رونویسی کامل و دقیق فایل صوتی ارائه شده است:

[ 0m0s969ms - 0m17s799ms ] توی صفحه موتور خالق یه پروژه ای قبلاً ایجاد کرده بودم که فایل هاشو ساخت و بعدش هم پوش و کامیت کردم به گیت هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید میموند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق.
[ 0m17s799ms - 0m37s799ms ] ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می کنم اونم به صورت محلی احتمالاً لوکال توی لوکال هاست احتمالاً ذخیره میشه. چون فکر می کنم اون موقع که صفحه رو هارد رفرش می کنم اطلاعات رفته در صورتی که باید توی بک اند ذخیره می شد.
[ 0m37s799ms - 0m44s199ms ] هرچند که اینا رو توی فایل هاشو توی گیت هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می موند.
```

## 📋 چک‌لیست مراحل (1 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق در بک‌اند** — این مرحله شامل شناسایی و جایگزینی مکانیسم فعلی ذخیره‌سازی موقت (احتمالاً لوکال استوریج یا سشن استوریج) پروژه‌های ایجاد شده در 'صفحه موتور خالق' با یک راهکار ذخیره‌سازی پایدار در بک‌اند است. این کار مستلزم طراحی و پیاده‌سازی یک مدل داده مناسب در پایگاه داده بک‌اند برای نگهداری اطلاعات پروژه و سابقه ک

---

# 🔹 مرحله 1: پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق در بک‌اند

**Scope:** این مرحله شامل شناسایی و جایگزینی مکانیسم فعلی ذخیره‌سازی موقت (احتمالاً لوکال استوریج یا سشن استوریج) پروژه‌های ایجاد شده در 'صفحه موتور خالق' با یک راهکار ذخیره‌سازی پایدار در بک‌اند است. این کار مستلزم طراحی و پیاده‌سازی یک مدل داده مناسب در پایگاه داده بک‌اند برای نگهداری اطلاعات پروژه و سابقه کار، اصلاح فرانت‌اند برای ارسال داده‌های پروژه به بک‌اند پس از ایجاد، و بازیابی این داده‌ها از بک‌اند هنگام بارگذاری 'صفحه موتور خالق' است. هدف اصلی اطمینان از این است که اطلاعات پروژه پس از رفرش صفحه یا مراجعه مجدد، از بین نرود.
**Key terms:** صفحه موتور خالق, پروژه, سابقه کار, لوکال هاست, بک‌اند, گیت‌هاب, هارد رفرش

**بخش مربوط از متن کاربر:**
```
توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/project.py` — `Project` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. snippet پیشنهادی برای افزودن فیلد `creator_history` به مدل `Project` است.
  ```python
  class Project(Base):
      __tablename__ = "projects"
      id: Mapped[str] = mapped_column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
      name: Mapped[str] = mapped_column(String, index=True)
      description: Mapped[Optional[str]] = mapped_column(String, nullable=True)
      project_type: Mapped[str] = mapped_column(String, default="generic")
      # ... سایر فیلدها ...
      creator_history: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True) # فیلد جدید
  ```
- `backend/app/api/routes/creator.py` — `create_creator_project` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. snippet پیشنهادی برای تعریف API Endpoints جدید است.
  ```python
  @router.post("/projects", response_model=CreatorProjectResponse)
  async def create_creator_project(request: CreatorProjectRequest, db: Session = Depends(get_db)):
      # منطق ذخیره پروژه و فایل‌ها در دیتابیس
      # استفاده از creator_history از request
      pass
  
  @router.get("/projects/{project_id}", response_model=CreatorProjectResponse)
  async def get_creator_project(project_id: str, db: Session = Depends(get_db)):
      # منطق بازیابی پروژه و فایل‌ها از دیتابیس
      pass
  ```
- `frontend/src/app/creator/page.tsx` — `CreatorPage` — این فایل deep-read شده اما محتوای آن ارائه نشده است. snippet پیشنهادی یک placeholder برای تغییرات UI است.
  ```tsx
  // در این فایل، منطق UI برای ایجاد پروژه و نمایش سابقه کار وجود دارد.
  // نیاز به فراخوانی API های بک‌اند برای ذخیره و بازیابی داده‌ها دارد.
  // مثال: useEffect برای بارگذاری پروژه‌ها، تابع handleCreateProject برای ارسال به بک‌اند.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: fastapi, nextjs. کتابخانه‌های مرتبط شامل SQLAlchemy برای ORM در بک‌اند، Pydantic برای اعتبارسنجی مدل‌ها در FastAPI، و React/Next.js به همراه Zustand برای مدیریت وضعیت در فرانت‌اند هستند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/creator_engine.py` — این سرویس احتمالاً منطق اصلی ایجاد پروژه را در خود دارد و باید برای استفاده از سرویس persistence جدید به‌روزرسانی شود.
- `backend/app/api/routes/github_import.py` (سطر 121) — این فایل نحوه ذخیره‌سازی پروژه‌های ایمپورت شده را نشان می‌دهد و می‌تواند الگویی برای ذخیره‌سازی پروژه‌های موتور خالق باشد. همچنین، مدل `Project` را import می‌کند.
- `backend/app/core/database.py` (سطر 10) — این فایل مسئول مدیریت سشن‌های دیتابیس است و هر سرویس persistence جدیدی به آن وابسته خواهد بود.
- `backend/app/services/project_service.py` — این سرویس احتمالا منطق عمومی مدیریت پروژه را در خود دارد و ممکن است نیاز به توسعه یا هماهنگی با منطق جدید داشته باشد.
- `backend/app/api/routes/projects.py` — این فایل مسیرهای API عمومی برای مدیریت پروژه‌ها را تعریف می‌کند و ممکن است نیاز به به‌روزرسانی برای شامل شدن پروژه‌های موتور خالق داشته باشد.

## 🌐 نقشهٔ وابستگی‌ها
مدل `Project` در `backend/app/models/project.py` یک موجودیت مرکزی است که توسط فایل‌های متعددی مانند `backend/app/api/routes/github_import.py`، `backend/app/api/routes/analysis.py`، `backend/app/api/routes/oversight.py` و `backend/app/api/routes/project_journal.py` import می‌شود. تغییرات در این مدل نیازمند بررسی دقیق این وابستگی‌ها است. سرویس‌های جدید یا توسعه‌یافته برای persistence (مانند `backend/app/services/creator_persistence_service.py` یا `backend/app/services/creator_engine.py`) به `backend/app/core/database.py` برای مدیریت سشن‌های دیتابیس وابسته خواهند بود. `frontend/src/app/creator/page.tsx` به API Endpoints جدید در `backend/app/api/routes/creator.py` برای ذخیره و بازیابی داده‌ها وابسته خواهد شد.

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی ذخیره‌سازی پایدار برای پروژه‌های ایجاد شده در 'صفحه موتور خالق' را دارد. مشکل اصلی این است که مکانیسم فعلی ذخیره‌سازی موقت (احتمالاً لوکال استوریج یا سشن استوریج) باعث می‌شود اطلاعات پروژه و 'سابقه کار' (توضیحات و ورودی‌های کاربر به AI) پس از 'هارد رفرش' صفحه یا مراجعه مجدد، از بین برود. کاربر اشاره می‌کند که فایل‌های پروژه به 'گیت‌هاب' پوش و کامیت شده‌اند، اما انتظار دارد که 'سابقه کار' و جزئیات پروژه در خود 'صفحه موتور خالق' نیز به صورت پایدار در 'بک‌اند' ذخیره شود.

این تسک شامل طراحی و پیاده‌سازی یک مدل داده مناسب در پایگاه داده بک‌اند برای نگهداری اطلاعات پروژه و سابقه کار، اصلاح فرانت‌اند برای ارسال داده‌های پروژه به بک‌اند پس از ایجاد، و بازیابی این داده‌ها از بک‌اند هنگام بارگذاری 'صفحه موتور خالق' است. هدف اصلی اطمینان از این است که اطلاعات پروژه پس از رفرش صفحه یا مراجعه مجدد، از بین نرود.

**شواهد در کد:**
*   فایل `backend/app/api/routes/github_import.py` (خطوط 109-131) نشان می‌دهد که پروژه‌های ایمپورت شده از GitHub با استفاده از `service.save_imported_project(result)` در دیتابیس ذخیره می‌شوند و `db_project_id` برگردانده می‌شود. این نشان می‌دهد که مدل `Project` (احتمالاً در `backend/app/models/project.py`) برای ذخیره‌سازی پروژه‌ها وجود دارد و می‌توان آن را برای پروژه‌های موتور خالق نیز استفاده یا توسعه داد.
*   فایل `backend/app/api/routes/analysis.py` (خطوط 271-306) نحوه دریافت و ذخیره `AnalysisReport` را نشان می‌دهد که یک مدل داده پیچیده مرتبط با پروژه است. این الگو می‌تواند برای ذخیره 'سابقه کار' موتور خالق نیز به کار رود.
*   فایل `frontend/src/app/creator/page.tsx` به عنوان 'صفحه موتور خالق' شناسایی شده است که نقطه اصلی تعامل کاربر برای ایجاد پروژه است و نیاز به تغییرات فرانت‌اندی دارد.
*   فایل `backend/app/core/database.py` (خطوط 10-11) نحوه ایجاد و مدیریت سشن‌های دیتابیس را نشان می‌دهد که برای هرگونه عملیات ذخیره‌سازی در بک‌اند ضروری است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فیلد `creator_history` از نوع JSONB به مدل `Project` در `backend/app/models/project.py` اضافه شود.
- [ ] API Endpoint `POST /api/creator/projects` در `backend/app/api/routes/creator.py` با موفقیت یک پروژه جدید را به همراه 'سابقه کار' آن در دیتابیس ذخیره کند و `project_id` را برگرداند.
- [ ] API Endpoint `GET /api/creator/projects/{project_id}` در `backend/app/api/routes/creator.py` با موفقیت جزئیات یک پروژه شامل `creator_history` و فایل‌های مرتبط را از دیتابیس بازیابی کند.
- [ ] پس از ایجاد یک پروژه در 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، داده‌های پروژه و 'سابقه کار' به بک‌اند ارسال و ذخیره شوند.
- [ ] پس از 'هارد رفرش' یا مراجعه مجدد به 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، پروژه‌های قبلی و 'سابقه کار' آن‌ها از بک‌اند بارگذاری و نمایش داده شوند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق، مراحل زیر پیشنهاد می‌شود:

1.  **توسعه مدل داده `Project`:** یک فیلد جدید (مثلاً `creator_history` از نوع JSONB) به مدل `Project` در `backend/app/models/project.py` اضافه شود تا 'سابقه کار' و جزئیات مربوط به فرآیند ایجاد پروژه توسط AI (مانند پرامپت‌های اولیه، مراحل طی شده، و خروجی‌های میانی) را ذخیره کند. همچنین، اطمینان حاصل شود که `project_type` برای پروژه‌های ایجاد شده توسط موتور خالق به درستی تنظیم شود.
2.  **ایجاد/توسعه API Endpoints در بک‌اند:** در `backend/app/api/routes/creator.py`، API Endpoints جدیدی برای `POST /api/creator/projects` (برای ذخیره پروژه جدید)، `PUT /api/creator/projects/{project_id}` (برای به‌روزرسانی پروژه و سابقه کار) و `GET /api/creator/projects/{project_id}` (برای بازیابی پروژه و سابقه کار) پیاده‌سازی شود. این Endpoints از `backend/app/services/creator_engine.py` یا یک سرویس جدید برای منطق ذخیره‌سازی استفاده خواهند کرد.
3.  **پیاده‌سازی منطق سرویس در بک‌اند:** یک سرویس جدید (مثلاً `backend/app/services/creator_persistence_service.py`) ایجاد شود یا `backend/app/services/creator_engine.py` توسعه یابد تا منطق CRUD برای پروژه‌های موتور خالق را با استفاده از مدل `Project` و `ProjectFile` پیاده‌سازی کند. این سرویس با `backend/app/core/database.py` برای تعامل با دیتابیس کار خواهد کرد.
4.  **اصلاح فرانت‌اند:** در `frontend/src/app/creator/page.tsx`، منطق مربوط به ایجاد و به‌روزرسانی پروژه تغییر یابد تا به جای ذخیره‌سازی موقت، داده‌ها را از طریق API Endpoints جدید به بک‌اند ارسال کند. همچنین، هنگام بارگذاری صفحه، پروژه‌های موجود از بک‌اند بازیابی و نمایش داده شوند. این شامل مدیریت وضعیت (state management) برای نمایش لیست پروژه‌ها و جزئیات هر پروژه است.

## 💡 نمونه‌های قبل/بعد
**مدل Project (backend/app/models/project.py)**

_قبل:_
```
class Project(Base):
    __tablename__ = "projects"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    name: Mapped[str] = mapped_column(String, index=True)
    description: Mapped[Optional[str]] = mapped_column(String, nullable=True)
    project_type: Mapped[str] = mapped_column(String, default="generic")
```

_بعد:_
```
class Project(Base):
    __tablename__ = "projects"
    id: Mapped[str] = mapped_column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    name: Mapped[str] = mapped_column(String, index=True)
    description: Mapped[Optional[str]] = mapped_column(String, nullable=True)
    project_type: Mapped[str] = mapped_column(String, default="creator_engine") # تغییر پیش‌فرض یا اضافه کردن نوع جدید
    creator_history: Mapped[Optional[dict]] = mapped_column(JSON, nullable=True) # فیلد جدید برای سابقه کار
```

**منطق ذخیره‌سازی در frontend/src/app/creator/page.tsx**

_قبل:_
```
// منطق فعلی: ذخیره موقت در state محلی یا localStorage
const [currentProject, setCurrentProject] = useState(null);

const handleCreateProject = (projectData) => {
    setCurrentProject(projectData);
    localStorage.setItem('creatorProject', JSON.stringify(projectData));
};
```

_بعد:_
```
// منطق جدید: ارسال به بک‌اند برای ذخیره‌سازی پایدار
import axios from 'axios';

const [currentProject, setCurrentProject] = useState(null);

const handleCreateProject = async (projectData) => {
    try {
        const response = await axios.post('/api/creator/projects', projectData);
        setCurrentProject(response.data.project_data);
        // Optional: remove local storage item
        localStorage.removeItem('creatorProject');
    } catch (error) {
        console.error('Failed to save project:', error);
    }
};

useEffect(() => {
    const loadProjects = async () => {
        try {
            const response = await axios.get('/api/creator/projects');
            // Set projects to state
        } catch (error) {
            console.error('Failed to load projects:', error);
        }
    };
    loadProjects();
}, []);
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_creator_api.py`
- `npm run test -- frontend/src/app/creator/page.test.tsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **مهاجرت دیتابیس:** افزودن فیلد `creator_history` به مدل `Project` در `backend/app/models/project.py` نیازمند اجرای مهاجرت دیتابیس (مانند Alembic) است. این می‌تواند بر داده‌های موجود تأثیر بگذارد و در صورت عدم مدیریت صحیح، منجر به از دست رفتن داده یا ناسازگاری شود.
2. **سازگاری با عقب:** سایر بخش‌های سیستم که از مدل `Project` استفاده می‌کنند (مانند `backend/app/api/routes/github_import.py`، `backend/app/api/routes/analysis.py`، `backend/app/services/oversight_service.py`) باید بررسی شوند تا اطمینان حاصل شود که تغییرات جدید باعث شکست آن‌ها نمی‌شود. فیلد جدید باید nullable باشد تا با پروژه‌های قدیمی سازگار باشد.
3. **پیچیدگی مدیریت وضعیت فرانت‌اند:** ادغام ذخیره‌سازی پایدار با مدیریت وضعیت موجود در `frontend/src/app/creator/page.tsx` (احتمالاً با Zustand) نیازمند مدیریت دقیق حالت‌های بارگذاری، ذخیره و خطا برای جلوگیری از ناسازگاری داده‌ها و بهبود تجربه کاربری است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'فیلد `creator_history` از نوع JSONB به مدل `Project` در `backend/app/models/project.py` اضافه شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class Project', 'creator_history: Mapped\\[Optional\\[dict\\]\\] = mapped_column\\(JSON, nullable=True\\)'], 'files_hint': ['backend/app/models/project.py']}}
- [ ] {'text': "API Endpoint `POST /api/creator/projects` در `backend/app/api/routes/creator.py` با موفقیت یک پروژه جدید را به همراه 'سابقه کار' آن در دیتابیس ذخیره کند و `project_id` را برگرداند.", 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/creator/projects', 'body': {'name': 'Test Project', 'description': 'A project created by Creator Engine', 'initial_prompt': 'Create a simple web app', 'generated_files': [{'path': 'index.html', 'content': '<html></html>'}]}, 'expected_status': 200, 'required_fields': ['success', 'project_id']}}
- [ ] {'text': 'API Endpoint `GET /api/creator/projects/{project_id}` در `backend/app/api/routes/creator.py` با موفقیت جزئیات یک پروژه شامل `creator_history` و فایل\u200cهای مرتبط را از دیتابیس بازیابی کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/api/creator/projects/{project_id}', 'expected_status': 200, 'required_fields': ['success', 'project_data.id', 'project_data.creator_history', 'project_data.files']}}
- [ ] {'text': "پس از ایجاد یک پروژه در 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، داده\u200cهای پروژه و 'سابقه کار' به بک\u200cاند ارسال و ذخیره شوند.", 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/creator'}, {'action': 'type', 'selector': "[data-testid='project-name-input']", 'text': 'My Persistent Project'}, {'action': 'type', 'selector': "[data-testid='prompt-input']", 'text': 'Build a simple Python FastAPI backend'}, {'action': 'click', 'selector': "[data-testid='create-project-button']"}, {'action': 'wait_for_network_idle', 'timeout_ms': 5000}, {'action': 'assert_api_call', 'method': 'POST', 'path_contains': '/api/creator/projects', 'status': 200}]}}
- [ ] {'text': "پس از 'هارد رفرش' یا مراجعه مجدد به 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، پروژه\u200cهای قبلی و 'سابقه کار' آن\u200cها از بک\u200cاند بارگذاری و نمایش داده شوند.", 'verify_method': 'ui_interaction', 'verify_plan': {'ui_steps': [{'action': 'navigate', 'url': '/creator'}, {'action': 'reload_page'}, {'action': 'wait_for_selector', 'selector': "[data-testid='project-list-item-My Persistent Project']", 'timeout_ms': 5000}, {'action': 'assert_visible', 'selector': "[data-testid='project-list-item-My Persistent Project']"}]}}

## Acceptance Criteria

1. فیلد `creator_history` از نوع JSONB به مدل `Project` در `backend/app/models/project.py` اضافه شود. _(verify: static)_
2. API Endpoint `POST /api/creator/projects` در `backend/app/api/routes/creator.py` با موفقیت یک پروژه جدید را به همراه 'سابقه کار' آن در دیتابیس ذخیره کند و `project_id` را برگرداند. _(verify: api_response)_
3. API Endpoint `GET /api/creator/projects/{project_id}` در `backend/app/api/routes/creator.py` با موفقیت جزئیات یک پروژه شامل `creator_history` و فایل‌های مرتبط را از دیتابیس بازیابی کند. _(verify: api_response)_
4. پس از ایجاد یک پروژه در 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، داده‌های پروژه و 'سابقه کار' به بک‌اند ارسال و ذخیره شوند. _(verify: ui_interaction)_
5. پس از 'هارد رفرش' یا مراجعه مجدد به 'صفحه موتور خالق' (`frontend/src/app/creator/page.tsx`)، پروژه‌های قبلی و 'سابقه کار' آن‌ها از بک‌اند بارگذاری و نمایش داده شوند. _(verify: ui_interaction)_

## Task Steps

### Step 1: پیاده‌سازی ذخیره‌سازی پایدار پروژه‌های موتور خالق در بک‌اند
**Status:** `pending` (0%)
**Scope:** این مرحله شامل شناسایی و جایگزینی مکانیسم فعلی ذخیره‌سازی موقت (احتمالاً لوکال استوریج یا سشن استوریج) پروژه‌های ایجاد شده در 'صفحه موتور خالق' با یک راهکار ذخیره‌سازی پایدار در بک‌اند است. این کار مستلزم طراحی و پیاده‌سازی یک مدل داده مناسب در پایگاه داده بک‌اند برای نگهداری اطلاعات پروژه و سابقه کار، اصلاح فرانت‌اند برای ارسال داده‌های پروژه به بک‌اند پس از ایجاد، و بازیابی این داده‌ها از بک‌اند هنگام بارگذاری 'صفحه موتور خالق' است. هدف اصلی اطمینان از این است که اطلاعات پروژه پس از رفرش صفحه یا مراجعه مجدد، از بین نرود.
**Excerpt:**
```
توی صفحه موتور خالق یه پروژه‌ای قبلاً ایجاد کرده بودم که فایل‌هاشو ساخت و بعدشم پوش و کامیت کردم به گیت‌هاب و بعدش دیدم بازم هستش یعنی قاعدتاً باید می‌موند اون پروژه و توضیحات اون چیزایی که داده بودم به عنوان سابقه کار تو اون صفحه موتور خالق. ولی انگار بعد چند روز که مراجعه کردم دیگه نیستش. فکر می‌کنم اونم به صورت محلی احتمالاً لوکال توی لوکال‌هاست احتمالاً ذخیره میشه. چون فکر می‌کنم اون موقع که صفحه رو هارد رفرش می‌کنم اطلاعات رفته در صورتی که باید توی بک‌اند ذخیره می‌شد. هرچند که اینا رو توی فایل‌هاشو توی گیت‌هاب دارم ولی باید تو خود اونم به عنوان سابقه کار می‌موند.
```
