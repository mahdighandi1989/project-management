---
task_id: 56770b0f-0a9e-48d9-a260-7f61081d9bfa
title: duplicate model profile endpoints در routes/models.py و routes/model_profiles.py
type: refactor
priority: high
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T04:21:36.109729+00:00'
updated_at: '2026-05-29T16:43:45.857788+00:00'
archived: true
archived_at: '2026-05-17T10:21:05.763076+00:00'
target_files:
- backend/app/api/routes/models.py
- backend/app/api/routes/model_profiles.py
---

# duplicate model profile endpoints در routes/models.py و routes/model_profiles.py

## Raw Idea

دو فایل route مجزا برای پروفایل مدل‌ها وجود دارد: `backend/app/api/routes/models.py` (prefix='/models') و `backend/app/api/routes/model_profiles.py` (prefix='/api/models'). هر دو endpoint‌های مشابهی دارند: `/profiles`, `/rankings`, `/leaderboard`. این duplication باعث سردرگمی frontend و نگهداری دشوار می‌شود. frontend (`frontend/src/app/model-profiles/page.tsx`) از endpoint‌های `/api/models/profiles` و `/api/models/leaderboard` استفاده می‌کند که در `model_profiles.py` تعریف شده‌اند، در حالی که `models.py` نیز endpoint‌های `/models/profiles` و `/models/leaderboard` دارد که ممکن است پاسخ متفاوتی بدهند.
---
[scan #2 at 2026-05-15T07:37:26.220013+00:00]
دو فایل route مجزا برای پروفایل مدل‌ها وجود دارد: `backend/app/api/routes/models.py` (prefix=/models) و `backend/app/api/routes/model_profiles.py` (prefix=/api/models). هر دو endpoint‌های GET /profiles و GET /rankings را ارائه می‌دهند اما با response shape متفاوت. این باعث سردرگمی frontend و مصرف‌کن
---
[scan #3 at 2026-05-15T10:28:15.935633+00:00]
دو فایل route مختلف، endpoint‌های تقریباً یکسانی برای پروفایل مدل‌ها ارائه می‌دهند. `backend/app/api/routes/models.py` شامل `GET /models/profiles` (خط ۲۶۶)، `GET /models/leaderboard` (خط ۲۸۹) و `GET /models/rankings` (خط ۳۵۰) است. `backend/app/api/routes/model_profiles.py` شامل `GET /api/models/prof
---
[scan #4 at 2026-05-15T16:41:56.813501+00:00]
دو فایل route مجزا برای پروفایل مدل‌ها وجود دارد: `backend/app/api/routes/models.py` (prefix=/models) و `backend/app/api/routes/model_profiles.py` (prefix=/api/models). هر دو endpoint‌های `/profiles` و `/rankings` را ارائه می‌دهند با منطق متفاوت. این باعث سردرگمی frontend و نگهداری دشوار می‌شود. فای
---
[scan #5 at 2026-05-15T19:16:11.956697+00:00]
دو فایل route مختلف، endpoint‌های مشابهی برای پروفایل مدل‌ها ارائه می‌دهند. `backend/app/api/routes/models.py` شامل `/models/profiles` (خط 266)، `/models/leaderboard` (خط 289) و `/models/rankings` (خط 350) است، در حالی که `backend/app/api/routes/model_profiles.py` شامل `/api/models/profiles` (خط 142

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
duplicate model profile endpoints در routes/models.py و routes/model_profiles.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/models.py:266-287` — `get_model_profiles` — این endpoint با `/models/profiles` تکراری است با `model_profiles.py` که در `/api/models/profiles` سرویس می‌دهد
  ```python
  @router.get("/profiles")
  async def get_model_profiles():
      """
      دریافت پروفایل همه مدل‌های AI
      """
      try:
          from ...services.model_profiler import get_model_profiler
          profiler = get_model_profiler()
          profiles = profiler.get_all_profiles()
  ```
- `backend/app/api/routes/model_profiles.py:142-245` — `get_all_profiles` — این endpoint کامل‌تر است و از دیتابیس می‌خواند
  ```python
  @router.get("/profiles")
  async def get_all_profiles(
      sort_by: str = Query("overall_score", description="فیلد مرتب‌سازی"),
      order: str = Query("desc", description="ترتیب: asc یا desc"),
      limit: int = Query(50, description="تعداد نتایج"),
      use_fallback: bool = Query(True, description="استفاده از داده‌های پیش‌فرض در صورت خالی بودن"),
      db=Depends(get_db)
  ):
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Next.js 14 App Router

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/models.py` (سطر 266) — دارای endpoint تکراری `/profiles` و `/leaderboard` و `/rankings`
- `backend/app/api/routes/model_profiles.py` (سطر 142) — دارای endpoint اصلی `/profiles` با قابلیت‌های بیشتر
- `frontend/src/app/model-profiles/page.tsx` (سطر 116) — از `/api/models/profiles` و `/api/models/leaderboard` استفاده می‌کند
- `frontend/src/app/models/page.tsx` (سطر 1) — ممکن است از endpoint اشتباه استفاده کند

## 🌐 نقشهٔ وابستگی‌ها
هر دو route در `backend/app/main.py` import شده‌اند. frontend از endpoint‌های `model_profiles.py` استفاده می‌کند.

## 🔍 Context و وضعیت فعلی
دو فایل route مجزا برای پروفایل مدل‌ها وجود دارد: `backend/app/api/routes/models.py` (prefix='/models') و `backend/app/api/routes/model_profiles.py` (prefix='/api/models'). هر دو endpoint‌های مشابهی دارند: `/profiles`, `/rankings`, `/leaderboard`. این duplication باعث سردرگمی frontend و نگهداری دشوار می‌شود. frontend (`frontend/src/app/model-profiles/page.tsx`) از endpoint‌های `/api/models/profiles` و `/api/models/leaderboard` استفاده می‌کند که در `model_profiles.py` تعریف شده‌اند، در حالی که `models.py` نیز endpoint‌های `/models/profiles` و `/models/leaderboard` دارد که ممکن است پاسخ متفاوتی بدهند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] اعمال تغییر بدون شکستن تست‌های موجود
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ادغام endpoint‌های پروفایل مدل در یک فایل route واحد. پیشنهاد: تمام endpoint‌های مربوط به پروفایل را در `model_profiles.py` نگه دار و endpoint‌های تکراری را از `models.py` حذف کن. سپس frontend را به‌روز کن تا فقط از یک مسیر استفاده کند.

## 💡 نمونه‌های قبل/بعد
**حذف endpoint تکراری از models.py**

_قبل:_
```
@router.get("/profiles")
async def get_model_profiles():
    ...
```

_بعد:_
```
# حذف شود — از model_profiles.py استفاده کنید
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run build`
- `npm run lint`

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. اعمال تغییر بدون شکستن تست‌های موجود _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است _(verify: static)_
