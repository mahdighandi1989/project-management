---
task_id: 1f3f3fcc-c8ab-40f6-8670-e21c564999f7
title: AI Manager بدون پوشش تست — ۳۸ فایل به آن وابسته‌اند
type: bug
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T16:38:42.702604+00:00'
updated_at: '2026-05-20T04:24:34.487978+00:00'
archived: true
archived_at: '2026-05-17T11:01:52.863730+00:00'
target_files:
- backend/app/services/ai_manager.py
---

# AI Manager بدون پوشش تست — ۳۸ فایل به آن وابسته‌اند

## Raw Idea

فایل `backend/app/services/ai_manager.py` با ۳۸ import کننده، هسته مرکزی سیستم AI است. این سرویس مدیریت تمام providerهای AI (OpenAI, Claude, Gemini, DeepSeek, Perplexity, Groq, OpenRouter) را بر عهده دارد و توسط analysis, models, orchestrator, project_health, project_journal و بسیاری از routeها استفاده می‌شود. عدم وجود تست برای این فایل به معنی عدم اطمینان از عملکرد صحیح fallback mechanism، انتخاب مدل، مدیریت خطا و rate limiting است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
AI Manager بدون پوشش تست — ۳۸ فایل به آن وابسته‌اند

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/ai_manager.py:1-500` — `AI Manager` — کلاس اصلی که باید تست شود
  ```python
  class AIManager:
      def __init__(self):
          self._providers = {}
          self._init_errors = {}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Python 3.11+ با پشتیبانی از ۷ provider مختلف AI

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/analysis.py` (سطر 113) — از ai_manager برای تحلیل استفاده می‌کند
- `backend/app/api/routes/models.py` (سطر 94) — از ai_manager برای لیست مدل‌ها استفاده می‌کند
- `backend/app/api/routes/orchestrator.py` (سطر 323) — از ai_manager برای orchestration استفاده می‌کند
- `backend/app/api/routes/project_health.py` (سطر 368) — از ai_manager برای تحلیل سلامت استفاده می‌کند
- `backend/app/api/routes/project_journal.py` (سطر 29) — از ai_manager برای ژورنال استفاده می‌کند
- `backend/app/services/ai_base.py` — `ai_manager.py` این فایل را import می‌کند
- `backend/app/services/openai_service.py` — `ai_manager.py` این فایل را import می‌کند
- `backend/app/services/claude_service.py` — `ai_manager.py` این فایل را import می‌کند
- `backend/app/services/gemini_service.py` — `ai_manager.py` این فایل را import می‌کند

## 🌐 نقشهٔ وابستگی‌ها
هاب مرکزی با ۳۸ وابسته مستقیم — هر تغییری در این فایل می‌تواند کل سیستم را تحت تأثیر قرار دهد

## 🔍 Context و وضعیت فعلی
فایل `backend/app/services/ai_manager.py` با ۳۸ import کننده، هسته مرکزی سیستم AI است. این سرویس مدیریت تمام providerهای AI (OpenAI, Claude, Gemini, DeepSeek, Perplexity, Groq, OpenRouter) را بر عهده دارد و توسط analysis, models, orchestrator, project_health, project_journal و بسیاری از routeها استفاده می‌شود. عدم وجود تست برای این فایل به معنی عدم اطمینان از عملکرد صحیح fallback mechanism، انتخاب مدل، مدیریت خطا و rate limiting است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] اعمال تغییر بدون شکستن تست‌های موجود
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد تست‌های واحد برای ai_manager با mock کردن providerهای مختلف و تست سناریوهای fallback، خطا، و انتخاب مدل

## 💡 نمونه‌های قبل/بعد
**تست fallback mechanism**

_قبل:_
```
بدون تست — fallback هرگز آزمایش نشده
```

_بعد:_
```
def test_fallback_when_primary_fails():
    mock_primary = AsyncMock(side_effect=Exception('API Error'))
    mock_fallback = AsyncMock(return_value='success')
    manager = AIM
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run build`
- `npm run lint`

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. اعمال تغییر بدون شکستن تست‌های موجود _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است _(verify: static)_
