---
task_id: e89127c2-6272-4fec-981c-8ca4e889e6a7
title: توابع کمکی duplicate در orchestrator.py و project_memory.py برای استخراج تکنولوژی از فایل‌ها
type: refactor
priority: medium
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T07:39:28.210292+00:00'
updated_at: '2026-06-02T17:46:23.902333+00:00'
archived: true
archived_at: '2026-05-17T18:33:17.761543+00:00'
target_files:
- backend/app/api/routes/orchestrator.py
- backend/app/api/routes/project_memory.py
---

# توابع کمکی duplicate در orchestrator.py و project_memory.py برای استخراج تکنولوژی از فایل‌ها

## Raw Idea

تابع extract_technologies_from_project در orchestrator.py (خط 22-128) و منطق مشابه استخراج تکنولوژی از extension فایل‌ها در project_memory.py (خط 44-96) هر دو یک نگاشت extension→tech را تعریف کرده‌اند. این duplication باعث می‌شود تغییر در یک نگاشت (مثلاً افزودن .svelte) در دیگری اعمال نشود و رفتار سیستم ناهماهنگ شود. orchestrator.py از این تابع برای auto-setup و project_memory.py برای commit به GitHub استفاده می‌کند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
توابع کمکی duplicate در orchestrator.py و project_memory.py برای استخراج تکنولوژی از فایل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/orchestrator.py:22-128` — `extract_technologies_from_project` — این تابع باید به ماژول مشترک منتقل شود
  ```python
  def extract_technologies_from_project(project: Dict, estimated_files: List[str]) -> List[str]:
      technologies = set()
      project_type_tech = {
          "web_app": ["HTML", "CSS", "JavaScript"],
          ...
      }
  ```
- `backend/app/api/routes/project_memory.py:44-96` — `extract_code_blocks` — نگاشت duplicate در تابع extract_code_blocks
  ```python
  extension_tech = {
          ".py": "Python",
          ".js": "JavaScript",
          ...
      }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Python 3.11

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/orchestrator.py` (سطر 374) — استفاده از extract_technologies_from_project در smart_setup
- `backend/app/api/routes/project_memory.py` (سطر 346) — استفاده از extension_tech در commit_to_github
- `backend/app/services/smart_orchestrator.py` — `orchestrator.py` این فایل را import می‌کند
- `backend/app/services/ai_manager.py` — `orchestrator.py` این فایل را import می‌کند
- `backend/app/services/project_service.py` — `orchestrator.py` این فایل را import می‌کند
- `backend/app/services/creator_engine.py` — `orchestrator.py` این فایل را import می‌کند
- `backend/app/main.py` — این فایل `orchestrator.py` را import می‌کند (caller)
- `backend/app/core/database.py` — `project_memory.py` این فایل را import می‌کند

## 🌐 نقشهٔ وابستگی‌ها
هر دو route از این توابع استفاده می‌کنند؛ تغییر آن‌ها نیاز به آپدیت import در هر دو فایل دارد.

## 🔍 Context و وضعیت فعلی
تابع extract_technologies_from_project در orchestrator.py (خط 22-128) و منطق مشابه استخراج تکنولوژی از extension فایل‌ها در project_memory.py (خط 44-96) هر دو یک نگاشت extension→tech را تعریف کرده‌اند. این duplication باعث می‌شود تغییر در یک نگاشت (مثلاً افزودن .svelte) در دیگری اعمال نشود و رفتار سیستم ناهماهنگ شود. orchestrator.py از این تابع برای auto-setup و project_memory.py برای commit به GitHub استفاده می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] ماژول core/tech_detector.py ایجاد شود و نگاشت extension→tech را در خود داشته باشد
- [ ] orchestrator.py از تابع جدید import کند و تابع قدیمی حذف شود
- [ ] project_memory.py از تابع جدید import کند و نگاشت duplicate حذف شود
- [ ] همه تست‌های موجود پاس شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. یک ماژول مشترک backend/app/core/tech_detector.py ایجاد کن که نگاشت extension→tech و تابع extract_technologies_from_files را در خود داشته باشد. سپس در orchestrator.py و project_memory.py از آن import کن.

## 💡 نمونه‌های قبل/بعد
**انتقال به ماژول مشترک**

_قبل:_
```
# orchestrator.py
def extract_technologies_from_project(...):
    extension_tech = {...}
```

_بعد:_
```
# core/tech_detector.py
def extract_technologies_from_files(files):
    extension_tech = {...}

# orchestrator.py
from ...core.tech_detector import extract_technologies_from_files
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/ -x`
- `ruff check backend/app/core/tech_detector.py`

## ⚠️ ریسک‌ها و موارد احتیاط
کم — فقط refactor داخلی، بدون تغییر API

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: medium
- تخمین زمان: small

## Acceptance Criteria

1. ماژول core/tech_detector.py ایجاد شود و نگاشت extension→tech را در خود داشته باشد _(verify: static)_
2. orchestrator.py از تابع جدید import کند و تابع قدیمی حذف شود _(verify: static)_
3. project_memory.py از تابع جدید import کند و نگاشت duplicate حذف شود _(verify: static)_
4. همه تست‌های موجود پاس شوند _(verify: backend_test)_
