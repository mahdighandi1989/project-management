---
task_id: eee38d80-3c81-4e4c-afe5-de868861709d
title: مسیرهای API تکراری و ناسازگار برای پروفایل مدل‌ها
type: refactor
priority: high
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T05:08:31.157376+00:00'
updated_at: '2026-05-20T04:22:49.025866+00:00'
archived: true
archived_at: '2026-05-17T10:25:38.743788+00:00'
target_files:
- backend/app/api/routes/model_profiles.py
- backend/app/api/routes/models.py
- backend/app/api/routes/models.py
---

# مسیرهای API تکراری و ناسازگار برای پروفایل مدل‌ها

## Raw Idea

دو مجموعه route جداگانه برای پروفایل مدل‌ها وجود دارد: یکی در `backend/app/api/routes/model_profiles.py` با prefix `/api/models` و دیگری در `backend/app/api/routes/models.py` با prefix `/models`. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند. این باعث سردرگمی در فرانت‌اند و نگهداری دشوار می‌شود. فایل `model_profiles.py` (۸۰۷ خط) عمدتاً کد تکراری از `models.py` است با تغییرات جزئی در نام فیلدها و ساختار پاسخ.
---
[scan #2 at 2026-05-15T06:05:54.062975+00:00]
دو مجموعه route جداگانه برای پروفایل مدل‌ها وجود دارد: یکی در `backend/app/api/routes/model_profiles.py` با prefix `/api/models` و دیگری در `backend/app/api/routes/models.py` با prefix `/models`. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند. این duplication باعث سردرگمی در frontend 
---
[scan #3 at 2026-05-17T07:15:00.442782+00:00]
دو endpoint مجزا برای پروفایل مدل‌ها وجود دارد: یکی در `backend/app/api/routes/models.py` (خط ۲۶۶-۲۸۶) با مسیر `/models/profiles` و دیگری در `backend/app/api/routes/model_profiles.py` (خط

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
مسیرهای API تکراری و ناسازگار برای پروفایل مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/model_profiles.py:1-807` — `کل فایل` — کل این فایل باید حذف شود و منطق آن به models.py منتقل شود
  ```python
  router = APIRouter(prefix="/api/models", tags=["model-profiles"])
  ```
- `backend/app/api/routes/models.py:266-287` — `get_model_profiles` — این endpoint باید با منطق model_profiles.py ادغام شود
  ```python
  @router.get("/profiles")
  async def get_model_profiles():
  ```
- `backend/app/api/routes/models.py:350-400` — `get_model_rankings` — این endpoint نیز تکراری است
  ```python
  @router.get("/rankings")
  async def get_model_rankings():
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Next.js 14 App Router

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/main.py` (سطر 43) — هر دو route در main.py import شده‌اند
- `frontend/src/app/model-profiles/page.tsx` (سطر 116) — فرانت‌اند از endpoint‌های مختلفی استفاده می‌کند
- `frontend/src/app/models/page.tsx` (سطر 300) — صفحه models نیز پروفایل‌ها را نمایش می‌دهد

## 🌐 نقشهٔ وابستگی‌ها
تغییر روی ۲ فایل route و ۱ فایل main.py تأثیر می‌گذارد. فرانت‌اند باید endpoint جدید را استفاده کند.

## 🔍 Context و وضعیت فعلی
دو مجموعه route جداگانه برای پروفایل مدل‌ها وجود دارد: یکی در `backend/app/api/routes/model_profiles.py` با prefix `/api/models` و دیگری در `backend/app/api/routes/models.py` با prefix `/models`. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند. این باعث سردرگمی در فرانت‌اند و نگهداری دشوار می‌شود. فایل `model_profiles.py` (۸۰۷ خط) عمدتاً کد تکراری از `models.py` است با تغییرات جزئی در نام فیلدها و ساختار پاسخ.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل model_profiles.py حذف شود
- [ ] همه endpoint‌های پروفایل در models.py موجود باشند
- [ ] main.py فقط models.py را import کند
- [ ] فرانت‌اند با endpoint جدید کار کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ادغام endpoint‌های پروفایل مدل از `model_profiles.py` به `models.py` و حذف `model_profiles.py`. مسیر یکسان `/models/profiles` و `/models/rankings` را در `models.py` نگه دار.

## 💡 نمونه‌های قبل/بعد
**ادغام route‌ها**

_قبل:_
```
model_profiles.py: router = APIRouter(prefix="/api/models")
models.py: router = APIRouter(prefix="/models")
```

_بعد:_
```
models.py: router = APIRouter(prefix="/models")
# همه endpoint‌های پروفایل اینجا هستند
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `curl http://localhost:8000/models/profiles | jq .success`
- `curl http://localhost:8000/models/rankings | jq .success`
- `curl http://localhost:8000/api/models/profiles | jq .error  # باید 404 بدهد`

## ⚠️ ریسک‌ها و موارد احتیاط
فرانت‌اند ممکن است به endpoint قدیمی وابسته باشد؛ نیاز به بررسی frontend/src/app/model-profiles/page.tsx

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. فایل model_profiles.py حذف شود _(verify: static)_
2. همه endpoint‌های پروفایل در models.py موجود باشند _(verify: static)_
3. main.py فقط models.py را import کند _(verify: static)_
4. فرانت‌اند با endpoint جدید کار کند _(verify: ui_interaction)_

## Task Steps

### Step 1: بررسی کامل ساختار و وابستگی‌های دو فایل route برای شناسایی تفاوت‌ها و شباهت‌ها
**Status:** `done` (100%)
**Scope:** این مرحله شامل تحلیل کامل فایل‌های `backend/app/api/routes/model_profiles.py` و `backend/app/api/routes/models.py` است. باید تمام endpointها، توابع، مدل‌های Pydantic، وابستگی‌ها (importها)، و نحوه استفاده از دیتابیس یا سرویس‌ها در هر دو فایل شناسایی شوند. تفاوت‌های دقیق در نام فیلدها، ساختار پاسخ، و منطق business باید مستند شوند. این مرحله شامل هیچ تغییری در کد نیست، فقط تحلیل و مستندسازی است. خروجی این مرحله یک سند یا کامنت‌های دقیق در کد است که مشخص می‌کند کدام بخش‌ها تکراری، کدام بخش‌ها منحصربه‌فرد، و کدام بخش‌ها نیاز به ادغام دارند.
**Excerpt:**
```
فایل `model_profiles.py` (۸۰۷ خط) عمدتاً کد تکراری از `models.py` است با تغییرات جزئی در نام فیلدها و ساختار پاسخ. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند.
```

### Step 2: بررسی وابستگی‌های فرانت‌اند به endpointهای تکراری
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی کامل فایل‌های فرانت‌اند (به ویژه `frontend/src/app/model-profiles/page.tsx` و `frontend/src/app/models/page.tsx`) برای شناسایی تمام درخواست‌های API به endpointهای `/api/models/profiles`، `/api/models/rankings`، `/models/profiles`، و `/models/rankings` است. باید مشخص شود کدام کامپوننت‌ها از کدام endpoint استفاده می‌کنند و ساختار پاسخ مورد انتظار آن‌ها چیست. این مرحله فقط تحلیل است و هیچ تغییری در کد فرانت‌اند ایجاد نمی‌کند.
**Excerpt:**
```
`frontend/src/app/model-profiles/page.tsx` (سطر 116) — فرانت‌اند از endpoint‌های مختلفی استفاده می‌کند. `frontend/src/app/models/page.tsx` (سطر 300) — صفحه models نیز پروفایل‌ها را نمایش می‌دهد.
```

### Step 3: انتقال منطق منحصربه‌فرد از model_profiles.py به models.py (ادغام endpointهای /profiles)
**Status:** `done` (100%)
**Scope:** این مرحله شامل ادغام endpoint `/profiles` از `model_profiles.py` به `models.py` است. باید تمام منطق business، query دیتابیس، و ساختار پاسخ منحصربه‌فرد از `model_profiles.py` استخراج شده و به endpoint موجود `/profiles` در `models.py` اضافه شود. اگر endpoint `/profiles` در `models.py` وجود ندارد، باید ایجاد شود. اگر وجود دارد، باید با منطق جدید ترکیب شود. نکته حیاتی: باید از یک命名 convention واحد برای فیلدها استفاده شود (ترجیحاً snake_case). پس از این مرحله، endpoint `/profiles` در `models.py` باید تمام قابلیت‌های هر دو فایل قبلی را پشتیبانی کند.
**Excerpt:**
```
فایل `model_profiles.py` (۸۰۷ خط) عمدتاً کد تکراری از `models.py` است با تغییرات جزئی در نام فیلدها و ساختار پاسخ. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند.
```

### Step 4: انتقال منطق منحصربه‌فرد از model_profiles.py به models.py (ادغام endpointهای /rankings)
**Status:** `done` (100%)
**Scope:** این مرحله مشابه مرحله ۳ است اما برای endpoint `/rankings`. منطق business، query دیتابیس، و ساختار پاسخ منحصربه‌فرد از `model_profiles.py` برای endpoint `/rankings` باید به `models.py` منتقل شود. endpoint `/rankings` در `models.py` باید به‌روزرسانی شود تا تمام قابلیت‌های هر دو فایل را پشتیبانی کند. نکته حیاتی: باید از یک命名 convention واحد برای فیلدها استفاده شود.
**Excerpt:**
```
فایل `model_profiles.py` (۸۰۷ خط) عمدتاً کد تکراری از `models.py` است با تغییرات جزئی در نام فیلدها و ساختار پاسخ. هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند.
```

### Step 5: به‌روزرسانی main.py برای حذف import و registration مدل‌های تکراری
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی فایل `backend/app/main.py` است. import مربوط به `model_profiles.py` باید حذف شود و router آن از لیست `app.include_router` خارج شود. همچنین باید اطمینان حاصل شود که router جدید `models.py` (با prefix مناسب) به‌درستی ثبت شده است. نکته حیاتی: اگر `model_profiles.py` هنوز به طور کامل حذف نشده، این مرحله فقط import را غیرفعال می‌کند، نه حذف فایل را.
**Excerpt:**
```
`backend/app/main.py` (سطر 43) — هر دو route در main.py import شده‌اند
```

### Step 6: حذف فایل model_profiles.py پس از اطمینان از عدم وابستگی
**Status:** `done` (100%)
**Scope:** این مرحله شامل حذف فیزیکی فایل `backend/app/api/routes/model_profiles.py` است. قبل از حذف، باید یک بررسی نهایی انجام شود که هیچ import یا وابستگی دیگری به این فایل در پروژه وجود نداشته باشد (با استفاده از grep یا ابزار مشابه). اگر وابستگی‌ای یافت شد، باید ابتدا آن وابستگی‌ها به‌روزرسانی شوند. این مرحله فقط حذف فایل است و هیچ تغییر دیگری در کد ایجاد نمی‌کند.
**Excerpt:**
```
کل این فایل باید حذف شود و منطق آن به models.py منتقل شود
```

### Step 7: به‌روزرسانی فرانت‌اند برای استفاده از endpoint واحد (صفحه model-profiles)
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی فایل `frontend/src/app/model-profiles/page.tsx` است. تمام درخواست‌های API که به endpointهای قدیمی `/api/models/profiles` و `/api/models/rankings` اشاره می‌کنند، باید به endpointهای جدید `/models/profiles` و `/models/rankings` تغییر یابند. همچنین ساختار پاسخ مورد انتظار باید با ساختار جدید تطبیق داده شود (در صورت تغییر نام فیلدها). نکته حیاتی: باید از یک سرویس API متمرکز (در صورت وجود) استفاده شود تا تغییرات در یک نقطه اعمال شوند.
**Excerpt:**
```
`frontend/src/app/model-profiles/page.tsx` (سطر 116) — فرانت‌اند از endpoint‌های مختلفی استفاده می‌کند
```

### Step 8: به‌روزرسانی فرانت‌اند برای استفاده از endpoint واحد (صفحه models)
**Status:** `done` (100%)
**Scope:** این مرحله مشابه مرحله ۷ است اما برای فایل `frontend/src/app/models/page.tsx`. تمام درخواست‌های API که به endpointهای قدیمی `/api/models/profiles` و `/api/models/rankings` اشاره می‌کنند، باید به endpointهای جدید `/models/profiles` و `/models/rankings` تغییر یابند. ساختار پاسخ مورد انتظار باید با ساختار جدید تطبیق داده شود.
**Excerpt:**
```
`frontend/src/app/models/page.tsx` (سطر 300) — صفحه models نیز پروفایل‌ها را نمایش می‌دهد
```

### Step 9: نوشتن تست‌های یکپارچه‌سازی برای endpointهای ادغام‌شده
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی برای endpointهای `/models/profiles` و `/models/rankings` است. تست‌ها باید بررسی کنند که: ۱) endpointها به درستی کار می‌کنند و پاسخ 200 برمی‌گردانند. ۲) ساختار پاسخ شامل تمام فیلدهای مورد انتظار است. ۳) داده‌های بازگشتی با داده‌های قبلی (از هر دو endpoint قدیمی) سازگار است. ۴) endpointهای قدیمی (`/api/models/profiles` و `/api/models/rankings`) دیگر در دسترس نیستند (404). تست‌ها باید در فایل `backend/tests/test_models_routes.py` یا فایل مشابه نوشته شوند.
**Excerpt:**
```
هر دو endpoint‌های `/profiles` و `/rankings` را تعریف می‌کنند. این باعث سردرگمی در فرانت‌اند و نگهداری دشوار می‌شود.
```

### Step 10: بررسی نهایی و مستندسازی تغییرات در README یا CHANGELOG
**Status:** `done` (100%)
**Scope:** این مرحله شامل بررسی نهایی تمام تغییرات اعمال‌شده، اطمینان از عدم وجود خطا، و به‌روزرسانی مستندات پروژه است. باید یک بخش در CHANGELOG یا README اضافه شود که توضیح دهد: ۱) endpointهای `/api/models/profiles` و `/api/models/rankings` حذف شده‌اند. ۲) endpointهای `/models/profiles` و `/models/rankings` جایگزین آن‌ها شده‌اند. ۳) هرگونه تغییر در ساختار پاسخ (فیلدهای جدید/حذف‌شده). ۴) تاریخ اعمال تغییرات.
**Excerpt:**
```
دو مجموعه route جداگانه برای پروفایل مدل‌ها وجود دارد... این باعث سردرگمی در فرانت‌اند و نگهداری دشوار می‌شود.
```
