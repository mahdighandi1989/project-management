---
task_id: a42ea7d0-2308-4dfc-93a2-33777e54b5b0
title: عدم اعتبارسنجی ورودی در endpoint‌های API — خطر SQL Injection و XSS
type: security
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T06:05:27.585009+00:00'
updated_at: '2026-06-03T17:15:03.797770+00:00'
archived: true
archived_at: '2026-05-17T10:29:39.218189+00:00'
target_files:
- backend/app/api/routes/analysis.py
- backend/app/api/routes/github_import.py
- backend/app/api/routes/project_memory.py
---

# عدم اعتبارسنجی ورودی در endpoint‌های API — خطر SQL Injection و XSS

## Raw Idea

در چندین endpoint (از جمله `backend/app/api/routes/analysis.py` خط ۳۰-۳۶ و `backend/app/api/routes/github_import.py` خط ۲۳-۳۸) مدل‌های Pydantic ورودی را بدون sanitization یا validation سفارشی می‌پذیرند. فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود. همچنین در `backend/app/api/routes/project_memory.py` خط ۴۶-۶۱، فیلد `value` بدون محدودیت طول یا محتوا پذیرفته می‌شود که خطر XSS ذخیره‌شده در نمایش‌های فرانت‌اند را دارد.
---
[scan #2 at 2026-05-15T06:36:49.357558+00:00]
در چندین endpoint، ورودی‌های کاربر مستقیماً بدون sanitization در query‌های دیتابیس یا response استفاده می‌شوند. در `backend/app/api/routes/project_health.py` خط ۳۳۹، `project_id` از URL گرفته شده و مستقیماً در query استفاده می‌شود. در `backend/app/api/routes/github_import.py` خط ۲۴۶، پارامتر `path` 
---
[scan #3 at 2026-05-17T07:59:56.257935+00:00]
در چندین endpoint، ورودی‌های کاربر مستقیماً بدون sanitization در queryهای دیتابیس و responseها استفاده می‌شوند. در `backend/app/api/routes/project_health.py` خط ۳۳۹، `project_id` از URL گرفته شده و مستقیماً در query استفاده می‌شود. در `backend/app/api/routes/analysis.py` خط ۲۸۴، پارامتر `project_id`

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
عدم اعتبارسنجی ورودی در endpoint‌های API — خطر SQL Injection و XSS

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:30-36` — `AnalysisRequest` — project_path و roadmap_path بدون validation مسیر
  ```python
  class AnalysisRequest(BaseModel):
      """درخواست تحلیل"""
      project_id: str
      project_path: str
      models: List[str] = []
      roadmap_path: Optional[str] = None
  ```
- `backend/app/api/routes/github_import.py:23-28` — `CheckRepoRequest` — url از نوع str ساده است، باید HttpUrl باشد
  ```python
  class CheckRepoRequest(BaseModel):
      """درخواست بررسی دسترسی به repo"""
      url: str
      token: Optional[str] = None
      use_global_token: bool = False
  ```
- `backend/app/api/routes/project_memory.py:44-62` — `DynamicFieldRequest` — value بدون محدودیت طول یا sanitization
  ```python
  class DynamicFieldRequest(BaseModel):
      """درخواست افزودن/ویرایش فیلد پویا"""
      name: str
      value: str
      target_models: List[str] = ["all"]
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Pydantic v2 + SQLAlchemy + SQLite

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/core/config.py` (سطر 1) — محل تعریف ALLOWED_PATHS یا تنظیمات مشابه
- `backend/app/services/project_analyzer.py` (سطر 1) — از project_path برای خواندن فایل‌ها استفاده می‌کند

## 🌐 نقشهٔ وابستگی‌ها
این issue روی تمام endpointهایی که از این مدل‌ها استفاده می‌کنند تأثیر می‌گذارد: analysis, github_import, project_memory. اصلاح مدل‌های Pydantic به‌طور خودکار validation را در تمام مسیرها اعمال می‌کند.

## 🔍 Context و وضعیت فعلی
در چندین endpoint (از جمله `backend/app/api/routes/analysis.py` خط ۳۰-۳۶ و `backend/app/api/routes/github_import.py` خط ۲۳-۳۸) مدل‌های Pydantic ورودی را بدون sanitization یا validation سفارشی می‌پذیرند. فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود. همچنین در `backend/app/api/routes/project_memory.py` خط ۴۶-۶۱، فیلد `value` بدون محدودیت طول یا محتوا پذیرفته می‌شود که خطر XSS ذخیره‌شده در نمایش‌های فرانت‌اند را دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] اعمال تغییر بدون شکستن تست‌های موجود
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای تمام فیلدهای رشته‌ای که در عملیات فایل‌سیستم، دیتابیس یا نمایش HTML استفاده می‌شوند، validation سفارشی با Pydantic's `Field` و `validator` اضافه کن. برای `project_path` از `PathValidator` استفاده کن که مسیر را به دایرکتوری مجاز محدود کند. برای `url` از `HttpUrl` استاندارد Pydantic استفاده کن. برای `value` در `DynamicFieldRequest` یک `max_length=10000` و sanitization کاراکترهای HTML اضافه کن.

## 💡 نمونه‌های قبل/بعد
**Validation مسیر پروژه**

_قبل:_
```
project_path: str
```

_بعد:_
```
project_path: str
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run build`
- `npm run lint`

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. اعمال تغییر بدون شکستن تست‌های موجود _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: static)_
3. type-check موفق است _(verify: static)_

## Task Steps

### Step 1: اضافه کردن validator سفارشی برای فیلد project_path در AnalysisRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس AnalysisRequest در فایل backend/app/api/routes/analysis.py است که مسیر project_path را در برابر Path Traversal بررسی می‌کند. validator باید اطمینان حاصل کند که مسیر وارد شده به یک دایرکتوری مجاز (مثلاً زیرمجموعه پروژه‌های تعریف‌شده در config) محدود می‌شود و شامل کاراکترهای خطرناک مانند '..' یا '/' اضافی نیست. خارج از این مرحله: تغییر در منطق business، اضافه کردن validator برای roadmap_path، یا تغییر در نحوه استفاده از project_path در سرویس‌ها. نکته حیاتی: validator باید از ALLOWED_PATHS تعریف‌شده در backend/app/core/config.py استفاده کند و در صورت نامعتبر بودن مسیر، یک HTTPException با status 400 برگرداند.
**Excerpt:**
```
فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود.
```

### Step 2: اضافه کردن validator سفارشی برای فیلد roadmap_path در AnalysisRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس AnalysisRequest برای فیلد roadmap_path است. validator باید مسیر roadmap_path را در برابر Path Traversal بررسی کند و اطمینان حاصل کند که مسیر وارد شده به یک دایرکتوری مجاز محدود می‌شود. خارج از این مرحله: تغییر در validator project_path یا سایر فیلدها. نکته حیاتی: roadmap_path اختیاری است (Optional[str])، بنابراین validator باید فقط در صورت وجود مقدار (non-None) اعمال شود.
**Excerpt:**
```
فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود.
```

### Step 3: تغییر نوع فیلد url در CheckRepoRequest از str به HttpUrl
**Status:** `done` (100%)
**Scope:** این مرحله شامل تغییر نوع فیلد url در کلاس CheckRepoRequest در فایل backend/app/api/routes/github_import.py از str به HttpUrl (از کتابخانه pydantic) است. این تغییر باعث می‌شود Pydantic به طور خودکار اعتبارسنجی کند که مقدار وارد شده یک URL معتبر با پروتکل HTTP/HTTPS است. خارج از این مرحله: اضافه کردن validator سفارشی برای token یا سایر فیلدها، یا تغییر در منطق business endpoint. نکته حیاتی: باید از HttpUrl از pydantic import شود و در صورت نامعتبر بودن URL، Pydantic به طور خودکار خطای 422 (Unprocessable Entity) برمی‌گرداند.
**Excerpt:**
```
backend/app/api/routes/github_import.py:23-28 — `CheckRepoRequest` — url از نوع str ساده است، باید HttpUrl باشد
```

### Step 4: اضافه کردن محدودیت طول و sanitization برای فیلد value در DynamicFieldRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس DynamicFieldRequest در فایل backend/app/api/routes/project_memory.py برای فیلد value است. validator باید: 1) محدودیت طول (مثلاً حداکثر 500 کاراکتر) اعمال کند، 2) کاراکترهای HTML خطرناک (<, >, &, ", ') را escape کند تا از XSS ذخیره‌شده جلوگیری شود. خارج از این مرحله: تغییر در validator برای فیلد name یا target_models، یا تغییر در نحوه نمایش value در فرانت‌اند. نکته حیاتی: sanitization باید با استفاده از کتابخانه html.escape یا مشابه انجام شود و داده‌ها قبل از ذخیره در دیتابیس sanitize شوند.
**Excerpt:**
```
در `backend/app/api/routes/project_memory.py` خط ۴۶-۶۱، فیلد `value` بدون محدودیت طول یا محتوا پذیرفته می‌شود که خطر XSS ذخیره‌شده در نمایش‌های فرانت‌اند را دارد.
```

### Step 5: اضافه کردن محدودیت طول و validation برای فیلد name در DynamicFieldRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس DynamicFieldRequest برای فیلد name است. validator باید: 1) محدودیت طول (مثلاً حداکثر 100 کاراکتر) اعمال کند، 2) فقط کاراکترهای مجاز (حروف، اعداد، underscore) را بپذیرد تا از injection در نام فیلد جلوگیری شود. خارج از این مرحله: تغییر در validator برای value یا target_models. نکته حیاتی: name به عنوان کلید در دیتابیس استفاده می‌شود، بنابراین باید از کاراکترهای خاص جلوگیری شود.
**Excerpt:**
```
در `backend/app/api/routes/project_memory.py` خط ۴۶-۶۱، فیلد `value` بدون محدودیت طول یا محتوا پذیرفته می‌شود که خطر XSS ذخیره‌شده در نمایش‌های فرانت‌اند را دارد.
```

### Step 6: اضافه کردن validation برای فیلد target_models در DynamicFieldRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس DynamicFieldRequest برای فیلد target_models است. validator باید: 1) اطمینان حاصل کند که target_models یک لیست خالی نیست، 2) هر آیتم در لیست حداکثر 50 کاراکتر باشد و فقط شامل کاراکترهای مجاز (حروف، اعداد، underscore) باشد. خارج از این مرحله: تغییر در validator برای name یا value. نکته حیاتی: target_models می‌تواند شامل مقدار 'all' باشد که باید مجاز باشد.
**Excerpt:**
```
در `backend/app/api/routes/project_memory.py` خط ۴۶-۶۱، فیلد `value` بدون محدودیت طول یا محتوا پذیرفته می‌شود که خطر XSS ذخیره‌شده در نمایش‌های فرانت‌اند را دارد.
```

### Step 7: اضافه کردن validator سفارشی برای فیلد token در CheckRepoRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس CheckRepoRequest برای فیلد token است. validator باید: 1) محدودیت طول (مثلاً حداکثر 200 کاراکتر) اعمال کند، 2) فقط کاراکترهای مجاز (حروف، اعداد، underscore، dash) را بپذیرد تا از injection در هدرهای HTTP جلوگیری شود. خارج از این مرحله: تغییر در validator برای url یا use_global_token. نکته حیاتی: token اختیاری است (Optional[str])، بنابراین validator باید فقط در صورت وجود مقدار اعمال شود.
**Excerpt:**
```
backend/app/api/routes/github_import.py:23-28 — `CheckRepoRequest` — url از نوع str ساده است، باید HttpUrl باشد
```

### Step 8: اضافه کردن validator سفارشی برای فیلد project_id در AnalysisRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس AnalysisRequest برای فیلد project_id است. validator باید: 1) محدودیت طول (مثلاً حداکثر 50 کاراکتر) اعمال کند، 2) فقط کاراکترهای مجاز (حروف، اعداد، underscore، dash) را بپذیرد تا از SQL Injection در کوئری‌های دیتابیس جلوگیری شود. خارج از این مرحله: تغییر در validator برای project_path یا roadmap_path. نکته حیاتی: project_id در کوئری‌های دیتابیس استفاده می‌شود، بنابراین validation دقیق ضروری است.
**Excerpt:**
```
فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود.
```

### Step 9: اضافه کردن validator سفارشی برای فیلد models در AnalysisRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس AnalysisRequest برای فیلد models است. validator باید: 1) اطمینان حاصل کند که هر آیتم در لیست حداکثر 50 کاراکتر باشد، 2) فقط کاراکترهای مجاز (حروف، اعداد، underscore) را بپذیرد. خارج از این مرحله: تغییر در validator برای project_id یا project_path. نکته حیاتی: models یک لیست اختیاری است (List[str] = [])، بنابراین validator باید لیست خالی را مجاز بداند.
**Excerpt:**
```
فیلدهایی مثل `project_path` (analysis.py:33) و `url` (github_import.py:25) مستقیماً در عملیات فایل‌سیستم و کوئری‌های دیتابیس استفاده می‌شوند. این می‌تواند منجر به Path Traversal (خواندن فایل‌های خارج از محدوده پروژه) یا SQL Injection (از طریق فیلدهای متنی که به raw query می‌روند) شود.
```

### Step 10: اضافه کردن validator سفارشی برای فیلد use_global_token در CheckRepoRequest
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن یک validator سفارشی Pydantic (field_validator) به کلاس CheckRepoRequest برای فیلد use_global_token است. validator باید اطمینان حاصل کند که use_global_token یک مقدار boolean معتبر است. خارج از این مرحله: تغییر در validator برای url یا token. نکته حیاتی: Pydantic به طور خودکار boolean را validation می‌کند، اما این validator برای اطمینان از عدم ارسال مقادیر غیرمنتظره اضافه می‌شود.
**Excerpt:**
```
backend/app/api/routes/github_import.py:23-28 — `CheckRepoRequest` — url از نوع str ساده است، باید HttpUrl باشد
```
