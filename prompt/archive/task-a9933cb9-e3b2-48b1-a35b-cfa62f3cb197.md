---
task_id: a9933cb9-e3b2-48b1-a35b-cfa62f3cb197
title: عدم وجود فایل requirements.txt یا pyproject.toml برای Backend
type: bug
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-10T15:27:10.029452+00:00'
updated_at: '2026-05-29T16:39:30.706464+00:00'
archived: true
archived_at: '2026-05-11T08:16:47.249293+00:00'
target_files:
- backend/requirements.txt
- backend/pyproject.toml
---

# عدم وجود فایل requirements.txt یا pyproject.toml برای Backend

## Raw Idea

پروژه فاقد فایل وابستگی‌های Python (requirements.txt یا pyproject.toml) است. این موضوع باعث می‌شود نصب وابستگی‌ها با pip غیرممکن باشد و Dockerfile نیز ناقص بماند. همچنین ابزارهای امنیتی مانند pip-audit یا safety نمی‌توانند آسیب‌پذیری‌های وابستگی‌ها را بررسی کنند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف
عدم وجود فایل requirements.txt یا pyproject.toml برای Backend

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/requirements.txt` — فایل وجود ندارد - باید ایجاد شود
- `backend/pyproject.toml` — فایل وجود ندارد - می‌تواند جایگزین requirements.txt شود

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Python 3.11+, FastAPI, pip/poetry

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/Dockerfile` (سطر 1) — Dockerfile برای نصب وابستگی‌ها به requirements.txt نیاز دارد
- `backend/.env.example` (سطر 1) — نمونه فایل env برای تنظیم متغیرهای محیطی

## 🌐 نقشهٔ وابستگی‌ها
این تسک برای کل Backend حیاتی است. بدون فایل وابستگی، نصب، استقرار و ممیزی امنیتی غیرممکن است.

## 🔍 Context و وضعیت فعلی
پروژه فاقد فایل وابستگی‌های Python (requirements.txt یا pyproject.toml) است. این موضوع باعث می‌شود نصب وابستگی‌ها با pip غیرممکن باشد و Dockerfile نیز ناقص بماند. همچنین ابزارهای امنیتی مانند pip-audit یا safety نمی‌توانند آسیب‌پذیری‌های وابستگی‌ها را بررسی کنند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] فایل requirements.txt یا pyproject.toml در مسیر backend/ وجود داشته باشد
- [ ] دستور pip install -r requirements.txt بدون خطا اجرا شود
- [ ] Dockerfile بتواند با استفاده از این فایل image را build کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل requirements.txt یا pyproject.toml با لیست کامل وابستگی‌های Python شامل FastAPI, uvicorn, SQLAlchemy, pydantic, httpx, python-dotenv, و غیره. همچنین اضافه کردن lock file (مثلاً poetry.lock یا pipfile.lock) برای بازتولیدپذیری.

## 💡 نمونه‌های قبل/بعد
**قبل: عدم وجود فایل وابستگی**

_قبل:_
```
backend/ (بدون requirements.txt یا pyproject.toml)
```

_بعد:_
```
backend/requirements.txt (شامل fastapi==0.110.0, uvicorn==0.27.0, ...)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd backend && pip install -r requirements.txt`
- `cd backend && pip-audit -r requirements.txt`

## ⚠️ ریسک‌ها و موارد احتیاط
احتمال ناسازگاری نسخه‌های وابستگی‌ها با کد موجود. نیاز به تست کامل پس از نصب.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: small

## Acceptance Criteria

1. فایل requirements.txt یا pyproject.toml در مسیر backend/ وجود داشته باشد _(verify: static)_
2. دستور pip install -r requirements.txt بدون خطا اجرا شود _(verify: backend_test)_
3. Dockerfile بتواند با استفاده از این فایل image را build کند _(verify: backend_test)_
