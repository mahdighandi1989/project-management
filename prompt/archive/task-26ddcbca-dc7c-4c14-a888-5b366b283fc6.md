---
task_id: 26ddcbca-dc7c-4c14-a888-5b366b283fc6
title: '[منطق] عدم وجود مکانیزم Hallucination Guard در pipeline'
type: logic_audit
priority: medium
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T06:41:00.928076+00:00'
updated_at: '2026-05-20T04:23:54.117716+00:00'
archived: true
archived_at: '2026-05-18T04:23:32.019762+00:00'
tags:
- merged
---

# [منطق] عدم وجود مکانیزم Hallucination Guard در pipeline

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

در هیچ‌کدام از کامپوننت‌ها به مکانیزم‌های کاهش توهم (hallucination guard) اشاره نشده است. با توجه به اینکه pipeline با مدل‌های زبانی بزرگ کار می‌کند، عدم وجود چنین مکانیزمی (مانند grounding, fact-checking, یا self-consistency) می‌تواند منجر به تولید اطلاعات نادرست شود.

## 💥 پیامد (impact)
خروجی‌های نادرست و گمراه‌کننده برای کاربران. کاهش اعتماد به سیستم. ریسک‌های قانونی و اخلاقی.

## 🛠 پیشنهاد رفع اولیه
یک ماژول hallucination guard به ai_manager اضافه کنید. این ماژول می‌تواند شامل: 1) grounding پاسخ‌ها به منابع معتبر (در صورت وجود)، 2) validation خودکار با استفاده از مدل دوم برای fact-checking، 3) تشخیص و پرچم‌گذاری پاسخ‌های با قطعیت پایین باشد.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-17T08:38:15.359851+00:00]
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ‌کدام از کامپوننت‌ها مکانیزم مشخصی برای جلوگیری از hallucination (مانند grounding, fact-checking, یا confidence scoring) ندارند. oversight_strong_prompt فقط ساختار پرامپت را تعریف می‌کند.

## 💥 پیامد (impact)
خطر تولید اطلاعات ن

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود مکانیزم Hallucination Guard در pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

در هیچ‌کدام از کامپوننت‌ها به مکانیزم‌های کاهش توهم (hallucination guard) اشاره نشده است. با توجه به اینکه pipeline با مدل‌های زبانی بزرگ کار می‌کند، عدم وجود چنین مکانیزمی (مانند grounding, fact-checking, یا self-consistency) می‌تواند منجر به تولید اطلاعات نادرست شود.

## 💥 پیامد (impact)
خروجی‌های نادرست و گمراه‌کننده برای کاربران. کاهش اعتماد به سیستم. ریسک‌های قانونی و اخلاقی.

## 🛠 پیشنهاد رفع اولیه
یک ماژول hallucination guard به ai_manager اضافه کنید. این ماژول می‌تواند شامل: 1) grounding پاسخ‌ها به منابع معتبر (در صورت وجود)، 2) validation خودکار با استفاده از مدل دوم برای fact-checking، 3) تشخیص و پرچم‌گذاری پاسخ‌های با قطعیت پایین باشد.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و شناسایی وضعیت فعلی pipeline ai_llm و کامپوننت‌های مرتبط
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و بررسی کامل ساختار پروژه برای یافتن تمام فایل‌های مرتبط با pipeline `ai_llm` و به‌ویژه ماژول `ai_manager` است. هدف، شناسایی فایل‌های موجود، کلاس‌ها، توابع و هرگونه پیاده‌سازی قبلی مرتبط با Hallucination Guard (مانند grounding, fact-checking, self-consistency) می‌باشد. این مرحله صرفاً یک مرحله کشف و مستندسازی است و هیچ تغییری در کد ایجاد نمی‌کند. خروجی این مرحله یک گزارش از وضعیت موجود است که برای مراحل بعدی استفاده می‌شود.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد: در هیچ‌کدام از کامپوننت‌ها به مکانیزم‌های کاهش توهم (hallucination guard) اشاره نشده است. ... یک ماژول hallucination guard به ai_manager اضافه کنید. ... پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را مستقل بررسی کن. اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر است، بر اساس قضاوت خودت عمل کن
```

### Step 2: ایجاد ماژول پایه HallucinationGuard در ai_manager
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول جدید به نام `hallucination_guard.py` در دایرکتوری `ai_manager` است. این ماژول شامل یک کلاس پایه (Base) یا یک مجموعه توابع است که ساختار اصلی برای سه مکانیزم (grounding, fact-checking, low-confidence detection) را تعریف می‌کند. در این مرحله، فقط interface و stub functions (با raise NotImplementedError یا log ساده) ایجاد می‌شود. منطق اصلی در مراحل بعدی پیاده‌سازی می‌شود. این مرحله foundation را می‌سازد.
**Excerpt:**
```
یک ماژول hallucination guard به ai_manager اضافه کنید. این ماژول می‌تواند شامل: 1) grounding پاسخ‌ها به منابع معتبر (در صورت وجود)، 2) validation خودکار با استفاده از مدل دوم برای fact-checking، 3) تشخیص و پرچم‌گذاری پاسخ‌های با قطعیت پایین باشد.
```

### Step 3: پیاده‌سازی مکانیزم Grounding (اتصال به منابع معتبر)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی متد `ground_response` در کلاس `HallucinationGuard` است. این متد باید یک پاسخ (text) و یک لیست از منابع معتبر (مانند URLs، اسناد، یا نتایج جستجو) دریافت کند و پاسخ را با استناد به این منابع غنی‌سازی کند. منطق می‌تواند شامل: استخراج key phrases از پاسخ، جستجوی آن‌ها در منابع، و اضافه کردن citation یا footnote به پاسخ باشد. در صورت عدم وجود منبع، پاسخ بدون تغییر باقی می‌ماند. این مرحله نیازمند تعریف interface برای منابع (مثلاً یک dataclass `Source`) است.
**Excerpt:**
```
1) grounding پاسخ‌ها به منابع معتبر (در صورت وجود)
```

### Step 4: پیاده‌سازی مکانیزم Fact-Checking با مدل دوم
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی متد `fact_check_response` است. این متد باید یک پاسخ (text) را دریافت کرده و با استفاده از یک مدل LLM دوم (یا یک سرویس fact-checking) صحت ادعاهای مطرح شده در پاسخ را بررسی کند. منطق شامل: ارسال پاسخ به مدل دوم با یک پرامپت خاص (مثلاً 'آیا این جملات از نظر واقعی درست هستند؟')، دریافت نتیجه (تأیید/رد/نامشخص)، و برگرداندن یک گزارش fact-check است. این مرحله نیازمند تعریف یک کلاینت برای مدل دوم (مثلاً `SecondLLMClient`) یا استفاده از یک سرویس موجود است.
**Excerpt:**
```
2) validation خودکار با استفاده از مدل دوم برای fact-checking
```

### Step 5: پیاده‌سازی مکانیزم تشخیص و پرچم‌گذاری پاسخ‌های با قطعیت پایین
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی متد `detect_low_confidence` است. این متد باید یک پاسخ (text) و احتمالاً metadata (مانند logprobs از مدل اصلی) را دریافت کرده و سطح قطعیت پاسخ را ارزیابی کند. منطق می‌تواند شامل: تحلیل عبارات مبهم (مثلاً 'maybe', 'I think')، بررسی طول پاسخ، یا استفاده از یک آستانه برای logprobs باشد. خروجی یک پرچم (boolean) و یک امتیاز قطعیت (float) است. اگر پاسخ با قطعیت پایین تشخیص داده شود، باید یک هشدار به لاگ اضافه شود.
**Excerpt:**
```
3) تشخیص و پرچم‌گذاری پاسخ‌های با قطعیت پایین
```

### Step 6: یکپارچه‌سازی HallucinationGuard در pipeline اصلی ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اصلاح pipeline اصلی `ai_llm` (احتمالاً در فایل `pipeline.py` یا `ai_manager.py`) برای استفاده از ماژول `HallucinationGuard` است. پس از تولید پاسخ توسط مدل اصلی، پاسخ باید از طریق متدهای `ground_response` (در صورت وجود منابع)، `fact_check_response`، و `detect_low_confidence` عبور داده شود. بر اساس نتایج fact-check و قطعیت، ممکن است پاسخ اصلاح شود، یک هشدار به آن اضافه شود، یا به طور کلی رد شود. این مرحله نیازمند تعریف یک استراتژی (مثلاً 'reject if low confidence and fact-check fails') است.
**Excerpt:**
```
یک ماژول hallucination guard به ai_manager اضافه کنید. ... coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند
```

### Step 7: نوشتن تست‌های واحد (Unit Tests) برای ماژول HallucinationGuard
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد کامل برای تمام متدهای کلاس `HallucinationGuard` است. تست‌ها باید شامل موارد زیر باشند: تست `ground_response` با منابع مختلف و بدون منبع، تست `fact_check_response` با mock کردن مدل دوم برای حالات مختلف (تأیید، رد، خطا)، تست `detect_low_confidence` با پاسخ‌های قطعی و مبهم. همچنین تست‌هایی برای edge cases مانند ورودی خالی، منابع خالی، و خطاهای غیرمنتظره. تست‌ها باید در فایل `tests/test_hallucination_guard.py` قرار گیرند.
**Excerpt:**
```
نوشتن تست Z
```

### Step 8: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) برای pipeline اصلاح‌شده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که pipeline کامل `ai_llm` را با HallucinationGuard یکپارچه‌شده آزمایش می‌کند. این تست‌ها باید از یک mock برای مدل LLM اصلی و مدل دوم استفاده کنند تا وابستگی خارجی نداشته باشند. سناریوهای تست شامل: پاسخ صحیح که از همه فیلترها عبور می‌کند، پاسخ نادرست که توسط fact-check رد می‌شود، پاسخ با قطعیت پایین که پرچم‌گذاری می‌شود، و ترکیبی از این موارد. تست‌ها باید در فایل `tests/test_pipeline_integration.py` قرار گیرند.
**Excerpt:**
```
نوشتن تست Z
```

### Step 9: بازبینی نهایی و مستندسازی (Audit & Documentation)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بازبینی نهایی تمام تغییرات ایجاد شده، اطمینان از عدم وجود conflict با سایر بخش‌های کد، و به‌روزرسانی مستندات مرتبط (مانند README یا docstrings) است. همچنین یک commit message جامع و یک PR description دقیق که شامل checklist از تمام مراحل انجام شده است، نوشته می‌شود. در این مرحله، اطمینان حاصل می‌شود که تمام Acceptance Criteria (رفتارهای قابل مشاهده) پوشش داده شده‌اند.
**Excerpt:**
```
در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.
```
