---
task_id: 15c111f6-b3a7-460f-82e5-b27dc89518df
title: 'اصل ۱ — مرحله‌ای ولی کامل: هر مرحله pull-ready، deployable، بدون قطعهٔ half-done (10 مرحله)'
type: feature_request
priority: high
execution_priority: 100
status: awaiting_review
external_status: pending
verification_status: partial
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-12T14:48:36.331203+00:00'
updated_at: '2026-06-02T17:40:35.723567+00:00'
archived: true
archived_at: '2026-05-12T16:38:19.509510+00:00'
target_files:
- backend/requirements.txt
---

# اصل ۱ — مرحله‌ای ولی کامل: هر مرحله pull-ready، deployable، بدون قطعهٔ half-done (10 مرحله)

## Raw Idea

اصل ۱ — مرحله‌ای ولی کامل: هر مرحله pull-ready، deployable، بدون قطعهٔ half-done. هیچ TODO به مرحلهٔ بعد موکول نمی‌شود مگر صراحتاً out-of-scope.

اصل ۲ — بدون خلاصه‌سازی در استخراج: پرامپت به مدل بصری صریحاً «متن کامل، literal، هیچ‌چیز drop نشود» می‌خواهد.

اصل ۳ — mapping داینامیک مدل: gemini hard-code نمی‌شود؛ همیشه از pick_best_extraction_model(mime_type) که حسب enabled models + capability map انتخاب می‌کند.

اصل ۴ — resume from scratch ممنوع: هر segment به‌محض اتمام در DB ذخیره. crash میانه → ادامه از segment بعدی.

اصل ۵ — فایل اصلی پس از استخراج حذف شود: متن استخراج‌شده جایگزین. verifyهای بعدی به فایل اصلی نیاز ندارند.

اصل ۶ — backend capacity: semaphore + chunk streaming + memory watchdog. هیچ‌گاه OOM crash.

وضعیت زیرساخت (audit شد)
Capability	وضعیت	یادداشت
Upload endpoint + drag-drop UI	✅ موجود	upload.py, FileUpload.tsx — اما تمام فایل را در RAM می‌خواند
Storage 500MB cap	✅ موجود	storage_service.py
Gemini integration + images base64	✅ موجود	gemini_service.py
ModelCapability enum + registry	✅ موجود	models_registry.py
/models صفحهٔ مدیریت	✅ موجود	enable/disable + per-model settings + temporary_enabled field (استفاده‌نشده)
ai_manager routing با capability	✅ موجود	—
idea_to_prompt multi-pass	✅ موجود	—
Task JSON store + recovery on boot	✅ موجود	—
Chunked/resumable upload	❌ غایب	کل فایل در RAM = OOM روی 500MB
PDF/DOCX/XLSX/audio/video libs	❌ غایب	pypdf, python-docx, openpyxl, Pillow, pydub لازم
Extraction service	❌ غایب	—
ExtractionSegment persistence	❌ غایب	—
File ↔ idea_to_prompt link	❌ غایب	—
UI "Gemini disabled" alert + toggle	❌ غایب	—
Temp-enable workflow + notify	🟡 ناقص	DB field هست، logic نیست
Task detail: نمایش extracted text	❌ غایب	—
Dynamic vision-model picker	🟡 ناقص	enum هست، helper نیست
🪜 مراحل (۱۰ stage مستقل، هر کدام یک commit)
مرحلهٔ ۱ — وابستگی‌ها + Capability Registry
افزودن pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به requirements
extend ModelCapability با VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT
helper pick_best_extraction_model(mime_type) → بهترین مدل enabled با ranking
map mime → capability (image/* → IMAGE_ANALYSIS، video/* → VIDEO_UNDERSTANDING، …)
Validation: pytest روی picker + UI /models capabilities جدید را نشان دهد
مرحلهٔ ۲ — Backend: Streaming + Resumable Upload
table UploadSession (id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress)
endpoints: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}
aiofiles streaming → 5MB chunks → RAM ثابت
on boot: cleanup orphans >24h
Validation: upload 500MB، RSS RAM ~50MB بماند نه 500MB؛ crash midway → resume
مرحلهٔ ۳ — Frontend: Multi-file Drag-Drop + Chunked + Resumable
کامپوننت <TaskFilePicker /> در form ایده
چند فایل، ترتیب آپلود = file_order
progress bar per file + retry/cancel
localStorage session → resume on reload
chunk client = 5MB، parallel limit=2
Validation: drag 3×100MB، refresh، resume درست
مرحلهٔ ۴ — Extraction Service (هستهٔ feature)
file_extraction_service.py با routing per mime:
image/* → Gemini vision (full literal)
application/pdf → pypdf برای text-pages + Gemini برای صفحات scan/image
xlsx/docx → openpyxl/docx → metadata + Gemini برای interpretation
audio/* → Gemini transcription (full literal)
video/* → frame-by-frame هر 2s + audio parallel → Gemini
پرامپت استخراج: حسب user_idea، اول از مدل headings/sections بپرس → بعد per heading text کامل extract کن
بدون خلاصه‌سازی: instruction صریح: «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی»
streaming: per-segment yield → بلافاصله DB
Validation: 100-page PDF → 100 segment، crash mid → resume از segment آخر، نه از صفر
مرحلهٔ ۵ — Persistence Layer
table FileExtraction (task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used)
table ExtractionSegment (extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used)
repo extraction_repo.py
GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text
پس از completed → حذف temp_path
Validation: post-extraction، file روی disk نیست؛ restart → existing data persisted
مرحلهٔ ۶ — Gemini Toggle Flow
در /tasks/from-idea با upload_session_ids → اگر pick_best_extraction_model = None → return {"status": "blocked_no_vision_model", "candidates": [...]}
Frontend modal: "مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]"
backend POST /models/settings/{id}/temp-activate?trigger_task=... → temporary_enabled=True + notify telegram
post-extraction hook → revert + notify telegram
Validation: Gemini off → modal، toggle بزنی، extraction، خودکار revert + 2 notif
مرحلهٔ ۷ — Wiring idea_to_prompt با extraction
idea_to_prompt(idea, upload_session_ids=[...]) پارامتر جدید
await همهٔ extractions تمام شوند (هر کدام stream می‌کند، در DB)
متن کامل هر فایل به‌ترتیب file_order به idea append شود (ساختار ## فایل #N: filename)
multi-pass plan هر فایل را به stage مرتبط ربط دهد
اگر متن کل >50KB → ref به extraction_id نه inline
Validation: 3 فایل → پرامپت ≥3 stage، هر stage به فایل مربوطه ref
مرحلهٔ ۸ — UI: نمایش متن استخراج‌شده
TaskCard: badge "📎 N فایل، M segment"
modal/page: لیست فایل‌ها به‌ترتیب → per file segments collapsible → search + copy
Validation: نمایش، order، search، copy
مرحلهٔ ۹ — RAM/CPU Guardrails
Semaphore(1) سراسری برای extraction
ویدئو/صوت بزرگ: ffmpeg chunk به 5min → per chunk Gemini
memory watchdog: اگر RSS >80% → delay segment بعدی
timeout سخت 5min per segment → mark failed → ادامه
boot recovery: extracting + last_segment >5min → failed → resume
Validation: 500MB video → 25 chunk، RAM ثابت؛ SIGKILL mid → resume
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent: race، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram
رفع همهٔ ایرادات پیدا‌شده
smoke test سراسری: full flow (3 files → extract → idea_to_prompt → task → verify) بدون خطا

-------------------------------------
اصل ۱: بدون خلاصه‌سازی، بدون شورت‌کات. هر مرحله pull-ready.
اصل ۲: هر مرحله یک commit مستقل.
اصل ۳: backward-compatible — feature موجود web نباید break شود.
اصل ۴: تا دکمهٔ submit زده نشود، هیچ تسک/پروژه ساخته نمی‌شود — فقط buffer.
اصل ۵: ترتیب رسیدن = file_order در Telegram، مثل web.
اصل ۶: اگر کاربر هیچ متنی ننوشته ولی فایل فرستاده، محتوای استخراج‌شده = ایدهٔ کاربر؛ multi-pass باید این را بفهمد.
اصل ۷: «جمع‌بندی» = aggregated full text + meta-progress (تعداد segmentها، زمان، مدل) — نه summarization محتوا.
اصل ۸: progress در هر دو platform (Telegram + Frontend) در زمان واقعی قابل مشاهده.

مراحل (۱۰ stage مستقل، هر کدام یک commit)
مرحلهٔ ۱ — Compose Buffer State + Auto-Enter Mode
ساختار _compose_buffers: Dict[chat_id, ComposeBuffer] + persistence در compose_buffers.json
ComposeBuffer: chat_id, watched_id?, mode (task|project), started_at, items: List[ComposeItem], text_parts: List[str], task_draft_id
ComposeItem: {order, type, file_id, mime, size, telegram_file_path?, upload_session_id?, added_at}
auto-enter: اگر کاربر در phase=idle است و یک media می‌فرستد (voice/video_note/video/photo/document/audio/animation) → خودکار compose mode فعال شود + پیام راهنما: "📦 تشخیص داده شد می‌خواهی تسک ترکیبی بسازی. هر تعداد فایل/متن می‌خواهی بفرست. در پایان دکمه را بزن."
⚠️ اگر کاربر در awaiting_idea یا creator_* phase است، compose mode به همان context ضمیمه شود (نه phase جدید)
TTL: ۳۰ دقیقه
restart-safe: بازیابی buffer از JSON در boot
Validation: smoke تست auto-enter + persistence
مرحلهٔ ۲ — Telegram Media Handlers (همهٔ ۸ نوع)
handlers per type: voice, video_note, video, photo, audio, document, animation
پایپ‌لاین:
getFile(file_id) از Telegram Bot API
download (≤20MB Bot API limit، خطای واضح اگر بزرگ‌تر)
UploadSession ساختن: filename derived از type (voice → voice_YYYY-MM-DD-HH-MM-SS.ogg)
mime mapping: voice→audio/ogg، video_note→video/mp4، photo→image/jpeg
append_chunk + mark_completed
buffer.items.append با upload_session_id
متن (message.text غیر-دستوری) → اضافه به text_parts همراه order
اولویت: order = ترتیب رسیدن کلی (mixed با text و file)
Validation: هر ۸ نوع تست شود
مرحلهٔ ۳ — Persistent ReplyKeyboard (دکمه‌های بزرگ، دائم) + Inline status
دو لایه:
ReplyKeyboard پایین صفحه با دکمه‌های بزرگ:
✅ ساخت تسک با همه
❌ لغو همه
این keyboard تا پایان compose dormant می‌ماند (نه disappear با هر پیام)
inline status message که با هر افزودن آیتم، edit می‌شود (نه duplicate):
📦 در حال ساخت تسک — N آیتم: + لیست + اطلاعات هر آیتم
پس از submit/cancel، ReplyKeyboard حذف می‌شود (reply_markup={"remove_keyboard": True})
diff check قبل از editMessageText (جلوگیری از telegram error "message not modified")
Validation: ReplyKeyboard ظاهر/مخفی، status message یک‌تا و edit-only
مرحلهٔ ۴ — Submit Pipeline (Task)
callback ✅ ساخت تسک:
dedup token check
اگر buffer خالی → "هیچ چیز برای ارسال نیست"
اگر watched_id ست نیست → ابتدا picker پروژه نشان بده، سپس submit
idea = "\n\n".join(text_parts).strip()
اگر idea خالی است و فایل هست → idea = "" (نه placeholder) و در پیام به idea_to_prompt یک flag intent_from_files_only=True ارسال شود
idea_to_prompt(idea, upload_session_ids=...) ← اگر intent_from_files_only، prompt مخصوص که می‌گوید "ایدهٔ کاربر فقط در فایل‌های پیوست است"
dedup check find_similar_active_tasks
create_task با upload_session_ids
buffer پاک، ReplyKeyboard remove
_send_task_created_message(...) با PDF (که در مرحلهٔ ۸ تکمیل می‌شود)
error path: exception → notify + keep buffer + ReplyKeyboard همچنان باقی
Validation: end-to-end synthetic
مرحلهٔ ۵ — Submit Pipeline (Project — NEW)
متد جدید /new_project flow هم compose mode را پشتیبانی کند:
کاربر /new_project می‌زند → enter creator_compose mode
file/voice/photo می‌فرستد → buffer
دکمهٔ ReplyKeyboard: ✅ ساخت پروژه با همه
submit → extraction → idea تولید می‌شود از فایل‌ها → idea_to_prompt_for_creator فعلی صدا زده می‌شود با attachments
creator-pipeline موجود (با تغییر کم) همان کار را می‌کند
این feature موازی با /new_task در همان compose buffer (با mode='project') کار می‌کند
Validation: smoke test full new_project با voice + photo
مرحلهٔ ۶ — Live Progress (Backend → SSE/Polling endpoint + Telegram editMessage)
در _resolve_attachments_for_idea و extract_session، یک progress_callback(stage, current, total, detail) اختیاری
services/oversight_progress.py:
ProgressTracker با in-memory store {task_draft_id: {stage, current, total, detail, updated_at}}
endpoint GET /api/oversight/progress/{task_draft_id} → snapshot
Telegram: یک «status message» در شروع submit، با progress callback خود نسخهٔ پیام را edit می‌کند (هر ۱۰ ثانیه — debounced)
Frontend: همان endpoint را poll می‌کند (با interval ۲s) و progress bar live نشان می‌دهد
Validation: یک فایل بزرگ → دیدن چند update در Telegram + frontend
مرحلهٔ ۷ — PDF 4-Page Chunking + جمع‌بندی نهایی
extract_pdf_pages_4_at_a_time:
text-pages با pypdf → هر یک per-page یک segment (سریع)
scan-pages → batchهای ۴‌تایی → render هر صفحه به PNG با pdftoppm → Gemini با ۴ تصویر یکجا، prompt: "متن کامل و literal همهٔ ۴ صفحه را به ترتیب شماره صفحه استخراج کن، هیچ خلاصه‌سازی نکن"
در نهایت segment آخر: segment_title = "📋 جمع‌بندی نهایی" با محتوای:
تعداد کل صفحات
تعداد text-pages و scan-pages
مدل استفاده‌شده
زمان شروع/پایان
char count کلی
hash sha256 از full text
بدون خلاصه‌سازی محتوا — فقط متادیتای پروسه
per chunk progress update روی tracker
Validation: PDF 20 صفحه (مخلوط) → ۵ batch، segment جمع‌بندی در آخر
مرحلهٔ ۸ — PDF Feedback Content (متن کامل extraction در PDF)
در oversight_verify_pdf.py:_build_verify_report_html:
یک section جدید ## 📎 متن کامل استخراج‌شده از فایل‌های پیوست
per file: filename + mime + model_used + total_segments + شروع/پایان
همهٔ segments به ترتیب — متن کامل (نه truncated)
این کار حجم PDF را زیاد می‌کند ولی کاربر صریحاً خواسته
برای فایل‌های خیلی بزرگ (>5MB متن کلی)، یک "appendix mode" که فقط جمع‌بندی + لینک می‌گذارد و یک PDF جدا برای متن کامل می‌فرستد
Validation: تسک با ۲ فایل ۱۰۰KB متن — PDF شامل کل متن
مرحلهٔ ۹ — Format Diversity Expansion (هم Telegram هم Frontend)
whitelist extension expanded:
صوت: mp3, wav, ogg, m4a, flac, aac, opus
تصویر: jpg, png, gif, webp, bmp, tiff, heic, svg
ویدئو: mp4, mov, avi, mkv, webm, m4v
سند: pdf, docx, doc, xlsx, xls, pptx, ppt, rtf, odt, ods, odp, epub
متن: txt, md, csv, json, xml, yaml, yml, toml, log, ini
کد: py, js, ts, tsx, jsx, java, c, cpp, h, hpp, go, rs, rb, php, html, css, scss, sql, sh, bash, ps1, dockerfile, kt, swift, dart, ipynb, r
archive: zip, tar, tar.gz, 7z (extract recursive, treat each file as a sub-segment)
mime fallback از extension اگر application/octet-stream
extraction routing:
code → text plain + heading per "// === <filename> ==="
ipynb → parse JSON → cells با ترتیب
SVG → XML text content
archive → extract به temp → recursive list فایل‌ها (هر file داخل یک segment با path-as-title)
icon mapping در frontend updated
Validation: هر فرمت smoke test
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent با سؤالات:
آیا compose buffer در همهٔ scenarioها (cancel، timeout، restart، duplicate click) درست cleanup می‌شود؟
آیا ReplyKeyboard در همهٔ paths (submit/cancel/error/timeout) به‌درستی حذف می‌شود؟
آیا /new_project با attachments backward-compatible است؟
آیا progress updates spam ایجاد نمی‌کنند؟
آیا media handlers با همهٔ ۸ نوع پیام درست کار می‌کنند؟
آیا PDF 4-page chunking روی edge cases (1 صفحه، 100+ صفحه) کار می‌کند؟
آیا متن کامل استخراج در PDF correctly drilled-down است؟
آیا format diversity (archive recursive) memory leak ندارد؟
موارد یافت‌شده fix می‌شوند
final integration test: یک سناریوی واقعی (voice 30s + photo + متن "این صدا را تحلیل کن") → tasks بدون خطا
نکات اجرای حیاتی
Telegram Bot API 20MB: برای فایل بزرگ‌تر، خطای واضح + پیشنهاد «از وب آپلود کنید».
editMessageText idempotency: diff check قبل از هر edit.
Compose buffer persistence: TTL ۳۰ دقیقه + boot recovery (مشابه oversight_extraction).
ReplyKeyboard removal: در همهٔ exit paths (submit, cancel, error, timeout).
متن کامل extraction در PDF: ممکن است حجم را خیلی بالا ببرد — بررسی Telegram document limit (50MB) و در صورت لزوم compress یا split.
«جمع‌بندی» هرگز معنای summarization محتوا نیست — فقط aggregated full text + متادیتای پروسه.
intent_from_files_only flag: در idea_to_prompt و multi-pass باید explicit در system prompt گفته شود.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
اصل ۱ — مرحله‌ای ولی کامل: هر مرحله pull-ready، deployable، بدون قطعهٔ half-done. هیچ TODO به مرحلهٔ بعد موکول نمی‌شود مگر صراحتاً out-of-scope.

اصل ۲ — بدون خلاصه‌سازی در استخراج: پرامپت به مدل بصری صریحاً «متن کامل، literal، هیچ‌چیز drop نشود» می‌خواهد.

اصل ۳ — mapping داینامیک مدل: gemini hard-code نمی‌شود؛ همیشه از pick_best_extraction_model(mime_type) که حسب enabled models + capability map انتخاب می‌کند.

اصل ۴ — resume from scratch ممنوع: هر segment به‌محض اتمام در DB ذخیره. crash میانه → ادامه از segment بعدی.

اصل ۵ — فایل اصلی پس از استخراج حذف شود: متن استخراج‌شده جایگزین. verifyهای بعدی به فایل اصلی نیاز ندارند.

اصل ۶ — backend capacity: semaphore + chunk streaming + memory watchdog. هیچ‌گاه OOM crash.

وضعیت زیرساخت (audit شد)
Capability	وضعیت	یادداشت
Upload endpoint + drag-drop UI	✅ موجود	upload.py, FileUpload.tsx — اما تمام فایل را در RAM می‌خواند
Storage 500MB cap	✅ موجود	storage_service.py
Gemini integration + images base64	✅ موجود	gemini_service.py
ModelCapability enum + registry	✅ موجود	models_registry.py
/models صفحهٔ مدیریت	✅ موجود	enable/disable + per-model settings + temporary_enabled field (استفاده‌نشده)
ai_manager routing با capability	✅ موجود	—
idea_to_prompt multi-pass	✅ موجود	—
Task JSON store + recovery on boot	✅ موجود	—
Chunked/resumable upload	❌ غایب	کل فایل در RAM = OOM روی 500MB
PDF/DOCX/XLSX/audio/video libs	❌ غایب	pypdf, python-docx, openpyxl, Pillow, pydub لازم
Extraction service	❌ غایب	—
ExtractionSegment persistence	❌ غایب	—
File ↔ idea_to_prompt link	❌ غایب	—
UI "Gemini disabled" alert + toggle	❌ غایب	—
Temp-enable workflow + notify	🟡 ناقص	DB field هست، logic نیست
Task detail: نمایش extracted text	❌ غایب	—
Dynamic vision-model picker	🟡 ناقص	enum هست، helper نیست
🪜 مراحل (۱۰ stage مستقل، هر کدام یک commit)
مرحلهٔ ۱ — وابستگی‌ها + Capability Registry
افزودن pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به requirements
extend ModelCapability با VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT
helper pick_best_extraction_model(mime_type) → بهترین مدل enabled با ranking
map mime → capability (image/* → IMAGE_ANALYSIS، video/* → VIDEO_UNDERSTANDING، …)
Validation: pytest روی picker + UI /models capabilities جدید را نشان دهد
مرحلهٔ ۲ — Backend: Streaming + Resumable Upload
table UploadSession (id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress)
endpoints: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}
aiofiles streaming → 5MB chunks → RAM ثابت
on boot: cleanup orphans >24h
Validation: upload 500MB، RSS RAM ~50MB بماند نه 500MB؛ crash midway → resume
مرحلهٔ ۳ — Frontend: Multi-file Drag-Drop + Chunked + Resumable
کامپوننت <TaskFilePicker /> در form ایده
چند فایل، ترتیب آپلود = file_order
progress bar per file + retry/cancel
localStorage session → resume on reload
chunk client = 5MB، parallel limit=2
Validation: drag 3×100MB، refresh، resume درست
مرحلهٔ ۴ — Extraction Service (هستهٔ feature)
file_extraction_service.py با routing per mime:
image/* → Gemini vision (full literal)
application/pdf → pypdf برای text-pages + Gemini برای صفحات scan/image
xlsx/docx → openpyxl/docx → metadata + Gemini برای interpretation
audio/* → Gemini transcription (full literal)
video/* → frame-by-frame هر 2s + audio parallel → Gemini
پرامپت استخراج: حسب user_idea، اول از مدل headings/sections بپرس → بعد per heading text کامل extract کن
بدون خلاصه‌سازی: instruction صریح: «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی»
streaming: per-segment yield → بلافاصله DB
Validation: 100-page PDF → 100 segment، crash mid → resume از segment آخر، نه از صفر
مرحلهٔ ۵ — Persistence Layer
table FileExtraction (task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used)
table ExtractionSegment (extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used)
repo extraction_repo.py
GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text
پس از completed → حذف temp_path
Validation: post-extraction، file روی disk نیست؛ restart → existing data persisted
مرحلهٔ ۶ — Gemini Toggle Flow
در /tasks/from-idea با upload_session_ids → اگر pick_best_extraction_model = None → return {"status": "blocked_no_vision_model", "candidates": [...]}
Frontend modal: "مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]"
backend POST /models/settings/{id}/temp-activate?trigger_task=... → temporary_enabled=True + notify telegram
post-extraction hook → revert + notify telegram
Validation: Gemini off → modal، toggle بزنی، extraction، خودکار revert + 2 notif
مرحلهٔ ۷ — Wiring idea_to_prompt با extraction
idea_to_prompt(idea, upload_session_ids=[...]) پارامتر جدید
await همهٔ extractions تمام شوند (هر کدام stream می‌کند، در DB)
متن کامل هر فایل به‌ترتیب file_order به idea append شود (ساختار ## فایل #N: filename)
multi-pass plan هر فایل را به stage مرتبط ربط دهد
اگر متن کل >50KB → ref به extraction_id نه inline
Validation: 3 فایل → پرامپت ≥3 stage، هر stage به فایل مربوطه ref
مرحلهٔ ۸ — UI: نمایش متن استخراج‌شده
TaskCard: badge "📎 N فایل، M segment"
modal/page: لیست فایل‌ها به‌ترتیب → per file segments collapsible → search + copy
Validation: نمایش، order، search، copy
مرحلهٔ ۹ — RAM/CPU Guardrails
Semaphore(1) سراسری برای extraction
ویدئو/صوت بزرگ: ffmpeg chunk به 5min → per chunk Gemini
memory watchdog: اگر RSS >80% → delay segment بعدی
timeout سخت 5min per segment → mark failed → ادامه
boot recovery: extracting + last_segment >5min → failed → resume
Validation: 500MB video → 25 chunk، RAM ثابت؛ SIGKILL mid → resume
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent: race، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram
رفع همهٔ ایرادات پیدا‌شده
smoke test سراسری: full flow (3 files → extract → idea_to_prompt → task → verify) بدون خطا

-------------------------------------
اصل ۱: بدون خلاصه‌سازی، بدون شورت‌کات. هر مرحله pull-ready.
اصل ۲: هر مرحله یک commit مستقل.
اصل ۳: backward-compatible — feature موجود web نباید break شود.
اصل ۴: تا دکمهٔ submit زده نشود، هیچ تسک/پروژه ساخته نمی‌شود — فقط buffer.
اصل ۵: ترتیب رسیدن = file_order در Telegram، مثل web.
اصل ۶: اگر کاربر هیچ متنی ننوشته ولی فایل فرستاده، محتوای استخراج‌شده = ایدهٔ کاربر؛ multi-pass باید این را بفهمد.
اصل ۷: «جمع‌بندی» = aggregated full text + meta-progress (تعداد segmentها، زمان، مدل) — نه summarization محتوا.
اصل ۸: progress در هر دو platform (Telegram + Frontend) در زمان واقعی قابل مشاهده.

مراحل (۱۰ stage مستقل، هر کدام یک commit)
مرحلهٔ ۱ — Compose Buffer State + Auto-Enter Mode
ساختار _compose_buffers: Dict[chat_id, ComposeBuffer] + persistence در compose_buffers.json
ComposeBuffer: chat_id, watched_id?, mode (task|project), started_at, items: List[ComposeItem], text_parts: List[str], task_draft_id
ComposeItem: {order, type, file_id, mime, size, telegram_file_path?, upload_session_id?, added_at}
auto-enter: اگر کاربر در phase=idle است و یک media می‌فرستد (voice/video_note/video/photo/document/audio/animation) → خودکار compose mode فعال شود + پیام راهنما: "📦 تشخیص داده شد می‌خواهی تسک ترکیبی بسازی. هر تعداد فایل/متن می‌خواهی بفرست. در پایان دکمه را بزن."
⚠️ اگر کاربر در awaiting_idea یا creator_* phase است، compose mode به همان context ضمیمه شود (نه phase جدید)
TTL: ۳۰ دقیقه
restart-safe: بازیابی buffer از JSON در boot
Validation: smoke تست auto-enter + persistence
مرحلهٔ ۲ — Telegram Media Handlers (همهٔ ۸ نوع)
handlers per type: voice, video_note, video, photo, audio, document, animation
پایپ‌لاین:
getFile(file_id) از Telegram Bot API
download (≤20MB Bot API limit، خطای واضح اگر بزرگ‌تر)
UploadSession ساختن: filename derived از type (voice → voice_YYYY-MM-DD-HH-MM-SS.ogg)
mime mapping: voice→audio/ogg، video_note→video/mp4، photo→image/jpeg
append_chunk + mark_completed
buffer.items.append با upload_session_id
متن (message.text غیر-دستوری) → اضافه به text_parts همراه order
اولویت: order = ترتیب رسیدن کلی (mixed با text و file)
Validation: هر ۸ نوع تست شود
مرحلهٔ ۳ — Persistent ReplyKeyboard (دکمه‌های بزرگ، دائم) + Inline status
دو لایه:
ReplyKeyboard پایین صفحه با دکمه‌های بزرگ:
✅ ساخت تسک با همه
❌ لغو همه
این keyboard تا پایان compose dormant می‌ماند (نه disappear با هر پیام)
inline status message که با هر افزودن آیتم، edit می‌شود (نه duplicate):
📦 در حال ساخت تسک — N آیتم: + لیست + اطلاعات هر آیتم
پس از submit/cancel، ReplyKeyboard حذف می‌شود (reply_markup={"remove_keyboard": True})
diff check قبل از editMessageText (جلوگیری از telegram error "message not modified")
Validation: ReplyKeyboard ظاهر/مخفی، status message یک‌تا و edit-only
مرحلهٔ ۴ — Submit Pipeline (Task)
callback ✅ ساخت تسک:
dedup token check
اگر buffer خالی → "هیچ چیز برای ارسال نیست"
اگر watched_id ست نیست → ابتدا picker پروژه نشان بده، سپس submit
idea = "\n\n".join(text_parts).strip()
اگر idea خالی است و فایل هست → idea = "" (نه placeholder) و در پیام به idea_to_prompt یک flag intent_from_files_only=True ارسال شود
idea_to_prompt(idea, upload_session_ids=...) ← اگر intent_from_files_only، prompt مخصوص که می‌گوید "ایدهٔ کاربر فقط در فایل‌های پیوست است"
dedup check find_similar_active_tasks
create_task با upload_session_ids
buffer پاک، ReplyKeyboard remove
_send_task_created_message(...) با PDF (که در مرحلهٔ ۸ تکمیل می‌شود)
error path: exception → notify + keep buffer + ReplyKeyboard همچنان باقی
Validation: end-to-end synthetic
مرحلهٔ ۵ — Submit Pipeline (Project — NEW)
متد جدید /new_project flow هم compose mode را پشتیبانی کند:
کاربر /new_project می‌زند → enter creator_compose mode
file/voice/photo می‌فرستد → buffer
دکمهٔ ReplyKeyboard: ✅ ساخت پروژه با همه
submit → extraction → idea تولید می‌شود از فایل‌ها → idea_to_prompt_for_creator فعلی صدا زده می‌شود با attachments
creator-pipeline موجود (با تغییر کم) همان کار را می‌کند
این feature موازی با /new_task در همان compose buffer (با mode='project') کار می‌کند
Validation: smoke test full new_project با voice + photo
مرحلهٔ ۶ — Live Progress (Backend → SSE/Polling endpoint + Telegram editMessage)
در _resolve_attachments_for_idea و extract_session، یک progress_callback(stage, current, total, detail) اختیاری
services/oversight_progress.py:
ProgressTracker با in-memory store {task_draft_id: {stage, current, total, detail, updated_at}}
endpoint GET /api/oversight/progress/{task_draft_id} → snapshot
Telegram: یک «status message» در شروع submit، با progress callback خود نسخهٔ پیام را edit می‌کند (هر ۱۰ ثانیه — debounced)
Frontend: همان endpoint را poll می‌کند (با interval ۲s) و progress bar live نشان می‌دهد
Validation: یک فایل بزرگ → دیدن چند update در Telegram + frontend
مرحلهٔ ۷ — PDF 4-Page Chunking + جمع‌بندی نهایی
extract_pdf_pages_4_at_a_time:
text-pages با pypdf → هر یک per-page یک segment (سریع)
scan-pages → batchهای ۴‌تایی → render هر صفحه به PNG با pdftoppm → Gemini با ۴ تصویر یکجا، prompt: "متن کامل و literal همهٔ ۴ صفحه را به ترتیب شماره صفحه استخراج کن، هیچ خلاصه‌سازی نکن"
در نهایت segment آخر: segment_title = "📋 جمع‌بندی نهایی" با محتوای:
تعداد کل صفحات
تعداد text-pages و scan-pages
مدل استفاده‌شده
زمان شروع/پایان
char count کلی
hash sha256 از full text
بدون خلاصه‌سازی محتوا — فقط متادیتای پروسه
per chunk progress update روی tracker
Validation: PDF 20 صفحه (مخلوط) → ۵ batch، segment جمع‌بندی در آخر
مرحلهٔ ۸ — PDF Feedback Content (متن کامل extraction در PDF)
در oversight_verify_pdf.py:_build_verify_report_html:
یک section جدید ## 📎 متن کامل استخراج‌شده از فایل‌های پیوست
per file: filename + mime + model_used + total_segments + شروع/پایان
همهٔ segments به ترتیب — متن کامل (نه truncated)
این کار حجم PDF را زیاد می‌کند ولی کاربر صریحاً خواسته
برای فایل‌های خیلی بزرگ (>5MB متن کلی)، یک "appendix mode" که فقط جمع‌بندی + لینک می‌گذارد و یک PDF جدا برای متن کامل می‌فرستد
Validation: تسک با ۲ فایل ۱۰۰KB متن — PDF شامل کل متن
مرحلهٔ ۹ — Format Diversity Expansion (هم Telegram هم Frontend)
whitelist extension expanded:
صوت: mp3, wav, ogg, m4a, flac, aac, opus
تصویر: jpg, png, gif, webp, bmp, tiff, heic, svg
ویدئو: mp4, mov, avi, mkv, webm, m4v
سند: pdf, docx, doc, xlsx, xls, pptx, ppt, rtf, odt, ods, odp, epub
متن: txt, md, csv, json, xml, yaml, yml, toml, log, ini
کد: py, js, ts, tsx, jsx, java, c, cpp, h, hpp, go, rs, rb, php, html, css, scss, sql, sh, bash, ps1, dockerfile, kt, swift, dart, ipynb, r
archive: zip, tar, tar.gz, 7z (extract recursive, treat each file as a sub-segment)
mime fallback از extension اگر application/octet-stream
extraction routing:
code → text plain + heading per "// === <filename> ==="
ipynb → parse JSON → cells با ترتیب
SVG → XML text content
archive → extract به temp → recursive list فایل‌ها (هر file داخل یک segment با path-as-title)
icon mapping در frontend updated
Validation: هر فرمت smoke test
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent با سؤالات:
آیا compose buffer در همهٔ scenarioها (cancel، timeout، restart، duplicate click) درست cleanup می‌شود؟
آیا ReplyKeyboard در همهٔ paths (submit/cancel/error/timeout) به‌درستی حذف می‌شود؟
آیا /new_project با attachments backward-compatible است؟
آیا progress updates spam ایجاد نمی‌کنند؟
آیا media handlers با همهٔ ۸ نوع پیام درست کار می‌کنند؟
آیا PDF 4-page chunking روی edge cases (1 صفحه، 100+ صفحه) کار می‌کند؟
آیا متن کامل استخراج در PDF correctly drilled-down است؟
آیا format diversity (archive recursive) memory leak ندارد؟
موارد یافت‌شده fix می‌شوند
final integration test: یک سناریوی واقعی (voice 30s + photo + متن "این صدا را تحلیل کن") → tasks بدون خطا
نکات اجرای حیاتی
Telegram Bot API 20MB: برای فایل بزرگ‌تر، خطای واضح + پیشنهاد «از وب آپلود کنید».
editMessageText idempotency: diff check قبل از هر edit.
Compose buffer persistence: TTL ۳۰ دقیقه + boot recovery (مشابه oversight_extraction).
ReplyKeyboard removal: در همهٔ exit paths (submit, cancel, error, timeout).
متن کامل extraction در PDF: ممکن است حجم را خیلی بالا ببرد — بررسی Telegram document limit (50MB) و در صورت لزوم compress یا split.
«جمع‌بندی» هرگز معنای summarization محتوا نیست — فقط aggregated full text + متادیتای پروسه.
intent_from_files_only flag: در idea_to_prompt و multi-pass باید explicit در system prompt گفته شود.
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: افزودن وابستگی‌ها و گسترش ModelCapability Registry با picker helper** — این مرحله شامل افزودن کتابخانه‌های pypdf, python-docx, openpyxl, Pillow, pydub به فایل requirements.txt و گسترش enum ModelCapability در models_registry.py با مقادیر جدید VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT است. همچنین یک تابع کمکی pick_best_extraction_model
- [x] **مرحله 2: پیاده‌سازی Backend Streaming و Resumable Upload با جدول UploadSession** — این مرحله شامل ایجاد جدول جدید UploadSession در دیتابیس با فیلدهای id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress و پیاده‌سازی چهار endpoint جدید است: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /u
- [x] **مرحله 3: ساخت کامپوننت Frontend Multi-file Drag-Drop با Chunked و Resumable Upload** — این مرحله شامل ایجاد کامپوننت جدید <TaskFilePicker /> در فرم ایده است که از multi-file drag-drop پشتیبانی می‌کند. فایل‌ها با ترتیب آپلود (file_order) ذخیره می‌شوند. یک progress bar per-file به همراه دکمه‌های retry و cancel نمایش داده می‌شود. از localStorage برای ذخیره session و قابلیت resume پس از r
- [x] **مرحله 4: پیاده‌سازی Extraction Service هسته با routing per mime type** — این مرحله شامل ایجاد فایل file_extraction_service.py است که بر اساس mime type به سرویس‌های مختلف routing می‌کند: image/* → Gemini vision با full literal، application/pdf → pypdf برای text-pages + Gemini برای scan pages، xlsx/docx → openpyxl/docx برای metadata + Gemini برای interpretation، audio/* → 
- [x] **مرحله 5: ایجاد Persistence Layer با جداول FileExtraction و ExtractionSegment** — این مرحله شامل ایجاد دو جدول جدید در دیتابیس است: FileExtraction با فیلدهای task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used و ExtractionSegment با فیلدهای extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_
- [x] **مرحله 6: پیاده‌سازی Gemini Toggle Flow برای زمان عدم وجود مدل بصری فعال** — این مرحله شامل تغییر endpoint /tasks/from-idea است به طوری که اگر upload_session_ids وجود داشته باشد و pick_best_extraction_model None برگرداند، response با status 'blocked_no_vision_model' و لیست candidates برگردانده شود. در frontend یک modal نمایش داده می‌شود با پیام «مدل بصری فعال نیست — Gemini 2
- [x] **مرحله 7: اتصال idea_to_prompt با extraction و append متن کامل فایل‌ها** — این مرحله شامل افزودن پارامتر جدید upload_session_ids به تابع idea_to_prompt است. این تابع منتظر می‌ماند تا همه extractions تمام شوند (هر کدام stream می‌کنند و در DB ذخیره می‌شوند). سپس متن کامل هر فایل به ترتیب file_order به idea append می‌شود با ساختار '## فایل #N: filename'. multi-pass plan هر فا
- [x] **مرحله 8: ایجاد UI برای نمایش متن استخراج‌شده در TaskCard و modal** — این مرحله شامل به‌روزرسانی TaskCard برای نمایش badge '📎 N فایل، M segment' است. یک modal یا صفحه جدید ایجاد می‌شود که لیست فایل‌ها را به ترتیب نمایش می‌دهد و per file segments به صورت collapsible با قابلیت search و copy نمایش داده می‌شود. خارج از این مرحله: backend extraction logic، Telegram integra
- [x] **مرحله 9: پیاده‌سازی RAM/CPU Guardrails با Semaphore، Memory Watchdog و Timeout** — این مرحله شامل افزودن Semaphore(1) سراسری برای extraction، chunk کردن ویدئو/صوت بزرگ با ffmpeg به قطعات 5 دقیقه‌ای و پردازش per chunk با Gemini، پیاده‌سازی memory watchdog که اگر RSS >80% باشد segment بعدی را به تأخیر می‌اندازد، timeout سخت 5 دقیقه per segment که در صورت تجاوز segment را failed mark
- [ ] **مرحله 10: Audit نهایی با Explore Agent و رفع Bug و Smoke Test سراسری** — این مرحله شامل اجرای Explore agent برای بررسی race condition، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram است. تمام ایرادات یافت‌شده رفع می‌شوند. سپس یک smoke test سراسری اجرا می‌شود: full flow با 3 فایل → extract → idea_to_pro

---

# 🔹 مرحله 1: افزودن وابستگی‌ها و گسترش ModelCapability Registry با picker helper

**Scope:** این مرحله شامل افزودن کتابخانه‌های pypdf, python-docx, openpyxl, Pillow, pydub به فایل requirements.txt و گسترش enum ModelCapability در models_registry.py با مقادیر جدید VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT است. همچنین یک تابع کمکی pick_best_extraction_model(mime_type) ایجاد می‌کند که بر اساس mime type، بهترین مدل فعال را با ranking برمی‌گرداند و یک mapping از mime به capability ایجاد می‌کند (مثلاً image/* → IMAGE_ANALYSIS). خارج از این مرحله: پیاده‌سازی منطق استخراج واقعی، تغییرات UI، یا endpoint‌های جدید. نکته حیاتی: این مرحله پایه‌ای است و باید کاملاً backward-compatible باشد تا featureهای موجود web نشکنند.
**Key terms:** requirements.txt, ModelCapability, models_registry.py, pick_best_extraction_model, IMAGE_ANALYSIS, VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT, pypdf, python-docx, openpyxl, Pillow, pydub

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۱ — وابستگی‌ها + Capability Registry
افزودن pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به requirements
extend ModelCapability با VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT
helper pick_best_extraction_model(mime_type) → بهترین مدل enabled با ranking
map mime → capability (image/* → IMAGE_ANALYSIS، video/* → VIDEO_UNDERSTANDING، …)
Validation: pytest روی picker + UI /models capabilities جدید را نشان دهد
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن وابستگی‌های استخراج فایل و گسترش ModelCapability Registry با picker helper

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/requirements.txt:40-45` — `dependencies`
  ```
  # 🆕 File extraction (Stage 1 — multimodal task attachments)
  # سبک — فقط متادیتا/متن. خود تحلیل بصری/صوتی/ویدیو با Gemini انجام می‌شود.
  pypdf>=4.0.0
  python-docx>=1.1.0
  openpyxl>=3.1.0
  Pillow>=10
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن وابستگی‌های pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به فایل requirements.txt و گسترش enum ModelCapability در models_registry.py با مقادیر جدید VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT را دارد. همچنین یک تابع کمکی pick_best_extraction_model(mime_type) ایجاد می‌کند که بر اساس mime type، بهترین مدل فعال را با ranking برمی‌گرداند و یک mapping از mime به capability ایجاد می‌کند (مثلاً image/* → IMAGE_ANALYSIS). خارج از این مرحله: پیاده‌سازی منطق استخراج واقعی، تغییرات UI، یا endpoint‌های جدید. نکته حیاتی: این مرحله پایه‌ای است و باید کاملاً backward-compatible باشد تا featureهای موجود web نشکنند.

شواهد در کد واقعی:
- فایل requirements.txt (خطوط 1-40): در حال حاضر شامل کتابخانه‌های pypdf, python-docx, openpyxl, Pillow, pydub نیست.
- فایل backend/app/core/models_registry.py (خطوط 22-38): enum ModelCapability در حال حاضر شامل TEXT, CODE, REASONING, VISION, IMAGE_ANALYSIS, IMAGE_GENERATION, FAST_RESPONSE, LONG_CONTEXT, THINKING, MULTIMODAL است. مقادیر VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING در خطوط 36-38 به عنوان کامنت (Stage 1 — multimodal extraction) ذکر شده‌اند اما فعال نیستند.
- فایل backend/app/core/models_registry.py (خطوط 479-509): تابع mime_to_required_capability از قبل وجود دارد و mapping mime → capability را انجام می‌دهد.
- فایل backend/app/core/models_registry.py (خطوط 517-580): تابع pick_best_extraction_model از قبل وجود دارد و بهترین مدل را بر اساس mime type انتخاب می‌کند.
- فایل backend/app/core/models_registry.py (خط 514): متغیر DEFAULT_EXTRACTION_MODEL_ID = "gemini-2.5-flash" از قبل تعریف شده است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن کتابخانه‌های pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به فایل requirements.txt (بعد از خط 40، بخش # 🆕 File extraction).
2. فعال‌سازی مقادیر VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING در enum ModelCapability در فایل backend/app/core/models_registry.py (خطوط 36-38) با حذف کامنت و اضافه کردن کاما.
3. افزودن قابلیت LONG_CONTEXT به مدل‌های مناسب در MODEL_REGISTRY (مانند gpt-4o, claude-sonnet-4-20250514, gemini-2.5-pro).
4. اطمینان از backward-compatibility: تابع pick_best_extraction_model (خطوط 517-580) و mime_to_required_capability (خطوط 479-509) از قبل وجود دارند و نیازی به تغییر ندارند.
5. اجرای pytest برای validation تابع picker و بررسی نمایش capabilities جدید در UI /models.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: small

---

# 🔹 مرحله 2: پیاده‌سازی Backend Streaming و Resumable Upload با جدول UploadSession

**Scope:** این مرحله شامل ایجاد جدول جدید UploadSession در دیتابیس با فیلدهای id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress و پیاده‌سازی چهار endpoint جدید است: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}. فایل‌ها به صورت streaming با aiofiles و chunks 5MB پردازش می‌شوند تا RAM ثابت بماند. همچنین یک cron job برای پاک‌سازی orphan session‌های قدیمی‌تر از 24 ساعت در boot اضافه می‌شود. خارج از این مرحله: frontend کامپوننت، extraction service، یا logic مربوط به idea_to_prompt. نکته حیاتی: این مرحله باید تضمین کند که آپلود یک فایل 500MB حافظه RAM را تا حدود 50MB نگه دارد نه 500MB و قابلیت resume از mid-crash را داشته باشد.
**Key terms:** UploadSession, POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}, aiofiles, 5MB chunks, RAM ثابت, orphan cleanup

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۲ — Backend: Streaming + Resumable Upload
table UploadSession (id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress)
endpoints: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}
aiofiles streaming → 5MB chunks → RAM ثابت
on boot: cleanup orphans >24h
Validation: upload 500MB، RSS RAM ~50MB بماند نه 500MB؛ crash midway → resume
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی Backend Streaming و Resumable Upload با جدول UploadSession

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_extraction.py:40-43` — `import of oversight_upload_session` — این فایل از UploadSession فعلی استفاده می‌کند. مدل جدید باید با این ساختار سازگار باشد یا این import به مدل جدید تغییر کند.
  ```python
  from .oversight_upload_session import (
      get_upload_session_service, UploadSession,
  )
  ```
- `backend/app/api/r`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک سیستم آپلود فایل با قابلیت streaming و resume در backend را دارد. این شامل ایجاد جدول جدید `UploadSession` در دیتابیس با فیلدهای `id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress` است. چهار endpoint جدید باید ایجاد شوند: `POST /uploads/start`, `POST /uploads/{id}/chunk`, `POST /uploads/{id}/complete`, `GET /uploads/{id}`, `DELETE /uploads/{id}`. فایل‌ها باید به صورت streaming با استفاده از `aiofiles` و chunks 5MB پردازش شوند تا مصرف RAM ثابت بماند. یک cron job برای پاک‌سازی orphan session‌های قدیمی‌تر از 24 ساعت در boot اضافه شود. نکته حیاتی: آپلود یک فایل 500MB باید حافظه RAM را تا حدود 50MB نگه دارد نه 500MB و قابلیت resume از mid-crash را داشته باشد. خارج از این مرحله: frontend کامپوننت، extraction service، یا logic مربوط به idea_to_prompt. در کد فعلی پروژه، فایل `backend/app/services/oversight_extraction.py` (خطوط 40-43) از `oversight_upload_session` برای مدیریت session‌های آپلود استفاده می‌کند که شامل کلاس `UploadSession` و تابع `get_upload_session_service` است. همچنین فایل `backend/app/services/oversight_upload_session.py` (که deep-read نشده) احتمالاً حاوی پیاده‌سازی فعلی session‌ها است. فایل `backend/app/api/routes/analysis.py` (خطوط 46-80) نمونه‌ای از یک endpoint POST با `BackgroundTasks` را نشان می‌دهد که می‌تواند الگویی برای endpointهای جدید باشد. فایل `backend/app/main.py` (deep-read شده) نقطه ورود برای ثبت routerهای جدید است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل دیتابیس `UploadSession` در فایل جدید `backend/app/models/upload_session.py` با فیلدهای مشخص‌شده (id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress). 2. ایجاد فایل سرویس جدید `backend/app/services/upload_service.py` با توابع: `start_upload`, `append_chunk` (با aiofiles و chunks 5MB), `complete_upload`, `get_upload`, `delete_upload`, `cleanup_orphans` (برای cron job). 3. ایجاد فایل router جدید `backend/app/api/routes/uploads.py` با چهار endpoint: `POST /uploads/start`, `POST /uploads/{id}/chunk`, `POST /uploads/{id}/complete`, `GET /uploads/{id}`, `DELETE /uploads/{id}`. 4. ثبت router در `backend/app/main.py` با `app.include_router(uploads_router)`. 5. اضافه کردن cron job در `backend/app/services/background_scheduler.py` برای پاک‌سازی orphan session‌های قدیمی‌تر از 24 ساعت در boot. 6. استفاده از `aiofiles.open` با حالت `'ab'` برای append chunkها به فایل موقت در `temp_path`. 7. پیاده‌سازی resume با بررسی `bytes_received` در endpoint `POST /uploads/{id}/chunk` و استفاده از `Content-Range` header. 8. تضمین مصرف RAM ~50MB برای فایل 500MB با پردازش chunk به chunk و عدم نگهداری کل فایل در حافظه.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 3: ساخت کامپوننت Frontend Multi-file Drag-Drop با Chunked و Resumable Upload

**Scope:** این مرحله شامل ایجاد کامپوننت جدید <TaskFilePicker /> در فرم ایده است که از multi-file drag-drop پشتیبانی می‌کند. فایل‌ها با ترتیب آپلود (file_order) ذخیره می‌شوند. یک progress bar per-file به همراه دکمه‌های retry و cancel نمایش داده می‌شود. از localStorage برای ذخیره session و قابلیت resume پس از reload استفاده می‌کند. chunk size در سمت کلاینت 5MB و parallel limit=2 است. خارج از این مرحله: backend endpoint‌ها، extraction service، یا logic مربوط به Telegram. نکته حیاتی: این کامپوننت باید با backend جدید (مرحله ۲) هماهنگ باشد و drag 3 فایل 100MB را بدون مشکل مدیریت کند.
**Key terms:** <TaskFilePicker />, file_order, progress bar, retry, cancel, localStorage, 5MB chunk, parallel limit=2

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۳ — Frontend: Multi-file Drag-Drop + Chunked + Resumable
کامپوننت <TaskFilePicker /> در form ایده
چند فایل، ترتیب آپلود = file_order
progress bar per file + retry/cancel
localStorage session → resume on reload
chunk client = 5MB، parallel limit=2
Validation: drag 3×100MB، refresh، resume درست
```

## 🎯 هدف (خلاصه ساختاریافته)
ساخت کامپوننت Frontend Multi-file Drag-Drop با Chunked و Resumable Upload — <TaskFilePicker />

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/components/TaskFilePicker.tsx:1-100` — `TaskFilePicker` — فایل deep-read شده است. کامپوننت فعلی ساده است و باید به multi-file drag-drop با chunked upload تبدیل شود.
  ```tsx
  کامپوننت فعلی یک input ساده برای انتخاب فایل است. باید به طور کامل بازنویسی شود.
  ```
- `frontend/src`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ساخت کامپوننت جدید <TaskFilePicker /> در فرم ایده را دارد که از multi-file drag-drop پشتیبانی می‌کند. فایل‌ها با ترتیب آپلود (file_order) ذخیره می‌شوند. یک progress bar per-file به همراه دکمه‌های retry و cancel نمایش داده می‌شود. از localStorage برای ذخیره session و قابلیت resume پس از reload استفاده می‌کند. chunk size در سمت کلاینت 5MB و parallel limit=2 است. خارج از این مرحله: backend endpoint‌ها، extraction service، یا logic مربوط به Telegram. نکته حیاتی: این کامپوننت باید با backend جدید (مرحله ۲) هماهنگ باشد و drag 3 فایل 100MB را بدون مشکل مدیریت کند. بخش مربوط از درخواست اصلی کاربر: 'مرحلهٔ ۳ — Frontend: Multi-file Drag-Drop + Chunked + Resumable. کامپوننت <TaskFilePicker /> در form ایده. چند فایل، ترتیب آپلود = file_order. progress bar per file + retry/cancel. localStorage session → resume on reload. chunk client = 5MB، parallel limit=2. Validation: drag 3×100MB، refresh، resume درست'. کلیدواژه‌ها: <TaskFilePicker />, file_order, progress bar, retry, cancel, localStorage, 5MB chunk, parallel limit=2. در کد فعلی پروژه، فایل frontend/src/components/TaskFilePicker.tsx وجود دارد (deep-read شده) که یک کامپوننت ساده برای انتخاب فایل است و باید بازنویسی شود. همچنین frontend/src/app/creator/page.tsx (deep-read شده) فرم ایده را شامل می‌شود که <TaskFilePicker /> در آن استفاده می‌شود. backend/app/services/oversight_extraction.py (deep-read شده) سرویس extraction است که endpointهای آپلود فایل را مدیریت می‌کند و باید با chunked upload هماهنگ شود. backend/app/api/routes/oversight.py (deep-read شده) روتر مربوط به oversight است که endpointهای آپلود را تعریف می‌کند. frontend/src/lib/api.ts و frontend/src/services/api.ts سرویس‌های API فرانت‌اند هستند که باید تابع upload با پشتیبانی از chunked و resume اضافه کنند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بازنویسی کامل frontend/src/components/TaskFilePicker.tsx: اضافه کردن قابلیت drag-drop چند فایل، مدیریت state برای لیست فایل‌ها با file_order، progress bar per-file، دکمه‌های retry و cancel. 2. پیاده‌سازی logic chunked upload در frontend/src/lib/api.ts: تابع uploadFileInChunks که فایل را به chunks 5MB تقسیم کند، با parallel limit=2، و از AbortController برای cancel استفاده کند. 3. پیاده‌سازی resume با localStorage: ذخیره sessionId و metadata هر فایل (نام، size، chunks آپلود شده) در localStorage، و در صورت reload، resume از آخرین chunk آپلود شده. 4. هماهنگی با backend: اطمینان از اینکه endpointهای backend (backend/app/api/routes/oversight.py و backend/app/services/oversight_extraction.py) از chunked upload با پارامترهای file_id، chunk_index، total_chunks پشتیبانی می‌کنند. 5. یکپارچه‌سازی در frontend/src/app/creator/page.tsx: جایگزینی کامپوننت قدیمی با <TaskFilePicker /> جدید و ارسال file_order به backend هنگام submit فرم.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 4: پیاده‌سازی Extraction Service هسته با routing per mime type

**Scope:** این مرحله شامل ایجاد فایل file_extraction_service.py است که بر اساس mime type به سرویس‌های مختلف routing می‌کند: image/* → Gemini vision با full literal، application/pdf → pypdf برای text-pages + Gemini برای scan pages، xlsx/docx → openpyxl/docx برای metadata + Gemini برای interpretation، audio/* → Gemini transcription با full literal، video/* → frame-by-frame هر 2 ثانیه + audio parallel → Gemini. پرامپت استخراج بر اساس user_idea ابتدا headings/sections را می‌پرسد و سپس per heading متن کامل را extract می‌کند. دستور صریح «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی» در پرامپت گنجانده می‌شود. خروجی به صورت streaming per-segment yield شده و بلافاصله در DB ذخیره می‌شود. خارج از این مرحله: persistence layer، UI نمایش، یا Telegram integration. نکته حیاتی: این سرویس باید قابلیت resume از crash را داشته باشد و یک PDF 100 صفحه‌ای را به 100 segment تبدیل کند.
**Key terms:** file_extraction_service.py, Gemini vision, pypdf, openpyxl, python-docx, Gemini transcription, frame-by-frame, per-segment yield, user_idea, headings/sections

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۴ — Extraction Service (هستهٔ feature)
file_extraction_service.py با routing per mime:
image/* → Gemini vision (full literal)
application/pdf → pypdf برای text-pages + Gemini برای صفحات scan/image
xlsx/docx → openpyxl/docx → metadata + Gemini برای interpretation
audio/* → Gemini transcription (full literal)
video/* → frame-by-frame هر 2s + audio parallel → Gemini
پرامپت استخراج: حسب user_idea، اول از مدل headings/sections بپرس → بعد per heading text کامل extract کن
بدون خلاصه‌سازی: instruction صریح: «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی»
streaming: per-segment yield → بلافاصله DB
Validation: 100-page PDF → 100 segment، crash mid → resume از segment آخر، نه از صفر
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سرویس استخراج فایل (file_extraction_service.py) با routing بر اساس mime type و قابلیت resume از crash

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی Extraction Service هسته با routing per mime type را دارد. این مرحله شامل ایجاد فایل file_extraction_service.py است که بر اساس mime type به سرویس‌های مختلف routing می‌کند: image/* → Gemini vision با full literal، application/pdf → pypdf برای text-pages + Gemini برای scan pages، xlsx/docx → openpyxl/docx برای metadata + Gemini برای interpretation، audio/* → Gemini transcription با full literal، video/* → frame-by-frame هر 2 ثانیه + audio parallel → Gemini. پرامپت استخراج بر اساس user_idea ابتدا headings/sections را می‌پرسد و سپس per heading متن کامل را extract می‌کند. دستور صریح «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی» در پرامپت گنجانده می‌شود. خروجی به صورت streaming per-segment yield شده و بلافاصله در DB ذخیره می‌شود. خارج از این مرحله: persistence layer، UI نمایش، یا Telegram integration. نکته حیاتی: این سرویس باید قابلیت resume از crash را داشته باشد و یک PDF 100 صفحه‌ای را به 100 segment تبدیل کند. بخش مربوط از درخواست اصلی کاربر: مرحلهٔ ۴ — Extraction Service (هستهٔ feature) file_extraction_service.py با routing per mime: image/* → Gemini vision (full literal)، application/pdf → pypdf برای text-pages + Gemini برای صفحات scan/image، xlsx/docx → openpyxl/docx → metadata + Gemini برای interpretation، audio/* → Gemini transcription (full literal)، video/* → frame-by-frame هر 2s + audio parallel → Gemini. پرامپت استخراج: حسب user_idea، اول از مدل headings/sections بپرس → بعد per heading text کامل extract کن. بدون خلاصه‌سازی: instruction صریح: «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی». streaming: per-segment yield → بلافاصله DB. Validation: 100-page PDF → 100 segment، crash mid → resume از segment آخر، نه از صفر. کلیدواژه‌ها: file_extraction_service.py, Gemini vision, pypdf, openpyxl, python-docx, Gemini transcription, frame-by-frame, per-segment yield, user_idea, headings/sections. شواهد در کد واقعی پروژه: فایل backend/app/services/oversight_extraction.py (خطوط 1-800) یک سرویس extraction کامل با routing per-mime (text, PDF, DOCX, XLSX, image, audio, video) دارد که شامل _ai_extract_text (خط 396)، _plan_headings (خط 461)، _extract_pdf_pages (خط 568)، _extract_docx_paragraphs (خط 591)، _extract_xlsx_sheets (خط 615)، _extract_zip_archive (خط 757) و _extract_ipynb (خط 717) است. این فایل از ExtractionRepo برای persistence استفاده می‌کند (خط 199) و قابلیت boot_recover_stale_extractions (خط 342) برای resume از crash دارد. فایل backend/app/api/routes/analysis.py (خطوط 1-268) یک endpoint استریمینگ (run_analysis_stream) با SSE دارد که می‌تواند الگوی per-segment yield را ارائه دهد. فایل backend/app/services/ai_manager.py و backend/app/services/ai_base.py برای فراخوانی AI provider-agnostic استفاده می‌شوند. فایل backend/app/core/models_registry.py تابع pick_best_extraction_model را برای انتخاب مدل بر اساس mime فراهم می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/file_extraction_service.py با کلاس FileExtractionService که متد اصلی extract_file(file_path, mime_type, user_idea, session_id) را دارد. 2. پیاده‌سازی routing per mime type: برای image/* از Gemini vision (full literal) با فراخوانی _ai_extract_text از oversight_extraction.py (خط 396) استفاده شود. برای application/pdf: ابتدا pypdf برای استخراج متن از صفحات text-based (مشابه _extract_pdf_p

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 5: ایجاد Persistence Layer با جداول FileExtraction و ExtractionSegment

**Scope:** این مرحله شامل ایجاد دو جدول جدید در دیتابیس است: FileExtraction با فیلدهای task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used و ExtractionSegment با فیلدهای extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used. همچنین یک repository به نام extraction_repo.py برای دسترسی به این داده‌ها ایجاد می‌شود. دو endpoint جدید GET /tasks/{id}/extractions و GET /tasks/{id}/extractions/{id}/full-text اضافه می‌شوند. پس از تکمیل extraction، temp_path فایل حذف می‌شود. خارج از این مرحله: logic استخراج، UI نمایش، یا Telegram integration. نکته حیاتی: پس از extraction، فایل اصلی نباید روی disk باقی بماند و restart سرور باید داده‌های موجود را حفظ کند.
**Key terms:** FileExtraction, ExtractionSegment, extraction_repo.py, GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text, temp_path, task_id, session_id

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۵ — Persistence Layer
table FileExtraction (task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used)
table ExtractionSegment (extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used)
repo extraction_repo.py
GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text
پس از completed → حذف temp_path
Validation: post-extraction، file روی disk نیست؛ restart → existing data persisted
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد Persistence Layer برای FileExtraction و ExtractionSegment با جداول دیتابیس و Repository جدید

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_extraction.py:154-192` — `FileExtraction و ExtractionSegment dataclasses`
  ```python
  @dataclass
  class ExtractionSegment:
      id: str
      extraction_id: str
      segment_index: int
      segment_title: str
      text: str = ""
      raw_excerpt: str = ""
      page_or_timestamp: str = ""
      model_used: str = ""
      started_at: str = field(default_factory=now_iso)
      finished_at: Optional[str] = None
      status: str = "pending"
      error: Optional[str] = None
  
  @dataclass
  class FileExtraction:
      id: str
      task_id: Optional[str]
      session_id
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد Persistence Layer با دو جدول جدید در دیتابیس را دارد: FileExtraction با فیلدهای task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used و ExtractionSegment با فیلدهای extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used. همچنین یک repository به نام extraction_repo.py برای دسترسی به این داده‌ها ایجاد می‌شود. دو endpoint جدید GET /tasks/{id}/extractions و GET /tasks/{id}/extractions/{id}/full-text اضافه می‌شوند. پس از تکمیل extraction، temp_path فایل حذف می‌شود. خارج از این مرحله: logic استخراج، UI نمایش، یا Telegram integration. نکته حیاتی: پس از extraction، فایل اصلی نباید روی disk باقی بماند و restart سرور باید داده‌های موجود را حفظ کند.

کلیدواژه‌های کاربر: FileExtraction, ExtractionSegment, extraction_repo.py, GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text, temp_path, task_id, session_id

شواهد در کد واقعی: در فایل backend/app/services/oversight_extraction.py خطوط 154-192 دو دیتاکلاس FileExtraction و ExtractionSegment وجود دارند که دقیقاً با فیلدهای درخواستی کاربر مطابقت دارند (FileExtraction: id, task_id, session_id, file_order, original_filename, mime_type, total_segments, completed_segments, status, model_used, started_at, finished_at, error, full_text_cache — ExtractionSegment: id, extraction_id, segment_index, segment_title, text, raw_excerpt, page_or_timestamp, model_used, started_at, finished_at, status, error). همچنین یک ExtractionRepo کلاس در خطوط 199-329 وجود دارد که عملیات CRUD را روی JSON انجام می‌دهد. کاربر می‌خواهد این persistence از JSON به دیتابیس واقعی (SQLite از طریق SQLAlchemy) منتقل شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد دو مدل دیتابیس جدید در backend/app/models/ با نام‌های FileExtractionModel و ExtractionSegmentModel با استفاده از SQLAlchemy و Base از backend/app/core/database.py
2. ایجاد فایل جدید backend/app/services/extraction_repo.py که از SessionLocal برای عملیات CRUD استفاده کند و متدهای create_extraction, update_extraction, add_segment, update_segment, get, get_segments, list_by_task, list_by_session, full_text را پیاده‌سازی کند
3. اضافه کردن دو endpoint جدید در backend/app/api/routes/oversight.py: GET /tasks/{id}/extractions و GET /tasks/{id}/extractions/{id}/full-text
4. در endpoint GET /tasks/{id}/extractions/{id}/full-text، پس از بازگشت full_text، temp_path فایل اصلی را حذف کند (با استفاده از os.remove)
5. اطمینان از اینکه در boot_recover_stale_extractions در oversight_extraction.py خط 342-385، داده‌ها از دیتابیس خوانده شوند نه از JSON
6. مهاجرت داده‌های موجود از extractions.json به دیتابیس در startup

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 6: پیاده‌سازی Gemini Toggle Flow برای زمان عدم وجود مدل بصری فعال

**Scope:** این مرحله شامل تغییر endpoint /tasks/from-idea است به طوری که اگر upload_session_ids وجود داشته باشد و pick_best_extraction_model None برگرداند، response با status 'blocked_no_vision_model' و لیست candidates برگردانده شود. در frontend یک modal نمایش داده می‌شود با پیام «مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]». یک endpoint جدید POST /models/settings/{id}/temp-activate?trigger_task=... اضافه می‌شود که temporary_enabled=True را ست کرده و از طریق Telegram notification ارسال می‌کند. پس از اتمام extraction، temporary_enabled به False برگردانده شده و notification دوم ارسال می‌شود. خارج از این مرحله: logic استخراج، UI نمایش متن، یا Telegram media handlers. نکته حیاتی: این flow باید کاملاً end-to-end تست شود: Gemini off → modal → toggle → extraction → auto revert + 2 notif.
**Key terms:** /tasks/from-idea, pick_best_extraction_model, blocked_no_vision_model, POST /models/settings/{id}/temp-activate, temporary_enabled, Telegram notification, Gemini 2.5 Flash

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۶ — Gemini Toggle Flow
در /tasks/from-idea با upload_session_ids → اگر pick_best_extraction_model = None → return {"status": "blocked_no_vision_model", "candidates": [...]}
Frontend modal: "مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]"
backend POST /models/settings/{id}/temp-activate?trigger_task=... → temporary_enabled=True + notify telegram
post-extraction hook → revert + notify telegram
Validation: Gemini off → modal، toggle بزنی، extraction، خودکار revert + 2 notif
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی Gemini Toggle Flow برای زمان عدم وجود مدل بصری فعال در endpoint /tasks/from-idea

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک flow کامل به نام 'Gemini Toggle Flow' را داده است که در endpoint /tasks/from-idea اجرا می‌شود. منطق درخواست: اگر upload_session_ids در درخواست وجود داشته باشد و تابع pick_best_extraction_model مقدار None برگرداند (یعنی هیچ مدل بصری فعالی یافت نشد)، endpoint باید response با status 'blocked_no_vision_model' و لیست candidates (مدل‌های پیشنهادی) برگرداند. در frontend یک modal نمایش داده شود با پیام «مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]». یک endpoint جدید POST /models/settings/{id}/temp-activate?trigger_task=... در backend اضافه شود که temporary_enabled=True را روی مدل (مثلاً Gemini 2.5 Flash) ست کند و از طریق Telegram notification ارسال کند. پس از اتمام extraction (در یک post-extraction hook)، temporary_enabled به False برگردانده شود و notification دوم ارسال شود. خارج از این مرحله: logic استخراج، UI نمایش متن، یا Telegram media handlers. نکته حیاتی: این flow باید کاملاً end-to-end تست شود: Gemini off → modal → toggle → extraction → auto revert + 2 notif. کلیدواژه‌های اصلی: /tasks/from-idea, pick_best_extraction_model, blocked_no_vision_model, POST /models/settings/{id}/temp-activate, temporary_enabled, Telegram notification, Gemini 2.5 Flash. در کد فعلی، فایل backend/app/services/oversight_extraction.py احتمالاً حاوی تابع pick_best_extraction_model است (بر اساس نام فایل و deep context). فایل backend/app/api/routes/analysis.py شامل endpoint /tasks/from-idea است. فایل backend/app/services/notification_service.py سرویس ارسال نوتیفیکیشن تلگرام را مدیریت می‌کند. فایل backend/app/api/routes/models.py شامل endpointهای مدیریت مدل‌ها (احتمالاً /models/settings/{id}) است. فایل frontend/src/components/ExtractionDefaultPicker.tsx کامپوننت انتخاب مدل extraction در frontend است. فایل backend/app/services/oversight_extraction.py در خطوط مرتبط با pick_best_extraction_model باید بررسی شود. فایل backend/app/api/routes/analysis.py در خطوط مرتبط با /tasks/from-idea باید بررسی شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل backend/app/services/oversight_extraction.py، تابع pick_best_extraction_model را اصلاح کن تا وقتی هیچ مدل بصری فعالی یافت نشد، None برگرداند (هم‌اکنون احتمالاً exception می‌زند یا مدل پیش‌فرض برمی‌گرداند). 2. در فایل backend/app/api/routes/analysis.py، endpoint /tasks/from-idea را اصلاح کن: بعد از فراخوانی pick_best_extraction_model، اگر result None بود، response با status 'blocked_no_vision_model' و لیست candidates (مثلاً ['gemini-2.5-flash']) برگردان. 3. یک endpoint جدید POST /models/settings/{id}/temp-activate در فایل backend/app/api/routes/models.py اضافه کن که پارامتر query trigger_task را بگیرد، temporary_enabled=True را روی مدل (با id مشخص) ست کند، و از طریق backend/app/services/notification_service.py یک نوتیفیکیشن تلگرام با رویداد task_from_inspector یا مشابه ارسال کند. 4. در فایل backend/app/services/oversight_extraction.py، یک post-extraction hook (مثلاً تابع revert_temp_activation) اضافه کن که بعد از اتمام extraction، temporary_enabled را به False برگرداند و notification دوم ارسال کند. 5. در frontend، فایل frontend/src/components/ExtractionDefaultPicker.tsx را اصلاح کن: وقتی response با status 'blocked_no_vision_model' دریافت شد، یک modal با پیام «مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]» نمایش بده. toggle باید endpoint POST /models/settings/{id}/temp-activate?trigger_task={task_id} را صدا بزند. 6. در فایل frontend/src/app/analysis/page.tsx، منطق مدیریت response از /tasks/from-idea را به‌

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 7: اتصال idea_to_prompt با extraction و append متن کامل فایل‌ها

**Scope:** این مرحله شامل افزودن پارامتر جدید upload_session_ids به تابع idea_to_prompt است. این تابع منتظر می‌ماند تا همه extractions تمام شوند (هر کدام stream می‌کنند و در DB ذخیره می‌شوند). سپس متن کامل هر فایل به ترتیب file_order به idea append می‌شود با ساختار '## فایل #N: filename'. multi-pass plan هر فایل را به stage مرتبط ربط می‌دهد. اگر متن کل از 50KB بیشتر شود، به جای inline کردن، یک ref به extraction_id قرار می‌دهد. خارج از این مرحله: UI نمایش متن، Telegram integration، یا PDF generation. نکته حیاتی: با 3 فایل، پرامپت نهایی باید حداقل 3 stage داشته باشد و هر stage به فایل مربوطه ref دهد.
**Key terms:** idea_to_prompt, upload_session_ids, file_order, ## فایل #N: filename, multi-pass plan, extraction_id, 50KB

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۷ — Wiring idea_to_prompt با extraction
idea_to_prompt(idea, upload_session_ids=[...]) پارامتر جدید
await همهٔ extractions تمام شوند (هر کدام stream می‌کند، در DB)
متن کامل هر فایل به‌ترتیب file_order به idea append شود (ساختار ## فایل #N: filename)
multi-pass plan هر فایل را به stage مرتبط ربط دهد
اگر متن کل >50KB → ref به extraction_id نه inline
Validation: 3 فایل → پرامپت ≥3 stage، هر stage به فایل مربوطه ref
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال idea_to_prompt با سرویس extraction و append متن کامل فایل‌های پیوست تسک

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/creator_idea_to`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست feature با اولویت high داده است: «اتصال idea_to_prompt با extraction و append متن کامل فایل‌ها». این مرحله شامل افزودن پارامتر جدید `upload_session_ids` به تابع `idea_to_prompt` است. این تابع منتظر می‌ماند تا همه extractions تمام شوند (هر کدام stream می‌کنند و در DB ذخیره می‌شوند). سپس متن کامل هر فایل به ترتیب `file_order` به idea append می‌شود با ساختار `'## فایل #N: filename'`. multi-pass plan هر فایل را به stage مرتبط ربط می‌دهد. اگر متن کل از 50KB بیشتر شود، به جای inline کردن، یک ref به `extraction_id` قرار می‌دهد. خارج از این مرحله: UI نمایش متن، Telegram integration، یا PDF generation. نکته حیاتی: با 3 فایل، پرامپت نهایی باید حداقل 3 stage داشته باشد و هر stage به فایل مربوطه ref دهد. کلیدواژه‌ها: idea_to_prompt, upload_session_ids, file_order, ## فایل #N: filename, multi-pass plan, extraction_id, 50KB. شواهد در کد واقعی: فایل `backend/app/services/creator_idea_to_prompt.py` (deep-read شده) تابع `idea_to_prompt` را دارد که در حال حاضر پارامتر `upload_session_ids` را ندارد. فایل `backend/app/services/oversight_extraction.py` (deep-read شده) شامل کلاس `ExtractionRepo` (خط 199) و متدهای `list_by_session` (خط 324) و `full_text` (خط 327) است که برای دریافت متن کامل فایل‌ها استفاده می‌شود. همچنین `FileExtraction` (خط 174) دارای فیلدهای `file_order` (خط 178) و `full_text_cache` (خط 189) است. فایل `backend/app/services/oversight_upload_session.py` (deep-read شده) شامل `UploadSession` و `get_upload_session_service` است که برای مدیریت session‌های آپلود استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `backend/app/services/creator_idea_to_prompt.py`، امضای تابع `idea_to_prompt` را تغییر بده تا پارامتر جدید `upload_session_ids: Optional[List[str]] = None` را بپذیرد. 2. در بدنه تابع، اگر `upload_session_ids` داده شده باشد، یک حلقه روی هر session_id بزن و با `get_extraction_repo().list_by_session(session_id)` لیست `FileExtraction`ها را بگیر. 3. برای هر `FileExtraction`، با `repo.get_segments(extraction_id)` segmentها را بگیر و با `repo.full_text(extraction_id)` متن کامل را استخراج کن. 4. صبر کن تا همه extractions وضعیت `extracted` داشته باشند (با polling روی `repo.get(extraction_id).status` هر 2 ثانیه تا timeout 300 ثانیه). 5. متن کامل هر فایل را به ترتیب `file_order` به `idea` append کن با ساختار `'## فایل #N: filename\n{full_text}'`. 6. اگر طول متن کل (idea + append شده) از 50KB بیشتر شد، به جای inline کردن متن، یک ref به `extraction_id` قرار بده: `'## فایل #N: filename (متن کامل در extraction_id: {extraction_id})'`. 7. برای multi-pass plan، یک stage جدید به پرامپت اضافه کن که به هر فایل ref دهد: `'Stage {i}: تحلیل فایل {filename} با extraction_id {extraction_id}'`. 8. validation: اگر تعداد فایل‌ها >= 3 باشد، پرامپت نهایی باید حداقل 3 stage داشته باشد. 9. در فایل `backend/app/api/routes/oversight.py` (deep-read شده)، یک endpoint جدید یا پارامتر جدید به endpoint موجود اضافه کن تا `upload_session_ids` را از request دریافت کند و به `idea_to_prompt` پاس دهد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 8: ایجاد UI برای نمایش متن استخراج‌شده در TaskCard و modal

**Scope:** این مرحله شامل به‌روزرسانی TaskCard برای نمایش badge '📎 N فایل، M segment' است. یک modal یا صفحه جدید ایجاد می‌شود که لیست فایل‌ها را به ترتیب نمایش می‌دهد و per file segments به صورت collapsible با قابلیت search و copy نمایش داده می‌شود. خارج از این مرحله: backend extraction logic، Telegram integration، یا PDF generation. نکته حیاتی: نمایش باید order فایل‌ها را رعایت کند و قابلیت search و copy در segments فراهم باشد.
**Key terms:** TaskCard, 📎 N فایل، M segment, modal, collapsible, search, copy, file order

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۸ — UI: نمایش متن استخراج‌شده
TaskCard: badge "📎 N فایل، M segment"
modal/page: لیست فایل‌ها به‌ترتیب → per file segments collapsible → search + copy
Validation: نمایش، order، search، copy
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد UI برای نمایش متن استخراج‌شده در TaskCard و modal

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_extraction.py:174-192` — `FileExtraction` — این dataclass داده‌های استخراج فایل را ذخیره می‌کند. فیلدهای `file_order`، `total_segments` و `completed_segments` برای badge و order استفاده می‌شوند.
  ```python
  @dataclass
  class FileExtraction:
      id: str
      task_id: Optional[str]
      session_id: str
      file_order: int
      original_filename: str
      mime_type: str
      total_segments: int = 0
      completed_segments: int = 0
      status: str = "pending"
      model_used: str = ""
      started_at: str = field(default_factory=now_iso)
      finished_at: Optional[str] = None
      error: Optional[str] = None
      full_text_cache: str = ""
  ```
- `backend/app/services/oversight_extraction.py:155-167` — `Extraction`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد UI برای نمایش متن استخراج‌شده در TaskCard و modal را داده است. این درخواست شامل به‌روزرسانی TaskCard برای نمایش badge '📎 N فایل، M segment' است. یک modal یا صفحه جدید ایجاد می‌شود که لیست فایل‌ها را به ترتیب نمایش می‌دهد و per file segments به صورت collapsible با قابلیت search و copy نمایش داده می‌شود. خارج از این مرحله: backend extraction logic، Telegram integration، یا PDF generation. نکته حیاتی: نمایش باید order فایل‌ها را رعایت کند و قابلیت search و copy در segments فراهم باشد. کلیدواژه‌ها: TaskCard, 📎 N فایل، M segment, modal, collapsible, search, copy, file order. در کد فعلی، فایل `backend/app/services/oversight_extraction.py` شامل کلاس‌های `FileExtraction` (خط 174) و `ExtractionSegment` (خط 155) است که داده‌های استخراج را ذخیره می‌کنند. فایل `backend/app/api/routes/oversight.py` احتمالاً endpointهایی برای دریافت این داده‌ها دارد. در frontend، فایل‌های `frontend/src/app/oversight/page.tsx` و `frontend/src/components/InspectorBridge.tsx` میزبان کامپوننت‌های مرتبط هستند. همچنین فایل `frontend/src/lib/api.ts` و `frontend/src/services/api.ts` برای فراخوانی API استفاده می‌شوند. برای پیاده‌سازی، باید یک کامپوننت جدید `ExtractedTextViewer` ایجاد شود که داده‌های استخراج را از API دریافت کرده و در modal نمایش دهد.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد کامپوننت `frontend/src/components/ExtractedTextViewer.tsx` که داده‌های استخراج را از endpoint `GET /oversight/extractions/{task_id}` دریافت کند. 2. به‌روزرسانی `TaskCard` (احتمالاً در `frontend/src/app/oversight/page.tsx`) برای نمایش badge '📎 N فایل، M segment' با استفاده از داده‌های `FileExtraction` و `ExtractionSegment`. 3. ایجاد modal با استفاده از `react-modal` یا `dialog` HTML5 که لیست فایل‌ها را به ترتیب `file_order` نمایش دهد. 4. برای هر فایل، segments به صورت collapsible با `details/summary` یا state مدیریت‌شده نمایش داده شود. 5. افزودن قابلیت search با فیلتر کردن segments بر اساس متن. 6. افزودن دکمه copy برای هر segment که متن را به clipboard کپی کند. 7. اطمینان از رعایت order فایل‌ها با sort بر اساس `file_order` از `FileExtraction.file_order` (خط 178). 8. تست با داده‌های mock از `ExtractionRepo` (خط 199).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 9: پیاده‌سازی RAM/CPU Guardrails با Semaphore، Memory Watchdog و Timeout

**Scope:** این مرحله شامل افزودن Semaphore(1) سراسری برای extraction، chunk کردن ویدئو/صوت بزرگ با ffmpeg به قطعات 5 دقیقه‌ای و پردازش per chunk با Gemini، پیاده‌سازی memory watchdog که اگر RSS >80% باشد segment بعدی را به تأخیر می‌اندازد، timeout سخت 5 دقیقه per segment که در صورت تجاوز segment را failed mark کرده و ادامه می‌دهد، و boot recovery که extracting segment‌های با last_segment >5min را failed کرده و resume می‌کند. خارج از این مرحله: logic استخراج، UI، یا Telegram integration. نکته حیاتی: یک ویدئوی 500MB باید به 25 chunk تقسیم شود و RAM ثابت بماند؛ SIGKILL در میانه کار باید قابلیت resume داشته باشد.
**Key terms:** Semaphore(1), ffmpeg chunk, 5min, memory watchdog, RSS >80%, timeout 5min, boot recovery, SIGKILL

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۹ — RAM/CPU Guardrails
Semaphore(1) سراسری برای extraction
ویدئو/صوت بزرگ: ffmpeg chunk به 5min → per chunk Gemini
memory watchdog: اگر RSS >80% → delay segment بعدی
timeout سخت 5min per segment → mark failed → ادامه
boot recovery: extracting + last_segment >5min → failed → resume
Validation: 500MB video → 25 chunk، RAM ثابت؛ SIGKILL mid → resume
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی RAM/CPU Guardrails با Semaphore، Memory Watchdog و Timeout در سرویس استخراج فایل

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی RAM/CPU Guardrails با Semaphore، Memory Watchdog و Timeout را دارد. این درخواست شامل افزودن Semaphore(1) سراسری برای extraction، chunk کردن ویدئو/صوت بزرگ با ffmpeg به قطعات 5 دقیقه‌ای و پردازش per chunk با Gemini، پیاده‌سازی memory watchdog که اگر RSS >80% باشد segment بعدی را به تأخیر می‌اندازد، timeout سخت 5 دقیقه per segment که در صورت تجاوز segment را failed mark کرده و ادامه می‌دهد، و boot recovery که extracting segment‌های با last_segment >5min را failed کرده و resume می‌کند. خارج از این مرحله: logic استخراج، UI، یا Telegram integration. نکته حیاتی: یک ویدئوی 500MB باید به 25 chunk تقسیم شود و RAM ثابت بماند؛ SIGKILL در میانه کار باید قابلیت resume داشته باشد.

کلیدواژه‌های کاربر: Semaphore(1), ffmpeg chunk, 5min, memory watchdog, RSS >80%, timeout 5min, boot recovery, SIGKILL

شواهد در کد واقعی پروژه:
- فایل `backend/app/services/oversight_extraction.py` خط 67: `_EXTRACTION_SEMAPHORE = asyncio.Semaphore(1)` — Semaphore(1) سراسری از قبل وجود دارد اما در هیچ تابعی استفاده نشده است.
- فایل `backend/app/services/oversight_extraction.py` خط 70-91: تابع `_wait_for_memory_headroom` که اگر RAM سیستم بالای آستانه (MEMORY_THRESHOLD_PCT = 80.0 در خط 64) باشد تا max_wait صبر می‌کند — این memory watchdog است اما فقط در startup فراخوانی می‌شود.
- فایل `backend/app/services/oversight_extraction.py` خط 100-147: تابع `_ffmpeg_chunk_av` که audio/video را به chunkهای N ثانیه‌ای تقسیم می‌کند (AV_CHUNK_SECONDS = 300 در خط 63 = 5 دقیقه) — این chunk کردن با ffmpeg است.
- فایل `backend/app/services/oversight_extraction.py` خط 61: `PER_SEGMENT_TIMEOUT_SEC: int = 300` (5 دقیقه) — timeout سخت per segment.
- فایل `backend/app/services/oversight_extraction.py` خط 342-385: تابع `boot_recover_stale_extractions` که extractionهای stale (status='extracting' بدون فعالیت) را شناسایی و mark failed می‌کند — این boot recovery است اما cutoff 5 دقیقه دارد.

نقص فعلی: Semaphore(1) در خط 67 تعریف شده اما در هیچ تابع extraction (مانند `_ai_extract_text` در خط 396 یا `_plan_headings` در خط 461) استفاده نشده است. Memory watchdog (`_wait_for_memory_headroom`) فقط در startup فراخوانی می‌شود و نه قبل از هر segment. Timeout 5 دقیقه در `PER_SEGMENT_TIMEOUT_SEC` تعریف شده اما در `_ai_extract_text` پیاده‌سازی نشده است. Boot recovery در خط 342-385 وجود دارد اما باید segment‌های extracting با last_segment >5min را failed کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **فعال‌سازی Semaphore(1) در تابع اصلی extraction**: در فایل `backend/app/services/oversight_extraction.py`، تابع `_ai_extract_text` (خط 396) را با context manager `async with _EXTRACTION_SEMAPHORE:` بپیچید تا فقط یک extraction همزمان اجرا شود.

2. **اضافه کردن memory watchdog قبل از هر segment**: در تابع `_ai_extract_text` (خط 396)، قبل از فراخوانی `mgr.generate` (خط 452)، تابع `_wait_for_memory_headroom` را فراخوانی کنید تا اگر RSS >80% باشد، segment بعدی به تأخیر بیفتد.

3. **پیاده‌سازی timeout سخت 5 دقیقه per segment**: در تابع `_ai

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 10: Audit نهایی با Explore Agent و رفع Bug و Smoke Test سراسری

**Scope:** این مرحله شامل اجرای Explore agent برای بررسی race condition، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram است. تمام ایرادات یافت‌شده رفع می‌شوند. سپس یک smoke test سراسری اجرا می‌شود: full flow با 3 فایل → extract → idea_to_prompt → task → verify بدون خطا. خارج از این مرحله: feature جدید یا تغییرات معماری. نکته حیاتی: این مرحله نهایی است و باید تضمین کند که همه چیز backward-compatible است و featureهای موجود web نشکسته‌اند.
**Key terms:** Explore agent, race condition, file-handle leak, path traversal, SQL injection, orphan cleanup, regression, verify, checklist, telegram, smoke test

**بخش مربوط از متن کاربر:**
```
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent: race، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram
رفع همهٔ ایرادات پیدا‌شده
smoke test سراسری: full flow (3 files → extract → idea_to_prompt → task → verify) بدون خطا
```

## 🎯 هدف (خلاصه ساختاریافته)
اجرای Audit نهایی با Explore Agent و رفع Bug و Smoke Test سراسری

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_extraction.py:67-68` — `_EXTRACTION_SEMAPHORE` — این semaphore ممکن است race condition ایجاد کند زیرا فقط یک extraction همزمان مجاز است. باید بررسی شود که آیا این محدودیت باعث deadlock یا blocking می‌شود.
  ```python
  _EXTRACTION_SEMAPHORE = asyncio.Semaphore(1)
  ```
- `backend/app/services/oversight_extraction.py:233-246` — `ExtractionRepo._save`
  ```python
  def _save(self) -> None:
      try:
          data = {
              "extractions": [e.to_dict() for e in self._extractions.values()],
              "segments": {
                  ext_id: [s.to_dict() for s in segs]
                  for ext_id, segs in self._segments.items()
              },
          }
          tmp = EXTRACTIONS_FILE.with_suffix(".json.tmp")
          tmp.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
          tmp.replace(EXTRACTIONS_FILE)
      except Exception
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل اجرای Explore agent برای بررسی race condition، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram است. تمام ایرادات یافت‌شده رفع می‌شوند. سپس یک smoke test سراسری اجرا می‌شود: full flow با 3 فایل → extract → idea_to_prompt → task → verify بدون خطا. خارج از این مرحله: feature جدید یا تغییرات معماری. نکته حیاتی: این مرحله نهایی است و باید تضمین کند که همه چیز backward-compatible است و featureهای موجود web نشکسته‌اند.

بر اساس کد واقعی پروژه:
- در `backend/app/services/oversight_extraction.py` (خطوط 54-64) سقف‌های ایمنی مانند `MAX_PAGES_PER_PDF` و `SEGMENT_TEXT_MAX_CHARS` وجود دارد که ممکن است race condition در `_EXTRACTION_SEMAPHORE` (خط 67) ایجاد کند.
- در `backend/app/services/oversight_extraction.py` (خطوط 199-247) کلاس `ExtractionRepo` از JSON persistence استفاده می‌کند که ممکن است file-handle leak در `_save` (خط 233) داشته باشد.
- در `backend/app/services/oversight_extraction.py` (خط 461-497) تابع `_plan_headings` از user_idea برای تولید headings استفاده می‌کند که ممکن است SQL injection در heading رخ دهد.
- در `backend/app/services/oversight_extraction.py` (خط 70-91) تابع `_wait_for_memory_headroom` ممکن است orphan cleanup نداشته باشد.
- در `backend/app/services/oversight_telegram_compose.py` regression روی verify/checklist/telegram باید بررسی شود.
- در `backend/app/services/creator_idea_to_prompt.py` مسیر idea_to_prompt باید برای smoke test بدون خطا کار کند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. اجرای Explore agent روی `backend/app/services/oversight_extraction.py` برای یافتن race condition در `_EXTRACTION_SEMAPHORE` (خط 67) و file-handle leak در `ExtractionRepo._save` (خط 233).
2. بررسی path traversal در `_plan_headings` (خط 461) با user_idea.
3. بررسی SQL injection در heading از طریق `_plan_headings` (خط 461-497).
4. رفع orphan cleanup در `_wait_for_memory_headroom` (خط 70-91).
5. اجرای regression test روی `backend/app/services/oversight_telegram_compose.py` برای verify/checklist/telegram.
6. اجرای smoke test سراسری: full flow با 3 فایل → extract → idea_to_prompt → task → verify بدون خطا.
7. اطمینان از backward-compatibility و عدم شکستن featureهای موجود web.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: security
- اولویت: high
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: backend_test)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: backend_test)_

## Task Steps

### Step 1: افزودن وابستگی‌ها و گسترش ModelCapability Registry با picker helper
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن کتابخانه‌های pypdf, python-docx, openpyxl, Pillow, pydub به فایل requirements.txt و گسترش enum ModelCapability در models_registry.py با مقادیر جدید VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT است. همچنین یک تابع کمکی pick_best_extraction_model(mime_type) ایجاد می‌کند که بر اساس mime type، بهترین مدل فعال را با ranking برمی‌گرداند و یک mapping از mime به capability ایجاد می‌کند (مثلاً image/* → IMAGE_ANALYSIS). خارج از این مرحله: پیاده‌سازی منطق استخراج واقعی، تغییرات UI، یا endpoint‌های جدید. نکته حیاتی: این مرحله پایه‌ای است و باید کاملاً backward-compatible باشد تا featureهای موجود web نشکنند.
**Excerpt:**
```
مرحلهٔ ۱ — وابستگی‌ها + Capability Registry
افزودن pypdf>=4.0, python-docx>=1.1, openpyxl>=3.1, Pillow>=10, pydub>=0.25 به requirements
extend ModelCapability با VIDEO_UNDERSTANDING, AUDIO_UNDERSTANDING, DOCUMENT_UNDERSTANDING, LONG_CONTEXT
helper pick_best_extraction_model(mime_type) → بهترین مدل enabled با ranking
map mime → capability (image/* → IMAGE_ANALYSIS، video/* → VIDEO_UNDERSTANDING، …)
Validation: pytest روی picker + UI /models capabilities جدید را نشان دهد
```

### Step 2: پیاده‌سازی Backend Streaming و Resumable Upload با جدول UploadSession
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد جدول جدید UploadSession در دیتابیس با فیلدهای id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress و پیاده‌سازی چهار endpoint جدید است: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}. فایل‌ها به صورت streaming با aiofiles و chunks 5MB پردازش می‌شوند تا RAM ثابت بماند. همچنین یک cron job برای پاک‌سازی orphan session‌های قدیمی‌تر از 24 ساعت در boot اضافه می‌شود. خارج از این مرحله: frontend کامپوننت، extraction service، یا logic مربوط به idea_to_prompt. نکته حیاتی: این مرحله باید تضمین کند که آپلود یک فایل 500MB حافظه RAM را تا حدود 50MB نگه دارد نه 500MB و قابلیت resume از mid-crash را داشته باشد.
**Excerpt:**
```
مرحلهٔ ۲ — Backend: Streaming + Resumable Upload
table UploadSession (id, task_draft_id, filename, mime, total_size, bytes_received, status, temp_path, file_order, extraction_progress)
endpoints: POST /uploads/start, POST /uploads/{id}/chunk, POST /uploads/{id}/complete, GET /uploads/{id}, DELETE /uploads/{id}
aiofiles streaming → 5MB chunks → RAM ثابت
on boot: cleanup orphans >24h
Validation: upload 500MB، RSS RAM ~50MB بماند نه 500MB؛ crash midway → resume
```

### Step 3: ساخت کامپوننت Frontend Multi-file Drag-Drop با Chunked و Resumable Upload
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد کامپوننت جدید <TaskFilePicker /> در فرم ایده است که از multi-file drag-drop پشتیبانی می‌کند. فایل‌ها با ترتیب آپلود (file_order) ذخیره می‌شوند. یک progress bar per-file به همراه دکمه‌های retry و cancel نمایش داده می‌شود. از localStorage برای ذخیره session و قابلیت resume پس از reload استفاده می‌کند. chunk size در سمت کلاینت 5MB و parallel limit=2 است. خارج از این مرحله: backend endpoint‌ها، extraction service، یا logic مربوط به Telegram. نکته حیاتی: این کامپوننت باید با backend جدید (مرحله ۲) هماهنگ باشد و drag 3 فایل 100MB را بدون مشکل مدیریت کند.
**Excerpt:**
```
مرحلهٔ ۳ — Frontend: Multi-file Drag-Drop + Chunked + Resumable
کامپوننت <TaskFilePicker /> در form ایده
چند فایل، ترتیب آپلود = file_order
progress bar per file + retry/cancel
localStorage session → resume on reload
chunk client = 5MB، parallel limit=2
Validation: drag 3×100MB، refresh، resume درست
```

### Step 4: پیاده‌سازی Extraction Service هسته با routing per mime type
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد فایل file_extraction_service.py است که بر اساس mime type به سرویس‌های مختلف routing می‌کند: image/* → Gemini vision با full literal، application/pdf → pypdf برای text-pages + Gemini برای scan pages، xlsx/docx → openpyxl/docx برای metadata + Gemini برای interpretation، audio/* → Gemini transcription با full literal، video/* → frame-by-frame هر 2 ثانیه + audio parallel → Gemini. پرامپت استخراج بر اساس user_idea ابتدا headings/sections را می‌پرسد و سپس per heading متن کامل را extract می‌کند. دستور صریح «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی» در پرامپت گنجانده می‌شود. خروجی به صورت streaming per-segment yield شده و بلافاصله در DB ذخیره می‌شود. خارج از این مرحله: persistence layer، UI نمایش، یا Telegram integration. نکته حیاتی: این سرویس باید قابلیت resume از crash را داشته باشد و یک PDF 100 صفحه‌ای را به 100 segment تبدیل کند.
**Excerpt:**
```
مرحلهٔ ۴ — Extraction Service (هستهٔ feature)
file_extraction_service.py با routing per mime:
image/* → Gemini vision (full literal)
application/pdf → pypdf برای text-pages + Gemini برای صفحات scan/image
xlsx/docx → openpyxl/docx → metadata + Gemini برای interpretation
audio/* → Gemini transcription (full literal)
video/* → frame-by-frame هر 2s + audio parallel → Gemini
پرامپت استخراج: حسب user_idea، اول از مدل headings/sections بپرس → بعد per heading text کامل extract کن
بدون خلاصه‌سازی: instruction صریح: «هیچ‌چیز drop نکن، literal، حتی صحبت‌های اضافی»
streaming: per-segment yield → بلافاصله DB
Validation: 100-page PDF → 100 segment، crash mid → resume از segment آخر، نه از صفر
```

### Step 5: ایجاد Persistence Layer با جداول FileExtraction و ExtractionSegment
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد دو جدول جدید در دیتابیس است: FileExtraction با فیلدهای task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used و ExtractionSegment با فیلدهای extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used. همچنین یک repository به نام extraction_repo.py برای دسترسی به این داده‌ها ایجاد می‌شود. دو endpoint جدید GET /tasks/{id}/extractions و GET /tasks/{id}/extractions/{id}/full-text اضافه می‌شوند. پس از تکمیل extraction، temp_path فایل حذف می‌شود. خارج از این مرحله: logic استخراج، UI نمایش، یا Telegram integration. نکته حیاتی: پس از extraction، فایل اصلی نباید روی disk باقی بماند و restart سرور باید داده‌های موجود را حفظ کند.
**Excerpt:**
```
مرحلهٔ ۵ — Persistence Layer
table FileExtraction (task_id, session_id, file_order, filename, mime, total/completed_segments, status, model_used)
table ExtractionSegment (extraction_id, segment_index, title, text, raw_excerpt, page/timestamp, started/finished_at, model_used)
repo extraction_repo.py
GET /tasks/{id}/extractions, GET /tasks/{id}/extractions/{id}/full-text
پس از completed → حذف temp_path
Validation: post-extraction، file روی disk نیست؛ restart → existing data persisted
```

### Step 6: پیاده‌سازی Gemini Toggle Flow برای زمان عدم وجود مدل بصری فعال
**Status:** `done` (100%)
**Scope:** این مرحله شامل تغییر endpoint /tasks/from-idea است به طوری که اگر upload_session_ids وجود داشته باشد و pick_best_extraction_model None برگرداند، response با status 'blocked_no_vision_model' و لیست candidates برگردانده شود. در frontend یک modal نمایش داده می‌شود با پیام «مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]». یک endpoint جدید POST /models/settings/{id}/temp-activate?trigger_task=... اضافه می‌شود که temporary_enabled=True را ست کرده و از طریق Telegram notification ارسال می‌کند. پس از اتمام extraction، temporary_enabled به False برگردانده شده و notification دوم ارسال می‌شود. خارج از این مرحله: logic استخراج، UI نمایش متن، یا Telegram media handlers. نکته حیاتی: این flow باید کاملاً end-to-end تست شود: Gemini off → modal → toggle → extraction → auto revert + 2 notif.
**Excerpt:**
```
مرحلهٔ ۶ — Gemini Toggle Flow
در /tasks/from-idea با upload_session_ids → اگر pick_best_extraction_model = None → return {"status": "blocked_no_vision_model", "candidates": [...]}
Frontend modal: "مدل بصری فعال نیست — Gemini 2.5 Flash؟ [toggle]"
backend POST /models/settings/{id}/temp-activate?trigger_task=... → temporary_enabled=True + notify telegram
post-extraction hook → revert + notify telegram
Validation: Gemini off → modal، toggle بزنی، extraction، خودکار revert + 2 notif
```

### Step 7: اتصال idea_to_prompt با extraction و append متن کامل فایل‌ها
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن پارامتر جدید upload_session_ids به تابع idea_to_prompt است. این تابع منتظر می‌ماند تا همه extractions تمام شوند (هر کدام stream می‌کنند و در DB ذخیره می‌شوند). سپس متن کامل هر فایل به ترتیب file_order به idea append می‌شود با ساختار '## فایل #N: filename'. multi-pass plan هر فایل را به stage مرتبط ربط می‌دهد. اگر متن کل از 50KB بیشتر شود، به جای inline کردن، یک ref به extraction_id قرار می‌دهد. خارج از این مرحله: UI نمایش متن، Telegram integration، یا PDF generation. نکته حیاتی: با 3 فایل، پرامپت نهایی باید حداقل 3 stage داشته باشد و هر stage به فایل مربوطه ref دهد.
**Excerpt:**
```
مرحلهٔ ۷ — Wiring idea_to_prompt با extraction
idea_to_prompt(idea, upload_session_ids=[...]) پارامتر جدید
await همهٔ extractions تمام شوند (هر کدام stream می‌کند، در DB)
متن کامل هر فایل به‌ترتیب file_order به idea append شود (ساختار ## فایل #N: filename)
multi-pass plan هر فایل را به stage مرتبط ربط دهد
اگر متن کل >50KB → ref به extraction_id نه inline
Validation: 3 فایل → پرامپت ≥3 stage، هر stage به فایل مربوطه ref
```

### Step 8: ایجاد UI برای نمایش متن استخراج‌شده در TaskCard و modal
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی TaskCard برای نمایش badge '📎 N فایل، M segment' است. یک modal یا صفحه جدید ایجاد می‌شود که لیست فایل‌ها را به ترتیب نمایش می‌دهد و per file segments به صورت collapsible با قابلیت search و copy نمایش داده می‌شود. خارج از این مرحله: backend extraction logic، Telegram integration، یا PDF generation. نکته حیاتی: نمایش باید order فایل‌ها را رعایت کند و قابلیت search و copy در segments فراهم باشد.
**Excerpt:**
```
مرحلهٔ ۸ — UI: نمایش متن استخراج‌شده
TaskCard: badge "📎 N فایل، M segment"
modal/page: لیست فایل‌ها به‌ترتیب → per file segments collapsible → search + copy
Validation: نمایش، order، search، copy
```

### Step 9: پیاده‌سازی RAM/CPU Guardrails با Semaphore، Memory Watchdog و Timeout
**Status:** `done` (100%)
**Scope:** این مرحله شامل افزودن Semaphore(1) سراسری برای extraction، chunk کردن ویدئو/صوت بزرگ با ffmpeg به قطعات 5 دقیقه‌ای و پردازش per chunk با Gemini، پیاده‌سازی memory watchdog که اگر RSS >80% باشد segment بعدی را به تأخیر می‌اندازد، timeout سخت 5 دقیقه per segment که در صورت تجاوز segment را failed mark کرده و ادامه می‌دهد، و boot recovery که extracting segment‌های با last_segment >5min را failed کرده و resume می‌کند. خارج از این مرحله: logic استخراج، UI، یا Telegram integration. نکته حیاتی: یک ویدئوی 500MB باید به 25 chunk تقسیم شود و RAM ثابت بماند؛ SIGKILL در میانه کار باید قابلیت resume داشته باشد.
**Excerpt:**
```
مرحلهٔ ۹ — RAM/CPU Guardrails
Semaphore(1) سراسری برای extraction
ویدئو/صوت بزرگ: ffmpeg chunk به 5min → per chunk Gemini
memory watchdog: اگر RSS >80% → delay segment بعدی
timeout سخت 5min per segment → mark failed → ادامه
boot recovery: extracting + last_segment >5min → failed → resume
Validation: 500MB video → 25 chunk، RAM ثابت؛ SIGKILL mid → resume
```

### Step 10: Audit نهایی با Explore Agent و رفع Bug و Smoke Test سراسری
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای Explore agent برای بررسی race condition، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram است. تمام ایرادات یافت‌شده رفع می‌شوند. سپس یک smoke test سراسری اجرا می‌شود: full flow با 3 فایل → extract → idea_to_prompt → task → verify بدون خطا. خارج از این مرحله: feature جدید یا تغییرات معماری. نکته حیاتی: این مرحله نهایی است و باید تضمین کند که همه چیز backward-compatible است و featureهای موجود web نشکسته‌اند.
**Excerpt:**
```
مرحلهٔ ۱۰ — Audit نهایی + Bug-fix
Explore agent: race، file-handle leak، path traversal، SQL injection در heading، orphan cleanup، regression روی verify/checklist/telegram
رفع همهٔ ایرادات پیدا‌شده
smoke test سراسری: full flow (3 files → extract → idea_to_prompt → task → verify) بدون خطا
```
