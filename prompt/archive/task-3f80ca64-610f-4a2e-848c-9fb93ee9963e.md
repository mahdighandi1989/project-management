---
task_id: 3f80ca64-610f-4a2e-848c-9fb93ee9963e
title: '[منطق] عدم وجود hallucination guard در سطح pipeline'
type: logic_audit
priority: high
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T16:43:08.735041+00:00'
updated_at: '2026-06-02T17:48:01.575342+00:00'
archived: true
archived_at: '2026-05-18T04:27:44.357467+00:00'
tags:
- merged
---

# [منطق] عدم وجود hallucination guard در سطح pipeline

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها مکانیزمی برای تشخیص یا کاهش توهم (hallucination) مانند fact-checking, source grounding, یا confidence scoring ندارند. این در oversight که نیازمند دقت بالاست حیاتی است.

## 💥 پیامد (impact)
احتمال تولید پاسخ‌های نادرست یا گمراه‌کننده در تسک‌های حساس oversight

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guard در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها مکانیزمی برای تشخیص یا کاهش توهم (hallucination) مانند fact-checking, source grounding, یا confidence scoring ندارند. این در oversight که نیازمند دقت بالاست حیاتی است.

## 💥 پیامد (impact)
احتمال تولید پاسخ‌های نادرست یا گمراه‌کننده در تسک‌های حساس oversight

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت موجود pipeline ai_llm و شناسایی کامپوننت‌های فاقد hallucination guard
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن تمام فایل‌های مرتبط با pipeline ai_llm (شامل فایل‌های پیکربندی، ماژول‌های پردازش، endpointها و تست‌ها) است. هدف شناسایی دقیق کامپوننت‌هایی است که خروجی LLM تولید می‌کنند و فاقد هرگونه مکانیزم fact-checking, source grounding یا confidence scoring هستند. همچنین باید مشخص شود که context اصلی (target_locations, related_files) چگونه در pipeline جریان دارد. خارج از این مرحله: پیاده‌سازی هرگونه تغییر یا اصلاح کد. نکته حیاتی: این مرحله صرفاً تحلیلی و مستندسازی است و باید خروجی آن یک گزارش مکتوب (مثلاً در فایل docs/ یا issue) باشد که فرض‌های ناسازگار هر دو طرف (کامپوننت‌های بدون guard و نیازمندی‌های oversight) را ثبت کند.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها مکانیزمی برای تشخیص یا کاهش توهم (hallucination) مانند fact-checking, source grounding, یا confidence scoring ندارند. این در oversight که نیازمند دقت بالاست حیاتی است.

📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 2: طراحی و پیاده‌سازی ماژول HallucinationGuard با قابلیت fact-checking و source grounding
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول جدید (مثلاً hallucination_guard.py) در مسیر مناسب pipeline ai_llm است. این ماژول باید حداقل سه قابلیت اصلی داشته باشد: (1) fact-checking: تطبیق ادعاهای خروجی LLM با context اصلی (target_locations, related_files) با استفاده از روش‌های تطبیق متنی یا semantic similarity. (2) source grounding: اطمینان از اینکه هر بخش از خروجی به یک منبع مشخص در context اشاره دارد. (3) confidence scoring: اختصاص یک امتیاز اطمینان به کل خروجی یا بخش‌های آن بر اساس میزان تطابق با context. خروجی این ماژول باید یک ساختار داده (مثلاً dataclass) شامل: متن اصلی، لیست inconsistency‌ها (در صورت وجود)، و امتیاز اطمینان کلی باشد. خارج از این مرحله: یکپارچه‌سازی این ماژول با pipeline اصلی و نوشتن تست‌ها. نکته حیاتی: این ماژول باید مستقل و قابل تست باشد و به هیچ ماژول دیگری وابستگی نداشته باشد.
**Excerpt:**
```
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

🛠 پیشنهاد رفع اولیه
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند
```

### Step 3: یکپارچه‌سازی HallucinationGuard در pipeline ai_llm به عنوان یک stage پس از تولید خروجی LLM
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اصلاح pipeline ai_llm برای اضافه کردن HallucinationGuard به عنوان یک stage اجباری پس از stage تولید خروجی توسط LLM است. باید مشخص شود که context (target_locations, related_files) چگونه از stages قبلی به این stage جدید منتقل می‌شود. اگر context در pipeline موجود نیست، باید مکانیزمی برای جمع‌آوری و ارسال آن به HallucinationGuard اضافه شود. خروجی نهایی pipeline باید شامل گزارش HallucinationGuard باشد (نه فقط متن LLM). خارج از این مرحله: اصلاح UI برای نمایش گزارش، نوشتن تست‌های integration. نکته حیاتی: این یکپارچه‌سازی نباید pipeline موجود را بشکند و باید به صورت backward-compatible انجام شود (مثلاً با استفاده از feature flag یا پارامتر اختیاری).
**Excerpt:**
```
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 4: طراحی و پیاده‌سازی endpoint API برای دریافت گزارش HallucinationGuard به همراه خروجی LLM
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یا اصلاح یک endpoint API (احتمالاً در FastAPI) است که خروجی نهایی pipeline ai_llm (شامل متن و hallucination_report) را به صورت ساختاریافته بازمی‌گرداند. این endpoint باید بتواند hallucination_report را به صورت جداگانه یا همراه با متن اصلی برگرداند. همچنین باید قابلیت فیلتر کردن inconsistency‌ها بر اساس severity یا نوع را داشته باشد. خارج از این مرحله: اصلاح UI frontend (NextJS) برای نمایش این داده‌ها. نکته حیاتی: این endpoint باید با ساختار داده‌ای که HallucinationGuard تولید می‌کند سازگار باشد و از Pydantic models برای validation استفاده کند.
**Excerpt:**
```
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.
```

### Step 5: اصلاح UI (NextJS) برای نمایش گزارش HallucinationGuard در کنار خروجی LLM
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اصلاح کامپوننت‌های React در NextJS است که خروجی pipeline ai_llm را نمایش می‌دهند. باید یک پنل یا بخش جدید اضافه شود که hallucination_report را به صورت بصری نمایش دهد: (1) یک نشانگر confidence score (مثلاً به صورت نوار رنگی یا عدد). (2) لیست inconsistency‌ها با جزئیات (متن ناسازگار، منبع مورد انتظار، severity). (3) قابلیت expand/collapse برای جزئیات بیشتر. خارج از این مرحله: نوشتن تست‌های end-to-end. نکته حیاتی: UI باید به گونه‌ای طراحی شود که حتی اگر hallucination_report موجود نباشد (برای backward compatibility)، صفحه به درستی کار کند.
**Excerpt:**
```
اضافه کردن یک ماژول hallucination guard که خروجی را با context اصلی (target_locations, related_files) تطبیق دهد و inconsistencyها را گزارش کند

🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.
```

### Step 6: نوشتن unit tests برای ماژول HallucinationGuard
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن مجموعه‌ای از unit tests برای ماژول HallucinationGuard است که در مرحله 2 ساخته شد. تست‌ها باید پوشش دهند: (1) تشخیص inconsistency زمانی که خروجی LLM با context در تضاد است. (2) عدم تشخیص inconsistency زمانی که خروجی با context سازگار است. (3) محاسبه صحیح confidence score در سناریوهای مختلف. (4) رفتار با context خالی یا ناقص. (5) تست‌های edge cases مانند ورودی‌های بسیار طولانی یا تکراری. خارج از این مرحله: نوشتن integration tests یا end-to-end tests. نکته حیاتی: تست‌ها باید مستقل از pipeline و API باشند و فقط ماژول را تست کنند.
**Excerpt:**
```
✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد
```

### Step 7: نوشتن integration tests برای pipeline ai_llm با HallucinationGuard یکپارچه‌شده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن integration tests است که pipeline ai_llm را به صورت کامل (از ورودی تا خروجی) با HallucinationGuard یکپارچه‌شده تست می‌کند. تست‌ها باید: (1) یک سناریوی کامل را شبیه‌سازی کنند (شامل context و prompt). (2) بررسی کنند که خروجی نهایی شامل hallucination_report است. (3) بررسی کنند که pipeline بدون خطا اجرا می‌شود. (4) بررسی کنند که pipeline با HallucinationGuard غیرفعال (feature flag) همچنان کار می‌کند. خارج از این مرحله: تست‌های end-to-end با UI. نکته حیاتی: این تست‌ها باید از ماژول‌های واقعی (نه mock) استفاده کنند تا یکپارچگی واقعی را تأیید کنند.
**Excerpt:**
```
✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
```

### Step 8: نوشتن PR description جامع با توضیح ناسازگاری، راه‌حل، و checklist کامیت‌ها
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک PR description کامل است که: (1) ناسازگاری منطقی اولیه (عدم وجود hallucination guard) را شرح می‌دهد. (2) فرض‌های ناسازگار هر دو طرف (کامپوننت‌های بدون guard و نیازمندی‌های oversight) را مستند می‌کند. (3) ground truth تعیین‌شده (ضرورت وجود hallucination guard) را توضیح می‌دهد. (4) راه‌حل پیاده‌سازی‌شده (ماژول HallucinationGuard و یکپارچه‌سازی آن) را شرح می‌دهد. (5) یک checklist از تمام کامیت‌های انجام‌شده (مراحل 1 تا 7) به ترتیب منطقی ارائه می‌دهد. خارج از این مرحله: هیچ تغییری در کد ایجاد نمی‌شود. نکته حیاتی: PR description باید به اندازه کافی دقیق باشد تا یک reviewer بتواند بدون نیاز به خواندن کل کد، متوجه تغییرات شود.
**Excerpt:**
```
✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] PR description توضیح می‌دهد

🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```
