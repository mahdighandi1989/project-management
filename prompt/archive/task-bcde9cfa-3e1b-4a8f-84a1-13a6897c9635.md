---
task_id: bcde9cfa-3e1b-4a8f-84a1-13a6897c9635
title: فعال‌سازی توکن Cloud Code برای Claude در بازرس
type: other
priority: medium
execution_priority: 3000
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-06-02T20:42:03.982778+00:00'
updated_at: '2026-06-02T21:49:40.897755+00:00'
archived: true
archived_at: '2026-06-02T21:49:30.310903+00:00'
target_files:
- backend/app/core/config.py
- backend/app/services/claude_service.py
- backend/app/services/ai_manager.py
- backend/app/services/inspector_agent.py
- backend/app/api/routes/cloud_code_agents.py
- backend/app/services/cloud_code_agent_service.py
- backend/app/api/routes/external_prompts.py
- backend/app/services/oversight_service.py
- backend/app/api/routes/oversight.py
- backend/app/models/inspector_session.py
- frontend/src/app/oversight/page.tsx
- backend/tests/test_external_prompts_api.py
- README.md
- backend/.env.example
- .github/workflows/claude-auto-task.yml
---

# فعال‌سازی توکن Cloud Code برای Claude در بازرس

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_534004_AgADuiAA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 6,532 char متن_

## هدف و معرفی پروژه (Goal and Project Introduction)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
[01:00] و خب خیلی هم ضعیف انجام میشه به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] (صدای خش‌خش و نویز تا انتهای فایل)

## ویژگی‌های کلیدی اپلیکیشن (Key Application Features)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه هم استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
[01:00] و خب خیلی هم ضعیف انجام میشه به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متخلف فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه هم اون زمانشو فقط داره استفاده می‌کنه.
[02:00] یعنی لیمیت تایم فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کرده از این طریق باشه.
[02:10] (صدای خش‌خش و نویز)
[02:15] (صدای خش‌خش و نویز)
[02:20] (صدای خش‌خش و نویز)
[02:25] (صدای خش‌خش و نویز)
[02:30] (صدای خش‌خش و نویز)

## مخاطبان هدف و ارزش پیشنهادی (Target Audience and Value Proposition)  _(at: audio/ogg)_

--- صفحه ۱ ---
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای‌پی‌آی استفاده بکنه و فقط از اشتراک ماهانه‌م استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک‌فلو هم استفاده میشه.
[00:26] می‌خوام همین توکن به عنوان توی چت باز مثل توی تب باز مثل ویژه هم استفاده بشه. الان توی چت قسمت چت تب باز مثل ویژه خب یه سری مدل‌ها هست و اون آی‌فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات، اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام میدن.
[01:01] و خب خیلی هم ضعیف انجام میشه. به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متخف فعلی انجام میدن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمیدم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌م اون زمانشو فقط داره استفاده می‌کنه.
[01:59] یعنی لیمیتمو فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه. یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.
[02:22] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.
[02:28] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.

## چالش‌ها، ریسک‌ها و راهکارهای احتمالی (Potential Challenges, Risks, and Solutions)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای‌پی‌آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک‌فلو هم استفاده میشه.
[00:26] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی‌فریم هم هست که مثلاً ما اونجا از توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام میدن.
[01:01] و خب خیلی هم ضعیف انجام میشه بخاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقبه فعلی انجام میدن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمیدم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
[01:59] یعنی لیمیت هم فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] [صدای نویز]
[02:32] [صدای نویز]

## برنامه‌ریزی اولیه و مراحل توسعه (Initial Planning and Development Stages)  _(at: audio/ogg)_

[خطا: [gemini] ]

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_534004_AgADuiAA.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 6,532 char متن_

## هدف و معرفی پروژه (Goal and Project Introduction)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
[01:00] و خب خیلی هم ضعیف انجام میشه به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] (صدای خش‌خش و نویز تا انتهای فایل)

## ویژگی‌های کلیدی اپلیکیشن (Key Application Features)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه هم استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
[01:00] و خب خیلی هم ضعیف انجام میشه به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متخلف فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه هم اون زمانشو فقط داره استفاده می‌کنه.
[02:00] یعنی لیمیت تایم فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کرده از این طریق باشه.
[02:10] (صدای خش‌خش و نویز)
[02:15] (صدای خش‌خش و نویز)
[02:20] (صدای خش‌خش و نویز)
[02:25] (صدای خش‌خش و نویز)
[02:30] (صدای خش‌خش و نویز)

## مخاطبان هدف و ارزش پیشنهادی (Target Audience and Value Proposition)  _(at: audio/ogg)_

--- صفحه ۱ ---
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای‌پی‌آی استفاده بکنه و فقط از اشتراک ماهانه‌م استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک‌فلو هم استفاده میشه.
[00:26] می‌خوام همین توکن به عنوان توی چت باز مثل توی تب باز مثل ویژه هم استفاده بشه. الان توی چت قسمت چت تب باز مثل ویژه خب یه سری مدل‌ها هست و اون آی‌فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات، اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام میدن.
[01:01] و خب خیلی هم ضعیف انجام میشه. به خاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متخف فعلی انجام میدن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمیدم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌م اون زمانشو فقط داره استفاده می‌کنه.
[01:59] یعنی لیمیتمو فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه. یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.
[02:22] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.
[02:28] یعنی در واقع این توکن کلاد کد برای این منظور استفاده بشه.

## چالش‌ها، ریسک‌ها و راهکارهای احتمالی (Potential Challenges, Risks, and Solutions)  _(at: audio/ogg)_

[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای‌پی‌آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک‌فلو هم استفاده میشه.
[00:26] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی‌فریم هم هست که مثلاً ما اونجا از توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام میدن.
[01:01] و خب خیلی هم ضعیف انجام میشه بخاطر اینکه حالا هم مدل‌ها ضعیفه هم دسترسی‌ها درست تعریف نشده. ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارهای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقبه فعلی انجام میدن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمیدم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
[01:59] یعنی لیمیت هم فقط استفاده می‌کنه و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
[02:11] [صدای نویز]
[02:32] [صدای نویز]

## برنامه‌ریزی اولیه و مراحل توسعه (Initial Planning and Development Stages)  _(at: audio/ogg)_

[خطا: [gemini] ]
```

## 📋 چک‌لیست مراحل (9 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: طراحی و پیاده‌سازی لایه دسترسی به توکن Cloud Code** — این مرحله شامل طراحی و پیاده‌سازی یک ماژول یا سرویس بک‌اند اختصاصی است که مسئولیت مدیریت و استفاده از توکن Cloud Code موجود را بر عهده دارد. هدف این است که دسترسی به توکن و مکانیزم‌های احراز هویت مربوط به Cloud Code به صورت متمرکز و امن انجام شود و سایر سرویس‌ها بتوانند از طریق این لایه با Cloud Cod
- [x] **مرحله 2: توسعه سرویس تعامل با ایجنت‌های Cloud Code** — در این مرحله، یک سرویس بک‌اند جدید توسعه می‌یابد که مسئولیت اصلی آن تعامل با ایجنت‌های Cloud Code است. این سرویس پرامپت‌های دریافتی را ساختارمند کرده و برای پردازش و اجرای کارها به ایجنت‌های Cloud Code ارسال می‌کند. این سرویس از لایه دسترسی به توکن (مرحله ۱) برای احراز هویت استفاده خواهد کرد و منطق 
- [x] **مرحله 3: پیاده‌سازی Endpoint جدید برای پردازش چت با Cloud Code** — این مرحله شامل ایجاد یک یا چند Endpoint جدید در API بک‌اند چت است که به طور خاص برای دریافت درخواست‌های پردازش پرامپت توسط Cloud Code طراحی شده‌اند. این Endpointها درخواست‌ها را از فرانت‌اند دریافت کرده و آن‌ها را به سرویس تعامل با ایجنت‌های Cloud Code (مرحله ۲) ارجاع می‌دهند. این Endpointها باید از
- [x] **مرحله 4: اصلاح منطق مسیریابی درخواست‌ها در بک‌اند چت** — در این مرحله، منطق موجود در بک‌اند سیستم چت بازرس ویژه اصلاح می‌شود تا بتواند بر اساس انتخاب کاربر (Cloud Code یا مدل‌های فعلی) درخواست‌های ورودی را به درستی مسیریابی کند. این شامل افزودن یک شرط به منطق پردازش درخواست است که اگر گزینه Cloud Code انتخاب شده باشد، درخواست به Endpoint جدید (مرحله ۳) هد
- [x] **مرحله 5: افزودن گزینه انتخاب Cloud Code به رابط کاربری تب بازرس ویژه** — این مرحله شامل توسعه و ادغام یک عنصر رابط کاربری جدید (مانند دکمه رادیویی، منوی کشویی یا سوئیچ) در 'تب بازرس ویژه' است. این عنصر به کاربر اجازه می‌دهد تا 'Cloud Code' را به عنوان موتور پردازش پرامپت خود انتخاب کند. طراحی این عنصر باید با ظاهر و احساس کلی رابط کاربری موجود سازگار باشد.
- [x] **مرحله 6: به‌روزرسانی منطق ارسال درخواست چت در فرانت‌اند** — در این مرحله، منطق ارسال درخواست چت در فرانت‌اند 'تب بازرس ویژه' اصلاح می‌شود. این اصلاح شامل افزودن پارامتری به درخواست‌های ارسالی به بک‌اند است که نشان‌دهنده انتخاب کاربر (Cloud Code یا مدل‌های فعلی) باشد. همچنین، در صورت انتخاب Cloud Code، درخواست باید به Endpoint جدید (مرحله ۳) ارسال شود، در حال
- [x] **مرحله 7: تست‌های واحد و یکپارچه‌سازی بک‌اند** — این مرحله شامل نوشتن و اجرای تست‌های واحد (Unit Tests) برای ماژول دسترسی به توکن Cloud Code، سرویس تعامل با ایجنت‌های Cloud Code و Endpointهای جدید API است. همچنین، تست‌های یکپارچه‌سازی (Integration Tests) برای تأیید عملکرد صحیح مسیریابی درخواست‌ها در بک‌اند و ارتباط بین سرویس‌های مختلف انجام می‌شود
- [x] **مرحله 8: تست‌های رابط کاربری و End-to-End** — این مرحله شامل توسعه و اجرای تست‌های رابط کاربری (UI Tests) برای تأیید نمایش صحیح و عملکرد عنصر انتخاب Cloud Code در 'تب بازرس ویژه' است. علاوه بر این، تست‌های End-to-End (E2E Tests) برای شبیه‌سازی کامل جریان کاربر از انتخاب Cloud Code، ارسال پرامپت و دریافت پاسخ از طریق سیستم کامل (فرانت‌اند و بک‌ا
- [x] **مرحله 9: مستندسازی و آماده‌سازی برای استقرار** — این مرحله شامل به‌روزرسانی مستندات فنی مربوط به معماری جدید، APIهای اضافه شده، و نحوه پیکربندی و استقرار سرویس Cloud Code است. همچنین، شامل آماده‌سازی محیط‌های استقرار (مانند تنظیم متغیرهای محیطی، مدیریت دسترسی‌ها و پیکربندی‌های لازم) برای انتقال کد به محیط‌های تست و تولید است. هدف اطمینان از قابلیت

---

# 🔹 مرحله 1: طراحی و پیاده‌سازی لایه دسترسی به توکن Cloud Code

**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک ماژول یا سرویس بک‌اند اختصاصی است که مسئولیت مدیریت و استفاده از توکن Cloud Code موجود را بر عهده دارد. هدف این است که دسترسی به توکن و مکانیزم‌های احراز هویت مربوط به Cloud Code به صورت متمرکز و امن انجام شود و سایر سرویس‌ها بتوانند از طریق این لایه با Cloud Code ارتباط برقرار کنند. این مرحله شامل منطق پردازش پرامپت یا رابط کاربری نیست.
**Key terms:** توکن کلاد کد, اشتراک ماهانه, مرکز نظارت, ورک فلو, چت بازرس, تب بازرس ویژه

**بخش مربوط از متن کاربر:**
```
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه. [00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن پشتیبانی از توکن Cloud Code به چت بازرس

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/core/config.py` — `Settings` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این snippet یک مثال مفهومی برای اضافه کردن `EXTERNAL_TOOL_TOKEN` به تنظیمات است.
  ```python
  class Settings(BaseSettings):
      # ... existing settings ...
      EXTERNAL_TOOL_TOKEN: Optional[str] = None # New setting
  
  settings = Settings()
  ```
- `backend/app/services/claude_service.py` — `ClaudeService` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این snippet یک مثال مفهومی برای تغییر `__init__` و متد `generate_text` در `ClaudeService` است.
  ```python
  class ClaudeService(AIBaseService):
      def __init__(self, api_key: str, external_token: Optional[str] = None):
          self.api_key = api_key
          self.external_token = external_token
          # ... other initializations ...
  
      async def generate_text(self, prompt: str, model: str) -> str:
          token_to_use = self.external_token if self.external_token else self.api_key
          # ... use token_to_use for actual API call ...
  ```
- `backend/app/services/ai_manager.py` — `AIManager` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این snippet یک مثال مفهومی برای تغییر متد `get_service` در `AIManager` است.
  ```python
  from ...core.config import settings
  
  class AIManager:
      # ... existing methods ...
  
      def get_service(self, model_id: str, use_external_token_for_claude: bool = False):
          if "claude" in model_id:
              if use_external_token_for_claude and settings.EXTERNAL_TOOL_TOKEN:
                  return ClaudeService(
                      api_key=settings.CLAUDE_API_KEY,
                      external_token=settings.EXTERNAL_TOOL_TOKEN
                  )
              return ClaudeService(api_key=settings.CLAUDE_API_KEY)
          # ... existing logic for other models ...
  ```
- `backend/app/services/inspector_agent.py` — `InspectorAgent` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این snippet یک مثال مفهومی برای نحوهٔ فراخوانی `ai_manager` با توکن خارجی در `InspectorAgent` است.
  ```python
  from ...core.config import settings
  from ...services.ai_manager import get_ai_manager
  
  class InspectorAgent:
      # ... existing methods ...
  
      async def process_inspector_query(self, query: str, project_id: str):
          ai_manager = get_ai_manager()
          # Logic to determine if Claude is needed and if external token should be used
          if settings.EXTERNAL_TOOL_TOKEN: # Example condition
              response = await ai_manager.generate_text(
                  prompt=query, model="claude-opus", use_external_token_for_claude=True
              )
          else:
              response = await ai_manager.generate_text(prompt=query, model="claude-opus")
          return response
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: `fastapi`, `nextjs`. کتابخانه‌های مرتبط در بک‌اند شامل `httpx` (برای فراخوانی‌های HTTP به APIهای AI), `pydantic` (برای مدل‌های داده و تنظیمات), و `os` (برای دسترسی به متغیرهای محیطی) هستند. استفاده از `Optional[str]` از ماژول `typing` برای مدیریت توکن‌های اختیاری نیز ضروری است.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/external_prompts.py` (سطر 91) — این فایل در حال حاضر از `EXTERNAL_TOOL_TOKEN` برای احراز هویت ابزارهای خارجی استفاده می‌کند و منبع اصلی این توکن در بک‌اند است. تغییرات باید با نحوهٔ مدیریت توکن در اینجا سازگار باشد.
- `backend/app/api/routes/oversight.py` (سطر -1) — این روتر احتمالاً نقطهٔ ورود برای درخواست‌های مربوط به "تب بازرس ویژه" است و باید بتواند context استفاده از توکن خارجی را به سرویس‌های پایین‌دستی (مانند `inspector_agent.py`) منتقل کند.
- `backend/app/models/inspector_session.py` (سطر -1) — مدل‌های مربوط به Inspector Session ممکن است نیاز به ذخیره یا ارجاع به نوع توکن مورد استفاده داشته باشند، یا حداقل برای درک context Inspector Chat مهم است.
- `backend/app/services/notification_service.py` (سطر -1) — تابع `_emit_runner_notification` در `external_prompts.py` (خطوط 35-82) از این سرویس استفاده می‌کند. اگر استفاده از توکن خارجی در Inspector Chat منجر به رویدادهای مشابه (مانند شروع/پایان یک عملیات) شود، این سرویس ممکن است نیاز به به‌روزرسانی داشته باشد.
- `backend/app/services/claude_runner_bootstrap.py` (سطر -1) — این سرویس مربوط به راه‌اندازی Claude Runner است و ممکن است در آینده نیاز به هماهنگی با نحوهٔ استفاده از توکن در Inspector Chat داشته باشد، به خصوص اگر منطق مشترکی برای مدیریت توکن‌ها توسعه یابد.

## 🌐 نقشهٔ وابستگی‌ها
این تغییرات بر روی لایهٔ سرویس‌های هوش مصنوعی و سرویس‌های بازرس تأثیر می‌گذارد. `backend/app/services/inspector_agent.py` که هستهٔ منطق "چت بازرس" است، به `backend/app/services/ai_manager.py` وابسته است تا بتواند مدل‌های AI را فراخوانی کند. `ai_manager.py` نیز به نوبهٔ خود به پیاده‌سازی‌های خاص پروایدرها مانند `backend/app/services/claude_service.py` وابسته است تا درخواست‌ها را به APIهای مربوطه ارسال کند. `backend/app/core/config.py` منبع اصلی برای بارگذاری `EXTERNAL_TOOL_TOKEN` از متغیرهای محیطی خواهد بود و اطمینان از دسترسی امن و متمرکز به این توکن را فراهم می‌کند. `backend/app/api/routes/oversight.py` به عنوان نقطهٔ ورود API برای Inspector، این جریان را آغاز می‌کند و باید بتواند پارامترهای لازم را به سرویس‌های پایین‌دستی منتقل کند. این وابستگی‌ها نشان می‌دهند که تغییر در یک لایه، نیازمند هماهنگی و به‌روزرسانی در لایه‌های دیگر است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که "توکن کلاد کد" که در حال حاضر توسط "مرکز نظارت" و "ورک فلو" (مانند `claude-auto-task.yml` در GitHub Actions) استفاده می‌شود، به "چت بازرس" در "تب بازرس ویژه" نیز گسترش یابد. هدف این است که "چت بازرس" بتواند از "اشتراک ماهانه" Cloud Code بهره‌مند شود و ارتباط با Cloud Code را از طریق یک لایه متمرکز و امن مدیریت کند، بدون اینکه نیاز به استفاده مستقیم از APIهای عمومی باشد. این پیاده‌سازی باید بر روی ماژول‌ها و سرویس‌های بک‌اند متمرکز باشد و شامل منطق پردازش پرامپت یا رابط کاربری نباشد.

در حال حاضر، `backend/app/api/routes/external_prompts.py` از `EXTERNAL_TOOL_TOKEN` (که احتمالاً همان `CLAUDE_CODE_OAUTH_TOKEN` در ورک‌فلو است) برای احراز هویت ابزارهای خارجی استفاده می‌کند. این توکن از متغیر محیطی `EXTERNAL_TOOL_TOKEN` بارگذاری می‌شود (خطوط 91-99 در `backend/app/api/routes/external_prompts.py`): `expected = os.environ.get("EXTERNAL_TOOL_TOKEN", "").strip()`. سرویس `backend/app/services/oversight_service.py` نیز در مدیریت تسک‌های مربوط به این ورک‌فلو نقش دارد، به طوری که `external_prompts.py` آن را در خط 21 ایمپورت می‌کند: `from ...services.oversight_service import get_oversight_service, now_iso`.

برای تحقق این درخواست، نیاز است که سرویس `inspector_agent.py` که مسئول منطق "چت بازرس" است، بتواند از این توکن برای فراخوانی مدل‌های Claude استفاده کند. این امر مستلزم تغییراتی در `claude_service.py` برای پذیرش یک توکن جایگزین و همچنین در `ai_manager.py` برای هدایت درخواست‌های مربوط به بازرس به `ClaudeService` با استفاده از `EXTERNAL_TOOL_TOKEN` است. این رویکرد تضمین می‌کند که "چت بازرس" می‌تواند از قابلیت‌های Cloud Code با استفاده از مکانیزم احراز هویت موجود و امن بهره‌مند شود، همانطور که در "مرکز نظارت" و "ورک فلو" پیاده‌سازی شده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فیلد `EXTERNAL_TOOL_TOKEN` باید به کلاس `Settings` در `backend/app/core/config.py` اضافه شود و از متغیر محیطی بارگذاری شود.
- [ ] کلاس `ClaudeService` در `backend/app/services/claude_service.py` باید پارامتر `external_token: Optional[str]` را در `__init__` خود بپذیرد و از آن برای احراز هویت در فراخوانی‌های API استفاده کند.
- [ ] متد `get_service` (یا مشابه آن) در `backend/app/services/ai_manager.py` باید یک پارامتر `use_external_token_for_claude: bool` داشته باشد و در صورت `True` بودن، `EXTERNAL_TOOL_TOKEN` را به `ClaudeService` ارسال کند.
- [ ] سرویس `inspector_agent.py` باید `ai_manager` را با پرچم `use_external_token_for_claude=True` فراخوانی کند تا از توکن Cloud Code برای مدل‌های Claude استفاده شود.
- [ ] یک درخواست API به endpoint مربوط به چت بازرس (مثلاً `/api/oversight/inspector/chat`) که از توکن Cloud Code استفاده می‌کند، باید با موفقیت پاسخ AI را دریافت کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی پشتیبانی از توکن Cloud Code در چت بازرس، مراحل زیر پیشنهاد می‌شود:

1.  **به‌روزرسانی `backend/app/core/config.py`**: یک فیلد جدید `EXTERNAL_TOOL_TOKEN` از نوع `Optional[str]` به کلاس `Settings` اضافه کنید تا این توکن از متغیرهای محیطی بارگذاری شده و در سراسر برنامه قابل دسترسی باشد. این کار باعث می‌شود که توکن به صورت متمرکز مدیریت شود.

2.  **اصلاح `backend/app/services/claude_service.py`**: متد `__init__` کلاس `ClaudeService` را به‌روزرسانی کنید تا یک پارامتر اختیاری `external_token: Optional[str] = None` را بپذیرد. سپس، متدهای داخلی که فراخوانی‌های API به Claude را انجام می‌دهند (مانند `generate_text` یا `_make_api_call`) را تغییر دهید تا در صورت وجود `external_token`، از آن برای احراز هویت استفاده کنند؛ در غیر این صورت، به `CLAUDE_API_KEY` پیش‌فرض بازگردند. این تغییر انعطاف‌پذیری لازم برای استفاده از توکن‌های مختلف را فراهم می‌کند.

3.  **اصلاح `backend/app/services/ai_manager.py`**: متد `get_service` یا `generate_text` را به‌روزرسانی کنید تا یک پارامتر اختیاری `use_external_token_for_claude: bool = False` را بپذیرد. اگر `use_external_token_for_claude` برابر `True` باشد و مدل درخواستی از نوع Claude باشد، `EXTERNAL_TOOL_TOKEN` را از `config.settings` دریافت کرده و هنگام ساخت یا فراخوانی `ClaudeService`، آن را به عنوان `external_token` ارسال کند. این لایه مسئول تصمیم‌گیری در مورد استفاده از توکن مناسب است.

4.  **اصلاح `backend/app/services/inspector_agent.py`**: در هر جایی که `inspector_agent` نیاز به فراخوانی مدل‌های AI (به‌ویژه Claude) برای پردازش درخواست‌های "چت بازرس" دارد، `EXTERNAL_TOOL_TOKEN` را از `config.settings` بازیابی کرده و آن را از طریق `ai_manager` به `ClaudeService` ارسال کند. این نقطهٔ مصرف‌کنندهٔ اصلی توکن برای قابلیت جدید است.

5.  **بررسی `backend/app/api/routes/oversight.py`**: اطمینان حاصل شود که هر endpoint مرتبط با "چت بازرس" (که احتمالاً در این روتر قرار دارد) می‌تواند یک سیگنال (مثلاً یک هدر سفارشی مانند `X-Inspector-Use-External-Token` یا یک پارامتر در بدنه درخواست) را به `inspector_agent` ارسال کند تا از `EXTERNAL_TOOL_TOKEN` استفاده شود. این کار امکان کنترل از سمت فرانت‌اند را فراهم می‌کند.

## 💡 نمونه‌های قبل/بعد
**`backend/app/core/config.py` - اضافه کردن `EXTERNAL_TOOL_TOKEN`**

_قبل:_
```
class Settings(BaseSettings):
    OPENAI_API_KEY: Optional[str] = None
    CLAUDE_API_KEY: Optional[str] = None
```

_بعد:_
```
class Settings(BaseSettings):
    OPENAI_API_KEY: Optional[str] = None
    CLAUDE_API_KEY: Optional[str] = None
    EXTERNAL_TOOL_TOKEN: Optional[str] = None # New field for Cloud Code token
```

**`backend/app/services/claude_service.py` - `__init__` و استفاده از توکن**

_قبل:_
```
class ClaudeService(AIBaseService):
    def __init__(self, api_key: str):
        self.api_key = api_key

    async def _make_api_call(self, payload: dict) -> dict:
        headers = {"Authorization": f"Bearer {self.api_key}"}
        # ... API call logic ...
```

_بعد:_
```
class ClaudeService(AIBaseService):
    def __init__(self, api_key: str, external_token: Optional[str] = None):
        self.api_key = api_key
        self.external_token = external_token # New field

    async def _make_api_call(self, payload: dict) -> dict:
        token_to_use = self.external_token if self.external_token else self.api_key
        headers = {"Authorization": f"Bearer {token_to_use}"}
        # ... API call logic ...
```

**`backend/app/services/ai_manager.py` - منطق انتخاب توکن در `get_service`**

_قبل:_
```
from ...core.config import settings

class AIManager:
    def get_service(self, model_id: str):
        if "claude" in model_id:
            return ClaudeService(api_key=settings.CLAUDE_API_KEY)
        # ...
```

_بعد:_
```
from ...core.config import settings

class AIManager:
    def get_service(self, model_id: str, use_external_token_for_claude: bool = False):
        if "claude" in model_id:
            if use_external_token_for_claude and settings.EXTERNAL_TOOL_TOKEN:
                return ClaudeService(
                    api_key=settings.CLAUDE_API_KEY, # Fallback or default
                    external_token=settings.EXTERNAL_TOOL_TOKEN
                )
            return ClaudeService(api_key=settings.CLAUDE_API_KEY)
        # ...
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/`
- `uvicorn app.main:app --reload --port 8000`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **ریسک امنیتی افشای توکن:** `EXTERNAL_TOOL_TOKEN` یک توکن حساس است. اگر در `backend/app/core/config.py` به درستی مدیریت نشود یا به صورت ناامن در لاگ‌ها یا پاسخ‌های API ظاهر شود، می‌تواند منجر به افشای اطلاعات شود. باید اطمینان حاصل شود که این توکن فقط در لایه‌های داخلی و برای احراز هویت استفاده می‌شود.
2.  **تأثیر بر عملکرد `external_prompts.py`:** تغییرات در نحوهٔ بارگذاری یا استفاده از `EXTERNAL_TOOL_TOKEN` در `backend/app/core/config.py` یا `backend/app/services/ai_manager.py` نباید بر عملکرد فعلی `backend/app/api/routes/external_prompts.py` که از این توکن برای ورک‌فلوها استفاده می‌کند، تأثیر منفی بگذارد. باید سازگاری رو به عقب حفظ شود.
3.  **خطای احراز هویت:** اگر `inspector_agent.py` به اشتباه توکن را ارسال کند یا `ai_manager.py` آن را به اشتباه به سرویس‌های AI دیگر (غیر از Claude) ارسال کند، ممکن است منجر به خطاهای احراز هویت و عدم کارکرد صحیح سیستم شود. تست‌های دقیق برای اطمینان از ارسال صحیح توکن ضروری است.
4.  **پیچیدگی مدیریت مدل:** اضافه شدن منطق شرطی برای انتخاب توکن در `backend/app/services/ai_manager.py` می‌تواند پیچیدگی این سرویس را افزایش دهد و نگهداری آن را دشوارتر کند، به خصوص اگر تعداد پروایدرها و انواع توکن‌ها در آینده افزایش یابد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: توسعه سرویس تعامل با ایجنت‌های Cloud Code

**Scope:** در این مرحله، یک سرویس بک‌اند جدید توسعه می‌یابد که مسئولیت اصلی آن تعامل با ایجنت‌های Cloud Code است. این سرویس پرامپت‌های دریافتی را ساختارمند کرده و برای پردازش و اجرای کارها به ایجنت‌های Cloud Code ارسال می‌کند. این سرویس از لایه دسترسی به توکن (مرحله ۱) برای احراز هویت استفاده خواهد کرد و منطق اصلی پردازش پرامپت توسط Cloud Code را در خود جای می‌دهد.
**Key terms:** ایجنت‌های کلاد کد, ساختارمند کردن پرامپت, کارای اجرایی, اجرای پرامپت, مدل‌های متقاعد فعلی

**بخش مربوط از متن کاربر:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه. [01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده.
```

## 🎯 هدف (خلاصه ساختاریافته)
توسعه سرویس تعامل با ایجنت‌های Cloud Code

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/cloud_code_agents.py` — `router` — این فایل جدید برای تعریف مسیرهای API مربوط به تعامل با ایجنت‌های Cloud Code ایجاد خواهد شد. ساختار آن مشابه `external_prompts.py` خواهد بود.
- `backend/app/services/cloud_code_agent_service.py` — `CloudCodeAgentService` — این فایل جدید شامل منطق کسب‌وکار برای تعامل با Cloud Code API خارجی، ساختارمند کردن پرامپت‌ها و مدیریت وضعیت تسک‌ها خواهد بود.
- `backend/app/api/routes/external_prompts.py:103-106` — `require_external_token` — این تابع برای احراز هویت درخواست‌های ورودی از Cloud Code agent در مسیرهای جدید استفاده خواهد شد.
  ```python
  def require_external_token(
      x_external_token: Optional[str] = Header(default=None, alias="X-External-Token"),
  ) -> None:
      """FastAPI dependency برای endpoint های ابزار خارجی."""
      _check_external_token(x_external_token)
  ```
- `backend/app/services/oversight_service.py` — `OversightService` — این سرویس نیاز به بررسی و احتمالاً به‌روزرسانی دارد تا بتواند تسک‌ها را برای Cloud Code agentها مدیریت کند و وضعیت `external_status` و `external_locked_by` را به‌درستی به‌روزرسانی کند. ممکن است نیاز به افزودن فیلدی برای تمایز نوع agent باشد.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی‌شده: `fastapi, nextjs`. کتابخانه‌های مرتبط: `fastapi` برای تعریف مسیرها و مدل‌های Pydantic، `httpx` یا `aiohttp` برای فراخوانی API خارجی Cloud Code، `sqlalchemy` برای تعامل با دیتابیس.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/main.py` — مسیرهای API جدید (`cloud_code_agents.py`) باید در برنامه اصلی FastAPI ثبت شوند.
- `backend/app/services/ai_manager.py` — سرویس جدید ممکن است برای ساختارمند کردن پرامپت‌ها یا استفاده از قابلیت‌های 'مدل‌های متقاعد فعلی' به `ai_manager` نیاز داشته باشد.
- `backend/app/core/database.py` — هر سرویس یا مدل جدیدی که نیاز به ذخیره‌سازی داده دارد، با دیتابیس تعامل خواهد داشت.
- `backend/.env.example` — متغیر محیطی `EXTERNAL_TOOL_TOKEN` برای احراز هویت Cloud Code agent ضروری است و باید به‌درستی پیکربندی شود.
- `backend/app/models/inspector_session.py` — تسک‌های مدیریت شده توسط `oversight_service` اغلب به پروژه‌ها یا جلسات بازرسی مرتبط هستند. تسک‌های Cloud Code نیز احتمالاً در این چارچوب عمل خواهند کرد.

## 🌐 نقشهٔ وابستگی‌ها
این سرویس جدید به شدت به `backend/app/services/oversight_service.py` وابسته خواهد بود تا بتواند تسک‌ها را از سیستم مدیریت تسک مرکزی دریافت و وضعیت آن‌ها را به‌روزرسانی کند. احراز هویت آن از طریق `backend/app/api/routes/external_prompts.py` و تابع `require_external_token` انجام می‌شود که خود به `EXTERNAL_TOOL_TOKEN` در فایل‌های پیکربندی (مانند `backend/.env.example`) وابسته است. برای ساختارمند کردن پرامپت‌ها، ممکن است از `backend/app/services/ai_manager.py` برای دسترسی به قابلیت‌های مدل‌های AI موجود استفاده کند. همچنین، برای ذخیره‌سازی و بازیابی داده‌های مربوط به تسک‌ها، با `backend/app/core/database.py` و مدل‌های مرتبط (مانند `OversightTask` که در `oversight_service.py` مدیریت می‌شود) تعامل خواهد داشت.

## 🔍 Context و وضعیت فعلی
کاربر درخواست توسعه یک سرویس بک‌اند جدید را دارد که مسئولیت اصلی آن تعامل با ایجنت‌های Cloud Code است. این سرویس باید پرامپت‌های دریافتی را ساختارمند کرده و برای پردازش و اجرای کارها به ایجنت‌های Cloud Code ارسال کند. این سرویس از لایه دسترسی به توکن موجود (که در `backend/app/api/routes/external_prompts.py` با `X-External-Token` و `EXTERNAL_TOOL_TOKEN` پیاده‌سازی شده) برای احراز هویت استفاده خواهد کرد. منطق اصلی پردازش پرامپت توسط Cloud Code را در خود جای می‌دهد.

همانطور که کاربر اشاره کرده: "ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه. مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده."

این به معنای ایجاد یک مکانیزم مشابه با `backend/app/api/routes/external_prompts.py` برای مدیریت چرخه حیات تسک‌ها (claim, progress, complete, fail) است، اما با تمرکز بر Cloud Code. سرویس جدید باید قابلیت "ساختارمند کردن پرامپت" را داشته باشد و "کارای اجرایی" و "اجرای پرامپت" را مشابه "مدل‌های متقاعد فعلی" (که توسط `backend/app/services/ai_manager.py` و `backend/app/services/deep_analysis_service.py` مدیریت می‌شوند) انجام دهد. این پیاده‌سازی باید از ساختار موجود برای مدیریت تسک‌ها در `backend/app/services/oversight_service.py` بهره ببرد و از `backend/app/core/database.py` برای تعامل با دیتابیس استفاده کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] سرویس `CloudCodeAgentService` ایجاد شده باشد: فایل `backend/app/services/cloud_code_agent_service.py` با کلاس `CloudCodeAgentService` و متدهای اولیه (مانند `claim_task`, `send_prompt_to_cloud_code`, `complete_task`) وجود داشته باشد.
- [ ] مسیرهای API برای Cloud Code Agents تعریف شده باشد: فایل `backend/app/api/routes/cloud_code_agents.py` شامل endpointهای `GET /cloud-code/next-task`, `POST /cloud-code/{task_id}/claim`, `POST /cloud-code/{task_id}/complete`, `POST /cloud-code/{task_id}/fail`, `POST /cloud-code/{task_id}/progress` باشد.
- [ ] احراز هویت `X-External-Token` برای مسیرهای Cloud Code اعمال شود: هر endpoint در `backend/app/api/routes/cloud_code_agents.py` باید از `Depends(require_external_token)` استفاده کند.
- [ ] تسک‌های Cloud Code Agent در `oversight_service` به‌درستی مدیریت شوند: پس از `POST /cloud-code/{task_id}/claim`، وضعیت `external_status` تسک در دیتابیس به "claimed" و `external_locked_by` به شناسه agent مربوطه به‌روزرسانی شود.
- [ ] قابلیت "ساختارمند کردن پرامپت" در سرویس Cloud Code Agent وجود داشته باشد: سرویس جدید باید بتواند یک پرامپت خام را به فرمت مورد نیاز Cloud Code تبدیل کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **ایجاد فایل سرویس جدید**: یک فایل جدید به نام `backend/app/services/cloud_code_agent_service.py` ایجاد شود. این سرویس شامل منطق اصلی برای: دریافت تسک از `oversight_service`، ساختارمند کردن پرامپت برای Cloud Code (با استفاده از `ai_manager` یا منطق داخلی)، ارسال پرامپت به API خارجی Cloud Code، مدیریت وضعیت اجرای تسک در Cloud Code و به‌روزرسانی وضعیت تسک در `oversight_service` (claimed, in_progress, done, failed) خواهد بود.
2.  **ایجاد فایل مسیر API جدید**: یک فایل جدید به نام `backend/app/api/routes/cloud_code_agents.py` ایجاد شود. این فایل شامل endpointهای FastAPI برای تعامل با Cloud Code agent خواهد بود، مشابه endpointهای موجود در `backend/app/api/routes/external_prompts.py`. این endpointها شامل `GET /cloud-code/next-task`، `POST /cloud-code/{task_id}/claim`، `POST /cloud-code/{task_id}/progress`، `POST /cloud-code/{task_id}/complete` و `POST /cloud-code/{task_id}/fail` خواهند بود.
3.  **پیاده‌سازی احراز هویت**: در `backend/app/api/routes/cloud_code_agents.py`، از `require_external_token` (تعریف شده در `backend/app/api/routes/external_prompts.py` خط ۱۰۳) برای احراز هویت درخواست‌ها استفاده شود.
4.  **به‌روزرسانی `oversight_service`**: بررسی شود که آیا `backend/app/services/oversight_service.py` نیاز به تغییراتی برای پشتیبانی از نوع جدیدی از external agent (Cloud Code) دارد یا خیر. احتمالاً نیاز به افزودن یک فیلد `agent_type` یا مشابه آن به مدل `OversightTask` برای تمایز بین Claude Runner و Cloud Code Agent است.
5.  **مدل‌سازی پرامپت**: اگر ساختارمند کردن پرامپت پیچیده است، ممکن است نیاز به یک مدل Pydantic جدید برای فرمت پرامپت Cloud Code باشد.

## 💡 نمونه‌های قبل/بعد
**افزودن مسیرهای API جدید برای Cloud Code Agents در `main.py`**

_قبل:_
```
from app.api.routes import (
    ai_usage, analysis, audio, chat, config, creator, debate, diagrams,
    external, external_projects, external_prompts, github_import,
    model_profiles, models, notifications, orchestrator, oversight,
    project_health, project_journal, project_memory, project_structure,
    projects, render_logs, runtime, screen_recording, security_analysis,
    settings, simple_projects, system_prompts, unified_api, upload
)
# ...
app.include_router(external_prompts.router)
```

_بعد:_
```
from app.api.routes import (
    ai_usage, analysis, audio, chat, config, creator, debate, diagrams,
    external, external_projects, external_prompts, cloud_code_agents, # 🆕
    github_import, model_profiles, models, notifications, orchestrator, oversight,
    project_health, project_journal, project_memory, project_structure,
    projects, render_logs, runtime, screen_recording, security_analysis,
    settings, simple_projects, system_prompts, unified_api, upload
)
# ...
app.include_router(external_prompts.router)
app.include_router(cloud_code_agents.router) # 🆕
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_cloud_code_agent_interaction.py`
- `uvicorn app.main:app --reload --port 8000`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **تداخل با `external_prompts.py`**: اگر منطق مدیریت تسک در `backend/app/services/oversight_service.py` به اندازه کافی عمومی نباشد، افزودن Cloud Code agent ممکن است باعث تداخل یا پیچیدگی در مدیریت `external_status` و `external_locked_by` شود، به‌ویژه اگر نیاز به تمایز بین انواع مختلف agent (Claude vs. Cloud Code) باشد.
2.  **امنیت توکن**: استفاده از `EXTERNAL_TOOL_TOKEN` برای یک سرویس جدید نیازمند اطمینان از این است که Cloud Code agent به همان سطح دسترسی Claude Auto-Runner نیاز دارد و هیچ آسیب‌پذیری جدیدی ایجاد نمی‌شود.
3.  **پیچیدگی ساختارمند کردن پرامپت**: "ساختارمند کردن پرامپت" برای Cloud Code ممکن است نیازمند منطق پیچیده‌ای باشد که به مدل‌های AI موجود (از طریق `backend/app/services/ai_manager.py`) وابسته باشد و این می‌تواند سربار و وابستگی‌های جدیدی ایجاد کند.
4.  **پایداری فراخوانی API خارجی**: فراخوانی API خارجی Cloud Code (که در این پروژه نیست) نیازمند مدیریت خطا، retry و timeout قوی است تا پایداری سیستم حفظ شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 3: پیاده‌سازی Endpoint جدید برای پردازش چت با Cloud Code

**Scope:** این مرحله شامل ایجاد یک یا چند Endpoint جدید در API بک‌اند چت است که به طور خاص برای دریافت درخواست‌های پردازش پرامپت توسط Cloud Code طراحی شده‌اند. این Endpointها درخواست‌ها را از فرانت‌اند دریافت کرده و آن‌ها را به سرویس تعامل با ایجنت‌های Cloud Code (مرحله ۲) ارجاع می‌دهند. این Endpointها باید از نظر امنیتی محافظت شده و قابلیت مدیریت بار را داشته باشند.
**Key terms:** Endpoint, API بک‌اند چت, تب بازرس ویژه, مدل‌ها, کلیدشون, تنظیمات

**بخش مربوط از متن کاربر:**
```
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن Endpoint چت Cloud Code به تب بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py` — `chat_cloud_code_endpoint` — ایجاد یک Endpoint جدید برای دریافت درخواست‌های چت Cloud Code از فرانت‌اند تب بازرس ویژه. این Endpoint از `require_external_token` برای احراز هویت استفاده می‌کند و پاسخ را به صورت استریم (SSE) برمی‌گرداند.
  ```python
  from fastapi import APIRouter, Depends, Header, HTTPException, Query, BackgroundTasks
  from fastapi.responses import StreamingResponse
  import asyncio
  import json
  import logging
  
  from ...services.oversight_service import get_oversight_service
  from ...services.inspector_agent import get_inspector_agent
  from ...api.routes.external_prompts import require_external_token # Reusing auth
  from pydantic import BaseModel
  
  logger = logging.getLogger(__name__)
  
  router = APIRouter(prefix="/oversight", tags=["Oversight"])
  
  class CloudCodeChatRequest(BaseModel):
      project_id: str
      prompt_text: str
      session_id: str # For continuing a chat session
      model_id: str = "claude-opus-4-8" # Default to a strong Cloud Code model
  
  @router.post("/inspector/chat-cloud-code", dependencies=[Depends(require_external_token)])
  async def chat_with_cloud_code_inspector(
      request: CloudCodeChatRequest,
  ):
      """
      پردازش درخواست چت با Cloud Code در تب بازرس ویژه.
      این Endpoint از X-External-Token برای احراز هویت استفاده می‌کند.
      پاسخ به صورت استریم (SSE) ارسال می‌شود.
      """
      progress_queue: asyncio.Queue = asyncio.Queue()
  
      async def progress_callback(progress_data: dict):
          await progress_queue.put(progress_data)
  
      async def generate_events():
          try:
              inspector_agent = get_inspector_agent()
              async for event_data in inspector_agent.chat_with_cloud_code(
                  project_id=request.project_id,
                  session_id=request.session_id,
                  prompt_text=request.prompt_text,
                  model_id=request.model_id,
                  progress_callback=progress_callback
              ):
                  data = json.dumps(event_data, ensure_ascii=False, default=str)
                  yield f"event: progress\ndata: {data}\n\n"
          except Exception as e:
              logger.error(f"Error in Cloud Code chat stream: {e}", exc_info=True)
              yield f"event: error\ndata: {json.dumps({'error': str(e)})}\n\n"
          finally:
              yield f"event: done\ndata: {{}}\n\n"
  
      return StreamingResponse(
          generate_events(),
          media_type="text/event-stream",
          headers={
              "Cache-Control": "no-cache",
              "Connection": "keep-alive",
              "X-Accel-Buffering": "no",
              "Access-Control-Allow-Origin": "*"
          }
      )
  ```
- `backend/app/services/inspector_agent.py` — `chat_with_cloud_code` — افزودن متد `chat_with_cloud_code` به سرویس `InspectorAgentService` برای مدیریت منطق اصلی چت با Cloud Code. این متد از `ai_manager` برای تعامل با مدل‌های AI استفاده می‌کند و پاسخ را به صورت استریم برمی‌گرداند و تاریخچه چت را در `InspectorSession` ذخیره می‌کند.
  ```python
  import logging
  from typing import AsyncGenerator, Callable, Dict, Any, Optional
  
  from ..core.database import SessionLocal
  from ..services.ai_manager import get_ai_manager
  from ..models.inspector_session import InspectorSession, InspectorPromptField # Assuming these models exist
  
  logger = logging.getLogger(__name__)
  
  class InspectorAgentService:
      def __init__(self):
          self.ai_manager = get_ai_manager()
  
      async def chat_with_cloud_code(
          self,
          project_id: str,
          session_id: str,
          prompt_text: str,
          model_id: str,
          progress_callback: Callable[[Dict[str, Any]], Any]
      ) -> AsyncGenerator[Dict[str, Any], None]:
          db = SessionLocal()
          try:
              # Load or create inspector session
              session = db.query(InspectorSession).filter(
                  InspectorSession.id == session_id, InspectorSession.project_id == project_id
              ).first()
              if not session:
                  session = InspectorSession(id=session_id, project_id=project_id, history=[])
                  db.add(session)
                  db.commit()
                  db.refresh(session)
  
              # Add user prompt to history
              session.history.append({"role": "user", "content": prompt_text})
              db.commit()
  
              yield {"event": "start", "message": "شروع پردازش Cloud Code..."}
              await progress_callback({"event": "progress", "stage": "AI Processing"})
  
              # Get AI model and stream response
              model = self.ai_manager.get_model(model_id)
              if not model:
                  raise ValueError(f"Model {model_id} not found or not active.")
  
              full_response_content = ""
              async for chunk in model.stream_chat_completion(
                  messages=session.history,
                  model_id=model_id,
                  temperature=0.7,
                  max_tokens=4000
              ):
                  if chunk.content:
                      full_response_content += chunk.content
                      yield {"event": "chunk", "content": chunk.content}
                      await progress_callback({"event": "progress", "stage": "Streaming response"})
  
              # Add AI response to history
              session.history.append({"role": "assistant", "content": full_response_content})
              db.commit()
              yield {"event": "end", "message": "پردازش Cloud Code تکمیل شد.", "full_response": full_response_content}
  
          except Exception as e:
              logger.error(f"Error in InspectorAgentService.chat_with_cloud_code: {e}", exc_info=True)
              yield {"event": "error", "message": str(e)}
          finally:
              db.close()
  
  def get_inspector_agent() -> InspectorAgentService:
      return InspectorAgentService()
  ```
- `backend/app/api/routes/external_prompts.py:90-99` — `require_external_token` — استفاده مجدد از `require_external_token` برای احراز هویت Endpoint جدید چت Cloud Code، مطابق با درخواست کاربر برای استفاده از «همین توکن».
  ```python
  def _check_external_token(provided: Optional[str]) -> None:
      expected = os.environ.get("EXTERNAL_TOOL_TOKEN", "").strip()
      if not expected:
          raise HTTPException(
              status_code=503,
              detail="EXTERNAL_TOOL_TOKEN env var تنظیم نشده — endpoint غیرفعال است.",
          )
      if not provided or provided.strip() != expected:
          raise HTTPException(status_code=401, detail="Invalid X-External-Token")
  
  def require_external_token(
      x_external_token: Optional[str] = Header(default=None, alias="X-External-Token"),
  ) -> None:
      """FastAPI dependency برای endpoint های ابزار خارجی."""
      _check_external_token(x_external_token)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack اصلی پروژه شامل FastAPI برای بک‌اند و Next.js برای فرانت‌اند است. زبان اصلی Python است. کتابخانه‌های کلیدی شامل Pydantic برای اعتبارسنجی مدل‌ها، SQLAlchemy برای ORM دیتابیس (SQLite)، و `httpx`/`aiohttp` برای فراخوانی‌های HTTP به سرویس‌های AI هستند. `asyncio` برای مدیریت عملیات ناهمگام و `StreamingResponse` برای ارسال رویدادهای Server-Sent Events (SSE) استفاده می‌شود. این پروژه از چندین پروایدر AI پشتیبانی می‌کند که از طریق `ai_manager.py` مدیریت می‌شوند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` (سطر 21) — این سرویس وظایف کلی Oversight را مدیریت می‌کند و ممکن است نیاز به هماهنگی با فعالیت‌های Inspector داشته باشد، هرچند `inspector_agent.py` مستقیماً با آن تعامل ندارد.
- `backend/app/services/ai_manager.py` (سطر 113) — برای انتخاب و فراخوانی مدل‌های AI مورد استفاده در چت Cloud Code ضروری است. متد `stream_chat_completion` از این سرویس فراخوانی می‌شود.
- `backend/app/core/database.py` (سطر 116) — برای مدیریت Sessionهای دیتابیس و ذخیره/بازیابی `InspectorSession` استفاده می‌شود.
- `backend/app/models/inspector_session.py` (سطر 19) — برای ذخیره تاریخچه چت و وضعیت هر Session بازرس ویژه نیاز به به‌روزرسانی یا استفاده از این مدل است. فیلد `history` برای ذخیره مکالمات استفاده می‌شود.
- `backend/app/api/routes/analysis.py` (سطر 83) — الگوی پیاده‌سازی StreamingResponse (SSE) در `run_analysis_stream` برای Endpoint جدید چت Cloud Code استفاده خواهد شد.

## 🌐 نقشهٔ وابستگی‌ها
Endpoint جدید در `backend/app/api/routes/oversight.py` به `backend/app/services/inspector_agent.py` وابسته خواهد بود. `inspector_agent.py` به نوبه خود از `backend/app/services/ai_manager.py` برای تعامل با مدل‌های AI و از `backend/app/core/database.py` برای مدیریت Sessionهای دیتابیس و مدل `backend/app/models/inspector_session.py` استفاده می‌کند. احراز هویت Endpoint جدید از `require_external_token` در `backend/app/api/routes/external_prompts.py` بهره می‌برد. الگوی استریمینگ پاسخ از `backend/app/api/routes/analysis.py` الهام گرفته شده است. این ساختار تضمین می‌کند که منطق چت به درستی تفکیک شده و از قابلیت‌های موجود پروژه استفاده می‌شود.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که یک یا چند Endpoint جدید در API بک‌اند چت ایجاد شود که به طور خاص برای دریافت درخواست‌های پردازش پرامپت توسط Cloud Code طراحی شده‌اند. این Endpointها باید درخواست‌ها را از فرانت‌اند دریافت کرده و آن‌ها را به سرویس تعامل با ایجنت‌های Cloud Code ارجاع دهند. تأکید شده است که این Endpointها باید از نظر امنیتی محافظت شده و قابلیت مدیریت بار را داشته باشند.

کاربر به صراحت اشاره می‌کند: «همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.»

این درخواست نشان می‌دهد که نیاز به گسترش قابلیت‌های چت در «تب بازرس ویژه» (Special Inspector Tab) برای استفاده از «Cloud Code» وجود دارد. با توجه به وجود `backend/app/api/routes/external_prompts.py` که تعامل با ابزارهای خارجی (مانند Claude Auto-Runner که نوعی Cloud Code است) را مدیریت می‌کند و از `X-External-Token` برای احراز هویت استفاده می‌کند، می‌توان از این الگو برای Endpoint جدید بهره برد. سرویس `backend/app/services/inspector_agent.py` نیز مسئول منطق اصلی تب بازرس است و باید برای پردازش این درخواست‌ها به‌روزرسانی شود. همچنین، نیاز به یک مکانیزم پاسخ‌دهی Real-time (استریم) مشابه `run_analysis_stream` در `backend/app/api/routes/analysis.py` برای نمایش پیشرفت و نتایج چت وجود دارد. این Endpoint جدید باید بتواند پرامپت‌ها را از فرانت‌اند دریافت کرده و با استفاده از مدل‌های AI پیکربندی شده در تنظیمات، آن‌ها را پردازش کند و نتایج را به صورت استریم بازگرداند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Endpoint جدید `POST /oversight/inspector/chat-cloud-code` باید در `backend/app/api/routes/oversight.py` تعریف شود.
- [ ] Endpoint `chat_with_cloud_code_inspector` باید از `require_external_token` برای احراز هویت استفاده کند.
- [ ] متد `chat_with_cloud_code` باید به `InspectorAgentService` در `backend/app/services/inspector_agent.py` اضافه شود و یک `AsyncGenerator` برای استریم پاسخ برگرداند.
- [ ] فراخوانی Endpoint `POST /oversight/inspector/chat-cloud-code` با `X-External-Token` معتبر و `project_id`, `session_id`, `prompt_text` باید پاسخ استریمینگ (SSE) شامل رویدادهای `start`, `chunk`, `end` و `progress` را برگرداند.
- [ ] تاریخچه چت (شامل پرامپت کاربر و پاسخ AI) باید در `InspectorSession` مربوطه در دیتابیس ذخیره شود و پس از هر تعامل به‌روزرسانی گردد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی قابلیت چت با Cloud Code در تب بازرس ویژه، مراحل زیر پیشنهاد می‌شود:

1.  **ایجاد Endpoint جدید در `backend/app/api/routes/oversight.py`:** یک Endpoint `POST` جدید (مثلاً `/oversight/inspector/chat-cloud-code`) اضافه شود که `project_id`، `prompt_text` و `session_id` را دریافت کند. این Endpoint باید از `require_external_token` (از `backend/app/api/routes/external_prompts.py`) برای احراز هویت استفاده کند.
2.  **پیاده‌سازی منطق چت در `backend/app/services/inspector_agent.py`:** یک متد جدید (مثلاً `chat_with_cloud_code`) به این سرویس اضافه شود. این متد مسئول دریافت پرامپت، بارگذاری یا ایجاد یک `InspectorSession`، و فراخوانی `ai_manager.py` با مدل‌های مناسب Cloud Code خواهد بود. این متد باید قابلیت استریم پاسخ را داشته باشد و تاریخچه چت را در دیتابیس ذخیره کند.
3.  **استفاده از `ai_manager.py`:** `inspector_agent.py` باید از `get_ai_manager()` برای انتخاب و فراخوانی مدل‌های AI با قابلیت‌های Cloud Code استفاده کند.
4.  **مدیریت Session:** اطمینان حاصل شود که `backend/app/models/inspector_session.py` می‌تواند تاریخچه چت و وضعیت مربوط به تعامل با Cloud Code را ذخیره کند. در صورت نیاز، فیلدهای جدیدی به این مدل اضافه شود.
5.  **پاسخ استریمینگ:** Endpoint جدید باید پاسخ را به صورت Server-Sent Events (SSE) استریم کند تا پیشرفت و پاسخ‌های AI به صورت Real-time به فرانت‌اند ارسال شود، مشابه الگوی پیاده‌سازی شده در `backend/app/api/routes/analysis.py` در تابع `run_analysis_stream`.

## 💡 نمونه‌های قبل/بعد
**قبل: `backend/app/api/routes/oversight.py` (بدون Endpoint چت Cloud Code)**

_قبل:_
```
router = APIRouter(prefix="/oversight", tags=["Oversight"])

# ... سایر Endpoint های Oversight ...
```

_بعد:_
```
from fastapi import APIRouter, Depends, Header, HTTPException, Query, BackgroundTasks
from fastapi.responses import StreamingResponse
import asyncio
import json
import logging

from ...services.oversight_service import get_oversight_service
from ...services.inspector_agent import get_inspector_agent
from ...api.routes.external_prompts import require_external_token # Reusing auth
from pydantic import BaseModel

logger = logging.getLogger(__name__)

router = APIRouter(prefix="/oversight", tags=["Oversight"])

class CloudCodeChatRequest(BaseModel):
    project_id: str
    prompt_text: str
    session_id: str # For continuing a chat session
    model_id: str = "claude-opus-4-8" # Default to a strong Cloud Code model

@router.post("/inspector/chat-cloud-code", dependencies=[Depends(require_external_token)])
async def chat_with_cloud_code_inspector(
    request: CloudCodeChatRequest,
):
    """
    پردازش درخواست چت با Cloud Code در تب بازرس ویژه.
    این Endpoint از X-External-Token برای احراز هویت استفاده می‌کند.
    پاسخ به صورت استریم (SSE) ارسال می‌شود.
    """
    progress_queue: asyncio.Queue = asyncio.Queue()

    async def progress_callback(progress_data: dict):
        await progress_queue.put(progress_data)

    async def generate_events():
        try:
            inspector_agent = get_inspector_agent()
            async for event_data in inspector_agent.chat_with_cloud_code(
                project_id=request.project_id,
                session_id=request.session_id,
                prompt_text=request.prompt_text,
                model_id=request.model_id,
                progress_callback=progress_callback
            ):
                data = json.dumps(event_data, ensure_ascii=False, default=str)
                yield f"event: progress\ndata: {data}\n\n"
        except Exception as e:
            logger.error(f"Error in Cloud Code chat stream: {e}", exc_info=True)
            yield f"event: error\ndata: {json.dumps({'error': str(e)})}\n\n"
        finally:
            yield f"event: done\ndata: {{}}\n\n"

    return StreamingResponse(
        generate_events(),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",
            "Access-Control-Allow-Origin": "*"
        }
    )
```

**قبل: `backend/app/services/inspector_agent.py` (بدون متد چت Cloud Code)**

_قبل:_
```
import logging

logger = logging.getLogger(__name__)

class InspectorAgentService:
    def __init__(self):
        pass # Existing initialization

# ... سایر متدهای InspectorAgentService ...

def get_inspector_agent() -> InspectorAgentService:
    return InspectorAgentService()
```

_بعد:_
```
import logging
from typing import AsyncGenerator, Callable, Dict, Any, Optional

from ..core.database import SessionLocal
from ..services.ai_manager import get_ai_manager
from ..models.inspector_session import InspectorSession, InspectorPromptField # Assuming these models exist

logger = logging.getLogger(__name__)

class InspectorAgentService:
    def __init__(self):
        self.ai_manager = get_ai_manager()

    async def chat_with_cloud_code(
        self,
        project_id: str,
        session_id: str,
        prompt_text: str,
        model_id: str,
        progress_callback: Callable[[Dict[str, Any]], Any]
    ) -> AsyncGenerator[Dict[str, Any], None]:
        db = SessionLocal()
        try:
            # Load or create inspector session
            session = db.query(InspectorSession).filter(
                InspectorSession.id == session_id, InspectorSession.project_id == project_id
            ).first()
            if not session:
                session = InspectorSession(id=session_id, project_id=project_id, history=[])
                db.add(session)
                db.commit()
                db.refresh(session)

            # Add user prompt to history
            session.history.append({"role": "user", "content": prompt_text})
            db.commit()

            yield {"event": "start", "message": "شروع پردازش Cloud Code..."}
            await progress_callback({"event": "progress", "stage": "AI Processing"})

            # Get AI model and stream response
            model = self.ai_manager.get_model(model_id)
            if not model:
                raise ValueError(f"Model {model_id} not found or not active.")

            full_response_content = ""
            async for chunk in model.stream_chat_completion(
                messages=session.history,
                model_id=model_id,
                temperature=0.7,
                max_tokens=4000
            ):
                if chunk.content:
                    full_response_content += chunk.content
                    yield {"event": "chunk", "content": chunk.content}
                    await progress_callback({"event": "progress", "stage": "Streaming response"})

            # Add AI response to history
            session.history.append({"role": "assistant", "content": full_response_content})
            db.commit()
            yield {"event": "end", "message": "پردازش Cloud Code تکمیل شد.", "full_response": full_response_content}

        except Exception as e:
            logger.error(f"Error in InspectorAgentService.chat_with_cloud_code: {e}", exc_info=True)
            yield {"event": "error", "message": str(e)}
        finally:
            db.close()

def get_inspector_agent() -> InspectorAgentService:
    return InspectorAgentService()
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_routes.py`
- `pytest backend/tests/test_inspector_agent_chat.py`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **افزایش بار روی سرویس‌های AI**: هر درخواست چت جدید به مدل‌های AI منجر به افزایش مصرف توکن و بار روی سرویس‌های AI (مانند Claude) می‌شود. این امر می‌تواند هزینه‌ها را افزایش داده و در صورت عدم مدیریت صحیح، منجر به محدودیت نرخ (rate limiting) شود. `ai_manager.py` باید قابلیت مدیریت بار و Fallback را داشته باشد.
2. **پیچیدگی مدیریت Session**: حفظ و به‌روزرسانی `InspectorSession` در دیتابیس برای هر تعامل چت، نیازمند مدیریت دقیق تراکنش‌ها و اطمینان از سازگاری داده‌ها است. خطاهای دیتابیس می‌تواند منجر به از دست رفتن تاریخچه چت شود.
3. **امنیت توکن**: استفاده از `X-External-Token` در فرانت‌اند نیازمند مدیریت امن این توکن در سمت کلاینت است تا از افشا شدن آن جلوگیری شود. اگر توکن به اشتباه در کد فرانت‌اند یا localStorage ذخیره شود، می‌تواند آسیب‌پذیری امنیتی ایجاد کند.
4. **پایداری استریمینگ**: پیاده‌سازی Server-Sent Events (SSE) نیازمند مدیریت صحیح اتصالات و خطاها در طولانی‌مدت است. قطعی شبکه یا خطاهای سمت سرور می‌تواند منجر به قطع شدن استریم و تجربه کاربری نامطلوب شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: اصلاح منطق مسیریابی درخواست‌ها در بک‌اند چت

**Scope:** در این مرحله، منطق موجود در بک‌اند سیستم چت بازرس ویژه اصلاح می‌شود تا بتواند بر اساس انتخاب کاربر (Cloud Code یا مدل‌های فعلی) درخواست‌های ورودی را به درستی مسیریابی کند. این شامل افزودن یک شرط به منطق پردازش درخواست است که اگر گزینه Cloud Code انتخاب شده باشد، درخواست به Endpoint جدید (مرحله ۳) هدایت شود؛ در غیر این صورت، به مسیر پردازش مدل‌های فعلی ادامه یابد.
**Key terms:** منطق مسیریابی, بک‌اند سیستم چت, تب بازرس ویژه, گزینه Cloud Code, مدل‌های فعلی

**بخش مربوط از متن کاربر:**
```
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی مسیریابی چت بازرس ویژه برای Cloud Code و مدل‌های داخلی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py` — `process_inspector_chat` — این فایل deep-read نشده است. این snippet یک طرح پیشنهادی برای افزودن یک endpoint جدید یا اصلاح یک endpoint موجود در `backend/app/api/routes/oversight.py` است که منطق مسیریابی را پیاده‌سازی می‌کند. کد ارائه شده صرفاً یک طرح پیشنهادی است و از محتوای واقعی فایل نیست.
  ```python
  from fastapi import APIRouter, Depends, HTTPException
  from pydantic import BaseModel
  from typing import Optional
  
  from ...services.oversight_service import get_oversight_service
  from ...services.ai_manager import get_ai_manager
  
  router = APIRouter(prefix="/oversight", tags=["Oversight"])
  
  class InspectorChatRequest(BaseModel):
      project_id: str
      prompt_content: str
      processing_mode: str = "internal_ai" # 'cloud_code' or 'internal_ai'
  
  @router.post("/process_inspector_chat")
  async def process_inspector_chat(request: InspectorChatRequest):
      oversight_service = get_oversight_service()
      if request.processing_mode == "cloud_code":
          # Logic to submit task to external runner
          task_summary = await oversight_service.submit_inspector_task_to_external_runner(
              project_id=request.project_id,
              prompt_content=request.prompt_content,
              agent_id="claude-auto-task"
          )
          return {"status": "task_submitted", "task": task_summary}
      else: # Default to internal_ai
          ai_manager = get_ai_manager()
          # Placeholder for actual internal AI processing
          response = await ai_manager.process_chat_prompt(
              project_id=request.project_id,
              prompt_content=request.prompt_content
          )
          return {"status": "processed_by_internal_ai", "response": response}
  ```
- `backend/app/api/routes/external_prompts.py:137-153` — `_task_summary` — این تابع ساختار یک تسک را که توسط سیستم `external_prompts` مدیریت می‌شود، نشان می‌دهد. منطق مسیریابی جدید برای 'Cloud Code' باید تسک‌هایی را ایجاد کند که با این ساختار سازگار باشند تا توسط Claude Auto-Runner قابل پردازش باشند.
  ```python
  def _task_summary(task: Any) -> Dict[str, Any]:
      return {
          "task_id": task.id,
          "watched_id": task.watched_id,
          "project": task.project_full_name,
          "title": task.title,
          "type": task.type,
          "priority": task.priority,
          "execution_priority": getattr(task, "execution_priority", 100),
          "status": task.status,
          "external_status": getattr(task, "external_status", "pending"),
          "verification_status": task.verification_status,
          "github_prompt_path": getattr(task, "github_prompt_path", None),
          "external_attempts": getattr(task, "external_attempts", 0),
          "created_at": task.created_at,
          "updated_at": task.updated_at,
      }
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده شامل FastAPI برای بک‌اند و Python به عنوان زبان اصلی است. برای مدیریت مدل‌های AI از `ai_manager` و سرویس‌های پروایدرهای مختلف (مانند OpenAI, Claude) استفاده می‌شود. دیتابیس از SQLite (با SQLAlchemy) استفاده می‌کند. برای تعامل با ابزارهای خارجی مانند Claude Code Action، از مکانیزم همگام‌سازی پرامپت با GitHub و APIهای `external_prompts` استفاده می‌شود که شامل مدیریت lease و وضعیت‌های `external_status` است.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` — این سرویس مسئول مدیریت کلی تسک‌های oversight است و متد جدید `submit_inspector_task_to_external_runner` در آن پیاده‌سازی خواهد شد. این سرویس توسط `backend/app/api/routes/oversight.py` فراخوانی می‌شود.
- `backend/app/services/ai_manager.py` — این سرویس مسئول مدیریت و فراخوانی مدل‌های AI داخلی است و در مسیر 'مدل‌های فعلی' توسط endpoint چت بازرس ویژه فراخوانی خواهد شد.
- `backend/app/models/system_prompt.py` — تسک‌های ایجاد شده برای Cloud Code احتمالاً از این مدل برای ذخیره‌سازی استفاده می‌کنند. ممکن است نیاز به افزودن فیلدهای مرتبط با وضعیت اجرای خارجی (مانند `external_status`) به این مدل باشد.
- `backend/app/core/database.py` — متد جدید در `oversight_service.py` برای ذخیره تسک‌های جدید به دیتابیس نیاز به `SessionLocal` از این فایل دارد.
- `backend/app/services/prompt_github_sync.py` — پس از ایجاد یک تسک جدید برای Cloud Code، `oversight_service` باید `_schedule_prompt_sync` را فراخوانی کند که به این سرویس برای همگام‌سازی تسک با GitHub وابسته است تا Claude Auto-Runner بتواند آن را بردارد.

## 🌐 نقشهٔ وابستگی‌ها
منطق مسیریابی جدید در `backend/app/api/routes/oversight.py` به `backend/app/services/oversight_service.py` برای مدیریت تسک‌های Cloud Code و به `backend/app/services/ai_manager.py` برای پردازش با مدل‌های داخلی وابسته است. `oversight_service.py` نیز برای ذخیره‌سازی تسک‌ها به `backend/app/models/system_prompt.py` و `backend/app/core/database.py` وابسته است. همچنین، برای فعال‌سازی Claude Auto-Runner، `oversight_service.py` از `_schedule_prompt_sync` (که بخشی از `backend/app/services/prompt_github_sync.py` است) استفاده می‌کند تا تسک‌ها را با GitHub همگام‌سازی کند. `backend/app/api/routes/external_prompts.py` نیز رابطی است که Claude Auto-Runner برای دریافت و به‌روزرسانی وضعیت این تسک‌ها از آن استفاده می‌کند.

## 🔍 Context و وضعیت فعلی
این تسک بر روی اصلاح منطق مسیریابی درخواست‌ها در بک‌اند سیستم چت بازرس ویژه تمرکز دارد. هدف این است که سیستم بتواند بر اساس انتخاب کاربر، درخواست‌های ورودی را به درستی مسیریابی کند. کاربر به صراحت درخواست کرده است که اگر گزینه 'Cloud Code' انتخاب شده باشد، درخواست به 'Endpoint جدید (مرحله ۳)' هدایت شود؛ در غیر این صورت، به مسیر پردازش 'مدل‌های فعلی' ادامه یابد. تأکید شده است که منطق موجود برای 'مدل‌های فعلی' نباید حذف شود و 'سر جای خودش باشد'، بلکه 'اینم یه گزینه باشه'. همچنین اشاره شده که 'لیمیت زمانو فقط استفاده می‌کنه' که نشان می‌دهد مسیر Cloud Code ممکن است ویژگی‌های متفاوتی در مدیریت زمان داشته باشد، که با مکانیزم‌های lease و timeout در `backend/app/api/routes/external_prompts.py` همخوانی دارد. این تغییرات نیازمند افزودن یک پارامتر `processing_mode` به endpoint مربوط به چت بازرس ویژه در `backend/app/api/routes/oversight.py` و پیاده‌سازی منطق شرطی برای هدایت درخواست به سرویس‌های مناسب است. مسیر 'Cloud Code' به معنای ایجاد یک تسک جدید در `oversight_service` است که توسط `claude-auto-task` (که از طریق `backend/app/api/routes/external_prompts.py` مدیریت می‌شود) برداشته و اجرا خواهد شد. مسیر 'مدل‌های فعلی' نیز از طریق `ai_manager` و سرویس‌های مرتبط با آن پردازش می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] Endpoint `POST /oversight/process_inspector_chat` (یا endpoint مشابه موجود) باید در `backend/app/api/routes/oversight.py` وجود داشته باشد و پارامتر `processing_mode` را بپذیرد.
- [ ] هنگامی که `processing_mode` برابر با 'cloud_code' است، فراخوانی به متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` انجام شود و یک تسک جدید با `external_status='pending'` در دیتابیس ایجاد شود.
- [ ] هنگامی که `processing_mode` برابر با 'internal_ai' است (یا پارامتر `processing_mode` ارسال نشود)، درخواست به منطق پردازش مدل‌های داخلی (از طریق `ai_manager`) هدایت شود و پاسخ مناسب برگردانده شود.
- [ ] متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` باید `_schedule_prompt_sync` را فراخوانی کند تا تسک جدید با GitHub همگام‌سازی شود.
- [ ] مدل `backend/app/models/system_prompt.py` باید فیلدهای `external_status`, `external_locked_by`, `external_lease_until`, `verification_status` و `external_attempts` را پشتیبانی کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این قابلیت، مراحل زیر پیشنهاد می‌شود:
1.  **اصلاح Endpoint چت بازرس ویژه:** در فایل `backend/app/api/routes/oversight.py` (که مسئول مسیرهای API مربوط به بازرس ویژه است)، یک endpoint موجود برای پردازش چت (مثلاً `/oversight/process_inspector_chat` یا مشابه آن) باید اصلاح شود یا یک endpoint جدید ایجاد گردد. این endpoint باید یک پارامتر جدید به نام `processing_mode` (با مقادیر احتمالی 'cloud_code' و 'internal_ai') را در `BaseModel` درخواست خود بپذیرد.
2.  **پیاده‌سازی منطق مسیریابی:** داخل تابع handler این endpoint، یک شرط `if/else` اضافه شود:
    *   **اگر `processing_mode` برابر با 'cloud_code' باشد:** یک متد جدید در `backend/app/services/oversight_service.py` (مثلاً `submit_inspector_task_to_external_runner`) فراخوانی شود. این متد مسئول ایجاد یک `SystemPrompt` جدید با محتوای چت کاربر، تنظیم `status` و `external_status` آن به 'pending' و سپس فراخوانی `_schedule_prompt_sync` برای همگام‌سازی با GitHub خواهد بود تا توسط Claude Auto-Runner برداشته شود.
    *   **اگر `processing_mode` برابر با 'internal_ai' باشد (یا پیش‌فرض):** منطق موجود برای پردازش چت با مدل‌های داخلی فراخوانی شود. این منطق احتمالاً شامل استفاده از `backend/app/services/ai_manager.py` برای انتخاب و فراخوانی مدل‌های AI است.
3.  **افزودن متد به `oversight_service`:** در `backend/app/services/oversight_service.py`، متد `submit_inspector_task_to_external_runner` پیاده‌سازی شود که یک تسک جدید از نوع `SystemPrompt` ایجاد کرده و آن را در دیتابیس ذخیره کند. این متد باید اطمینان حاصل کند که فیلدهای مربوط به `external_status` و `external_locked_by` به درستی مقداردهی اولیه شده‌اند.
4.  **به‌روزرسانی مدل `SystemPrompt` (در صورت نیاز):** اطمینان حاصل شود که مدل `backend/app/models/system_prompt.py` شامل فیلدهایی مانند `external_status`, `external_locked_by`, `external_lease_until`, `verification_status` و `external_attempts` است تا بتواند تسک‌های مربوط به Cloud Code را به درستی مدیریت کند. (این فایل در deep context نیست و نیاز به بررسی دارد.)

## 💡 نمونه‌های قبل/بعد
**مثال قبل (الگوی پردازش درخواست در analysis.py)**

_قبل:_
```
# انتخاب مدل‌ها
                model_ids = request.models if request.models else None
                if not model_ids:
                    available = ai_manager.get_available_models()
                    model_ids = [m.id for m in available] 

                # اجرای تحلیل
                final_result = await deep_analyzer.run_full_analysis(
                    project_id=request.project_id,
                    files=files,
                    roadmap_content=roadmap_content,
                    readme_content=readme_content,
                    model_ids=model_ids,
                    instruction=""
                )
```

_بعد:_
```
# منطق مسیریابی بر اساس processing_mode (در oversight.py)
                if request.processing_mode == "cloud_code":
                    task_summary = await oversight_service.submit_inspector_task_to_external_runner(
                        project_id=request.project_id,
                        prompt_content=request.prompt_content,
                        agent_id="claude-auto-task"
                    )
                    return {"status": "task_submitted", "task": task_summary}
                else: # Default to internal_ai
                    ai_manager = get_ai_manager()
                    response = await ai_manager.process_chat_prompt(
                        project_id=request.project_id,
                        prompt_content=request.prompt_content
                    )
                    return {"status": "processed_by_internal_ai", "response": response}
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_routing.py`
- `curl -X POST -H "Content-Type: application/json" -d '{"project_id": "proj123", "prompt_content": "Analyze this code", "processing_mode": "cloud_code"}' http://localhost:8000/oversight/process_inspector_chat`
- `curl -X POST -H "Content-Type: application/json" -d '{"project_id": "proj123", "prompt_content": "Explain this concept", "processing_mode": "internal_ai"}' http://localhost:8000/oversight/process_inspector_chat`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **تأثیر بر عملکرد موجود:** تغییر یا افزودن endpoint در `backend/app/api/routes/oversight.py` ممکن است بر سایر عملکردهای تب بازرس ویژه تأثیر بگذارد. نیاز به تست جامع برای اطمینان از عدم رگرسیون است.
2.  **سازگاری مدل `SystemPrompt`:** اگر مدل `backend/app/models/system_prompt.py` فیلدهای مورد نیاز برای مدیریت وضعیت خارجی تسک‌ها (مانند `external_status`) را نداشته باشد، نیاز به تغییرات شمای دیتابیس و migration خواهد بود که می‌تواند ریسک‌زا باشد.
3.  **وابستگی به `prompt_github_sync`:** عملکرد صحیح مسیر Cloud Code به همگام‌سازی موفق تسک با GitHub از طریق `backend/app/services/prompt_github_sync.py` وابسته است. هرگونه مشکل در این سرویس می‌تواند باعث عدم اجرای تسک توسط Claude Auto-Runner شود.
4.  **مدیریت خطا:** اطمینان از مدیریت خطای قوی در هر دو مسیر (Cloud Code و مدل‌های داخلی) برای جلوگیری از crash شدن سرویس در صورت بروز مشکل در فراخوانی‌های خارجی یا داخلی ضروری است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: افزودن گزینه انتخاب Cloud Code به رابط کاربری تب بازرس ویژه

**Scope:** این مرحله شامل توسعه و ادغام یک عنصر رابط کاربری جدید (مانند دکمه رادیویی، منوی کشویی یا سوئیچ) در 'تب بازرس ویژه' است. این عنصر به کاربر اجازه می‌دهد تا 'Cloud Code' را به عنوان موتور پردازش پرامپت خود انتخاب کند. طراحی این عنصر باید با ظاهر و احساس کلی رابط کاربری موجود سازگار باشد.
**Key terms:** رابط کاربری, تب بازرس ویژه, گزینه انتخاب, Cloud Code, موتور پردازش پرامپت

**بخش مربوط از متن کاربر:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. [02:00] اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن گزینه انتخاب Cloud Code به رابط کاربری تب بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. فرض بر این است که یک endpoint برای شروع یا به‌روزرسانی جلسه بازرسی وجود دارد که باید پارامتر `prompt_engine` را بپذیرد.
- `backend/app/services/inspector_agent.py` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. منطق اصلی پردازش پرامپت در این سرویس باید بر اساس `prompt_engine` تصمیم بگیرد که از AI محلی استفاده کند یا تسک را برای Cloud Code آماده کند.
- `backend/app/models/inspector_session.py` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. مدل دیتابیس برای ذخیره اطلاعات جلسه بازرس ویژه. فیلد جدید `prompt_engine` برای ذخیره انتخاب کاربر اضافه خواهد شد.
- `frontend/src/app/oversight/page.tsx` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. صفحه فرانت‌اند تب بازرس ویژه. یک عنصر UI برای انتخاب موتور پردازش پرامپت (Local AI / Cloud Code) باید اضافه شود.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack شناسایی شده: `fastapi`, `nextjs`. کتابخانه‌های مرتبط: `pydantic` برای تعریف مدل‌ها در FastAPI، `SQLAlchemy` (از طریق `SessionLocal` در `core/database.py`) برای تعامل با دیتابیس، `axios` در فرانت‌اند برای فراخوانی API، و `react` برای ساخت UI. مکانیزم `Server-Sent Events` (SSE) در `backend/app/api/routes/analysis.py` (خطوط 83-268) نشان می‌دهد که پروژه از استریمینگ برای به‌روزرسانی‌های Real-time استفاده می‌کند که ممکن است برای نمایش وضعیت تسک‌های Cloud Code نیز مفید باشد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/external_prompts.py` (سطر 253) — این فایل APIهایی را برای ابزارهای خارجی (مانند Cloud Code) فراهم می‌کند تا تسک‌ها را دریافت و وضعیت آن‌ها را به‌روز کنند. منطق جدید بک‌اند باید تسک‌ها را به گونه‌ای آماده کند که توسط این API قابل برداشت باشند.
- `backend/app/services/oversight_service.py` (سطر 298) — این سرویس وظایف `OversightTask` را مدیریت می‌کند و شامل متدهایی برای همگام‌سازی تسک‌ها با GitHub است که برای فعال‌سازی Claude Auto-Runner ضروری است.
- `.github/workflows/claude-auto-task.yml` (سطر 1) — این فایل GitHub Actions workflow است که توسط Claude Code برای اجرای تسک‌های خارجی استفاده می‌شود. منطق بک‌اند باید تسک‌ها را به گونه‌ای تنظیم کند که این workflow آن‌ها را pick up کند.
- `backend/app/services/ai_manager.py` (سطر 113) — اگر "Local AI" انتخاب شود، `inspector_agent` همچنان از `ai_manager` برای انتخاب و فراخوانی مدل‌های AI استفاده خواهد کرد.
- `backend/app/services/prompt_github_sync.py` (سطر 24) — این سرویس مسئول همگام‌سازی پرامپت‌ها با GitHub است که توسط `oversight_service` برای push کردن تسک‌های Cloud Code استفاده می‌شود.

## 🌐 نقشهٔ وابستگی‌ها
این تسک بر روی چندین بخش اصلی پروژه تأثیر می‌گذارد. در بک‌اند، `backend/app/api/routes/oversight.py` به عنوان نقطه ورود API برای تب بازرس ویژه، باید به‌روزرسانی شود تا انتخاب کاربر برای موتور پردازش پرامپت را دریافت کند. `backend/app/models/inspector_session.py` نیاز به یک فیلد جدید برای ذخیره این انتخاب دارد. منطق اصلی پردازش در `backend/app/services/inspector_agent.py` باید بر اساس این انتخاب، یا از `backend/app/services/ai_manager.py` برای پردازش محلی استفاده کند، یا با `backend/app/services/oversight_service.py` و `backend/app/services/prompt_github_sync.py` تعامل داشته باشد تا یک `OversightTask` را برای اجرای خارجی توسط `claude-auto-task.yml` آماده کند. در فرانت‌اند، `frontend/src/app/oversight/page.tsx` مسئول نمایش و مدیریت عنصر UI جدید است.

## 🔍 Context و وضعیت فعلی
کاربر درخواست کرده است که یک گزینه جدید برای انتخاب "Cloud Code" به عنوان "موتور پردازش پرامپت" در "رابط کاربری تب بازرس ویژه" اضافه شود. این مرحله شامل توسعه و ادغام یک عنصر رابط کاربری جدید (مانند دکمه رادیویی، منوی کشویی یا سوئیچ) در 'تب بازرس ویژه' است. این عنصر به کاربر اجازه می‌دهد تا 'Cloud Code' را به عنوان موتور پردازش پرامپت خود انتخاب کند. طراحی این عنصر باید با ظاهر و احساس کلی رابط کاربری موجود سازگار باشد. کاربر به صراحت بیان کرده است: "ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه." این بدان معناست که گزینه‌های موجود باید حفظ شوند و "Cloud Code" یک گزینه اضافی باشد که از "توکن کلاد کد" برای احراز هویت و پردازش استفاده می‌کند.

شواهد در کد واقعی پروژه نشان می‌دهد که `backend/app/api/routes/external_prompts.py` (خطوط 1-10) مسئول مدیریت تعامل با ابزارهای خارجی مانند "Cloud Code" است. تابع `_emit_runner_notification` (خطوط 35-82) و endpointهای `claim_prompt` (خطوط 253-304) و `complete_prompt` (خطوط 340-435) در این فایل، جریان کاری Claude Auto-Runner را پشتیبانی می‌کنند. فایل `.github/workflows/claude-auto-task.yml` نیز نحوهٔ فعال‌سازی و اجرای تسک‌ها توسط Claude Code را پس از push شدن به فولدر `prompt/` توضیح می‌دهد. بنابراین، منطق بک‌اند باید تسک‌ها را به گونه‌ای آماده کند که توسط این workflow قابل برداشت باشند. این تغییر نیازمند به‌روزرسانی فرانت‌اند برای نمایش گزینه، به‌روزرسانی مدل داده برای ذخیره انتخاب کاربر، و تغییر منطق سرویس بک‌اند برای هدایت پرامپت‌ها به مسیر پردازش محلی یا خارجی (Cloud Code) است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک گزینه (مثلاً Radio Button یا Dropdown) با عنوان "Prompt Processing Engine" در "تب بازرس ویژه" در `frontend/src/app/oversight/page.tsx` قابل مشاهده باشد.
- [ ] گزینه "Local AI" به صورت پیش‌فرض در UI انتخاب شده باشد.
- [ ] پس از انتخاب "Cloud Code" در UI و شروع یک جلسه بازرسی، این انتخاب در دیتابیس برای `InspectorSession` مربوطه ذخیره شود.
- [ ] اگر "Cloud Code" انتخاب شود، `inspector_agent` باید یک `OversightTask` با `external_status='pending'` و `status='pending'` ایجاد کند و آن را برای همگام‌سازی با GitHub برنامه‌ریزی کند.
- [ ] اگر "Local AI" انتخاب شود، پردازش پرامپت باید از طریق `ai_manager` و مدل‌های محلی انجام شود (رفتار فعلی حفظ شود).
- [ ] پس از ارسال یک تسک برای "Cloud Code"، workflow `claude-auto-task.yml` در GitHub Actions باید trigger شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی این قابلیت، مراحل زیر پیشنهاد می‌شود:

1.  **Frontend (`frontend/src/app/oversight/page.tsx`):**
    *   یک کامپوننت UI جدید (مثلاً یک RadioGroup یا Dropdown) برای انتخاب "Prompt Processing Engine" اضافه شود. گزینه‌ها باید شامل "Local AI" (پیش‌فرض) و "Cloud Code" باشند.
    *   مقدار انتخاب شده باید در state کامپوننت مدیریت شود و هنگام ارسال پرامپت به بک‌اند، به عنوان یک پارامتر (مثلاً `prompt_engine`) در payload درخواست گنجانده شود.

2.  **Backend - Model (`backend/app/models/inspector_session.py`):**
    *   فیلد جدیدی به `InspectorSession` (و `InspectorSessionSchema`) اضافه شود، مثلاً `prompt_engine: str = Field(default="local_ai", description="موتور پردازش پرامپت: 'local_ai' یا 'cloud_code'")`.

3.  **Backend - API Route (`backend/app/api/routes/oversight.py`):**
    *   endpoint مربوط به شروع یک جلسه بازرسی یا پردازش پرامپت (احتمالاً یک POST endpoint) باید به‌روزرسانی شود تا پارامتر `prompt_engine` را در `request.body` بپذیرد.

4.  **Backend - Service Logic (`backend/app/services/inspector_agent.py`):**
    *   در متد اصلی پردازش پرامپت، بر اساس مقدار `prompt_engine`:
        *   اگر `prompt_engine == "local_ai"`: از منطق موجود برای پردازش با `ai_manager` استفاده شود.
        *   اگر `prompt_engine == "cloud_code"`: یک `OversightTask` جدید ایجاد یا به‌روزرسانی شود. `task.external_status` به `"pending"` و `task.status` به `"pending"` تنظیم شود. `task.external_locked_by` و `task.external_lease_until` باید `None` باشند. سپس `oversight_service._schedule_prompt_sync(task, rebuild_index=True)` فراخوانی شود تا تسک به GitHub push شده و workflow `claude-auto-task.yml` را trigger کند. پاسخ به فرانت‌اند باید شامل اطلاعاتی باشد که نشان دهد تسک برای پردازش خارجی ارسال شده است.

5.  **Backend - Oversight Service (`backend/app/services/oversight_service.py`):**
    *   اطمینان حاصل شود که `_schedule_prompt_sync` و منطق مربوط به `external_status` به درستی کار می‌کنند و تسک‌های جدید را برای Claude Auto-Runner قابل برداشت می‌کنند.

## 💡 نمونه‌های قبل/بعد
**Backend - `InspectorSession` Model (Conceptual)**

_قبل:_
```
class InspectorSession(BaseModel):
    id: str
    project_id: str
    # ... other fields
```

_بعد:_
```
class InspectorSession(BaseModel):
    id: str
    project_id: str
    prompt_engine: str = Field(default="local_ai", description="موتور پردازش پرامپت: 'local_ai' یا 'cloud_code'")
    # ... other fields
```

**Frontend - `oversight/page.tsx` UI (Conceptual)**

_قبل:_
```
// ... existing UI elements
<button onClick={handleProcessPrompt}>Process Prompt</button>
```

_بعد:_
```
// ... existing UI elements
<div className="flex items-center space-x-4 mb-4">
  <label className="inline-flex items-center">
    <input type="radio" name="promptEngine" value="local_ai" checked={selectedEngine === 'local_ai'} onChange={handleEngineChange} className="form-radio" />
    <span className="ml-2">Local AI</span>
  </label>
  <label className="inline-flex items-center">
    <input type="radio" name="promptEngine" value="cloud_code" checked={selectedEngine === 'cloud_code'} onChange={handleEngineChange} className="form-radio" />
    <span className="ml-2">Cloud Code</span>
  </label>
</div>
<button onClick={handleProcessPrompt}>Process Prompt</button>
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/`
- `npm run lint`
- `npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **Race Conditions in Task Claiming:** اگر چندین `claude-auto-task.yml` workflow همزمان trigger شوند یا `external_prompts/next` به درستی lock نکند، ممکن است چندین runner یک تسک را claim کنند. `external_prompts.py` (خطوط 274-278) دارای منطق `already_claimed` است، اما باید اطمینان حاصل شود که `_schedule_prompt_sync` و `trigger_workflow_dispatch` به درستی با `concurrency` در `claude-auto-task.yml` (خطوط 30-33) هماهنگ هستند.
2.  **Data Consistency:** اطمینان از اینکه `InspectorSession` و `OversightTask` به درستی همگام‌سازی می‌شوند و وضعیت‌ها در طول چرخه حیات تسک (pending, claimed, in_progress, done, failed) به درستی به‌روزرسانی می‌شوند.
3.  **Performance Impact:** افزایش تعداد pushها به GitHub برای همگام‌سازی تسک‌ها ممکن است بر عملکرد کلی سیستم یا محدودیت‌های API گیت‌هاب تأثیر بگذارد، هرچند `_schedule_prompt_sync` دارای debounce است.
4.  **Frontend/Backend API Mismatch:** هرگونه تغییر در payload API در `backend/app/api/routes/oversight.py` باید به دقت در `frontend/src/app/oversight/page.tsx` منعکس شود تا از خطاهای ارتباطی جلوگیری شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: به‌روزرسانی منطق ارسال درخواست چت در فرانت‌اند

**Scope:** در این مرحله، منطق ارسال درخواست چت در فرانت‌اند 'تب بازرس ویژه' اصلاح می‌شود. این اصلاح شامل افزودن پارامتری به درخواست‌های ارسالی به بک‌اند است که نشان‌دهنده انتخاب کاربر (Cloud Code یا مدل‌های فعلی) باشد. همچنین، در صورت انتخاب Cloud Code، درخواست باید به Endpoint جدید (مرحله ۳) ارسال شود، در حالی که برای مدل‌های فعلی، به Endpointهای موجود ارسال خواهد شد.
**Key terms:** منطق ارسال درخواست چت, فرانت‌اند, تب بازرس ویژه, پارامتر انتخاب, Endpoint جدید, Endpointهای موجود

**بخش مربوط از متن کاربر:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن انتخاب Cloud Code به چت بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/oversight.py` — `router` — این Endpoint جدید برای دریافت درخواست‌های چت از فرانت‌اند در حالت 'Cloud Code' است. باید از `require_external_token` برای احراز هویت استفاده کند.
  ```python
  @router.post("/chat-with-cloud-code", response_model=Dict[str, Any])
  async def chat_with_cloud_code(
      request: CloudCodeChatRequest,
      x_external_token: Optional[str] = Header(default=None, alias="X-External-Token"),
      # ... other dependencies like Depends(require_external_token) ...
  ):
      # Implementation to create and potentially claim task
      pass
  ```
- `backend/app/services/oversight_service.py` — `OversightService` — متد جدیدی در `OversightService` برای کپسوله کردن منطق ایجاد `OversightTask` برای چت Cloud Code.
  ```python
  class OversightService:
      # ... existing methods ...
  
      def create_chat_task_for_cloud_code(self, project_id: str, chat_message: str) -> Any:
          """
          Creates a new OversightTask specifically for Cloud Code chat.
          """
          new_task = OversightTask(
              id=str(uuid.uuid4()),
              project_id=project_id,
              title=f"Cloud Code Chat: {chat_message[:50]}...",
              prompt=chat_message,
              status="pending",
              external_status="pending",
              type="chat_interaction",
              created_at=now_iso(),
              updated_at=now_iso(),
              execution_priority=50,
          )
          self.tasks.append(new_task)
          self._save_tasks()
          self._recompute_execution_priorities(new_task)
          self._schedule_prompt_sync(new_task, rebuild_index=True)
          return new_task
  ```
- `frontend/src/app/oversight/page.tsx` — `InspectorPage` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این snippet نشان‌دهنده تغییرات UI و منطق ارسال پیام در فرانت‌اند است.
  ```tsx
  const [chatMode, setChatMode] = useState<'current_models' | 'cloud_code'>('current_models');
  
  const handleSendMessage = async (message: string) => {
      if (chatMode === 'cloud_code') {
          await axios.post('/api/oversight/chat-with-cloud-code', {
              project_id: currentProjectId,
              message: message,
          }, {
              headers: { 'X-External-Token': 'YOUR_CLOUD_CODE_TOKEN' }
          });
      } else {
          await axios.post('/api/chat', {
              project_id: currentProjectId,
              message: message,
              model_id: selectedModel,
          });
      }
  };
  
  return (
      <div>
          {/* ... existing inspector UI ... */}
          <div className="chat-mode-selector">
              <label><input type="radio" value="current_models" checked={chatMode === 'current_models'} onChange={() => setChatMode('current_models')} /> Current Models</label>
              <label><input type="radio" value="cloud_code" checked={chatMode === 'cloud_code'} onChange={() => setChatMode('cloud_code')} /> Cloud Code</label>
          </div>
          {/* ... chat input and display ... */}
      </div>
  );
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
بک‌اند با FastAPI و پایتون توسعه یافته است و از Pydantic برای اعتبارسنجی داده‌ها و SQLAlchemy برای تعامل با دیتابیس (SQLite) استفاده می‌کند. فرانت‌اند از Next.js و React استفاده می‌کند، با Axios برای درخواست‌های HTTP و Zustand برای مدیریت وضعیت. یکپارچه‌سازی 'Cloud Code' از GitHub Actions workflows (همانطور که در `.github/workflows/claude-auto-task.yml` دیده می‌شود) و یک API اختصاصی برای پرامپت‌های خارجی (`backend/app/api/routes/external_prompts.py`) برای مدیریت و اجرای تسک‌ها توسط ایجنت‌های خارجی مانند Claude Code بهره می‌برد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/external_prompts.py` (سطر 35) — این فایل مکانیزم‌های اصلی برای تعامل با ابزارهای خارجی مانند Claude Auto-Runner را تعریف می‌کند. Endpoint جدید برای Cloud Code Chat باید با این مکانیزم‌ها همسو باشد و از توابع کمکی مانند `_emit_runner_notification` استفاده کند.
- `backend/app/services/notification_service.py` — توسط `external_prompts.py` (خط 51) import می‌شود. برای ارسال نوتیفیکیشن‌های مربوط به وضعیت تسک‌های Cloud Code Chat (شروع، تکمیل، شکست) به کاربر نهایی (مثلاً از طریق تلگرام) استفاده خواهد شد.
- `backend/app/services/ai_manager.py` — این سرویس مدیریت مرکزی مدل‌های AI را بر عهده دارد. مسیر 'Current Models' در فرانت‌اند همچنان از این سرویس برای تعامل با مدل‌های مختلف استفاده خواهد کرد.
- `backend/app/core/database.py` — سرویس `oversight_service` برای ذخیره `OversightTask` جدید در دیتابیس به `SessionLocal` از این فایل وابسته است.
- `.github/workflows/claude-auto-task.yml` — این فایل GitHub Actions workflow برای Claude Auto Task Runner است. تسک‌های جدیدی که توسط Endpoint چت Cloud Code ایجاد می‌شوند، باید توسط این workflow قابل برداشت و اجرا باشند. این امر مستلزم آن است که `status` و `external_status` تسک‌ها با `PICKABLE_STATUSES` در `prompt_github_sync.py` همخوانی داشته باشند.

## 🌐 نقشهٔ وابستگی‌ها
این ویژگی جدید یک جریان تعامل چت را معرفی می‌کند. کامپوننت فرانت‌اند، احتمالاً `frontend/src/app/oversight/page.tsx`، نقطه ورود خواهد بود و ورودی کاربر را بر اساس یک `chatMode` جدید مسیریابی می‌کند. برای حالت 'Cloud Code'، این کامپوننت یک Endpoint جدید در `backend/app/api/routes/oversight.py` (مثلاً `/oversight/chat-with-cloud-code`) را فراخوانی می‌کند. این Endpoint جدید به `backend/app/services/oversight_service.py` وابسته است تا یک `OversightTask` را در دیتابیس (از طریق `backend/app/core/database.py` و مدل `OversightTask` مربوطه) ایجاد و مدیریت کند. `oversight_service` سپس مکانیزم `prompt_github_sync` (که توسط `backend/app/services/prompt_github_sync.py` مدیریت می‌شود) را برای در دسترس قرار دادن تسک برای رانرهای خارجی فعال می‌کند. ماژول `backend/app/api/routes/external_prompts.py`، به ویژه تابع `_emit_runner_notification` آن، برای ارائه بازخورد در مورد چرخه عمر تسک Cloud Code مرتبط خواهد بود. برای حالت 'Current Models'، فرانت‌اند به تعامل با Endpointهای چت موجود ادامه می‌دهد که به نوبه خود به `backend/app/services/ai_manager.py` برای هماهنگی با ارائه‌دهندگان مختلف AI متکی هستند.

## 🔍 Context و وضعیت فعلی
کاربر درخواست به‌روزرسانی منطق ارسال درخواست چت در فرانت‌اند 'تب بازرس ویژه' را دارد. این اصلاح شامل افزودن پارامتری به درخواست‌های ارسالی به بک‌اند است که نشان‌دهنده انتخاب کاربر (Cloud Code یا مدل‌های فعلی) باشد. همچنین، در صورت انتخاب Cloud Code، درخواست باید به Endpoint جدید (مرحله ۳) ارسال شود، در حالی که برای مدل‌های فعلی، به Endpointهای موجود ارسال خواهد شد.

کاربر به صراحت بیان می‌کند: "[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه." این نشان می‌دهد که انتخاب 'Cloud Code' باید از 'توکن کلاد کد' و 'آخرین ایجنت‌هایی که مال اون کلاد کد هست' استفاده کند.

**تحلیل و شواهد در کد:**
1.  **فرانت‌اند ('تب بازرس ویژه'):** با توجه به ساختار پروژه و سرویس‌های مرتبط با `oversight` در بک‌اند، منطقی است که 'تب بازرس ویژه' در `frontend/src/app/oversight/page.tsx` پیاده‌سازی شده باشد. این فایل نیاز به تغییرات UI برای افزودن انتخاب 'Cloud Code' و 'مدل‌های فعلی' و همچنین تغییر در منطق ارسال درخواست چت دارد.
2.  **بک‌اند (Endpoint جدید برای Cloud Code):** برای پشتیبانی از 'Cloud Code'، نیاز به یک Endpoint جدید در بک‌اند داریم. فایل `backend/app/api/routes/external_prompts.py` مسئول مدیریت تعامل با ابزارهای خارجی مانند 'Claude Auto-Runner' است (خطوط 35-48 و 395-435 به `_emit_runner_notification` و `_verify_then_chain` اشاره دارند که با Claude Auto-Runner کار می‌کنند). این Endpoint جدید احتمالاً در `backend/app/api/routes/oversight.py` (که با `oversight_service` کار می‌کند) یا `backend/app/api/routes/external_prompts.py` اضافه خواهد شد و وظیفه ایجاد یک `OversightTask` جدید را بر عهده خواهد داشت که توسط Claude Code قابل برداشت باشد.
3.  **بک‌اند (Endpointهای موجود برای مدل‌های فعلی):** برای 'مدل‌های فعلی'، درخواست‌ها باید به Endpointهای موجود ارسال شوند. با توجه به ساختار پروژه، `backend/app/api/routes/chat.py` (که در لیست فایل‌ها موجود است) محتمل‌ترین مکان برای این Endpointها است که از `backend/app/services/ai_manager.py` برای مدیریت مدل‌ها استفاده می‌کند.
4.  **سرویس `oversight_service`:** سرویس `backend/app/services/oversight_service.py` (که توسط `external_prompts.py` در خط 21 import شده) مسئول مدیریت تسک‌ها است. Endpoint جدید بک‌اند برای Cloud Code Chat باید از این سرویس برای ایجاد `OversightTask` استفاده کند.
5.  **احراز هویت:** `backend/app/api/routes/external_prompts.py` از `X-External-Token` (خطوط 91-99) برای احراز هویت ابزارهای خارجی استفاده می‌کند. Endpoint جدید نیز باید این مکانیزم را رعایت کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تب بازرس ویژه در فرانت‌اند باید یک عنصر UI (مانند Radio Button یا Dropdown) برای انتخاب بین 'Cloud Code' و 'Current Models' برای تعاملات چت نمایش دهد.
- [ ] یک Endpoint جدید POST به نام `/api/oversight/chat-with-cloud-code` در `backend/app/api/routes/oversight.py` باید وجود داشته باشد که `project_id` و `message` را بپذیرد و یک وضعیت موفقیت‌آمیز همراه با `task_id` ایجاد شده را برگرداند. این Endpoint باید از `X-External-Token` برای احراز هویت استفاده کند.
- [ ] هنگامی که یک پیام چت از طریق گزینه 'Cloud Code' ارسال می‌شود، یک `OversightTask` جدید باید در دیتابیس ایجاد شود که فیلد `prompt` آن حاوی پیام چت، `status` آن 'pending'، `external_status` آن 'pending' و `type` آن 'chat_interaction' باشد.
- [ ] هنگامی که یک پیام چت از طریق گزینه 'Current Models' ارسال می‌شود، درخواست باید به Endpoint چت موجود (مثلاً `/api/chat`) بدون تغییر در payload یا رفتار آن مسیریابی شود.
- [ ] Endpoint جدید `/api/oversight/chat-with-cloud-code` باید درخواست‌های بدون `X-External-Token` معتبر را با کد وضعیت 401 رد کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **بک‌اند - تعریف مدل درخواست:** یک مدل Pydantic جدید (مثلاً `CloudCodeChatRequest`) در `backend/app/api/routes/oversight.py` یا یک فایل مدل مرتبط تعریف شود که شامل `project_id` و `message` باشد.
2.  **بک‌اند - ایجاد Endpoint جدید (`backend/app/api/routes/oversight.py`):**
    a.  یک POST endpoint جدید به نام `/oversight/chat-with-cloud-code` اضافه شود.
    b.  این Endpoint باید `CloudCodeChatRequest` را دریافت کند و از `require_external_token` (از `backend/app/api/routes/external_prompts.py`) برای احراز هویت استفاده کند.
    c.  درون این Endpoint، با استفاده از `get_oversight_service()`، یک متد جدید (مثلاً `create_chat_task_for_cloud_code`) فراخوانی شود تا یک `OversightTask` جدید با `prompt` حاوی پیام چت کاربر، `status='pending'`، `external_status='pending'` و `type='chat_interaction'` ایجاد و ذخیره شود.
    d.  این Endpoint باید `task_id` تسک ایجاد شده را برگرداند.
3.  **بک‌اند - افزودن متد به `oversight_service.py`:**
    a.  یک متد جدید به کلاس `OversightService` در `backend/app/services/oversight_service.py` اضافه شود (مثلاً `create_chat_task_for_cloud_code(self, project_id: str, chat_message: str)`).
    b.  این متد مسئول ساخت و ذخیره `OversightTask` جدید در دیتابیس و فراخوانی `_recompute_execution_priorities` و `_schedule_prompt_sync` برای اطمینان از قابل برداشت بودن تسک توسط Claude Auto-Runner باشد.
4.  **فرانت‌اند - تغییرات UI (`frontend/src/app/oversight/page.tsx` - فرض شده):**
    a.  یک عنصر UI (مانند Radio Button یا Dropdown) برای انتخاب بین 'Cloud Code' و 'Current Models' به 'تب بازرس ویژه' اضافه شود.
    b.  یک `state` جدید (مثلاً `chatMode`) برای نگهداری انتخاب کاربر اضافه شود.
5.  **فرانت‌اند - تغییر منطق ارسال چت (`frontend/src/app/oversight/page.tsx` - فرض شده):**
    a.  تابع ارسال پیام چت (مثلاً `handleSendMessage`) اصلاح شود تا بر اساس مقدار `chatMode`، درخواست را به Endpoint صحیح ارسال کند.
    b.  اگر `chatMode` برابر 'cloud_code' بود، درخواست Axios به `/api/oversight/chat-with-cloud-code` ارسال شود (با `X-External-Token` در هدر).
    c.  اگر `chatMode` برابر 'current_models' بود، درخواست به Endpoint موجود چت (مثلاً `/api/chat`) ارسال شود.

## 💡 نمونه‌های قبل/بعد
**منطق ارسال پیام چت در فرانت‌اند (conceptual)**

_قبل:_
```
// frontend/src/app/oversight/page.tsx (conceptual)
const handleSendMessage = async (message: string) => {
    // Always sends to the default chat endpoint
    await axios.post('/api/chat', {
        project_id: currentProjectId,
        message: message,
        model_id: selectedModel,
    });
    // ... update chat history ...
};
```

_بعد:_
```
// frontend/src/app/oversight/page.tsx (conceptual)
const [chatMode, setChatMode] = useState<'current_models' | 'cloud_code'>('current_models');

const handleSendMessage = async (message: string) => {
    if (chatMode === 'cloud_code') {
        // New logic: Send to Cloud Code specific endpoint
        await axios.post('/api/oversight/chat-with-cloud-code', {
            project_id: currentProjectId,
            message: message,
        }, {
            headers: { 'X-External-Token': 'YOUR_CLOUD_CODE_TOKEN' } // Needs secure handling
        });
    } else {
        // Existing logic: Send to current models chat endpoint
        await axios.post('/api/chat', {
            project_id: currentProjectId,
            message: message,
            model_id: selectedModel,
        });
    }
    // ... update chat history ...
};
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_oversight_chat_task.py`
- `npm run test -- frontend/src/app/oversight/page.test.tsx`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **امنیت `X-External-Token`**: ارسال مستقیم `X-External-Token` از فرانت‌اند (در `frontend/src/app/oversight/page.tsx`) یک ریسک امنیتی است زیرا توکن می‌تواند در مرورگر کاربر افشا شود. باید مکانیزم امن‌تری مانند پروکسی کردن درخواست از طریق یک Endpoint بک‌اند که توکن را به صورت داخلی اضافه می‌کند، در نظر گرفته شود.
2.  **بارگذاری `oversight_service`**: تعاملات چت مکرر از طریق Cloud Code می‌تواند منجر به ایجاد تعداد زیادی `OversightTask` شود. این امر ممکن است بر عملکرد `oversight_service._save_tasks()` و `_recompute_execution_priorities()` در `backend/app/services/oversight_service.py` تأثیر بگذارد، به خصوص اگر حجم بالایی از درخواست‌ها وجود داشته باشد.
3.  **تجربه کاربری چت Cloud Code**: از آنجایی که تسک‌های Cloud Code ماهیت ناهمزمان دارند و شامل GitHub Actions (همانطور که در `.github/workflows/claude-auto-task.yml` تعریف شده) می‌شوند، تجربه چت ممکن است کندتر از فراخوانی‌های مستقیم API به 'Current Models' باشد. فرانت‌اند باید ماهیت ناهمزمان را به وضوح به کاربر اطلاع دهد و نشانگرهای بارگذاری/وضعیت مناسبی را ارائه کند.
4.  **همگام‌سازی با Claude Auto-Runner**: تسک‌های چت جدید باید به درستی توسط workflow 'Claude Auto Task Runner' در `.github/workflows/claude-auto-task.yml` شناسایی و پردازش شوند. این امر مستلزم آن است که `status` و `external_status` تسک‌ها با `PICKABLE_STATUSES` و `PICKABLE_EXTERNAL_STATUSES` در `backend/app/services/prompt_github_sync.py` همخوانی داشته باشند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: تست‌های واحد و یکپارچه‌سازی بک‌اند

**Scope:** این مرحله شامل نوشتن و اجرای تست‌های واحد (Unit Tests) برای ماژول دسترسی به توکن Cloud Code، سرویس تعامل با ایجنت‌های Cloud Code و Endpointهای جدید API است. همچنین، تست‌های یکپارچه‌سازی (Integration Tests) برای تأیید عملکرد صحیح مسیریابی درخواست‌ها در بک‌اند و ارتباط بین سرویس‌های مختلف انجام می‌شود. هدف اطمینان از صحت، امنیت و پایداری اجزای بک‌اند است.
**Key terms:** تست‌های واحد, تست‌های یکپارچه‌سازی, ماژول دسترسی به توکن, سرویس تعامل با ایجنت‌ها, Endpointهای API, مسیریابی درخواست‌ها

**بخش مربوط از متن کاربر:**
```
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تست‌های واحد و یکپارچه‌سازی برای Cloud Code Runner API

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/external_prompts.py:91-99` — `_check_external_token` — این تابع مسئول اعتبارسنجی توکن `X-External-Token` است که توسط ابزارهای خارجی مانند Cloud Code برای دسترسی به API استفاده می‌شود. تست‌های واحد برای این بخش حیاتی هستند.
  ```python
  def _check_external_token(provided: Optional[str]) -> None:
      expected = os.environ.get("EXTERNAL_TOOL_TOKEN", "").strip()
      if not expected:
          raise HTTPException(
              status_code=503,
              detail="EXTERNAL_TOOL_TOKEN env var تنظیم نشده — endpoint غیرفعال است.",
          )
      if not provided or provided.strip() != expected:
          raise HTTPException(status_code=401, detail="Invalid X-External-Token")
  ```
- `backend/app/api/routes/external_prompts.py:253-304` — `claim_prompt` — این Endpoint اصلی برای گرفتن قفل یک تسک توسط یک ایجنت خارجی است. تست‌های یکپارچه‌سازی برای این Endpoint باید سناریوهای مختلف قفل‌گذاری و تمدید lease را پوشش دهند.
  ```python
  @router.post("/{task_id}/claim", dependencies=[Depends(require_external_token)])
  async def claim_prompt(task_id: str, payload: ClaimRequest):
      """قفل گرفتن یک تسک برای اجرا (با lease).
  
      اگر تسک قبلاً claim شده و lease معتبر دارد، 409 برمی‌گرداند مگر اینکه
      همان agent_id باشد (renewal).
      """
      service = get_oversight_service()
      async with service._lock:
          t = next((x for x in service.tasks if x.id == task_id), None)
          if not t:
              raise HTTPException(status_code=404, detail="task not found")
          if t.archived:
              raise HTTPException(status_code=409, detail="task archived")
          if t.status not in PICKABLE_STATUSES:
              raise HTTPException(
                  status_code=409,
                  detail=f"task status '{t.status}' not pickable",
              )
          cur_status = getattr(t, "external_status", "pending") or "pending"
          cur_lock = getattr(t, "external_locked_by", None)
          if (
              cur_status in ("claimed", "in_progress")
              and not _is_lease_expired(t)
              and cur_lock != payload.agent_id
          ):
              raise HTTPException(
                  status_code=409,
                  detail={
                      "error": "already_claimed",
                      "locked_by": cur_lock,
                      "lease_until": t.external_lease_until,
                  },
              )
          lease_until = (
              datetime.now(timezone.utc) + timedelta(minutes=payload.lease_minutes)
          ).isoformat()
          t.external_status = "claimed"
          t.external_locked_by = payload.agent_id
          t.external_locked_at = now_iso()
          t.external_lease_until = lease_until
          t.external_attempts = int(getattr(t, "external_attempts", 0) or 0) + 1
          t.updated_at = now_iso()
          service._save_tasks()
          service._recompute_execution_priorities(t)
          service._schedule_prompt_sync(t, rebuild_index=True)
      # 🆕 Telegram feedback: 🏃 شروع
      _emit_runner_notification(
          event="external_runner_claimed", task=t, agent_id=payload.agent_id,
      )
      return {"success": True, "task": _task_summary(t), "lease_until": lease_until}
  ```
- `backend/tests/test_external_prompts_api.py` — `New Test File` — این مسیر برای ایجاد فایل تست جدید برای پوشش دادن Endpointهای Cloud Code Runner API و منطق مرتبط با آن است.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: fastapi, nextjs. این تسک عمدتاً بر روی بخش بک‌اند FastAPI متمرکز است. کتابخانه‌های مرتبط شامل `pytest` برای فریم‌ورک تست، `httpx` (یا `TestClient` از FastAPI) برای تست‌های یکپارچه‌سازی API، و `unittest.mock` برای شبیه‌سازی وابستگی‌ها هستند. همچنین، `os` برای دسترسی به متغیرهای محیطی و `datetime` برای مدیریت lease در `external_prompts.py` استفاده می‌شود.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/.env.example` (سطر 40) — متغیر محیطی `EXTERNAL_TOOL_TOKEN` که برای احراز هویت Cloud Code Runner استفاده می‌شود، در اینجا تعریف شده است و برای تست‌های احراز هویت ضروری است.
- `.github/workflows/claude-auto-task.yml` (سطر 30) — این فایل workflow نحوه تعامل Claude Auto Task Runner با Endpointهای `/external/prompts/*` را نشان می‌دهد و برای طراحی تست‌های یکپارچه‌سازی و سناریوهای واقعی استفاده از API حیاتی است.
- `backend/app/services/oversight_service.py` (سطر 21) — سرویس `oversight_service` توسط Endpointهای `external_prompts.py` برای مدیریت وضعیت تسک‌ها، قفل‌گذاری و ذخیره‌سازی استفاده می‌شود. تست‌ها باید تعامل با این سرویس را شبیه‌سازی یا تست کنند.
- `backend/app/services/notification_service.py` (سطر 51) — تابع `_emit_runner_notification` در `external_prompts.py` از این سرویس برای ارسال نوتیفیکیشن‌های تلگرام استفاده می‌کند. تست‌ها باید اطمینان حاصل کنند که این نوتیفیکیشن‌ها در زمان‌های صحیح فراخوانی می‌شوند (با mocking).
- `backend/app/services/oversight_verifier.py` (سطر 463) — این سرویس توسط تابع `_verify_then_chain` در `external_prompts.py` پس از تکمیل تسک توسط Claude فراخوانی می‌شود. تست‌ها باید تعامل با این سرویس را در جریان پس از تکمیل تسک پوشش دهند.
- `backend/app/services/claude_runner_bootstrap.py` (سطر 573) — این سرویس توسط `_verify_then_chain` برای trigger کردن workflowهای GitHub Actions پس از تکمیل یا نیاز به retry تسک استفاده می‌شود. تست‌ها باید فراخوانی صحیح این سرویس را تأیید کنند.

## 🌐 نقشهٔ وابستگی‌ها
Endpointهای `external_prompts.py` به شدت به `oversight_service.py` وابسته هستند که مسئول مدیریت و ذخیره‌سازی وضعیت تسک‌ها است. این Endpointها همچنین از `notification_service.py` برای ارسال بازخورد به کاربر (مانند تلگرام) استفاده می‌کنند. پس از تکمیل یک تسک، جریان `_verify_then_chain` آغاز می‌شود که به نوبه خود با `oversight_verifier.py` برای تأیید نتیجه و با `claude_runner_bootstrap.py` برای trigger کردن workflowهای GitHub Actions بعدی تعامل دارد. احراز هویت این Endpointها نیز به متغیر محیطی `EXTERNAL_TOOL_TOKEN` وابسته است که در `backend/.env.example` تعریف می‌شود. این وابستگی‌های متقابل نیاز به تست‌های واحد و یکپارچه‌سازی دقیق برای اطمینان از صحت جریان داده و منطق را برجسته می‌کند.

## 🔍 Context و وضعیت فعلی
این تسک بر اساس درخواست کاربر برای «تست‌های واحد و یکپارچه‌سازی بک‌اند» متمرکز است. کاربر به صراحت نیاز به نوشتن و اجرای تست‌های واحد (Unit Tests) برای «ماژول دسترسی به توکن Cloud Code»، «سرویس تعامل با ایجنت‌های Cloud Code» و «Endpointهای جدید API» را مطرح کرده است. همچنین، تأکید بر «تست‌های یکپارچه‌سازی (Integration Tests)» برای تأیید «عملکرد صحیح مسیریابی درخواست‌ها در بک‌اند» و «ارتباط بین سرویس‌های مختلف» وجود دارد. هدف اصلی این تست‌ها «اطمینان از صحت، امنیت و پایداری اجزای بک‌اند» است.

بخش مهمی از درخواست کاربر به دلیل پشت این نیاز اشاره دارد: «مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.» این نشان می‌دهد که ادغام Cloud Code یک قابلیت حیاتی و جدید است که هدف آن کاهش هزینه‌های API Key و استفاده بهینه از اشتراک ماهانه است. بنابراین، اطمینان از عملکرد صحیح این بخش از طریق تست‌های جامع، از اهمیت بالایی برخوردار است.

در کدبیس موجود، فایل `backend/app/api/routes/external_prompts.py` به وضوح به عنوان «External Prompts API» برای «ابزارهای خارجی (Cloud Code و …)» تعریف شده است. این فایل شامل منطق احراز هویت با `X-External-Token` (خطوط 91-100) و Endpointهایی مانند `/next` (خط 189)، `/claim` (خط 253)، `/progress` (خط 307)، `/complete` (خط 340) و `/fail` (خط 555) است که مستقیماً با «سرویس تعامل با ایجنت‌های Cloud Code» و «Endpointهای جدید API» مطابقت دارند. همچنین، فایل `.github/workflows/claude-auto-task.yml` نحوه استفاده از این APIها توسط Claude Auto Task Runner را نشان می‌دهد و `EXTERNAL_TOOL_TOKEN` در `backend/.env.example` تعریف شده است. این ساختار، نقاط کلیدی برای پیاده‌سازی تست‌های درخواستی کاربر را مشخص می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست‌های واحد برای تابع `_check_external_token` در `backend/app/api/routes/external_prompts.py` باید سناریوهای توکن معتبر، نامعتبر و عدم تنظیم متغیر محیطی `EXTERNAL_TOOL_TOKEN` را پوشش دهند و در هر حالت کد وضعیت HTTP صحیح را برگردانند.
- [ ] Endpoint `POST /external/prompts/{task_id}/claim` باید با توکن معتبر، تسک را با موفقیت claim کند و وضعیت `external_status` را به `claimed` تغییر دهد و `external_lease_until` را تنظیم کند. در صورت توکن نامعتبر، باید کد وضعیت 401 برگرداند.
- [ ] Endpoint `POST /external/prompts/{task_id}/complete` باید با توکن معتبر، تسک را به وضعیت `applied_externally_pending_verify` منتقل کند و `external_status` را به `done` تغییر دهد. همچنین، باید `_emit_runner_notification` را فراخوانی کند و در صورت عدم تطابق `agent_id` یا توکن نامعتبر، خطای مناسب برگرداند.
- [ ] تابع `_verify_then_chain` باید پس از تکمیل تسک، `oversight_verifier.verify_task` را فراخوانی کند و بر اساس نتیجه تأیید (done, partial, regressed)، رفتار صحیح (آزاد کردن lock، retry همان تسک، یا trigger کردن تسک بعدی) را انجام دهد.
- [ ] فایل `backend/tests/test_external_prompts_api.py` باید ایجاد شده و حداقل شامل 10 تست واحد و 5 تست یکپارچه‌سازی برای Endpointهای Cloud Code Runner باشد.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی تست‌های واحد و یکپارچه‌سازی برای Cloud Code Runner API، مراحل زیر پیشنهاد می‌شود:

1.  **ایجاد فایل تست جدید:** یک فایل تست جدید به نام `backend/tests/test_external_prompts_api.py` ایجاد شود.
2.  **تست‌های واحد برای احراز هویت:** توابع `_check_external_token` و `require_external_token` در `backend/app/api/routes/external_prompts.py` باید با استفاده از mocking برای `os.environ` تست شوند تا سناریوهای توکن معتبر، نامعتبر و تنظیم نشده پوشش داده شود.
3.  **تست‌های واحد برای منطق داخلی Endpointها:** منطق مربوط به `claim_prompt`, `report_progress`, `complete_prompt`, و `fail_prompt` در `backend/app/api/routes/external_prompts.py` باید با mocking سرویس‌های وابسته مانند `get_oversight_service()` و `_emit_runner_notification` تست شود. این شامل بررسی تغییر وضعیت تسک‌ها، مدیریت lease، و افزایش `external_attempts` است.
4.  **تست‌های یکپارچه‌سازی API:** با استفاده از `TestClient` از FastAPI، Endpointهای `/external/prompts/next`, `/external/prompts/{task_id}/claim`, `/external/prompts/{task_id}/progress`, `/external/prompts/{task_id}/complete`, و `/external/prompts/{task_id}/fail` باید تست شوند. این تست‌ها باید شامل سناریوهای موفقیت‌آمیز، توکن نامعتبر، تسک یافت نشد، تسک قبلاً claim شده، و lease منقضی شده باشد.
5.  **تست جریان `_verify_then_chain`:** تابع `_verify_then_chain` در `backend/app/api/routes/external_prompts.py` که پس از `complete_prompt` به صورت پس‌زمینه اجرا می‌شود، باید تست شود. این شامل mocking `verify_task` از `oversight_verifier.py` و `trigger_workflow_dispatch` از `claude_runner_bootstrap.py` برای تأیید رفتار صحیح در سناریوهای `done`, `partial`, `max_retries` و `regressed` است.
6.  **پوشش تست:** اطمینان حاصل شود که تست‌ها پوشش مناسبی برای خطوط کد مرتبط با Cloud Code Runner API فراهم می‌کنند.

## 💡 نمونه‌های قبل/بعد
**کد فعلی (بخشی از `_check_external_token`)**

_قبل:_
```
expected = os.environ.get("EXTERNAL_TOOL_TOKEN", "").strip()
    if not expected:
        raise HTTPException(
            status_code=503,
            detail="EXTERNAL_TOOL_TOKEN env var تنظیم نشده — endpoint غیرفعال است.",
        )
    if not provided or provided.strip() != expected:
        raise HTTPException(status_code=401, detail="Invalid X-External-Token")
```

_بعد:_
```
```python
# backend/tests/test_external_prompts_api.py

import pytest
from fastapi import HTTPException
from unittest.mock import patch
from backend.app.api.routes.external_prompts import _check_external_token

@patch('os.environ.get')
def test_check_external_token_valid(mock_getenv):
    mock_getenv.return_value = 'test_token'
    _check_external_token('test_token') # Should not raise

@patch('os.environ.get')
def test_check_external_token_invalid(mock_getenv):
    mock_getenv.return_value = 'test_token'
    with pytest.raises(HTTPException) as exc_info:
        _check_external_token('wrong_token')
    assert exc_info.value.status_code == 401

@patch('os.environ.get')
def test_check_external_token_not_set(mock_getenv):
    mock_getenv.return_value = ''
    with pytest.raises(HTTPException) as exc_info:
        _check_external_token('any_token')
    assert exc_info.value.status_code == 503
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_external_prompts_api.py`

## ⚠️ ریسک‌ها و موارد احتیاط
1. **پیچیدگی Mocking:** سرویس `oversight_service.py` دارای منطق پیچیده مدیریت وضعیت و قفل‌گذاری است. Mocking صحیح آن برای تست‌های واحد `external_prompts.py` می‌تواند چالش‌برانگیز باشد و ممکن است منجر به تست‌هایی شود که رفتار واقعی سیستم را به درستی منعکس نمی‌کنند.
2. **تست جریان‌های Async:** تابع `_verify_then_chain` یک جریان پس‌زمینه ناهمگام است. تست کردن این جریان و اطمینان از اینکه تمام فراخوانی‌های داخلی (مانند `verify_task` و `trigger_workflow_dispatch`) به درستی انجام می‌شوند، نیاز به مدیریت دقیق `asyncio` و mocking دارد.
3. **وابستگی به متغیرهای محیطی:** تست‌های احراز هویت به متغیر `EXTERNAL_TOOL_TOKEN` وابسته هستند. مدیریت این متغیر در محیط تست (مثلاً با `monkeypatch` در pytest) باید به دقت انجام شود تا از تداخل با تنظیمات واقعی جلوگیری شود.
4. **پوشش جامع سناریوها:** با توجه به اهمیت Cloud Code Runner برای کاهش هزینه‌ها، عدم پوشش کافی سناریوهای لبه (مانند lease منقضی شده، تلاش‌های ناموفق متوالی، یا خطاهای غیرمنتظره در سرویس‌های وابسته) می‌تواند منجر به مشکلات پایداری در تولید شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: large

---

# 🔹 مرحله 8: تست‌های رابط کاربری و End-to-End

**Scope:** این مرحله شامل توسعه و اجرای تست‌های رابط کاربری (UI Tests) برای تأیید نمایش صحیح و عملکرد عنصر انتخاب Cloud Code در 'تب بازرس ویژه' است. علاوه بر این، تست‌های End-to-End (E2E Tests) برای شبیه‌سازی کامل جریان کاربر از انتخاب Cloud Code، ارسال پرامپت و دریافت پاسخ از طریق سیستم کامل (فرانت‌اند و بک‌اند) انجام می‌شود. این تست‌ها باید اطمینان حاصل کنند که تجربه کاربری یکپارچه و بدون مشکل است.
**Key terms:** تست‌های رابط کاربری, تست‌های End-to-End, تب بازرس ویژه, عنصر انتخاب Cloud Code, ارسال پرامپت, دریافت پاسخ

**بخش مربوط از متن کاربر:**
```
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تست‌های UI و E2E برای عنصر انتخاب Cloud Code در تب بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/external_prompts.py:253-303` — `claim_prompt` — این endpoint برای تست‌های E2E حیاتی است زیرا نحوه claim شدن یک تسک توسط یک ابزار خارجی (مانند Cloud Code) را مدیریت می‌کند.
  ```python
  @router.post("/{task_id}/claim", dependencies=[Depends(require_external_token)])
  async def claim_prompt(task_id: str, payload: ClaimRequest):
      """قفل گرفتن یک تسک برای اجرا (با lease).
  
      اگر تسک قبلاً claim شده و lease معتبر دارد، 409 برمی‌گرداند مگر اینکه
      همان agent_id باشد (renewal).
      """
      service = get_oversight_service()
      async with service._lock:
          t = next((x for x in service.tasks if x.id == task_id), None)
          if not t:
              raise HTTPException(status_code=404, detail="task not found")
          if t.archived:
              raise HTTPException(status_code=409, detail="task archived")
          if t.status not in PICKABLE_STATUSES:
              raise HTTPException(
                  status_code=409,
                  detail=f"task status '{t.status}' not pickable",
              )
          cur_status = getattr(t, "external_status", "pending") or "pending"
          cur_lock = getattr(t, "external_locked_by", None)
          if (
              cur_status in ("claimed", "in_progress")
              and not _is_lease_expired(t)
              and cur_lock != payload.agent_id
          ):
              raise HTTPException(
                  status_code=409,
                  detail={
                      "error": "already_claimed",
                      "locked_by": cur_lock,
                      "lease_until": t.external_lease_until,
                  },
              )
          lease_until = (
              datetime.now(timezone.utc) + timedelta(minutes=payload.lease_minutes)
          ).isoformat()
          t.external_status = "claimed"
          t.external_locked_by = payload.agent_id
          t.external_locked_at = now_iso()
          t.external_lease_until = lease_until
          t.external_attempts = int(getattr(t, "external_attempts", 0) or 0) + 1
          t.updated_at = now_iso()
          service._save_tasks()
          service._recompute_execution_priorities(t)
          service._schedule_prompt_sync(t, rebuild_index=True)
      # 🆕 Telegram feedback: 🏃 شروع
      _emit_runner_notification(
          event="external_runner_claimed", task=t, agent_id=payload.agent_id,
      )
      return {"success": True, "task": _task_summary(t), "lease_until": lease_until}
  ```
- `backend/app/api/routes/external_prompts.py:340-435` — `complete_prompt` — این endpoint تکمیل تسک توسط ابزار خارجی را گزارش می‌دهد و جریان `_verify_then_chain` را آغاز می‌کند که بخش مهمی از تست‌های E2E است.
  ```python
  @router.post("/{task_id}/complete", dependencies=[Depends(require_external_token)])
  async def complete_prompt(task_id: str, payload: CompleteRequest):
      """گزارش تکمیل توسط ابزار خارجی.
  
      این endpoint تسک را به‌صورت "applied_externally_pending_verify" علامت می‌زند.
      تأیید نهایی فقط با verify انجام می‌شود (نقش EXTERNAL_TOOL نمی‌تواند آرشیو کند).
      """
      service = get_oversight_service()
      async with service._lock:
          t = next((x for x in service.tasks if x.id == task_id), None)
          if not t:
              raise HTTPException(status_code=404, detail="task not found")
          if t.external_locked_by != payload.agent_id:
              raise HTTPException(
                  status_code=403,
                  detail="agent_id mismatch — only the claimant can complete",
              )
          t.external_status = "done"
          t.external_locked_by = None
          t.external_lease_until = None
          t.external_last_error = None
          # علامت applied — برای ورود به جریان verify
          t.verification_status = "applied_externally_pending_verify"
          t.manually_marked_applied_at = now_iso()
          if payload.summary:
              t.last_summary = payload.summary[:5000]
          if payload.evidence:
              try:
                  t.applied_evidence = {
                      **(t.applied_evidence or {}),
                      "external_tool": payload.evidence,
                      "agent_id": payload.agent_id,
                      "completed_at": now_iso(),
                  }
              except Exception:
                  pass
          t.updated_at = now_iso()
          service._save_tasks()
          service._recompute_execution_priorities(t)
          service._schedule_prompt_sync(t, rebuild_index=True)
      # 🆕 Telegram feedback: ✅ تکمیل
      _emit_runner_notification(
          event="external_runner_completed",
          task=t,
          agent_id=payload.agent_id,
          extra=(payload.summary or "")[:500],
      )
      # 🤖🔬 (Claude Auto-Runner — verify-then-chain) — به‌جای chain-next مستقیم،
      # **اول verify می‌زنیم** روی همین تسک. تا تمام شدن verify، lock فعال
      # می‌شود تا هیچ workflow دیگری برای این watched trigger نشود (حتی اگر
      # فولدر prompt/ تغییر کند). بعد از verify، بسته به نتیجه:
      #   - done → auto-archive (در خود verifier) + chain-next
      #   - partial و retries مانده → re-trigger همان تسک (auto-loop)
      #   - max retries → TODO file + chain-next
      if (
          (payload.agent_id or "").lower() in (
              "claude-code-action", "claude-runner", "claude-auto-task"
          )
          and getattr(t, "watched_id", None)
      ):
          try:
              watched = service._find_watched(t.watched_id)
              # 🆕 (manual single-task) — verify بعد از complete باید بدون توجه به
              # claude_runner_enabled اجرا شود (تنها شرط: workflow نصب شده باشد).
              # اگر کاربر manual single-task trigger زده، انتظار دارد همان موقع
              # verify شود و در صورت partial، Claude دوباره روی همان تسک کار کند.
              if (
                  watched is not None
                  and getattr(watched, "claude_runner_workflow_path", None)
              ):
                  # acquire verify lock — تا تمام شدن verify، تسک بعدی شروع نشود
                  if service._acquire_verify_lock(watched.id, t.id):
                      service._save_watched()
                      logger.info(
                          f"verify-after-complete: locked watched={watched.id} "
                          f"on task={t.id} — scheduling verify"
                      )
                      asyncio.create_task(
                          _verify_then_chain(
                              task_id=t.id,
                              watched_id=watched.id,
                              agent_id=payload.agent_id,
                          )
                      )
                  else:
                      logger.warning(
                          f"verify-after-complete: could not acquire lock for "
                          f"watched={watched.id} task={t.id} (already locked)"
                      )
              except Exception as _vfy_e:
                  logger.warning(
                      f"verify-after-complete scheduling failed for task {t.id}: "
                      f"{_vfy_e}"
                  )
      return {"success": True, "task": _task_summary(t)}
  ```
- `frontend/src/app/oversight/page.tsx` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این صفحه فرانت‌اند مربوط به 'تب بازرس ویژه' است که عنصر UI مورد نظر در آن قرار خواهد گرفت و تست‌های UI آن را هدف قرار می‌دهند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
پروژه از `fastapi` برای بک‌اند و `nextjs` برای فرانت‌اند استفاده می‌کند. تست‌ها از `pytest` برای تست‌های E2E بک‌اند و `Playwright` (همانطور که در `requirements.txt` مشخص شده) برای تست‌های UI فرانت‌اند و تست‌های E2E بین‌بخشی بهره خواهند برد.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` (سطر 21) — این سرویس وظیفه مدیریت تسک‌ها و وضعیت‌های آن‌ها را بر عهده دارد که توسط `external_prompts.py` دستکاری و در جریان E2E تأیید می‌شوند.
- `backend/app/services/oversight_verifier.py` — تابع `_verify_then_chain` در `external_prompts.py` (خط 463) از `verify_task` این سرویس برای تأیید عمیق تسک‌ها استفاده می‌کند که جزء لاینفک جریان E2E است.
- `backend/app/services/claude_runner_bootstrap.py` — تابع `_verify_then_chain` در `external_prompts.py` (خط 572) از `trigger_workflow_dispatch` این سرویس برای فعال‌سازی workflow بعدی GitHub Actions استفاده می‌کند که بخش نهایی جریان E2E است.
- `.github/workflows/claude-auto-task.yml` — این فایل GitHub Actions workflow نحوه عملکرد Claude Auto Task Runner (Cloud Code) را تعریف می‌کند، از جمله `timeout-minutes` و نحوه استفاده از `target_task_id`. تست‌های E2E باید به طور ضمنی اعتبار سنجی کنند که تعامل بک‌اند با انتظارات این workflow همسو است.
- `requirements.txt` (سطر 26) — شامل `playwright` (خط 26) است که فریم‌ورک انتخابی برای تست‌های UI و E2E فرانت‌اند و بک‌اند است.

## 🌐 نقشهٔ وابستگی‌ها
عنصر انتخاب Cloud Code در فرانت‌اند (صفحه `frontend/src/app/oversight/page.tsx`) با endpointهای API در `backend/app/api/routes/external_prompts.py` تعامل خواهد داشت، به ویژه `/claim` و `/complete`. این endpointها به نوبه خود به `backend/app/services/oversight_service.py` برای مدیریت و پایداری وضعیت تسک‌ها وابسته هستند. endpoint `complete_prompt` جریان ناهمزمان `_verify_then_chain` را فعال می‌کند که از `backend/app/services/oversight_verifier.py` برای تأیید عمیق و از `backend/app/services/claude_runner_bootstrap.py` برای ارسال workflowهای GitHub Actions بعدی استفاده می‌کند. کل جریان E2E به تعامل بین UI فرانت‌اند، API `external_prompts` و مدیریت وضعیت تسک توسط `oversight_service` وابسته است که در نهایت به منطق `_verify_then_chain` ختم می‌شود.

## 🔍 Context و وضعیت فعلی
این تسک بر توسعه و اجرای تست‌های رابط کاربری (UI Tests) و End-to-End (E2E Tests) برای تأیید عملکرد صحیح عنصر انتخاب Cloud Code در 'تب بازرس ویژه' تمرکز دارد. هدف اصلی اطمینان از نمایش صحیح و عملکرد این عنصر در فرانت‌اند و همچنین شبیه‌سازی کامل جریان کاربر از انتخاب Cloud Code، ارسال پرامپت و دریافت پاسخ از طریق سیستم کامل (شامل فرانت‌اند و بک‌اند) است. این تست‌ها باید تجربه کاربری یکپارچه و بدون مشکل را تضمین کنند.

کاربر به طور خاص به یک سناریوی مربوط به "[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه." اشاره کرده است. این نشان می‌دهد که عنصر انتخاب Cloud Code ممکن است شامل گزینه‌هایی برای تنظیم محدودیت‌های زمانی یا استراتژی‌های اجرای خاص برای Claude Auto-Runner باشد. تست‌های E2E باید شامل سناریوهایی باشند که این گزینه‌ها را پوشش دهند و تأیید کنند که بک‌اند (مسیرهای `backend/app/api/routes/external_prompts.py`) این تنظیمات را به درستی دریافت و پردازش می‌کند.

شواهد در کد نشان می‌دهد که سیستم دارای یک زیرساخت قوی برای تعامل با ابزارهای خارجی مانند Claude Auto-Runner است. فایل `backend/app/api/routes/external_prompts.py` شامل endpointهایی برای `list_next_prompts`, `get_prompt`, `claim_prompt`, `report_progress`, `complete_prompt`, و `fail_prompt` است که توسط ابزارهای خارجی برای مدیریت تسک‌ها استفاده می‌شوند. همچنین، `_emit_runner_notification` (خطوط 35-85) و `_verify_then_chain` (خطوط 441-586) در همین فایل، جریان پس از تکمیل تسک توسط Claude Auto-Runner را مدیریت می‌کنند. فایل `backend/app/services/oversight_service.py` (که توسط `external_prompts.py` در خط 21 import شده) وظیفه مدیریت تسک‌ها و وضعیت‌های آن‌ها را بر عهده دارد. در فرانت‌اند، `frontend/src/app/oversight/page.tsx` صفحه مربوط به "تب بازرس ویژه" است که عنصر UI مورد نظر در آن قرار خواهد گرفت. وجود `playwright` در `requirements.txt` (خط 26) و فایل‌های تست موجود در `backend/tests/` (مانند `test_runtime_verify_integration.py`) نشان‌دهنده آمادگی پروژه برای تست‌های UI و E2E است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] یک تست UI جدید باید وجود "عنصر انتخاب Cloud Code" را در صفحه `/oversight` تأیید کند.
- [ ] یک تست UI جدید باید قابلیت انتخاب گزینه‌های مختلف (از جمله گزینه مربوط به "[02:00] لیمیت زمان") و تأیید تغییر وضعیت UI را پوشش دهد.
- [ ] یک تست E2E باید جریان کامل انتخاب Cloud Code، ارسال پرامپت، claim شدن تسک توسط یک agent شبیه‌سازی شده، و تکمیل موفقیت‌آمیز آن را از طریق APIهای `external_prompts` (مانند `claim_prompt` و `complete_prompt` در `backend/app/api/routes/external_prompts.py`) شبیه‌سازی و تأیید کند که وضعیت تسک به درستی به `applied_externally_pending_verify` و سپس به `done` تغییر می‌کند.
- [ ] یک تست E2E باید سناریوی شکست در اجرای Cloud Code را شبیه‌سازی کند (فراخوانی `fail_prompt` در `backend/app/api/routes/external_prompts.py`) و تأیید کند که وضعیت تسک به `failed` یا `pending` (در صورت `retry=True`) تغییر می‌کند و `external_last_error` ثبت می‌شود.
- [ ] تأیید شود که پس از فراخوانی `complete_prompt`، تابع `_verify_then_chain` در `backend/app/api/routes/external_prompts.py` به صورت asynchronous فراخوانی می‌شود و منطق آن (مانند `_acquire_verify_lock` و `_release_verify_lock` در `backend/app/services/oversight_service.py`) به درستی اجرا می‌شود.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1.  **ایجاد فایل تست UI جدید**: یک فایل تست Playwright جدید در `frontend/tests/ui/oversight_cloud_code.spec.ts` (یا مشابه) ایجاد شود.
2.  **پیاده‌سازی تست UI**: ناوبری به صفحه `/oversight`، شناسایی و تأیید وجود "عنصر انتخاب Cloud Code" (مثلاً یک دکمه، دراپ‌داون یا چک‌باکس)، شبیه‌سازی تعامل با این عنصر (مثلاً انتخاب یک گزینه یا فعال/غیرفعال کردن) و تأیید تغییرات بصری یا وضعیت UI پس از تعامل.
3.  **ایجاد فایل تست E2E جدید**: یک فایل تست پایتون جدید در `backend/tests/e2e/test_cloud_code_flow.py` ایجاد شود.
4.  **پیاده‌سازی تست E2E**: شبیه‌سازی یک تسک جدید که نیاز به اجرای Cloud Code دارد، فراخوانی endpoint `GET /external/prompts/next` برای دریافت تسک، فراخوانی `POST /external/prompts/{task_id}/claim` با `agent_id` مشخص، شبیه‌سازی تکمیل تسک توسط Cloud Code با فراخوانی `POST /external/prompts/{task_id}/complete`، تأیید اینکه `verification_status` تسک به `applied_externally_pending_verify` تغییر کرده است (همانطور که در خط 362 `backend/app/api/routes/external_prompts.py` مشخص شده)، و تأیید اینکه `_verify_then_chain` (خط 441 `backend/app/api/routes/external_prompts.py`) به درستی فراخوانی شده و وضعیت نهایی تسک (مثلاً `done` یا `partial`) را منعکس می‌کند. همچنین، اضافه کردن سناریوهایی برای تست گزینه‌های مربوط به محدودیت زمانی یا استراتژی‌های اجرا که کاربر به آن اشاره کرده است، در صورت وجود این گزینه‌ها در UI و API.

## 💡 نمونه‌های قبل/بعد
**ساختار فایل تست UI جدید (Playwright)**

_قبل:_
```
```typescript
// No existing file for this specific UI test
```
```

_بعد:_
```
```typescript
// frontend/tests/ui/oversight_cloud_code.spec.ts
import { test, expect } from '@playwright/test';

test.describe('Cloud Code Selection Element in Oversight Tab', () => {
  test('should display the Cloud Code selection element', async ({ page }) => {
    await page.goto('/oversight');
    // Assuming a data-testid for the element
    const cloudCodeSelector = page.locator('[data-testid="cloud-code-selector"]');
    await expect(cloudCodeSelector).toBeVisible();
  });

  test('should allow selecting a Cloud Code option', async ({ page }) => {
    await page.goto('/oversight');
    const cloudCodeSelector = page.locator('[data-testid="cloud-code-selector"]');
    await expect(cloudCodeSelector).toBeVisible();
    // Simulate selecting an option, e.g., from a dropdown
    await cloudCodeSelector.selectOption('option-with-time-limit');
    // Verify some visual feedback or state change
    await expect(page.locator('[data-testid="selected-option-display"]')).toHaveText('Option with Time Limit');
  });
});
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/e2e/test_cloud_code_flow.py`
- `npx playwright test frontend/tests/ui/oversight_cloud_code.spec.ts`

## ⚠️ ریسک‌ها و موارد احتیاط
1.  **وابستگی به زیرساخت GitHub Actions**: تست‌های E2E که شامل شبیه‌سازی کامل جریان Claude Auto-Runner هستند، ممکن است نیاز به mocking پیچیده یا حتی یک محیط تست ایزوله برای GitHub Actions داشته باشند تا از تداخل با محیط واقعی جلوگیری شود. این امر پیچیدگی تست را افزایش می‌دهد.
2.  **تغییرات در APIهای `external_prompts`**: هرگونه تغییر در schema یا منطق endpointهای `claim_prompt` یا `complete_prompt` در `backend/app/api/routes/external_prompts.py` می‌تواند تست‌های E2E موجود را بشکند و نیاز به به‌روزرسانی داشته باشد.
3.  **عدم وجود عنصر UI**: اگر "عنصر انتخاب Cloud Code" هنوز در `frontend/src/app/oversight/page.tsx` پیاده‌سازی نشده باشد، تست‌های UI اولیه ممکن است fail شوند تا زمانی که این عنصر اضافه شود.
4.  **پیچیدگی mocking `oversight_service`**: برای تست‌های واحد یا E2E که نیاز به ایزوله کردن `external_prompts.py` دارند، mocking صحیح `get_oversight_service()` و متدهای داخلی آن (مانند `_save_tasks`, `_recompute_execution_priorities`, `_schedule_prompt_sync`) می‌تواند چالش‌برانگیز باشد.
5.  **مدیریت `asyncio.create_task`**: تست `_verify_then_chain` که به صورت `asyncio.create_task` فراخوانی می‌شود، نیاز به رویکرد خاصی در تست‌های پایتون دارد تا اطمینان حاصل شود که task پس‌زمینه به درستی اجرا و تکمیل می‌شود و نتایج آن قابل مشاهده است.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: high
- تخمین زمان: medium

---

# 🔹 مرحله 9: مستندسازی و آماده‌سازی برای استقرار

**Scope:** این مرحله شامل به‌روزرسانی مستندات فنی مربوط به معماری جدید، APIهای اضافه شده، و نحوه پیکربندی و استقرار سرویس Cloud Code است. همچنین، شامل آماده‌سازی محیط‌های استقرار (مانند تنظیم متغیرهای محیطی، مدیریت دسترسی‌ها و پیکربندی‌های لازم) برای انتقال کد به محیط‌های تست و تولید است. هدف اطمینان از قابلیت نگهداری و استقرار آسان سیستم است.
**Key terms:** مستندات فنی, معماری جدید, APIها, پیکربندی, استقرار, متغیرهای محیطی, مدیریت دسترسی‌ها

**بخش مربوط از متن کاربر:**
```
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
مستندسازی و آماده‌سازی استقرار سرویس Cloud Code

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `README.md:1-70` — این فایل اصلی مستندات پروژه است و نیاز به به‌روزرسانی برای پوشش قابلیت Cloud Code دارد.
  ```markdown
  # AI Creator Engine - موتور خالق هوشمند
  
  یک پلتفرم جامع برای تولید، مدیریت و استقرار پروژه‌های نرم‌افزاری با هوش مصنوعی
  
  **نسخه:** 2.0.0 | **وضعیت:** در حال توسعه فعال
  
  ---
  
  ## فهرست مطالب
  
  - [معرفی](#معرفی)
  - [قابلیت‌ها](#قابلیت‌ها)
  - [پیش‌نیازها](#پیش‌نیازها)
  - [نصب و راه‌اندازی](#نصب-و-راه‌اندازی)
  - [ساختار پروژه](#ساختار-پروژه)
  - [پروایدرهای AI](#پروایدرهای-ai)
  - [استفاده](#استفاده)
  - [API Reference](#api-reference)
  - [تنظیمات](#تنظیمات)
  - [استقرار](#استقرار)
  - [مشارکت](#مشارکت)
  
  ---
  
  ## معرفی
  
  **AI Creator Engine** یک سیستم کامل برای:
  
  1. **تولید پروژه با AI** - با یک توضیح ساده، پروژه کامل (backend + frontend) بسازید
  2. **مناظره AI** - چند مدل هوش مصنوعی با هم بحث کنند و بهترین راه‌حل را بدهند
  3. **مدیریت چند پروایدر** - OpenAI, Claude, Gemini, DeepSeek, Perplexity و بیشتر
  4. **استقرار یک‌کلیکی** - مستقیماً به Render, Railway یا Vercel
  5. **ایمپورت از GitHub** - پروژه‌های موجود را وارد و مدیریت کنید
  6. **تولید نمودار** - دیاگرام‌های Mermaid برای ساختار، جریان و کلاس
  ```
- `backend/.env.example:1-20` — این فایل باید شامل توضیحات واضح برای `EXTERNAL_TOOL_TOKEN` باشد.
  ```
  # ===========================================
  # تنظیمات پایه
  # ===========================================
  APP_NAME="AI Debate & Project Management System"
  DEBUG=true
  ENVIRONMENT=development
  
  # ===========================================
  # API Keys
  # کلیدهای API خود را از سایت‌های مربوطه دریافت کنید
  # ===========================================
  
  # OpenAI - https://platform.openai.com/api-keys
  OPENAI_API_KEY=sk-...
  
  # Anthropic Claude - https://console.anthropic.com/
  CLAUDE_API_KEY=sk-ant-...
  
  # Google Gemini - https://aistudio.google.com/app/apikey
  GEMINI_API_KEY=AIza...
  ```
- `backend/app/api/routes/external_prompts.py:91-99` — `_check_external_token` — این تابع مسئول اعتبارسنجی توکن ابزار خارجی است و docstringهای endpointهای مرتبط باید نحوه استفاده از آن را توضیح دهند.
  ```python
  def _check_external_token(provided: Optional[str]) -> None:
      expected = os.environ.get("EXTERNAL_TOOL_TOKEN", "").strip()
      if not expected:
          raise HTTPException(
              status_code=503,
              detail="EXTERNAL_TOOL_TOKEN env var تنظیم نشده — endpoint غیرفعال است.",
          )
      if not provided or provided.strip() != expected:
          raise HTTPException(status_code=401, detail="Invalid X-External-Token")
  ```
- `.github/workflows/claude-auto-task.yml:1-20` — این فایل workflow اصلی برای Claude Auto Task Runner است و نیاز به کامنت‌های به‌روزرسانی شده دارد.
  ```
  # 🤖 Claude Auto Task Runner (auto-generated by oversight backend)
  #
  # این فایل خودکار توسط پنل oversight روی این ریپو نصب شده است.
  # هرگاه تسکی به فولدر prompt/ این ریپو push شود (توسط backend sync یا
  # دستی)، GitHub Actions این workflow را trigger می‌کند و Claude Code
  # تسک‌های pending را به ترتیب اولویت اجرا می‌کند.
  #
  # برای غیرفعال کردن: از پنل oversight روی این پروژه، toggle «اجرای
  # خودکار با Claude Code» را خاموش کنید — این فایل خودکار حذف می‌شود.
  # (یا این فایل را manually delete کنید.)
  
  name: Claude Auto Task Runner
  
  # 'on' را به‌صورت string quote می‌کنیم چون در YAML 1.1، on/off/yes/no
  # مقدار boolean هستند. GitHub Actions هر دو فرم را می‌پذیرد ولی این
  # فرم با همهٔ YAML parserها سازگار است.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack تشخیص داده شده: `fastapi`, `nextjs`. این تسک عمدتاً بر روی بخش `fastapi` (backend) و جنبه‌های DevOps/استقرار (GitHub Actions, Docker) تمرکز دارد. کتابخانه‌های مرتبط شامل `PyNaCl` (برای رمزگذاری GitHub Secrets) و `httpx` (برای فراخوانی‌های API) هستند که در `requirements.txt` ذکر شده‌اند.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_service.py` — این سرویس 'مرکز نظارت' است که تسک‌ها را مدیریت می‌کند و `external_prompts.py` با آن تعامل دارد. مستندات باید این ارتباط را توضیح دهند.
- `backend/app/services/claude_runner_bootstrap.py` — این فایل شامل منطق `trigger_workflow_dispatch` است که توسط `external_prompts.py` برای راه‌اندازی workflowهای GitHub استفاده می‌شود. مستندات باید این بخش از 'ورک فلو' را پوشش دهند.
- `backend/app/services/notification_service.py` — این سرویس برای ارسال نوتیفیکیشن‌های مربوط به وضعیت اجرای تسک‌ها (مانند شروع، تکمیل، شکست) استفاده می‌شود که بخشی از تجربه کاربر با 'ورک فلو' است و باید در مستندات اشاره شود.

## 🌐 نقشهٔ وابستگی‌ها
این تسک عمدتاً بر مستندات و پیکربندی محیطی تمرکز دارد. فایل `README.md` به‌عنوان نقطه ورود اصلی مستندات، باید به‌روزرسانی شود. `backend/.env.example` برای تعریف متغیرهای محیطی حیاتی مانند `EXTERNAL_TOOL_TOKEN` که توسط `backend/app/api/routes/external_prompts.py` برای احراز هویت ابزارهای خارجی استفاده می‌شود، اهمیت دارد. فایل `.github/workflows/claude-auto-task.yml` نیز به‌عنوان تعریف‌کنندهٔ «ورک فلو» Claude Auto Task Runner، باید کامنت‌های واضحی داشته باشد. سرویس `backend/app/services/oversight_service.py` مدیریت اصلی تسک‌ها را بر عهده دارد که `external_prompts.py` با آن تعامل می‌کند. `backend/app/services/claude_runner_bootstrap.py` مسئول راه‌اندازی workflowهای GitHub است و `backend/app/services/notification_service.py` نوتیفیکیشن‌های مربوط به این workflowها را ارسال می‌کند.

## 🔍 Context و وضعیت فعلی
این تسک بر به‌روزرسانی مستندات فنی و آماده‌سازی محیط‌های استقرار برای سرویس Cloud Code تمرکز دارد. کاربر درخواست کرده است که با توجه به اینکه «الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه»، نیاز به «مستندسازی و آماده‌سازی برای استقرار» وجود دارد. این شامل «به‌روزرسانی مستندات فنی مربوط به معماری جدید، APIهای اضافه شده، و نحوه پیکربندی و استقرار سرویس Cloud Code» است. همچنین، «آماده‌سازی محیط‌های استقرار (مانند تنظیم متغیرهای محیطی، مدیریت دسترسی‌ها و پیکربندی‌های لازم) برای انتقال کد به محیط‌های تست و تولید» ضروری است. هدف اصلی «اطمینان از قابلیت نگهداری و استقرار آسان سیستم» است. کلیدواژه‌های اصلی شامل «مستندات فنی, معماری جدید, APIها, پیکربندی, استقرار, متغیرهای محیطی, مدیریت دسترسی‌ها» هستند. شواهد در کد نشان می‌دهد که `backend/app/api/routes/external_prompts.py` (خطوط 91-99) مسئول اعتبارسنجی `X-External-Token` در برابر `EXTERNAL_TOOL_TOKEN` است و `_emit_runner_notification` (خطوط 35-85) برای فیدبک تلگرام در workflow استفاده می‌شود. همچنین، فایل `.github/workflows/claude-auto-task.yml` مستقیماً به «Claude Auto Task Runner» و استفاده از `claude_code_oauth_token` اشاره دارد که نشان‌دهندهٔ پیاده‌سازی فعلی این سرویس است. بنابراین، مستندات باید این جزئیات را پوشش دهند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل `README.md` باید شامل یک بخش جدید با عنوان «ادغام با Claude Code / External AI Runners» باشد که نحوه کارکرد این قابلیت را توضیح دهد.
- [ ] بخش «پیش‌نیازها» در `README.md` باید `EXTERNAL_TOOL_TOKEN` و `CLAUDE_CODE_OAUTH_TOKEN` (به‌عنوان GitHub Secret) را به‌عنوان پیش‌نیازهای فعال‌سازی Cloud Code ذکر کند.
- [ ] فایل `backend/.env.example` باید شامل یک کامنت واضح و جامع برای `EXTERNAL_TOOL_TOKEN` باشد که هدف آن را توضیح دهد.
- [ ] docstringهای endpointهای `external_prompts.py` (به‌ویژه `/next`, `/claim`, `/complete`, `/fail`) باید به‌طور کامل نحوه تعامل با ابزارهای خارجی و استفاده از `X-External-Token` را توضیح دهند.
- [ ] فایل `.github/workflows/claude-auto-task.yml` باید کامنت‌های به‌روزرسانی شده‌ای داشته باشد که نقش `claude_code_oauth_token` و ارتباط آن با backend را شفاف کند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای برآورده کردن درخواست کاربر، اقدامات زیر پیشنهاد می‌شود:
1.  **به‌روزرسانی `README.md`:**
    *   یک بخش جدید با عنوان «ادغام با Claude Code / External AI Runners» در قسمت «قابلیت‌ها» اضافه شود که توضیح دهد چگونه سیستم با Claude Code از طریق GitHub Actions و APIهای `external_prompts` تعامل دارد.
    *   بخش «پیش‌نیازها» به‌روزرسانی شود تا `EXTERNAL_TOOL_TOKEN` (برای backend) و `CLAUDE_CODE_OAUTH_TOKEN` (به‌عنوان GitHub Secret برای workflow) را به‌عنوان پیش‌نیازهای کلیدی برای فعال‌سازی این قابلیت ذکر کند.
    *   در بخش «نصب و راه‌اندازی»، یک زیربخش برای «پیکربندی Claude Auto Task Runner» اضافه شود که مراحل کلی فعال‌سازی workflow در GitHub Actions و تنظیم GitHub Secrets را توضیح دهد.
2.  **به‌روزرسانی `backend/.env.example`:**
    *   یک توضیح واضح و جامع برای متغیر محیطی `EXTERNAL_TOOL_TOKEN` اضافه شود که نقش آن را در احراز هویت ابزارهای خارجی مانند Claude Code Runner مشخص کند.
3.  **بررسی و بهبود Docstringهای `backend/app/api/routes/external_prompts.py`:**
    *   اطمینان حاصل شود که docstringهای endpointهایی مانند `/next`, `/{task_id}/claim`, `/{task_id}/complete`, `/{task_id}/fail` به‌طور کامل نحوه تعامل ابزارهای خارجی با «مرکز نظارت» (oversight center) و استفاده از `X-External-Token` را توضیح می‌دهند.
4.  **به‌روزرسانی کامنت‌های `.github/workflows/claude-auto-task.yml`:**
    *   کامنت‌های موجود در این فایل به‌روزرسانی یا تکمیل شوند تا نقش `claude_code_oauth_token` و نحوه ارتباط آن با backend و مدیریت تسک‌ها را به‌طور شفاف بیان کنند.

## 💡 نمونه‌های قبل/بعد
**افزودن توضیحات EXTERNAL_TOOL_TOKEN در .env.example**

_قبل:_
```
# ===========================================
# API Keys
# کلیدهای API خود را از سایت‌های مربوطه دریافت کنید
# ===========================================
```

_بعد:_
```
# ===========================================
# API Keys
# کلیدهای API خود را از سایت‌های مربوطه دریافت کنید
# ===========================================

# External Tool Token (برای ابزارهای خارجی مانند Claude Code Runner)
# این توکن برای احراز هویت درخواست‌ها از ابزارهای خارجی به endpointهای /external/prompts استفاده می‌شود.
EXTERNAL_TOOL_TOKEN=your-external-tool-secret-token
```

**افزودن بخش Cloud Code Integration به README.md**

_قبل:_
```
### هوش مصنوعی

| قابلیت | توضیح | وضعیت |
|--------|-------|-------|
| Multi-Provider | 7 پروایدر AI | ✅ |
| Smart Select | انتخاب هوشمند مدل | ✅ |
| Fallback | جایگزینی خودکار | ✅
```

_بعد:_
```
### هوش مصنوعی

| قابلیت | توضیح | وضعیت |
|--------|-------|-------|
| Multi-Provider | 7 پروایدر AI | ✅ |
| Smart Select | انتخاب هوشمند مدل | ✅ |
| Fallback | جایگزینی خودکار | ✅ |
| Cloud Code Integration | اجرای خودکار تسک‌ها با Claude Code | ✅ |

### ادغام با Claude Code / External AI Runners

سیستم AI Creator Engine قابلیت ادغام عمیق با ابزارهای کدنویسی خارجی مانند Claude Code را از طریق GitHub Actions فراهم می‌کند. این ادغام به Claude Code اجازه می‌دهد تا تسک‌های تولید شده توسط «مرکز نظارت» را به‌صورت خودکار دریافت، اجرا و نتایج را گزارش کند. این فرآیند از طریق یک توکن اختصاصی (`EXTERNAL_TOOL_TOKEN`) برای احراز هویت و یک GitHub Workflow (`claude-auto-task.yml`) برای هماهنگی اجرا می‌شود.
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `grep -q '### ادغام با Claude Code / External AI Runners' README.md`
- `grep -q 'EXTERNAL_TOOL_TOKEN=your-external-tool-secret-token' backend/.env.example`
- `grep -q 'claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}' .github/workflows/claude-auto-task.yml`

## ⚠️ ریسک‌ها و موارد احتیاط
ریسک اصلی این تسک، عدم وضوح یا اشتباه در مستندسازی است که می‌تواند منجر به دشواری در پیکربندی و استقرار سرویس Cloud Code توسط کاربران شود. تغییرات در `README.md` و `backend/.env.example` باید دقیق باشند تا از سردرگمی جلوگیری شود. به‌روزرسانی کامنت‌ها در `.github/workflows/claude-auto-task.yml` باید بدون تغییر در منطق workflow انجام شود تا عملکرد فعلی آن مختل نشود. عدم وضوح در docstringهای `backend/app/api/routes/external_prompts.py` می‌تواند باعث استفاده نادرست از API توسط ابزارهای خارجی شود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] {'text': 'فیلد `EXTERNAL_TOOL_TOKEN` باید به کلاس `Settings` در `backend/app/core/config.py` اضافه شود و از متغیر محیطی بارگذاری شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class Settings\\(BaseSettings\\):', 'EXTERNAL_TOOL_TOKEN: Optional\\[str\\] = None'], 'files_hint': ['backend/app/core/config.py']}}
- [ ] {'text': 'کلاس `ClaudeService` در `backend/app/services/claude_service.py` باید پارامتر `external_token: Optional[str]` را در `__init__` خود بپذیرد و از آن برای احراز هویت در فراخوانی\u200cهای API استفاده کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['def __init__\\(self, api_key: str, external_token: Optional\\[str\\] = None\\)', 'token_to_use = self.external_token if self.external_token else self.api_key'], 'files_hint': ['backend/app/services/claude_service.py']}}
- [ ] {'text': 'متد `get_service` (یا مشابه آن) در `backend/app/services/ai_manager.py` باید یک پارامتر `use_external_token_for_claude: bool` داشته باشد و در صورت `True` بودن، `EXTERNAL_TOOL_TOKEN` را به `ClaudeService` ارسال کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['def get_service\\(.*use_external_token_for_claude: bool = False.*\\)', 'ClaudeService\\(.*external_token=settings.EXTERNAL_TOOL_TOKEN.*\\)'], 'files_hint': ['backend/app/services/ai_manager.py']}}
- [ ] {'text': 'سرویس `inspector_agent.py` باید `ai_manager` را با پرچم `use_external_token_for_claude=True` فراخوانی کند تا از توکن Cloud Code برای مدل\u200cهای Claude استفاده شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['get_ai_manager\\(.*use_external_token_for_claude=True.*\\)'], 'files_hint': ['backend/app/services/inspector_agent.py']}}
- [ ] {'text': 'یک درخواست API به endpoint مربوط به چت بازرس (مثلاً `/api/oversight/inspector/chat`) که از توکن Cloud Code استفاده می\u200cکند، باید با موفقیت پاسخ AI را دریافت کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/api/oversight/inspector/chat', 'headers': {'X-Inspector-Use-External-Token': 'true', 'Authorization': 'Bearer <VALID_EXTERNAL_TOKEN>'}, 'body': {'project_id': 'some_project_id', 'query': "Analyze this code snippet: print('hello')", 'model': 'claude-opus'}, 'expected_status': 200, 'required_fields': ['response_text']}}
- [ ] {'text': 'سرویس `CloudCodeAgentService` ایجاد شده باشد: فایل `backend/app/services/cloud_code_agent_service.py` با کلاس `CloudCodeAgentService` و متدهای اولیه (مانند `claim_task`, `send_prompt_to_cloud_code`, `complete_task`) وجود داشته باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['class CloudCodeAgentService', 'def claim_task', 'def send_prompt_to_cloud_code', 'def complete_task'], 'files_hint': ['backend/app/services/cloud_code_agent_service.py']}}
- [ ] {'text': 'مسیرهای API برای Cloud Code Agents تعریف شده باشد: فایل `backend/app/api/routes/cloud_code_agents.py` شامل endpointهای `GET /cloud-code/next-task`, `POST /cloud-code/{task_id}/claim`, `POST /cloud-code/{task_id}/complete`, `POST /cloud-code/{task_id}/fail`, `POST /cloud-code/{task_id}/progress` باشد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['@router.get("/next-task")', '@router.post("/{task_id}/claim")', '@router.post("/{task_id}/complete")', '@router.post("/{task_id}/fail")', '@router.post("/{task_id}/progress")'], 'files_hint': ['backend/app/api/routes/cloud_code_agents.py']}}
- [ ] {'text': 'احراز هویت `X-External-Token` برای مسیرهای Cloud Code اعمال شود: هر endpoint در `backend/app/api/routes/cloud_code_agents.py` باید از `Depends(require_external_token)` استفاده کند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'GET', 'path': '/cloud-code/next-task', 'expected_status': 401, 'headers': {}, 'note': 'درخواست بدون توکن باید 401 برگرداند'}}
- [ ] {'text': 'تسک\u200cهای Cloud Code Agent در `oversight_service` به\u200cدرستی مدیریت شوند: پس از `POST /cloud-code/{task_id}/claim`، وضعیت `external_status` تسک در دیتابیس به "claimed" و `external_locked_by` به شناسه agent مربوطه به\u200cروزرسانی شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_cloud_code_agent_interaction.py::test_claim_task_updates_status', 'marker': 'verify'}}
- [ ] {'text': 'قابلیت "ساختارمند کردن پرامپت" در سرویس Cloud Code Agent وجود داشته باشد: سرویس جدید باید بتواند یک پرامپت خام را به فرمت مورد نیاز Cloud Code تبدیل کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['def structure_prompt_for_cloud_code'], 'files_hint': ['backend/app/services/cloud_code_agent_service.py']}}
- [ ] {'text': 'Endpoint جدید `POST /oversight/inspector/chat-cloud-code` باید در `backend/app/api/routes/oversight.py` تعریف شود.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['@router.post("/inspector/chat-cloud-code"', 'async def chat_with_cloud_code_inspector'], 'files_hint': ['backend/app/api/routes/oversight.py']}}
- [ ] {'text': 'Endpoint `chat_with_cloud_code_inspector` باید از `require_external_token` برای احراز هویت استفاده کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['dependencies=\\[Depends\\(require_external_token\\)\\]'], 'files_hint': ['backend/app/api/routes/oversight.py']}}
- [ ] {'text': 'متد `chat_with_cloud_code` باید به `InspectorAgentService` در `backend/app/services/inspector_agent.py` اضافه شود و یک `AsyncGenerator` برای استریم پاسخ برگرداند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['async def chat_with_cloud_code\\(', '-> AsyncGenerator\\[Dict\\[str, Any\\], None\\]'], 'files_hint': ['backend/app/services/inspector_agent.py']}}
- [ ] {'text': 'فراخوانی Endpoint `POST /oversight/inspector/chat-cloud-code` با `X-External-Token` معتبر و `project_id`, `session_id`, `prompt_text` باید پاسخ استریمینگ (SSE) شامل رویدادهای `start`, `chunk`, `end` و `progress` را برگرداند.', 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/oversight/inspector/chat-cloud-code', 'headers': {'X-External-Token': 'YOUR_VALID_EXTERNAL_TOKEN'}, 'json_payload': {'project_id': 'test-project-id', 'session_id': 'test-session-id', 'prompt_text': 'Hello Cloud Code, how are you?', 'model_id': 'claude-opus-4-8'}, 'expected_status': 200, 'stream_events': ['event: start', 'event: chunk', 'event: end', 'event: progress']}}
- [ ] {'text': 'تاریخچه چت (شامل پرامپت کاربر و پاسخ AI) باید در `InspectorSession` مربوطه در دیتابیس ذخیره شود و پس از هر تعامل به\u200cروزرسانی گردد.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_inspector_agent_chat.py::test_chat_history_persistence', 'marker': 'verify'}}
- [ ] {'text': 'Endpoint `POST /oversight/process_inspector_chat` (یا endpoint مشابه موجود) باید در `backend/app/api/routes/oversight.py` وجود داشته باشد و پارامتر `processing_mode` را بپذیرد.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['@router.post("/process_inspector_chat")', 'class InspectorChatRequest(BaseModel):', 'processing_mode: str'], 'files_hint': ['backend/app/api/routes/oversight.py']}}
- [ ] {'text': "هنگامی که `processing_mode` برابر با 'cloud_code' است، فراخوانی به متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` انجام شود و یک تسک جدید با `external_status='pending'` در دیتابیس ایجاد شود.", 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/oversight/process_inspector_chat', 'body': {'project_id': 'test_project_id', 'prompt_content': 'Test prompt for Cloud Code', 'processing_mode': 'cloud_code'}, 'expected_status': 200, 'required_fields': ['status', 'task'], 'post_check_db': {'model': 'SystemPrompt', 'filter': {'project_id': 'test_project_id', 'type': 'inspector_chat', 'external_status': 'pending'}, 'expected_count': 1}}}
- [ ] {'text': "هنگامی که `processing_mode` برابر با 'internal_ai' است (یا پارامتر `processing_mode` ارسال نشود)، درخواست به منطق پردازش مدل\u200cهای داخلی (از طریق `ai_manager`) هدایت شود و پاسخ مناسب برگردانده شود.", 'verify_method': 'api_response', 'verify_plan': {'method': 'POST', 'path': '/oversight/process_inspector_chat', 'body': {'project_id': 'test_project_id', 'prompt_content': 'Test prompt for internal AI', 'processing_mode': 'internal_ai'}, 'expected_status': 200, 'required_fields': ['status', 'response'], 'mock_services': ['ai_manager.process_chat_prompt']}}
- [ ] {'text': 'متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` باید `_schedule_prompt_sync` را فراخوانی کند تا تسک جدید با GitHub همگام\u200cسازی شود.', 'verify_method': 'backend_test', 'verify_plan': {'test_path': 'backend/tests/test_oversight_service.py::test_submit_inspector_task_triggers_sync', 'marker': 'verify'}}
- [ ] {'text': 'مدل `backend/app/models/system_prompt.py` باید فیلدهای `external_status`, `external_locked_by`, `external_lease_until`, `verification_status` و `external_attempts` را پشتیبانی کند.', 'verify_method': 'static', 'verify_plan': {'grep_patterns': ['external_status', 'external_locked_by', 'external_lease_until', 'verification_status', 'external_attempts'], 'files_hint': ['backend/app/models/system_prompt.py']}}

## Acceptance Criteria

1. فیلد `EXTERNAL_TOOL_TOKEN` باید به کلاس `Settings` در `backend/app/core/config.py` اضافه شود و از متغیر محیطی بارگذاری شود. _(verify: static)_
2. کلاس `ClaudeService` در `backend/app/services/claude_service.py` باید پارامتر `external_token: Optional[str]` را در `__init__` خود بپذیرد و از آن برای احراز هویت در فراخوانی‌های API استفاده کند. _(verify: static)_
3. متد `get_service` (یا مشابه آن) در `backend/app/services/ai_manager.py` باید یک پارامتر `use_external_token_for_claude: bool` داشته باشد و در صورت `True` بودن، `EXTERNAL_TOOL_TOKEN` را به `ClaudeService` ارسال کند. _(verify: static)_
4. سرویس `inspector_agent.py` باید `ai_manager` را با پرچم `use_external_token_for_claude=True` فراخوانی کند تا از توکن Cloud Code برای مدل‌های Claude استفاده شود. _(verify: static)_
5. یک درخواست API به endpoint مربوط به چت بازرس (مثلاً `/api/oversight/inspector/chat`) که از توکن Cloud Code استفاده می‌کند، باید با موفقیت پاسخ AI را دریافت کند. _(verify: api_response)_
6. سرویس `CloudCodeAgentService` ایجاد شده باشد: فایل `backend/app/services/cloud_code_agent_service.py` با کلاس `CloudCodeAgentService` و متدهای اولیه (مانند `claim_task`, `send_prompt_to_cloud_code`, `complete_task`) وجود داشته باشد. _(verify: static)_
7. مسیرهای API برای Cloud Code Agents تعریف شده باشد: فایل `backend/app/api/routes/cloud_code_agents.py` شامل endpointهای `GET /cloud-code/next-task`, `POST /cloud-code/{task_id}/claim`, `POST /cloud-code/{task_id}/complete`, `POST /cloud-code/{task_id}/fail`, `POST /cloud-code/{task_id}/progress` باشد. _(verify: static)_
8. احراز هویت `X-External-Token` برای مسیرهای Cloud Code اعمال شود: هر endpoint در `backend/app/api/routes/cloud_code_agents.py` باید از `Depends(require_external_token)` استفاده کند. _(verify: api_response)_
9. تسک‌های Cloud Code Agent در `oversight_service` به‌درستی مدیریت شوند: پس از `POST /cloud-code/{task_id}/claim`، وضعیت `external_status` تسک در دیتابیس به "claimed" و `external_locked_by` به شناسه agent مربوطه به‌روزرسانی شود. _(verify: backend_test)_
10. قابلیت "ساختارمند کردن پرامپت" در سرویس Cloud Code Agent وجود داشته باشد: سرویس جدید باید بتواند یک پرامپت خام را به فرمت مورد نیاز Cloud Code تبدیل کند. _(verify: static)_
11. Endpoint جدید `POST /oversight/inspector/chat-cloud-code` باید در `backend/app/api/routes/oversight.py` تعریف شود. _(verify: static)_
12. Endpoint `chat_with_cloud_code_inspector` باید از `require_external_token` برای احراز هویت استفاده کند. _(verify: static)_
13. متد `chat_with_cloud_code` باید به `InspectorAgentService` در `backend/app/services/inspector_agent.py` اضافه شود و یک `AsyncGenerator` برای استریم پاسخ برگرداند. _(verify: static)_
14. فراخوانی Endpoint `POST /oversight/inspector/chat-cloud-code` با `X-External-Token` معتبر و `project_id`, `session_id`, `prompt_text` باید پاسخ استریمینگ (SSE) شامل رویدادهای `start`, `chunk`, `end` و `progress` را برگرداند. _(verify: api_response)_
15. تاریخچه چت (شامل پرامپت کاربر و پاسخ AI) باید در `InspectorSession` مربوطه در دیتابیس ذخیره شود و پس از هر تعامل به‌روزرسانی گردد. _(verify: backend_test)_
16. Endpoint `POST /oversight/process_inspector_chat` (یا endpoint مشابه موجود) باید در `backend/app/api/routes/oversight.py` وجود داشته باشد و پارامتر `processing_mode` را بپذیرد. _(verify: static)_
17. هنگامی که `processing_mode` برابر با 'cloud_code' است، فراخوانی به متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` انجام شود و یک تسک جدید با `external_status='pending'` در دیتابیس ایجاد شود. _(verify: api_response)_
18. هنگامی که `processing_mode` برابر با 'internal_ai' است (یا پارامتر `processing_mode` ارسال نشود)، درخواست به منطق پردازش مدل‌های داخلی (از طریق `ai_manager`) هدایت شود و پاسخ مناسب برگردانده شود. _(verify: api_response)_
19. متد `submit_inspector_task_to_external_runner` در `backend/app/services/oversight_service.py` باید `_schedule_prompt_sync` را فراخوانی کند تا تسک جدید با GitHub همگام‌سازی شود. _(verify: backend_test)_
20. مدل `backend/app/models/system_prompt.py` باید فیلدهای `external_status`, `external_locked_by`, `external_lease_until`, `verification_status` و `external_attempts` را پشتیبانی کند. _(verify: static)_
21. یک گزینه (مثلاً Radio Button یا Dropdown) با عنوان "Prompt Processing Engine" در "تب بازرس ویژه" در `frontend/src/app/oversight/page.tsx` قابل مشاهده باشد. _(verify: ui_interaction)_
22. گزینه "Local AI" به صورت پیش‌فرض در UI انتخاب شده باشد. _(verify: ui_interaction)_
23. پس از انتخاب "Cloud Code" در UI و شروع یک جلسه بازرسی، این انتخاب در دیتابیس برای `InspectorSession` مربوطه ذخیره شود. _(verify: api_response)_
24. اگر "Cloud Code" انتخاب شود، `inspector_agent` باید یک `OversightTask` با `external_status='pending'` و `status='pending'` ایجاد کند و آن را برای همگام‌سازی با GitHub برنامه‌ریزی کند. _(verify: backend_test)_
25. اگر "Local AI" انتخاب شود، پردازش پرامپت باید از طریق `ai_manager` و مدل‌های محلی انجام شود (رفتار فعلی حفظ شود). _(verify: backend_test)_
26. پس از ارسال یک تسک برای "Cloud Code"، workflow `claude-auto-task.yml` در GitHub Actions باید trigger شود. _(verify: manual_only)_
27. تب بازرس ویژه در فرانت‌اند باید یک عنصر UI (مانند Radio Button یا Dropdown) برای انتخاب بین 'Cloud Code' و 'Current Models' برای تعاملات چت نمایش دهد. _(verify: ui_interaction)_
28. یک Endpoint جدید POST به نام `/api/oversight/chat-with-cloud-code` در `backend/app/api/routes/oversight.py` باید وجود داشته باشد که `project_id` و `message` را بپذیرد و یک وضعیت موفقیت‌آمیز همراه با `task_id` ایجاد شده را برگرداند. این Endpoint باید از `X-External-Token` برای احراز هویت استفاده کند. _(verify: api_response)_
29. هنگامی که یک پیام چت از طریق گزینه 'Cloud Code' ارسال می‌شود، یک `OversightTask` جدید باید در دیتابیس ایجاد شود که فیلد `prompt` آن حاوی پیام چت، `status` آن 'pending'، `external_status` آن 'pending' و `type` آن 'chat_interaction' باشد. _(verify: backend_test)_
30. هنگامی که یک پیام چت از طریق گزینه 'Current Models' ارسال می‌شود، درخواست باید به Endpoint چت موجود (مثلاً `/api/chat`) بدون تغییر در payload یا رفتار آن مسیریابی شود. _(verify: ui_interaction)_
31. Endpoint جدید `/api/oversight/chat-with-cloud-code` باید درخواست‌های بدون `X-External-Token` معتبر را با کد وضعیت 401 رد کند. _(verify: api_response)_
32. تست‌های واحد برای تابع `_check_external_token` در `backend/app/api/routes/external_prompts.py` باید سناریوهای توکن معتبر، نامعتبر و عدم تنظیم متغیر محیطی `EXTERNAL_TOOL_TOKEN` را پوشش دهند و در هر حالت کد وضعیت HTTP صحیح را برگردانند. _(verify: backend_test)_
33. Endpoint `POST /external/prompts/{task_id}/claim` باید با توکن معتبر، تسک را با موفقیت claim کند و وضعیت `external_status` را به `claimed` تغییر دهد و `external_lease_until` را تنظیم کند. در صورت توکن نامعتبر، باید کد وضعیت 401 برگرداند. _(verify: api_response)_
34. Endpoint `POST /external/prompts/{task_id}/complete` باید با توکن معتبر، تسک را به وضعیت `applied_externally_pending_verify` منتقل کند و `external_status` را به `done` تغییر دهد. همچنین، باید `_emit_runner_notification` را فراخوانی کند و در صورت عدم تطابق `agent_id` یا توکن نامعتبر، خطای مناسب برگرداند. _(verify: api_response)_
35. تابع `_verify_then_chain` باید پس از تکمیل تسک، `oversight_verifier.verify_task` را فراخوانی کند و بر اساس نتیجه تأیید (done, partial, regressed)، رفتار صحیح (آزاد کردن lock، retry همان تسک، یا trigger کردن تسک بعدی) را انجام دهد. _(verify: backend_test)_
36. فایل `backend/tests/test_external_prompts_api.py` باید ایجاد شده و حداقل شامل 10 تست واحد و 5 تست یکپارچه‌سازی برای Endpointهای Cloud Code Runner باشد. _(verify: static)_
37. یک تست UI جدید باید وجود "عنصر انتخاب Cloud Code" را در صفحه `/oversight` تأیید کند. _(verify: ui_interaction)_
38. یک تست UI جدید باید قابلیت انتخاب گزینه‌های مختلف (از جمله گزینه مربوط به "[02:00] لیمیت زمان") و تأیید تغییر وضعیت UI را پوشش دهد. _(verify: ui_interaction)_
39. یک تست E2E باید جریان کامل انتخاب Cloud Code، ارسال پرامپت، claim شدن تسک توسط یک agent شبیه‌سازی شده، و تکمیل موفقیت‌آمیز آن را از طریق APIهای `external_prompts` (مانند `claim_prompt` و `complete_prompt` در `backend/app/api/routes/external_prompts.py`) شبیه‌سازی و تأیید کند که وضعیت تسک به درستی به `applied_externally_pending_verify` و سپس به `done` تغییر می‌کند. _(verify: api_response)_
40. یک تست E2E باید سناریوی شکست در اجرای Cloud Code را شبیه‌سازی کند (فراخوانی `fail_prompt` در `backend/app/api/routes/external_prompts.py`) و تأیید کند که وضعیت تسک به `failed` یا `pending` (در صورت `retry=True`) تغییر می‌کند و `external_last_error` ثبت می‌شود. _(verify: api_response)_
41. تأیید شود که پس از فراخوانی `complete_prompt`، تابع `_verify_then_chain` در `backend/app/api/routes/external_prompts.py` به صورت asynchronous فراخوانی می‌شود و منطق آن (مانند `_acquire_verify_lock` و `_release_verify_lock` در `backend/app/services/oversight_service.py`) به درستی اجرا می‌شود. _(verify: backend_test)_
42. فایل `README.md` باید شامل یک بخش جدید با عنوان «ادغام با Claude Code / External AI Runners» باشد که نحوه کارکرد این قابلیت را توضیح دهد. _(verify: static)_
43. بخش «پیش‌نیازها» در `README.md` باید `EXTERNAL_TOOL_TOKEN` و `CLAUDE_CODE_OAUTH_TOKEN` (به‌عنوان GitHub Secret) را به‌عنوان پیش‌نیازهای فعال‌سازی Cloud Code ذکر کند. _(verify: static)_
44. فایل `backend/.env.example` باید شامل یک کامنت واضح و جامع برای `EXTERNAL_TOOL_TOKEN` باشد که هدف آن را توضیح دهد. _(verify: static)_
45. docstringهای endpointهای `external_prompts.py` (به‌ویژه `/next`, `/claim`, `/complete`, `/fail`) باید به‌طور کامل نحوه تعامل با ابزارهای خارجی و استفاده از `X-External-Token` را توضیح دهند. _(verify: static)_
46. فایل `.github/workflows/claude-auto-task.yml` باید کامنت‌های به‌روزرسانی شده‌ای داشته باشد که نقش `claude_code_oauth_token` و ارتباط آن با backend را شفاف کند. _(verify: static)_

## Task Steps

### Step 1: طراحی و پیاده‌سازی لایه دسترسی به توکن Cloud Code
**Status:** `done` (100%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک ماژول یا سرویس بک‌اند اختصاصی است که مسئولیت مدیریت و استفاده از توکن Cloud Code موجود را بر عهده دارد. هدف این است که دسترسی به توکن و مکانیزم‌های احراز هویت مربوط به Cloud Code به صورت متمرکز و امن انجام شود و سایر سرویس‌ها بتوانند از طریق این لایه با Cloud Code ارتباط برقرار کنند. این مرحله شامل منطق پردازش پرامپت یا رابط کاربری نیست.
**Excerpt:**
```
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه. [00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه.
```

### Step 2: توسعه سرویس تعامل با ایجنت‌های Cloud Code
**Status:** `done` (100%)
**Scope:** در این مرحله، یک سرویس بک‌اند جدید توسعه می‌یابد که مسئولیت اصلی آن تعامل با ایجنت‌های Cloud Code است. این سرویس پرامپت‌های دریافتی را ساختارمند کرده و برای پردازش و اجرای کارها به ایجنت‌های Cloud Code ارسال می‌کند. این سرویس از لایه دسترسی به توکن (مرحله ۱) برای احراز هویت استفاده خواهد کرد و منطق اصلی پردازش پرامپت توسط Cloud Code را در خود جای می‌دهد.
**Excerpt:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه. [01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده.
```

### Step 3: پیاده‌سازی Endpoint جدید برای پردازش چت با Cloud Code
**Status:** `done` (100%)
**Scope:** این مرحله شامل ایجاد یک یا چند Endpoint جدید در API بک‌اند چت است که به طور خاص برای دریافت درخواست‌های پردازش پرامپت توسط Cloud Code طراحی شده‌اند. این Endpointها درخواست‌ها را از فرانت‌اند دریافت کرده و آن‌ها را به سرویس تعامل با ایجنت‌های Cloud Code (مرحله ۲) ارجاع می‌دهند. این Endpointها باید از نظر امنیتی محافظت شده و قابلیت مدیریت بار را داشته باشند.
**Excerpt:**
```
[00:30] می‌خوام همین توکن به عنوان توی چت بازرس توی تب بازرس ویژه هم استفاده بشه. الان توی چت قسمت چت تب بازرس ویژه خب یه سری مدل‌ها هست و اون آی فریم هم هست که مثلاً ما اونجا توسط اون مدل‌ها که کلیدشون قرار داده شده توی تنظیمات اون مدل‌ها پرامپت ما رو چیز می‌کنن و حالا بررسی می‌کنن و کارای اجرایی رو انجام می‌دن.
```

### Step 4: اصلاح منطق مسیریابی درخواست‌ها در بک‌اند چت
**Status:** `done` (100%)
**Scope:** در این مرحله، منطق موجود در بک‌اند سیستم چت بازرس ویژه اصلاح می‌شود تا بتواند بر اساس انتخاب کاربر (Cloud Code یا مدل‌های فعلی) درخواست‌های ورودی را به درستی مسیریابی کند. این شامل افزودن یک شرط به منطق پردازش درخواست است که اگر گزینه Cloud Code انتخاب شده باشد، درخواست به Endpoint جدید (مرحله ۳) هدایت شود؛ در غیر این صورت، به مسیر پردازش مدل‌های فعلی ادامه یابد.
**Excerpt:**
```
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

### Step 5: افزودن گزینه انتخاب Cloud Code به رابط کاربری تب بازرس ویژه
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه و ادغام یک عنصر رابط کاربری جدید (مانند دکمه رادیویی، منوی کشویی یا سوئیچ) در 'تب بازرس ویژه' است. این عنصر به کاربر اجازه می‌دهد تا 'Cloud Code' را به عنوان موتور پردازش پرامپت خود انتخاب کند. طراحی این عنصر باید با ظاهر و احساس کلی رابط کاربری موجود سازگار باشد.
**Excerpt:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. [02:00] اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

### Step 6: به‌روزرسانی منطق ارسال درخواست چت در فرانت‌اند
**Status:** `done` (100%)
**Scope:** در این مرحله، منطق ارسال درخواست چت در فرانت‌اند 'تب بازرس ویژه' اصلاح می‌شود. این اصلاح شامل افزودن پارامتری به درخواست‌های ارسالی به بک‌اند است که نشان‌دهنده انتخاب کاربر (Cloud Code یا مدل‌های فعلی) باشد. همچنین، در صورت انتخاب Cloud Code، درخواست باید به Endpoint جدید (مرحله ۳) ارسال شود، در حالی که برای مدل‌های فعلی، به Endpointهای موجود ارسال خواهد شد.
**Excerpt:**
```
[01:00] ولی می‌خوام یه چیز هم اضافه بشه که با انتخاب اون بتونه از همین توکن کلاد کد استفاده بکنه. یعنی تمام کارای مثلاً حالا توی قسمت اگه اون انتخاب شده باشه بتونه از آخرین ایجنت‌هایی که مال اون کلاد کد هست استفاده بکنه.
```

### Step 7: تست‌های واحد و یکپارچه‌سازی بک‌اند
**Status:** `done` (100%)
**Scope:** این مرحله شامل نوشتن و اجرای تست‌های واحد (Unit Tests) برای ماژول دسترسی به توکن Cloud Code، سرویس تعامل با ایجنت‌های Cloud Code و Endpointهای جدید API است. همچنین، تست‌های یکپارچه‌سازی (Integration Tests) برای تأیید عملکرد صحیح مسیریابی درخواست‌ها در بک‌اند و ارتباط بین سرویس‌های مختلف انجام می‌شود. هدف اطمینان از صحت، امنیت و پایداری اجزای بک‌اند است.
**Excerpt:**
```
[01:30] مثلاً پرامپت رو خودش ساختارمند کنه و کارای اجرایی و اون اجرای پرامپت و تمام چیزایی که الان مثلاً مدل‌ها مدل‌های متقاعد فعلی انجام می‌دن رو اون انجام بده. چون اینجوری هم دیگه هزینه کلید نمی‌دم و هم از آخرین مدل‌های کلاد کد دارم استفاده می‌کنم که هزینه‌ای هم نداره و فقط از اشتراک ماهانه‌ام اون زمانشو فقط داره استفاده می‌کنه.
```

### Step 8: تست‌های رابط کاربری و End-to-End
**Status:** `done` (100%)
**Scope:** این مرحله شامل توسعه و اجرای تست‌های رابط کاربری (UI Tests) برای تأیید نمایش صحیح و عملکرد عنصر انتخاب Cloud Code در 'تب بازرس ویژه' است. علاوه بر این، تست‌های End-to-End (E2E Tests) برای شبیه‌سازی کامل جریان کاربر از انتخاب Cloud Code، ارسال پرامپت و دریافت پاسخ از طریق سیستم کامل (فرانت‌اند و بک‌اند) انجام می‌شود. این تست‌ها باید اطمینان حاصل کنند که تجربه کاربری یکپارچه و بدون مشکل است.
**Excerpt:**
```
[02:00] یعنی لیمیت زمانو فقط استفاده می‌کنه. و این خیلی قوی‌تره. اونا حذف نشه، اونا سر جای خودش باشه ولی اینم یه گزینه باشه که اگه انتخابش کردم از این طریق باشه.
```

### Step 9: مستندسازی و آماده‌سازی برای استقرار
**Status:** `done` (100%)
**Scope:** این مرحله شامل به‌روزرسانی مستندات فنی مربوط به معماری جدید، APIهای اضافه شده، و نحوه پیکربندی و استقرار سرویس Cloud Code است. همچنین، شامل آماده‌سازی محیط‌های استقرار (مانند تنظیم متغیرهای محیطی، مدیریت دسترسی‌ها و پیکربندی‌های لازم) برای انتقال کد به محیط‌های تست و تولید است. هدف اطمینان از قابلیت نگهداری و استقرار آسان سیستم است.
**Excerpt:**
```
[00:00] می‌خوام با توجه به اینکه الان دیگه توکن کلاد کد رو ما داریم و می‌تونیم وصل بشیم به کلاد کد بدون اینکه از ای پی آی استفاده بکنه و فقط از اشتراک ماهانه‌ام استفاده بکنه و الان این رو در واقع توی مرکز نظارت و جاهایی اینو متصل کردیم و داره توی ورک فلو هم استفاده میشه.
```
