---
task_id: 734c83d3-6b6c-406c-a4de-39a0264a90d3
title: عدم وجود فایل package.json برای Frontend
type: bug
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-10T15:27:10.029596+00:00'
updated_at: '2026-06-03T04:13:07.373715+00:00'
archived: true
archived_at: '2026-05-11T20:17:17.975178+00:00'
target_files:
- frontend/package.json
---

# عدم وجود فایل package.json برای Frontend

## Raw Idea

پروژه فاقد فایل package.json برای Frontend است. این فایل برای مدیریت وابستگی‌های JavaScript/TypeScript ضروری است. بدون آن، نصب npm packages، اجرای دستورات build و تست غیرممکن است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف
عدم وجود فایل package.json برای Frontend

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/package.json` — فایل وجود ندارد - باید ایجاد شود

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Node.js 18+, React/Next.js, npm/yarn

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/` (سطر 1) — کل پوشه frontend نیازمند package.json است

## 🌐 نقشهٔ وابستگی‌ها
این تسک برای کل Frontend حیاتی است. بدون package.json، توسعه و استقرار Frontend غیرممکن است.

## 🔍 Context و وضعیت فعلی
پروژه فاقد فایل package.json برای Frontend است. این فایل برای مدیریت وابستگی‌های JavaScript/TypeScript ضروری است. بدون آن، نصب npm packages، اجرای دستورات build و تست غیرممکن است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] فایل package.json در مسیر frontend/ وجود داشته باشد
- [ ] دستور npm install بدون خطا اجرا شود
- [ ] دستور npm run build (یا معادل) بدون خطا اجرا شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد فایل package.json در مسیر frontend/ با لیست کامل وابستگی‌ها (React, Next.js, axios, tailwindcss و غیره) و اسکریپت‌های لازم (dev, build, lint, test). همچنین اضافه کردن package-lock.json یا yarn.lock.

## 💡 نمونه‌های قبل/بعد
**قبل: عدم وجود package.json**

_قبل:_
```
frontend/ (بدون package.json)
```

_بعد:_
```
frontend/package.json (شامل dependencies و scripts)
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd frontend && npm install`
- `cd frontend && npm run build`

## ⚠️ ریسک‌ها و موارد احتیاط
احتمال ناسازگاری نسخه‌های وابستگی‌ها با کد موجود. نیاز به تست کامل پس از نصب.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: small

## Acceptance Criteria

1. فایل package.json در مسیر frontend/ وجود داشته باشد _(verify: static)_
2. دستور npm install بدون خطا اجرا شود _(verify: backend_test)_
3. دستور npm run build (یا معادل) بدون خطا اجرا شود _(verify: backend_test)_
