---
task_id: 3e009678-6312-438d-90c2-578d446b8cb9
title: '[منطق] عدم هماهنگی بین prompt_helper و oversight_strong_prompt در مدیریت متغیرها'
type: logic_audit
priority: high
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T17:37:48.677899+00:00'
updated_at: '2026-06-02T17:48:57.060243+00:00'
archived: true
archived_at: '2026-05-18T04:29:09.429776+00:00'
tags:
- merged
---

# [منطق] عدم هماهنگی بین prompt_helper و oversight_strong_prompt در مدیریت متغیرها

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

prompt_helper از variables dictionary برای جایگزینی در پرامپت‌های دیتابیسی استفاده می‌کند، در حالی که oversight_strong_prompt پارامترهای مشخصی (title, user_goal, target_locations) را به صورت positional دریافت می‌کند. این دو رویکرد با هم ناسازگار هستند و ممکن است تداخل ایجاد کنند.

## 💥 پیامد (impact)
اگر oversight_strong_prompt بخواهد از prompt_helper برای بارگذاری پرامپت‌های پویا استفاده کند، ساختار متغیرها mismatch خواهد داشت. این باعث خطاهای runtime یا پرامپت‌های ناقص می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند. oversight_strong_prompt باید بازنویسی شود تا پارامترهای خود را از context dictionary استخراج کند، نه از positional arguments.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم هماهنگی بین prompt_helper و oversight_strong_prompt در مدیریت متغیرها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

prompt_helper از variables dictionary برای جایگزینی در پرامپت‌های دیتابیسی استفاده می‌کند، در حالی که oversight_strong_prompt پارامترهای مشخصی (title, user_goal, target_locations) را به صورت positional دریافت می‌کند. این دو رویکرد با هم ناسازگار هستند و ممکن است تداخل ایجاد کنند.

## 💥 پیامد (impact)
اگر oversight_strong_prompt بخواهد از prompt_helper برای بارگذاری پرامپت‌های پویا استفاده کند، ساختار متغیرها mismatch خواهد داشت. این باعث خطاهای runtime یا پرامپت‌های ناقص می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند. oversight_strong_prompt باید بازنویسی شود تا پارامترهای خود را از context dictionary استخراج کند، نه از positional arguments.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و شناسایی فایل‌های مرتبط با prompt_helper و oversight_strong_prompt در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن تمام فایل‌های مرتبط با prompt_helper و oversight_strong_prompt در pipeline ai_llm است. باید مشخص شود که prompt_helper چگونه از variables dictionary استفاده می‌کند و oversight_strong_prompt چگونه پارامترهای positional (title, user_goal, target_locations) را دریافت می‌کند. همچنین باید فایل‌های دیگری که از این دو ماژول استفاده می‌کنند شناسایی شوند. خارج از این مرحله: تغییر کد، نوشتن تست، یا هرگونه اصلاح. نکته حیاتی: این مرحله صرفاً اکتشافی است و باید تمام وابستگی‌ها و نحوه استفاده از این دو ماژول در سراسر پروژه مستند شود.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

prompt_helper از variables dictionary برای جایگزینی در پرامپت‌های دیتابیسی استفاده می‌کند، در حالی که oversight_strong_prompt پارامترهای مشخصی (title, user_goal, target_locations) را به صورت positional دریافت می‌کند. این دو رویکرد با هم ناسازگار هستند و ممکن است تداخل ایجاد کنند.

📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_
```

### Step 2: طراحی interface واحد (Protocol/ABC) برای همه سازنده‌های پرامپت با context dictionary
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل طراحی و پیاده‌سازی یک interface انتزاعی (با استفاده از Protocol یا ABC در Python) است که یک متد واحد مانند build_prompt(context: dict) -> str را تعریف می‌کند. این interface باید استاندارد کند که همه سازنده‌های پرامپت از یک dictionary به نام context استفاده کنند. خارج از این مرحله: تغییر پیاده‌سازی موجود prompt_helper یا oversight_strong_prompt. نکته حیاتی: این interface باید به اندازه کافی generic باشد تا همه موارد استفاده فعلی و آینده را پوشش دهد، اما آنقدر ساده باشد که adoption آسان باشد.
**Excerpt:**
```
یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند. oversight_strong_prompt باید بازنویسی شود تا پارامترهای خود را از context dictionary استخراج کند، نه از positional arguments.
```

### Step 3: بازنویسی prompt_helper برای استفاده از context dictionary و interface جدید
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بازنویسی prompt_helper است تا به جای استفاده از variables dictionary به صورت مستقیم، از context dictionary استاندارد استفاده کند و interface تعریف شده در مرحله قبل را implement کند. همچنین باید اطمینان حاصل شود که تمام کدهایی که از prompt_helper استفاده می‌کنند با signature جدید سازگار هستند. خارج از این مرحله: تغییر oversight_strong_prompt. نکته حیاتی: باید backward compatibility تا حد امکان حفظ شود یا migration path واضحی ارائه دهد.
**Excerpt:**
```
prompt_helper از variables dictionary برای جایگزینی در پرامپت‌های دیتابیسی استفاده می‌کند... یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند.
```

### Step 4: بازنویسی oversight_strong_prompt برای دریافت پارامترها از context dictionary به جای positional arguments
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بازنویسی oversight_strong_prompt است تا به جای دریافت title, user_goal, target_locations به صورت positional arguments، آن‌ها را از context dictionary استخراج کند. همچنین باید interface جدید را implement کند. تمام کدهایی که oversight_strong_prompt را صدا می‌زنند باید به روز شوند. خارج از این مرحله: تغییر prompt_helper. نکته حیاتی: باید اطمینان حاصل شود که keys استاندارد در context dictionary (مثلاً 'title', 'user_goal', 'target_locations') به درستی تعریف و مستند شده‌اند.
**Excerpt:**
```
oversight_strong_prompt باید بازنویسی شود تا پارامترهای خود را از context dictionary استخراج کند، نه از positional arguments.

oversight_strong_prompt پارامترهای مشخصی (title, user_goal, target_locations) را به صورت positional دریافت می‌کند.
```

### Step 5: به‌روزرسانی تمام کدهای استفاده‌کننده از oversight_strong_prompt برای استفاده از context dictionary
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی تمام مکان‌هایی است که oversight_strong_prompt صدا زده می‌شود و به‌روزرسانی آن‌ها برای استفاده از context dictionary به جای positional arguments. این شامل pipeline ai_llm و هر ماژول دیگری است که از oversight_strong_prompt استفاده می‌کند. خارج از این مرحله: تغییر prompt_helper یا سایر سازنده‌های پرامپت. نکته حیاتی: باید اطمینان حاصل شود که تمام callerها keys صحیح را در context dictionary قرار می‌دهند.
**Excerpt:**
```
اگر oversight_strong_prompt بخواهد از prompt_helper برای بارگذاری پرامپت‌های پویا استفاده کند، ساختار متغیرها mismatch خواهد داشت. این باعث خطاهای runtime یا پرامپت‌های ناقص می‌شود.
```

### Step 6: نوشتن تست‌های unit برای interface جدید و هر دو سازنده پرامپت (prompt_helper و oversight_strong_prompt)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های unit برای: 1) interface جدید (مثلاً اینکه همه implementerها signature صحیح دارند)، 2) prompt_helper با context dictionary، 3) oversight_strong_prompt با context dictionary. تست‌ها باید موارد edge مانند keys گمشده، keys اضافی، و types مختلف را پوشش دهند. خارج از این مرحله: integration tests یا end-to-end tests. نکته حیاتی: تست‌ها باید در isolation اجرا شوند و به دیتابیس یا سرویس‌های خارجی وابسته نباشند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند.
```

### Step 7: نوشتن integration test برای pipeline ai_llm با استفاده از سازنده‌های پرامپت جدید
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test است که pipeline ai_llm را با استفاده از prompt_helper و oversight_strong_prompt جدید (که هر دو از context dictionary استفاده می‌کنند) اجرا می‌کند. تست باید یک سناریوی واقعی را شبیه‌سازی کند و خروجی نهایی را بررسی کند. خارج از این مرحله: تست‌های unit (مرحله قبل) یا تست‌های end-to-end. نکته حیاتی: این تست باید نشان دهد که دو سازنده پرامپت با هم هماهنگ کار می‌کنند و هیچ mismatch وجود ندارد.
**Excerpt:**
```
اگر oversight_strong_prompt بخواهد از prompt_helper برای بارگذاری پرامپت‌های پویا استفاده کند، ساختار متغیرها mismatch خواهد داشت. این باعث خطاهای runtime یا پرامپت‌های ناقص می‌شود.

این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 8: مستندسازی interface جدید و استاندارد context dictionary در docs/ یا README
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن مستندات برای interface جدید (PromptBuilder) و استاندارد context dictionary است. باید مشخص شود که چه keys در context dictionary مجاز هستند، چه typeهایی دارند، و چگونه باید از آن‌ها استفاده کرد. همچنین باید migration guide برای توسعه‌دهندگانی که از positional arguments استفاده می‌کردند ارائه شود. خارج از این مرحله: تغییر کد یا نوشتن تست. نکته حیاتی: مستندات باید به زبان ساده و با مثال‌های عملی باشد.
**Excerpt:**
```
یک interface واحد برای همه سازنده‌های پرامپت تعریف کنید که از یک dictionary استاندارد (context) استفاده کند.
```

### Step 9: بررسی و حذف کدهای قدیمی مرتبط با positional arguments در صورت عدم استفاده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی تمام کدهایی است که از positional arguments قدیمی استفاده می‌کنند و حذف آن‌ها در صورت عدم استفاده. اگر کدی هنوز به positional arguments وابسته است، باید migration کامل شود. خارج از این مرحله: تغییر interface یا نوشتن تست جدید. نکته حیاتی: قبل از حذف، باید با grep/search مطمئن شد که هیچ کد active دیگری به آن وابسته نیست.
**Excerpt:**
```
oversight_strong_prompt پارامترهای مشخصی (title, user_goal, target_locations) را به صورت positional دریافت می‌کند.

این دو رویکرد با هم ناسازگار هستند و ممکن است تداخل ایجاد کنند.
```
