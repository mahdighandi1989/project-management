---
task_id: ad0a47f0-302a-4a79-a8a3-728263db2ae7
title: '[Effectiveness] عدم وجود خطا در ۳۰ روز اخیر نشان‌دهنده پوشش ناقص سناریوهای خطا است'
type: logic_audit
priority: medium
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T07:41:34.840865+00:00'
updated_at: '2026-05-20T04:24:13.293438+00:00'
archived: true
archived_at: '2026-05-18T04:23:32.019762+00:00'
tags:
- merged
---

# [Effectiveness] عدم وجود خطا در ۳۰ روز اخیر نشان‌دهنده پوشش ناقص سناریوهای خطا است

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود

## 📊 وضعیت فعلی
error_rate_30d: 0.0% - هیچ خطایی ثبت نشده است که می‌تواند به دلیل عدم تست سناریوهای شکست سرویس‌های AI باشد

## 🛠 اقدام پیشنهادی
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] عدم وجود خطا در ۳۰ روز اخیر نشان‌دهنده پوشش ناقص سناریوهای خطا است

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود

## 📊 وضعیت فعلی
error_rate_30d: 0.0% - هیچ خطایی ثبت نشده است که می‌تواند به دلیل عدم تست سناریوهای شکست سرویس‌های AI باشد

## 🛠 اقدام پیشنهادی
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: static)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: static)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: static)_

## Task Steps

### Step 1: بررسی و تحلیل وضعیت فعلی ai_manager و fallback mechanism
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و بررسی کامل کدهای موجود در repository برای یافتن فایل‌های مرتبط با ai_manager، مکانیزم fallback، و هرگونه تست موجود است. باید مشخص شود که آیا fallback mechanism از قبل پیاده‌سازی شده یا خیر، و اگر شده، چه سناریوهای خطایی پوشش داده شده است. این مرحله شامل هیچ تغییری در کد نیست و صرفاً یک audit اولیه است. نکته حیاتی: قبل از هر اقدامی، باید با grep/search دقیق، تمام فایل‌های مرتبط با AI manager و fallback را شناسایی کرد.
**Excerpt:**
```
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
```

### Step 2: طراحی و پیاده‌سازی سناریوهای شکست برای ai_manager (قطع connection به OpenAI)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد (unit tests) برای ai_manager است که سناریوی قطع connection به OpenAI را شبیه‌سازی می‌کند. باید از mocking یا monkey-patching برای شبیه‌سازی timeout یا خطای network استفاده شود. تست باید بررسی کند که در صورت بروز خطا، fallback mechanism فعال شده و مدل جایگزین (مثلاً Anthropic یا مدل محلی) فراخوانی می‌شود. این مرحله شامل تست‌های E2E یا integration tests نیست. نکته حیاتی: تست‌ها باید مستقل از سرویس‌های خارجی باشند و با mock کار کنند.
**Excerpt:**
```
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

## 📊 وضعیت فعلی
error_rate_30d: 0.0% - هیچ خطایی ثبت نشده است که می‌تواند به دلیل عدم تست سناریوهای شکست سرویس‌های AI باشد
```

### Step 3: طراحی و پیاده‌سازی سناریوهای شکست برای ai_manager (timeout سرویس AI)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای شبیه‌سازی timeout سرویس AI (مثلاً OpenAI) است. باید با استفاده از mock، یک timeout مصنوعی ایجاد کرد و بررسی کرد که سیستم پس از timeout به مدل جایگزین fallback می‌کند. همچنین باید بررسی شود که timeout به درستی در لاگ‌ها ثبت می‌شود. این مرحله مستقل از مرحله قبل است و سناریوی متفاوتی را پوشش می‌دهد. نکته حیاتی: timeout باید قابل تنظیم باشد تا در تست‌های مختلف قابل استفاده باشد.
**Excerpt:**
```
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود

افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین
```

### Step 4: طراحی و پیاده‌سازی سناریوهای شکست برای ai_manager (خطای authentication/API key نامعتبر)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای شبیه‌سازی خطای authentication (مثلاً 401 Unauthorized از OpenAI) است. باید با mock کردن پاسخ API، یک خطای authentication ایجاد کرد و بررسی کرد که سیستم به مدل جایگزین fallback می‌کند. همچنین باید بررسی شود که خطا به درستی در لاگ‌ها ثبت می‌شود. این مرحله سناریوی متفاوتی از دو مرحله قبل را پوشش می‌دهد. نکته حیاتی: تست باید بررسی کند که fallback به مدل جایگزین با API key متفاوت انجام می‌شود.
**Excerpt:**
```
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند
```

### Step 5: طراحی و پیاده‌سازی سناریوهای شکست برای ai_manager (خطای rate limit)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای شبیه‌سازی خطای rate limit (مثلاً 429 Too Many Requests از OpenAI) است. باید با mock کردن پاسخ API، یک خطای rate limit ایجاد کرد و بررسی کرد که سیستم پس از retry یا fallback به مدل جایگزین، درخواست را پردازش می‌کند. همچنین باید بررسی شود که rate limit به درستی در لاگ‌ها ثبت می‌شود. نکته حیاتی: تست باید بررسی کند که سیستم به طور خودکار retry نمی‌کند مگر اینکه policy مشخصی وجود داشته باشد.
**Excerpt:**
```
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند
```

### Step 6: طراحی و پیاده‌سازی سناریوهای شکست برای ai_manager (خطای invalid response format از سرویس AI)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای شبیه‌سازی خطای invalid response format (مثلاً پاسخ JSON نامعتبر یا فیلدهای缺失) از سرویس AI است. باید با mock کردن پاسخ API، یک پاسخ نامعتبر ایجاد کرد و بررسی کرد که سیستم به مدل جایگزین fallback می‌کند. همچنین باید بررسی شود که خطا به درستی در لاگ‌ها ثبت می‌شود. نکته حیاتی: این سناریو باید شامل مواردی مثل missing fields, wrong types, empty response باشد.
**Excerpt:**
```
افزودن تست‌های سناریوی شکست برای ai_manager (مثلاً قطع connection به OpenAI) و بررسی فعال شدن fallback به مدل جایگزین

سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند
```

### Step 7: افزودن metric/log برای ثبت نرخ خطا در production
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن metric یا log structure به ai_manager است که نرخ خطاهای کنترل‌شده (مثل timeout, authentication error, rate limit, invalid response) را در production ثبت کند. باید یک metric با نام error_rate یا معادل آن ایجاد شود که تعداد خطاها را بر تعداد کل درخواست‌ها تقسیم کند. همچنین باید یک log با سطح ERROR برای هر خطا و یک log با سطح INFO برای fallback ثبت شود. نکته حیاتی: metric باید قابل query در سیستم monitoring (مثل Prometheus, Datadog, یا custom dashboard) باشد.
**Excerpt:**
```
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود

metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 8: نوشتن تست E2E که outcome target را اندازه می‌گیرد
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست E2E (end-to-end) است که سناریوی واقعی شکست سرویس AI را شبیه‌سازی می‌کند و بررسی می‌کند که outcome target (حداقل ۲-۳٪ خطاهای کنترل‌شده) محقق می‌شود. تست باید با استفاده از mock server یا stub برای سرویس AI، چندین درخواست با خطاهای مختلف ارسال کند و سپس بررسی کند که نرخ خطا در metric/log به مقدار مورد نظر رسیده است. نکته حیاتی: تست باید مستقل از محیط production باشد و با استفاده از test fixtures کار کند.
**Excerpt:**
```
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود

test E2E که outcome را اندازه می‌گیرد عبور می‌کند
```

### Step 9: بازنویسی outcome target به صورت measurable و اضافه کردن به documentation
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بازنویسی outcome target (هدف مطلوب) به صورت measurable و قابل اندازه‌گیری است. باید مشخص شود که «حداقل ۲-۳٪ خطاهای کنترل‌شده» به چه معناست و چگونه اندازه‌گیری می‌شود. همچنین باید این target به documentation پروژه (مثلاً README یا docs/outcome_targets.md) اضافه شود. نکته حیاتی: target باید با metric/log اضافه شده در مرحله قبل همخوانی داشته باشد.
**Excerpt:**
```
outcome target به‌صورت measurable بازنویسی شد

## 🎯 هدف مطلوب (outcome target)
سیستم باید حداقل ۲-۳٪ خطاهای کنترل‌شده در شرایط مرزی (مثلاً timeout سرویس AI) ثبت کند تا اطمینان حاصل شود fallback mechanism به درستی فعال می‌شود
```

### Step 10: اجرای تست‌ها و بررسی عدم شکست تست‌های موجود
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای کامل تست‌های پروژه (pytest) و بررسی اینکه هیچ تستی fail نمی‌شود. همچنین باید linter (مثل flake8, pylint) و type-checker (مثل mypy) را اجرا کرد و مطمئن شد که هیچ warning یا خطایی وجود ندارد. اگر تست‌های جدید fail شوند، باید اصلاح شوند. نکته حیاتی: این مرحله باید بعد از تمام تغییرات انجام شود و تضمین کند که regression رخ نداده است.
**Excerpt:**
```
هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
linter بدون warning عبور می‌کند
type-check موفق
```
