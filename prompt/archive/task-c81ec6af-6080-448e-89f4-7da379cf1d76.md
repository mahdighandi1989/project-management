---
task_id: c81ec6af-6080-448e-89f4-7da379cf1d76
title: '[منطق] عدم وجود مکانیزم fallback مشخص در ai_manager'
type: logic_audit
priority: high
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T06:39:16.190950+00:00'
updated_at: '2026-06-02T17:45:28.282571+00:00'
archived: true
archived_at: '2026-05-18T04:23:32.019762+00:00'
tags:
- merged
---

# [منطق] عدم وجود مکانیزم fallback مشخص در ai_manager

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

مستندات به 'مدیریت fallback' اشاره دارد، اما جزئیات آن مشخص نیست. با توجه به اینکه ai_manager با چندین سرویس (OpenAI, Claude) کار می‌کند، یک استراتژی fallback شفاف (مثلاً ترتیب fallback، timeout، تعداد تلاش مجدد) ضروری است. همچنین مشخص نیست که آیا fallback بر اساس خطاهای سرویس (مثلاً rate limit) یا کیفیت پاسخ (validation failure) انجام می‌شود.

## 💥 پیامد (impact)
در صورت خرابی یک سرویس AI، pipeline بدون fallback می‌ماند و خطا به کاربر نمایش داده می‌شود. عدم مدیریت هوشمند fallback می‌تواند منجر به افزایش latency یا هزینه شود.

## 🛠 پیشنهاد رفع اولیه
یک استراتژی fallback واضح در ai_manager پیاده‌سازی کنید: ترتیب fallback (مثلاً OpenAI -> Claude -> ...)، حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود مکانیزم fallback مشخص در ai_manager

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

مستندات به 'مدیریت fallback' اشاره دارد، اما جزئیات آن مشخص نیست. با توجه به اینکه ai_manager با چندین سرویس (OpenAI, Claude) کار می‌کند، یک استراتژی fallback شفاف (مثلاً ترتیب fallback، timeout، تعداد تلاش مجدد) ضروری است. همچنین مشخص نیست که آیا fallback بر اساس خطاهای سرویس (مثلاً rate limit) یا کیفیت پاسخ (validation failure) انجام می‌شود.

## 💥 پیامد (impact)
در صورت خرابی یک سرویس AI، pipeline بدون fallback می‌ماند و خطا به کاربر نمایش داده می‌شود. عدم مدیریت هوشمند fallback می‌تواند منجر به افزایش latency یا هزینه شود.

## 🛠 پیشنهاد رفع اولیه
یک استراتژی fallback واضح در ai_manager پیاده‌سازی کنید: ترتیب fallback (مثلاً OpenAI -> Claude -> ...)، حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی ai_manager و pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن فایل‌های مرتبط با ai_manager، pipeline ai_llm، و هرگونه پیاده‌سازی fallback موجود است. باید فایل‌های اصلی (مانند ai_manager.py، هرگونه کلاس سرویس OpenAI/Claude، فایل‌های config، و pipeline) شناسایی شوند. همچنین باید بررسی شود که آیا استراتژی fallback، timeout، retry، یا backoff قبلاً پیاده‌سازی شده است یا خیر. خروجی این مرحله یک مستند کوتاه (در قالب کامیت یا فایل markdown) از وضعیت فعلی است. این مرحله شامل هیچ تغییری در کد نمی‌شود.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد: مستندات به 'مدیریت fallback' اشاره دارد، اما جزئیات آن مشخص نیست. با توجه به اینکه ai_manager با چندین سرویس (OpenAI, Claude) کار می‌کند، یک استراتژی fallback شفاف (مثلاً ترتیب fallback، timeout، تعداد تلاش مجدد) ضروری است. همچنین مشخص نیست که آیا fallback بر اساس خطاهای سرویس (مثلاً rate limit) یا کیفیت پاسخ (validation failure) انجام می‌شود. ... پیش از شروع، با grep/search و خواندن فایل‌های مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
```

### Step 2: طراحی و پیاده‌سازی کلاس FallbackStrategy با پشتیبانی از ترتیب fallback
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک کلاس جدید (مثلاً FallbackStrategy) در ماژول ai_manager است که ترتیب fallback بین سرویس‌ها (مثلاً OpenAI -> Claude) را مدیریت کند. این کلاس باید یک لیست مرتب از سرویس‌ها را دریافت کرده و متدی برای انتخاب سرویس بعدی در صورت شکست ارائه دهد. همچنین باید از یک استراتژی configurable پشتیبانی کند (مثلاً از طریق فایل config یا environment variable). این مرحله شامل timeout، retry، یا validation نمی‌شود.
**Excerpt:**
```
یک استراتژی fallback واضح در ai_manager پیاده‌سازی کنید: ترتیب fallback (مثلاً OpenAI -> Claude -> ...)، حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 3: پیاده‌سازی مکانیزم timeout برای هر سرویس AI
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن قابلیت timeout به فراخوانی‌های سرویس‌های AI (OpenAI, Claude) در ai_manager است. باید یک timeout configurable (مثلاً از طریق config یا environment variable) برای هر سرویس تعریف شود. اگر سرویس در زمان مشخص شده پاسخ ندهد، باید خطای timeout صادر شود تا مکانیزم fallback فعال گردد. این مرحله شامل retry یا backoff نمی‌شود.
**Excerpt:**
```
حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 4: پیاده‌سازی مکانیزم retry با backoff برای خطاهای موقت سرویس
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن قابلیت retry با backoff (مثلاً exponential backoff) به فراخوانی‌های سرویس‌های AI است. باید تعداد تلاش مجدد configurable (مثلاً max_retries) و فاصله زمانی بین تلاش‌ها (مثلاً base_delay) تعریف شود. retry فقط برای خطاهای موقت (مثلاً rate limit, 5xx) انجام شود، نه برای خطاهای دائمی (مثلاً 4xx). این مرحله شامل fallback به سرویس دیگر نمی‌شود.
**Excerpt:**
```
تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 5: تعریف شرط‌های fallback: خطای سرویس، خطای validation، timeout
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تعریف منطق شرط‌های fallback در ai_manager است. باید مشخص شود که در چه شرایطی fallback به سرویس بعدی فعال شود: (1) خطای سرویس (مثلاً 5xx, rate limit بعد از retry), (2) خطای validation (پاسخ سرویس معتبر نیست), (3) timeout. این منطق باید در یک تابع یا کلاس جداگانه (مثلاً FallbackCondition) پیاده‌سازی شود که ورودی خطا را گرفته و تصمیم بگیرد آیا fallback لازم است یا خیر.
**Excerpt:**
```
شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 6: یکپارچه‌سازی FallbackStrategy، timeout، retry، و شرط‌ها در ai_manager
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یکپارچه‌سازی همه اجزای ساخته شده در مراحل قبل (FallbackStrategy, timeout, retry, FallbackCondition) در کلاس اصلی ai_manager است. باید متد اصلی فراخوانی AI (مثلاً call_ai) بازنویسی شود تا: (1) سرویس فعلی را با timeout فراخوانی کند, (2) در صورت خطا، retry کند, (3) بعد از retry، شرط fallback را بررسی کند, (4) در صورت نیاز، به سرویس بعدی fallback کند. این مرحله شامل تست integration نیست.
**Excerpt:**
```
یک استراتژی fallback واضح در ai_manager پیاده‌سازی کنید: ترتیب fallback (مثلاً OpenAI -> Claude -> ...)، حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff، و شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 7: افزودن logging و metrics برای fallback events
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن logging مناسب برای تمام رویدادهای fallback است: زمان fallback، سرویس مبدأ، سرویس مقصد، دلیل fallback (timeout, error, validation). همچنین می‌توان metrics ساده (مثلاً شمارنده fallback) به logging اضافه کرد. این logging باید در سطح INFO باشد تا در monitoring قابل مشاهده باشد. این مرحله شامل پیاده‌سازی dashboard یا alerting نیست.
**Excerpt:**
```
در صورت خرابی یک سرویس AI، pipeline بدون fallback می‌ماند و خطا به کاربر نمایش داده می‌شود. عدم مدیریت هوشمند fallback می‌تواند منجر به افزایش latency یا هزینه شود.
```

### Step 8: نوشتن unit tests برای FallbackStrategy
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests کامل برای کلاس FallbackStrategy است. تست‌ها باید موارد زیر را پوشش دهند: (1) ترتیب صحیح fallback, (2) بازگشت None بعد از اتمام سرویس‌ها, (3) پشتیبانی از config پویا, (4) رفتار با لیست خالی. این تست‌ها باید با pytest و mock نوشته شوند.
**Excerpt:**
```
یک استراتژی fallback واضح در ai_manager پیاده‌سازی کنید: ترتیب fallback (مثلاً OpenAI -> Claude -> ...)
```

### Step 9: نوشتن unit tests برای timeout و retry logic
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای مکانیزم timeout و retry با backoff است. تست‌ها باید موارد زیر را پوشش دهند: (1) timeout صحیح, (2) تعداد retry صحیح, (3) backoff exponential, (4) عدم retry برای خطاهای دائمی. این تست‌ها باید با mock سرویس و asyncio/time mocking نوشته شوند.
**Excerpt:**
```
حداکثر زمان انتظار (timeout) برای هر سرویس، تعداد تلاش مجدد (retry) با backoff
```

### Step 10: نوشتن unit tests برای FallbackCondition
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای تابع/کلاس FallbackCondition است. تست‌ها باید موارد زیر را پوشش دهند: (1) fallback برای ServiceError, (2) fallback برای ValidationError, (3) fallback برای TimeoutError, (4) عدم fallback برای خطاهای دیگر (مثلاً ValueError).
**Excerpt:**
```
شرط‌های fallback (خطای سرویس، خطای validation، timeout).
```

### Step 11: نوشتن integration tests برای fallback کامل در ai_manager
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن integration tests برای سناریوی کامل fallback در ai_manager است. تست‌ها باید با mock کردن سرویس‌های OpenAI و Claude، سناریوهای زیر را شبیه‌سازی کنند: (1) سرویس اول fail (timeout) -> fallback به سرویس دوم, (2) سرویس اول fail (validation) -> fallback به سرویس دوم, (3) همه سرویس‌ها fail -> خطای نهایی, (4) سرویس اول موفق -> بدون fallback. این تست‌ها باید از ai_manager به صورت یکپارچه استفاده کنند.
**Excerpt:**
```
در صورت خرابی یک سرویس AI، pipeline بدون fallback می‌ماند و خطا به کاربر نمایش داده می‌شود.
```

### Step 12: به‌روزرسانی مستندات ai_manager با استراتژی fallback
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل به‌روزرسانی مستندات موجود (مثلاً README یا docs/) برای توضیح استراتژی fallback جدید است. باید موارد زیر مستند شود: (1) ترتیب fallback پیش‌فرض, (2) نحوه config کردن timeout و retry, (3) شرط‌های fallback, (4) نحوه مشاهده logهای fallback. این مستندات باید به زبان ساده و قابل فهم برای توسعه‌دهندگان باشد.
**Excerpt:**
```
مستندات به 'مدیریت fallback' اشاره دارد، اما جزئیات آن مشخص نیست.
```
