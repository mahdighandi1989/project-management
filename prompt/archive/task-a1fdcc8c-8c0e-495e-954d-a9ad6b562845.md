---
task_id: a1fdcc8c-8c0e-495e-954d-a9ad6b562845
title: '# 🎯 پرامپت Phase 5 V4 — ارتقای کامل Auto-Scan + Task Creation (10 مرحله)'
type: refactor
priority: critical
execution_priority: 100
status: done
external_status: pending
verification_status: done
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-14T18:22:41.705269+00:00'
updated_at: '2026-05-20T04:22:22.041280+00:00'
archived: true
archived_at: '2026-05-14T19:04:51.507242+00:00'
target_files:
- backend/app/services/scan_v5/comprehensive_inventory.py
- backend/app/services/scan_v5/purpose_extractor.py
- backend/app/models/oversight.py
---

# # 🎯 پرامپت Phase 5 V4 — ارتقای کامل Auto-Scan + Task Creation (10 مرحله)

## Raw Idea

# 🎯 پرامپت Phase 5 V4 — ارتقای کامل Auto-Scan + Task Creation
> **این پرامپت تمام ۱۴ بند درخواست خام را پوشش می‌دهد. هر فاز با reference به بندهای درخواست خام نشان داده شده.**
---
## 📋 فهرست بندهای درخواست خام (Cross-reference)
| # | بند درخواست خام | پوشش در فاز |
|---|---|---|
| **R1** | scan جدا از verify؛ scan ایجاد task، verify بررسی task | مقدمه |
| **R2** | ایده‌گیری از Phase 4 + به‌روزرسانی گزینه‌های UI | کل فازها + فاز ۹ |
| **R3** | پوشش از ریز تا درشت، یک دور برای کل سیستم | فاز ۱ |
| **R4** | scan باید قوی‌تر از verify باشد | کل فازها |
| **R5** | چک‌لیست برای auto-tasks + Smart mode هوشمند | فاز ۷، ۸ |
| **R6** | Telegram notification برای auto-tasks (caption + silent default) | فاز ۸ |
| **R7** | Delta detection + Bidirectional dependency | فاز ۳ |
| **R8** | شناسایی گزینه‌های قدیمی + task برای آن‌ها (⭐ خیلی خیلی مهم) | فاز ۲ |
| **R9** | Smart prompt + Smart checklist (بدون false-positive) | فاز ۷ |
| **R10** | Logic Audit (هدف، منطق، coherence) | فاز ۵ |
| **R11** | مثال trade — باگ منطقی در هماهنگی | فاز ۵ |
| **R12** | Telegram notification system audit | فاز ۶ |
| **R13** | Smart prompt برای manual + auto | فاز ۷ |
| **R14** | Inspector tab integration (session + screenshots + chats + archive) | فاز ۴ |
---
## 📌 مقدمه و فلسفه
### **R1** — scan ≠ verify
| سیستم | چه می‌کند |
|---|---|
| **verify** (Phase 4، تمام) | روی **تسک‌های موجود** بررسی می‌کند آیا انجام شده یا نه |
| **scan** (این Phase 5) | کل سیستم را بررسی می‌کند تا **تسک‌های واقعاً لازم** کشف و ایجاد شوند |
اگر scan خوب کار نکند → تسک واقعاً لازم اصلاً ساخته نمی‌شود → verify هرگز فرصت بررسی آن را پیدا نمی‌کند. **پس scan حتی از verify مهم‌تر است.**
### **R4** — scan باید قوی‌تر از verify باشد
verify شواهد جمع می‌کند برای **یک task مشخص**. scan شواهد جمع می‌کند برای **کل سیستم**. پس scan ابزارهای بیشتری نیاز دارد:
- همه‌ی Phase 4 components (smart_nav, vision, code-aware, backend-log, inspector_probe)
- **به‌علاوه**: comprehensive inventory، purpose extraction، delta analysis، logical coherence، outcome analysis، notification audit، semantic stale detection
### **چهار پایه**
1. **پوشش ریز تا درشت** (R3)
2. **تغییر-آگاهی + bidirectional dependency** (R7)
3. **رفتار-محوری در پرامپت و چک‌لیست** (R9, R13)
4. **logic awareness از روز اول — purpose، coherence، effectiveness** (R10, R11)
### **هدف نهایی**
بعد از یک scan، **تمام تسک‌های واقعاً لازم** کشف و ساخته شوند، **با چک‌لیست هوشمند، با notification صحیح، با session بازرس کامل**:
- features جدید
- features ناقص
- bugs ساختاری
- bugs منطقی (R11)
- features قدیمی و dead options (R8)
- componentهای منطقاً ناسازگار (R10)
- featuresی که کاربر دیگر نمی‌داند چیست (R8) → با AI explanation
- notification ها ناقص یا اشتباه (R12)
---
## 🅰️ فاز ۱ — Comprehensive Inventory + Purpose Extraction
> پوشش **R3** (ریز تا درشت)، پایه برای همه فازهای بعدی
### مسئله
الان `run_deep_scan` فقط ۳۵ فایل deep-read + import graph می‌بیند. backend endpoints، UI elements، DB schema، env vars، configs، **notifications، inspector sessions، UI options** هرگز inventory نمی‌شوند.
### تغییرات
**۱-A. Structural Inventory (۱۲ لایه):**
ماژول جدید: `backend/app/services/scan_v5/comprehensive_inventory.py`
| # | لایه | چه چیزی جمع می‌شود | روش |
|---|---|---|---|
| 1 | Files | همه فایل‌های git-tracked | `git ls-files` |
| 2 | Backend endpoints | همه `@router.{get,post,…}` + WebSocket + background tasks | AST parse |
| 3 | UI elements | همه `<button>`, `<form>`, `<input>`, `<Select>`, `<Link>`, modal triggers, dropdown items | parse `.tsx` |
| 4 | DB schema | همه dataclass + Column + migrations | inspect |
| 5 | Env vars | همه `os.environ.get`, `os.getenv`, `process.env.X` | regex |
| 6 | Config files | همه `.json`, `.yaml`, `.toml`, `.env*` | glob |
| 7 | Dependencies | `requirements.txt`, `package.json` | parse |
| 8 | Scripts | همه `.sh`, `pyproject::scripts`, `package.json::scripts` | parse |
| 9 | Cron/scheduled | همه `apscheduler`, `BackgroundTasks`, `asyncio.create_task` | regex |
| 10 | Routes | همه frontend `app/**/page.tsx` + backend route | walk |
| 11 | **Notification calls (R12)** | همه `notify_event(...)`, `send_telegram(...)`, `bot.send_message(...)` + event_type + silent flag + caption template | regex/AST |
| 12 | **UI options/settings (R8)** | همه checkbox/slider/dropdown در UI + field name در WatchedProject + default value | parse + cross-ref |
**۱-B. Purpose Extraction (R10):**
ماژول جدید: `backend/app/services/scan_v5/purpose_extractor.py`
برای **هر file/module/feature/option** که در inventory هست:
```python
purpose_inventory[item_id] = {
    "stated_purpose": "چه کاری *قرار است* بکند",
    "evidence_sources": ["comments", "docstrings", "tests", "raw_idea", "task_history", "commit_messages"],
    "expected_inputs": [...],
    "expected_outputs": [...],
    "interacting_with": [...],          # سایر componentهای همکار
    "creation_context": {                # R8 — کاربر فراموش کرده چی هست
        "first_seen_commit": "...",
        "first_seen_date": "...",
        "originating_task_id": "...",    # کدام task این را ساخت
        "originating_raw_idea": "...",   # متن خام درخواست
    },
    "current_usage": "...",              # آیا هنوز استفاده می‌شود؟ کجا؟
}
```
**منابع AI برای purpose:**
- محتوای فایل + docstrings + JSDoc
- test files مرتبط
- `WatchedProject.user_notes` (raw_idea اصلی پروژه)
- task history (task هایی که این item را modify کرده‌اند)
- commit messages روی این path
**۱-C. Storage:**
`WatchedProject` فیلدهای جدید:
```python
last_scan_inventory: Optional[Dict[str, Any]] = None
last_scan_purpose_map: Optional[Dict[str, Dict]] = None
last_scan_at_v5: Optional[str] = None  # timestamp scan جدید
```
### Acceptance Criteria فاز ۱
- `inventory.backend_endpoints` ≥ ۵۰
- `inventory.ui_elements` ≥ ۳۰
- `inventory.notification_calls` ≥ ۱۰
- `inventory.ui_options` همه‌ی checkbox/slider/dropdown ها را شامل شود
- `purpose_map` برای ≥ ۸۰% فایل‌های مهم
- برای هر UI option، `creation_context` نشان دهد از کدام task ساخته شد
---
## 🅱️ فاز ۲ — Stale Detection + Feature Inventory Panel
> **مهم‌ترین فاز برای R8 — حل مشکل «گزینه‌های قدیمی که نمی‌دانم چی هست»**
### مسئله (R8 — تأکید سه‌باره)
> «خیلی‌هاش قدیمیه و کار نمی‌کنه و خیلی‌هاش نیاز داره بدونم چی بوده، برای چیه و هر سری باید بهش سر بزنم»
### تغییرات
**۲-A. Structural Stale (۸ نوع):**
ماژول جدید: `backend/app/services/scan_v5/stale_detector.py`
| نوع | شناسایی |
|---|---|
| Dead UI buttons | onClick handler ندارد / empty / به endpoint 404 می‌رود |
| Dead frontend routes | در nav menu نیست و در هیچ Link/router.push نیست |
| Dead backend endpoints | در هیچ frontend fetch نیست + در Render logs ۳۰ روز اخیر صدا نشد |
| Unused functions/classes | reverse import = 0 و entry point نیست |
| Unused dataclass fields | در هیچ‌جا read/write نمی‌شود |
| Unused env vars | تعریف شده ولی هیچ `os.environ.get` |
| Orphan files | reverse import = 0 |
| Stale dependencies | در requirements/package.json هست ولی import نمی‌شود |
**۲-B. Semantic Stale (R10 — ۵ نوع):**
| نوع | شناسایی |
|---|---|
| Purpose-mismatched | `stated_purpose` با `actual_behavior` نمی‌خواند |
| Hidden purpose (R8) | کاربر نمی‌داند چی هست — هیچ doc، نام مبهم، creation_context قدیمی |
| Inconsistent با تغییرات اخیر | کد فرض می‌کند رفتار قدیمی X، ولی X تغییر کرده (نیاز به فاز ۳) |
| Outdated business assumption | threshold/config که با realities فعلی نمی‌خواند |
| Forgotten by user (R8 — جدید) | UI option/setting که کاربر هر بار باید بپرسد چی هست |
**۲-C. AI-Generated Documentation برای هر option (R8):**
برای **هر** UI option/setting/feature/button که در inventory.ui_options یا inventory.ui_elements هست:
```python
ai_documentation[option_id] = {
    "name": "...",
    "what_it_does": "AI explanation — این گزینه دقیقاً چه کاری می‌کند",
    "when_added": "تاریخ + commit",
    "originating_idea": "متن خام درخواست اولیه",
    "current_status": "active | possibly_stale | broken | unknown",
    "dependencies": ["..."],         # forward
    "dependents": ["..."],           # reverse
    "recommended_action": "keep | remove | refactor | document"
}
```
**۲-D. Feature Inventory Panel در UI:**
`frontend/src/app/oversight/page.tsx` — اضافه شدن panel جدید:
```
🗺 Feature Inventory
├─ 🔧 Settings (12)
│   ├─ ✓ verify_mode (active — Phase 4, از task #abc1234)
│   ├─ ✓ dedup_threshold (active — Phase 3)
│   ├─ ⚠️ confirmation_streak_required (possibly_stale — ۹۰ روز unused)
│   └─ ...
├─ 🎛 UI Buttons (45)
├─ 🌐 Backend Endpoints (78)
└─ ⚙️ Env Vars (23)
```
برای هر مدخل: hover → AI explanation کامل.
**۲-E. Task Generation:**
برای هر stale item:
- **cleanup** task (structural) یا **audit** task (semantic) یا **document** task (hidden purpose)
- AC رفتار-محور: «یا حذف شود، یا با docstring/comment توضیح داده شود چرا نگه داشته، یا ارتقا یابد»
- priority بر اساس impact
### Acceptance Criteria فاز ۲
- روی این پروژه ≥ ۵ structural stale + ≥ ۳ semantic stale + ≥ ۸ hidden-purpose options
- Feature Inventory Panel با ≥ ۲۰ مدخل
- هر stale → task با explanation کامل (نه فقط «این unused است»)
- روی project trade مثال کاربر (R11)، scan تشخیص دهد گزینه‌هایی که برای trading فعلاً ضرر می‌دهند
---
## 🅲️ فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact
> پوشش **R7** — کاربر صریحاً bidirectional خواست
### مسئله (R7)
> «چه وابستگی‌هایی داشته **به چه چیزهایی وابسته بوده** و **چه چیزهایی بهش وابسته بودن**»
این **دو طرفه** است:
- forward: این فایل به چه چیزی وابسته است
- reverse: چه چیزهایی به این فایل وابسته‌اند
### تغییرات
**۳-A. ذخیره prev state:**
`WatchedProject`:
```python
prev_scan_state: Optional[Dict[str, Any]] = None
# {file_path → {sha, size, last_modified, imports_hash, purpose_hash, ui_elements_hash}}
```
**۳-B. compute delta — ۶ نوع (R7: «تغییر کرده یا اضافه شده یا کم شده یا ویرایش شده»):**
| نوع | تشخیص |
|---|---|
| `add` | در current، نه در prev |
| `remove` | در prev، نه در current |
| `modify` | sha متفاوت |
| `rename` | sha مشابه، path متفاوت |
| `move` | content مشابه، path متفاوت |
| `signature-change` | نام تابع ثابت، parameters یا return-type تغییر |
**۳-C. Bidirectional Dependency (R7 صریح):**
ماژول جدید: `backend/app/services/scan_v5/dependency_analyzer.py`
برای هر changed file:
```python
{
    "dependencies": [...],   # forward — file → چه چیزی import می‌کند
    "dependents": [...],     # reverse — چه چیزهایی → این file import می‌کنند
    "logical_dependencies": [...],   # forward منطقی — کدام components روی این تکیه دارند
    "logical_dependents": [...],     # reverse منطقی — این از کدام components انتظار دارد
}
```
تمایز بین **import-based** و **logical**:
- import: AST/grep
- logical: AI روی purpose_inventory تشخیص می‌دهد (مثلاً «این تابع threshold X را می‌خواند، اگر X تغییر کرد رفتار تغییر می‌کند»)
**۳-D. Logical Impact Analysis (R10):**
AI روی هر تغییر + dependents تحلیل می‌کند:
- آیا تغییر این فایل، **منطق** dependents را می‌شکند؟
- مثال: تغییر `threshold=0.65` → `threshold=0.8` → چه componentهای dependent behavior متفاوت نشان می‌دهند؟
- مثال: تغییر فرمت output `parse_signal()` → چه callerها به update نیاز دارند؟
- مثال: حذف یک env var → چه قسمت‌هایی crash می‌کنند؟
**۳-E. Task Generation:**
برای هر changed file + dependent در خطر:
- task با badge `🔄 وابسته به تغییر`
- AC رفتار-محور: «بعد از این تغییر، dependents X و Y باید همچنان behavior Z تولید کنند»
- اگر logical impact پیچیده است → priority بالاتر + reference به scan inspector session
### Acceptance Criteria فاز ۳
- بعد از دو scan متوالی، delta با ۶ نوع تشخیص داده شود
- اگر signature تغییر کرد و ۳ caller دارد، ≥ ۱ task برای بررسی caller ها
- logical impact detection: اگر threshold تغییر کرد، AI تحلیل کند نه فقط alert سینتکسی
- bidirectional dependency: هم dependencies (forward) هم dependents (reverse) برای هر changed item
---
## 🅳 فاز ۴ — Runtime Discovery + Outcome + Inspector Session
> پوشش **R14** — Inspector tab integration — این مهم‌ترین آپشن از قلم افتاده
### مسئله (R14 صریح)
کاربر گفت: «در بررسی‌های عمیق از تب بازرس ویژه هم کمک می‌گیره و **اسکرین‌ها** رو می‌گیره و در تلگرام **همراه با گزارش** بفرسته و **چت‌ها** رو در یه session ثبت کنه و **بایگانی** کنه؟»
این **اصلاً در پرامپت‌های قبلی نبود**. فعلاً Inspector فقط در verify (Phase 4) استفاده می‌شد.
### تغییرات
**۴-A. Runtime Discovery (با ایده از Phase 4):**
ماژول جدید: `backend/app/services/scan_v5/runtime_discovery.py`
| منبع | استفاده |
|---|---|
| `navigation_helper::extract_nav_links_from_page` | nav links واقعی frontend |
| Playwright on each route | screenshot + status code → 404 detection |
| `backend_log_probe::_fetch_relevant_logs` 30-day | endpoints واقعاً called شده |
| `vision_helper::analyze_screenshot` | کشف feature های UI |
| `code_aware_verifier::_fetch_recent_commits_with_diff` | recent commits context |
خروجی: `runtime_state` شامل `routes_alive[]`, `routes_404[]`, `endpoints_called_recently[]`, `endpoints_never_called[]`, `ui_features_visible[]`, `recent_commits[]`.
**۴-B. Outcome Data Collection:**
برای هر پروژه، scan تلاش کند outcome data بیابد:
| نوع پروژه | outcome data |
|---|---|
| Trading | trade logs, P&L history |
| AI/Chat | conversation outcomes |
| Web service | error rates, latency from logs |
| Scheduling | task completion rates |
| Notification | delivery + open rates |
روش‌ها: Render logs filtered + DB tables outcome-naming + file artifacts.
**۴-C. Scan Inspector Session (R14 — حیاتی):**
ماژول جدید: `backend/app/services/scan_v5/scan_inspector_session.py`
**هر scan یک inspector session باز می‌کند** (مشابه verify در Phase 4):
```python
session = create_scan_inspector_session(
    watched_id=...,
    scan_id=...,
    project_name=...,
)
```
**در طول scan:**
- همه AI calls (purpose extraction، stale detection، logic audit، etc.) → پیام در session با role="ai", content=request+response
- همه Playwright actions → پیام با role="action", screenshot=path
- همه screenshot ها → ذخیره روی disk + reference در session
- runtime probe outputs → پیام با role="probe"
**در پایان scan:**
- session archived
- screenshots → آرشیو شده در Telegram (مثل bundle Phase 4)
- bundle PDF تولید می‌شود شامل:
  - findings + tasks ایجاد شده
  - delta summary
  - logic audit findings
  - inventory summary
  - تمام screenshots
- Telegram message + PDF attachment
- در UI، scan session در inspector tab با badge `🔍 Scan Session` (متمایز از `🔬 Verify Session`)
**۴-D. اضافه شدن tab "Scan Sessions" در Inspector UI:**
`frontend/src/app/projects/[id]/page.tsx` (یا هر جا که inspector tab هست):
- علاوه بر لیست verify sessions، scan sessions هم نمایش داده شوند
- کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند
- archive option
### Acceptance Criteria فاز ۴
- بعد از هر scan، یک inspector session با ≥ ۱۰ پیام (AI calls + actions + probes) ساخته شود
- ≥ ۵ screenshot ذخیره شود
- Telegram message + bundle PDF با همه‌ی screenshots و findings ارسال شود
- در Inspector tab، session آرشیو شده دیده شود با badge `🔍 Scan`
- اگر runtime discovery فعال است، روی هر route یک screenshot + Vision analysis
---
## 🅴 فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)
> پوشش **R10, R11** — مهم‌ترین فاز معنایی
### مسئله (R10, R11)
> «یه پروژه دارم که هوشمند ترید کنه ولی بدتر از یه آدم معمولی ترید می‌کنه — باگ منطقی در هماهنگی بین AI و داده‌ها»
scan نباید **فقط خطاها** را ببیند، بلکه **منطق و هدف** را هم.
### تغییرات
**۵-A. Pipeline Coherence Detection:**
ماژول جدید: `backend/app/services/scan_v5/coherence_analyzer.py`
شناسایی pipelines (chains از componentهای همکار) با استفاده از `purpose_inventory[X].interacting_with`.
برای هر chain، AI بررسی coherence:
| الگو | چه بررسی شود |
|---|---|
| **Data pipeline** | schema هر مرحله، handling empty result |
| **AI/LLM chain** | prompt format ↔ model ↔ parser سازگار؟ validation؟ |
| **Business logic (R11 مثال trade)** | signal ↔ risk model، position ↔ account size، stop-loss ↔ timeframe |
| **Auth/Permission** | همه‌ی mutation از permission گذر می‌کند؟ |
| **Feedback loop** | outcome به config/model برمی‌گردد؟ |
| **Notification chain (R12)** | event → notify_event → caption → silent decision → delivery — همه consistent؟ |
**۵-B. Logical Anti-pattern Detection:**
ماژول جدید: `backend/app/services/scan_v5/anti_pattern_detector.py`
AI روی purpose_inventory + کد، این الگوها را پیدا می‌کند:
| Anti-pattern | مثال |
|---|---|
| داده بی‌مصرف | API call، DB write، ولی هیچ read |
| AI بدون validation | response parse می‌شود ولی validity چک نمی‌شود |
| Magic threshold | `> 0.65:` بدون توضیح |
| Conflicting defaults | یک field default متفاوت در جاهای مختلف |
| Silent failure در crucial path | `except: pass` در business logic |
| Broken feedback loop (R11) | outcome لاگ ولی به model نمی‌رسد |
| Stale assumption | کد فرض می‌کند رفتار سرویس X خاصه، X تغییر کرد |
| Over/under-engineering | برای ساده ۵ لایه، برای پیچیده hardcode |
| Conditional inconsistency | conditions با تغییرات اخیر inconsistent |
| Threshold-Outcome mismatch (R11) | parameters نتایج مطلوب تولید نمی‌کنند |
| Notification mismatch (R12) | event critical ولی silent=True، یا opposite |
**۵-C. Hidden Requirements Discovery (R8):**
برای هر feature که کاربر فراموش کرده «چی هست برای چیه»:
- AI روی `creation_context` (از فاز ۱) + task history + commit messages تحلیل
- استخراج: «این feature چه وقت، چرا، با چه هدفی اضافه شد؟»
- نتیجه:
  - هدف منسوخ → task `cleanup`
  - هدف معتبر، پیاده‌سازی ضعیف → task `refactor`
  - هدف معتبر، پیاده‌سازی خوب، docs نیست → task `document`
**۵-D. Outcome-based Effectiveness Audit (R11):**
اگر outcome data دارد (از فاز ۴):
- AI تحلیل می‌کند آیا outcome با `stated_purpose` می‌خواند
- مثال R11: trade — اگر purpose="earn profit" و win-rate=30% → effectiveness LOW
- اگر LOW → task `logic_audit` با priority بالا
**۵-E. Task Generation:**
نوع جدید task: `logic_audit`
- AC outcome-oriented:
  - ❌ «این کد را fix کن»
  - ✅ «بعد از این تغییر، win-rate باید ≥40% یا parameters محافظه‌کارانه‌تر شود»
- AI explanation کامل: چرا اشتباه + چه راه‌حل + چه impact
### Acceptance Criteria فاز ۵
- روی پروژه فعلی ≥ ۳ logic issue + ≥ ۱ pipeline coherence + ≥ ۲ anti-pattern logical
- روی پروژه trade فرضی (R11)، scan باگ منطقی هماهنگی AI-data شناسایی کند
- task `logic_audit` با AC outcome-oriented + AI explanation کامل
---
## 🅵 فاز ۶ — Notification System Audit (R12)
> پوشش **R6, R12** — صریحاً audit notification ها
### مسئله (R12)
> «در پرامپت برای پیام‌رسانی تلگرام هم جایی در نظر گرفتی؟ در بررسی‌ها نوع اطلاع‌رسانیش هم بررسی بشه و بهینه بشه»
### تغییرات
**۶-A. Notification Inventory (از فاز ۱ — لایه ۱۱):**
برای هر `notify_event(...)` call در کد:
```python
notification_inventory[call_id] = {
    "file_path": "...",
    "line": ...,
    "event_type": "...",          # "task_created" | "verify_done" | ...
    "caption_template": "...",
    "silent_default": True | False | None,
    "attachments": [...],
    "trigger_condition": "...",   # کد منطق در کجا fire می‌شود
    "frequency_estimate": "...",  # احتمال در روز
}
```
**۶-B. Notification Coherence Audit (R12):**
AI روی notification_inventory:
| سوال | چه چیزی audit شود |
|---|---|
| caption کامل است؟ | همه‌ی فیلدهای مهم در caption هستند؟ (title, intent, link, attachments) |
| silent/sound مناسب است؟ | event critical → sound. event routine → silent |
| attachments صحیح؟ | task creation → prompt.md، verify done → bundle.pdf، scan done → scan-bundle.pdf |
| timing مناسب؟ | spam جلوگیری شده؟ batching هست؟ |
| stale notifications؟ | event type که دیگر کد آن وجود ندارد |
| missing notifications؟ | event critical که notification ندارد |
| **scan-specific (R12)**: | scan completion notification با همه‌ی مدارک ضروری: findings, tasks created, delta, logic audit results, inspector session reference |
**۶-C. Notification Templates Suggestions:**
برای هر notification که audit آن مشکل دارد → task `notification_audit`:
مثال task:
```yaml
title: "ارتقای caption notification scan_completed"
type: notification_audit
priority: medium
raw_idea: |
  notification scan_completed فعلاً فقط count tasks ایجاد شده را می‌دهد.
  باید شامل: delta summary + logic audit findings + inspector session link
  + attachment با PDF کامل (مشابه verify bundle).
behavior_observable: |
  بعد از این تغییر، notification scan_completed باید شامل:
  - title پروژه
  - تعداد tasks جدید
  - delta count (add/remove/modify)
  - logic audit count
  - link به scan inspector session
  - PDF attachment با همه screenshots و findings
acceptance_signal: |
  بعد از یک scan تست، Telegram message + PDF received با همه فیلدها
```
**۶-D. Audit existing notification flows:**
- `notification_service.py` — همه `notify_event` ها
- `oversight_telegram_compose.py` — caption builders
- `oversight_mega_bundle.py` — bundle PDFs
- بررسی silent default برای auto-tasks (R6)
### Acceptance Criteria فاز ۶
- audit notification ها: ≥ ۲ مورد ارتقا قابل پیشنهاد
- task `notification_audit` ایجاد شود برای هر مورد
- caption template جدید برای scan_completed تعریف شود
- silent default برای auto-task و scan notifications به `True` تنظیم شود
- field `WatchedProject.auto_task_notify_sound: bool = False`
- field `WatchedProject.scan_notify_sound: bool = False`
---
## 🅶 فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)
> پوشش **R5, R9, R13** — برای **manual + auto** هر دو
### مسئله (R9 صریح)
> «در تولید ایده خام به پرامپت، پرامپت در برخی قسمت‌ها دقیق و واضح تولید نمیشه... باید هوشمندتر بشه خود متن پرامپت و چک‌لیستی که ازش تولید میشه»
### مسئله (R13 صریح)
> «این بهبود پرامپت‌نویسی تسک‌ها آیا فقط برای تسک‌های سیستمی یا تسک‌های دستی هم شامل میشه؟»
**جواب: هر دو.**
### تغییرات
**۷-A. ساختار غنی AC (هم manual، هم auto):**
برای **manual** path در `oversight_service.py:idea_to_prompt` (~3454):
برای **auto** path در `oversight_service.py:task creation from scan` (~4700-4740):
برای **scan-generated** path در `oversight_strong_prompt.py:build_strong_prompt`:
هر AC ساختار غنی:
```python
{
    "text": "...",                          # توضیح اصلی (می‌تواند نام بدهد)
    "behavior": "...",                      # رفتار قابل مشاهده — چه چیزی observable است
    "acceptance_signal": "...",             # سؤال قابل-verify
    "business_intent": "...",               # چرا این لازم است
    "alternative_implementations": [...],   # نام‌های جایگزین قابل قبول (R9 — جلوگیری از نام-محوری)
    "non_goals": "...",                     # چه چیزی این AC نیست
    "false_positive_guard": "...",          # چه شواهد ضعیفی نشانه done نیست (R9)
}
```
**۷-B. ساختار غنی task_step (در `_ai_plan_steps_from_idea`):**
برای manual و auto (R13):
```python
{
    "title": "...",
    "scope": "...",
    "behavior_observable": "...",       # خروجی observable
    "verification_hint": "...",         # کجا verify بیابد
    "business_intent": "...",           # چرا این مرحله
    "non_goals": "...",
}
```
**۷-C. Smart Checklist Mode (R5):**
> «حالت هوشمند که برای تولید چک‌لیست یا عدم تولیدش حسب نیاز هست»
`WatchedProject.auto_task_checklist_mode: Literal["auto","always","never"] = "auto"`
- `auto`: AI تصمیم می‌گیرد بر اساس پیچیدگی (≥3 stage → checklist، single-action → skip)
- `always`: همیشه چک‌لیست
- `never`: هرگز
این برای **هم manual و هم auto** کار می‌کند.
**۷-D. Update prompt templates:**
در `oversight_strong_prompt.py`:
- اضافه شدن section "🎯 معیار رفتاری + business intent + alternative implementations"
- AI صریح راهنمایی شود: AC نام-محور بد است، AC رفتار-محور خوب
**۷-E. جلوگیری از false-positive (R9):**
> «جوری هم نشه که سیستم کاری که بد انجام شده... در verify تیک بزنه که کامل شده»
دستورالعمل به AI:
- اگر AC vague → split به sub-behaviors concrete
- هرگز AC بدون `acceptance_signal` نسازد
- `false_positive_guard` فیلد: چه شواهد ضعیفی نباید سبب done شود
- مثال: «اگر فقط فایل با نام وجود دارد، done نیست — باید رفتار X هم باشد»
**۷-F. Integration با فازهای قبلی:**
پرامپت تولید‌شده استفاده کند از:
- `purpose_inventory` (فاز ۱)
- `runtime_state` (فاز ۴)
- `logic_audit_findings` (فاز ۵)
- `notification_audit` (فاز ۶)
### Acceptance Criteria فاز ۷
- **manual** path: پرامپت‌های جدید برای ۳ ایده‌ی نمونه شامل `behavior` + `acceptance_signal` + `business_intent` + `alternative_implementations` + `false_positive_guard`
- **auto** path: همان ساختار برای task های scan-generated
- چک‌لیست‌ها شامل `behavior_observable` در هر step
- mode `auto` در `_ai_plan_steps_from_idea` کار می‌کند برای هر دو
- verify روی same tasks: false-negative ≤ ۲۰%، false-positive ≤ ۵%
- backward compat: AC های قدیمی (فقط text) همچنان کار می‌کنند
---
## 🅷 فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration
> پوشش **R5, R6** — تسک‌های scan باید مثل manual باشند
### مسئله (R5, R6)
- تسک‌های auto-scan الان `prompt + AC` دارند ولی `task_steps` ندارند
- Telegram notification برای آن‌ها audit نشده
- کاربر می‌خواهد silent default
### تغییرات
**۸-A. task_steps برای auto-tasks:**
در `oversight_service.py:4700-4740`:
- بعد از `build_strong_prompt`، اگر `auto_task_checklist_mode != "never"`:
  - if `mode == "always"` → همیشه call `_ai_plan_steps_from_idea`
  - if `mode == "auto"` → AI تصمیم می‌گیرد بر اساس پیچیدگی
**۸-B. Telegram Notification Template (R6):**
`notification_service.py`:
```python
# Caption template برای auto-scan task
{
    "🤖 تسک جدید از scan خودکار",
    "📌 {title}",
    "🎯 {business_intent[:200]}",
    "📋 چک‌لیست: {N step}" if has_steps else None,
    "🧠 Logic concerns: {logic_audit_count}" if has_logic_issues else None,
    "🔄 وابسته به تغییر: {dependent_changes}" if delta_dependent else None,
    "📊 Outcome impact: {outcome_score}" if outcome_data else None,
    "🔗 [لینک کارت]",
    "🔍 [Inspector session]",
}
# Attachments:
- prompt_full.md (full task prompt)
- scan_inspector_session_link
```
**۸-C. Silent default + UI control:**
`WatchedProject` fields:
```python
auto_task_notify_sound: bool = False     # silent for auto-tasks
scan_notify_sound: bool = False           # silent for scan completion
```
در UI: checkbox `🔔 صدای notification برای تسک‌های auto-scan`.
**۸-D. Scan-completion notification:**
علاوه بر notification هر task، یک scan-completion notification:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
### Acceptance Criteria فاز ۸
- بعد از scan: همه auto-tasks در UI چک‌لیست دارند (اگر mode=auto و پیچیده‌اند)
- caption تلگرام شامل همه فیلدها
- silent default = true
- scan_completed notification با Bundle PDF + Inspector session link
- regression: تسک‌های دستی رفتار قبلی را حفظ کنند
---
## 🅸 فاز ۹ — UI Redesign (R2, R8)
> پوشش **R2** — به‌روزرسانی گزینه‌ها
### مسئله (R2)
> «گزینه‌هاش هم به روز رسانی بشن»
### تغییرات
**۹-A. بازنگری scan_depth modes:**
از (فعلی):
```
quick (3 pass) | standard (6 pass) | deep (12 pass، پیش‌فرض) | thorough (12 + health + roadmap)
```
به:
```
⚡ quick (5 pass) — frontend + backend + security + delta + stale
⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
🔬 deep (12 pass) — همه + impact analysis
🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)
```
**۹-B. بازنگری criteria_weights:**
به‌جای ۶ slider:
- preset selector: "general", "security-first", "tests-first", "feature-completeness", "logic-quality"
- sliders در `<details>` (advanced)
**۹-C. بازنگری Smart Task Lifecycle:**
- بررسی هر گزینه: کار می‌کند؟
- label با وضعیت فعلی
**۹-D. گزینه‌های جدید Phase 5:**
`WatchedProject` فیلدهای جدید (همه با default sensible):
```python
# Coverage
inventory_layers: List[str] = ["all"]    # یا subset
# Intelligence
stale_detection_enabled: bool = True
delta_analysis_enabled: bool = True
runtime_discovery_enabled: bool = True
outcome_data_enabled: bool = True
logic_audit_enabled: bool = True
notification_audit_enabled: bool = True
inspector_session_enabled: bool = True   # R14
# Lifecycle
auto_task_checklist_mode: str = "auto"   # R5
cleanup_tasks_enabled: bool = True
# Notifications
auto_task_notify_sound: bool = False     # R6
scan_notify_sound: bool = False          # R6
```
**۹-E. بازطراحی Layout — ۴ tabs (به‌جای ۳):**
| Tab | محتوا |
|---|---|
| **🔍 Coverage** | scan_depth + inventory_layers + criteria preset + auto_models |
| **🧠 Intelligence** | stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session |
| **🔁 Lifecycle** | dedup + auto-regenerate + checklist mode + cleanup |
| **🔔 Notifications** | auto_task_sound + scan_sound + notification_audit + custom templates |
**۹-F. AI explanation برای هر گزینه (R8):**
هر گزینه دارای help icon ⓘ + AI-generated description (از فاز ۲):
- این گزینه دقیقاً چه می‌کند
- کِی اضافه شد
- وضعیت فعلی (active / possibly stale / unknown)
**۹-G. Feature Inventory Panel (R8):**
panel جدید در UI با لیست همه options + AI explanation (از فاز ۲).
### Acceptance Criteria فاز ۹
- در UI، ۴ tab وجود دارد
- mode `balanced` پیش‌فرض جدید
- mode `ultra` در دسترس
- backward compat: scan_depth قدیمی همچنان کار کند
- هر گزینه دارای tooltip + AI explanation
- Feature Inventory Panel functional
---
## 🅹 فاز ۱۰ — Meta-validation
> خود این پرامپت قابل verify باشد
### مسئله
کاربر می‌خواهد این پرامپت به task تبدیل شود و توسط verify Phase 4 سنجیده شود.
### تغییرات
**۱۰-A. این پرامپت به task تبدیل:**
- ایده‌ی خام = این متن
- AC ها = AC های هر فاز ۱-۹
- task_steps = ۱۰ فاز
**۱۰-B. AC قابل-verify دقیق:**
هر AC در هر فاز measurable نوشته شده (مثلاً «≥ ۵ stale item کشف شود»).
**۱۰-C. Iteration loop:**
- پیاده‌سازی → verify deep (Phase 4) → نتیجه
- اگر `done < 100%` → iteration بعدی
- اگر `done = 100%` → فاز بعدی
- log در `phase5_meta_validation.md`
**۱۰-D. شناسایی verify bugs:**
- اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify
- اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive)
- log همه‌ی این موارد
### Acceptance Criteria فاز ۱۰
- پرامپت نهایی شامل ۱۰ task_step
- هر step دارای `behavior_observable` + `verification_hint`
- بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد
- log باگ‌های verify identified
---
## 🗂️ نقشه آدرس‌ها
| فاز | فایل | نوع |
|---|---|---|
| 1 | `backend/app/services/scan_v5/comprehensive_inventory.py` | new |
| 1 | `backend/app/services/scan_v5/purpose_extractor.py` | new |
| 1 | `backend/app/services/oversight_service.py:161` | + 3 fields |
| 2 | `backend/app/services/scan_v5/stale_detector.py` | new |
| 2 | `backend/app/services/scan_v5/feature_documenter.py` | new |
| 2 | `frontend/src/app/oversight/page.tsx` | + Feature Inventory panel |
| 3 | `backend/app/services/scan_v5/delta_analyzer.py` | new |
| 3 | `backend/app/services/scan_v5/dependency_analyzer.py` | new |
| 3 | `backend/app/services/oversight_service.py:161` | + `prev_scan_state` |
| 4 | `backend/app/services/scan_v5/runtime_discovery.py` | new |
| 4 | `backend/app/services/scan_v5/outcome_analyzer.py` | new |
| 4 | `backend/app/services/scan_v5/scan_inspector_session.py` | new |
| 4 | `frontend/src/app/projects/[id]/page.tsx` (Inspector tab) | + Scan Sessions section |
| 5 | `backend/app/services/scan_v5/coherence_analyzer.py` | new |
| 5 | `backend/app/services/scan_v5/anti_pattern_detector.py` | new |
| 5 | `backend/app/services/oversight_service.py` | + task type "logic_audit" |
| 6 | `backend/app/services/scan_v5/notification_auditor.py` | new |
| 6 | `backend/app/services/notification_service.py` | audit |
| 6 | `backend/app/services/oversight_telegram_compose.py` | + scan template |
| 7 | `backend/app/services/oversight_strong_prompt.py` | + behavior + business_intent + false_positive_guard |
| 7 | `backend/app/services/oversight_service.py:3454` (idea_to_prompt) | structured AC |
| 7 | `backend/app/services/oversight_service.py:2673` (_ai_plan_steps_from_idea) | structured steps |
| 7 | `backend/app/services/oversight_service.py:4700-4740` (auto-task creation) | apply same to auto |
| 8 | `backend/app/services/notification_service.py` | new templates |
| 8 | `backend/app/services/oversight_mega_bundle.py` | + scan-bundle PDF |
| 9 | `frontend/src/app/oversight/page.tsx` | 4 tabs redesign |
| 9 | `backend/app/services/oversight_service.py:161` | + ۱۰ field جدید |
| 10 | meta — هر فاز AC مشخص | — |
---
## 🎯 ترتیب اجرا
**Round 1 — پایه:**
1. فاز ۱ — Comprehensive Inventory + Purpose
**Round 2 — تحلیل ساختاری:**
2. فاز ۲ — Stale Detection + Feature Inventory Panel
3. فاز ۳ — Delta + Bidirectional Dependency
**Round 3 — تحلیل دینامیک:**
4. فاز ۴ — Runtime + Outcome + Inspector Session
5. فاز ۵ — Logical Audit
6. فاز ۶ — Notification Audit
**Round 4 — تولید + UI:**
7. فاز ۷ — Smart Prompt (manual + auto)
8. فاز ۸ — Auto-tasks Like Manual + Telegram
9. فاز ۹ — UI Redesign
**Round 5 — نهایی:**
10. فاز ۱۰ — Meta-validation
هر Round با runtime test قبل از push. در پایان: یک scan کامل روی این پروژه برای meta-validation.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
# 🎯 پرامپت Phase 5 V4 — ارتقای کامل Auto-Scan + Task Creation
> **این پرامپت تمام ۱۴ بند درخواست خام را پوشش می‌دهد. هر فاز با reference به بندهای درخواست خام نشان داده شده.**
---
## 📋 فهرست بندهای درخواست خام (Cross-reference)
| # | بند درخواست خام | پوشش در فاز |
|---|---|---|
| **R1** | scan جدا از verify؛ scan ایجاد task، verify بررسی task | مقدمه |
| **R2** | ایده‌گیری از Phase 4 + به‌روزرسانی گزینه‌های UI | کل فازها + فاز ۹ |
| **R3** | پوشش از ریز تا درشت، یک دور برای کل سیستم | فاز ۱ |
| **R4** | scan باید قوی‌تر از verify باشد | کل فازها |
| **R5** | چک‌لیست برای auto-tasks + Smart mode هوشمند | فاز ۷، ۸ |
| **R6** | Telegram notification برای auto-tasks (caption + silent default) | فاز ۸ |
| **R7** | Delta detection + Bidirectional dependency | فاز ۳ |
| **R8** | شناسایی گزینه‌های قدیمی + task برای آن‌ها (⭐ خیلی خیلی مهم) | فاز ۲ |
| **R9** | Smart prompt + Smart checklist (بدون false-positive) | فاز ۷ |
| **R10** | Logic Audit (هدف، منطق، coherence) | فاز ۵ |
| **R11** | مثال trade — باگ منطقی در هماهنگی | فاز ۵ |
| **R12** | Telegram notification system audit | فاز ۶ |
| **R13** | Smart prompt برای manual + auto | فاز ۷ |
| **R14** | Inspector tab integration (session + screenshots + chats + archive) | فاز ۴ |
---
## 📌 مقدمه و فلسفه
### **R1** — scan ≠ verify
| سیستم | چه می‌کند |
|---|---|
| **verify** (Phase 4، تمام) | روی **تسک‌های موجود** بررسی می‌کند آیا انجام شده یا نه |
| **scan** (این Phase 5) | کل سیستم را بررسی می‌کند تا **تسک‌های واقعاً لازم** کشف و ایجاد شوند |
اگر scan خوب کار نکند → تسک واقعاً لازم اصلاً ساخته نمی‌شود → verify هرگز فرصت بررسی آن را پیدا نمی‌کند. **پس scan حتی از verify مهم‌تر است.**
### **R4** — scan باید قوی‌تر از verify باشد
verify شواهد جمع می‌کند برای **یک task مشخص**. scan شواهد جمع می‌کند برای **کل سیستم**. پس scan ابزارهای بیشتری نیاز دارد:
- همه‌ی Phase 4 components (smart_nav, vision, code-aware, backend-log, inspector_probe)
- **به‌علاوه**: comprehensive inventory، purpose extraction، delta analysis، logical coherence، outcome analysis، notification audit، semantic stale detection
### **چهار پایه**
1. **پوشش ریز تا درشت** (R3)
2. **تغییر-آگاهی + bidirectional dependency** (R7)
3. **رفتار-محوری در پرامپت و چک‌لیست** (R9, R13)
4. **logic awareness از روز اول — purpose، coherence، effectiveness** (R10, R11)
### **هدف نهایی**
بعد از یک scan، **تمام تسک‌های واقعاً لازم** کشف و ساخته شوند، **با چک‌لیست هوشمند، با notification صحیح، با session بازرس کامل**:
- features جدید
- features ناقص
- bugs ساختاری
- bugs منطقی (R11)
- features قدیمی و dead options (R8)
- componentهای منطقاً ناسازگار (R10)
- featuresی که کاربر دیگر نمی‌داند چیست (R8) → با AI explanation
- notification ها ناقص یا اشتباه (R12)
---
## 🅰️ فاز ۱ — Comprehensive Inventory + Purpose Extraction
> پوشش **R3** (ریز تا درشت)، پایه برای همه فازهای بعدی
### مسئله
الان `run_deep_scan` فقط ۳۵ فایل deep-read + import graph می‌بیند. backend endpoints، UI elements، DB schema، env vars، configs، **notifications، inspector sessions، UI options** هرگز inventory نمی‌شوند.
### تغییرات
**۱-A. Structural Inventory (۱۲ لایه):**
ماژول جدید: `backend/app/services/scan_v5/comprehensive_inventory.py`
| # | لایه | چه چیزی جمع می‌شود | روش |
|---|---|---|---|
| 1 | Files | همه فایل‌های git-tracked | `git ls-files` |
| 2 | Backend endpoints | همه `@router.{get,post,…}` + WebSocket + background tasks | AST parse |
| 3 | UI elements | همه `<button>`, `<form>`, `<input>`, `<Select>`, `<Link>`, modal triggers, dropdown items | parse `.tsx` |
| 4 | DB schema | همه dataclass + Column + migrations | inspect |
| 5 | Env vars | همه `os.environ.get`, `os.getenv`, `process.env.X` | regex |
| 6 | Config files | همه `.json`, `.yaml`, `.toml`, `.env*` | glob |
| 7 | Dependencies | `requirements.txt`, `package.json` | parse |
| 8 | Scripts | همه `.sh`, `pyproject::scripts`, `package.json::scripts` | parse |
| 9 | Cron/scheduled | همه `apscheduler`, `BackgroundTasks`, `asyncio.create_task` | regex |
| 10 | Routes | همه frontend `app/**/page.tsx` + backend route | walk |
| 11 | **Notification calls (R12)** | همه `notify_event(...)`, `send_telegram(...)`, `bot.send_message(...)` + event_type + silent flag + caption template | regex/AST |
| 12 | **UI options/settings (R8)** | همه checkbox/slider/dropdown در UI + field name در WatchedProject + default value | parse + cross-ref |
**۱-B. Purpose Extraction (R10):**
ماژول جدید: `backend/app/services/scan_v5/purpose_extractor.py`
برای **هر file/module/feature/option** که در inventory هست:
```python
purpose_inventory[item_id] = {
    "stated_purpose": "چه کاری *قرار است* بکند",
    "evidence_sources": ["comments", "docstrings", "tests", "raw_idea", "task_history", "commit_messages"],
    "expected_inputs": [...],
    "expected_outputs": [...],
    "interacting_with": [...],          # سایر componentهای همکار
    "creation_context": {                # R8 — کاربر فراموش کرده چی هست
        "first_seen_commit": "...",
        "first_seen_date": "...",
        "originating_task_id": "...",    # کدام task این را ساخت
        "originating_raw_idea": "...",   # متن خام درخواست
    },
    "current_usage": "...",              # آیا هنوز استفاده می‌شود؟ کجا؟
}
```
**منابع AI برای purpose:**
- محتوای فایل + docstrings + JSDoc
- test files مرتبط
- `WatchedProject.user_notes` (raw_idea اصلی پروژه)
- task history (task هایی که این item را modify کرده‌اند)
- commit messages روی این path
**۱-C. Storage:**
`WatchedProject` فیلدهای جدید:
```python
last_scan_inventory: Optional[Dict[str, Any]] = None
last_scan_purpose_map: Optional[Dict[str, Dict]] = None
last_scan_at_v5: Optional[str] = None  # timestamp scan جدید
```
### Acceptance Criteria فاز ۱
- `inventory.backend_endpoints` ≥ ۵۰
- `inventory.ui_elements` ≥ ۳۰
- `inventory.notification_calls` ≥ ۱۰
- `inventory.ui_options` همه‌ی checkbox/slider/dropdown ها را شامل شود
- `purpose_map` برای ≥ ۸۰% فایل‌های مهم
- برای هر UI option، `creation_context` نشان دهد از کدام task ساخته شد
---
## 🅱️ فاز ۲ — Stale Detection + Feature Inventory Panel
> **مهم‌ترین فاز برای R8 — حل مشکل «گزینه‌های قدیمی که نمی‌دانم چی هست»**
### مسئله (R8 — تأکید سه‌باره)
> «خیلی‌هاش قدیمیه و کار نمی‌کنه و خیلی‌هاش نیاز داره بدونم چی بوده، برای چیه و هر سری باید بهش سر بزنم»
### تغییرات
**۲-A. Structural Stale (۸ نوع):**
ماژول جدید: `backend/app/services/scan_v5/stale_detector.py`
| نوع | شناسایی |
|---|---|
| Dead UI buttons | onClick handler ندارد / empty / به endpoint 404 می‌رود |
| Dead frontend routes | در nav menu نیست و در هیچ Link/router.push نیست |
| Dead backend endpoints | در هیچ frontend fetch نیست + در Render logs ۳۰ روز اخیر صدا نشد |
| Unused functions/classes | reverse import = 0 و entry point نیست |
| Unused dataclass fields | در هیچ‌جا read/write نمی‌شود |
| Unused env vars | تعریف شده ولی هیچ `os.environ.get` |
| Orphan files | reverse import = 0 |
| Stale dependencies | در requirements/package.json هست ولی import نمی‌شود |
**۲-B. Semantic Stale (R10 — ۵ نوع):**
| نوع | شناسایی |
|---|---|
| Purpose-mismatched | `stated_purpose` با `actual_behavior` نمی‌خواند |
| Hidden purpose (R8) | کاربر نمی‌داند چی هست — هیچ doc، نام مبهم، creation_context قدیمی |
| Inconsistent با تغییرات اخیر | کد فرض می‌کند رفتار قدیمی X، ولی X تغییر کرده (نیاز به فاز ۳) |
| Outdated business assumption | threshold/config که با realities فعلی نمی‌خواند |
| Forgotten by user (R8 — جدید) | UI option/setting که کاربر هر بار باید بپرسد چی هست |
**۲-C. AI-Generated Documentation برای هر option (R8):**
برای **هر** UI option/setting/feature/button که در inventory.ui_options یا inventory.ui_elements هست:
```python
ai_documentation[option_id] = {
    "name": "...",
    "what_it_does": "AI explanation — این گزینه دقیقاً چه کاری می‌کند",
    "when_added": "تاریخ + commit",
    "originating_idea": "متن خام درخواست اولیه",
    "current_status": "active | possibly_stale | broken | unknown",
    "dependencies": ["..."],         # forward
    "dependents": ["..."],           # reverse
    "recommended_action": "keep | remove | refactor | document"
}
```
**۲-D. Feature Inventory Panel در UI:**
`frontend/src/app/oversight/page.tsx` — اضافه شدن panel جدید:
```
🗺 Feature Inventory
├─ 🔧 Settings (12)
│   ├─ ✓ verify_mode (active — Phase 4, از task #abc1234)
│   ├─ ✓ dedup_threshold (active — Phase 3)
│   ├─ ⚠️ confirmation_streak_required (possibly_stale — ۹۰ روز unused)
│   └─ ...
├─ 🎛 UI Buttons (45)
├─ 🌐 Backend Endpoints (78)
└─ ⚙️ Env Vars (23)
```
برای هر مدخل: hover → AI explanation کامل.
**۲-E. Task Generation:**
برای هر stale item:
- **cleanup** task (structural) یا **audit** task (semantic) یا **document** task (hidden purpose)
- AC رفتار-محور: «یا حذف شود، یا با docstring/comment توضیح داده شود چرا نگه داشته، یا ارتقا یابد»
- priority بر اساس impact
### Acceptance Criteria فاز ۲
- روی این پروژه ≥ ۵ structural stale + ≥ ۳ semantic stale + ≥ ۸ hidden-purpose options
- Feature Inventory Panel با ≥ ۲۰ مدخل
- هر stale → task با explanation کامل (نه فقط «این unused است»)
- روی project trade مثال کاربر (R11)، scan تشخیص دهد گزینه‌هایی که برای trading فعلاً ضرر می‌دهند
---
## 🅲️ فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact
> پوشش **R7** — کاربر صریحاً bidirectional خواست
### مسئله (R7)
> «چه وابستگی‌هایی داشته **به چه چیزهایی وابسته بوده** و **چه چیزهایی بهش وابسته بودن**»
این **دو طرفه** است:
- forward: این فایل به چه چیزی وابسته است
- reverse: چه چیزهایی به این فایل وابسته‌اند
### تغییرات
**۳-A. ذخیره prev state:**
`WatchedProject`:
```python
prev_scan_state: Optional[Dict[str, Any]] = None
# {file_path → {sha, size, last_modified, imports_hash, purpose_hash, ui_elements_hash}}
```
**۳-B. compute delta — ۶ نوع (R7: «تغییر کرده یا اضافه شده یا کم شده یا ویرایش شده»):**
| نوع | تشخیص |
|---|---|
| `add` | در current، نه در prev |
| `remove` | در prev، نه در current |
| `modify` | sha متفاوت |
| `rename` | sha مشابه، path متفاوت |
| `move` | content مشابه، path متفاوت |
| `signature-change` | نام تابع ثابت، parameters یا return-type تغییر |
**۳-C. Bidirectional Dependency (R7 صریح):**
ماژول جدید: `backend/app/services/scan_v5/dependency_analyzer.py`
برای هر changed file:
```python
{
    "dependencies": [...],   # forward — file → چه چیزی import می‌کند
    "dependents": [...],     # reverse — چه چیزهایی → این file import می‌کنند
    "logical_dependencies": [...],   # forward منطقی — کدام components روی این تکیه دارند
    "logical_dependents": [...],     # reverse منطقی — این از کدام components انتظار دارد
}
```
تمایز بین **import-based** و **logical**:
- import: AST/grep
- logical: AI روی purpose_inventory تشخیص می‌دهد (مثلاً «این تابع threshold X را می‌خواند، اگر X تغییر کرد رفتار تغییر می‌کند»)
**۳-D. Logical Impact Analysis (R10):**
AI روی هر تغییر + dependents تحلیل می‌کند:
- آیا تغییر این فایل، **منطق** dependents را می‌شکند؟
- مثال: تغییر `threshold=0.65` → `threshold=0.8` → چه componentهای dependent behavior متفاوت نشان می‌دهند؟
- مثال: تغییر فرمت output `parse_signal()` → چه callerها به update نیاز دارند؟
- مثال: حذف یک env var → چه قسمت‌هایی crash می‌کنند؟
**۳-E. Task Generation:**
برای هر changed file + dependent در خطر:
- task با badge `🔄 وابسته به تغییر`
- AC رفتار-محور: «بعد از این تغییر، dependents X و Y باید همچنان behavior Z تولید کنند»
- اگر logical impact پیچیده است → priority بالاتر + reference به scan inspector session
### Acceptance Criteria فاز ۳
- بعد از دو scan متوالی، delta با ۶ نوع تشخیص داده شود
- اگر signature تغییر کرد و ۳ caller دارد، ≥ ۱ task برای بررسی caller ها
- logical impact detection: اگر threshold تغییر کرد، AI تحلیل کند نه فقط alert سینتکسی
- bidirectional dependency: هم dependencies (forward) هم dependents (reverse) برای هر changed item
---
## 🅳 فاز ۴ — Runtime Discovery + Outcome + Inspector Session
> پوشش **R14** — Inspector tab integration — این مهم‌ترین آپشن از قلم افتاده
### مسئله (R14 صریح)
کاربر گفت: «در بررسی‌های عمیق از تب بازرس ویژه هم کمک می‌گیره و **اسکرین‌ها** رو می‌گیره و در تلگرام **همراه با گزارش** بفرسته و **چت‌ها** رو در یه session ثبت کنه و **بایگانی** کنه؟»
این **اصلاً در پرامپت‌های قبلی نبود**. فعلاً Inspector فقط در verify (Phase 4) استفاده می‌شد.
### تغییرات
**۴-A. Runtime Discovery (با ایده از Phase 4):**
ماژول جدید: `backend/app/services/scan_v5/runtime_discovery.py`
| منبع | استفاده |
|---|---|
| `navigation_helper::extract_nav_links_from_page` | nav links واقعی frontend |
| Playwright on each route | screenshot + status code → 404 detection |
| `backend_log_probe::_fetch_relevant_logs` 30-day | endpoints واقعاً called شده |
| `vision_helper::analyze_screenshot` | کشف feature های UI |
| `code_aware_verifier::_fetch_recent_commits_with_diff` | recent commits context |
خروجی: `runtime_state` شامل `routes_alive[]`, `routes_404[]`, `endpoints_called_recently[]`, `endpoints_never_called[]`, `ui_features_visible[]`, `recent_commits[]`.
**۴-B. Outcome Data Collection:**
برای هر پروژه، scan تلاش کند outcome data بیابد:
| نوع پروژه | outcome data |
|---|---|
| Trading | trade logs, P&L history |
| AI/Chat | conversation outcomes |
| Web service | error rates, latency from logs |
| Scheduling | task completion rates |
| Notification | delivery + open rates |
روش‌ها: Render logs filtered + DB tables outcome-naming + file artifacts.
**۴-C. Scan Inspector Session (R14 — حیاتی):**
ماژول جدید: `backend/app/services/scan_v5/scan_inspector_session.py`
**هر scan یک inspector session باز می‌کند** (مشابه verify در Phase 4):
```python
session = create_scan_inspector_session(
    watched_id=...,
    scan_id=...,
    project_name=...,
)
```
**در طول scan:**
- همه AI calls (purpose extraction، stale detection، logic audit، etc.) → پیام در session با role="ai", content=request+response
- همه Playwright actions → پیام با role="action", screenshot=path
- همه screenshot ها → ذخیره روی disk + reference در session
- runtime probe outputs → پیام با role="probe"
**در پایان scan:**
- session archived
- screenshots → آرشیو شده در Telegram (مثل bundle Phase 4)
- bundle PDF تولید می‌شود شامل:
  - findings + tasks ایجاد شده
  - delta summary
  - logic audit findings
  - inventory summary
  - تمام screenshots
- Telegram message + PDF attachment
- در UI، scan session در inspector tab با badge `🔍 Scan Session` (متمایز از `🔬 Verify Session`)
**۴-D. اضافه شدن tab "Scan Sessions" در Inspector UI:**
`frontend/src/app/projects/[id]/page.tsx` (یا هر جا که inspector tab هست):
- علاوه بر لیست verify sessions، scan sessions هم نمایش داده شوند
- کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند
- archive option
### Acceptance Criteria فاز ۴
- بعد از هر scan، یک inspector session با ≥ ۱۰ پیام (AI calls + actions + probes) ساخته شود
- ≥ ۵ screenshot ذخیره شود
- Telegram message + bundle PDF با همه‌ی screenshots و findings ارسال شود
- در Inspector tab، session آرشیو شده دیده شود با badge `🔍 Scan`
- اگر runtime discovery فعال است، روی هر route یک screenshot + Vision analysis
---
## 🅴 فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)
> پوشش **R10, R11** — مهم‌ترین فاز معنایی
### مسئله (R10, R11)
> «یه پروژه دارم که هوشمند ترید کنه ولی بدتر از یه آدم معمولی ترید می‌کنه — باگ منطقی در هماهنگی بین AI و داده‌ها»
scan نباید **فقط خطاها** را ببیند، بلکه **منطق و هدف** را هم.
### تغییرات
**۵-A. Pipeline Coherence Detection:**
ماژول جدید: `backend/app/services/scan_v5/coherence_analyzer.py`
شناسایی pipelines (chains از componentهای همکار) با استفاده از `purpose_inventory[X].interacting_with`.
برای هر chain، AI بررسی coherence:
| الگو | چه بررسی شود |
|---|---|
| **Data pipeline** | schema هر مرحله، handling empty result |
| **AI/LLM chain** | prompt format ↔ model ↔ parser سازگار؟ validation؟ |
| **Business logic (R11 مثال trade)** | signal ↔ risk model، position ↔ account size، stop-loss ↔ timeframe |
| **Auth/Permission** | همه‌ی mutation از permission گذر می‌کند؟ |
| **Feedback loop** | outcome به config/model برمی‌گردد؟ |
| **Notification chain (R12)** | event → notify_event → caption → silent decision → delivery — همه consistent؟ |
**۵-B. Logical Anti-pattern Detection:**
ماژول جدید: `backend/app/services/scan_v5/anti_pattern_detector.py`
AI روی purpose_inventory + کد، این الگوها را پیدا می‌کند:
| Anti-pattern | مثال |
|---|---|
| داده بی‌مصرف | API call، DB write، ولی هیچ read |
| AI بدون validation | response parse می‌شود ولی validity چک نمی‌شود |
| Magic threshold | `> 0.65:` بدون توضیح |
| Conflicting defaults | یک field default متفاوت در جاهای مختلف |
| Silent failure در crucial path | `except: pass` در business logic |
| Broken feedback loop (R11) | outcome لاگ ولی به model نمی‌رسد |
| Stale assumption | کد فرض می‌کند رفتار سرویس X خاصه، X تغییر کرد |
| Over/under-engineering | برای ساده ۵ لایه، برای پیچیده hardcode |
| Conditional inconsistency | conditions با تغییرات اخیر inconsistent |
| Threshold-Outcome mismatch (R11) | parameters نتایج مطلوب تولید نمی‌کنند |
| Notification mismatch (R12) | event critical ولی silent=True، یا opposite |
**۵-C. Hidden Requirements Discovery (R8):**
برای هر feature که کاربر فراموش کرده «چی هست برای چیه»:
- AI روی `creation_context` (از فاز ۱) + task history + commit messages تحلیل
- استخراج: «این feature چه وقت، چرا، با چه هدفی اضافه شد؟»
- نتیجه:
  - هدف منسوخ → task `cleanup`
  - هدف معتبر، پیاده‌سازی ضعیف → task `refactor`
  - هدف معتبر، پیاده‌سازی خوب، docs نیست → task `document`
**۵-D. Outcome-based Effectiveness Audit (R11):**
اگر outcome data دارد (از فاز ۴):
- AI تحلیل می‌کند آیا outcome با `stated_purpose` می‌خواند
- مثال R11: trade — اگر purpose="earn profit" و win-rate=30% → effectiveness LOW
- اگر LOW → task `logic_audit` با priority بالا
**۵-E. Task Generation:**
نوع جدید task: `logic_audit`
- AC outcome-oriented:
  - ❌ «این کد را fix کن»
  - ✅ «بعد از این تغییر، win-rate باید ≥40% یا parameters محافظه‌کارانه‌تر شود»
- AI explanation کامل: چرا اشتباه + چه راه‌حل + چه impact
### Acceptance Criteria فاز ۵
- روی پروژه فعلی ≥ ۳ logic issue + ≥ ۱ pipeline coherence + ≥ ۲ anti-pattern logical
- روی پروژه trade فرضی (R11)، scan باگ منطقی هماهنگی AI-data شناسایی کند
- task `logic_audit` با AC outcome-oriented + AI explanation کامل
---
## 🅵 فاز ۶ — Notification System Audit (R12)
> پوشش **R6, R12** — صریحاً audit notification ها
### مسئله (R12)
> «در پرامپت برای پیام‌رسانی تلگرام هم جایی در نظر گرفتی؟ در بررسی‌ها نوع اطلاع‌رسانیش هم بررسی بشه و بهینه بشه»
### تغییرات
**۶-A. Notification Inventory (از فاز ۱ — لایه ۱۱):**
برای هر `notify_event(...)` call در کد:
```python
notification_inventory[call_id] = {
    "file_path": "...",
    "line": ...,
    "event_type": "...",          # "task_created" | "verify_done" | ...
    "caption_template": "...",
    "silent_default": True | False | None,
    "attachments": [...],
    "trigger_condition": "...",   # کد منطق در کجا fire می‌شود
    "frequency_estimate": "...",  # احتمال در روز
}
```
**۶-B. Notification Coherence Audit (R12):**
AI روی notification_inventory:
| سوال | چه چیزی audit شود |
|---|---|
| caption کامل است؟ | همه‌ی فیلدهای مهم در caption هستند؟ (title, intent, link, attachments) |
| silent/sound مناسب است؟ | event critical → sound. event routine → silent |
| attachments صحیح؟ | task creation → prompt.md، verify done → bundle.pdf، scan done → scan-bundle.pdf |
| timing مناسب؟ | spam جلوگیری شده؟ batching هست؟ |
| stale notifications؟ | event type که دیگر کد آن وجود ندارد |
| missing notifications؟ | event critical که notification ندارد |
| **scan-specific (R12)**: | scan completion notification با همه‌ی مدارک ضروری: findings, tasks created, delta, logic audit results, inspector session reference |
**۶-C. Notification Templates Suggestions:**
برای هر notification که audit آن مشکل دارد → task `notification_audit`:
مثال task:
```yaml
title: "ارتقای caption notification scan_completed"
type: notification_audit
priority: medium
raw_idea: |
  notification scan_completed فعلاً فقط count tasks ایجاد شده را می‌دهد.
  باید شامل: delta summary + logic audit findings + inspector session link
  + attachment با PDF کامل (مشابه verify bundle).
behavior_observable: |
  بعد از این تغییر، notification scan_completed باید شامل:
  - title پروژه
  - تعداد tasks جدید
  - delta count (add/remove/modify)
  - logic audit count
  - link به scan inspector session
  - PDF attachment با همه screenshots و findings
acceptance_signal: |
  بعد از یک scan تست، Telegram message + PDF received با همه فیلدها
```
**۶-D. Audit existing notification flows:**
- `notification_service.py` — همه `notify_event` ها
- `oversight_telegram_compose.py` — caption builders
- `oversight_mega_bundle.py` — bundle PDFs
- بررسی silent default برای auto-tasks (R6)
### Acceptance Criteria فاز ۶
- audit notification ها: ≥ ۲ مورد ارتقا قابل پیشنهاد
- task `notification_audit` ایجاد شود برای هر مورد
- caption template جدید برای scan_completed تعریف شود
- silent default برای auto-task و scan notifications به `True` تنظیم شود
- field `WatchedProject.auto_task_notify_sound: bool = False`
- field `WatchedProject.scan_notify_sound: bool = False`
---
## 🅶 فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)
> پوشش **R5, R9, R13** — برای **manual + auto** هر دو
### مسئله (R9 صریح)
> «در تولید ایده خام به پرامپت، پرامپت در برخی قسمت‌ها دقیق و واضح تولید نمیشه... باید هوشمندتر بشه خود متن پرامپت و چک‌لیستی که ازش تولید میشه»
### مسئله (R13 صریح)
> «این بهبود پرامپت‌نویسی تسک‌ها آیا فقط برای تسک‌های سیستمی یا تسک‌های دستی هم شامل میشه؟»
**جواب: هر دو.**
### تغییرات
**۷-A. ساختار غنی AC (هم manual، هم auto):**
برای **manual** path در `oversight_service.py:idea_to_prompt` (~3454):
برای **auto** path در `oversight_service.py:task creation from scan` (~4700-4740):
برای **scan-generated** path در `oversight_strong_prompt.py:build_strong_prompt`:
هر AC ساختار غنی:
```python
{
    "text": "...",                          # توضیح اصلی (می‌تواند نام بدهد)
    "behavior": "...",                      # رفتار قابل مشاهده — چه چیزی observable است
    "acceptance_signal": "...",             # سؤال قابل-verify
    "business_intent": "...",               # چرا این لازم است
    "alternative_implementations": [...],   # نام‌های جایگزین قابل قبول (R9 — جلوگیری از نام-محوری)
    "non_goals": "...",                     # چه چیزی این AC نیست
    "false_positive_guard": "...",          # چه شواهد ضعیفی نشانه done نیست (R9)
}
```
**۷-B. ساختار غنی task_step (در `_ai_plan_steps_from_idea`):**
برای manual و auto (R13):
```python
{
    "title": "...",
    "scope": "...",
    "behavior_observable": "...",       # خروجی observable
    "verification_hint": "...",         # کجا verify بیابد
    "business_intent": "...",           # چرا این مرحله
    "non_goals": "...",
}
```
**۷-C. Smart Checklist Mode (R5):**
> «حالت هوشمند که برای تولید چک‌لیست یا عدم تولیدش حسب نیاز هست»
`WatchedProject.auto_task_checklist_mode: Literal["auto","always","never"] = "auto"`
- `auto`: AI تصمیم می‌گیرد بر اساس پیچیدگی (≥3 stage → checklist، single-action → skip)
- `always`: همیشه چک‌لیست
- `never`: هرگز
این برای **هم manual و هم auto** کار می‌کند.
**۷-D. Update prompt templates:**
در `oversight_strong_prompt.py`:
- اضافه شدن section "🎯 معیار رفتاری + business intent + alternative implementations"
- AI صریح راهنمایی شود: AC نام-محور بد است، AC رفتار-محور خوب
**۷-E. جلوگیری از false-positive (R9):**
> «جوری هم نشه که سیستم کاری که بد انجام شده... در verify تیک بزنه که کامل شده»
دستورالعمل به AI:
- اگر AC vague → split به sub-behaviors concrete
- هرگز AC بدون `acceptance_signal` نسازد
- `false_positive_guard` فیلد: چه شواهد ضعیفی نباید سبب done شود
- مثال: «اگر فقط فایل با نام وجود دارد، done نیست — باید رفتار X هم باشد»
**۷-F. Integration با فازهای قبلی:**
پرامپت تولید‌شده استفاده کند از:
- `purpose_inventory` (فاز ۱)
- `runtime_state` (فاز ۴)
- `logic_audit_findings` (فاز ۵)
- `notification_audit` (فاز ۶)
### Acceptance Criteria فاز ۷
- **manual** path: پرامپت‌های جدید برای ۳ ایده‌ی نمونه شامل `behavior` + `acceptance_signal` + `business_intent` + `alternative_implementations` + `false_positive_guard`
- **auto** path: همان ساختار برای task های scan-generated
- چک‌لیست‌ها شامل `behavior_observable` در هر step
- mode `auto` در `_ai_plan_steps_from_idea` کار می‌کند برای هر دو
- verify روی same tasks: false-negative ≤ ۲۰%، false-positive ≤ ۵%
- backward compat: AC های قدیمی (فقط text) همچنان کار می‌کنند
---
## 🅷 فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration
> پوشش **R5, R6** — تسک‌های scan باید مثل manual باشند
### مسئله (R5, R6)
- تسک‌های auto-scan الان `prompt + AC` دارند ولی `task_steps` ندارند
- Telegram notification برای آن‌ها audit نشده
- کاربر می‌خواهد silent default
### تغییرات
**۸-A. task_steps برای auto-tasks:**
در `oversight_service.py:4700-4740`:
- بعد از `build_strong_prompt`، اگر `auto_task_checklist_mode != "never"`:
  - if `mode == "always"` → همیشه call `_ai_plan_steps_from_idea`
  - if `mode == "auto"` → AI تصمیم می‌گیرد بر اساس پیچیدگی
**۸-B. Telegram Notification Template (R6):**
`notification_service.py`:
```python
# Caption template برای auto-scan task
{
    "🤖 تسک جدید از scan خودکار",
    "📌 {title}",
    "🎯 {business_intent[:200]}",
    "📋 چک‌لیست: {N step}" if has_steps else None,
    "🧠 Logic concerns: {logic_audit_count}" if has_logic_issues else None,
    "🔄 وابسته به تغییر: {dependent_changes}" if delta_dependent else None,
    "📊 Outcome impact: {outcome_score}" if outcome_data else None,
    "🔗 [لینک کارت]",
    "🔍 [Inspector session]",
}
# Attachments:
- prompt_full.md (full task prompt)
- scan_inspector_session_link
```
**۸-C. Silent default + UI control:**
`WatchedProject` fields:
```python
auto_task_notify_sound: bool = False     # silent for auto-tasks
scan_notify_sound: bool = False           # silent for scan completion
```
در UI: checkbox `🔔 صدای notification برای تسک‌های auto-scan`.
**۸-D. Scan-completion notification:**
علاوه بر notification هر task، یک scan-completion notification:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
### Acceptance Criteria فاز ۸
- بعد از scan: همه auto-tasks در UI چک‌لیست دارند (اگر mode=auto و پیچیده‌اند)
- caption تلگرام شامل همه فیلدها
- silent default = true
- scan_completed notification با Bundle PDF + Inspector session link
- regression: تسک‌های دستی رفتار قبلی را حفظ کنند
---
## 🅸 فاز ۹ — UI Redesign (R2, R8)
> پوشش **R2** — به‌روزرسانی گزینه‌ها
### مسئله (R2)
> «گزینه‌هاش هم به روز رسانی بشن»
### تغییرات
**۹-A. بازنگری scan_depth modes:**
از (فعلی):
```
quick (3 pass) | standard (6 pass) | deep (12 pass، پیش‌فرض) | thorough (12 + health + roadmap)
```
به:
```
⚡ quick (5 pass) — frontend + backend + security + delta + stale
⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
🔬 deep (12 pass) — همه + impact analysis
🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)
```
**۹-B. بازنگری criteria_weights:**
به‌جای ۶ slider:
- preset selector: "general", "security-first", "tests-first", "feature-completeness", "logic-quality"
- sliders در `<details>` (advanced)
**۹-C. بازنگری Smart Task Lifecycle:**
- بررسی هر گزینه: کار می‌کند؟
- label با وضعیت فعلی
**۹-D. گزینه‌های جدید Phase 5:**
`WatchedProject` فیلدهای جدید (همه با default sensible):
```python
# Coverage
inventory_layers: List[str] = ["all"]    # یا subset
# Intelligence
stale_detection_enabled: bool = True
delta_analysis_enabled: bool = True
runtime_discovery_enabled: bool = True
outcome_data_enabled: bool = True
logic_audit_enabled: bool = True
notification_audit_enabled: bool = True
inspector_session_enabled: bool = True   # R14
# Lifecycle
auto_task_checklist_mode: str = "auto"   # R5
cleanup_tasks_enabled: bool = True
# Notifications
auto_task_notify_sound: bool = False     # R6
scan_notify_sound: bool = False          # R6
```
**۹-E. بازطراحی Layout — ۴ tabs (به‌جای ۳):**
| Tab | محتوا |
|---|---|
| **🔍 Coverage** | scan_depth + inventory_layers + criteria preset + auto_models |
| **🧠 Intelligence** | stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session |
| **🔁 Lifecycle** | dedup + auto-regenerate + checklist mode + cleanup |
| **🔔 Notifications** | auto_task_sound + scan_sound + notification_audit + custom templates |
**۹-F. AI explanation برای هر گزینه (R8):**
هر گزینه دارای help icon ⓘ + AI-generated description (از فاز ۲):
- این گزینه دقیقاً چه می‌کند
- کِی اضافه شد
- وضعیت فعلی (active / possibly stale / unknown)
**۹-G. Feature Inventory Panel (R8):**
panel جدید در UI با لیست همه options + AI explanation (از فاز ۲).
### Acceptance Criteria فاز ۹
- در UI، ۴ tab وجود دارد
- mode `balanced` پیش‌فرض جدید
- mode `ultra` در دسترس
- backward compat: scan_depth قدیمی همچنان کار کند
- هر گزینه دارای tooltip + AI explanation
- Feature Inventory Panel functional
---
## 🅹 فاز ۱۰ — Meta-validation
> خود این پرامپت قابل verify باشد
### مسئله
کاربر می‌خواهد این پرامپت به task تبدیل شود و توسط verify Phase 4 سنجیده شود.
### تغییرات
**۱۰-A. این پرامپت به task تبدیل:**
- ایده‌ی خام = این متن
- AC ها = AC های هر فاز ۱-۹
- task_steps = ۱۰ فاز
**۱۰-B. AC قابل-verify دقیق:**
هر AC در هر فاز measurable نوشته شده (مثلاً «≥ ۵ stale item کشف شود»).
**۱۰-C. Iteration loop:**
- پیاده‌سازی → verify deep (Phase 4) → نتیجه
- اگر `done < 100%` → iteration بعدی
- اگر `done = 100%` → فاز بعدی
- log در `phase5_meta_validation.md`
**۱۰-D. شناسایی verify bugs:**
- اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify
- اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive)
- log همه‌ی این موارد
### Acceptance Criteria فاز ۱۰
- پرامپت نهایی شامل ۱۰ task_step
- هر step دارای `behavior_observable` + `verification_hint`
- بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد
- log باگ‌های verify identified
---
## 🗂️ نقشه آدرس‌ها
| فاز | فایل | نوع |
|---|---|---|
| 1 | `backend/app/services/scan_v5/comprehensive_inventory.py` | new |
| 1 | `backend/app/services/scan_v5/purpose_extractor.py` | new |
| 1 | `backend/app/services/oversight_service.py:161` | + 3 fields |
| 2 | `backend/app/services/scan_v5/stale_detector.py` | new |
| 2 | `backend/app/services/scan_v5/feature_documenter.py` | new |
| 2 | `frontend/src/app/oversight/page.tsx` | + Feature Inventory panel |
| 3 | `backend/app/services/scan_v5/delta_analyzer.py` | new |
| 3 | `backend/app/services/scan_v5/dependency_analyzer.py` | new |
| 3 | `backend/app/services/oversight_service.py:161` | + `prev_scan_state` |
| 4 | `backend/app/services/scan_v5/runtime_discovery.py` | new |
| 4 | `backend/app/services/scan_v5/outcome_analyzer.py` | new |
| 4 | `backend/app/services/scan_v5/scan_inspector_session.py` | new |
| 4 | `frontend/src/app/projects/[id]/page.tsx` (Inspector tab) | + Scan Sessions section |
| 5 | `backend/app/services/scan_v5/coherence_analyzer.py` | new |
| 5 | `backend/app/services/scan_v5/anti_pattern_detector.py` | new |
| 5 | `backend/app/services/oversight_service.py` | + task type "logic_audit" |
| 6 | `backend/app/services/scan_v5/notification_auditor.py` | new |
| 6 | `backend/app/services/notification_service.py` | audit |
| 6 | `backend/app/services/oversight_telegram_compose.py` | + scan template |
| 7 | `backend/app/services/oversight_strong_prompt.py` | + behavior + business_intent + false_positive_guard |
| 7 | `backend/app/services/oversight_service.py:3454` (idea_to_prompt) | structured AC |
| 7 | `backend/app/services/oversight_service.py:2673` (_ai_plan_steps_from_idea) | structured steps |
| 7 | `backend/app/services/oversight_service.py:4700-4740` (auto-task creation) | apply same to auto |
| 8 | `backend/app/services/notification_service.py` | new templates |
| 8 | `backend/app/services/oversight_mega_bundle.py` | + scan-bundle PDF |
| 9 | `frontend/src/app/oversight/page.tsx` | 4 tabs redesign |
| 9 | `backend/app/services/oversight_service.py:161` | + ۱۰ field جدید |
| 10 | meta — هر فاز AC مشخص | — |
---
## 🎯 ترتیب اجرا
**Round 1 — پایه:**
1. فاز ۱ — Comprehensive Inventory + Purpose
**Round 2 — تحلیل ساختاری:**
2. فاز ۲ — Stale Detection + Feature Inventory Panel
3. فاز ۳ — Delta + Bidirectional Dependency
**Round 3 — تحلیل دینامیک:**
4. فاز ۴ — Runtime + Outcome + Inspector Session
5. فاز ۵ — Logical Audit
6. فاز ۶ — Notification Audit
**Round 4 — تولید + UI:**
7. فاز ۷ — Smart Prompt (manual + auto)
8. فاز ۸ — Auto-tasks Like Manual + Telegram
9. فاز ۹ — UI Redesign
**Round 5 — نهایی:**
10. فاز ۱۰ — Meta-validation
هر Round با runtime test قبل از push. در پایان: یک scan کامل روی این پروژه برای meta-validation.
```

## 📋 چک‌لیست مراحل (10 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: فاز ۱ — Comprehensive Inventory + Purpose Extraction** — این مرحله شامل پیاده‌سازی کامل فاز ۱ از پرامپت کاربر است: ایجاد ماژول‌های جدید comprehensive_inventory.py و purpose_extractor.py در backend/app/services/scan_v5/. comprehensive_inventory.py باید ۱۲ لایه مختلف (Files, Backend endpoints, UI elements, DB schema, Env vars, Config files, Dependencies, Sc
- [x] **مرحله 2: فاز ۲ — Stale Detection + Feature Inventory Panel** — این مرحله شامل پیاده‌سازی کامل فاز ۲ از پرامپت کاربر است: ایجاد ماژول‌های جدید stale_detector.py و feature_documenter.py در backend/app/services/scan_v5/. stale_detector.py باید ۸ نوع structural stale (Dead UI buttons, Dead frontend routes, Dead backend endpoints, Unused functions/classes, Unused da
- [x] **مرحله 3: فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact** — این مرحله شامل پیاده‌سازی کامل فاز ۳ از پرامپت کاربر است: ایجاد ماژول‌های جدید delta_analyzer.py و dependency_analyzer.py در backend/app/services/scan_v5/. delta_analyzer.py باید ۶ نوع تغییر (add, remove, modify, rename, move, signature-change) را با مقایسه state فعلی و قبلی (ذخیره شده در WatchedPro
- [x] **مرحله 4: فاز ۴ — Runtime Discovery + Outcome + Inspector Session** — این مرحله شامل پیاده‌سازی کامل فاز ۴ از پرامپت کاربر است: ایجاد ماژول‌های جدید runtime_discovery.py, outcome_analyzer.py, scan_inspector_session.py در backend/app/services/scan_v5/. runtime_discovery.py باید از navigation_helper, Playwright, backend_log_probe, vision_helper, code_aware_verifier برای
- [x] **مرحله 5: فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)** — این مرحله شامل پیاده‌سازی کامل فاز ۵ از پرامپت کاربر است: ایجاد ماژول‌های جدید coherence_analyzer.py و anti_pattern_detector.py در backend/app/services/scan_v5/. coherence_analyzer.py باید pipeline coherence را برای ۶ نوع chain (Data pipeline, AI/LLM chain, Business logic, Auth/Permission, Feedback 
- [x] **مرحله 6: فاز ۶ — Notification System Audit (R12)** — این مرحله شامل پیاده‌سازی کامل فاز ۶ از پرامپت کاربر است: ایجاد ماژول جدید notification_auditor.py در backend/app/services/scan_v5/. این ماژول باید notification inventory (از فاز ۱ لایه ۱۱) را با فیلدهای file_path, line, event_type, caption_template, silent_default, attachments, trigger_condition, f
- [x] **مرحله 7: فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)** — این مرحله شامل پیاده‌سازی کامل فاز ۷ از پرامپت کاربر است: به‌روزرسانی oversight_strong_prompt.py و oversight_service.py برای پشتیبانی از ساختار غنی AC و task_step. در oversight_strong_prompt.py باید section جدید '🎯 معیار رفتاری + business intent + alternative implementations' اضافه شود. در oversight
- [x] **مرحله 8: فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration** — این مرحله شامل پیاده‌سازی کامل فاز ۸ از پرامپت کاربر است: به‌روزرسانی oversight_service.py در بخش auto-task creation (~4700-4740) برای اضافه کردن task_steps به auto-tasks با استفاده از _ai_plan_steps_from_idea و رعایت auto_task_checklist_mode. همچنین باید Telegram notification template جدید برای aut
- [x] **مرحله 9: فاز ۹ — UI Redesign (R2, R8)** — این مرحله شامل پیاده‌سازی کامل فاز ۹ از پرامپت کاربر است: بازطراحی کامل UI در frontend/src/app/oversight/page.tsx. scan_depth modes باید به ۴ حالت جدید (quick 5 pass, balanced 8 pass پیش‌فرض جدید, deep 12 pass, ultra 12 pass) تغییر یابد. criteria_weights باید به preset selector با گزینه‌های general,
- [x] **مرحله 10: فاز ۱۰ — Meta-validation** — این مرحله شامل پیاده‌سازی کامل فاز ۱۰ از پرامپت کاربر است: تبدیل این پرامپت به task با ۱۰ task_step (مطابق فازهای ۱ تا ۹) و AC های قابل-verify دقیق. هر AC باید measurable باشد (مثلاً '≥ ۵ stale item کشف شود'). باید یک iteration loop پیاده‌سازی شود: پیاده‌سازی → verify deep (Phase 4) → نتیجه. اگر don

---

# 🔹 مرحله 1: فاز ۱ — Comprehensive Inventory + Purpose Extraction

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۱ از پرامپت کاربر است: ایجاد ماژول‌های جدید comprehensive_inventory.py و purpose_extractor.py در backend/app/services/scan_v5/. comprehensive_inventory.py باید ۱۲ لایه مختلف (Files, Backend endpoints, UI elements, DB schema, Env vars, Config files, Dependencies, Scripts, Cron/scheduled, Routes, Notification calls, UI options/settings) را با روش‌های مشخص (git ls-files, AST parse, parse .tsx, inspect, regex, glob) اسکن کند. purpose_extractor.py باید برای هر آیتم inventory یک purpose_map با فیلدهای stated_purpose, evidence_sources, expected_inputs, expected_outputs, interacting_with, creation_context, current_usage استخراج کند. همچنین باید فیلدهای جدید last_scan_inventory, last_scan_purpose_map, last_scan_at_v5 به مدل WatchedProject اضافه شود. خارج از این مرحله: پیاده‌سازی فازهای ۲ تا ۱۰، تغییرات UI، و تحلیل‌های پیشرفته‌تر. نکته حیاتی: این فاز پایه تمام فازهای بعدی است و باید inventory حداقل ۵۰ backend endpoint و ۳۰ UI element و ۱۰ notification call را پوشش دهد.
**Key terms:** backend/app/services/scan_v5/comprehensive_inventory.py, backend/app/services/scan_v5/purpose_extractor.py, WatchedProject.last_scan_inventory, WatchedProject.last_scan_purpose_map, WatchedProject.last_scan_at_v5, AST parse, git ls-files, notify_event, os.environ.get, apscheduler

**بخش مربوط از متن کاربر:**
```
## 🅰️ فاز ۱ — Comprehensive Inventory + Purpose Extraction
> پوشش **R3** (ریز تا درشت)، پایه برای همه فازهای بعدی
### مسئله
الان `run_deep_scan` فقط ۳۵ فایل deep-read + import graph می‌بیند. backend endpoints، UI elements، DB schema، env vars، configs، **notifications، inspector sessions، UI options** هرگز inventory نمی‌شوند.
### تغییرات
**۱-A. Structural Inventory (۱۲ لایه):**
ماژول جدید: `backend/app/services/scan_v5/comprehensive_inventory.py`
| # | لایه | چه چیزی جمع می‌شود | روش |
|---|---|---|---|
| 1 | Files | همه فایل‌های git-tracked | `git ls-files` |
| 2 | Backend endpoints | همه `@router.{get,post,…}` + WebSocket + background tasks | AST parse |
| 3 | UI elements | همه `<button>`, `<form>`, `<input>`, `<Select>`, `<Link>`, modal triggers, dropdown items | parse `.tsx` |
| 4 | DB schema | همه dataclass + Column + migrations | inspect |
| 5 | Env vars | همه `os.environ.get`, `os.getenv`, `process.env.X` | regex |
| 6 | Config files | همه `.json`, `.yaml`, `.toml`, `.env*` | glob |
| 7 | Dependencies | `requirements.txt`, `package.json` | parse |
| 8 | Scripts | همه `.sh`, `pyproject::scripts`, `package.json::scripts` | parse |
| 9 | Cron/scheduled | همه `apscheduler`, `BackgroundTasks`, `asyncio.create_task` | regex |
| 10 | Routes | همه frontend `app/**/page.tsx` + backend route | walk |
| 11 | **Notification calls (R12)** | همه `notify_event(...)`, `send_telegram(...)`, `bot.send_message(...)` + event_type + silent flag + caption template | regex/AST |
| 12 | **UI options/settings (R8)** | همه checkbox/slider/dropdown در UI + field name در WatchedProject + default value | parse + cross-ref |
**۱-B. Purpose Extraction (R10):**
ماژول جدید: `backend/app/services/scan_v5/purpose_extractor.py`
برای **هر file/module/feature/option** که در inventory هست:
```python
purpose_inventory[item_id] = {
    "stated_purpose": "چه کاری *قرار است* بکند",
    "evidence_sources": ["comments", "docstrings", "tests", "raw_idea", "task_history", "commit_messages"],
    "expected_inputs": [...],
    "expected_outputs": [...],
    "interacting_with": [...],          # سایر componentهای همکار
    "creation_context": {                # R8 — کاربر فراموش کرده چی هست
        "first_seen_commit": "...",
        "first_seen_date": "...",
        "originating_task_id": "...",    # کدام task این را ساخت
        "originating_raw_idea": "...",   # متن خام درخواست
    },
    "current_usage": "...",              # آیا هنوز استفاده می‌شود؟ کجا؟
}
```
**منابع AI برای purpose:**
- محتوای فایل + docstrings + JSDoc
- test files مرتبط
- `WatchedProject.user_notes` (raw_idea اصلی پروژه)
- task history (task هایی که این item را modify کرده‌اند)
- commit messages روی این path
**۱-C. Storage:**
`WatchedProject` فیلدهای جدید:
```python
last_scan_inventory: Optional[Dict[str, Any]] = None
last_scan_purpose_map: Optional[Dict[str, Dict]] = None
last_scan_at_v5: Optional[str] = None  # timestamp scan جدید
```
### Acceptance Criteria فاز ۱
- `inventory.backend_endpoints` ≥ ۵۰
- `inventory.ui_elements` ≥ ۳۰
- `inventory.notification_calls` ≥ ۱۰
- `inventory.ui_options` همه‌ی checkbox/slider/dropdown ها را شامل شود
- `purpose_map` برای ≥ ۸۰% فایل‌های مهم
- برای هر UI option، `creation_context` نشان دهد از کدام task ساخته شد
```

python
purpose_inventory[item_id] = {
    "stated_purpose": "چه کاری *قرار است* بکند",
    "evidence_sources": ["comments", "docstrings", "tests", "raw_idea", "task_history", "commit_messages"],
    "expected_inputs": [...],
    "expected_outputs": [...],
    "interacting_with": [...],          # سایر componentهای همکار
    "creation_context": {                # R8 — کاربر فراموش کرده چی هست
        "first_seen_commit": "...",
        "first_seen_date": "...",
        "originating_task_id": "...",    # کدام task این را ساخت
        "originating_raw_idea": "...",   # متن خام درخواست
    },
    "current_usage": "...",              # آیا هنوز استفاده می‌شود؟ کجا؟
}
```
**منابع AI برای purpose:**
- محتوای فایل + docstrings + JSDoc
- test files مرتبط
- `WatchedProject.user_notes` (raw_idea اصلی پروژه)
- task history (task هایی که این item را modify کرده‌اند)
- commit messages روی این path
**۱-C. Storage:**
`WatchedProject` فیلدهای جدید:
```python
last_scan_inventory: Optional[Dict[str, Any]] = None
last_scan_purpose_map: Optional[Dict[str, Dict]] = None
last_scan_at_v5: Optional[str] = None  # timestamp scan جدید
```
### Acceptance Criteria فاز ۱
- `inventory.backend_endpoints` ≥ ۵۰
- `inventory.ui_elements` ≥ ۳۰
- `inventory.notification_calls` ≥ ۱۰
- `inventory.ui_options` همه‌ی checkbox/slider/dropdown ها را شامل شود
- `purpose_map` برای ≥ ۸۰% فایل‌های مهم
- برای هر UI option، `creation_context` نشان دهد از کدام task ساخته شد

--- کلیدواژه‌ها ---
backend/app/services/scan_v5/comprehensive_inventory.py, backend/app/services/scan_v5/purpose_extractor.py, WatchedProject.last_scan_inventory, WatchedProject.last_scan_purpose_map, WatchedProject.last_scan_at_v5, AST parse, git ls-files, notify_event, os.environ.get, apscheduler
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۱ — Comprehensive Inventory + Purpose Extraction برای scan_v5

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/scan_v5/comprehensive_inventory.py:1-350` — `کل فایل (جدید)` — فایل جدید — باید ۱۲ متد برای اسکن لایه‌های مختلف پیاده‌سازی شود. از AST parse برای backend endpoints، regex برای env vars، glob برای config files، و git ls-files برای files استفاده کند.
- `backend/app/services/scan_v5/purpose_extractor.py:1-200` — `کل فایل (جدید)` — فایل جدید — باید purpose_map با فیلدهای stated_purpose, evidence_sources, expected_inputs, expected_outputs, interacting_with, creation_context, current_usage استخراج کند. از docstrings, test files, commit messages, و WatchedProject.user_notes استفاده کند.
- `backend/app/models/oversight.py:50-80` — `کلاس WatchedProject` — افزودن سه فیلد جدید به مدل WatchedProject برای ذخیره نتایج اسکن فاز ۱.
  ```python
  last_scan_inventory: Optional[Dict[str, Any]] = None
  last_scan_purpose_map: Optional[Dict[str, Dict]] = None
  last_scan_at_v5: Optional[str] = None
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی کامل فاز ۱ از درخواست کاربر: ایجاد دو ماژول جدید comprehensive_inventory.py و purpose_extractor.py در مسیر backend/app/services/scan_v5/. comprehensive_inventory.py باید ۱۲ لایه مختلف (Files, Backend endpoints, UI elements, DB schema, Env vars, Config files, Dependencies, Scripts, Cron/scheduled, Routes, Notification calls, UI options/settings) را با روش‌های مشخص (git ls-files, AST parse, parse .tsx, inspect, regex, glob) اسکن کند. purpose_extractor.py باید برای هر آیتم inventory یک purpose_map با فیلدهای stated_purpose, evidence_sources, expected_inputs, expected_outputs, interacting_with, creation_context, current_usage استخراج کند. همچنین باید فیلدهای جدید last_scan_inventory, last_scan_purpose_map, last_scan_at_v5 به مدل WatchedProject اضافه شود. خارج از این مرحله: پیاده‌سازی فازهای ۲ تا ۱۰، تغییرات UI، و تحلیل‌های پیشرفته‌تر. نکته حیاتی: این فاز پایه تمام فازهای بعدی است و باید inventory حداقل ۵۰ backend endpoint و ۳۰ UI element و ۱۰ notification call را پوشش دهد. شواهد در کد: فایل‌های deep-read شده مانند backend/app/api/routes/oversight.py (که شامل notify_event و send_telegram است)، backend/app/api/routes/analysis.py (که شامل BackgroundTasks و asyncio.create_task است)، و frontend/src/app/oversight/page.tsx (که شامل UI elements است) نشان‌دهنده وجود لایه‌های مورد نیاز برای اسکن هستند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد دایرکتوری backend/app/services/scan_v5/ و فایل __init__.py. 2. پیاده‌سازی comprehensive_inventory.py با ۱۲ متد مجزا برای هر لایه (scan_files, scan_backend_endpoints, scan_ui_elements, scan_db_schema, scan_env_vars, scan_config_files, scan_dependencies, scan_scripts, scan_cron_scheduled, scan_routes, scan_notification_calls, scan_ui_options). 3. پیاده‌سازی purpose_extractor.py با متد extract_purpose که برای هر آیتم inventory purpose_map را با استفاده از docstrings, test files, commit messages, و WatchedProject.user_notes استخراج کند. 4. افزودن فیلدهای last_scan_inventory, last_scan_purpose_map, last_scan_at_v5 به مدل WatchedProject در فایل backend/app/models/oversight.py. 5. اتصال ماژول‌های جدید به oversight_service.py برای فراخوانی در run_deep_scan.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: large

---

# 🔹 مرحله 2: فاز ۲ — Stale Detection + Feature Inventory Panel

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۲ از پرامپت کاربر است: ایجاد ماژول‌های جدید stale_detector.py و feature_documenter.py در backend/app/services/scan_v5/. stale_detector.py باید ۸ نوع structural stale (Dead UI buttons, Dead frontend routes, Dead backend endpoints, Unused functions/classes, Unused dataclass fields, Unused env vars, Orphan files, Stale dependencies) و ۵ نوع semantic stale (Purpose-mismatched, Hidden purpose, Inconsistent با تغییرات اخیر, Outdated business assumption, Forgotten by user) را شناسایی کند. feature_documenter.py باید برای هر UI option/setting/feature/button یک AI documentation با فیلدهای name, what_it_does, when_added, originating_idea, current_status, dependencies, dependents, recommended_action تولید کند. همچنین باید پنل Feature Inventory در frontend/src/app/oversight/page.tsx اضافه شود که تمام آیتم‌ها را با دسته‌بندی و AI explanation نمایش دهد. برای هر stale item باید task (cleanup/audit/document) با AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۳ تا ۱۰، تغییرات runtime discovery. نکته حیاتی: این فاز مهم‌ترین فاز برای حل مشکل R8 (گزینه‌های قدیمی) است و باید حداقل ۵ structural stale و ۳ semantic stale و ۸ hidden-purpose option شناسایی کند.
**Key terms:** backend/app/services/scan_v5/stale_detector.py, backend/app/services/scan_v5/feature_documenter.py, frontend/src/app/oversight/page.tsx, Dead UI buttons, Dead backend endpoints, Unused functions/classes, Hidden purpose, Forgotten by user, AI documentation, Feature Inventory Panel

**بخش مربوط از متن کاربر:**
```
## 🅱️ فاز ۲ — Stale Detection + Feature Inventory Panel
> **مهم‌ترین فاز برای R8 — حل مشکل «گزینه‌های قدیمی که نمی‌دانم چی هست»**
### مسئله (R8 — تأکید سه‌باره)
> «خیلی‌هاش قدیمیه و کار نمی‌کنه و خیلی‌هاش نیاز داره بدونم چی بوده، برای چیه و هر سری باید بهش سر بزنم»
### تغییرات
**۲-A. Structural Stale (۸ نوع):**
ماژول جدید: `backend/app/services/scan_v5/stale_detector.py`
| نوع | شناسایی |
|---|---|
| Dead UI buttons | onClick handler ندارد / empty / به endpoint 404 می‌رود |
| Dead frontend routes | در nav menu نیست و در هیچ Link/router.push نیست |
| Dead backend endpoints | در هیچ frontend fetch نیست + در Render logs ۳۰ روز اخیر صدا نشد |
| Unused functions/classes | reverse import = 0 و entry point نیست |
| Unused dataclass fields | در هیچ‌جا read/write نمی‌شود |
| Unused env vars | تعریف شده ولی هیچ `os.environ.get` |
| Orphan files | reverse import = 0 |
| Stale dependencies | در requirements/package.json هست ولی import نمی‌شود |
**۲-B. Semantic Stale (R10 — ۵ نوع):**
| نوع | شناسایی |
|---|---|
| Purpose-mismatched | `stated_purpose` با `actual_behavior` نمی‌خواند |
| Hidden purpose (R8) | کاربر نمی‌داند چی هست — هیچ doc، نام مبهم، creation_context قدیمی |
| Inconsistent با تغییرات اخیر | کد فرض می‌کند رفتار قدیمی X، ولی X تغییر کرده (نیاز به فاز ۳) |
| Outdated business assumption | threshold/config که با realities فعلی نمی‌خواند |
| Forgotten by user (R8 — جدید) | UI option/setting که کاربر هر بار باید بپرسد چی هست |
**۲-C. AI-Generated Documentation برای هر option (R8):**
برای **هر** UI option/setting/feature/button که در inventory.ui_options یا inventory.ui_elements هست:
```python
ai_documentation[option_id] = {
    "name": "...",
    "what_it_does": "AI explanation — این گزینه دقیقاً چه کاری می‌کند",
    "when_added": "تاریخ + commit",
    "originating_idea": "متن خام درخواست اولیه",
    "current_status": "active | possibly_stale | broken | unknown",
    "dependencies": ["..."],         # forward
    "dependents": ["..."],           # reverse
    "recommended_action": "keep | remove | refactor | document"
}
```
**۲-D. Feature Inventory Panel در UI:**
`frontend/src/app/oversight/page.tsx` — اضافه شدن panel جدید:
```
🗺 Feature Inventory
├─ 🔧 Settings (12)
│   ├─ ✓ verify_mode (active — Phase 4, از task #abc1234)
│   ├─ ✓ dedup_threshold (active — Phase 3)
│   ├─ ⚠️ confirmation_streak_required (possibly_stale — ۹۰ روز unused)
│   └─ ...
├─ 🎛 UI Buttons (45)
├─ 🌐 Backend Endpoints (78)
└─ ⚙️ Env Vars (23)
```
برای هر مدخل: hover → AI explanation کامل.
**۲-E. Task Generation:**
برای هر stale item:
- **cleanup** task (structural) یا **audit** task (semantic) یا **document** task (hidden purpose)
- AC رفتار-محور: «یا حذف شود، یا با docstring/comment توضیح داده شود چرا نگه داشته، یا ارتقا یابد»
- priority بر اساس impact
### Acceptance Criteria فاز ۲
- روی این پروژه ≥ ۵ structural stale + ≥ ۳ semantic stale + ≥ ۸ hidden-purpose options
- Feature Inventory Panel با ≥ ۲۰ مدخل
- هر stale → task با explanation کامل (نه فقط «این unused است»)
- روی project trade مثال کاربر (R11)، scan تشخیص دهد گزینه‌هایی که برای trading فعلاً ضرر می‌دهند
```

python
ai_documentation[option_id] = {
    "name": "...",
    "what_it_does": "AI explanation — این گزینه دقیقاً چه کاری می‌کند",
    "when_added": "تاریخ + commit",
    "originating_idea": "متن خام درخواست اولیه",
    "current_status": "active | possibly_stale | broken | unknown",
    "dependencies": ["..."],         # forward
    "dependents": ["..."],           # reverse
    "recommended_action": "keep | remove | refactor | document"
}
```
**۲-D. Feature Inventory Panel در UI:**
`frontend/src/app/oversight/page.tsx` — اضافه شدن panel جدید:
```
🗺 Feature Inventory
├─ 🔧 Settings (12)
│   ├─ ✓ verify_mode (active — Phase 4, از task #abc1234)
│   ├─ ✓ dedup_threshold (active — Phase 3)
│   ├─ ⚠️ confirmation_streak_required (possibly_stale — ۹۰ روز unused)
│   └─ ...
├─ 🎛 UI Buttons (45)
├─ 🌐 Backend Endpoints (78)
└─ ⚙️ Env Vars (23)
```
برای هر مدخل: hover → AI explanation کامل.
**۲-E. Task Generation:**
برای هر stale item:
- **cleanup** task (structural) یا **audit** task (semantic) یا **document** task (hidden purpose)
- AC رفتار-محور: «یا حذف شود، یا با docstring/comment توضیح داده شود چرا نگه داشته، یا ارتقا یابد»
- priority بر اساس impact
### Acceptance Criteria فاز ۲
- روی این پروژه ≥ ۵ structural stale + ≥ ۳ semantic stale + ≥ ۸ hidden-purpose options
- Feature Inventory Panel با ≥ ۲۰ مدخل
- هر stale → task با explanation کامل (نه فقط «این unused است»)
- روی project trade مثال کاربر (R11)، scan تشخیص دهد گزینه‌هایی که برای trading فعلاً ضرر می‌دهند

--- کلیدواژه‌ها ---
backend/app/services/scan_v5/stale_detector.py, backend/app/services/scan_v5/feature_documenter.py, frontend/src/app/oversight/page.tsx, Dead UI buttons, Dead backend endpoints, Unused functions/classes, Hidden purpose, Forgotten by user, AI documentation, Feature Inventory Panel
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۲ — پیاده‌سازی Stale Detection و Feature Inventory Panel برای حل مشکل R8 (گزینه‌های قدیمی)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/oversight/page.tsx:1-50` — `OversightPage` — این فایل میزبان پنل جدید Feature Inventory خواهد بود. باید یک tab یا panel جدید با عنوان '🗺 Feature Inventory' اضافه شود.
  ```tsx
  این فایل در deep context موجود است و صفحه اصلی oversight را تشکیل می‌دهد. در حال حاضر محتوای دقیق خطوط آن در دسترس نیست، اما ساختار کلی آن شامل tabها و panelهای مختلف است.
  ```
- `backend/app/api/routes/oversight.py:1-100` — `oversight_router` — یک endpoint جدید مانند POST /oversight/scan-stale باید اضافه شود که StaleDetector را فراخوانی کرده و نتایج را بازگرداند.
  ```python
  این فایل در deep context موجود است و شامل endpointهای مربوط به oversight می‌شود. محتوای دقیق خطوط آن در دسترس نیست.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک پیاده‌سازی کامل فاز ۲ از درخواست کاربر است که شامل ایجاد دو ماژول جدید در backend/app/services/scan_v5/ می‌شود: stale_detector.py و feature_documenter.py. stale_detector.py باید ۸ نوع structural stale (Dead UI buttons, Dead frontend routes, Dead backend endpoints, Unused functions/classes, Unused dataclass fields, Unused env vars, Orphan files, Stale dependencies) و ۵ نوع semantic stale (Purpose-mismatched, Hidden purpose, Inconsistent با تغییرات اخیر, Outdated business assumption, Forgotten by user) را شناسایی کند. feature_documenter.py باید برای هر UI option/setting/feature/button یک AI documentation با فیلدهای name, what_it_does, when_added, originating_idea, current_status, dependencies, dependents, recommended_action تولید کند. همچنین باید پنل Feature Inventory در frontend/src/app/oversight/page.tsx اضافه شود که تمام آیتم‌ها را با دسته‌بندی و AI explanation نمایش دهد. برای هر stale item باید task (cleanup/audit/document) با AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۳ تا ۱۰، تغییرات runtime discovery. نکته حیاتی: این فاز مهم‌ترین فاز برای حل مشکل R8 (گزینه‌های قدیمی) است و باید حداقل ۵ structural stale و ۳ semantic stale و ۸ hidden-purpose option شناسایی کند. کلیدواژه‌ها: backend/app/services/scan_v5/stale_detector.py, backend/app/services/scan_v5/feature_documenter.py, frontend/src/app/oversight/page.tsx, Dead UI buttons, Dead backend endpoints, Unused functions/classes, Hidden purpose, Forgotten by user, AI documentation, Feature Inventory Panel. شواهد در کد: فایل frontend/src/app/oversight/page.tsx در deep context موجود است و می‌تواند میزبان پنل جدید باشد. فایل‌های backend/app/api/routes/oversight.py و backend/app/services/oversight_service.py زیرساخت backend را فراهم می‌کنند.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد دایرکتوری backend/app/services/scan_v5/ و فایل‌های __init__.py, stale_detector.py, feature_documenter.py. ۲. پیاده‌سازی کلاس StaleDetector در stale_detector.py با متدهای detect_structural_stale() و detect_semantic_stale(). ۳. پیاده‌سازی کلاس FeatureDocumenter در feature_documenter.py با متد generate_documentation() که از AI manager برای تولید توضیحات استفاده می‌کند. ۴. اضافه کردن endpoint جدید در backend/app/api/routes/oversight.py برای scan و دریافت نتایج stale detection. ۵. اضافه کردن پنل Feature Inventory در frontend/src/app/oversight/page.tsx با دسته‌بندی و hover explanation. ۶. پیاده‌سازی task generation برای هر stale item با AC رفتار-محور.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: critical
- تخمین زمان: large

---

# 🔹 مرحله 3: فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۳ از پرامپت کاربر است: ایجاد ماژول‌های جدید delta_analyzer.py و dependency_analyzer.py در backend/app/services/scan_v5/. delta_analyzer.py باید ۶ نوع تغییر (add, remove, modify, rename, move, signature-change) را با مقایسه state فعلی و قبلی (ذخیره شده در WatchedProject.prev_scan_state) تشخیص دهد. dependency_analyzer.py باید bidirectional dependency (هم forward dependencies و هم reverse dependents) را برای هر فایل تغییر یافته محاسبه کند و بین import-based و logical تمایز قائل شود. logical impact analysis باید با AI روی purpose_inventory تحلیل کند که آیا تغییر یک فایل، منطق dependents را می‌شکند. برای هر changed file + dependent در خطر باید task با badge '🔄 وابسته به تغییر' و AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۴ تا ۱۰، تغییرات UI. نکته حیاتی: این فاز پوشش R7 (Bidirectional dependency) را فراهم می‌کند و باید بعد از دو scan متوالی، delta با ۶ نوع تشخیص دهد و اگر signature تغییر کرد و ۳ caller دارد، حداقل ۱ task برای بررسی caller ها ایجاد کند.
**Key terms:** backend/app/services/scan_v5/delta_analyzer.py, backend/app/services/scan_v5/dependency_analyzer.py, WatchedProject.prev_scan_state, Bidirectional Dependency, Logical Impact Analysis, signature-change, import-based, logical dependencies, 🔄 وابسته به تغییر

**بخش مربوط از متن کاربر:**
```
## 🅲️ فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact
> پوشش **R7** — کاربر صریحاً bidirectional خواست
### مسئله (R7)
> «چه وابستگی‌هایی داشته **به چه چیزهایی وابسته بوده** و **چه چیزهایی بهش وابسته بودن**»
این **دو طرفه** است:
- forward: این فایل به چه چیزی وابسته است
- reverse: چه چیزهایی به این فایل وابسته‌اند
### تغییرات
**۳-A. ذخیره prev state:**
`WatchedProject`:
```python
prev_scan_state: Optional[Dict[str, Any]] = None
# {file_path → {sha, size, last_modified, imports_hash, purpose_hash, ui_elements_hash}}
```
**۳-B. compute delta — ۶ نوع (R7: «تغییر کرده یا اضافه شده یا کم شده یا ویرایش شده»):**
| نوع | تشخیص |
|---|---|
| `add` | در current، نه در prev |
| `remove` | در prev، نه در current |
| `modify` | sha متفاوت |
| `rename` | sha مشابه، path متفاوت |
| `move` | content مشابه، path متفاوت |
| `signature-change` | نام تابع ثابت، parameters یا return-type تغییر |
**۳-C. Bidirectional Dependency (R7 صریح):**
ماژول جدید: `backend/app/services/scan_v5/dependency_analyzer.py`
برای هر changed file:
```python
{
    "dependencies": [...],   # forward — file → چه چیزی import می‌کند
    "dependents": [...],     # reverse — چه چیزهایی → این file import می‌کنند
    "logical_dependencies": [...],   # forward منطقی — کدام components روی این تکیه دارند
    "logical_dependents": [...],     # reverse منطقی — این از کدام components انتظار دارد
}
```
تمایز بین **import-based** و **logical**:
- import: AST/grep
- logical: AI روی purpose_inventory تشخیص می‌دهد (مثلاً «این تابع threshold X را می‌خواند، اگر X تغییر کرد رفتار تغییر می‌کند»)
**۳-D. Logical Impact Analysis (R10):**
AI روی هر تغییر + dependents تحلیل می‌کند:
- آیا تغییر این فایل، **منطق** dependents را می‌شکند؟
- مثال: تغییر `threshold=0.65` → `threshold=0.8` → چه componentهای dependent behavior متفاوت نشان می‌دهند؟
- مثال: تغییر فرمت output `parse_signal()` → چه callerها به update نیاز دارند؟
- مثال: حذف یک env var → چه قسمت‌هایی crash می‌کنند؟
**۳-E. Task Generation:**
برای هر changed file + dependent در خطر:
- task با badge `🔄 وابسته به تغییر`
- AC رفتار-محور: «بعد از این تغییر، dependents X و Y باید همچنان behavior Z تولید کنند»
- اگر logical impact پیچیده است → priority بالاتر + reference به scan inspector session
### Acceptance Criteria فاز ۳
- بعد از دو scan متوالی، delta با ۶ نوع تشخیص داده شود
- اگر signature تغییر کرد و ۳ caller دارد، ≥ ۱ task برای بررسی caller ها
- logical impact detection: اگر threshold تغییر کرد، AI تحلیل کند نه فقط alert سینتکسی
- bidirectional dependency: هم dependencies (forward) هم dependents (reverse) برای هر changed item
```

python
prev_scan_state: Optional[Dict[str, Any]] = None
# {file_path → {sha, size, last_modified, imports_hash, purpose_hash, ui_elements_hash}}
```
**۳-B. compute delta — ۶ نوع (R7: «تغییر کرده یا اضافه شده یا کم شده یا ویرایش شده»):**
| نوع | تشخیص |
|---|---|
| `add` | در current، نه در prev |
| `remove` | در prev، نه در current |
| `modify` | sha متفاوت |
| `rename` | sha مشابه، path متفاوت |
| `move` | content مشابه، path متفاوت |
| `signature-change` | نام تابع ثابت، parameters یا return-type تغییر |
**۳-C. Bidirectional Dependency (R7 صریح):**
ماژول جدید: `backend/app/services/scan_v5/dependency_analyzer.py`
برای هر changed file:
```python
{
    "dependencies": [...],   # forward — file → چه چیزی import می‌کند
    "dependents": [...],     # reverse — چه چیزهایی → این file import می‌کنند
    "logical_dependencies": [...],   # forward منطقی — کدام components روی این تکیه دارند
    "logical_dependents": [...],     # reverse منطقی — این از کدام components انتظار دارد
}
```
تمایز بین **import-based** و **logical**:
- import: AST/grep
- logical: AI روی purpose_inventory تشخیص می‌دهد (مثلاً «این تابع threshold X را می‌خواند، اگر X تغییر کرد رفتار تغییر می‌کند»)
**۳-D. Logical Impact Analysis (R10):**
AI روی هر تغییر + dependents تحلیل می‌کند:
- آیا تغییر این فایل، **منطق** dependents را می‌شکند؟
- مثال: تغییر `threshold=0.65` → `threshold=0.8` → چه componentهای dependent behavior متفاوت نشان می‌دهند؟
- مثال: تغییر فرمت output `parse_signal()` → چه callerها به update نیاز دارند؟
- مثال: حذف یک env var → چه قسمت‌هایی crash می‌کنند؟
**۳-E. Task Generation:**
برای هر changed file + dependent در خطر:
- task با badge `🔄 وابسته به تغییر`
- AC رفتار-محور: «بعد از این تغییر، dependents X و Y باید همچنان behavior Z تولید کنند»
- اگر logical impact پیچیده است → priority بالاتر + reference به scan inspector session
### Acceptance Criteria فاز ۳
- بعد از دو scan متوالی، delta با ۶ نوع تشخیص داده شود
- اگر signature تغییر کرد و ۳ caller دارد، ≥ ۱ task برای بررسی caller ها
- logical impact detection: اگر threshold تغییر کرد، AI تحلیل کند نه فقط alert سینتکسی
- bidirectional dependency: هم dependencies (forward) هم dependents (reverse) برای هر changed item

--- کلیدواژه‌ها ---
backend/app/services/scan_v5/delta_analyzer.py, backend/app/services/scan_v5/dependency_analyzer.py, WatchedProject.prev_scan_state, Bidirectional Dependency, Logical Impact Analysis, signature-change, import-based, logical dependencies, 🔄 وابسته به تغییر
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact در scan_v5

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک مربوط به پیاده‌سازی کامل فاز ۳ از درخواست کاربر است: ایجاد ماژول‌های جدید delta_analyzer.py و dependency_analyzer.py در backend/app/services/scan_v5/. delta_analyzer.py باید ۶ نوع تغییر (add, remove, modify, rename, move, signature-change) را با مقایسه state فعلی و قبلی (ذخیره شده در WatchedProject.prev_scan_state) تشخیص دهد. dependency_analyzer.py باید bidirectional dependency (هم forward dependencies و هم reverse dependents) را برای هر فایل تغییر یافته محاسبه کند و بین import-based و logical تمایز قائل شود. logical impact analysis باید با AI روی purpose_inventory تحلیل کند که آیا تغییر یک فایل، منطق dependents را می‌شکند. برای هر changed file + dependent در خطر باید task با badge '🔄 وابسته به تغییر' و AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۴ تا ۱۰، تغییرات UI. نکته حیاتی: این فاز پوشش R7 (Bidirectional dependency) را فراهم می‌کند و باید بعد از دو scan متوالی، delta با ۶ نوع تشخیص دهد و اگر signature تغییر کرد و ۳ caller دارد، حداقل ۱ task برای بررسی caller ها ایجاد کند. کلیدواژه‌ها: backend/app/services/scan_v5/delta_analyzer.py, backend/app/services/scan_v5/dependency_analyzer.py, WatchedProject.prev_scan_state, Bidirectional Dependency, Logical Impact Analysis, signature-change, import-based, logical dependencies, 🔄 وابسته به تغییر. شواهد در کد: فایل backend/app/api/routes/oversight.py (خطوط ۱-۵۰) نشان‌دهنده endpointهای Oversight است که scan را آغاز می‌کنند. فایل backend/app/services/oversight_service.py (خطوط ۱-۱۰۰) حاوی کلاس OversightService با متدهای scan و auto_register_watched است. فایل backend/app/services/verify_runtime/code_aware_verifier.py (خطوط ۱-۵۰) نشان‌دهنده منطق code-aware است. فایل backend/app/services/oversight_deep_scan_service.py (خطوط ۱-۵۰) حاوی DeepScanService است. فایل backend/app/models/ai_profile.py (خطوط ۱-۵۰) مدل AIProfile را تعریف می‌کند. فایل backend/app/services/oversight_verifier.py (خطوط ۱-۵۰) حاوی VerifierService است. فایل backend/app/services/oversight_codex_service.py (خطوط ۱-۵۰) حاوی CodexService است. فایل backend/app/services/oversight_extraction.py (خطوط ۱-۵۰) حاوی ExtractionService است. فایل backend/app/services/oversight_inspector_bridge.py (خطوط ۱-۵۰) حاوی InspectorBridge است. فایل backend/app/services/oversight_mega_bundle.py (خطوط ۱-۵۰) حاوی MegaBundleService است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل جدید backend/app/services/scan_v5/delta_analyzer.py با کلاس DeltaAnalyzer که متد compute_delta(prev_state: dict, current_state: dict) -> List[DeltaChange] را پیاده‌سازی می‌کند. ۲. ایجاد فایل جدید backend/app/services/scan_v5/dependency_analyzer.py با کلاس DependencyAnalyzer که متدهای analyze_import_based(file_path: str, all_files: List[str]) -> Dict و analyze_logical(file_path: str, purpose_inventory: dict, ai_service: AIService) -> Dict را پیاده‌سازی می‌کند. ۳. افزودن فیلد prev_scan_state: Optional[Dict[str, Any]] به مدل WatchedProject در فایل backend/app/models/ai_profile.py (خطوط ۱-۵۰). ۴. به‌روزرسانی متد run_scan در OversightService (فایل backend/app/services/oversight_service.py) برای ذخیره state قبلی و فراخوانی DeltaAnalyzer. ۵. پیاده‌سازی logical impact analysis با AI در dependency_analyzer.py با استفاده از ai_manager (فایل backend/app/services/ai_manager.py). ۶. ایجاد task با badge '🔄 وابسته به تغییر' در فایل backend

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 4: فاز ۴ — Runtime Discovery + Outcome + Inspector Session

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۴ از پرامپت کاربر است: ایجاد ماژول‌های جدید runtime_discovery.py, outcome_analyzer.py, scan_inspector_session.py در backend/app/services/scan_v5/. runtime_discovery.py باید از navigation_helper, Playwright, backend_log_probe, vision_helper, code_aware_verifier برای کشف runtime_state شامل routes_alive, routes_404, endpoints_called_recently, endpoints_never_called, ui_features_visible, recent_commits استفاده کند. outcome_analyzer.py باید برای هر پروژه outcome data (trade logs, conversation outcomes, error rates, task completion rates, delivery rates) جمع‌آوری کند. scan_inspector_session.py باید برای هر scan یک inspector session باز کند (مشابه verify در Phase 4) که شامل همه AI calls, Playwright actions, screenshots, runtime probe outputs باشد و در پایان session archived + bundle PDF + Telegram message ارسال شود. همچنین باید tab 'Scan Sessions' در Inspector UI اضافه شود. خارج از این مرحله: پیاده‌سازی فازهای ۵ تا ۱۰. نکته حیاتی: این فاز پوشش R14 (Inspector tab integration) را فراهم می‌کند و باید بعد از هر scan یک inspector session با ≥ ۱۰ پیام و ≥ ۵ screenshot ایجاد کند.
**Key terms:** backend/app/services/scan_v5/runtime_discovery.py, backend/app/services/scan_v5/outcome_analyzer.py, backend/app/services/scan_v5/scan_inspector_session.py, frontend/src/app/projects/[id]/page.tsx, navigation_helper::extract_nav_links_from_page, Playwright, backend_log_probe::_fetch_relevant_logs, vision_helper::analyze_screenshot, code_aware_verifier::_fetch_recent_commits_with_diff, create_scan_inspector_session, 🔍 Scan Session, bundle PDF

**بخش مربوط از متن کاربر:**
```
## 🅳 فاز ۴ — Runtime Discovery + Outcome + Inspector Session
> پوشش **R14** — Inspector tab integration — این مهم‌ترین آپشن از قلم افتاده
### مسئله (R14 صریح)
کاربر گفت: «در بررسی‌های عمیق از تب بازرس ویژه هم کمک می‌گیره و **اسکرین‌ها** رو می‌گیره و در تلگرام **همراه با گزارش** بفرسته و **چت‌ها** رو در یه session ثبت کنه و **بایگانی** کنه؟»
این **اصلاً در پرامپت‌های قبلی نبود**. فعلاً Inspector فقط در verify (Phase 4) استفاده می‌شد.
### تغییرات
**۴-A. Runtime Discovery (با ایده از Phase 4):**
ماژول جدید: `backend/app/services/scan_v5/runtime_discovery.py`
| منبع | استفاده |
|---|---|
| `navigation_helper::extract_nav_links_from_page` | nav links واقعی frontend |
| Playwright on each route | screenshot + status code → 404 detection |
| `backend_log_probe::_fetch_relevant_logs` 30-day | endpoints واقعاً called شده |
| `vision_helper::analyze_screenshot` | کشف feature های UI |
| `code_aware_verifier::_fetch_recent_commits_with_diff` | recent commits context |
خروجی: `runtime_state` شامل `routes_alive[]`, `routes_404[]`, `endpoints_called_recently[]`, `endpoints_never_called[]`, `ui_features_visible[]`, `recent_commits[]`.
**۴-B. Outcome Data Collection:**
برای هر پروژه، scan تلاش کند outcome data بیابد:
| نوع پروژه | outcome data |
|---|---|
| Trading | trade logs, P&L history |
| AI/Chat | conversation outcomes |
| Web service | error rates, latency from logs |
| Scheduling | task completion rates |
| Notification | delivery + open rates |
روش‌ها: Render logs filtered + DB tables outcome-naming + file artifacts.
**۴-C. Scan Inspector Session (R14 — حیاتی):**
ماژول جدید: `backend/app/services/scan_v5/scan_inspector_session.py`
**هر scan یک inspector session باز می‌کند** (مشابه verify در Phase 4):
```python
session = create_scan_inspector_session(
    watched_id=...,
    scan_id=...,
    project_name=...,
)
```
**در طول scan:**
- همه AI calls (purpose extraction، stale detection، logic audit، etc.) → پیام در session با role="ai", content=request+response
- همه Playwright actions → پیام با role="action", screenshot=path
- همه screenshot ها → ذخیره روی disk + reference در session
- runtime probe outputs → پیام با role="probe"
**در پایان scan:**
- session archived
- screenshots → آرشیو شده در Telegram (مثل bundle Phase 4)
- bundle PDF تولید می‌شود شامل:
  - findings + tasks ایجاد شده
  - delta summary
  - logic audit findings
  - inventory summary
  - تمام screenshots
- Telegram message + PDF attachment
- در UI، scan session در inspector tab با badge `🔍 Scan Session` (متمایز از `🔬 Verify Session`)
**۴-D. اضافه شدن tab "Scan Sessions" در Inspector UI:**
`frontend/src/app/projects/[id]/page.tsx` (یا هر جا که inspector tab هست):
- علاوه بر لیست verify sessions، scan sessions هم نمایش داده شوند
- کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند
- archive option
### Acceptance Criteria فاز ۴
- بعد از هر scan، یک inspector session با ≥ ۱۰ پیام (AI calls + actions + probes) ساخته شود
- ≥ ۵ screenshot ذخیره شود
- Telegram message + bundle PDF با همه‌ی screenshots و findings ارسال شود
- در Inspector tab، session آرشیو شده دیده شود با badge `🔍 Scan`
- اگر runtime discovery فعال است، روی هر route یک screenshot + Vision analysis
```

python
session = create_scan_inspector_session(
    watched_id=...,
    scan_id=...,
    project_name=...,
)
```
**در طول scan:**
- همه AI calls (purpose extraction، stale detection، logic audit، etc.) → پیام در session با role="ai", content=request+response
- همه Playwright actions → پیام با role="action", screenshot=path
- همه screenshot ها → ذخیره روی disk + reference در session
- runtime probe outputs → پیام با role="probe"
**در پایان scan:**
- session archived
- screenshots → آرشیو شده در Telegram (مثل bundle Phase 4)
- bundle PDF تولید می‌شود شامل:
  - findings + tasks ایجاد شده
  - delta summary
  - logic audit findings
  - inventory summary
  - تمام screenshots
- Telegram message + PDF attachment
- در UI، scan session در inspector tab با badge `🔍 Scan Session` (متمایز از `🔬 Verify Session`)
**۴-D. اضافه شدن tab "Scan Sessions" در Inspector UI:**
`frontend/src/app/projects/[id]/page.tsx` (یا هر جا که inspector tab هست):
- علاوه بر لیست verify sessions، scan sessions هم نمایش داده شوند
- کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند
- archive option
### Acceptance Criteria فاز ۴
- بعد از هر scan، یک inspector session با ≥ ۱۰ پیام (AI calls + actions + probes) ساخته شود
- ≥ ۵ screenshot ذخیره شود
- Telegram message + bundle PDF با همه‌ی screenshots و findings ارسال شود
- در Inspector tab، session آرشیو شده دیده شود با badge `🔍 Scan`
- اگر runtime discovery فعال است، روی هر route یک screenshot + Vision analysis

--- کلیدواژه‌ها ---
backend/app/services/scan_v5/runtime_discovery.py, backend/app/services/scan_v5/outcome_analyzer.py, backend/app/services/scan_v5/scan_inspector_session.py, frontend/src/app/projects/[id]/page.tsx, navigation_helper::extract_nav_links_from_page, Playwright, backend_log_probe::_fetch_relevant_logs, vision_helper::analyze_screenshot, code_aware_verifier::_fetch_recent_commits_with_diff, create_scan_inspector_session, 🔍 Scan Session, bundle PDF
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی فاز ۴: Runtime Discovery، Outcome Analyzer و Scan Inspector Session در scan_v5

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک پیاده‌سازی کامل فاز ۴ از درخواست کاربر است که شامل ایجاد سه ماژول جدید در backend/app/services/scan_v5/ می‌شود: runtime_discovery.py, outcome_analyzer.py, scan_inspector_session.py. runtime_discovery.py باید از navigation_helper::extract_nav_links_from_page (موجود در backend/app/services/verify_runtime/navigation_helper.py)، Playwright، backend_log_probe::_fetch_relevant_logs (موجود در backend/app/services/verify_runtime/backend_log_probe.py)، vision_helper::analyze_screenshot (موجود در backend/app/services/verify_runtime/vision_helper.py)، و code_aware_verifier::_fetch_recent_commits_with_diff (موجود در backend/app/services/verify_runtime/code_aware_verifier.py) برای کشف runtime_state شامل routes_alive[], routes_404[], endpoints_called_recently[], endpoints_never_called[], ui_features_visible[], recent_commits[] استفاده کند. outcome_analyzer.py باید برای هر پروژه outcome data (trade logs, conversation outcomes, error rates, task completion rates, delivery rates) جمع‌آوری کند. scan_inspector_session.py باید برای هر scan یک inspector session باز کند (مشابه verify در Phase 4) که شامل همه AI calls, Playwright actions, screenshots, runtime probe outputs باشد و در پایان session archived + bundle PDF + Telegram message ارسال شود. همچنین باید tab 'Scan Sessions' در Inspector UI (frontend/src/app/projects/[id]/page.tsx) اضافه شود. نکته حیاتی: این فاز پوشش R14 (Inspector tab integration) را فراهم می‌کند و باید بعد از هر scan یک inspector session با ≥ ۱۰ پیام و ≥ ۵ screenshot ایجاد کند. خارج از این مرحله: پیاده‌سازی فازهای ۵ تا ۱۰.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل backend/app/services/scan_v5/runtime_discovery.py: کلاس RuntimeDiscovery با متد discover_runtime_state(watched_id, scan_id) که از navigation_helper::extract_nav_links_from_page برای استخراج لینک‌های ناوبری، Playwright برای گرفتن اسکرین‌شات و تشخیص status code (200/404) روی هر route، backend_log_probe::_fetch_relevant_logs برای یافتن endpoint‌های واقعاً فراخوانی شده در ۳۰ روز اخیر، vision_helper::analyze_screenshot برای کشف ویژگی‌های UI، و code_aware_verifier::_fetch_recent_commits_with_diff برای دریافت commit‌های اخیر استفاده کند. خروجی: dict شامل routes_alive[], routes_404[], endpoints_called_recently[], endpoints_never_called[], ui_features_visible[], recent_commits[]. 2. ایجاد فایل backend/app/services/scan_v5/outcome_analyzer.py: کلاس OutcomeAnalyzer با متد collect_outcome_data(project_id, project_type) که بر اساس نوع پروژه (Trading, AI/Chat, Web service, Scheduling, Notification) داده‌های متفاوتی جمع‌آوری می‌کند: Render logs filtered + DB tables outcome-naming + file artifacts. 3. ایجاد فایل backend/app/services/scan_v5/scan_inspector_session.py: تابع create_scan_inspector_session(watched_id, scan_id, project_name) که یک inspector session باز می‌کند. در طول scan: همه AI calls → پیام با role='ai', content=request+response; همه Playwright actions → پیام با role='action', screenshot=path; همه screenshot‌ها → ذخیره روی disk + reference در session; runtime probe outputs → پیام با role='probe'. در پایان scan: session archived, screenshots آرشیو شده در Telegram, bundle PDF شامل findings + tasks + delta summary + logic audit findings + inventory summary + تمام screenshots, Telegram message + PDF attachment. در UI، scan session در inspector tab با badge '🔍 Scan Session' (متمایز از '🔬 Verify Session'). 4. ویرایش frontend/src/app/projects/[id]/page.tsx: اضافه کردن tab 'Scan Sessions' در Inspector UI که علاوه بر لیست verify sessions، scan sessions را هم نمایش دهد. کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند. archive option.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 5: فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۵ از پرامپت کاربر است: ایجاد ماژول‌های جدید coherence_analyzer.py و anti_pattern_detector.py در backend/app/services/scan_v5/. coherence_analyzer.py باید pipeline coherence را برای ۶ نوع chain (Data pipeline, AI/LLM chain, Business logic, Auth/Permission, Feedback loop, Notification chain) با استفاده از purpose_inventory بررسی کند. anti_pattern_detector.py باید ۱۱ نوع logical anti-pattern (داده بی‌مصرف, AI بدون validation, Magic threshold, Conflicting defaults, Silent failure در crucial path, Broken feedback loop, Stale assumption, Over/under-engineering, Conditional inconsistency, Threshold-Outcome mismatch, Notification mismatch) را شناسایی کند. همچنین باید Hidden Requirements Discovery (R8) برای feature‌های فراموش شده و Outcome-based Effectiveness Audit (R11) انجام دهد. نوع task جدید 'logic_audit' با AC outcome-oriented و AI explanation کامل ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۶ تا ۱۰. نکته حیاتی: این فاز مهم‌ترین فاز معنایی است و باید روی پروژه فعلی ≥ ۳ logic issue و ≥ ۱ pipeline coherence و ≥ ۲ anti-pattern logical شناسایی کند.
**Key terms:** backend/app/services/scan_v5/coherence_analyzer.py, backend/app/services/scan_v5/anti_pattern_detector.py, Pipeline Coherence Detection, Logical Anti-pattern Detection, Hidden Requirements Discovery, Outcome-based Effectiveness Audit, logic_audit, Magic threshold, Broken feedback loop, Threshold-Outcome mismatch

**بخش مربوط از متن کاربر:**
```
## 🅴 فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)
> پوشش **R10, R11** — مهم‌ترین فاز معنایی
### مسئله (R10, R11)
> «یه پروژه دارم که هوشمند ترید کنه ولی بدتر از یه آدم معمولی ترید می‌کنه — باگ منطقی در هماهنگی بین AI و داده‌ها»
scan نباید **فقط خطاها** را ببیند، بلکه **منطق و هدف** را هم.
### تغییرات
**۵-A. Pipeline Coherence Detection:**
ماژول جدید: `backend/app/services/scan_v5/coherence_analyzer.py`
شناسایی pipelines (chains از componentهای همکار) با استفاده از `purpose_inventory[X].interacting_with`.
برای هر chain، AI بررسی coherence:
| الگو | چه بررسی شود |
|---|---|
| **Data pipeline** | schema هر مرحله، handling empty result |
| **AI/LLM chain** | prompt format ↔ model ↔ parser سازگار؟ validation؟ |
| **Business logic (R11 مثال trade)** | signal ↔ risk model، position ↔ account size، stop-loss ↔ timeframe |
| **Auth/Permission** | همه‌ی mutation از permission گذر می‌کند؟ |
| **Feedback loop** | outcome به config/model برمی‌گردد؟ |
| **Notification chain (R12)** | event → notify_event → caption → silent decision → delivery — همه consistent؟ |
**۵-B. Logical Anti-pattern Detection:**
ماژول جدید: `backend/app/services/scan_v5/anti_pattern_detector.py`
AI روی purpose_inventory + کد، این الگوها را پیدا می‌کند:
| Anti-pattern | مثال |
|---|---|
| داده بی‌مصرف | API call، DB write، ولی هیچ read |
| AI بدون validation | response parse می‌شود ولی validity چک نمی‌شود |
| Magic threshold | `> 0.65:` بدون توضیح |
| Conflicting defaults | یک field default متفاوت در جاهای مختلف |
| Silent failure در crucial path | `except: pass` در business logic |
| Broken feedback loop (R11) | outcome لاگ ولی به model نمی‌رسد |
| Stale assumption | کد فرض می‌کند رفتار سرویس X خاصه، X تغییر کرد |
| Over/under-engineering | برای ساده ۵ لایه، برای پیچیده hardcode |
| Conditional inconsistency | conditions با تغییرات اخیر inconsistent |
| Threshold-Outcome mismatch (R11) | parameters نتایج مطلوب تولید نمی‌کنند |
| Notification mismatch (R12) | event critical ولی silent=True، یا opposite |
**۵-C. Hidden Requirements Discovery (R8):**
برای هر feature که کاربر فراموش کرده «چی هست برای چیه»:
- AI روی `creation_context` (از فاز ۱) + task history + commit messages تحلیل
- استخراج: «این feature چه وقت، چرا، با چه هدفی اضافه شد؟»
- نتیجه:
  - هدف منسوخ → task `cleanup`
  - هدف معتبر، پیاده‌سازی ضعیف → task `refactor`
  - هدف معتبر، پیاده‌سازی خوب، docs نیست → task `document`
**۵-D. Outcome-based Effectiveness Audit (R11):**
اگر outcome data دارد (از فاز ۴):
- AI تحلیل می‌کند آیا outcome با `stated_purpose` می‌خواند
- مثال R11: trade — اگر purpose="earn profit" و win-rate=30% → effectiveness LOW
- اگر LOW → task `logic_audit` با priority بالا
**۵-E. Task Generation:**
نوع جدید task: `logic_audit`
- AC outcome-oriented:
  - ❌ «این کد را fix کن»
  - ✅ «بعد از این تغییر، win-rate باید ≥40% یا parameters محافظه‌کارانه‌تر شود»
- AI explanation کامل: چرا اشتباه + چه راه‌حل + چه impact
### Acceptance Criteria فاز ۵
- روی پروژه فعلی ≥ ۳ logic issue + ≥ ۱ pipeline coherence + ≥ ۲ anti-pattern logical
- روی پروژه trade فرضی (R11)، scan باگ منطقی هماهنگی AI-data شناسایی کند
- task `logic_audit` با AC outcome-oriented + AI explanation کامل
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۵ — Logical Audit: پیاده‌سازی ماژول‌های coherence_analyzer.py و anti_pattern_detector.py برای شناسایی ۶ نوع Pipeline Coherence و ۱۱ نوع Logical Anti-pattern

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی کامل فاز ۵ از پرامپت خود را دارد که شامل ایجاد دو ماژول جدید backend/app/services/scan_v5/coherence_analyzer.py و backend/app/services/scan_v5/anti_pattern_detector.py است. coherence_analyzer.py باید pipeline coherence را برای ۶ نوع chain (Data pipeline, AI/LLM chain, Business logic, Auth/Permission, Feedback loop, Notification chain) با استفاده از purpose_inventory بررسی کند. anti_pattern_detector.py باید ۱۱ نوع logical anti-pattern (داده بی‌مصرف, AI بدون validation, Magic threshold, Conflicting defaults, Silent failure در crucial path, Broken feedback loop, Stale assumption, Over/under-engineering, Conditional inconsistency, Threshold-Outcome mismatch, Notification mismatch) را شناسایی کند. همچنین باید Hidden Requirements Discovery (R8) برای feature‌های فراموش شده و Outcome-based Effectiveness Audit (R11) انجام دهد. نوع task جدید 'logic_audit' با AC outcome-oriented و AI explanation کامل ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۶ تا ۱۰. نکته حیاتی: این فاز مهم‌ترین فاز معنایی است و باید روی پروژه فعلی ≥ ۳ logic issue و ≥ ۱ pipeline coherence و ≥ ۲ anti-pattern logical شناسایی کند. در کد واقعی پروژه، فایل backend/app/services/notification_service.py (خطوط ۱۱۹-۳۳۵) شامل EVENT_REGISTRY است که برای Notification chain coherence (R12) حیاتی است — باید بررسی شود که event → notify_event → caption → silent decision → delivery همه consistent هستند. همچنین فایل backend/app/api/routes/notifications.py (خطوط ۵۱-۱۲۷) شامل endpoint‌های GET /api/notifications/status, PUT /api/notifications/prefs, POST /api/notifications/test, POST /api/notifications/notify, POST /api/notifications/telegram/webhook, POST /api/notifications/telegram/set-webhook, POST /api/notifications/telegram/delete-webhook است که برای coherence chain باید بررسی شوند. فایل frontend/src/components/NotificationSettingsPanel.tsx (خطوط ۹۳-۲۹۹) شامل UI تنظیمات نوتیفیکیشن است که باید با backend هماهنگ باشد. کلیدواژه‌های اصلی: backend/app/services/scan_v5/coherence_analyzer.py, backend/app/services/scan_v5/anti_pattern_detector.py, Pipeline Coherence Detection, Logical Anti-pattern Detection, Hidden Requirements Discovery, Outcome-based Effectiveness Audit, logic_audit, Magic threshold, Broken feedback loop, Threshold-Outcome mismatch.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد دایرکتوری backend/app/services/scan_v5/ (اگر وجود ندارد) و فایل __init__.py. ۲. پیاده‌سازی coherence_analyzer.py: تابع analyze_pipeline_coherence(purpose_inventory: dict) -> List[PipelineIssue] که ۶ نوع chain را با استفاده از purpose_inventory بررسی می‌کند. برای Notification chain (R12)، باید از EVENT_REGISTRY در notification_service.py (خطوط ۱۱۹-۳۳۵) و endpoint‌های notifications.py (خطوط ۵۱-۱۲۷) استفاده کند. ۳. پیاده‌سازی anti_pattern_detector.py: تابع detect_logical_anti_patterns(purpose_inventory: dict, code_files: dict) -> List[AntiPatternIssue] که ۱۱ نوع anti-pattern را شناسایی می‌کند. برای Magic threshold، باید کدهای backend/app/services/notification_service.py (خطوط ۴۹۸-۴۹۹) که len(text) > 4000 را بررسی می‌کند، تحلیل کند. ۴. پیاده‌سازی Hidden Requirements Discovery: تابع discover_hidden_requirements(creation_context: dict, task_history: list, commit_messages: list) -> List[HiddenRequirement]. ۵. پیاده‌سازی Outcome-based Effectiveness Audit: تابع audit_outcome_effectiveness(outcome_data: dict, stated_purpose: str) -> EffectivenessResult. ۶. ایجاد نوع task جدید logic_audit در backend/app/services/oversight_service.py با AC outcome-oriented و AI explanation کامل. ۷. اتصال ماژول‌های جدید به scan pipeline موجود

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 6: فاز ۶ — Notification System Audit (R12)

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۶ از پرامپت کاربر است: ایجاد ماژول جدید notification_auditor.py در backend/app/services/scan_v5/. این ماژول باید notification inventory (از فاز ۱ لایه ۱۱) را با فیلدهای file_path, line, event_type, caption_template, silent_default, attachments, trigger_condition, frequency_estimate کامل کند. سپس Notification Coherence Audit انجام دهد که شامل بررسی کامل بودن caption, مناسب بودن silent/sound, صحت attachments, timing, stale notifications, missing notifications, و scan-specific notifications است. برای هر notification مشکل‌دار باید task 'notification_audit' با AC مشخص ایجاد شود. همچنین باید فیلدهای جدید WatchedProject.auto_task_notify_sound و WatchedProject.scan_notify_sound اضافه شود. notification_service.py, oversight_telegram_compose.py, oversight_mega_bundle.py باید برای پشتیبانی از scan completion notification با caption جدید و bundle PDF به‌روزرسانی شوند. خارج از این مرحله: پیاده‌سازی فازهای ۷ تا ۱۰. نکته حیاتی: این فاز پوشش R6 و R12 را فراهم می‌کند و باید حداقل ۲ مورد ارتقا برای notification ها پیشنهاد دهد.
**Key terms:** backend/app/services/scan_v5/notification_auditor.py, backend/app/services/notification_service.py, backend/app/services/oversight_telegram_compose.py, backend/app/services/oversight_mega_bundle.py, WatchedProject.auto_task_notify_sound, WatchedProject.scan_notify_sound, notify_event, notification_audit, scan_completed, silent default

**بخش مربوط از متن کاربر:**
```
## 🅵 فاز ۶ — Notification System Audit (R12)
> پوشش **R6, R12** — صریحاً audit notification ها
### مسئله (R12)
> «در پرامپت برای پیام‌رسانی تلگرام هم جایی در نظر گرفتی؟ در بررسی‌ها نوع اطلاع‌رسانیش هم بررسی بشه و بهینه بشه»
### تغییرات
**۶-A. Notification Inventory (از فاز ۱ — لایه ۱۱):**
برای هر `notify_event(...)` call در کد:
```python
notification_inventory[call_id] = {
    "file_path": "...",
    "line": ...,
    "event_type": "...",          # "task_created" | "verify_done" | ...
    "caption_template": "...",
    "silent_default": True | False | None,
    "attachments": [...],
    "trigger_condition": "...",   # کد منطق در کجا fire می‌شود
    "frequency_estimate": "...",  # احتمال در روز
}
```
**۶-B. Notification Coherence Audit (R12):**
AI روی notification_inventory:
| سوال | چه چیزی audit شود |
|---|---|
| caption کامل است؟ | همه‌ی فیلدهای مهم در caption هستند؟ (title, intent, link, attachments) |
| silent/sound مناسب است؟ | event critical → sound. event routine → silent |
| attachments صحیح؟ | task creation → prompt.md، verify done → bundle.pdf، scan done → scan-bundle.pdf |
| timing مناسب؟ | spam جلوگیری شده؟ batching هست؟ |
| stale notifications؟ | event type که دیگر کد آن وجود ندارد |
| missing notifications؟ | event critical که notification ندارد |
| **scan-specific (R12)**: | scan completion notification با همه‌ی مدارک ضروری: findings, tasks created, delta, logic audit results, inspector session reference |
**۶-C. Notification Templates Suggestions:**
برای هر notification که audit آن مشکل دارد → task `notification_audit`:
مثال task:
```yaml
title: "ارتقای caption notification scan_completed"
type: notification_audit
priority: medium
raw_idea: |
  notification scan_completed فعلاً فقط count tasks ایجاد شده را می‌دهد.
  باید شامل: delta summary + logic audit findings + inspector session link
  + attachment با PDF کامل (مشابه verify bundle).
behavior_observable: |
  بعد از این تغییر، notification scan_completed باید شامل:
  - title پروژه
  - تعداد tasks جدید
  - delta count (add/remove/modify)
  - logic audit count
  - link به scan inspector session
  - PDF attachment با همه screenshots و findings
acceptance_signal: |
  بعد از یک scan تست، Telegram message + PDF received با همه فیلدها
```
**۶-D. Audit existing notification flows:**
- `notification_service.py` — همه `notify_event` ها
- `oversight_telegram_compose.py` — caption builders
- `oversight_mega_bundle.py` — bundle PDFs
- بررسی silent default برای auto-tasks (R6)
### Acceptance Criteria فاز ۶
- audit notification ها: ≥ ۲ مورد ارتقا قابل پیشنهاد
- task `notification_audit` ایجاد شود برای هر مورد
- caption template جدید برای scan_completed تعریف شود
- silent default برای auto-task و scan notifications به `True` تنظیم شود
- field `WatchedProject.auto_task_notify_sound: bool = False`
- field `WatchedProject.scan_notify_sound: bool = False`
```

python
notification_inventory[call_id] = {
    "file_path": "...",
    "line": ...,
    "event_type": "...",          # "task_created" | "verify_done" | ...
    "caption_template": "...",
    "silent_default": True | False | None,
    "attachments": [...],
    "trigger_condition": "...",   # کد منطق در کجا fire می‌شود
    "frequency_estimate": "...",  # احتمال در روز
}
```
**۶-B. Notification Coherence Audit (R12):**
AI روی notification_inventory:
| سوال | چه چیزی audit شود |
|---|---|
| caption کامل است؟ | همه‌ی فیلدهای مهم در caption هستند؟ (title, intent, link, attachments) |
| silent/sound مناسب است؟ | event critical → sound. event routine → silent |
| attachments صحیح؟ | task creation → prompt.md، verify done → bundle.pdf، scan done → scan-bundle.pdf |
| timing مناسب؟ | spam جلوگیری شده؟ batching هست؟ |
| stale notifications؟ | event type که دیگر کد آن وجود ندارد |
| missing notifications؟ | event critical که notification ندارد |
| **scan-specific (R12)**: | scan completion notification با همه‌ی مدارک ضروری: findings, tasks created, delta, logic audit results, inspector session reference |
**۶-C. Notification Templates Suggestions:**
برای هر notification که audit آن مشکل دارد → task `notification_audit`:
مثال task:
```yaml
title: "ارتقای caption notification scan_completed"
type: notification_audit
priority: medium
raw_idea: |
  notification scan_completed فعلاً فقط count tasks ایجاد شده را می‌دهد.
  باید شامل: delta summary + logic audit findings + inspector session link
  + attachment با PDF کامل (مشابه verify bundle).
behavior_observable: |
  بعد از این تغییر، notification scan_completed باید شامل:
  - title پروژه
  - تعداد tasks جدید
  - delta count (add/remove/modify)
  - logic audit count
  - link به scan inspector session
  - PDF attachment با همه screenshots و findings
acceptance_signal: |
  بعد از یک scan تست، Telegram message + PDF received با همه فیلدها
```
**۶-D. Audit existing notification flows:**
- `notification_service.py` — همه `notify_event` ها
- `oversight_telegram_compose.py` — caption builders
- `oversight_mega_bundle.py` — bundle PDFs
- بررسی silent default برای auto-tasks (R6)
### Acceptance Criteria فاز ۶
- audit notification ها: ≥ ۲ مورد ارتقا قابل پیشنهاد
- task `notification_audit` ایجاد شود برای هر مورد
- caption template جدید برای scan_completed تعریف شود
- silent default برای auto-task و scan notifications به `True` تنظیم شود
- field `WatchedProject.auto_task_notify_sound: bool = False`
- field `WatchedProject.scan_notify_sound: bool = False`

--- کلیدواژه‌ها ---
backend/app/services/scan_v5/notification_auditor.py, backend/app/services/notification_service.py, backend/app/services/oversight_telegram_compose.py, backend/app/services/oversight_mega_bundle.py, WatchedProject.auto_task_notify_sound, WatchedProject.scan_notify_sound, notify_event, notification_audit, scan_completed, silent default
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۶ — پیاده‌سازی Notification System Audit (R12) با ماژول notification_auditor.py و به‌روزرسانی notification_service.py, oversight_telegram_compose.py, oversight_mega_bundle.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک پیاده‌سازی کامل فاز ۶ از درخواست کاربر است: ایجاد ماژول جدید `backend/app/services/scan_v5/notification_auditor.py` که notification inventory (از فاز ۱ لایه ۱۱) را با فیلدهای `file_path`, `line`, `event_type`, `caption_template`, `silent_default`, `attachments`, `trigger_condition`, `frequency_estimate` کامل کند. سپس Notification Coherence Audit انجام دهد که شامل بررسی کامل بودن caption, مناسب بودن silent/sound, صحت attachments, timing, stale notifications, missing notifications, و scan-specific notifications است. برای هر notification مشکل‌دار باید task 'notification_audit' با AC مشخص ایجاد شود. همچنین باید فیلدهای جدید `WatchedProject.auto_task_notify_sound` و `WatchedProject.scan_notify_sound` اضافه شود. `notification_service.py`, `oversight_telegram_compose.py`, `oversight_mega_bundle.py` باید برای پشتیبانی از scan completion notification با caption جدید و bundle PDF به‌روزرسانی شوند. خارج از این مرحله: پیاده‌سازی فازهای ۷ تا ۱۰. نکته حیاتی: این فاز پوشش R6 و R12 را فراهم می‌کند و باید حداقل ۲ مورد ارتقا برای notification ها پیشنهاد دهد. شواهد در کد واقعی: در `backend/app/services/notification_service.py` خطوط 119-335 (EVENT_REGISTRY) رویدادهای `scan_started`, `scan_done`, `scan_critical_found`, `scan_failed` تعریف شده‌اند اما caption template و attachments برای scan_completed وجود ندارد. در `backend/app/services/oversight_mega_bundle.py` خطوط 60-480 تابع `build_mega_bundle_md` فقط برای verify bundle طراحی شده و برای scan bundle نیاز به توسعه دارد. در `backend/app/services/oversight_telegram_compose.py` (deep-read شده) caption builders برای scan events وجود ندارد. فیلدهای `WatchedProject.auto_task_notify_sound` و `WatchedProject.scan_notify_sound` در مدل‌های پروژه (احتمالاً `backend/app/models/project.py`) باید اضافه شوند. کلیدواژه‌های کاربر: `backend/app/services/scan_v5/notification_auditor.py`, `backend/app/services/notification_service.py`, `backend/app/services/oversight_telegram_compose.py`, `backend/app/services/oversight_mega_bundle.py`, `WatchedProject.auto_task_notify_sound`, `WatchedProject.scan_notify_sound`, `notify_event`, `notification_audit`, `scan_completed`, `silent default`.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `backend/app/services/scan_v5/notification_auditor.py` با کلاس `NotificationAuditor` که متد `build_inventory()` برای اسکن تمام `notify_event(...)` calls در کدبیس (با جستجوی grep روی `notify_event` در `backend/app/services/`) و متد `audit_coherence()` برای بررسی coherence بر اساس معیارهای فاز ۶-B (caption کامل، silent/sound مناسب، attachments صحیح، timing، stale notifications، missing notifications، scan-specific notifications) دارد. 2. برای هر notification مشکل‌دار، متد `create_audit_task()` یک task از نوع `notification_audit` با AC مشخص ایجاد کند. 3. اضافه کردن فیلدهای `auto_task_notify_sound: bool = False` و `scan_notify_sound: bool = False` به مدل `WatchedProject` در `backend/app/models/project.py`. 4. به‌روزرسانی `backend/app/services/notification_service.py`: افزودن event `scan_completed` به EVENT_REGISTRY (خطوط 119-335) با caption template جدید شامل `delta_summary`, `logic_audit_findings`, `inspector_session_link`. 5. به‌روزرسانی `backend/app/services/oversight_telegram_compose.py`: افزودن تابع `compose_scan_completed_caption()` که caption کامل با فیلدهای title پروژه، تعداد tasks جدید، delta count، logic audit count، link به scan inspector

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 7: فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۷ از پرامپت کاربر است: به‌روزرسانی oversight_strong_prompt.py و oversight_service.py برای پشتیبانی از ساختار غنی AC و task_step. در oversight_strong_prompt.py باید section جدید '🎯 معیار رفتاری + business intent + alternative implementations' اضافه شود. در oversight_service.py:idea_to_prompt (~3454) و oversight_service.py:task creation from scan (~4700-4740) و oversight_service.py:_ai_plan_steps_from_idea (~2673) باید ساختار AC به فیلدهای text, behavior, acceptance_signal, business_intent, alternative_implementations, non_goals, false_positive_guard ارتقا یابد. همچنین باید Smart Checklist Mode با فیلد WatchedProject.auto_task_checklist_mode (Literal["auto","always","never"]) پیاده‌سازی شود که برای هر دو مسیر manual و auto کار کند. دستورالعمل جلوگیری از false-positive (R9) باید به AI داده شود. خارج از این مرحله: پیاده‌سازی فازهای ۸ تا ۱۰. نکته حیاتی: این فاز پوشش R5, R9, R13 را فراهم می‌کند و باید پرامپت‌های جدید برای ۳ ایده نمونه شامل behavior + acceptance_signal + business_intent + alternative_implementations + false_positive_guard تولید کند.
**Key terms:** backend/app/services/oversight_strong_prompt.py, backend/app/services/oversight_service.py:3454, backend/app/services/oversight_service.py:4700-4740, backend/app/services/oversight_service.py:2673, WatchedProject.auto_task_checklist_mode, Smart Checklist Mode, false_positive_guard, behavior_observable, acceptance_signal, business_intent, alternative_implementations

**بخش مربوط از متن کاربر:**
```
## 🅶 فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)
> پوشش **R5, R9, R13** — برای **manual + auto** هر دو
### مسئله (R9 صریح)
> «در تولید ایده خام به پرامپت، پرامپت در برخی قسمت‌ها دقیق و واضح تولید نمیشه... باید هوشمندتر بشه خود متن پرامپت و چک‌لیستی که ازش تولید میشه»
### مسئله (R13 صریح)
> «این بهبود پرامپت‌نویسی تسک‌ها آیا فقط برای تسک‌های سیستمی یا تسک‌های دستی هم شامل میشه؟»
**جواب: هر دو.**
### تغییرات
**۷-A. ساختار غنی AC (هم manual، هم auto):**
برای **manual** path در `oversight_service.py:idea_to_prompt` (~3454):
برای **auto** path در `oversight_service.py:task creation from scan` (~4700-4740):
برای **scan-generated** path در `oversight_strong_prompt.py:build_strong_prompt`:
هر AC ساختار غنی:
```python
{
    "text": "...",                          # توضیح اصلی (می‌تواند نام بدهد)
    "behavior": "...",                      # رفتار قابل مشاهده — چه چیزی observable است
    "acceptance_signal": "...",             # سؤال قابل-verify
    "business_intent": "...",               # چرا این لازم است
    "alternative_implementations": [...],   # نام‌های جایگزین قابل قبول (R9 — جلوگیری از نام-محوری)
    "non_goals": "...",                     # چه چیزی این AC نیست
    "false_positive_guard": "...",          # چه شواهد ضعیفی نشانه done نیست (R9)
}
```
**۷-B. ساختار غنی task_step (در `_ai_plan_steps_from_idea`):**
برای manual و auto (R13):
```python
{
    "title": "...",
    "scope": "...",
    "behavior_observable": "...",       # خروجی observable
    "verification_hint": "...",         # کجا verify بیابد
    "business_intent": "...",           # چرا این مرحله
    "non_goals": "...",
}
```
**۷-C. Smart Checklist Mode (R5):**
> «حالت هوشمند که برای تولید چک‌لیست یا عدم تولیدش حسب نیاز هست»
`WatchedProject.auto_task_checklist_mode: Literal["auto","always","never"] = "auto"`
- `auto`: AI تصمیم می‌گیرد بر اساس پیچیدگی (≥3 stage → checklist، single-action → skip)
- `always`: همیشه چک‌لیست
- `never`: هرگز
این برای **هم manual و هم auto** کار می‌کند.
**۷-D. Update prompt templates:**
در `oversight_strong_prompt.py`:
- اضافه شدن section "🎯 معیار رفتاری + business intent + alternative implementations"
- AI صریح راهنمایی شود: AC نام-محور بد است، AC رفتار-محور خوب
**۷-E. جلوگیری از false-positive (R9):**
> «جوری هم نشه که سیستم کاری که بد انجام شده... در verify تیک بزنه که کامل شده»
دستورالعمل به AI:
- اگر AC vague → split به sub-behaviors concrete
- هرگز AC بدون `acceptance_signal` نسازد
- `false_positive_guard` فیلد: چه شواهد ضعیفی نباید سبب done شود
- مثال: «اگر فقط فایل با نام وجود دارد، done نیست — باید رفتار X هم باشد»
**۷-F. Integration با فازهای قبلی:**
پرامپت تولید‌شده استفاده کند از:
- `purpose_inventory` (فاز ۱)
- `runtime_state` (فاز ۴)
- `logic_audit_findings` (فاز ۵)
- `notification_audit` (فاز ۶)
### Acceptance Criteria فاز ۷
- **manual** path: پرامپت‌های جدید برای ۳ ایده‌ی نمونه شامل `behavior` + `acceptance_signal` + `business_intent` + `alternative_implementations` + `false_positive_guard`
- **auto** path: همان ساختار برای task های scan-generated
- چک‌لیست‌ها شامل `behavior_observable` در هر step
- mode `auto` در `_ai_plan_steps_from_idea` کار می‌کند برای هر دو
- verify روی same tasks: false-negative ≤ ۲۰%، false-positive ≤ ۵%
- backward compat: AC های قدیمی (فقط text) همچنان کار می‌کنند
```

python
{
    "text": "...",                          # توضیح اصلی (می‌تواند نام بدهد)
    "behavior": "...",                      # رفتار قابل مشاهده — چه چیزی observable است
    "acceptance_signal": "...",             # سؤال قابل-verify
    "business_intent": "...",               # چرا این لازم است
    "alternative_implementations": [...],   # نام‌های جایگزین قابل قبول (R9 — جلوگیری از نام-محوری)
    "non_goals": "...",                     # چه چیزی این AC نیست
    "false_positive_guard": "...",          # چه شواهد ضعیفی نشانه done نیست (R9)
}
```
**۷-B. ساختار غنی task_step (در `_ai_plan_steps_from_idea`):**
برای manual و auto (R13):
```python
{
    "title": "...",
    "scope": "...",
    "behavior_observable": "...",       # خروجی observable
    "verification_hint": "...",         # کجا verify بیابد
    "business_intent": "...",           # چرا این مرحله
    "non_goals": "...",
}
```
**۷-C. Smart Checklist Mode (R5):**
> «حالت هوشمند که برای تولید چک‌لیست یا عدم تولیدش حسب نیاز هست»
`WatchedProject.auto_task_checklist_mode: Literal["auto","always","never"] = "auto"`
- `auto`: AI تصمیم می‌گیرد بر اساس پیچیدگی (≥3 stage → checklist، single-action → skip)
- `always`: همیشه چک‌لیست
- `never`: هرگز
این برای **هم manual و هم auto** کار می‌کند.
**۷-D. Update prompt templates:**
در `oversight_strong_prompt.py`:
- اضافه شدن section "🎯 معیار رفتاری + business intent + alternative implementations"
- AI صریح راهنمایی شود: AC نام-محور بد است، AC رفتار-محور خوب
**۷-E. جلوگیری از false-positive (R9):**
> «جوری هم نشه که سیستم کاری که بد انجام شده... در verify تیک بزنه که کامل شده»
دستورالعمل به AI:
- اگر AC vague → split به sub-behaviors concrete
- هرگز AC بدون `acceptance_signal` نسازد
- `false_positive_guard` فیلد: چه شواهد ضعیفی نباید سبب done شود
- مثال: «اگر فقط فایل با نام وجود دارد، done نیست — باید رفتار X هم باشد»
**۷-F. Integration با فازهای قبلی:**
پرامپت تولید‌شده استفاده کند از:
- `purpose_inventory` (فاز ۱)
- `runtime_state` (فاز ۴)
- `logic_audit_findings` (فاز ۵)
- `notification_audit` (فاز ۶)
### Acceptance Criteria فاز ۷
- **manual** path: پرامپت‌های جدید برای ۳ ایده‌ی نمونه شامل `behavior` + `acceptance_signal` + `business_intent` + `alternative_implementations` + `false_positive_guard`
- **auto** path: همان ساختار برای task های scan-generated
- چک‌لیست‌ها شامل `behavior_observable` در هر step
- mode `auto` در `_ai_plan_steps_from_idea` کار می‌کند برای هر دو
- verify روی same tasks: false-negative ≤ ۲۰%، false-positive ≤ ۵%
- backward compat: AC های قدیمی (فقط text) همچنان کار می‌کنند

--- کلیدواژه‌ها ---
backend/app/services/oversight_strong_prompt.py, backend/app/services/oversight_service.py:3454, backend/app/services/oversight_service.py:4700-4740, backend/app/services/oversight_service.py:2673, WatchedProject.auto_task_checklist_mode, Smart Checklist Mode, false_positive_guard, behavior_observable, acceptance_signal, business_intent, alternative_implementations
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۷ — Smart Prompt + Smart Checklist: ارتقای ساختار AC و task_step در oversight_strong_prompt.py و oversight_service.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک پیاده‌سازی کامل فاز ۷ از درخواست کاربر است که شامل به‌روزرسانی دو فایل کلیدی backend/app/services/oversight_strong_prompt.py و backend/app/services/oversight_service.py برای پشتیبانی از ساختار غنی AC و task_step می‌باشد. کاربر خواسته است:

1. در oversight_strong_prompt.py یک section جدید با عنوان '🎯 معیار رفتاری + business intent + alternative implementations' اضافه شود.
2. در oversight_service.py در سه نقطه: idea_to_prompt (~3454)، task creation from scan (~4700-4740) و _ai_plan_steps_from_idea (~2673) ساختار AC از حالت ساده (فقط text) به فیلدهای زیر ارتقا یابد:
   - text: توضیح اصلی
   - behavior: رفتار قابل مشاهده
   - acceptance_signal: سؤال قابل verify
   - business_intent: چرا این لازم است
   - alternative_implementations: نام‌های جایگزین قابل قبول (R9)
   - non_goals: چه چیزی این AC نیست
   - false_positive_guard: چه شواهد ضعیفی نشانه done نیست (R9)
3. Smart Checklist Mode با فیلد جدید WatchedProject.auto_task_checklist_mode از نوع Literal["auto","always","never"] با مقدار پیش‌فرض "auto" پیاده‌سازی شود که برای هر دو مسیر manual و auto کار کند.
4. در _ai_plan_steps_from_idea ساختار task_step به فیلدهای title, scope, behavior_observable, verification_hint, business_intent, non_goals ارتقا یابد.
5. دستورالعمل جلوگیری از false-positive (R9) به AI داده شود.
6. پرامپت‌های جدید برای ۳ ایده نمونه شامل behavior + acceptance_signal + business_intent + alternative_implementations + false_positive_guard تولید کند.

کلیدواژه‌های دقیق از متن کاربر: backend/app/services/oversight_strong_prompt.py, backend/app/services/oversight_service.py:3454, backend/app/services/oversight_service.py:4700-4740, backend/app/services/oversight_service.py:2673, WatchedProject.auto_task_checklist_mode, Smart Checklist Mode, false_positive_guard, behavior_observable, acceptance_signal, business_intent, alternative_implementations.

شواهد در کد واقعی: در فایل backend/app/services/oversight_strong_prompt.py خطوط 322-337 بخش acceptance_criteria فعلی فقط از فیلد text پشتیبانی می‌کند (خط 325: c_text = (c.get("text") if isinstance(c, dict) else c) or ""). در backend/app/services/oversight_service.py خطوط 260-380 dataclass OversightTask دارای فیلد acceptance_criteria: List[Any] (خط 294) و task_steps: List[Dict[str, Any]] (خط 350) است که باید ارتقا یابند. همچنین WatchedProject (خطوط 132-256) فاقد فیلد auto_task_checklist_mode است.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. مراحل پیاده‌سازی:
1. در backend/app/services/oversight_strong_prompt.py:
   - بعد از خط 337 (بخش acceptance_criteria) یک section جدید با عنوان '🎯 معیار رفتاری + business intent + alternative implementations' اضافه کن.
   - در تابع build_strong_prompt (خط 158) پارامتر جدیدی برای دریافت AC غنی اضافه کن.
   - در حلقه‌ی تولید AC (خطوط 324-328) به جای فقط text، فیلدهای behavior, acceptance_signal, business_intent, alternative_implementations, non_goals, false_positive_guard را نیز نمایش بده.
   - یک راهنمایی صریح به AI اضافه کن که AC نام-محور بد است و AC رفتار-محور خوب.
2. در backend/app/services/oversight_service.py:
   - در WatchedProject (خطوط 132-256) فیلد auto_task_checklist_mode: str = "auto" با Literal["auto","always","never"] اضافه کن

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 8: فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۸ از پرامپت کاربر است: به‌روزرسانی oversight_service.py در بخش auto-task creation (~4700-4740) برای اضافه کردن task_steps به auto-tasks با استفاده از _ai_plan_steps_from_idea و رعایت auto_task_checklist_mode. همچنین باید Telegram notification template جدید برای auto-scan task ها در notification_service.py پیاده‌سازی شود که شامل caption با فیلدهای title, business_intent, checklist count, logic concerns, dependent changes, outcome impact, links باشد. scan-completion notification نیز باید با خلاصه کامل و bundle PDF و inspector session link ارسال شود. فیلدهای WatchedProject.auto_task_notify_sound و WatchedProject.scan_notify_sound باید در UI با checkbox '🔔 صدای notification برای تسک‌های auto-scan' قابل تنظیم باشند. oversight_mega_bundle.py باید برای تولید scan-bundle PDF به‌روزرسانی شود. خارج از این مرحله: پیاده‌سازی فازهای ۹ و ۱۰. نکته حیاتی: این فاز پوشش R5 و R6 را کامل می‌کند و باید بعد از scan همه auto-tasks در UI چک‌لیست داشته باشند و caption تلگرام شامل همه فیلدها باشد.
**Key terms:** backend/app/services/oversight_service.py:4700-4740, backend/app/services/notification_service.py, backend/app/services/oversight_mega_bundle.py, WatchedProject.auto_task_notify_sound, WatchedProject.scan_notify_sound, auto_task_checklist_mode, _ai_plan_steps_from_idea, scan_completed, Bundle PDF, Inspector session

**بخش مربوط از متن کاربر:**
```
## 🅷 فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration
> پوشش **R5, R6** — تسک‌های scan باید مثل manual باشند
### مسئله (R5, R6)
- تسک‌های auto-scan الان `prompt + AC` دارند ولی `task_steps` ندارند
- Telegram notification برای آن‌ها audit نشده
- کاربر می‌خواهد silent default
### تغییرات
**۸-A. task_steps برای auto-tasks:**
در `oversight_service.py:4700-4740`:
- بعد از `build_strong_prompt`، اگر `auto_task_checklist_mode != "never"`:
  - if `mode == "always"` → همیشه call `_ai_plan_steps_from_idea`
  - if `mode == "auto"` → AI تصمیم می‌گیرد بر اساس پیچیدگی
**۸-B. Telegram Notification Template (R6):**
`notification_service.py`:
```python
# Caption template برای auto-scan task
{
    "🤖 تسک جدید از scan خودکار",
    "📌 {title}",
    "🎯 {business_intent[:200]}",
    "📋 چک‌لیست: {N step}" if has_steps else None,
    "🧠 Logic concerns: {logic_audit_count}" if has_logic_issues else None,
    "🔄 وابسته به تغییر: {dependent_changes}" if delta_dependent else None,
    "📊 Outcome impact: {outcome_score}" if outcome_data else None,
    "🔗 [لینک کارت]",
    "🔍 [Inspector session]",
}
# Attachments:
- prompt_full.md (full task prompt)
- scan_inspector_session_link
```
**۸-C. Silent default + UI control:**
`WatchedProject` fields:
```python
auto_task_notify_sound: bool = False     # silent for auto-tasks
scan_notify_sound: bool = False           # silent for scan completion
```
در UI: checkbox `🔔 صدای notification برای تسک‌های auto-scan`.
**۸-D. Scan-completion notification:**
علاوه بر notification هر task، یک scan-completion notification:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
### Acceptance Criteria فاز ۸
- بعد از scan: همه auto-tasks در UI چک‌لیست دارند (اگر mode=auto و پیچیده‌اند)
- caption تلگرام شامل همه فیلدها
- silent default = true
- scan_completed notification با Bundle PDF + Inspector session link
- regression: تسک‌های دستی رفتار قبلی را حفظ کنند
```

python
# Caption template برای auto-scan task
{
    "🤖 تسک جدید از scan خودکار",
    "📌 {title}",
    "🎯 {business_intent[:200]}",
    "📋 چک‌لیست: {N step}" if has_steps else None,
    "🧠 Logic concerns: {logic_audit_count}" if has_logic_issues else None,
    "🔄 وابسته به تغییر: {dependent_changes}" if delta_dependent else None,
    "📊 Outcome impact: {outcome_score}" if outcome_data else None,
    "🔗 [لینک کارت]",
    "🔍 [Inspector session]",
}
# Attachments:
- prompt_full.md (full task prompt)
- scan_inspector_session_link
```
**۸-C. Silent default + UI control:**
`WatchedProject` fields:
```python
auto_task_notify_sound: bool = False     # silent for auto-tasks
scan_notify_sound: bool = False           # silent for scan completion
```
در UI: checkbox `🔔 صدای notification برای تسک‌های auto-scan`.
**۸-D. Scan-completion notification:**
علاوه بر notification هر task، یک scan-completion notification:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
### Acceptance Criteria فاز ۸
- بعد از scan: همه auto-tasks در UI چک‌لیست دارند (اگر mode=auto و پیچیده‌اند)
- caption تلگرام شامل همه فیلدها
- silent default = true
- scan_completed notification با Bundle PDF + Inspector session link
- regression: تسک‌های دستی رفتار قبلی را حفظ کنند

--- کلیدواژه‌ها ---
backend/app/services/oversight_service.py:4700-4740, backend/app/services/notification_service.py, backend/app/services/oversight_mega_bundle.py, WatchedProject.auto_task_notify_sound, WatchedProject.scan_notify_sound, auto_task_checklist_mode, _ai_plan_steps_from_idea, scan_completed, Bundle PDF, Inspector session
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration (اضافه کردن task_steps به auto-tasks، Telegram template جدید، UI control برای sound، scan-completion notification)

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک پیاده‌سازی کامل فاز ۸ از درخواست کاربر است که شامل چهار بخش اصلی می‌شود:

**۸-A. task_steps برای auto-tasks:** در فایل `backend/app/services/oversight_service.py` در محدوده خطوط 4700-4740 (بر اساس اشاره کاربر)، بعد از فراخوانی `build_strong_prompt`، اگر `auto_task_checklist_mode != "never"` باشد، باید منطق زیر اضافه شود:
- اگر `mode == "always"` → همیشه تابع `_ai_plan_steps_from_idea` فراخوانی شود تا task_steps برای auto-task تولید کند.
- اگر `mode == "auto"` → AI خودش بر اساس پیچیدگی ایده تصمیم بگیرد که آیا نیاز به task_steps هست یا خیر.
این کار باعث می‌شود auto-tasks نیز مانند manual tasks دارای task_steps باشند و در UI چک‌لیست داشته باشند.

**۸-B. Telegram Notification Template جدید (R6):** در فایل `backend/app/services/notification_service.py` باید یک caption template جدید برای auto-scan task ها پیاده‌سازی شود که شامل فیلدهای زیر باشد:
- `🤖 تسک جدید از scan خودکار`
- `📌 {title}`
- `🎯 {business_intent[:200]}`
- `📋 چک‌لیست: {N step}` (اگر has_steps)
- `🧠 Logic concerns: {logic_audit_count}` (اگر has_logic_issues)
- `🔄 وابسته به تغییر: {dependent_changes}` (اگر delta_dependent)
- `📊 Outcome impact: {outcome_score}` (اگر outcome_data)
- `🔗 [لینک کارت]`
- `🔍 [Inspector session]`
همچنین attachments شامل `prompt_full.md` و `scan_inspector_session_link`.

**۸-C. Silent default + UI control:** دو فیلد جدید به مدل `WatchedProject` اضافه شود:
- `auto_task_notify_sound: bool = False` (silent برای auto-tasks)
- `scan_notify_sound: bool = False` (silent برای scan completion)
در UI (احتمالاً در کامپوننت مربوط به تنظیمات WatchedProject) یک checkbox با عنوان `🔔 صدای notification برای تسک‌های auto-scan` اضافه شود که این دو فیلد را کنترل کند.

**۸-D. Scan-completion notification:** علاوه بر notification هر task، یک notification مجزا برای اتمام scan ارسال شود با قالب:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
برای تولید Bundle PDF باید `backend/app/services/oversight_mega_bundle.py` به‌روزرسانی شود تا بتواند scan-bundle PDF تولید کند.

**نکته حیاتی:** این فاز پوشش R5 و R6 را کامل می‌کند و باید بعد از scan همه auto-tasks در UI چک‌لیست داشته باشند و caption تلگرام شامل همه فیلدها باشد. خارج از این مرحله: پیاده‌سازی فازهای ۹ و ۱۰.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **oversight_service.py (خطوط 4700-4740):** بعد از `build_strong_prompt` در تابع ایجاد auto-task، شرط `if auto_task_checklist_mode != "never"` را اضافه کن. اگر `mode == "always"`، `_ai_plan_steps_from_idea` را صدا بزن. اگر `mode == "auto"`، از AI بخواه بر اساس پیچیدگی تصمیم بگیرد. نتیجه را به عنوان `task_steps` به task اضافه کن.
2. **notification_service.py:** یک تابع جدید `build_auto_task_caption` ایجاد کن که دیکشنری بالا را به یک متن Markdown تبدیل کند. همچنین یک تابع `send

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 9: فاز ۹ — UI Redesign (R2, R8)

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۹ از پرامپت کاربر است: بازطراحی کامل UI در frontend/src/app/oversight/page.tsx. scan_depth modes باید به ۴ حالت جدید (quick 5 pass, balanced 8 pass پیش‌فرض جدید, deep 12 pass, ultra 12 pass) تغییر یابد. criteria_weights باید به preset selector با گزینه‌های general, security-first, tests-first, feature-completeness, logic-quality تغییر کند و sliders در <details> (advanced) قرار گیرند. layout باید به ۴ tab (Coverage, Intelligence, Lifecycle, Notifications) بازطراحی شود. ۱۰ فیلد جدید به WatchedProject اضافه شود (inventory_layers, stale_detection_enabled, delta_analysis_enabled, runtime_discovery_enabled, outcome_data_enabled, logic_audit_enabled, notification_audit_enabled, inspector_session_enabled, auto_task_checklist_mode, cleanup_tasks_enabled). هر گزینه باید help icon ⓘ با AI-generated description (از فاز ۲) داشته باشد. Feature Inventory Panel (از فاز ۲) نیز باید در UI نمایش داده شود. خارج از این مرحله: پیاده‌سازی فاز ۱۰. نکته حیاتی: این فاز پوشش R2 را فراهم می‌کند و باید backward compatibility با scan_depth قدیمی حفظ شود.
**Key terms:** frontend/src/app/oversight/page.tsx, WatchedProject.inventory_layers, WatchedProject.stale_detection_enabled, WatchedProject.delta_analysis_enabled, WatchedProject.runtime_discovery_enabled, WatchedProject.outcome_data_enabled, WatchedProject.logic_audit_enabled, WatchedProject.notification_audit_enabled, WatchedProject.inspector_session_enabled, WatchedProject.auto_task_checklist_mode, WatchedProject.cleanup_tasks_enabled, scan_depth modes, criteria_weights, Feature Inventory Panel

**بخش مربوط از متن کاربر:**
```
## 🅸 فاز ۹ — UI Redesign (R2, R8)
> پوشش **R2** — به‌روزرسانی گزینه‌ها
### مسئله (R2)
> «گزینه‌هاش هم به روز رسانی بشن»
### تغییرات
**۹-A. بازنگری scan_depth modes:**
از (فعلی):
```
quick (3 pass) | standard (6 pass) | deep (12 pass، پیش‌فرض) | thorough (12 + health + roadmap)
```
به:
```
⚡ quick (5 pass) — frontend + backend + security + delta + stale
⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
🔬 deep (12 pass) — همه + impact analysis
🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)
```
**۹-B. بازنگری criteria_weights:**
به‌جای ۶ slider:
- preset selector: "general", "security-first", "tests-first", "feature-completeness", "logic-quality"
- sliders در `<details>` (advanced)
**۹-C. بازنگری Smart Task Lifecycle:**
- بررسی هر گزینه: کار می‌کند؟
- label با وضعیت فعلی
**۹-D. گزینه‌های جدید Phase 5:**
`WatchedProject` فیلدهای جدید (همه با default sensible):
```python
# Coverage
inventory_layers: List[str] = ["all"]    # یا subset
# Intelligence
stale_detection_enabled: bool = True
delta_analysis_enabled: bool = True
runtime_discovery_enabled: bool = True
outcome_data_enabled: bool = True
logic_audit_enabled: bool = True
notification_audit_enabled: bool = True
inspector_session_enabled: bool = True   # R14
# Lifecycle
auto_task_checklist_mode: str = "auto"   # R5
cleanup_tasks_enabled: bool = True
# Notifications
auto_task_notify_sound: bool = False     # R6
scan_notify_sound: bool = False          # R6
```
**۹-E. بازطراحی Layout — ۴ tabs (به‌جای ۳):**
| Tab | محتوا |
|---|---|
| **🔍 Coverage** | scan_depth + inventory_layers + criteria preset + auto_models |
| **🧠 Intelligence** | stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session |
| **🔁 Lifecycle** | dedup + auto-regenerate + checklist mode + cleanup |
| **🔔 Notifications** | auto_task_sound + scan_sound + notification_audit + custom templates |
**۹-F. AI explanation برای هر گزینه (R8):**
هر گزینه دارای help icon ⓘ + AI-generated description (از فاز ۲):
- این گزینه دقیقاً چه می‌کند
- کِی اضافه شد
- وضعیت فعلی (active / possibly stale / unknown)
**۹-G. Feature Inventory Panel (R8):**
panel جدید در UI با لیست همه options + AI explanation (از فاز ۲).
### Acceptance Criteria فاز ۹
- در UI، ۴ tab وجود دارد
- mode `balanced` پیش‌فرض جدید
- mode `ultra` در دسترس
- backward compat: scan_depth قدیمی همچنان کار کند
- هر گزینه دارای tooltip + AI explanation
- Feature Inventory Panel functional
```

quick (3 pass) | standard (6 pass) | deep (12 pass، پیش‌فرض) | thorough (12 + health + roadmap)
```
به:
```
⚡ quick (5 pass) — frontend + backend + security + delta + stale
⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
🔬 deep (12 pass) — همه + impact analysis
🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)
```
**۹-B. بازنگری criteria_weights:**
به‌جای ۶ slider:
- preset selector: "general", "security-first", "tests-first", "feature-completeness", "logic-quality"
- sliders در `<details>` (advanced)
**۹-C. بازنگری Smart Task Lifecycle:**
- بررسی هر گزینه: کار می‌کند؟
- label با وضعیت فعلی
**۹-D. گزینه‌های جدید Phase 5:**
`WatchedProject` فیلدهای جدید (همه با default sensible):
```python
# Coverage
inventory_layers: List[str] = ["all"]    # یا subset
# Intelligence
stale_detection_enabled: bool = True
delta_analysis_enabled: bool = True
runtime_discovery_enabled: bool = True
outcome_data_enabled: bool = True
logic_audit_enabled: bool = True
notification_audit_enabled: bool = True
inspector_session_enabled: bool = True   # R14
# Lifecycle
auto_task_checklist_mode: str = "auto"   # R5
cleanup_tasks_enabled: bool = True
# Notifications
auto_task_notify_sound: bool = False     # R6
scan_notify_sound: bool = False          # R6
```
**۹-E. بازطراحی Layout — ۴ tabs (به‌جای ۳):**
| Tab | محتوا |
|---|---|
| **🔍 Coverage** | scan_depth + inventory_layers + criteria preset + auto_models |
| **🧠 Intelligence** | stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session |
| **🔁 Lifecycle** | dedup + auto-regenerate + checklist mode + cleanup |
| **🔔 Notifications** | auto_task_sound + scan_sound + notification_audit + custom templates |
**۹-F. AI explanation برای هر گزینه (R8):**
هر گزینه دارای help icon ⓘ + AI-generated description (از فاز ۲):
- این گزینه دقیقاً چه می‌کند
- کِی اضافه شد
- وضعیت فعلی (active / possibly stale / unknown)
**۹-G. Feature Inventory Panel (R8):**
panel جدید در UI با لیست همه options + AI explanation (از فاز ۲).
### Acceptance Criteria فاز ۹
- در UI، ۴ tab وجود دارد
- mode `balanced` پیش‌فرض جدید
- mode `ultra` در دسترس
- backward compat: scan_depth قدیمی همچنان کار کند
- هر گزینه دارای tooltip + AI explanation
- Feature Inventory Panel functional

--- کلیدواژه‌ها ---
frontend/src/app/oversight/page.tsx, WatchedProject.inventory_layers, WatchedProject.stale_detection_enabled, WatchedProject.delta_analysis_enabled, WatchedProject.runtime_discovery_enabled, WatchedProject.outcome_data_enabled, WatchedProject.logic_audit_enabled, WatchedProject.notification_audit_enabled, WatchedProject.inspector_session_enabled, WatchedProject.auto_task_checklist_mode, WatchedProject.cleanup_tasks_enabled, scan_depth modes, criteria_weights, Feature Inventory Panel
```

## 🎯 هدف (خلاصه ساختاریافته)
بازطراحی کامل UI Oversight به ۴ تب و به‌روزرسانی scan_depth modes و criteria_weights

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک مربوط به پیاده‌سازی کامل فاز ۹ از درخواست کاربر است که شامل بازطراحی کامل UI در frontend/src/app/oversight/page.tsx می‌باشد. بر اساس تحلیل کد واقعی پروژه، فایل frontend/src/app/oversight/page.tsx در deep context موجود است و باید تغییرات زیر اعمال شود:

1. **تغییر scan_depth modes**: از حالت فعلی (quick 3 pass, standard 6 pass, deep 12 pass پیش‌فرض, thorough 12 pass + health + roadmap) به ۴ حالت جدید:
   - ⚡ quick (5 pass) — frontend + backend + security + delta + stale
   - ⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
   - 🔬 deep (12 pass) — همه + impact analysis
   - 🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)

2. **تغییر criteria_weights**: به‌جای ۶ slider، یک preset selector با گزینه‌های "general", "security-first", "tests-first", "feature-completeness", "logic-quality" قرار گیرد و sliders در <details> (advanced) مخفی شوند.

3. **بازطراحی Layout به ۴ tab**:
   - 🔍 Coverage: scan_depth + inventory_layers + criteria preset + auto_models
   - 🧠 Intelligence: stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session
   - 🔁 Lifecycle: dedup + auto-regenerate + checklist mode + cleanup
   - 🔔 Notifications: auto_task_sound + scan_sound + notification_audit + custom templates

4. **اضافه کردن ۱۰ فیلد جدید به WatchedProject**: inventory_layers, stale_detection_enabled, delta_analysis_enabled, runtime_discovery_enabled, outcome_data_enabled, logic_audit_enabled, notification_audit_enabled, inspector_session_enabled, auto_task_checklist_mode, cleanup_tasks_enabled. هر گزینه باید help icon ⓘ با AI-generated description داشته باشد.

5. **Feature Inventory Panel**: panel جدید در UI با لیست همه options + AI explanation.

نکته حیاتی: backward compatibility با scan_depth قدیمی باید حفظ شود. این فاز پوشش R2 را فراهم می‌کند و خارج از این مرحله پیاده‌سازی فاز ۱۰ است.

کلیدواژه‌های کاربر: frontend/src/app/oversight/page.tsx, WatchedProject.inventory_layers, WatchedProject.stale_detection_enabled, WatchedProject.delta_analysis_enabled, WatchedProject.runtime_discovery_enabled, WatchedProject.outcome_data_enabled, WatchedProject.logic_audit_enabled, WatchedProject.notification_audit_enabled, WatchedProject.inspector_session_enabled, WatchedProject.auto_task_checklist_mode, WatchedProject.cleanup_tasks_enabled, scan_depth modes, criteria_weights, Feature Inventory Panel

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. مراحل پیاده‌سازی به ترتیب:

1. **اضافه کردن فیلدهای جدید به مدل WatchedProject**: فایل backend/app/models/inspector_session.py (که در deep context موجود است) را بررسی کن. ۱۰ فیلد جدید (inventory_layers, stale_detection_enabled, delta_analysis_enabled, runtime_discovery_enabled, outcome_data_enabled, logic_audit_enabled, notification_audit_enabled, inspector_session_enabled, auto_task_checklist_mode, cleanup_tasks_enabled) را با default values مشخص شده به مدل اضافه کن.

2. **به‌روزرسانی API endpoint مربوط به WatchedProject**: فایل backend/app/api/routes/oversight.py (در deep context موجود است) را بررسی کن. endpointهای مربوط به CRUD WatchedProject را به‌روزرسانی کن تا فیلدهای جدید را پشتیبانی کنند.

3. **بازطراحی کامل frontend/src/app/oversight/page.tsx**:
   - ساختار ۴ تبی (Coverage, Intelligence, Lifecycle, Notifications) ایجاد کن
   - scan_depth selector را با ۴ حالت جدید (quick 5 pass, balanced 8 pass پیش‌فرض, deep

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 10: فاز ۱۰ — Meta-validation

**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۱۰ از پرامپت کاربر است: تبدیل این پرامپت به task با ۱۰ task_step (مطابق فازهای ۱ تا ۹) و AC های قابل-verify دقیق. هر AC باید measurable باشد (مثلاً '≥ ۵ stale item کشف شود'). باید یک iteration loop پیاده‌سازی شود: پیاده‌سازی → verify deep (Phase 4) → نتیجه. اگر done < 100% iteration بعدی، اگر done = 100% فاز بعدی. log در phase5_meta_validation.md ذخیره شود. همچنین باید verify bugs شناسایی شوند: اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify، اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive). خارج از این مرحله: هیچ فاز دیگری وجود ندارد. نکته حیاتی: این فاز خود-ارجاعی است و باید بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد.
**Key terms:** phase5_meta_validation.md, verify deep, Phase 4, iteration loop, verify bugs, false-positive, behavior_observable, verification_hint

**بخش مربوط از متن کاربر:**
```
## 🅹 فاز ۱۰ — Meta-validation
> خود این پرامپت قابل verify باشد
### مسئله
کاربر می‌خواهد این پرامپت به task تبدیل شود و توسط verify Phase 4 سنجیده شود.
### تغییرات
**۱۰-A. این پرامپت به task تبدیل:**
- ایده‌ی خام = این متن
- AC ها = AC های هر فاز ۱-۹
- task_steps = ۱۰ فاز
**۱۰-B. AC قابل-verify دقیق:**
هر AC در هر فاز measurable نوشته شده (مثلاً «≥ ۵ stale item کشف شود»).
**۱۰-C. Iteration loop:**
- پیاده‌سازی → verify deep (Phase 4) → نتیجه
- اگر `done < 100%` → iteration بعدی
- اگر `done = 100%` → فاز بعدی
- log در `phase5_meta_validation.md`
**۱۰-D. شناسایی verify bugs:**
- اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify
- اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive)
- log همه‌ی این موارد
### Acceptance Criteria فاز ۱۰
- پرامپت نهایی شامل ۱۰ task_step
- هر step دارای `behavior_observable` + `verification_hint`
- بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد
- log باگ‌های verify identified
```

## 🎯 هدف (خلاصه ساختاریافته)
فاز ۱۰ — Meta-validation: تبدیل پرامپت به task با ۱۰ task_step و AC های قابل-verify دقیق

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک مربوط به پیاده‌سازی کامل فاز ۱۰ از پرامپت کاربر است: تبدیل این پرامپت به task با ۱۰ task_step (مطابق فازهای ۱ تا ۹) و AC های قابل-verify دقیق. هر AC باید measurable باشد (مثلاً '≥ ۵ stale item کشف شود'). باید یک iteration loop پیاده‌سازی شود: پیاده‌سازی → verify deep (Phase 4) → نتیجه. اگر done < 100% iteration بعدی، اگر done = 100% فاز بعدی. log در phase5_meta_validation.md ذخیره شود. همچنین باید verify bugs شناسایی شوند: اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify، اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive). خارج از این مرحله: هیچ فاز دیگری وجود ندارد. نکته حیاتی: این فاز خود-ارجاعی است و باید بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد.

بخش مربوط از درخواست اصلی کاربر:
## 🅹 فاز ۱۰ — Meta-validation
> خود این پرامپت قابل verify باشد
### مسئله
کاربر می‌خواهد این پرامپت به task تبدیل شود و توسط verify Phase 4 سنجیده شود.
### تغییرات
**۱۰-A. این پرامپت به task تبدیل:**
- ایده‌ی خام = این متن
- AC ها = AC های هر فاز ۱-۹
- task_steps = ۱۰ فاز
**۱۰-B. AC قابل-verify دقیق:**
هر AC در هر فاز measurable نوشته شده (مثلاً «≥ ۵ stale item کشف شود»).
**۱۰-C. Iteration loop:**
- پیاده‌سازی → verify deep (Phase 4) → نتیجه
- اگر `done < 100%` → iteration بعدی
- اگر `done = 100%` → فاز بعدی
- log در `phase5_meta_validation.md`
**۱۰-D. شناسایی verify bugs:**
- اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify
- اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive)
- log همه‌ی این موارد
### Acceptance Criteria فاز ۱۰
- پرامپت نهایی شامل ۱۰ task_step
- هر step دارای `behavior_observable` + `verification_hint`
- بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد
- log باگ‌های verify identified

کلیدواژه‌ها: phase5_meta_validation.md, verify deep, Phase 4, iteration loop, verify bugs, false-positive, behavior_observable, verification_hint

شواهد در کد واقعی پروژه: فایل `backend/app/services/verify_runtime/code_aware_verifier.py` (که در deep context موجود نیست ولی در ساختار پروژه دیده می‌شود) و `backend/app/services/oversight_verifier.py` (که در deep context موجود نیست ولی در ساختار پروژه دیده می‌شود) و `backend/app/api/routes/oversight.py` (که در deep context موجود است) برای verify deep استفاده می‌شوند. فایل `backend/app/services/verify_runtime/runner.py` (که در ساختار پروژه دیده می‌شود) برای اجرای iteration loop استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria)
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ۱. ایجاد فایل `backend/app/services/verify_runtime/meta_validation.py` جدید برای پیاده‌سازی Meta-validation Service.
۲. در این فایل، یک کلاس `MetaValidationService` ایجاد کن که:
   - متد `convert_prompt_to_task(prompt_text: str) -> dict` داشته باشد که پرامپت را به task با ۱۰ task_step تبدیل کند.
   - متد `run_iteration_loop(task: dict) -> dict` داشته باشد که iteration loop را پیاده‌سازی کند: پیاده‌سازی → verify deep (Phase 4) → نتیجه.
   - متد `identify_verify_bugs(implementation_result: dict, verify_result: dict) -> list

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: refactor
- اولویت: critical
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: backend_test)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: backend_test)_

## Task Steps

### Step 1: فاز ۱ — Comprehensive Inventory + Purpose Extraction
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۱ از پرامپت کاربر است: ایجاد ماژول‌های جدید comprehensive_inventory.py و purpose_extractor.py در backend/app/services/scan_v5/. comprehensive_inventory.py باید ۱۲ لایه مختلف (Files, Backend endpoints, UI elements, DB schema, Env vars, Config files, Dependencies, Scripts, Cron/scheduled, Routes, Notification calls, UI options/settings) را با روش‌های مشخص (git ls-files, AST parse, parse .tsx, inspect, regex, glob) اسکن کند. purpose_extractor.py باید برای هر آیتم inventory یک purpose_map با فیلدهای stated_purpose, evidence_sources, expected_inputs, expected_outputs, interacting_with, creation_context, current_usage استخراج کند. همچنین باید فیلدهای جدید last_scan_inventory, last_scan_purpose_map, last_scan_at_v5 به مدل WatchedProject اضافه شود. خارج از این مرحله: پیاده‌سازی فازهای ۲ تا ۱۰، تغییرات UI، و تحلیل‌های پیشرفته‌تر. نکته حیاتی: این فاز پایه تمام فازهای بعدی است و باید inventory حداقل ۵۰ backend endpoint و ۳۰ UI element و ۱۰ notification call را پوشش دهد.
**Excerpt:**
```
## 🅰️ فاز ۱ — Comprehensive Inventory + Purpose Extraction
> پوشش **R3** (ریز تا درشت)، پایه برای همه فازهای بعدی
### مسئله
الان `run_deep_scan` فقط ۳۵ فایل deep-read + import graph می‌بیند. backend endpoints، UI elements، DB schema، env vars، configs، **notifications، inspector sessions، UI options** هرگز inventory نمی‌شوند.
### تغییرات
**۱-A. Structural Inventory (۱۲ لایه):**
ماژول جدید: `backend/app/services/scan_v5/comprehensive_inventory.py`
| # | لایه | چه چیزی جمع می‌شود | روش |
|---|---|---|---|
| 1 | Files | همه فایل‌های git-tracked | `git ls-files` |
| 2 | Backend endpoints | همه `@router.{get,post,…}` + WebSocket + background tasks | AST parse |
| 3 | UI elements | همه `<button>`, `<form>`, `<input>`, `<Select>`, `<Link>`, modal triggers, dropdown items | parse `.tsx` |
| 4 | DB schema | همه dataclass + Column + migrations | inspect |
| 5 | Env vars | همه `os.environ.get`, `os.getenv`, `process.env.X` | regex |
| 6 | Config files | همه `.json`, `.yaml`, `.toml`, `.env*` | glob |
| 7 | Dependencies | `requirements.txt`, `package.json` | parse |
| 8 | Scripts | همه `.sh`, `pyproject::scripts`, `package.json::scripts` | parse |
| 9 | Cron/scheduled | همه `apscheduler`, `BackgroundTasks`, `asyncio.create_task` | regex |
| 10 | Routes | همه frontend `app/**/page.tsx` + backend route | walk |
| 11 | **Notification calls (R12)** | همه `notify_event(...)`, `send_telegram(...)`, `bot.send_message(...)` + event_type + silent flag + caption template | regex/AST |
| 12 | **UI options/settings (R8)** | همه checkbox/slider/dropdown در UI + field name در WatchedProject + default value | parse + cross-ref |
**۱-B. Purpose Extraction (R10):**
ماژول جدید: `backend/app/services/scan_v5/purpose_extractor.py`
برای **هر file/module/feature/option** که در inventory هست:
```python
purpose_inventory[item_id] = {
    "stated_purpose": "چه کاری *قرار است* بکند",
    "evidence_sources": ["comments", "docstrings", "tests", "raw_idea", "task_history", "commit_messages"],
    "expected_inputs": [...],
    "expected_outputs": [...],
    "interacting_with": [...],          # سایر componentهای همکار
    "creation_context": {                # R8 — کاربر فراموش کرده چی هست
        "first_seen_commit": "...",
        "first_seen_date": "...",
        "originating_task_id": "...",    # کدام task این را ساخت
        "originating_raw_idea": "...",   # متن خام درخواست
    },
    "current_usage": "...",              # آیا هنوز استفاده می‌شود؟ کجا؟
}
```
**منابع AI برای purpose:**
- محتوای فایل + docstrings + JSDoc
- test files مرتبط
- `WatchedProject.user_notes` (raw_idea اصلی پروژه)
- task history (task هایی که این item را modify کرده‌اند)
- commit messages روی این path
**۱-C. Storage:**
`WatchedProject` فیلدهای جدید:
```python
last_scan_inventory: Optional[Dict[str, Any]] = None
last_scan_purpose_map: Optional[Dict[str, Dict]] = None
last_scan_at_v5: Optional[str] = None  # timestamp scan جدید
```
### Acceptance Criteria فاز ۱
- `inventory.backend_endpoints` ≥ ۵۰
- `inventory.ui_elements` ≥ ۳۰
- `inventory.notification_calls` ≥ ۱۰
- `inventory.ui_options` همه‌ی checkbox/slider/dropdown ها را شامل شود
- `purpose_map` برای ≥ ۸۰% فایل‌های مهم
- برای هر UI option، `creation_context` نشان دهد از کدام task ساخته شد
```

### Step 2: فاز ۲ — Stale Detection + Feature Inventory Panel
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۲ از پرامپت کاربر است: ایجاد ماژول‌های جدید stale_detector.py و feature_documenter.py در backend/app/services/scan_v5/. stale_detector.py باید ۸ نوع structural stale (Dead UI buttons, Dead frontend routes, Dead backend endpoints, Unused functions/classes, Unused dataclass fields, Unused env vars, Orphan files, Stale dependencies) و ۵ نوع semantic stale (Purpose-mismatched, Hidden purpose, Inconsistent با تغییرات اخیر, Outdated business assumption, Forgotten by user) را شناسایی کند. feature_documenter.py باید برای هر UI option/setting/feature/button یک AI documentation با فیلدهای name, what_it_does, when_added, originating_idea, current_status, dependencies, dependents, recommended_action تولید کند. همچنین باید پنل Feature Inventory در frontend/src/app/oversight/page.tsx اضافه شود که تمام آیتم‌ها را با دسته‌بندی و AI explanation نمایش دهد. برای هر stale item باید task (cleanup/audit/document) با AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۳ تا ۱۰، تغییرات runtime discovery. نکته حیاتی: این فاز مهم‌ترین فاز برای حل مشکل R8 (گزینه‌های قدیمی) است و باید حداقل ۵ structural stale و ۳ semantic stale و ۸ hidden-purpose option شناسایی کند.
**Excerpt:**
```
## 🅱️ فاز ۲ — Stale Detection + Feature Inventory Panel
> **مهم‌ترین فاز برای R8 — حل مشکل «گزینه‌های قدیمی که نمی‌دانم چی هست»**
### مسئله (R8 — تأکید سه‌باره)
> «خیلی‌هاش قدیمیه و کار نمی‌کنه و خیلی‌هاش نیاز داره بدونم چی بوده، برای چیه و هر سری باید بهش سر بزنم»
### تغییرات
**۲-A. Structural Stale (۸ نوع):**
ماژول جدید: `backend/app/services/scan_v5/stale_detector.py`
| نوع | شناسایی |
|---|---|
| Dead UI buttons | onClick handler ندارد / empty / به endpoint 404 می‌رود |
| Dead frontend routes | در nav menu نیست و در هیچ Link/router.push نیست |
| Dead backend endpoints | در هیچ frontend fetch نیست + در Render logs ۳۰ روز اخیر صدا نشد |
| Unused functions/classes | reverse import = 0 و entry point نیست |
| Unused dataclass fields | در هیچ‌جا read/write نمی‌شود |
| Unused env vars | تعریف شده ولی هیچ `os.environ.get` |
| Orphan files | reverse import = 0 |
| Stale dependencies | در requirements/package.json هست ولی import نمی‌شود |
**۲-B. Semantic Stale (R10 — ۵ نوع):**
| نوع | شناسایی |
|---|---|
| Purpose-mismatched | `stated_purpose` با `actual_behavior` نمی‌خواند |
| Hidden purpose (R8) | کاربر نمی‌داند چی هست — هیچ doc، نام مبهم، creation_context قدیمی |
| Inconsistent با تغییرات اخیر | کد فرض می‌کند رفتار قدیمی X، ولی X تغییر کرده (نیاز به فاز ۳) |
| Outdated business assumption | threshold/config که با realities فعلی نمی‌خواند |
| Forgotten by user (R8 — جدید) | UI option/setting که کاربر هر بار باید بپرسد چی هست |
**۲-C. AI-Generated Documentation برای هر option (R8):**
برای **هر** UI option/setting/feature/button که در inventory.ui_options یا inventory.ui_elements هست:
```python
ai_documentation[option_id] = {
    "name": "...",
    "what_it_does": "AI explanation — این گزینه دقیقاً چه کاری می‌کند",
    "when_added": "تاریخ + commit",
    "originating_idea": "متن خام درخواست اولیه",
    "current_status": "active | possibly_stale | broken | unknown",
    "dependencies": ["..."],         # forward
    "dependents": ["..."],           # reverse
    "recommended_action": "keep | remove | refactor | document"
}
```
**۲-D. Feature Inventory Panel در UI:**
`frontend/src/app/oversight/page.tsx` — اضافه شدن panel جدید:
```
🗺 Feature Inventory
├─ 🔧 Settings (12)
│   ├─ ✓ verify_mode (active — Phase 4, از task #abc1234)
│   ├─ ✓ dedup_threshold (active — Phase 3)
│   ├─ ⚠️ confirmation_streak_required (possibly_stale — ۹۰ روز unused)
│   └─ ...
├─ 🎛 UI Buttons (45)
├─ 🌐 Backend Endpoints (78)
└─ ⚙️ Env Vars (23)
```
برای هر مدخل: hover → AI explanation کامل.
**۲-E. Task Generation:**
برای هر stale item:
- **cleanup** task (structural) یا **audit** task (semantic) یا **document** task (hidden purpose)
- AC رفتار-محور: «یا حذف شود، یا با docstring/comment توضیح داده شود چرا نگه داشته، یا ارتقا یابد»
- priority بر اساس impact
### Acceptance Criteria فاز ۲
- روی این پروژه ≥ ۵ structural stale + ≥ ۳ semantic stale + ≥ ۸ hidden-purpose options
- Feature Inventory Panel با ≥ ۲۰ مدخل
- هر stale → task با explanation کامل (نه فقط «این unused است»)
- روی project trade مثال کاربر (R11)، scan تشخیص دهد گزینه‌هایی که برای trading فعلاً ضرر می‌دهند
```

### Step 3: فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۳ از پرامپت کاربر است: ایجاد ماژول‌های جدید delta_analyzer.py و dependency_analyzer.py در backend/app/services/scan_v5/. delta_analyzer.py باید ۶ نوع تغییر (add, remove, modify, rename, move, signature-change) را با مقایسه state فعلی و قبلی (ذخیره شده در WatchedProject.prev_scan_state) تشخیص دهد. dependency_analyzer.py باید bidirectional dependency (هم forward dependencies و هم reverse dependents) را برای هر فایل تغییر یافته محاسبه کند و بین import-based و logical تمایز قائل شود. logical impact analysis باید با AI روی purpose_inventory تحلیل کند که آیا تغییر یک فایل، منطق dependents را می‌شکند. برای هر changed file + dependent در خطر باید task با badge '🔄 وابسته به تغییر' و AC رفتار-محور ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۴ تا ۱۰، تغییرات UI. نکته حیاتی: این فاز پوشش R7 (Bidirectional dependency) را فراهم می‌کند و باید بعد از دو scan متوالی، delta با ۶ نوع تشخیص دهد و اگر signature تغییر کرد و ۳ caller دارد، حداقل ۱ task برای بررسی caller ها ایجاد کند.
**Excerpt:**
```
## 🅲️ فاز ۳ — Delta Detection + Bidirectional Dependency + Logical Impact
> پوشش **R7** — کاربر صریحاً bidirectional خواست
### مسئله (R7)
> «چه وابستگی‌هایی داشته **به چه چیزهایی وابسته بوده** و **چه چیزهایی بهش وابسته بودن**»
این **دو طرفه** است:
- forward: این فایل به چه چیزی وابسته است
- reverse: چه چیزهایی به این فایل وابسته‌اند
### تغییرات
**۳-A. ذخیره prev state:**
`WatchedProject`:
```python
prev_scan_state: Optional[Dict[str, Any]] = None
# {file_path → {sha, size, last_modified, imports_hash, purpose_hash, ui_elements_hash}}
```
**۳-B. compute delta — ۶ نوع (R7: «تغییر کرده یا اضافه شده یا کم شده یا ویرایش شده»):**
| نوع | تشخیص |
|---|---|
| `add` | در current، نه در prev |
| `remove` | در prev، نه در current |
| `modify` | sha متفاوت |
| `rename` | sha مشابه، path متفاوت |
| `move` | content مشابه، path متفاوت |
| `signature-change` | نام تابع ثابت، parameters یا return-type تغییر |
**۳-C. Bidirectional Dependency (R7 صریح):**
ماژول جدید: `backend/app/services/scan_v5/dependency_analyzer.py`
برای هر changed file:
```python
{
    "dependencies": [...],   # forward — file → چه چیزی import می‌کند
    "dependents": [...],     # reverse — چه چیزهایی → این file import می‌کنند
    "logical_dependencies": [...],   # forward منطقی — کدام components روی این تکیه دارند
    "logical_dependents": [...],     # reverse منطقی — این از کدام components انتظار دارد
}
```
تمایز بین **import-based** و **logical**:
- import: AST/grep
- logical: AI روی purpose_inventory تشخیص می‌دهد (مثلاً «این تابع threshold X را می‌خواند، اگر X تغییر کرد رفتار تغییر می‌کند»)
**۳-D. Logical Impact Analysis (R10):**
AI روی هر تغییر + dependents تحلیل می‌کند:
- آیا تغییر این فایل، **منطق** dependents را می‌شکند؟
- مثال: تغییر `threshold=0.65` → `threshold=0.8` → چه componentهای dependent behavior متفاوت نشان می‌دهند؟
- مثال: تغییر فرمت output `parse_signal()` → چه callerها به update نیاز دارند؟
- مثال: حذف یک env var → چه قسمت‌هایی crash می‌کنند؟
**۳-E. Task Generation:**
برای هر changed file + dependent در خطر:
- task با badge `🔄 وابسته به تغییر`
- AC رفتار-محور: «بعد از این تغییر، dependents X و Y باید همچنان behavior Z تولید کنند»
- اگر logical impact پیچیده است → priority بالاتر + reference به scan inspector session
### Acceptance Criteria فاز ۳
- بعد از دو scan متوالی، delta با ۶ نوع تشخیص داده شود
- اگر signature تغییر کرد و ۳ caller دارد، ≥ ۱ task برای بررسی caller ها
- logical impact detection: اگر threshold تغییر کرد، AI تحلیل کند نه فقط alert سینتکسی
- bidirectional dependency: هم dependencies (forward) هم dependents (reverse) برای هر changed item
```

### Step 4: فاز ۴ — Runtime Discovery + Outcome + Inspector Session
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۴ از پرامپت کاربر است: ایجاد ماژول‌های جدید runtime_discovery.py, outcome_analyzer.py, scan_inspector_session.py در backend/app/services/scan_v5/. runtime_discovery.py باید از navigation_helper, Playwright, backend_log_probe, vision_helper, code_aware_verifier برای کشف runtime_state شامل routes_alive, routes_404, endpoints_called_recently, endpoints_never_called, ui_features_visible, recent_commits استفاده کند. outcome_analyzer.py باید برای هر پروژه outcome data (trade logs, conversation outcomes, error rates, task completion rates, delivery rates) جمع‌آوری کند. scan_inspector_session.py باید برای هر scan یک inspector session باز کند (مشابه verify در Phase 4) که شامل همه AI calls, Playwright actions, screenshots, runtime probe outputs باشد و در پایان session archived + bundle PDF + Telegram message ارسال شود. همچنین باید tab 'Scan Sessions' در Inspector UI اضافه شود. خارج از این مرحله: پیاده‌سازی فازهای ۵ تا ۱۰. نکته حیاتی: این فاز پوشش R14 (Inspector tab integration) را فراهم می‌کند و باید بعد از هر scan یک inspector session با ≥ ۱۰ پیام و ≥ ۵ screenshot ایجاد کند.
**Excerpt:**
```
## 🅳 فاز ۴ — Runtime Discovery + Outcome + Inspector Session
> پوشش **R14** — Inspector tab integration — این مهم‌ترین آپشن از قلم افتاده
### مسئله (R14 صریح)
کاربر گفت: «در بررسی‌های عمیق از تب بازرس ویژه هم کمک می‌گیره و **اسکرین‌ها** رو می‌گیره و در تلگرام **همراه با گزارش** بفرسته و **چت‌ها** رو در یه session ثبت کنه و **بایگانی** کنه؟»
این **اصلاً در پرامپت‌های قبلی نبود**. فعلاً Inspector فقط در verify (Phase 4) استفاده می‌شد.
### تغییرات
**۴-A. Runtime Discovery (با ایده از Phase 4):**
ماژول جدید: `backend/app/services/scan_v5/runtime_discovery.py`
| منبع | استفاده |
|---|---|
| `navigation_helper::extract_nav_links_from_page` | nav links واقعی frontend |
| Playwright on each route | screenshot + status code → 404 detection |
| `backend_log_probe::_fetch_relevant_logs` 30-day | endpoints واقعاً called شده |
| `vision_helper::analyze_screenshot` | کشف feature های UI |
| `code_aware_verifier::_fetch_recent_commits_with_diff` | recent commits context |
خروجی: `runtime_state` شامل `routes_alive[]`, `routes_404[]`, `endpoints_called_recently[]`, `endpoints_never_called[]`, `ui_features_visible[]`, `recent_commits[]`.
**۴-B. Outcome Data Collection:**
برای هر پروژه، scan تلاش کند outcome data بیابد:
| نوع پروژه | outcome data |
|---|---|
| Trading | trade logs, P&L history |
| AI/Chat | conversation outcomes |
| Web service | error rates, latency from logs |
| Scheduling | task completion rates |
| Notification | delivery + open rates |
روش‌ها: Render logs filtered + DB tables outcome-naming + file artifacts.
**۴-C. Scan Inspector Session (R14 — حیاتی):**
ماژول جدید: `backend/app/services/scan_v5/scan_inspector_session.py`
**هر scan یک inspector session باز می‌کند** (مشابه verify در Phase 4):
```python
session = create_scan_inspector_session(
    watched_id=...,
    scan_id=...,
    project_name=...,
)
```
**در طول scan:**
- همه AI calls (purpose extraction، stale detection، logic audit، etc.) → پیام در session با role="ai", content=request+response
- همه Playwright actions → پیام با role="action", screenshot=path
- همه screenshot ها → ذخیره روی disk + reference در session
- runtime probe outputs → پیام با role="probe"
**در پایان scan:**
- session archived
- screenshots → آرشیو شده در Telegram (مثل bundle Phase 4)
- bundle PDF تولید می‌شود شامل:
  - findings + tasks ایجاد شده
  - delta summary
  - logic audit findings
  - inventory summary
  - تمام screenshots
- Telegram message + PDF attachment
- در UI، scan session در inspector tab با badge `🔍 Scan Session` (متمایز از `🔬 Verify Session`)
**۴-D. اضافه شدن tab "Scan Sessions" در Inspector UI:**
`frontend/src/app/projects/[id]/page.tsx` (یا هر جا که inspector tab هست):
- علاوه بر لیست verify sessions، scan sessions هم نمایش داده شوند
- کاربر بتواند هر scan session را باز کند و تمام مکالمات + screenshots آن را ببیند
- archive option
### Acceptance Criteria فاز ۴
- بعد از هر scan، یک inspector session با ≥ ۱۰ پیام (AI calls + actions + probes) ساخته شود
- ≥ ۵ screenshot ذخیره شود
- Telegram message + bundle PDF با همه‌ی screenshots و findings ارسال شود
- در Inspector tab، session آرشیو شده دیده شود با badge `🔍 Scan`
- اگر runtime discovery فعال است، روی هر route یک screenshot + Vision analysis
```

### Step 5: فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۵ از پرامپت کاربر است: ایجاد ماژول‌های جدید coherence_analyzer.py و anti_pattern_detector.py در backend/app/services/scan_v5/. coherence_analyzer.py باید pipeline coherence را برای ۶ نوع chain (Data pipeline, AI/LLM chain, Business logic, Auth/Permission, Feedback loop, Notification chain) با استفاده از purpose_inventory بررسی کند. anti_pattern_detector.py باید ۱۱ نوع logical anti-pattern (داده بی‌مصرف, AI بدون validation, Magic threshold, Conflicting defaults, Silent failure در crucial path, Broken feedback loop, Stale assumption, Over/under-engineering, Conditional inconsistency, Threshold-Outcome mismatch, Notification mismatch) را شناسایی کند. همچنین باید Hidden Requirements Discovery (R8) برای feature‌های فراموش شده و Outcome-based Effectiveness Audit (R11) انجام دهد. نوع task جدید 'logic_audit' با AC outcome-oriented و AI explanation کامل ایجاد شود. خارج از این مرحله: پیاده‌سازی فازهای ۶ تا ۱۰. نکته حیاتی: این فاز مهم‌ترین فاز معنایی است و باید روی پروژه فعلی ≥ ۳ logic issue و ≥ ۱ pipeline coherence و ≥ ۲ anti-pattern logical شناسایی کند.
**Excerpt:**
```
## 🅴 فاز ۵ — Logical Audit (Coherence + Anti-pattern + Outcome)
> پوشش **R10, R11** — مهم‌ترین فاز معنایی
### مسئله (R10, R11)
> «یه پروژه دارم که هوشمند ترید کنه ولی بدتر از یه آدم معمولی ترید می‌کنه — باگ منطقی در هماهنگی بین AI و داده‌ها»
scan نباید **فقط خطاها** را ببیند، بلکه **منطق و هدف** را هم.
### تغییرات
**۵-A. Pipeline Coherence Detection:**
ماژول جدید: `backend/app/services/scan_v5/coherence_analyzer.py`
شناسایی pipelines (chains از componentهای همکار) با استفاده از `purpose_inventory[X].interacting_with`.
برای هر chain، AI بررسی coherence:
| الگو | چه بررسی شود |
|---|---|
| **Data pipeline** | schema هر مرحله، handling empty result |
| **AI/LLM chain** | prompt format ↔ model ↔ parser سازگار؟ validation؟ |
| **Business logic (R11 مثال trade)** | signal ↔ risk model، position ↔ account size، stop-loss ↔ timeframe |
| **Auth/Permission** | همه‌ی mutation از permission گذر می‌کند؟ |
| **Feedback loop** | outcome به config/model برمی‌گردد؟ |
| **Notification chain (R12)** | event → notify_event → caption → silent decision → delivery — همه consistent؟ |
**۵-B. Logical Anti-pattern Detection:**
ماژول جدید: `backend/app/services/scan_v5/anti_pattern_detector.py`
AI روی purpose_inventory + کد، این الگوها را پیدا می‌کند:
| Anti-pattern | مثال |
|---|---|
| داده بی‌مصرف | API call، DB write، ولی هیچ read |
| AI بدون validation | response parse می‌شود ولی validity چک نمی‌شود |
| Magic threshold | `> 0.65:` بدون توضیح |
| Conflicting defaults | یک field default متفاوت در جاهای مختلف |
| Silent failure در crucial path | `except: pass` در business logic |
| Broken feedback loop (R11) | outcome لاگ ولی به model نمی‌رسد |
| Stale assumption | کد فرض می‌کند رفتار سرویس X خاصه، X تغییر کرد |
| Over/under-engineering | برای ساده ۵ لایه، برای پیچیده hardcode |
| Conditional inconsistency | conditions با تغییرات اخیر inconsistent |
| Threshold-Outcome mismatch (R11) | parameters نتایج مطلوب تولید نمی‌کنند |
| Notification mismatch (R12) | event critical ولی silent=True، یا opposite |
**۵-C. Hidden Requirements Discovery (R8):**
برای هر feature که کاربر فراموش کرده «چی هست برای چیه»:
- AI روی `creation_context` (از فاز ۱) + task history + commit messages تحلیل
- استخراج: «این feature چه وقت، چرا، با چه هدفی اضافه شد؟»
- نتیجه:
  - هدف منسوخ → task `cleanup`
  - هدف معتبر، پیاده‌سازی ضعیف → task `refactor`
  - هدف معتبر، پیاده‌سازی خوب، docs نیست → task `document`
**۵-D. Outcome-based Effectiveness Audit (R11):**
اگر outcome data دارد (از فاز ۴):
- AI تحلیل می‌کند آیا outcome با `stated_purpose` می‌خواند
- مثال R11: trade — اگر purpose="earn profit" و win-rate=30% → effectiveness LOW
- اگر LOW → task `logic_audit` با priority بالا
**۵-E. Task Generation:**
نوع جدید task: `logic_audit`
- AC outcome-oriented:
  - ❌ «این کد را fix کن»
  - ✅ «بعد از این تغییر، win-rate باید ≥40% یا parameters محافظه‌کارانه‌تر شود»
- AI explanation کامل: چرا اشتباه + چه راه‌حل + چه impact
### Acceptance Criteria فاز ۵
- روی پروژه فعلی ≥ ۳ logic issue + ≥ ۱ pipeline coherence + ≥ ۲ anti-pattern logical
- روی پروژه trade فرضی (R11)، scan باگ منطقی هماهنگی AI-data شناسایی کند
- task `logic_audit` با AC outcome-oriented + AI explanation کامل
```

### Step 6: فاز ۶ — Notification System Audit (R12)
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۶ از پرامپت کاربر است: ایجاد ماژول جدید notification_auditor.py در backend/app/services/scan_v5/. این ماژول باید notification inventory (از فاز ۱ لایه ۱۱) را با فیلدهای file_path, line, event_type, caption_template, silent_default, attachments, trigger_condition, frequency_estimate کامل کند. سپس Notification Coherence Audit انجام دهد که شامل بررسی کامل بودن caption, مناسب بودن silent/sound, صحت attachments, timing, stale notifications, missing notifications, و scan-specific notifications است. برای هر notification مشکل‌دار باید task 'notification_audit' با AC مشخص ایجاد شود. همچنین باید فیلدهای جدید WatchedProject.auto_task_notify_sound و WatchedProject.scan_notify_sound اضافه شود. notification_service.py, oversight_telegram_compose.py, oversight_mega_bundle.py باید برای پشتیبانی از scan completion notification با caption جدید و bundle PDF به‌روزرسانی شوند. خارج از این مرحله: پیاده‌سازی فازهای ۷ تا ۱۰. نکته حیاتی: این فاز پوشش R6 و R12 را فراهم می‌کند و باید حداقل ۲ مورد ارتقا برای notification ها پیشنهاد دهد.
**Excerpt:**
```
## 🅵 فاز ۶ — Notification System Audit (R12)
> پوشش **R6, R12** — صریحاً audit notification ها
### مسئله (R12)
> «در پرامپت برای پیام‌رسانی تلگرام هم جایی در نظر گرفتی؟ در بررسی‌ها نوع اطلاع‌رسانیش هم بررسی بشه و بهینه بشه»
### تغییرات
**۶-A. Notification Inventory (از فاز ۱ — لایه ۱۱):**
برای هر `notify_event(...)` call در کد:
```python
notification_inventory[call_id] = {
    "file_path": "...",
    "line": ...,
    "event_type": "...",          # "task_created" | "verify_done" | ...
    "caption_template": "...",
    "silent_default": True | False | None,
    "attachments": [...],
    "trigger_condition": "...",   # کد منطق در کجا fire می‌شود
    "frequency_estimate": "...",  # احتمال در روز
}
```
**۶-B. Notification Coherence Audit (R12):**
AI روی notification_inventory:
| سوال | چه چیزی audit شود |
|---|---|
| caption کامل است؟ | همه‌ی فیلدهای مهم در caption هستند؟ (title, intent, link, attachments) |
| silent/sound مناسب است؟ | event critical → sound. event routine → silent |
| attachments صحیح؟ | task creation → prompt.md، verify done → bundle.pdf، scan done → scan-bundle.pdf |
| timing مناسب؟ | spam جلوگیری شده؟ batching هست؟ |
| stale notifications؟ | event type که دیگر کد آن وجود ندارد |
| missing notifications؟ | event critical که notification ندارد |
| **scan-specific (R12)**: | scan completion notification با همه‌ی مدارک ضروری: findings, tasks created, delta, logic audit results, inspector session reference |
**۶-C. Notification Templates Suggestions:**
برای هر notification که audit آن مشکل دارد → task `notification_audit`:
مثال task:
```yaml
title: "ارتقای caption notification scan_completed"
type: notification_audit
priority: medium
raw_idea: |
  notification scan_completed فعلاً فقط count tasks ایجاد شده را می‌دهد.
  باید شامل: delta summary + logic audit findings + inspector session link
  + attachment با PDF کامل (مشابه verify bundle).
behavior_observable: |
  بعد از این تغییر، notification scan_completed باید شامل:
  - title پروژه
  - تعداد tasks جدید
  - delta count (add/remove/modify)
  - logic audit count
  - link به scan inspector session
  - PDF attachment با همه screenshots و findings
acceptance_signal: |
  بعد از یک scan تست، Telegram message + PDF received با همه فیلدها
```
**۶-D. Audit existing notification flows:**
- `notification_service.py` — همه `notify_event` ها
- `oversight_telegram_compose.py` — caption builders
- `oversight_mega_bundle.py` — bundle PDFs
- بررسی silent default برای auto-tasks (R6)
### Acceptance Criteria فاز ۶
- audit notification ها: ≥ ۲ مورد ارتقا قابل پیشنهاد
- task `notification_audit` ایجاد شود برای هر مورد
- caption template جدید برای scan_completed تعریف شود
- silent default برای auto-task و scan notifications به `True` تنظیم شود
- field `WatchedProject.auto_task_notify_sound: bool = False`
- field `WatchedProject.scan_notify_sound: bool = False`
```

### Step 7: فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۷ از پرامپت کاربر است: به‌روزرسانی oversight_strong_prompt.py و oversight_service.py برای پشتیبانی از ساختار غنی AC و task_step. در oversight_strong_prompt.py باید section جدید '🎯 معیار رفتاری + business intent + alternative implementations' اضافه شود. در oversight_service.py:idea_to_prompt (~3454) و oversight_service.py:task creation from scan (~4700-4740) و oversight_service.py:_ai_plan_steps_from_idea (~2673) باید ساختار AC به فیلدهای text, behavior, acceptance_signal, business_intent, alternative_implementations, non_goals, false_positive_guard ارتقا یابد. همچنین باید Smart Checklist Mode با فیلد WatchedProject.auto_task_checklist_mode (Literal["auto","always","never"]) پیاده‌سازی شود که برای هر دو مسیر manual و auto کار کند. دستورالعمل جلوگیری از false-positive (R9) باید به AI داده شود. خارج از این مرحله: پیاده‌سازی فازهای ۸ تا ۱۰. نکته حیاتی: این فاز پوشش R5, R9, R13 را فراهم می‌کند و باید پرامپت‌های جدید برای ۳ ایده نمونه شامل behavior + acceptance_signal + business_intent + alternative_implementations + false_positive_guard تولید کند.
**Excerpt:**
```
## 🅶 فاز ۷ — Smart Prompt + Smart Checklist (R9, R13)
> پوشش **R5, R9, R13** — برای **manual + auto** هر دو
### مسئله (R9 صریح)
> «در تولید ایده خام به پرامپت، پرامپت در برخی قسمت‌ها دقیق و واضح تولید نمیشه... باید هوشمندتر بشه خود متن پرامپت و چک‌لیستی که ازش تولید میشه»
### مسئله (R13 صریح)
> «این بهبود پرامپت‌نویسی تسک‌ها آیا فقط برای تسک‌های سیستمی یا تسک‌های دستی هم شامل میشه؟»
**جواب: هر دو.**
### تغییرات
**۷-A. ساختار غنی AC (هم manual، هم auto):**
برای **manual** path در `oversight_service.py:idea_to_prompt` (~3454):
برای **auto** path در `oversight_service.py:task creation from scan` (~4700-4740):
برای **scan-generated** path در `oversight_strong_prompt.py:build_strong_prompt`:
هر AC ساختار غنی:
```python
{
    "text": "...",                          # توضیح اصلی (می‌تواند نام بدهد)
    "behavior": "...",                      # رفتار قابل مشاهده — چه چیزی observable است
    "acceptance_signal": "...",             # سؤال قابل-verify
    "business_intent": "...",               # چرا این لازم است
    "alternative_implementations": [...],   # نام‌های جایگزین قابل قبول (R9 — جلوگیری از نام-محوری)
    "non_goals": "...",                     # چه چیزی این AC نیست
    "false_positive_guard": "...",          # چه شواهد ضعیفی نشانه done نیست (R9)
}
```
**۷-B. ساختار غنی task_step (در `_ai_plan_steps_from_idea`):**
برای manual و auto (R13):
```python
{
    "title": "...",
    "scope": "...",
    "behavior_observable": "...",       # خروجی observable
    "verification_hint": "...",         # کجا verify بیابد
    "business_intent": "...",           # چرا این مرحله
    "non_goals": "...",
}
```
**۷-C. Smart Checklist Mode (R5):**
> «حالت هوشمند که برای تولید چک‌لیست یا عدم تولیدش حسب نیاز هست»
`WatchedProject.auto_task_checklist_mode: Literal["auto","always","never"] = "auto"`
- `auto`: AI تصمیم می‌گیرد بر اساس پیچیدگی (≥3 stage → checklist، single-action → skip)
- `always`: همیشه چک‌لیست
- `never`: هرگز
این برای **هم manual و هم auto** کار می‌کند.
**۷-D. Update prompt templates:**
در `oversight_strong_prompt.py`:
- اضافه شدن section "🎯 معیار رفتاری + business intent + alternative implementations"
- AI صریح راهنمایی شود: AC نام-محور بد است، AC رفتار-محور خوب
**۷-E. جلوگیری از false-positive (R9):**
> «جوری هم نشه که سیستم کاری که بد انجام شده... در verify تیک بزنه که کامل شده»
دستورالعمل به AI:
- اگر AC vague → split به sub-behaviors concrete
- هرگز AC بدون `acceptance_signal` نسازد
- `false_positive_guard` فیلد: چه شواهد ضعیفی نباید سبب done شود
- مثال: «اگر فقط فایل با نام وجود دارد، done نیست — باید رفتار X هم باشد»
**۷-F. Integration با فازهای قبلی:**
پرامپت تولید‌شده استفاده کند از:
- `purpose_inventory` (فاز ۱)
- `runtime_state` (فاز ۴)
- `logic_audit_findings` (فاز ۵)
- `notification_audit` (فاز ۶)
### Acceptance Criteria فاز ۷
- **manual** path: پرامپت‌های جدید برای ۳ ایده‌ی نمونه شامل `behavior` + `acceptance_signal` + `business_intent` + `alternative_implementations` + `false_positive_guard`
- **auto** path: همان ساختار برای task های scan-generated
- چک‌لیست‌ها شامل `behavior_observable` در هر step
- mode `auto` در `_ai_plan_steps_from_idea` کار می‌کند برای هر دو
- verify روی same tasks: false-negative ≤ ۲۰%، false-positive ≤ ۵%
- backward compat: AC های قدیمی (فقط text) همچنان کار می‌کنند
```

### Step 8: فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۸ از پرامپت کاربر است: به‌روزرسانی oversight_service.py در بخش auto-task creation (~4700-4740) برای اضافه کردن task_steps به auto-tasks با استفاده از _ai_plan_steps_from_idea و رعایت auto_task_checklist_mode. همچنین باید Telegram notification template جدید برای auto-scan task ها در notification_service.py پیاده‌سازی شود که شامل caption با فیلدهای title, business_intent, checklist count, logic concerns, dependent changes, outcome impact, links باشد. scan-completion notification نیز باید با خلاصه کامل و bundle PDF و inspector session link ارسال شود. فیلدهای WatchedProject.auto_task_notify_sound و WatchedProject.scan_notify_sound باید در UI با checkbox '🔔 صدای notification برای تسک‌های auto-scan' قابل تنظیم باشند. oversight_mega_bundle.py باید برای تولید scan-bundle PDF به‌روزرسانی شود. خارج از این مرحله: پیاده‌سازی فازهای ۹ و ۱۰. نکته حیاتی: این فاز پوشش R5 و R6 را کامل می‌کند و باید بعد از scan همه auto-tasks در UI چک‌لیست داشته باشند و caption تلگرام شامل همه فیلدها باشد.
**Excerpt:**
```
## 🅷 فاز ۸ — Auto-tasks Like Manual + Full Telegram Integration
> پوشش **R5, R6** — تسک‌های scan باید مثل manual باشند
### مسئله (R5, R6)
- تسک‌های auto-scan الان `prompt + AC` دارند ولی `task_steps` ندارند
- Telegram notification برای آن‌ها audit نشده
- کاربر می‌خواهد silent default
### تغییرات
**۸-A. task_steps برای auto-tasks:**
در `oversight_service.py:4700-4740`:
- بعد از `build_strong_prompt`، اگر `auto_task_checklist_mode != "never"`:
  - if `mode == "always"` → همیشه call `_ai_plan_steps_from_idea`
  - if `mode == "auto"` → AI تصمیم می‌گیرد بر اساس پیچیدگی
**۸-B. Telegram Notification Template (R6):**
`notification_service.py`:
```python
# Caption template برای auto-scan task
{
    "🤖 تسک جدید از scan خودکار",
    "📌 {title}",
    "🎯 {business_intent[:200]}",
    "📋 چک‌لیست: {N step}" if has_steps else None,
    "🧠 Logic concerns: {logic_audit_count}" if has_logic_issues else None,
    "🔄 وابسته به تغییر: {dependent_changes}" if delta_dependent else None,
    "📊 Outcome impact: {outcome_score}" if outcome_data else None,
    "🔗 [لینک کارت]",
    "🔍 [Inspector session]",
}
# Attachments:
- prompt_full.md (full task prompt)
- scan_inspector_session_link
```
**۸-C. Silent default + UI control:**
`WatchedProject` fields:
```python
auto_task_notify_sound: bool = False     # silent for auto-tasks
scan_notify_sound: bool = False           # silent for scan completion
```
در UI: checkbox `🔔 صدای notification برای تسک‌های auto-scan`.
**۸-D. Scan-completion notification:**
علاوه بر notification هر task، یک scan-completion notification:
```
✅ scan تکمیل شد — {project_name}
📊 خلاصه:
- {N} task جدید ایجاد شد
- {N} delta change شناسایی
- {N} stale item
- {N} logic issue
- {N} notification audit
📎 Bundle PDF (با همه screenshots و findings)
🔍 Inspector session: [link]
```
### Acceptance Criteria فاز ۸
- بعد از scan: همه auto-tasks در UI چک‌لیست دارند (اگر mode=auto و پیچیده‌اند)
- caption تلگرام شامل همه فیلدها
- silent default = true
- scan_completed notification با Bundle PDF + Inspector session link
- regression: تسک‌های دستی رفتار قبلی را حفظ کنند
```

### Step 9: فاز ۹ — UI Redesign (R2, R8)
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۹ از پرامپت کاربر است: بازطراحی کامل UI در frontend/src/app/oversight/page.tsx. scan_depth modes باید به ۴ حالت جدید (quick 5 pass, balanced 8 pass پیش‌فرض جدید, deep 12 pass, ultra 12 pass) تغییر یابد. criteria_weights باید به preset selector با گزینه‌های general, security-first, tests-first, feature-completeness, logic-quality تغییر کند و sliders در <details> (advanced) قرار گیرند. layout باید به ۴ tab (Coverage, Intelligence, Lifecycle, Notifications) بازطراحی شود. ۱۰ فیلد جدید به WatchedProject اضافه شود (inventory_layers, stale_detection_enabled, delta_analysis_enabled, runtime_discovery_enabled, outcome_data_enabled, logic_audit_enabled, notification_audit_enabled, inspector_session_enabled, auto_task_checklist_mode, cleanup_tasks_enabled). هر گزینه باید help icon ⓘ با AI-generated description (از فاز ۲) داشته باشد. Feature Inventory Panel (از فاز ۲) نیز باید در UI نمایش داده شود. خارج از این مرحله: پیاده‌سازی فاز ۱۰. نکته حیاتی: این فاز پوشش R2 را فراهم می‌کند و باید backward compatibility با scan_depth قدیمی حفظ شود.
**Excerpt:**
```
## 🅸 فاز ۹ — UI Redesign (R2, R8)
> پوشش **R2** — به‌روزرسانی گزینه‌ها
### مسئله (R2)
> «گزینه‌هاش هم به روز رسانی بشن»
### تغییرات
**۹-A. بازنگری scan_depth modes:**
از (فعلی):
```
quick (3 pass) | standard (6 pass) | deep (12 pass، پیش‌فرض) | thorough (12 + health + roadmap)
```
به:
```
⚡ quick (5 pass) — frontend + backend + security + delta + stale
⚖️ balanced (8 pass — پیش‌فرض جدید) — quick + dependency + completeness + runtime
🔬 deep (12 pass) — همه + impact analysis
🧠 ultra (12 pass + logic audit + outcome + notification audit + inspector session)
```
**۹-B. بازنگری criteria_weights:**
به‌جای ۶ slider:
- preset selector: "general", "security-first", "tests-first", "feature-completeness", "logic-quality"
- sliders در `<details>` (advanced)
**۹-C. بازنگری Smart Task Lifecycle:**
- بررسی هر گزینه: کار می‌کند؟
- label با وضعیت فعلی
**۹-D. گزینه‌های جدید Phase 5:**
`WatchedProject` فیلدهای جدید (همه با default sensible):
```python
# Coverage
inventory_layers: List[str] = ["all"]    # یا subset
# Intelligence
stale_detection_enabled: bool = True
delta_analysis_enabled: bool = True
runtime_discovery_enabled: bool = True
outcome_data_enabled: bool = True
logic_audit_enabled: bool = True
notification_audit_enabled: bool = True
inspector_session_enabled: bool = True   # R14
# Lifecycle
auto_task_checklist_mode: str = "auto"   # R5
cleanup_tasks_enabled: bool = True
# Notifications
auto_task_notify_sound: bool = False     # R6
scan_notify_sound: bool = False          # R6
```
**۹-E. بازطراحی Layout — ۴ tabs (به‌جای ۳):**
| Tab | محتوا |
|---|---|
| **🔍 Coverage** | scan_depth + inventory_layers + criteria preset + auto_models |
| **🧠 Intelligence** | stale + delta + runtime + outcome + logic_audit + smart-prompt + inspector_session |
| **🔁 Lifecycle** | dedup + auto-regenerate + checklist mode + cleanup |
| **🔔 Notifications** | auto_task_sound + scan_sound + notification_audit + custom templates |
**۹-F. AI explanation برای هر گزینه (R8):**
هر گزینه دارای help icon ⓘ + AI-generated description (از فاز ۲):
- این گزینه دقیقاً چه می‌کند
- کِی اضافه شد
- وضعیت فعلی (active / possibly stale / unknown)
**۹-G. Feature Inventory Panel (R8):**
panel جدید در UI با لیست همه options + AI explanation (از فاز ۲).
### Acceptance Criteria فاز ۹
- در UI، ۴ tab وجود دارد
- mode `balanced` پیش‌فرض جدید
- mode `ultra` در دسترس
- backward compat: scan_depth قدیمی همچنان کار کند
- هر گزینه دارای tooltip + AI explanation
- Feature Inventory Panel functional
```

### Step 10: فاز ۱۰ — Meta-validation
**Status:** `done` (100%)
**Scope:** این مرحله شامل پیاده‌سازی کامل فاز ۱۰ از پرامپت کاربر است: تبدیل این پرامپت به task با ۱۰ task_step (مطابق فازهای ۱ تا ۹) و AC های قابل-verify دقیق. هر AC باید measurable باشد (مثلاً '≥ ۵ stale item کشف شود'). باید یک iteration loop پیاده‌سازی شود: پیاده‌سازی → verify deep (Phase 4) → نتیجه. اگر done < 100% iteration بعدی، اگر done = 100% فاز بعدی. log در phase5_meta_validation.md ذخیره شود. همچنین باید verify bugs شناسایی شوند: اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify، اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive). خارج از این مرحله: هیچ فاز دیگری وجود ندارد. نکته حیاتی: این فاز خود-ارجاعی است و باید بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد.
**Excerpt:**
```
## 🅹 فاز ۱۰ — Meta-validation
> خود این پرامپت قابل verify باشد
### مسئله
کاربر می‌خواهد این پرامپت به task تبدیل شود و توسط verify Phase 4 سنجیده شود.
### تغییرات
**۱۰-A. این پرامپت به task تبدیل:**
- ایده‌ی خام = این متن
- AC ها = AC های هر فاز ۱-۹
- task_steps = ۱۰ فاز
**۱۰-B. AC قابل-verify دقیق:**
هر AC در هر فاز measurable نوشته شده (مثلاً «≥ ۵ stale item کشف شود»).
**۱۰-C. Iteration loop:**
- پیاده‌سازی → verify deep (Phase 4) → نتیجه
- اگر `done < 100%` → iteration بعدی
- اگر `done = 100%` → فاز بعدی
- log در `phase5_meta_validation.md`
**۱۰-D. شناسایی verify bugs:**
- اگر کار واقعاً انجام شد ولی verify گفت not_done → باگ verify
- اگر کار نشد ولی verify گفت done → باگ verify (مهم‌تر، false-positive)
- log همه‌ی این موارد
### Acceptance Criteria فاز ۱۰
- پرامپت نهایی شامل ۱۰ task_step
- هر step دارای `behavior_observable` + `verification_hint`
- بعد از پیاده‌سازی همه فازها، verify deep این task را ≥ ۱۰/۱۰ done گزارش دهد
- log باگ‌های verify identified
```
