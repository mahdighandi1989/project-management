---
task_id: 64f13408-2972-4b0f-90ea-200c623642df
title: '[Effectiveness] عدم یکپارچگی notification_service با خطاهای واقعی کاربران'
type: logic_audit
priority: medium
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T06:07:23.129690+00:00'
updated_at: '2026-05-20T04:23:36.008124+00:00'
archived: true
archived_at: '2026-05-18T04:26:02.342574+00:00'
tags:
- merged
---

# [Effectiveness] عدم یکپارچگی notification_service با خطاهای واقعی کاربران

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
پس از اتصال notification_service به metrics خطا، ۱۰۰٪ خطاهای بحرانی در کمتر از ۵ دقیقه به تیم اطلاع‌رسانی شوند

## 📊 وضعیت فعلی
notification_service رویدادهای verify/scan/task را مدیریت می‌کند اما به error_rate واقعی متصل نیست

## 🛠 اقدام پیشنهادی
اتصال notification_service به metrics dashboard و ارسال خودکار هشدار برای error_rate > 1%

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.
---
[scan #2 at 2026-05-17T07:14:59.822550+00:00]
## 🎯 هدف مطلوب (outcome target)
تعریف و ثبت حداقل 3 KPI عملیاتی (مانند میانگین زمان پاسخ AI، نرخ موفقیت fallback، درصد نوتیفیکیشن‌های تحویل داده شده) در 30 روز آینده

## 📊 وضعیت فعلی
تنها error_rate و warn_count ثبت شده که هر دو صفر هستند، اما هیچ معیاری برای سنجش کیفیت پاسخ‌های AI یا عملکرد سرویس‌ه
---
[scan #3 at 2026-05-17T07:22:07.179584+00:00]
## 🎯 هدف مطلوب (outcome target)
ثبت و مانیتور کردن نرخ تحویل موفق برای هر نوع event (حداقل 95% تحویل موفق) در 30 روز آینده

## 📊 وضعیت فعلی
100 info log در 30 روز ثبت شده اما هیچ تفکیکی بر اساس نوع event یا نرخ موفقیت تحویل وجود ندارد

## 🛠 اقدام پیشنهادی
افزودن logging سطح‌بندی شده به notification_

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] عدم یکپارچگی notification_service با خطاهای واقعی کاربران

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
پس از اتصال notification_service به metrics خطا، ۱۰۰٪ خطاهای بحرانی در کمتر از ۵ دقیقه به تیم اطلاع‌رسانی شوند

## 📊 وضعیت فعلی
notification_service رویدادهای verify/scan/task را مدیریت می‌کند اما به error_rate واقعی متصل نیست

## 🛠 اقدام پیشنهادی
اتصال notification_service به metrics dashboard و ارسال خودکار هشدار برای error_rate > 1%

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: static)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: static)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: static)_

## Task Steps

### Step 1: بررسی وضعیت فعلی notification_service و metrics dashboard در repo
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن فایل‌های مرتبط با notification_service، metrics dashboard، error_rate، و هرگونه اتصال موجود بین آن‌ها است. باید مشخص شود که آیا قبلاً بخشی از این کار انجام شده یا خیر. همچنین باید ساختار فایل‌های موجود، endpointها، و نحوه مدیریت رویدادهای verify/scan/task بررسی شود. این مرحله شامل هیچ تغییری در کد نیست، فقط بررسی و مستندسازی وضعیت فعلی است.
**Excerpt:**
```
notification_service رویدادهای verify/scan/task را مدیریت می‌کند اما به error_rate واقعی متصل نیست
اتصال notification_service به metrics dashboard و ارسال خودکار هشدار برای error_rate > 1%
ممکن است بخشی یا تمامِ این درخواست قبلاً (به صورت کامل یا ناقص) در repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های مرتبط بررسی کن که چه چیزی از قبل وجود دارد.
```

### Step 2: تعریف outcome target قابل اندازه‌گیری برای error_rate
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تعریف یک معیار دقیق و قابل اندازه‌گیری برای error_rate است. باید مشخص شود که error_rate چگونه محاسبه می‌شود (مثلاً تعداد خطاهای بحرانی / کل درخواست‌ها در بازه زمانی ۵ دقیقه)، آستانه ۱٪ به چه معناست، و چه نوع خطاهایی 'بحرانی' محسوب می‌شوند. این تعریف باید در یک فایل پیکربندی یا داکیومنت ذخیره شود. خارج از این مرحله: پیاده‌سازی کد برای اندازه‌گیری.
**Excerpt:**
```
پس از اتصال notification_service به metrics خطا، ۱۰۰٪ خطاهای بحرانی در کمتر از ۵ دقیقه به تیم اطلاع‌رسانی شوند
ارسال خودکار هشدار برای error_rate > 1%
outcome target به‌صورت measurable بازنویسی شد
```

### Step 3: اضافه کردن metrics collection برای error_rate در notification_service
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن کد به notification_service برای جمع‌آوری metrics مربوط به error_rate است. باید یک metric counter یا gauge برای تعداد خطاهای بحرانی و تعداد کل درخواست‌ها ایجاد شود. این metrics باید در یک بازه زمانی مشخص (مثلاً ۵ دقیقه) محاسبه شوند. خارج از این مرحله: اتصال به dashboard یا ارسال notification.
**Excerpt:**
```
اتصال notification_service به metrics dashboard و ارسال خودکار هشدار برای error_rate > 1%
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 4: ایجاد endpoint برای metrics dashboard در notification_service
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint HTTP (مثلاً /metrics) در notification_service است که metrics جمع‌آوری شده (از جمله error_rate) را در قالبی قابل خواندن توسط Prometheus یا ابزار مشابه ارائه دهد. این endpoint باید metrics را در فرمت text/plain با ساختار Prometheus exposition format برگرداند. خارج از این مرحله: تنظیم actual dashboard در Grafana یا ابزار مشابه.
**Excerpt:**
```
اتصال notification_service به metrics dashboard
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 5: پیاده‌سازی منطق تشخیص error_rate > 1% و فعال‌سازی هشدار
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن منطق در notification_service است که به‌طور مداوم error_rate را بررسی کند و در صورت تجاوز از آستانه ۱٪، یک رویداد هشدار (alert event) ایجاد کند. این منطق باید در یک بازه زمانی configurable (مثلاً هر ۱ دقیقه) اجرا شود. خارج از این مرحله: ارسال actual notification (ایمیل، پیام، etc.) — فقط ایجاد رویداد هشدار.
**Excerpt:**
```
ارسال خودکار هشدار برای error_rate > 1%
پس از اتصال notification_service به metrics خطا، ۱۰۰٪ خطاهای بحرانی در کمتر از ۵ دقیقه به تیم اطلاع‌رسانی شوند
```

### Step 6: اتصال رویداد هشدار به کانال‌های اطلاع‌رسانی (Slack/Email/Webhook)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی ارسال notification واقعی به تیم است. باید یک یا چند کانال اطلاع‌رسانی (مثلاً Slack webhook، ایمیل، یا SMS) پیکربندی شود. رویداد هشدار ایجاد شده در مرحله قبل باید به این کانال‌ها ارسال شود. خارج از این مرحله: تنظیم دقیق قالب پیام یا شخصی‌سازی.
**Excerpt:**
```
۱۰۰٪ خطاهای بحرانی در کمتر از ۵ دقیقه به تیم اطلاع‌رسانی شوند
ارسال خودکار هشدار برای error_rate > 1%
```

### Step 7: نوشتن تست E2E برای سناریوی error_rate > 1% و اطلاع‌رسانی
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست end-to-end است که سناریوی کامل را شبیه‌سازی می‌کند: ایجاد خطاهای کافی برای رسیدن error_rate به بیش از ۱٪، و سپس بررسی اینکه notification در کانال مشخص شده ارسال می‌شود. تست باید مستقل از سرویس‌های خارجی باشد (با mock کردن Slack/Email). خارج از این مرحله: تست‌های unit برای تک تک توابع.
**Excerpt:**
```
test E2E که outcome را اندازه می‌گیرد عبور می‌کند
هیچ تستی fail نمی‌شود (npm run test / pytest)
```

### Step 8: نوشتن تست‌های unit برای منطق محاسبه error_rate و تشخیص آستانه
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های unit برای توابع جدیدی است که در مراحل قبل اضافه شده‌اند: تابع محاسبه error_rate، تابع بررسی آستانه، و تابع ایجاد رویداد هشدار. تست‌ها باید موارد مرزی (boundary cases) را پوشش دهند. خارج از این مرحله: تست‌های integration یا E2E.
**Excerpt:**
```
هیچ تستی fail نمی‌شود (npm run test / pytest)
نوشتن تست Z
```

### Step 9: اجرای linter و type-checker و رفع خطاها
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای linter (مثلاً flake8 یا ESLint) و type-checker (مثلاً mypy یا tsc) روی کل پروژه و رفع هرگونه warning یا error است. باید اطمینان حاصل شود که کد جدید با استانداردهای موجود هماهنگ است. خارج از این مرحله: تغییر در منطق کسب‌وکار.
**Excerpt:**
```
linter بدون warning عبور می‌کند
type-check موفق است (tsc --noEmit / mypy)
```

### Step 10: بررسی نهایی و مستندسازی تغییرات در README یا CHANGELOG
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی نهایی تمام تغییرات ایجاد شده، اطمینان از اینکه تمام Acceptance Criteria برآورده شده‌اند، و به‌روزرسانی مستندات پروژه (README، CHANGELOG، یا هر داکیومنت مرتبط) با توضیح feature جدید است. خارج از این مرحله: تغییرات بیشتر در کد.
**Excerpt:**
```
outcome target به‌صورت measurable بازنویسی شد
کد تغییر کرد تا outcome target محقق شود
هیچ تستی fail نمی‌شود
linter بدون warning عبور می‌کند
type-check موفق است
```
