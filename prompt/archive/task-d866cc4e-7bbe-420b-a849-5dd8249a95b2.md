---
task_id: d866cc4e-7bbe-420b-a849-5dd8249a95b2
title: '[منطق] عدم وجود hallucination guards در سطح pipeline'
type: logic_audit
priority: high
execution_priority: 100
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T10:28:53.067760+00:00'
updated_at: '2026-05-29T20:13:15.264037+00:00'
archived: true
archived_at: '2026-05-18T04:27:44.357467+00:00'
tags:
- merged
---

# [منطق] عدم وجود hallucination guards در سطح pipeline

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.

## 💥 پیامد (impact)
AI ممکن است اطلاعات نادرست، مسیرهای فایل غیرواقعی، یا کدهای غیرقابل اجرا تولید کند که مستقیماً به کاربر نهایی تحویل داده شود.

## 🛠 پیشنهاد رفع اولیه
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-15T17:35:35.740359+00:00]
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

در هیچ‌کدام از کامپوننت‌های معرفی‌شده (ai_manager, oversight_strong_prompt, prompt_helper) مکانیزمی برای کاهش hallucination دیده نمی‌شود. مواردی مانند grounding به منابع معتبر، fact-checking خودکار، یا محدودیت‌های confidence در خرو

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود hallucination guards در سطح pipeline

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.

## 💥 پیامد (impact)
AI ممکن است اطلاعات نادرست، مسیرهای فایل غیرواقعی، یا کدهای غیرقابل اجرا تولید کند که مستقیماً به کاربر نهایی تحویل داده شود.

## 🛠 پیشنهاد رفع اولیه
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی وضعیت فعلی pipeline ai_llm و شناسایی فایل‌های مرتبط
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن تمام فایل‌های مرتبط با pipeline ai_llm است. باید فایل‌های oversight_strong_prompt، ai_manager، و هر کامپوننت دیگری که در این pipeline وجود دارد شناسایی شوند. همچنین باید بررسی شود که آیا قبلاً مکانیزم‌های post-processing یا validation محتوایی وجود دارد یا خیر. این مرحله فقط شامل شناسایی و مستندسازی است، نه تغییر در کد.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد: هیچ یک از کامپوننت‌ها شامل مکانیزم‌های تشخیص یا کاهش hallucination نیستند. oversight_strong_prompt فقط یک template تولید می‌کند و ai_manager فقط مدل را انتخاب می‌کند. هیچ validation محتوایی (مانند بررسی consistency با context اصلی یا فایل‌های مرجع) وجود ندارد.
```

### Step 2: طراحی و پیاده‌سازی کلاس PostProcessor برای اعتبارسنجی مسیرهای فایل
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک کلاس جدید به نام PostProcessor (یا مشابه) است که وظیفه دارد مسیرهای فایل ذکر شده در خروجی AI را با target_locations تطبیق دهد. این کلاس باید بتواند مسیرهای فایل را از متن استخراج کند (با regex یا روش مشابه)، آن‌ها را با لیست target_locations مقایسه کند، و در صورت عدم تطابق، هشدار دهد یا مسیر را تصحیح کند. این مرحله فقط بخش اول از سه بخش پیشنهاد رفع اولیه را پوشش می‌دهد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.
```

### Step 3: پیاده‌سازی ماژول SyntaxChecker برای اعتبارسنجی syntax کدهای تولید شده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول یا کلاس جدید به نام SyntaxChecker است که وظیفه دارد کدهای تولید شده توسط AI را از نظر syntax بررسی کند. این ماژول باید بتواند کد را از خروجی استخراج کند (با تشخیص بلوک‌های کد)، زبان برنامه‌نویسی را تشخیص دهد، و با استفاده از ابزارهای مناسب (مانند ast.parse برای Python، یا subprocess برای سایر زبان‌ها) syntax را بررسی کند. این مرحله بخش دوم از سه بخش پیشنهاد رفع اولیه را پوشش می‌دهد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.
```

### Step 4: پیاده‌سازی ماژول FactChecker با استفاده از LLM دوم برای اعتبارسنجی محتوا
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک ماژول جدید به نام FactChecker است که از یک LLM دوم (مدل متفاوت یا همان مدل با پرامپت متفاوت) برای fact-checking خروجی AI استفاده می‌کند. این ماژول باید خروجی اصلی AI را به همراه context اصلی (مانند فایل‌های مرجع، پرامپت کاربر) به LLM دوم بدهد و از آن بخواهد consistency را بررسی کند. نتایج fact-checking باید به خروجی نهایی اضافه شود (مثلاً به‌عنوان هشدار یا annotation). این مرحله بخش سوم از سه بخش پیشنهاد رفع اولیه را پوشش می‌دهد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.
```

### Step 5: یکپارچه‌سازی PostProcessor، SyntaxChecker و FactChecker در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یکپارچه‌سازی سه ماژول ایجاد شده (PostProcessor، SyntaxChecker، FactChecker) در pipeline ai_llm است. باید یک orchestrator یا pipeline جدید ایجاد شود که این سه مرحله را به ترتیب اجرا کند: ابتدا تطبیق مسیرها، سپس بررسی syntax، و در نهایت fact-checking. خروجی نهایی باید شامل نتایج هر سه مرحله باشد (هشدارها، تصحیحات، annotations). این مرحله همچنین شامل به‌روزرسانی oversight_strong_prompt و ai_manager برای استفاده از این pipeline جدید است.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.
```

### Step 6: نوشتن تست‌های واحد برای PostProcessor، SyntaxChecker و FactChecker
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد (unit tests) برای هر یک از سه ماژول جدید است. برای PostProcessor: تست تطبیق مسیرهای معتبر و نامعتبر. برای SyntaxChecker: تست کدهای معتبر و نامعتبر در زبان‌های مختلف. برای FactChecker: تست consistency و inconsistency با context. این تست‌ها باید در فایل‌های تست موجود یا جدید اضافه شوند و با pytest (یا فریم‌ورک تست موجود) اجرا شوند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 7: نوشتن تست‌های یکپارچه‌سازی برای pipeline جدید
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی (integration tests) است که pipeline کامل ai_llm را با ماژول‌های جدید آزمایش می‌کند. این تست‌ها باید سناریوهای مختلف را پوشش دهند: خروجی کاملاً معتبر، خروجی با مسیرهای نامعتبر، خروجی با syntax errors، خروجی با inconsistency محتوایی، و ترکیبی از این موارد. تست‌ها باید بررسی کنند که pipeline به درستی خطاها را شناسایی و گزارش می‌کند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 8: به‌روزرسانی مستندات و اضافه کردن کامنت‌های توضیحی در کد
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل به‌روزرسانی مستندات موجود (مانند README، docs/) برای توضیح مکانیزم‌های جدید ضد hallucination است. همچنین باید کامنت‌های توضیحی به کدهای جدید اضافه شود تا خوانایی و نگهداری کد بهبود یابد. این مرحله شامل ایجاد یک فایل مستندات جدید (مثلاً docs/hallucination_guards.md) است که معماری و نحوه استفاده از ماژول‌های جدید را توضیح می‌دهد.
**Excerpt:**
```
یک لایه post-processing اضافه کنید که: 1) مسیرهای فایل ذکر شده در خروجی را با target_locations تطبیق دهد، 2) کدهای تولید شده را از نظر syntax بررسی کند، 3) از یک LLM دوم برای fact-checking استفاده کند.
```
