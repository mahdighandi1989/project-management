---
task_id: 708bd0c0-8018-4965-8056-3f7ffed447f6
title: '[منطق] عدم وجود مکانیزم اعتبارسنجی خروجی AI در ai_manager.py'
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T19:16:52.969939+00:00'
updated_at: '2026-05-20T04:25:22.662762+00:00'
---

# [منطق] عدم وجود مکانیزم اعتبارسنجی خروجی AI در ai_manager.py

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

در مستندات ai_manager.py هیچ اشاره‌ای به validation روی پاسخ‌های AI یا بررسی ساختار خروجی با output parser نشده است. این می‌تواند منجر به پردازش پاسخ‌های ناقص یا نادرست شود.

## 💥 پیامد (impact)
احتمال خطاهای زنجیره‌ای در pipeline، پردازش داده‌های نامعتبر و کاهش کیفیت خروجی نهایی

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک لایه validation بعد از دریافت پاسخ از هر provider که ساختار خروجی را با schema مورد انتظار تطبیق دهد و در صورت عدم تطابق، fallback یا retry mechanism فعال شود.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود مکانیزم اعتبارسنجی خروجی AI در ai_manager.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

در مستندات ai_manager.py هیچ اشاره‌ای به validation روی پاسخ‌های AI یا بررسی ساختار خروجی با output parser نشده است. این می‌تواند منجر به پردازش پاسخ‌های ناقص یا نادرست شود.

## 💥 پیامد (impact)
احتمال خطاهای زنجیره‌ای در pipeline، پردازش داده‌های نامعتبر و کاهش کیفیت خروجی نهایی

## 🛠 پیشنهاد رفع اولیه
اضافه کردن یک لایه validation بعد از دریافت پاسخ از هر provider که ساختار خروجی را با schema مورد انتظار تطبیق دهد و در صورت عدم تطابق، fallback یا retry mechanism فعال شود.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی فرض‌های ناسازگار در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن تمام فایل‌های مرتبط با pipeline ai_llm (شامل ai_manager.py، providerها، output parserها و هر فایل دیگری که در این pipeline نقش دارد) است. هدف شناسایی و مستندسازی فرض‌های ناسازگار بین بخش‌های مختلف کد است. این مرحله شامل: (1) خواندن ai_manager.py و شناسایی نحوه دریافت پاسخ از providerها، (2) بررسی output parserهای موجود و schemaهای مورد انتظار، (3) مستندسازی جایی که validation وجود ندارد و جایی که انتظار می‌رود وجود داشته باشد. خارج از این مرحله است: تغییر کد، نوشتن تست، یا پیاده‌سازی validation. نکته حیاتی: این مرحله صرفاً تشخیصی و مستندسازی است و باید خروجی آن یک گزارش مکتوب (مثلاً در فایل docs/ یا کامیت message) باشد.
**Excerpt:**
```
در مستندات ai_manager.py هیچ اشاره‌ای به validation روی پاسخ‌های AI یا بررسی ساختار خروجی با output parser نشده است. این می‌تواند منجر به پردازش پاسخ‌های ناقص یا نادرست شود. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 2: تعیین ground truth و align کردن فرض‌ها در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تصمیم‌گیری در مورد ground truth (حقیقت مرجع) برای ساختار خروجی مورد انتظار از هر provider در pipeline ai_llm است. بر اساس گزارش مرحله 1، باید مشخص شود که کدام فرض درست است و کدام طرف باید align شود. این شامل: (1) تعریف schema خروجی مورد انتظار برای هر provider (مثلاً Pydantic model یا dataclass)، (2) به‌روزرسانی مستندات ai_manager.py و فایل‌های مرتبط برای انعکاس این ground truth، (3) اصلاح هرگونه فرض نادرست در کد موجود (مثلاً اگر output parser انتظار فیلدی را دارد که provider برنمی‌گرداند). خارج از این مرحله است: پیاده‌سازی validation mechanism، نوشتن تست‌های integration. نکته حیاتی: این مرحله باید ground truth را به‌صورت صریح و قابل اجرا (مثلاً به صورت type hint یا schema class) تعریف کند.
**Excerpt:**
```
ground truth تعیین شد و طرف دیگر align شد. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 3: اضافه کردن لایه validation پس از دریافت پاسخ از هر provider
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک لایه validation است که بلافاصله پس از دریافت پاسخ از هر provider در ai_manager.py اجرا می‌شود. این لایه باید ساختار خروجی را با schema مورد انتظار (که در مرحله 2 تعریف شده) تطبیق دهد. این شامل: (1) ایجاد یک تابع/کلاس validator که پاسخ raw را گرفته و با schema مقایسه می‌کند، (2) در صورت تطابق، پاسخ را به مرحله بعد pipeline ارسال کند، (3) در صورت عدم تطابق، خطای مناسب تولید کند. خارج از این مرحله است: پیاده‌سازی fallback یا retry mechanism (مرحله بعد)، نوشتن تست‌های integration (مرحله بعد). نکته حیاتی: این validation باید در جریان اصلی (main flow) قرار گیرد، نه به‌صورت اختیاری.
**Excerpt:**
```
اضافه کردن یک لایه validation بعد از دریافت پاسخ از هر provider که ساختار خروجی را با schema مورد انتظار تطبیق دهد و در صورت عدم تطابق، fallback یا retry mechanism فعال شود.
```

### Step 4: پیاده‌سازی fallback mechanism برای پاسخ‌های نامعتبر
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اضافه کردن یک fallback mechanism است که در صورت عدم تطابق پاسخ با schema (که در مرحله 3 شناسایی می‌شود) فعال می‌گردد. این mechanism باید: (1) یک provider جایگزین (fallback provider) را امتحان کند، (2) اگر fallback provider هم پاسخ نامعتبر داد، یک پاسخ پیش‌فرض (default response) برگرداند، (3) تمام این رویدادها را log کند. خارج از این مرحله است: retry mechanism (مرحله بعد)، نوشتن تست‌های integration (مرحله بعد). نکته حیاتی: fallback provider باید از قبل تعریف شده باشد (مثلاً در config) و نباید باعث حلقه بی‌نهایت شود.
**Excerpt:**
```
در صورت عدم تطابق، fallback یا retry mechanism فعال شود.
```

### Step 5: پیاده‌سازی retry mechanism برای پاسخ‌های نامعتبر
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اضافه کردن یک retry mechanism است که در صورت عدم تطابق پاسخ با schema (قبل از fallback) فعال می‌شود. این mechanism باید: (1) حداکثر تعداد تلاش مجدد (مثلاً 3 بار) را تعریف کند، (2) بین تلاش‌ها از exponential backoff استفاده کند، (3) اگر بعد از حداکثر تلاش‌ها پاسخ همچنان نامعتبر بود، به fallback mechanism (مرحله 4) واگذار کند. خارج از این مرحله است: fallback mechanism (مرحله قبل)، نوشتن تست‌های integration (مرحله بعد). نکته حیاتی: retry باید با احتیاط پیاده‌سازی شود تا باعث افزایش latency بی‌رویه نشود.
**Excerpt:**
```
در صورت عدم تطابق، fallback یا retry mechanism فعال شود.
```

### Step 6: نوشتن integration test برای pipeline ai_llm با سناریوی validation موفق
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test است که pipeline ai_llm را با یک پاسخ معتبر از provider تست می‌کند. این test باید: (1) یک mock provider با پاسخ معتبر (مطابق schema) راه‌اندازی کند، (2) pipeline را اجرا کند، (3) تأیید کند که validation پاس شده و خروجی نهایی صحیح است. خارج از این مرحله است: تست سناریوهای خطا (مرحله بعد)، تست fallback/retry (مرحله بعد). نکته حیاتی: این test باید مستقل از provider واقعی باشد و از mocking استفاده کند.
**Excerpt:**
```
integration test برای pipeline `ai_llm` بدون شکست عبور
```

### Step 7: نوشتن integration test برای سناریوی پاسخ نامعتبر و fallback
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test است که سناریوی پاسخ نامعتبر از provider اول و fallback به provider دوم را تست می‌کند. این test باید: (1) یک mock provider اول با پاسخ نامعتبر راه‌اندازی کند، (2) یک mock provider دوم با پاسخ معتبر راه‌اندازی کند، (3) تأیید کند که fallback mechanism فعال شده و خروجی نهایی از provider دوم گرفته شده است. خارج از این مرحله است: تست retry mechanism (مرحله بعد). نکته حیاتی: این test باید تأیید کند که fallback به درستی log شده است.
**Excerpt:**
```
integration test برای pipeline `ai_llm` بدون شکست عبور
```

### Step 8: نوشتن integration test برای سناریوی retry و سپس fallback
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test است که سناریوی retry mechanism را تست می‌کند: (1) یک mock provider که در 2 بار اول پاسخ نامعتبر و در بار سوم پاسخ معتبر می‌دهد، (2) تأیید می‌کند که retry mechanism فعال شده و بعد از تلاش مجدد موفق، pipeline ادامه می‌یابد. همچنین یک سناریوی دیگر که بعد از حداکثر retryها همچنان نامعتبر است و fallback فعال می‌شود. خارج از این مرحله است: تست‌های unit برای validator (مرحله بعد). نکته حیاتی: این test باید exponential backoff را نیز mock کند تا test سریع اجرا شود.
**Excerpt:**
```
integration test برای pipeline `ai_llm` بدون شکست عبور
```

### Step 9: نوشتن unit test برای validator (تابع اعتبارسنجی خروجی)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit test برای تابع/کلاس validator است که در مرحله 3 پیاده‌سازی شده است. این test باید: (1) موارد معتبر (valid responses) را تست کند، (2) موارد نامعتبر (invalid responses) با خطاهای مختلف (فیلد گمشده، نوع اشتباه، مقدار خارج از محدوده) را تست کند، (3) موارد مرزی (edge cases) مثل پاسخ خالی یا null را تست کند. خارج از این مرحله است: integration test (مراحل قبل). نکته حیاتی: این test باید pure unit test باشد و هیچ وابستگی به provider یا pipeline نداشته باشد.
**Excerpt:**
```
اضافه کردن یک لایه validation بعد از دریافت پاسخ از هر provider که ساختار خروجی را با schema مورد انتظار تطبیق دهد
```

### Step 10: بررسی نهایی و اجرای تمام تست‌ها (audit)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای تمام تست‌های موجود (unit و integration) برای pipeline ai_llm و اطمینان از عبور همه آن‌هاست. همچنین شامل: (1) بررسی لاگ‌ها برای اطمینان از log شدن صحیح رویدادهای validation، fallback و retry، (2) بررسی مستندات برای اطمینان از به‌روز بودن، (3) ثبت یک کامیت نهایی با خلاصه تمام تغییرات. خارج از این مرحله است: اضافه کردن feature جدید. نکته حیاتی: اگر تستی شکست خورد، باید رفع شود و دوباره اجرا شود تا همه تست‌ها پاس شوند.
**Excerpt:**
```
integration test برای pipeline `ai_llm` بدون شکست عبور
```
