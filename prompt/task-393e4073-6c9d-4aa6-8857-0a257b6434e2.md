---
task_id: 393e4073-6c9d-4aa6-8857-0a257b6434e2
title: ناسازگاری نسخه‌های وابستگی‌های فرانت‌اند (Next.js 14 با reactflow 11 و mermaid 10)
type: bug
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T19:16:11.775627+00:00'
updated_at: '2026-06-03T17:22:02.851543+00:00'
target_files:
- frontend/package.json
- frontend/next.config.js
---

# ناسازگاری نسخه‌های وابستگی‌های فرانت‌اند (Next.js 14 با reactflow 11 و mermaid 10)

## Raw Idea

در `frontend/package.json`، Next.js نسخه `^14.1.0` با react-dom `^18.2.0` تعریف شده است. کتابخانه `reactflow` نسخه `^11.11.4` به react 18 وابسته است و با Next.js 14 App Router (که از React 18.3+ استفاده می‌کند) ناسازگاری جزئی در رندر سمت سرور (SSR) دارد. همچنین `mermaid` نسخه `^10.6.1` با `@braintree/sanitize-url@6.0.4` که در `package-lock.json` دیده می‌شود، در برخی محیط‌ها باعث خطای sanitization می‌شود. این ناسازگاری‌ها در صفحه `/diagrams` و `/projects/[id]` (که از reactflow استفاده می‌کند) می‌تواند باعث شکست build یا رفتار غیرمنتظره در runtime شود.
---
[scan #2 at 2026-05-17T07:09:21.884558+00:00]
در frontend/package.json، react و react-dom هر دو نسخه '^18.2.0' دارند، اما reactflow (که وابستگی اصلی است) نیازمند react 17 یا 18 است. با این حال، در frontend/package-lock.json، react نسخه 18.3.1 و react-dom نسخه 18.3.1 نصب شده‌اند که با reactflow 11.11.4 سازگار نیستند. این ناسازگاری باعث خطاهای ru

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
ناسازگاری نسخه‌های وابستگی‌های فرانت‌اند (Next.js 14 با reactflow 11 و mermaid 10)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/package.json:1-30` — `dependencies` — وابستگی‌های مشکل‌دار
  ```json
  "mermaid": "^10.6.1",
  "reactflow": "^11.11.4",
  ```
- `frontend/next.config.js:1-10` — `nextConfig` — نیاز به اضافه کردن transpilePackages برای reactflow
  ```jsx
  const nextConfig = {
    // missing transpilePackages
  };
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Next.js 14.1 + React 18.2 + reactflow 11.11.4 + mermaid 10.6.1

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/app/diagrams/page.tsx` (سطر 245) — از mermaid استفاده می‌کند (خط 245-277)
- `frontend/src/app/projects/[id]/page.tsx` (سطر 9) — از reactflow استفاده می‌کند (خط 9-19)

## 🌐 نقشهٔ وابستگی‌ها
mermaid و reactflow هر دو در frontend/package.json تعریف شده‌اند و در دو صفحهٔ اصلی استفاده می‌شوند. ناسازگاری با Next.js 14 App Router می‌تواند build را در محیط production بشکند.

## 🔍 Context و وضعیت فعلی
در `frontend/package.json`، Next.js نسخه `^14.1.0` با react-dom `^18.2.0` تعریف شده است. کتابخانه `reactflow` نسخه `^11.11.4` به react 18 وابسته است و با Next.js 14 App Router (که از React 18.3+ استفاده می‌کند) ناسازگاری جزئی در رندر سمت سرور (SSR) دارد. همچنین `mermaid` نسخه `^10.6.1` با `@braintree/sanitize-url@6.0.4` که در `package-lock.json` دیده می‌شود، در برخی محیط‌ها باعث خطای sanitization می‌شود. این ناسازگاری‌ها در صفحه `/diagrams` و `/projects/[id]` (که از reactflow استفاده می‌کند) می‌تواند باعث شکست build یا رفتار غیرمنتظره در runtime شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] build فرانت‌اند بدون خطا انجام شود
- [ ] صفحه diagrams بدون خطای sanitization رندر شود
- [ ] صفحه project detail با reactflow بدون خطای SSR رندر شود
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. نسخه reactflow را به `^11.11.4` نگه دارید اما در `next.config.js` از `transpilePackages` استفاده کنید تا SSR غیرفعال شود. برای mermaid، نسخه را به `^11.0.0` ارتقا دهید یا از `dynamic import` با `ssr: false` در کامپوننت‌های استفاده‌کننده استفاده کنید.

## 💡 نمونه‌های قبل/بعد
**رفع mermaid با dynamic import**

_قبل:_
```
import mermaid from 'mermaid';
```

_بعد:_
```
const mermaid = dynamic(() => import('mermaid'), { ssr: false });
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `cd frontend && npm run build`
- `cd frontend && npm run lint`

## ⚠️ ریسک‌ها و موارد احتیاط
کم — تغییرات فقط در next.config.js و importها

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: high
- تخمین زمان: small

## Acceptance Criteria

1. build فرانت‌اند بدون خطا انجام شود _(verify: backend_test)_
2. صفحه diagrams بدون خطای sanitization رندر شود _(verify: ui_interaction)_
3. صفحه project detail با reactflow بدون خطای SSR رندر شود _(verify: ui_interaction)_
