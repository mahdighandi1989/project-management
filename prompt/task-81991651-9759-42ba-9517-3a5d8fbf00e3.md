---
task_id: 81991651-9759-42ba-9517-3a5d8fbf00e3
title: '[منطق] عدم تطابق در نوع خروجی streaming در ai_base.py'
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-17T08:08:02.515279+00:00'
updated_at: '2026-05-20T04:26:06.958218+00:00'
---

# [منطق] عدم تطابق در نوع خروجی streaming در ai_base.py

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در بخش expected_outputs برای ai_base.py، خروجی streaming به صورت 'AsyncIterator[str]' تعریف شده است، در حالی که در کلاس‌های پایه AI معمولاً streaming به صورت AsyncIterator[Dict] یا AsyncIterator[AIResponseChunk] است که شامل metadata و token usage نیز می‌شود. این inconsistency باعث می‌شود که مصرف‌کنندگان streaming نتوانند به اطلاعات کامل دسترسی داشته باشند.

## 💥 پیامد (impact)
در صورت استفاده از streaming، مصرف‌کننده فقط رشته‌های خالی از metadata دریافت می‌کند و نمی‌تواند finish_reason یا tokens_used را در حین streaming پردازش کند.

## 🛠 پیشنهاد رفع اولیه
نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید که شامل model_id, content, finish_reason, tokens_used و timestamp باشد.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-17T08:13:21.950147+00:00]
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در expected_outputs برای ai_base.py، فیلد metadata به صورت 'metadata, f' نوشته شده که ناقص است. همچنین نوع دقیق metadata مشخص نیست (Dict, Any, Optional). این inconsistency باعث می‌شود که مصرف‌کنندگان نتوانند به درستی metadata را پردا
---
[scan #3 at 2026-05-17T08:41:37.412747+00:00]
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در بخش expected_outputs برای ai_base.py، خروجی streaming به صورت 'AsyncIterator[str]' تعریف شده است، در حالی که خروجی غیر-streaming شامل 'AIResponse' با فیلد 'content' است. این دو نوع خروجی از نظر schema ناسازگار هستند و ممکن است مصر

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم تطابق در نوع خروجی streaming در ai_base.py

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline data است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `data` یک ناسازگاری منطقی پیدا شد:

در بخش expected_outputs برای ai_base.py، خروجی streaming به صورت 'AsyncIterator[str]' تعریف شده است، در حالی که در کلاس‌های پایه AI معمولاً streaming به صورت AsyncIterator[Dict] یا AsyncIterator[AIResponseChunk] است که شامل metadata و token usage نیز می‌شود. این inconsistency باعث می‌شود که مصرف‌کنندگان streaming نتوانند به اطلاعات کامل دسترسی داشته باشند.

## 💥 پیامد (impact)
در صورت استفاده از streaming، مصرف‌کننده فقط رشته‌های خالی از metadata دریافت می‌کند و نمی‌تواند finish_reason یا tokens_used را در حین streaming پردازش کند.

## 🛠 پیشنهاد رفع اولیه
نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید که شامل model_id, content, finish_reason, tokens_used و timestamp باشد.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `data` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `data` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و شناسایی فایل‌های مرتبط با ناسازگاری streaming در ai_base.py
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو و شناسایی دقیق فایل‌های موجود در repository است که با ناسازگاری نوع خروجی streaming مرتبط هستند. باید فایل ai_base.py و سایر فایل‌های pipeline data که از streaming استفاده می‌کنند یا به آن وابسته هستند، پیدا شوند. همچنین باید بررسی شود که آیا کلاس AIResponseChunk از قبل وجود دارد یا خیر. این مرحله فقط شامل شناسایی است و هیچ تغییری در کد ایجاد نمی‌کند.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد: در بخش expected_outputs برای ai_base.py، خروجی streaming به صورت 'AsyncIterator[str]' تعریف شده است، در حالی که در کلاس‌های پایه AI معمولاً streaming به صورت AsyncIterator[Dict] یا AsyncIterator[AIResponseChunk] است که شامل metadata و token usage نیز می‌شود.
```

### Step 2: ایجاد یا اصلاح کلاس AIResponseChunk با فیلدهای model_id, content, finish_reason, tokens_used, timestamp
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تعریف یک کلاس داده (dataclass) به نام AIResponseChunk است که شامل فیلدهای model_id (str), content (str), finish_reason (Optional[str]), tokens_used (Optional[int]), timestamp (datetime) باشد. اگر این کلاس از قبل در فایل دیگری (مثلاً models.py یا types.py) وجود دارد، باید بررسی شود که آیا فیلدهای مورد نیاز را دارد یا خیر. اگر وجود دارد و کامل است، این مرحله فقط به‌روزرسانی مستندات یا importها را شامل می‌شود. اگر وجود ندارد، باید در یک فایل مناسب (مثلاً ai_base.py یا یک فایل types جدید) ایجاد شود.
**Excerpt:**
```
نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید که شامل model_id, content, finish_reason, tokens_used و timestamp باشد.
```

### Step 3: تغییر type hint خروجی متد streaming در کلاس پایه AI از AsyncIterator[str] به AsyncIterator[AIResponseChunk]
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یافتن متد یا property مربوط به streaming در کلاس پایه AI (احتمالاً در ai_base.py) و تغییر type hint خروجی آن از AsyncIterator[str] به AsyncIterator[AIResponseChunk] است. همچنین باید import مربوط به AIResponseChunk اضافه شود. این تغییر فقط در signature متد اعمال می‌شود و منطق پیاده‌سازی هنوز تغییر نمی‌کند.
**Excerpt:**
```
در بخش expected_outputs برای ai_base.py، خروجی streaming به صورت 'AsyncIterator[str]' تعریف شده است... نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید
```

### Step 4: به‌روزرسانی پیاده‌سازی متد streaming در کلاس پایه AI برای تولید AIResponseChunk به جای str
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تغییر منطق متد streaming در کلاس پایه AI است تا به جای yield کردن str ساده، اشیاء AIResponseChunk را yield کند. باید مقادیر model_id (از self یا config)، content (محتوای واقعی chunk)، finish_reason (None در حین streaming، مقدار واقعی در آخرین chunk)، tokens_used (None در حین streaming، مقدار واقعی در آخرین chunk)، و timestamp (زمان حال) را پر کند. این تغییر باید backward-compatible باشد به این معنا که اگر مصرف‌کننده‌ای فقط content را می‌خواهد، بتواند از chunk.content استفاده کند.
**Excerpt:**
```
در صورت استفاده از streaming، مصرف‌کننده فقط رشته‌های خالی از metadata دریافت می‌کند و نمی‌تواند finish_reason یا tokens_used را در حین streaming پردازش کند.
```

### Step 5: به‌روزرسانی type hints و پیاده‌سازی در تمام کلاس‌های مشتق‌شده از کلاس پایه AI که متد streaming را override می‌کنند
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو برای تمام کلاس‌هایی است که از کلاس پایه AI ارث‌بری می‌کنند و متد streaming را override کرده‌اند. برای هر کدام، type hint خروجی باید به AsyncIterator[AIResponseChunk] تغییر کند و پیاده‌سازی باید به‌روزرسانی شود تا AIResponseChunk yield کند. اگر کلاسی از super().stream() استفاده می‌کند، باید بررسی شود که خروجی آن به درستی تبدیل شود. این مرحله شامل تمام کلاس‌های موجود در pipeline data است.
**Excerpt:**
```
در pipeline `data` یک ناسازگاری منطقی پیدا شد... این inconsistency باعث می‌شود که مصرف‌کنندگان streaming نتوانند به اطلاعات کامل دسترسی داشته باشند.
```

### Step 6: به‌روزرسانی مصرف‌کنندگان streaming (کدهایی که از متد streaming استفاده می‌کنند) برای کار با AIResponseChunk
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجو برای تمام مکان‌هایی است که از متد streaming کلاس پایه AI یا کلاس‌های مشتق‌شده استفاده می‌کنند (مثلاً در endpointهای API، توابع کمکی، یا سایر pipelineها). برای هر مصرف‌کننده، باید کد به‌روزرسانی شود تا به جای کار با str، با اشیاء AIResponseChunk کار کند. اگر مصرف‌کننده فقط به content نیاز دارد، باید از chunk.content استفاده کند. اگر به finish_reason یا tokens_used نیاز دارد، باید از فیلدهای مربوطه استفاده کند.
**Excerpt:**
```
در صورت استفاده از streaming، مصرف‌کننده فقط رشته‌های خالی از metadata دریافت می‌کند و نمی‌تواند finish_reason یا tokens_used را در حین streaming پردازش کند.
```

### Step 7: به‌روزرسانی endpointهای API که streaming را به client ارسال می‌کنند (احتمالاً FastAPI) برای سریالایز کردن AIResponseChunk
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یافتن endpointهای FastAPI (یا سایر فریم‌ورک‌ها) است که خروجی streaming را به client ارسال می‌کنند. باید منطق سریالایز کردن اشیاء AIResponseChunk به فرمت مناسب (مثلاً JSON lines یا SSE) اضافه شود. اگر endpoint قبلاً str را به صورت SSE ارسال می‌کرده، باید به‌روزرسانی شود تا فیلدهای AIResponseChunk را به صورت JSON در هر رویداد ارسال کند. همچنین باید type hints endpoint به‌روزرسانی شود.
**Excerpt:**
```
نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید که شامل model_id, content, finish_reason, tokens_used و timestamp باشد.
```

### Step 8: نوشتن تست‌های واحد برای کلاس AIResponseChunk و متد streaming به‌روزرسانی‌شده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای کلاس AIResponseChunk (تست ایجاد نمونه، مقادیر پیش‌فرض، و غیره) و برای متد streaming به‌روزرسانی‌شده در کلاس پایه AI است. تست‌ها باید بررسی کنند که خروجی streaming از نوع AsyncIterator[AIResponseChunk] است، فیلدها به درستی پر شده‌اند، و در آخرین chunk finish_reason و tokens_used مقادیر مناسب دارند. همچنین باید تست backward-compatibility نوشته شود که مصرف‌کننده بتواند از chunk.content استفاده کند.
**Excerpt:**
```
این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 9: نوشتن تست‌های integration برای جریان کامل streaming از کلاس AI تا endpoint API
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های integration است که جریان کامل streaming را از فراخوانی متد streaming در کلاس AI تا دریافت خروجی توسط client از طریق endpoint API تست می‌کند. تست باید بررسی کند که client اشیاء JSON با فیلدهای AIResponseChunk دریافت می‌کند و metadata به درستی منتقل می‌شود. این تست‌ها باید در سطح API (با استفاده از TestClient FastAPI) نوشته شوند.
**Excerpt:**
```
در صورت استفاده از streaming، مصرف‌کننده فقط رشته‌های خالی از metadata دریافت می‌کند و نمی‌تواند finish_reason یا tokens_used را در حین streaming پردازش کند.
```

### Step 10: بررسی و به‌روزرسانی مستندات مرتبط با streaming (docstrings، README، و مستندات API)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل به‌روزرسانی docstrings متد streaming در کلاس پایه AI و کلاس‌های مشتق‌شده برای منعکس کردن نوع خروجی جدید AIResponseChunk است. همچنین باید مستندات API (اگر وجود دارد) به‌روزرسانی شود تا ساختار جدید خروجی streaming را توضیح دهد. اگر README یا مستندات دیگری به streaming اشاره دارند، باید به‌روزرسانی شوند.
**Excerpt:**
```
نوع خروجی streaming را به AsyncIterator[AIResponseChunk] تغییر دهید که شامل model_id, content, finish_reason, tokens_used و timestamp باشد.
```
