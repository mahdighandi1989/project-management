---
task_id: 97953fa4-0454-4dd4-930f-4f6b5932b310
title: 🔄 inspector_scan_bridge.py نیازمند بررسی به‌خاطر تغییر در oversight_deep_scan_service.py
type: dependency_update
priority: high
execution_priority: 2000
status: suggested
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-24T09:11:17.860045+00:00'
updated_at: '2026-05-31T09:08:44.699042+00:00'
target_files:
- backend/app/services/inspector_scan_bridge.py
---

# 🔄 inspector_scan_bridge.py نیازمند بررسی به‌خاطر تغییر در oversight_deep_scan_service.py

## Raw Idea

## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/oversight_deep_scan_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/services/inspector_scan_bridge.py`
- 🔥 risk level: **high**
- 📝 reason: این فایل به عنوان پل ارتباطی بین اسکنرها عمل می‌کند. تغییر در ساختار داده‌های خروجی، progress reporting یا متدهای اصلی deep_scan_service می‌تواند باعث شکست کامل پل ارتباطی شود.
- 🛠 recommended action: `needs_update`

## 🤔 چرا مهم است
تغییرات در فایل upstream می‌توانند contract را عوض کنند (signature، behavior، side-effect). فایل dependent ممکن است هنوز با فرض قدیمی کار کند → cascading failure که در test اصلی فایل تغییریافته دیده نمی‌شود.
---
[scan #2 at 2026-05-24T09:11:18.470805+00:00]
## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/oversight_deep_scan_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/services/oversight_service.py`
- 🔥 risk le
---
[scan #3 at 2026-05-24T09:11:18.783778+00:00]
## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/notification_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/api/routes/notifications.py`
- 🔥 risk level: **hi
---
[scan #4 at 2026-05-24T09:11:19.084109+00:00]
## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/notification_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/services/oversight_verifier.py`
- 🔥 risk level: *
---
[scan #5 at 2026-05-24T09:11:19.379740+00:00]
## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/deploy_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/api/routes/render_logs.py`
- 🔥 risk level: **high**
- 📝

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
🔄 inspector_scan_bridge.py نیازمند بررسی به‌خاطر تغییر در oversight_deep_scan_service.py

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/inspector_scan_bridge.py`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_deep_scan_service.py` — فایل upstream که تغییر کرده
- `backend/app/core/database.py` — `inspector_scan_bridge.py` این فایل را import می‌کند
- `backend/app/models/project.py` — `inspector_scan_bridge.py` این فایل را import می‌کند
- `backend/app/services/oversight_service.py` — `inspector_scan_bridge.py` این فایل را import می‌کند
- `backend/app/models/inspector_session.py` — `inspector_scan_bridge.py` این فایل را import می‌کند
- `backend/app/api/routes/render_logs.py` — این فایل `inspector_scan_bridge.py` را import می‌کند (caller)

## 🌐 نقشهٔ وابستگی‌ها
فایل `backend/app/services/inspector_scan_bridge.py` یکی از مصرف‌کنندگان `backend/app/services/oversight_deep_scan_service.py` است.

## 🔍 Context و وضعیت فعلی
## 📋 شرح
یک فایل اخیراً تغییر کرده و این فایل (dependent) به آن وابسته است. بدون بررسی، احتمال silent regression وجود دارد.

## 🔍 جزئیات
- 📂 فایل تغییریافته: `backend/app/services/oversight_deep_scan_service.py`
- 📂 فایل وابسته (نیازمند بررسی): `backend/app/services/inspector_scan_bridge.py`
- 🔥 risk level: **high**
- 📝 reason: این فایل به عنوان پل ارتباطی بین اسکنرها عمل می‌کند. تغییر در ساختار داده‌های خروجی، progress reporting یا متدهای اصلی deep_scan_service می‌تواند باعث شکست کامل پل ارتباطی شود.
- 🛠 recommended action: `needs_update`

## 🤔 چرا مهم است
تغییرات در فایل upstream می‌توانند contract را عوض کنند (signature، behavior، side-effect). فایل dependent ممکن است هنوز با فرض قدیمی کار کند → cascading failure که در test اصلی فایل تغییریافته دیده نمی‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] diff `backend/app/services/oversight_deep_scan_service.py` بررسی شد و تأثیر بر `backend/app/services/inspector_scan_bridge.py` مستند شد
- [ ] کد dependent با contract جدید align شد
- [ ] integration test که هر دو فایل را پوشش می‌دهد عبور می‌کند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: diff آخر در `backend/app/services/oversight_deep_scan_service.py` را بخوان — چی عوض شده.
گام ۲: کد در `backend/app/services/inspector_scan_bridge.py` که این تغییر را مصرف می‌کند پیدا کن.
گام ۳: تطبیق بده + integration test بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `python -m py_compile backend/app/services/inspector_scan_bridge.py`
- `ruff check backend/app/services/inspector_scan_bridge.py`
- `pytest -x`

## ⚠️ ریسک‌ها و موارد احتیاط
اگر فایل dependent در محل‌های دیگر هم استفاده می‌شود، تغییرات آن می‌تواند آن‌ها را break کند.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: dependency_update
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. diff `backend/app/services/oversight_deep_scan_service.py` بررسی شد و تأثیر بر `backend/app/services/inspector_scan_bridge.py` مستند شد _(verify: manual_only)_
2. کد dependent با contract جدید align شد _(verify: static)_
3. integration test که هر دو فایل را پوشش می‌دهد عبور می‌کند _(verify: backend_test)_
