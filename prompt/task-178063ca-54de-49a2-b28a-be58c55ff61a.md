---
task_id: 178063ca-54de-49a2-b28a-be58c55ff61a
title: ضبط دوحالته در بازرس ویژه با استخراج هوشمند
type: idea
priority: critical
execution_priority: 1000
status: pending
external_status: pending
verification_status: partial
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-06-02T20:26:10.851899+00:00'
updated_at: '2026-06-02T21:25:56.146382+00:00'
---

# ضبط دوحالته در بازرس ویژه با استخراج هوشمند

## Raw Idea

🎬  ضبط دوحالته در تب بازرس ویژه با استخراج هوشمند
۰. یادداشت اولیه و منشأ این پرامپت
این پرامپت برای رفع یک تسک قبلی (تسک ۱۷ مرحله‌ای با عنوان «افزودن قابلیت ضبط ویدئو در تب بازرس ویژه با لاگ‌ها و صدا و تبدیل به پرامپت») ساخته شده که در ضبط با Claude Code به‌صورت انحرافی از خواستهٔ اصلی پیاده‌سازی شد:

به‌جای ضبط محتوای داخل iframe بازرس ویژه، با getDisplayMedia() از کاربر می‌پرسد کدام تب/پنجره/کل صفحه را ضبط کند
لاگ‌ها (frontend console + backend Render logs) به ضبط متصل نشده‌اند
پرامپت بصری تولیدشده ۱-۲ خط سطحی است و به جزئیات تعاملات/صفحات/کلیک‌ها اشاره نمی‌کند
تعاملات کاربر (کلیک، اسکرول، جابجایی) ردیابی نمی‌شوند
کاربر صریحاً گفته:

«این هم خیلی جالبه و بعداً در جایی میشه ازش حتما استفاده‌هایی کرد و لذا نمی‌خوام این حذف بشه بلکه می‌خوام این هم باشه ... ولی مشکل اینه که این هم اصلاً درست انجام نمی‌شه ... لطفاً هر دو رو درست کن. یعنی هم درخواست اصلیم رو که اشتباهی فهمیده شده رو به شکل کاملاً دقیق انجام بده. و دوم اینکه همین قابلیت اشتباهی هم که الان هست رو حذف نکن بلکه اصلاحش کن ... کاملاً درستش کن که بشه در زمانی هم که خواستم ازینم استفاده کنم. حتی غیر فعالش هم نکن»

پس هر دو حالت باید موجود، انتخاب‌پذیر، و کاملاً عملیاتی باشند.

۱. هدف کلان — معماری دوحالتی
دکمهٔ ضبط ویدئو در تب بازرس ویژه، با کلیک یک Picker باز می‌کند که کاربر بین این دو حالت انتخاب می‌کند:

حالت A — «ضبط بازرس ویژه» (پیش‌فرض، درخواست اصلی)
ضبط فقط از محتوای داخل ناحیهٔ مانیتور/iframe بازرس ویژه که در آن پروژهٔ منتخب لود شده
لاگ‌های frontend (console) آن iframe + لاگ‌های backend پروژهٔ منتخب (از Render Logs API)
تعاملات کاربر داخل همان iframe: کلیک، اسکرول، جابجایی صفحات، تایپ ورودی (فقط نوع event، نه محتوای حروف برای حریم خصوصی)
صدای کاربر (میکروفون) + صدای سیستم (اختیاری)
AI بصری متمرکز فقط روی همان iframe — اگر کاربر در توضیح صوتی به چیزی خارج از iframe اشاره کند، AI تشخیص می‌دهد بی‌ربط است و در پرامپت لحاظ نمی‌کند
پرامپت قوی تولیدشده ذیل همان پروژهٔ iframe ثبت یا به چت همان inspector ارسال می‌شود
حالت B — «ضبط آزاد چندصفحه‌ای» (حفظ + اصلاح حالت فعلی غلط)
ضبط با getDisplayMedia() (همان روش فعلی)
کاربر هر چیزی انتخاب می‌کند: یک تب کروم، یک پنجره، یا کل دسکتاپ
AI بصری روی فریم‌های ضبط (هر ۵ ثانیه یک screenshot) تشخیص می‌دهد در هر مقطع کاربر کجاست و چه می‌کند:
تب دیگر کروم (مرتبط با پروژهٔ inspector / خارج از پروژه)
برنامهٔ دیگر دسکتاپ (IDE، فایل‌منیجر، …)
دسکتاپ خام (icons، wallpaper)
جابجایی بین صفحات/تب‌ها/برنامه‌ها با timestamp در پرامپت ثبت می‌شود
اسکرول‌ها و کلیک‌های قابل تشخیص از مقایسهٔ فریم‌های متوالی استنتاج می‌شوند (چون event listening در سایت‌های دیگر ممکن نیست)
AI متمایز می‌کند چه فعالیتی به پروژهٔ inspector ({repo_full_name}) مرتبط است و چه فعالیتی خارج
رفتار مشترک پس از پایان (هر دو حالت)
استخراج intent از صدا: نه فقط transcribe، بلکه فهم «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه چیزی را رد می‌کند، کجا منظورش است»
ویدئو با caption شامل خلاصهٔ پرامپت به تلگرام ارسال می‌شود
فایل ویدئو پس از ارسال موفق به تلگرام، از disk backend حذف می‌شود (هیچ persistence)
اگر چک‌باکس «ارسال به مرکز نظارت» روشن بود (پیش‌فرض خاموش): یک تسک جدید زیر همان پروژهٔ iframe ایجاد می‌شود
اگر چک‌باکس خاموش بود: پرامپت به چت inspector ارسال می‌شود (همان رفتار فعلی screenshot)
۲. زیرساخت‌های موجود — استفاده مجدد، نه ساخت موازی
⛔ هشدار بحرانی — کاربر صریحاً گفته:

«این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی کنی و لازم نیست چیز موازی انجام بدی ... لذا مراقب باش خراب نکنی»

این یعنی: کد موجود را نخوان و کنار نگذار. استفاده کن. اگر تغییری در آن لازم بود، فقط extension غیرتخریبی (parameter اختیاری اضافه) — نه refactor، نه replace.

نیاز	محل دقیق موجود	امضای کلیدی
Screenshot از iframe (Playwright backend)	backend/app/api/routes/render_logs.py و endpoint POST /api/render/inspector/screenshot (در فرانت‌اند فراخوانی می‌شود از frontend/src/app/projects/[id]/page.tsx:6242)	برمی‌گرداند base64 PNG
تابع کلاینت screenshot موجود	frontend/src/app/projects/[id]/page.tsx:6196-6328 تابع takeVisualDebugScreenshot()	مرجع برای الگوی API call + log gathering
لاگ‌های backend پروژهٔ iframe	backend/app/api/routes/render_logs.py:1299-1384	GET /api/render/logs?service_ids=&minutes=&limit=&level=&search=
sync لاگ‌های backend (pull از Render API)	backend/app/api/routes/render_logs.py:1387-1508	POST /api/render/logs/fetch
تحلیل بصری با AI (auto-select مدل)	backend/app/services/oversight_inspector_bridge.py:44-242	describe_screenshot_with_vision(base64_img, user_context, page_url, dom_text) → Dict — خودش از get_vision_models() انتخاب می‌کند و اگر هیچ مدلی نیست با پیام واضح رد می‌شود
ساخت تسک از inspector با حفظ raw_idea	backend/app/api/routes/oversight.py:1606-1645	POST /api/oversight/tasks/from-inspector با FromInspectorRequest
فیلد raw_idea در مدل تسک	backend/app/services/oversight_service.py:328-407	OversightTask.raw_idea: str = "" — جدا از OversightTask.prompt
ارسال فایل به تلگرام با caption	backend/app/services/notification_service.py:579-645	TelegramChannel.send_document(file_bytes, filename, *, caption) — caption تا ۱۰۲۰ کاراکتر
state چک‌باکس "ارسال به مرکز نظارت"	frontend/src/app/projects/[id]/page.tsx:673-675	sendToOversightVisual
handler ارسال نهایی به مرکز نظارت	frontend/src/app/projects/[id]/page.tsx:5198	sendToOversight('visual_debug')
URL واقعی iframe	frontend/src/app/projects/[id]/page.tsx:6189	تابع _readIframeActualUrl()
اگر در طول کار قطعهٔ موجود دیگری پیدا کردی که این لیست نام نبرده، همان را استفاده کن، نه نسخهٔ موازی. حداکثر تلاش: grep در backend/app/services/ با کلیدواژه‌های vision, audio, transcribe, visual_debug پیش از ساخت هر فایل جدید.

۳. مشخصات دقیق UI
۳.۱ محل دکمه
در frontend/src/app/projects/[id]/page.tsx خط 13694-13705، دقیقاً بعد از دکمهٔ screenshot (همان flex container absolute top-7 left-2 z-50):

{/* 🎬 دکمهٔ ضبط ویدئو (دو حالته) */}
<button
  onClick={() => setVideoRecordPickerOpen(true)}
  disabled={videoRecordingActive}
  title="ضبط ویدئو از iframe بازرس ویژه یا صفحات آزاد — با لاگ‌ها، صدا، تعاملات و پرامپت AI خودکار"
  className="..."  /* استایل دقیقاً مشابه دکمهٔ screenshot — اندازه و رنگ مشابه برای یکپارچگی بصری */
>
  {videoRecordingActive ? '⏺' : '🎬'}
</button>
شرط شلوغ نشدن UI: اگر در توسعه دیدی این flex container تنگ می‌شود، دکمه را فشرده‌تر کن (فقط آیکون، بدون متن). همان‌طور که کاربر گفته «جایی که حالا شلوغم نشه».

۳.۲ Picker حالت
با کلیک، یک popover/modal کوچک باز می‌شود با دو کارت بزرگ کنار هم:

کارت ۱ — «🎯 ضبط بازرس ویژه» (پیش‌فرض highlighted):

توضیح: «فقط محتوای داخل iframe ضبط می‌شود. تمام لاگ‌ها، تعاملات، اسکرول‌ها و کلیک‌های شما داخل همین مانیتور رصد می‌شود. AI فقط حول همین پروژه پرامپت می‌سازد. اگر در توضیح صوتی به چیزی خارج از این مانیتور اشاره کنید، AI آن را بی‌ربط تشخیص می‌دهد.»
کارت ۲ — «🌐 ضبط آزاد (تب/پنجره/دسکتاپ)»:

توضیح: «مرورگر از شما می‌پرسد کدام تب یا پنجره یا کل صفحه را ضبط کند. می‌توانید آزادانه بین صفحات سیر کنید. AI با تحلیل بصری فریم‌ها متوجه می‌شود در هر لحظه کجا هستید (تب کروم / برنامهٔ دیگر / دسکتاپ) و چه می‌کنید. کلیک‌ها و اسکرول‌ها از مقایسهٔ فریم‌ها استنتاج می‌شوند.»
زیر کارت‌ها — تنظیمات مشترک:

چک‌باکس 🎤 ضبط صدای میکروفون (پیش‌فرض روشن)
درپ‌دان منبع میکروفون: «پیش‌فرض سیستم» / «هندزفری/Bluetooth» / لیست تمام دستگاه‌های صوتی از navigator.mediaDevices.enumerateDevices() (شامل headset، USB mic، Bluetooth devices)
چک‌باکس 🔊 ضبط صدای سیستم (پیش‌فرض خاموش — چون نیاز به اجازهٔ خاص دارد و در همه مرورگرها پشتیبانی نمی‌شود)
دکمه‌های Modal:

[شروع ضبط] (سبز، فعال فقط اگر یک حالت انتخاب شده)
[انصراف] (خاکستری)
۳.۳ نوار وضعیت ضبط
پس از شروع، یک bar شناور پایین صفحه (یا گوشهٔ بالا-راست):

⏺ در حال ضبط: 00:23  |  🎯 iframe  |  🎤 ✓ مایک پیش‌فرض  |  🔊 ✗  |  [⏸ مکث]  [⏹ پایان و آماده‌سازی]
شمارش ثانیه‌ای زمان
در ۵۵ دقیقه (۵ دقیقه به سقف ۶۰ دقیقه): تغییر رنگ به نارنجی + toast هشدار «۵ دقیقه به پایان خودکار مانده»
در ۶۰ دقیقه: auto-stop + toast هشدار + گذار به مرحلهٔ آماده‌سازی
۳.۴ مرحلهٔ آماده‌سازی (Processing)
بین stop و modal پیش‌نمایش، یک modal با progress bar نمایش داده می‌شود:

⏳ در حال آماده‌سازی...
  ⚙️ مونتاژ ویدئو          [✓]
  🎙 transcribe صدا         [⏳ 45%]
  🔍 تحلیل بصری ۸ فریم      [⏳ 3/8]
  📊 جمع‌آوری لاگ‌ها        [✓]
  🤖 سنتز پرامپت قوی        [—]
این برای شفافیت — کاربر بداند سیستم در حال چه‌کاری است و چقدر مانده.

۳.۵ Modal پیش‌نمایش و ارسال
پس از آماده‌سازی، modal بزرگی باز می‌شود با این بخش‌ها (به ترتیب، از بالا به پایین):

🎥 پیش‌نمایش ویدئو — با <video controls> قابل پخش
📝 یادداشت اولیه (raw_idea) — یک textarea با placeholder:
«یادداشت اولیه (اختیاری) — این متن عین به عین در ابتدای پرامپت تسک حفظ می‌شود و در فیلد raw_idea ذخیره می‌گردد. اگر چیزی هست که می‌خواهید AI آن را به‌عنوان قصد اصلی شما درک کند، اینجا بنویسید.»

🎙 Transcript صدا (قابل ویرایش) — کاربر می‌تواند خطاهای transcribe را اصلاح کند، چون این متن به AI داده می‌شود
🔍 خلاصهٔ بصری (read-only، تولیدشده توسط describe_screenshot_with_vision) — قابل expand
📋 لاگ‌ها (به‌صورت accordion، collapsed by default):
Console logs frontend iframe (تعداد + پنل قابل باز شدن)
Backend logs Render (تعداد + پنل قابل باز شدن)
🖱 تعاملات کاربر (در حالت A، collapsed by default): لیست رویدادها با timestamp
🌐 نقشهٔ زمانی فعالیت (در حالت B): جدول [زمان | محل | فعالیت | مرتبط با پروژه؟]
🎯 پرامپت قوی تولیدشده (read-only، قابل copy) — AI generated، نمایش کامل بدون truncation
چک‌باکس [ ] «ارسال به مرکز نظارت به‌جای چت» — پیش‌فرض: خاموش (هرگز تیک زده نشود؛ کاربر صریحاً گفته «این رو تغییر ندی و پیش‌فرضش تیک نخورده باشه»)
دکمه‌ها:
[ارسال نهایی] (سبز، primary)
[لغو و دور انداختن] (قرمز، حذف کامل session)
[تولید مجدد پرامپت] (آبی) — اگر کاربر transcript را ویرایش کرد، می‌تواند AI را دوباره صدا بزند
۴. مشخصات دقیق حالت A — ضبط مانیتور iframe
۴.۱ منبع ویدئو — تصمیم قاطع
⚠️ چالش فنی: مانیتور بازرس ویژه یک <iframe> است که از یک دامنهٔ Render سرو می‌شود (cross-origin نسبت به دامنهٔ ai-creator-frontend). به این معنا:

❌ html2canvas روی iframe cross-origin → پنجرهٔ مشکی برمی‌گرداند (security)
❌ MediaRecorder روی iframe.contentDocument → دسترسی ممنوع (cross-origin)
❌ iframe.contentWindow.captureStream() → موجود نیست در هیچ مرورگری
✅ استفاده از screenshot endpoint موجود (POST /api/render/inspector/screenshot که از Playwright در backend استفاده می‌کند)
روش پیشنهادی (Definitive Recommendation): «screencast از طریق Playwright headless»

این روش بهترین کیفیت و کمترین پیچیدگی دارد:

در شروع، frontend یک endpoint جدید POST /api/recording/inspector/start-screencast صدا می‌زند با {project_id, url, fps=2}
backend یک Playwright session شروع می‌کند که صفحهٔ URL iframe را در headless mode باز می‌کند (روی همان viewport و resolution فرانت‌اند)
Playwright هر ۵۰۰ms (یا fps انتخاب‌شده) screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/frame_{seq:06d}.png ذخیره می‌کند
در پایان ضبط، با ffmpeg (اگر در dockerfile نصب است) frames به یک webm/mp4 تبدیل می‌شوند:
ffmpeg -framerate 2 -i frame_%06d.png -c:v libvpx-vp9 -b:v 600k output.webm
اگر ffmpeg در Dockerfile نیست، اضافه نکن (سایز image مهم است) — به‌جایش از pillow-based GIF یا webp animated استفاده کن (کیفیت پایین‌تر ولی بدون dependency).
جایگزین اگر Playwright session جدید سنگین است (fallback): استفاده از همان endpoint screenshot موجود POST /api/render/inspector/screenshot با polling هر ۵۰۰ms از frontend. سربار بیشتر روی شبکه ولی استفاده مجدد بدون تغییر backend.

انتخاب: گزینهٔ Playwright جدید (روش اصلی) را اول امتحان کن. اگر memory در Render محدود است (free tier 512MB)، به polling endpoint موجود سقوط کن.

۴.۲ صدا
صدا در سمت frontend ضبط می‌شود (چون Playwright headless میکروفون ندارد):

getUserMedia({ audio: { deviceId: selected_mic } }) برای میکروفون
اگر system_audio=true: getDisplayMedia({ audio: true, video: false }) ولی این یک پنجرهٔ پیکر باز می‌کند که در حالت A نخواستی. در حالت A پیشنهاد: صدای سیستم را غیرفعال بگذار، یا با هشدار:
«برای ضبط صدای سیستم در حالت A، مرورگر باید از شما اجازهٔ ضبط صفحه بخواهد. می‌خواهید ادامه دهید؟ (در غیر این صورت فقط میکروفون ضبط می‌شود.)»

ترکیب جریان‌های صوتی با Web Audio API → یک stream واحد → ضبط با MediaRecorder({mimeType: 'audio/webm'}) → ارسال chunks به backend هر ۱۰ ثانیه به POST /api/recording/inspector/{session_id}/audio-chunk.

۴.۳ لاگ‌های همگام
در طول ضبط:

هر ۱۰ ثانیه: POST /api/render/logs/fetch (با service_ids پروژهٔ منتخب) برای sync لاگ‌های جدید
در همان زمان: capture فعلی inspectorConsoleLogs (که قبلاً در state نگه‌داری می‌شود — خط 6259) به session تجمیع می‌شود
در لحظهٔ stop:

یک fetch نهایی + GET /api/render/logs?service_ids=...&minutes=<duration+2> برای اطمینان از داشتن همهٔ لاگ‌های دورهٔ ضبط
console logs کامل iframe از state موجود
۴.۴ تعاملات کاربر (با cross-origin handling)
چون iframe cross-origin است، event listening مستقیم ممکن نیست. دو راه ممکن:

روش ۱ (پیشنهادی): postMessage bridge

اگر کنترل کد سرور-سمت پروژه را داریم (Render preview)، یک قطعه کوچک JS در صفحات پروژه inject کنیم که این event ها را به parent بفرستد:

// در پروژهٔ مورد inspect (اگر possible)
['click','scroll','submit'].forEach(t => 
  window.addEventListener(t, e => 
    parent.postMessage({__inspector_event: true, type: t, ts: Date.now(), 
                        selector: e.target?.tagName, x: e.clientX, y: e.clientY}, '*')
  )
);
این روش ایده‌آل است ولی شرط دارد: کاربر باید این snippet را در پروژه‌های inspectable خودش inject کرده باشد. اگر نکرده، روش ۲.

روش ۲ (fallback - عمدتاً برای پروژه‌های third-party): استنتاج از فریم‌ها

از مقایسهٔ pixel-diff بین فریم‌های متوالی (با Pillow در backend) تشخیص بده:

آیا صفحه scroll شده (تغییر کل تصویر در محور Y) → ثبت scroll event
آیا یک تغییر موضعی رخ داده (تغییر در یک ناحیه) → احتمالاً click → ثبت inferred_click {area: bbox}
آیا URL تغییر کرده (از تغییر URL bar در صورت visible) → ثبت navigation
این روش approximate است ولی برای پرامپت کافی است.

تصمیم: روش ۱ را اول امتحان کن. اگر در ۲ ثانیه اول هیچ message دریافت نکردی، روش ۲ را فعال کن. هر دو روش رو در کد داشته باش.

ذخیرهٔ events: array از {ts: ms_since_start, type: string, details: object, source: 'postmessage'|'inferred'}.

۴.۵ سنتز پرامپت قوی — تعریف "قوی"
معیار «قوی» (که در آخر برای verify معتبر است):

✅ شامل یادداشت اولیه (raw_idea کاربر) به‌صورت دست‌نخورده در ابتدا
✅ شامل بلوک منشأ که می‌گوید این پرامپت از کجا آمده (ضبط بازرس ویژه، حالت A، تاریخ، مدت، لینک ویدئو در تلگرام)
✅ شامل Transcript تمیز صدا با timestamp (نه فقط متن خام)
✅ شامل خلاصهٔ بصری که توالی زمانی صفحات و تعاملات را توصیف می‌کند
✅ شامل استخراج intent: AI باید بفهمد «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه را رد می‌کند، کجاها منظورش است» — نه فقط transcribe خام
✅ شامل قطعات لاگ مرتبط (خطاها و warning های مرتبط با timestampهای فعالیت)
✅ شامل توصیف تعاملات (کلیک‌ها، اسکرول‌ها، navigationها) به زبان طبیعی
✅ شامل فهرستی از فایل‌ها/URLs/کامپوننت‌ها که AI تشخیص داده مرتبط هستند
✅ بدون truncation در هر اندازه‌ای — حتی اگر ۱۰۰هزار کاراکتر شد
✅ ساختاریافته با header markdown قابل خوانش توسط مدل AI بعدی
✅ actionable — یک agent دیگر باید بتواند با فقط این پرامپت کار را پیش ببرد

ساختار explicit پرامپت نهایی (که در OversightTask.prompt ذخیره می‌شود)
# {یادداشت اولیهٔ کاربر — عین متن textarea}

---

## 📍 منشأ این پرامپت

- **منبع**: ضبط در تب بازرس ویژه — حالت A (مانیتور iframe پروژه)
- **پروژه**: `{repo_full_name}`
- **تاریخ ضبط**: {ISO datetime}
- **مدت**: {minutes}m {seconds}s
- **ویدئو**: ارسال شد به تلگرام در پیام `{telegram_message_id or '—'}` (در سرور ذخیره نشد)
- **session_id**: `{session_id}` (موقت)

## 🎙 Transcript صدای کاربر (با timestamp)

[00:00] {…}
[00:12] {…}
...

## 🔍 خلاصهٔ بصری AI — توالی صفحات و رویدادها

[00:00-00:08] صفحهٔ X با عنصر Y دیده می‌شود. کاربر روی Z کلیک می‌کند.
[00:08-00:20] تغییر صفحه به URL='...'. اسکرول به وسط. کاربر در توضیح صوتی به دکمهٔ "..." اشاره می‌کند.
...

## 🎯 intent استخراج‌شده توسط AI

کاربر می‌خواهد: {یک خط واضح}
رد می‌کند: {اگر چیزی را رد کرد}
محل اشاره: {فایل/کامپوننت/صفحه‌ای که منظورش بوده}

## 🖱 تعاملات کاربر

| زمان | نوع | جزئیات |
|---|---|---|
| 00:03 | click | selector="button.submit" |
| 00:05 | scroll | y=420 |
...

## 🌐 [حالت B فقط] نقشهٔ زمانی فعالیت

| زمان | محل | مرتبط با پروژه؟ | فعالیت |
|---|---|---|---|
| 00:00-00:05 | تب کروم github.com/{repo} | ✓ بله | مرور کد |
| 00:05-00:10 | تب کروم imdb.com | ✗ خیر — خارج | (نامرتبط — حذف از بدنهٔ پرامپت) |
| 00:10-00:20 | پنجرهٔ VS Code | ✓ بله (دستی تشخیص داده شد) | ویرایش `auth.py` |
...

## 📋 لاگ‌های frontend (Console)
{relevant console errors/warnings from iframe}

## 📋 لاگ‌های backend (Render)
{relevant backend errors/warnings during recording period}

## 🔗 منابع شناسایی‌شده (URLs, فایل‌ها، کامپوننت‌ها)
- {url1}
- {file1.py}
- {component2.tsx}
## 💡 پرامپت قابل اجرای نهایی (برای agent دیگر)
{متن نهایی جامع که agent دیگر می‌تواند با آن کار را پیش ببرد — این بخش توسط AI با system prompt مخصوص تولید می‌شود}
۵. مشخصات دقیق حالت B — ضبط آزاد چندصفحه‌ای
۵.۱ منبع ویدئو
navigator.mediaDevices.getDisplayMedia({ video: {frameRate: 15}, audio: system_audio }) — کاربر در dialog مرورگر یک تب/پنجره/screen انتخاب می‌کند.

۵.۲ صدا
ترکیب میکروفون + system audio (اگر فعال) — همان روش حالت A بخش ۴.۲.

۵.۳ تشخیص محل کاربر (سه دسته جداگانه)
هر ۵ ثانیه یک screenshot از فریم استخراج می‌شود (از ویدئو blob با canvas) و به describe_screenshot_with_vision با system prompt اختصاصی:

این یک فریم از ضبط آزاد است. تشخیص بده در این فریم کاربر کجاست:
دسته‌ها:
1. تب کروم — اگر نوار آدرس مرورگر دیده می‌شود:
   - URL را از نوار آدرس استخراج کن
   - اگر domain یا path شامل '{repo_full_name}' یا '{render_preview_domain}' است → «مرتبط با پروژه»
   - وگرنه → «خارج از پروژه»
   
2. برنامهٔ دیگر دسکتاپ — اگر title bar یک برنامه (نه مرورگر) دیده می‌شود:
   - نام برنامه را تشخیص بده (VS Code, Slack, Terminal, …)
   - اگر نام پروژه یا فایل‌های آن visible است → «مرتبط (دستی)»
   - وگرنه → «خارج (دستی)»
3. دسکتاپ خام — اگر هیچ پنجرهٔ مشخصی روی صفحه نیست (icons + wallpaper):
   - دسته: «desktop»
   - فعالیت: کاربر در حال جابجایی پنجره‌ها یا navigation است
برای هر فریم خروجی بده:
{
  "category": "chrome_tab" | "other_app" | "desktop",
  "related_to_project": bool,
  "url_or_app_name": str,
  "activity_description": str (یک جمله),
  "user_action_inferred": str (مقایسه با فریم قبل: clicked/scrolled/typed/navigated)
}
۵.۴ تعاملات استنتاجی
چون event listening روی صفحات دیگر ممکن نیست:

AI vision (همان call بالا) خودش استنتاج می‌کند: «در این فاصلهٔ زمانی کاربر چه کاری انجام داد؟»
تغییرات بزرگ تصویر (با pixel-diff) → ثبت timestamp + توصیف AI
۵.۵ سنتز پرامپت
مشابه حالت A ولی با system prompt متفاوت برای text-generation نهایی:

کاربر در حال ضبط آزاد بین چند صفحه/برنامه بوده. ورودی‌های زیر را ترکیب کن و یک پرامپت قوی بساز که:
۱) ترتیب زمانی فعالیت‌های کاربر را به زبان طبیعی توصیف کند — کجا رفت، چه دید، چه کار کرد.
۲) اگر کاربر بین چند منبع جابجا شد، ارتباط بین آنها را در پرامپت توضیح بده (مثلاً «کاربر کد را در GitHub خواند، سپس به IDE رفت و فایل را باز کرد»).
۳) اگر کاربر به جایی خارج از پروژه رفت ولی در صدا به آن **اشاره نکرد** → از پرامپت نهایی حذف کن (نامرتبط).
۴) اگر کاربر به جایی خارج رفت **و در صدا به آن اشاره کرد** (مثلاً «این تو IMDB رو ببین، می‌خوام مثل این UI رو بسازم») → آن را در پرامپت به‌عنوان «مرجع/الهام» نگه دار.
۵) intent اصلی کاربر را در یک پاراگراف بالای پرامپت قرار بده.
۶. flow کامل — از کلیک تا تحویل
1. کاربر روی 🎬 کلیک
   ↓
2. Picker باز: انتخاب حالت A/B + تنظیمات صدا + منبع میکروفون
   ↓
3. کلیک [شروع ضبط]:
   - Frontend: POST /api/recording/inspector/start با {project_id, mode, audio_config, fps?}
   - Backend: ایجاد session، اگر mode='A' و روش Playwright انتخاب شد → شروع Playwright screencast
   - Frontend: شروع MediaRecorder برای صدا (mic + optional system audio)
   - Frontend: اگر mode='A' و postMessage bridge ممکن → شروع listener
   - Frontend: نمایش status bar
   ↓
4. در طول ضبط (background):
   - audio chunks هر 10s → POST /api/recording/inspector/{session_id}/audio-chunk
   - interactions هر 5s → POST /api/recording/inspector/{session_id}/interactions
   - backend logs sync هر 10s → POST /api/render/logs/fetch
   - frontend: نگه‌داری console logs در state (موجود)
   ↓
5. کاربر [⏹ پایان] (یا 60min auto-stop):
   - Frontend: MediaRecorder.stop()، ارسال آخرین chunks، POST /api/recording/inspector/{session_id}/stop
   ↓
6. Backend: نمایش modal "در حال آماده‌سازی" در frontend
   ↓
7. Backend processing (در سرور، با progress events که به frontend stream می‌شوند):
   a. مونتاژ video از frames (mode A) یا blob (mode B) با ffmpeg یا fallback
   b. مونتاژ audio chunks در یک فایل
   c. transcribe صدا (با audio_transcribe ماژول auto-select)
   d. استخراج 6-12 keyframe از ویدئو (هر دقیقه یکی، یا بر اساس scene-change)
   e. parallel: ارسال هر keyframe به describe_screenshot_with_vision()
   f. (mode B): پردازش frame analysis برای location tracking
   g. سنتز پرامپت قوی: ترکیب transcript + visual + interactions + logs → AI text model
   ↓
8. Frontend: modal پیش‌نمایش با همهٔ خروجی‌ها
   ↓
9. کاربر می‌بیند، یادداشت اضافه می‌کند، transcript را اصلاح می‌کند (اختیاری):
   - اگر transcript را اصلاح کرد → [تولید مجدد پرامپت] → step 7g دوباره
   ↓
10. کاربر [ارسال نهایی] می‌زند:
    - Frontend: POST /api/recording/inspector/{session_id}/finalize با {user_note, send_to_oversight, edited_transcript?}
    - Backend:
      i. ارسال ویدئو به تلگرام با send_document (caption شامل خلاصهٔ پرامپت)
      ii. اگر size > 45MB → split به chunks 45MB و چند call پشت سر هم با caption شماره‌گذاری‌شده
      iii. حذف کامل /tmp/inspector_recordings/{session_id} از disk
      iv. اگر send_to_oversight=true:
          POST /api/oversight/tasks/from-inspector-recording (endpoint جدید):
            - raw_idea = user_note
            - prompt = پرامپت قوی structure شده (طبق ۴.۵)
            - project_id = inspector's loaded project_id
          → برمی‌گرداند task_id
      v. اگر send_to_oversight=false:
          ارسال prompt به چت inspector (همان مسیر sendToOversight('visual_debug') با pre-built prompt)
    - Backend: برمی‌گرداند {success, task_id?, telegram_message_id?, chat_message_id?}
    ↓
11. Frontend: modal بسته می‌شود، toast «ارسال موفق» + لینک به تسک یا پیام چت
۷. ساختار endpoint‌های جدید
Path	Method	Body	Returns
/api/recording/inspector/start	POST	{project_id, mode: 'A'|'B', audio_config: {mic, system, mic_device_id?}, target_fps?}	{session_id, started_at, screencast_method?}
/api/recording/inspector/{sid}/audio-chunk	POST	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/video-chunk	POST (فقط mode B)	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/interactions	POST	{events: [{ts, type, details, source}]}	{ok}
/api/recording/inspector/{sid}/stop	POST	empty	{ok}
/api/recording/inspector/{sid}/process	POST	empty	SSE stream از پیشرفت پردازش
/api/recording/inspector/{sid}/preview	GET	—	{video_url_temp, transcript, visual_summary, interactions, logs, prompt}
/api/recording/inspector/{sid}/regenerate-prompt	POST	{edited_transcript}	{prompt}
/api/recording/inspector/{sid}/finalize	POST	{user_note, send_to_oversight, edited_transcript?}	{success, task_id?, telegram_message_id?, chat_message_id?}
/api/recording/inspector/{sid}/cancel	POST	empty	{ok} (cleanup کامل)
/api/oversight/tasks/from-inspector-recording	POST	{project_id, user_request, pre_built_prompt, recording_metadata}	{task_id, watched_id}
همگی در یک فایل جدید backend/app/api/routes/inspector_recording.py. در backend/app/main.py با prefix /api ثبت شود.

سرویس اصلی: backend/app/services/inspector_recording_service.py.

state جلسات: در RAM (یک dict در service singleton). cleanup خودکار: هر session که >۱۲۰ دقیقه active است auto-cancel + cleanup.

۸. مسیر فایل‌ها و حذف
ذخیرهٔ موقت: /tmp/inspector_recordings/{session_id}/
frames/ (mode A فقط)
audio_chunks/
video_chunks/ (mode B فقط)
final.webm (پس از مونتاژ)
transcript.txt
visual_summary.json
events.json
حذف کامل: پس از ارسال موفق به تلگرام یا پس از فراخوانی cancel
در صورت crash backend مید-process: cron-like job در scheduler_tick هر session با age > 120m را cleanup کند (اضافه به _sweep_pending_external_verifies یا یک sweeper جدید)
۹. مدیریت اندازهٔ فایل تلگرام
bitrate پیش‌فرض ویدئو: 600 kbps (برای 60min ≈ 270MB → بزرگ)
استراتژی: اگر فایل > 45MB → split
با ffmpeg -ss start -t duration -c copy chunk_N.webm برش بدون re-encode
هر chunk حداکثر 45MB
ارسال پشت سر هم با caption [1/N], [2/N], ..., [N/N] — caption نهایی شامل خلاصهٔ پرامپت
اگر تلگرام fail کرد:

۳ بار retry با backoff [2s, 5s, 10s]
اگر بازم fail:
نگه‌داری فایل برای ۲۴ ساعت در /tmp/inspector_recordings/{session_id}/
notification به کاربر: «ویدئو به تلگرام ارسال نشد. دانلود از: {temporary_download_url} (۲۴ ساعت معتبر)»
{temporary_download_url} در پرامپت تسک (بخش منشأ) درج می‌شود
پس از ۲۴ ساعت auto-cleanup
۱۰. transcribe صدا — استفاده از pattern موجود
⛔ هرگز موازی نساز. کاربر صریح:

«این از همون قابلیت دیباگ بصری باید برای استخراج استفاده کنه و مگه یادت نیست برای استخراج بصری مدل بصری خودکار انتخاب می‌شد یا لیست می‌اومد که انتخاب کنیم ... این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی‌اش کنی و لازم نیست چیز موازی انجام بدی»

روند بررسی پیش از ساخت:

در backend/app/services/oversight_inspector_bridge.py بگرد آیا تابعی برای transcribe صوت با همان pattern auto-select وجود دارد (search: audio, transcribe, whisper, speech)
در backend/app/services/ بگرد فایل‌هایی به نام audio_*.py, *_transcribe*.py, *_speech*.py
در مدل‌های موجود get_vision_models()/get_models() ببین آیا مدل‌هایی با capability=audio یا transcribe ثبت شده‌اند
اگر پیدا شد: همان را استفاده کن، حتی اگر نام transcribe_audio() ندارد و باید با adapter کوچک wrap شود.

اگر پیدا نشد: یک ماژول جدید backend/app/services/audio_transcribe.py با عین pattern describe_screenshot_with_vision بساز:

تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → {"text": str, "language": str, "duration_sec": float, "model_used": str}
داخل: شبیه oversight_inspector_bridge.py:61-89:
audio_models = get_audio_models() or [] (اگر get_audio_models موجود نیست، از env var check استفاده کن)
اولین provider در دسترس را انتخاب کن (OpenAI Whisper / Groq Whisper / …)
اگر هیچ‌کدام در دسترس نیست: log warning + برگردان {text: "", language: "", duration_sec: 0, model_used: ""} با هشدار در پرامپت نهایی «transcript صوت در دسترس نبود — هیچ provider برای transcribe پیکربندی نشده. می‌توانید OPENAI_API_KEY یا GROQ_API_KEY در Render env اضافه کنید»
هیچ کجا API call مستقیم به OpenAI/Anthropic/… نزن — همه از طریق ai_manager یا get_*_models() patterns.

۱۱. ساخت تسک و حفظ یادداشت اولیه (دقیق)
⛔ یادداشت اولیه را با پرامپت AI مخلوط نکن. کاربر صریح:

«اون یادداشت‌های اولیه که برای هر تسک میاد که باید باشه سر جاش ... البته اون یادداشت‌های اول پرامپت‌های تسک‌ها (نسخه به‌روزشده‌ش) باید ابتداش قرار بگیره و همچنین دقیق بگه از کجا ریشه گرفته»

ترجمه به ساختار:

اگر send_to_oversight=true:

یک تسک جدید با POST /api/oversight/tasks/from-inspector-recording
در OversightTask:
raw_idea = user_note (یادداشت اولیهٔ کاربر، عین متن textarea — نسخهٔ به‌روزشده‌اش که الان کاربر می‌نویسد، نه آنچه در ضبط گفت)
prompt = ساختار کامل طبق بخش ۴.۵ که با همان user_note در ابتدا شروع می‌شود (به عنوان heading اول)، بعد بلوک منشأ، بعد بقیه
type = "feature_request" (پیش‌فرض)
priority = "medium" (پیش‌فرض)
watched_id = پروژهٔ iframe
project_full_name = repo_full_name
فیلدهای موجود acceptance_criteria, task_steps = [] (تولید AC در این مرحله out of scope — اگر AI آنها را در پرامپت ذکر کرده، فقط در متن پرامپت می‌مانند)
target_files = [] (همان)
برای endpoint from-inspector-recording:

این یک endpoint جدید است. در backend/app/api/routes/oversight.py (همان فایلی که from-inspector در آن است) اضافه شود:

class FromInspectorRecordingRequest(BaseModel):
    project_id: str
    user_request: str          # یادداشت اولیه (raw_idea)
    pre_built_prompt: str      # پرامپت قوی آماده — هیچ پردازش جدید
    recording_metadata: Dict[str, Any]
    # شامل: {duration_sec, mode: 'A'|'B', telegram_message_id?, video_size_mb, session_id, completed_at}

@router.post("/tasks/from-inspector-recording")
async def create_task_from_inspector_recording(payload: FromInspectorRecordingRequest):
    service = get_oversight_service()
    # ⚠️ بر خلاف /from-inspector که خودش پرامپت می‌سازد، اینجا پرامپت آماده است
    # وظیفه: فقط ساخت تسک + حفظ raw_idea + نگه‌داری prompt
    new_task = await service.create_task(
        watched_id=resolve_watched_id_from_project_id(payload.project_id),
        title=extract_title_from_prompt(payload.pre_built_prompt) or "ضبط بازرس ویژه",
        raw_idea=payload.user_request,
        prompt=payload.pre_built_prompt,
        type="feature_request",
        priority="medium",
        execution_mode="manual",
    )
    return {"task_id": new_task.id, "watched_id": new_task.watched_id}
اگر send_to_oversight=false:

پرامپت کامل به چت inspector ارسال می‌شود (همان مسیر فعلی sendToOversight('visual_debug') با pre_built_prompt به‌جای regenerate)
یادداشت اولیه به‌عنوان bold heading در ابتدای پیام چت قرار می‌گیرد
۱۲. ارسال به تلگرام — جزئیات caption
caption تلگرام (حداکثر ۱۰۲۰ char، طبق محدودیت Telegram Bot API):

🎬 ضبط بازرس ویژه — حالت {🎯 A: iframe-focused | 🌐 B: free-multi-screen}
📁 پروژه: {repo_full_name}
⏱ مدت: {minutes}m {seconds}s
{اگر تسک ساخته شد: 🆔 task: {task_id_short} | 🔗 {frontend_url}/projects/{pid}/tasks/{task_id}}
{اگر به چت رفت: 💬 ارسال شد به چت inspector}
📝 یادداشت اولیه کاربر:
{user_note[:200]}{اگر بیشتر: ...}
🤖 خلاصهٔ پرامپت AI:
{first 500 chars of pre_built_prompt body section, after the source block}
اگر طول از 1020 char بیشتر شد، خلاصهٔ پرامپت کوتاه می‌شود (نه یادداشت اولیه).

۱۳. محدودیت‌های زمانی و امن
محدودیت	مقدار
حداکثر مدت ضبط	۶۰ دقیقه (سپس auto-stop + warning)
حداکثر bitrate ویدئو	1 Mbps (mode B) / 600 kbps پیشنهادی
target fps mode A	2 fps (Playwright screencast) — صرفه‌جویی
target fps mode B	15 fps (getDisplayMedia native)
حداکثر اندازه فایل قبل از split	45 MB
Idle session cleanup	۱۲۰ دقیقه بدون finalize → auto-cancel
Transcribe timeout	۹۰ ثانیه
Vision call timeout per screenshot	۳۰ ثانیه
تعداد screenshot استخراجی برای vision	min(۱۲, max(۶, duration_minutes))
Polling interval mode A (fallback)	۵۰۰ ms
۱۴. وابستگی‌های جدید
ffmpeg در Dockerfile: چک کن از قبل هست یا نه (با which ffmpeg در یک تست container). اگر هست استفاده کن. اگر نیست:
اولویت اول: اضافه به Dockerfile (یک خط apt install ffmpeg)
اگر سایز image دغدغه است: استفاده از pillow برای ساخت GIF animated (کیفیت پایین ولی بدون dependency)
pillow (PIL) برای pixel-diff تشخیص scroll/click — احتمالاً از قبل نصب است
frontend: بدون package جدید، فقط Web APIs (MediaRecorder, getDisplayMedia, getUserMedia, Web Audio API)
۱۵. منع‌های قطعی
❌ هرگز ویدئو را در DB یا persistent storage ذخیره نکن (هیچ مدل دیتابیس جدید برای ویدئو)
❌ هرگز ویدئو را پس از ارسال موفق به تلگرام نگه‌دار (حذف immediate)
❌ هرگز کد describe_screenshot_with_vision یا oversight_inspector_bridge.py را modify نکن — فقط استفاده کن
❌ هرگز from-inspector endpoint موجود را خراب نکن — فقط from-inspector-recording جدید اضافه کن
❌ هرگز پیش‌فرض چک‌باکس "ارسال به مرکز نظارت" را روشن نکن (کاربر صریح)
❌ هرگز پرامپت تولیدشده را در backend truncate نکن — بدون محدودیت توکن
❌ هرگز یادداشت اولیه (raw_idea) را با پرامپت AI overwrite کن — جدا نگه دار
❌ هرگز حالت B را حذف یا غیرفعال نکن — کاربر صریح: «حتی غیرفعالش هم نکن»
❌ هرگز بدون تأیید کاربر در preview modal چیزی ارسال نکن
❌ هرگز mode A را با getDisplayMedia پیاده نکن — کاربر صریح می‌خواهد iframe-only باشد، نه پنجرهٔ picker مرورگر
❌ هرگز ضبط را در حالت B بدون پیکر مرورگر شروع نکن — getDisplayMedia() ذاتاً پیکر را باز می‌کند، این طبیعی است
۱۶. AC رفتار-محور (برای verify)
not done
دکمهٔ 🎬 ضبط ویدئو در کنار دکمهٔ screenshot در تب بازرس ویژه قابل مشاهده است
not done
کلیک روی دکمه، Picker حالت A/B با تنظیمات صدا و انتخاب میکروفون باز می‌شود
not done
حالت A: ضبط ویدئو از مانیتور iframe بدون نمایش پیکر مرورگر شروع می‌شود
not done
حالت B: ضبط با getDisplayMedia() و پیکر مرورگر شروع می‌شود
not done
در هر دو حالت، نوار وضعیت با شمارش زمان و حالت نمایش داده می‌شود
not done
auto-stop در ۶۰ دقیقه با هشدار به کاربر
not done
پس از stop، modal "در حال آماده‌سازی" با progress mراحل (transcribe, vision, sync) نمایش داده می‌شود
not done
modal پیش‌نمایش شامل: ویدئو، transcript قابل ویرایش، خلاصهٔ بصری، لاگ‌ها، تعاملات، پرامپت قوی نهایی، چک‌باکس "ارسال به مرکز نظارت" که پیش‌فرض خاموش است
not done
در حالت A، پرامپت قوی شامل تعاملات کاربر داخل iframe (کلیک‌ها، اسکرول‌ها) با timestamp است
not done
در حالت B، پرامپت قوی شامل نقشهٔ زمانی فعالیت با دسته‌بندی (chrome_tab/other_app/desktop) و فلگ مرتبط/خارج از پروژه است
not done
پرامپت قوی بدون truncation ذخیره و نمایش داده می‌شود (می‌تواند هر طولی باشد)
not done
یادداشت اولیه کاربر (textarea) عیناً در OversightTask.raw_idea ذخیره می‌شود
not done
پرامپت قوی در OversightTask.prompt ذخیره می‌شود — با ساختار: یادداشت اولیه در heading اول → بلوک منشأ → بقیه
not done
با ارسال نهایی + چک‌باکس روشن: تسک جدید زیر پروژهٔ iframe ایجاد می‌شود (watched_id صحیح)
not done
با ارسال نهایی + چک‌باکس خاموش: پرامپت کامل به چت inspector ارسال می‌شود (دیده می‌شود مثل screenshot)
not done
ویدئو با caption شامل خلاصه به تلگرام ارسال می‌شود
not done
اگر ویدئو > 45MB، به chunks تقسیم و چندتا send_document پشت سر هم
not done
فایل ویدئو از /tmp/inspector_recordings/ پس از ارسال موفق به تلگرام حذف می‌شود (با os.path.exists تأیید شود)
not done
اگر کاربر "لغو و دور انداختن" را زد، فایل حذف و هیچ ارسالی صورت نمی‌گیرد
not done
AI vision (در حالت B) درست تشخیص می‌دهد کاربر در یک تب دیگر کروم خارج از پروژه است (تست با ضبط شامل تب IMDB یا مشابه)
not done
AI درست تشخیص می‌دهد اگر کاربر در صدا به چیزی خارج از iframe اشاره کرد (حالت A) و آن را در پرامپت لحاظ نمی‌کند
not done
AI درست intent را استخراج می‌کند: «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، کجاها منظورش است» (تست با ضبط حاوی صدای ابهام‌آمیز)
not done
مکانیزم visual debug (describe_screenshot_with_vision) unchanged باقی می‌ماند — کد آن لمس نشده
not done
linter بدون warning عبور می‌کند
not done
type-check موفق است (tsc --noEmit / mypy)
not done
هیچ migration دیتابیس مورد نیاز نیست (state در RAM، فایل‌ها در /tmp)
۱۷. وابستگی‌های upstream / downstream / cross-tier / side
upstream (متکی به):

describe_screenshot_with_vision (oversight_inspector_bridge.py:44) — استفاده، نه تغییر
TelegramChannel.send_document (notification_service.py:579) — استفاده
GET/POST /api/render/logs* (render_logs.py) — استفاده
OversightTask.raw_idea و OversightTask.prompt (oversight_service.py:328-407) — استفاده
inspectorIframeRef و _readIframeActualUrl() (page.tsx) — استفاده
inspectorConsoleLogs state (page.tsx) — خواندن
وجود Playwright در backend/app/services/browser_automation.py (یا معادل) — استفاده برای screencast
downstream (متکی این تسک می‌شوند):

چک‌باکس sendToOversightVisual (page.tsx:673-675) — همچنان همان معنا، فقط در modal جدید
تابع sendToOversight('visual_debug') (page.tsx:5198) — یک branch اضافه برای pre-built prompt
cross-tier (backend ↔ frontend):

۱۰ endpoint جدید REST
type definitions جدید: RecordingSession, FinalizeResult, FromInspectorRecordingRequest
state جدید frontend: videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession
side artifacts:

.env.example: اضافه شود (اگر transcribe نیاز به provider جدید داشت) — مثلاً # اختیاری برای transcribe سریع: GROQ_API_KEY=
README: یک پاراگراف کوتاه دربارهٔ این feature در بخش "بازرس ویژه"
Dockerfile: شاید ffmpeg (اگر نیست)
هیچ migration DB
هیچ تغییر i18n
هیچ تغییر در requirements.txt (تمام dependencies موجود کافی است)
۱۸. ترتیب پیاده‌سازی (foundation → core → integration → polish)
این تسک بزرگ است. به ۶ commit متوالی push شود (هر کدام به‌تنهایی verify-able):

Commit 1 — foundation

inspector_recording_service.py با stub functions
inspector_recording.py با endpoints stub (همگی برمی‌گردانند dummy data)
state in-memory + cleanup sweeper
ثبت در main.py
Commit 2 — audio transcribe

بررسی oversight_inspector_bridge.py و سایر فایل‌ها برای audio transcribe موجود
اگر هست: adapter ساده
اگر نیست: audio_transcribe.py با pattern auto-select از env vars (دقیقاً مثل describe_screenshot_with_vision)
تست: یک wav کوچک → text برگردد یا warning واضح
Commit 3 — recording mode A (iframe)

Playwright screencast: endpoint start-screencast + frame extraction
audio capture frontend + chunk upload
log sync (frontend console + backend Render logs)
postMessage bridge attempt + pixel-diff fallback برای interactions
مونتاژ نهایی frames به ویدئو
vision analysis از keyframes
prompt synthesis (متن نهایی بدون tructaion)
Commit 4 — recording mode B (free)

frontend MediaRecorder با getDisplayMedia
video chunk upload
vision-based location classifier (chrome_tab/other_app/desktop)
AI integration برای intent extraction در حالت B
prompt synthesis مخصوص حالت B
Commit 5 — finalize + Telegram + task creation

ارسال به Telegram با split برای فایل‌های بزرگ
حذف از disk
endpoint from-inspector-recording در oversight.py
اتصال به چت inspector (اگر چک‌باکس خاموش)
retry + fallback download URL
Commit 6 — UI کامل + polish

دکمهٔ ضبط در page.tsx
Picker modal
status bar
processing progress modal با SSE
preview modal با همهٔ بخش‌ها
regenerate prompt
error states + toasts
accessibility (keyboard navigation)
هر commit:

linter pass
type-check pass
syntax check (python -c "import ast; ast.parse(open(f).read())" برای py / tsc --noEmit برای ts)
push بلافاصله به default branch
بدون اجبار خلاصه‌سازی — اگر کامیت بزرگ شد، صبر کن تا تمام شود
۱۹. مدیریت TO-DO
این تسک هیچ بخش Manual-required ندارد — همه چیز قابل پیاده‌سازی خودکار است. کاربر تصمیمی سلیقه‌ای از ما نمی‌خواهد، credential خاصی لازم نیست (مگر اینکه provider transcribe در env نباشد، که آن یک TO-DO کوچک معتبر است در لیست سفید مورد ۱: «Credential که فقط کاربر دارد» — می‌توانی یک TO-DO سبک برای GROQ_API_KEY یا OPENAI_API_KEY بسازی اگر env در زمان اجرا خالی بود، ولی همان موقع باید پرامپت با هشدار transcript خالی تولید شود — نه stop کامل).

اگر هیچ TO-DO ساختی، در commit message صریح بنویس:

«بررسی شد — این تسک هیچ بخش Manual-required ندارد. ۰ فایل TO-DO ساخته شد.»

پایان پرامپت

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
🎬  ضبط دوحالته در تب بازرس ویژه با استخراج هوشمند
۰. یادداشت اولیه و منشأ این پرامپت
این پرامپت برای رفع یک تسک قبلی (تسک ۱۷ مرحله‌ای با عنوان «افزودن قابلیت ضبط ویدئو در تب بازرس ویژه با لاگ‌ها و صدا و تبدیل به پرامپت») ساخته شده که در ضبط با Claude Code به‌صورت انحرافی از خواستهٔ اصلی پیاده‌سازی شد:

به‌جای ضبط محتوای داخل iframe بازرس ویژه، با getDisplayMedia() از کاربر می‌پرسد کدام تب/پنجره/کل صفحه را ضبط کند
لاگ‌ها (frontend console + backend Render logs) به ضبط متصل نشده‌اند
پرامپت بصری تولیدشده ۱-۲ خط سطحی است و به جزئیات تعاملات/صفحات/کلیک‌ها اشاره نمی‌کند
تعاملات کاربر (کلیک، اسکرول، جابجایی) ردیابی نمی‌شوند
کاربر صریحاً گفته:

«این هم خیلی جالبه و بعداً در جایی میشه ازش حتما استفاده‌هایی کرد و لذا نمی‌خوام این حذف بشه بلکه می‌خوام این هم باشه ... ولی مشکل اینه که این هم اصلاً درست انجام نمی‌شه ... لطفاً هر دو رو درست کن. یعنی هم درخواست اصلیم رو که اشتباهی فهمیده شده رو به شکل کاملاً دقیق انجام بده. و دوم اینکه همین قابلیت اشتباهی هم که الان هست رو حذف نکن بلکه اصلاحش کن ... کاملاً درستش کن که بشه در زمانی هم که خواستم ازینم استفاده کنم. حتی غیر فعالش هم نکن»

پس هر دو حالت باید موجود، انتخاب‌پذیر، و کاملاً عملیاتی باشند.

۱. هدف کلان — معماری دوحالتی
دکمهٔ ضبط ویدئو در تب بازرس ویژه، با کلیک یک Picker باز می‌کند که کاربر بین این دو حالت انتخاب می‌کند:

حالت A — «ضبط بازرس ویژه» (پیش‌فرض، درخواست اصلی)
ضبط فقط از محتوای داخل ناحیهٔ مانیتور/iframe بازرس ویژه که در آن پروژهٔ منتخب لود شده
لاگ‌های frontend (console) آن iframe + لاگ‌های backend پروژهٔ منتخب (از Render Logs API)
تعاملات کاربر داخل همان iframe: کلیک، اسکرول، جابجایی صفحات، تایپ ورودی (فقط نوع event، نه محتوای حروف برای حریم خصوصی)
صدای کاربر (میکروفون) + صدای سیستم (اختیاری)
AI بصری متمرکز فقط روی همان iframe — اگر کاربر در توضیح صوتی به چیزی خارج از iframe اشاره کند، AI تشخیص می‌دهد بی‌ربط است و در پرامپت لحاظ نمی‌کند
پرامپت قوی تولیدشده ذیل همان پروژهٔ iframe ثبت یا به چت همان inspector ارسال می‌شود
حالت B — «ضبط آزاد چندصفحه‌ای» (حفظ + اصلاح حالت فعلی غلط)
ضبط با getDisplayMedia() (همان روش فعلی)
کاربر هر چیزی انتخاب می‌کند: یک تب کروم، یک پنجره، یا کل دسکتاپ
AI بصری روی فریم‌های ضبط (هر ۵ ثانیه یک screenshot) تشخیص می‌دهد در هر مقطع کاربر کجاست و چه می‌کند:
تب دیگر کروم (مرتبط با پروژهٔ inspector / خارج از پروژه)
برنامهٔ دیگر دسکتاپ (IDE، فایل‌منیجر، …)
دسکتاپ خام (icons، wallpaper)
جابجایی بین صفحات/تب‌ها/برنامه‌ها با timestamp در پرامپت ثبت می‌شود
اسکرول‌ها و کلیک‌های قابل تشخیص از مقایسهٔ فریم‌های متوالی استنتاج می‌شوند (چون event listening در سایت‌های دیگر ممکن نیست)
AI متمایز می‌کند چه فعالیتی به پروژهٔ inspector ({repo_full_name}) مرتبط است و چه فعالیتی خارج
رفتار مشترک پس از پایان (هر دو حالت)
استخراج intent از صدا: نه فقط transcribe، بلکه فهم «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه چیزی را رد می‌کند، کجا منظورش است»
ویدئو با caption شامل خلاصهٔ پرامپت به تلگرام ارسال می‌شود
فایل ویدئو پس از ارسال موفق به تلگرام، از disk backend حذف می‌شود (هیچ persistence)
اگر چک‌باکس «ارسال به مرکز نظارت» روشن بود (پیش‌فرض خاموش): یک تسک جدید زیر همان پروژهٔ iframe ایجاد می‌شود
اگر چک‌باکس خاموش بود: پرامپت به چت inspector ارسال می‌شود (همان رفتار فعلی screenshot)
۲. زیرساخت‌های موجود — استفاده مجدد، نه ساخت موازی
⛔ هشدار بحرانی — کاربر صریحاً گفته:

«این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی کنی و لازم نیست چیز موازی انجام بدی ... لذا مراقب باش خراب نکنی»

این یعنی: کد موجود را نخوان و کنار نگذار. استفاده کن. اگر تغییری در آن لازم بود، فقط extension غیرتخریبی (parameter اختیاری اضافه) — نه refactor، نه replace.

نیاز	محل دقیق موجود	امضای کلیدی
Screenshot از iframe (Playwright backend)	backend/app/api/routes/render_logs.py و endpoint POST /api/render/inspector/screenshot (در فرانت‌اند فراخوانی می‌شود از frontend/src/app/projects/[id]/page.tsx:6242)	برمی‌گرداند base64 PNG
تابع کلاینت screenshot موجود	frontend/src/app/projects/[id]/page.tsx:6196-6328 تابع takeVisualDebugScreenshot()	مرجع برای الگوی API call + log gathering
لاگ‌های backend پروژهٔ iframe	backend/app/api/routes/render_logs.py:1299-1384	GET /api/render/logs?service_ids=&minutes=&limit=&level=&search=
sync لاگ‌های backend (pull از Render API)	backend/app/api/routes/render_logs.py:1387-1508	POST /api/render/logs/fetch
تحلیل بصری با AI (auto-select مدل)	backend/app/services/oversight_inspector_bridge.py:44-242	describe_screenshot_with_vision(base64_img, user_context, page_url, dom_text) → Dict — خودش از get_vision_models() انتخاب می‌کند و اگر هیچ مدلی نیست با پیام واضح رد می‌شود
ساخت تسک از inspector با حفظ raw_idea	backend/app/api/routes/oversight.py:1606-1645	POST /api/oversight/tasks/from-inspector با FromInspectorRequest
فیلد raw_idea در مدل تسک	backend/app/services/oversight_service.py:328-407	OversightTask.raw_idea: str = "" — جدا از OversightTask.prompt
ارسال فایل به تلگرام با caption	backend/app/services/notification_service.py:579-645	TelegramChannel.send_document(file_bytes, filename, *, caption) — caption تا ۱۰۲۰ کاراکتر
state چک‌باکس "ارسال به مرکز نظارت"	frontend/src/app/projects/[id]/page.tsx:673-675	sendToOversightVisual
handler ارسال نهایی به مرکز نظارت	frontend/src/app/projects/[id]/page.tsx:5198	sendToOversight('visual_debug')
URL واقعی iframe	frontend/src/app/projects/[id]/page.tsx:6189	تابع _readIframeActualUrl()
اگر در طول کار قطعهٔ موجود دیگری پیدا کردی که این لیست نام نبرده، همان را استفاده کن، نه نسخهٔ موازی. حداکثر تلاش: grep در backend/app/services/ با کلیدواژه‌های vision, audio, transcribe, visual_debug پیش از ساخت هر فایل جدید.

۳. مشخصات دقیق UI
۳.۱ محل دکمه
در frontend/src/app/projects/[id]/page.tsx خط 13694-13705، دقیقاً بعد از دکمهٔ screenshot (همان flex container absolute top-7 left-2 z-50):

{/* 🎬 دکمهٔ ضبط ویدئو (دو حالته) */}
<button
  onClick={() => setVideoRecordPickerOpen(true)}
  disabled={videoRecordingActive}
  title="ضبط ویدئو از iframe بازرس ویژه یا صفحات آزاد — با لاگ‌ها، صدا، تعاملات و پرامپت AI خودکار"
  className="..."  /* استایل دقیقاً مشابه دکمهٔ screenshot — اندازه و رنگ مشابه برای یکپارچگی بصری */
>
  {videoRecordingActive ? '⏺' : '🎬'}
</button>
شرط شلوغ نشدن UI: اگر در توسعه دیدی این flex container تنگ می‌شود، دکمه را فشرده‌تر کن (فقط آیکون، بدون متن). همان‌طور که کاربر گفته «جایی که حالا شلوغم نشه».

۳.۲ Picker حالت
با کلیک، یک popover/modal کوچک باز می‌شود با دو کارت بزرگ کنار هم:

کارت ۱ — «🎯 ضبط بازرس ویژه» (پیش‌فرض highlighted):

توضیح: «فقط محتوای داخل iframe ضبط می‌شود. تمام لاگ‌ها، تعاملات، اسکرول‌ها و کلیک‌های شما داخل همین مانیتور رصد می‌شود. AI فقط حول همین پروژه پرامپت می‌سازد. اگر در توضیح صوتی به چیزی خارج از این مانیتور اشاره کنید، AI آن را بی‌ربط تشخیص می‌دهد.»
کارت ۲ — «🌐 ضبط آزاد (تب/پنجره/دسکتاپ)»:

توضیح: «مرورگر از شما می‌پرسد کدام تب یا پنجره یا کل صفحه را ضبط کند. می‌توانید آزادانه بین صفحات سیر کنید. AI با تحلیل بصری فریم‌ها متوجه می‌شود در هر لحظه کجا هستید (تب کروم / برنامهٔ دیگر / دسکتاپ) و چه می‌کنید. کلیک‌ها و اسکرول‌ها از مقایسهٔ فریم‌ها استنتاج می‌شوند.»
زیر کارت‌ها — تنظیمات مشترک:

چک‌باکس 🎤 ضبط صدای میکروفون (پیش‌فرض روشن)
درپ‌دان منبع میکروفون: «پیش‌فرض سیستم» / «هندزفری/Bluetooth» / لیست تمام دستگاه‌های صوتی از navigator.mediaDevices.enumerateDevices() (شامل headset، USB mic، Bluetooth devices)
چک‌باکس 🔊 ضبط صدای سیستم (پیش‌فرض خاموش — چون نیاز به اجازهٔ خاص دارد و در همه مرورگرها پشتیبانی نمی‌شود)
دکمه‌های Modal:

[شروع ضبط] (سبز، فعال فقط اگر یک حالت انتخاب شده)
[انصراف] (خاکستری)
۳.۳ نوار وضعیت ضبط
پس از شروع، یک bar شناور پایین صفحه (یا گوشهٔ بالا-راست):

⏺ در حال ضبط: 00:23  |  🎯 iframe  |  🎤 ✓ مایک پیش‌فرض  |  🔊 ✗  |  [⏸ مکث]  [⏹ پایان و آماده‌سازی]
شمارش ثانیه‌ای زمان
در ۵۵ دقیقه (۵ دقیقه به سقف ۶۰ دقیقه): تغییر رنگ به نارنجی + toast هشدار «۵ دقیقه به پایان خودکار مانده»
در ۶۰ دقیقه: auto-stop + toast هشدار + گذار به مرحلهٔ آماده‌سازی
۳.۴ مرحلهٔ آماده‌سازی (Processing)
بین stop و modal پیش‌نمایش، یک modal با progress bar نمایش داده می‌شود:

⏳ در حال آماده‌سازی...
  ⚙️ مونتاژ ویدئو          [✓]
  🎙 transcribe صدا         [⏳ 45%]
  🔍 تحلیل بصری ۸ فریم      [⏳ 3/8]
  📊 جمع‌آوری لاگ‌ها        [✓]
  🤖 سنتز پرامپت قوی        [—]
این برای شفافیت — کاربر بداند سیستم در حال چه‌کاری است و چقدر مانده.

۳.۵ Modal پیش‌نمایش و ارسال
پس از آماده‌سازی، modal بزرگی باز می‌شود با این بخش‌ها (به ترتیب، از بالا به پایین):

🎥 پیش‌نمایش ویدئو — با <video controls> قابل پخش
📝 یادداشت اولیه (raw_idea) — یک textarea با placeholder:
«یادداشت اولیه (اختیاری) — این متن عین به عین در ابتدای پرامپت تسک حفظ می‌شود و در فیلد raw_idea ذخیره می‌گردد. اگر چیزی هست که می‌خواهید AI آن را به‌عنوان قصد اصلی شما درک کند، اینجا بنویسید.»

🎙 Transcript صدا (قابل ویرایش) — کاربر می‌تواند خطاهای transcribe را اصلاح کند، چون این متن به AI داده می‌شود
🔍 خلاصهٔ بصری (read-only، تولیدشده توسط describe_screenshot_with_vision) — قابل expand
📋 لاگ‌ها (به‌صورت accordion، collapsed by default):
Console logs frontend iframe (تعداد + پنل قابل باز شدن)
Backend logs Render (تعداد + پنل قابل باز شدن)
🖱 تعاملات کاربر (در حالت A، collapsed by default): لیست رویدادها با timestamp
🌐 نقشهٔ زمانی فعالیت (در حالت B): جدول [زمان | محل | فعالیت | مرتبط با پروژه؟]
🎯 پرامپت قوی تولیدشده (read-only، قابل copy) — AI generated، نمایش کامل بدون truncation
چک‌باکس [ ] «ارسال به مرکز نظارت به‌جای چت» — پیش‌فرض: خاموش (هرگز تیک زده نشود؛ کاربر صریحاً گفته «این رو تغییر ندی و پیش‌فرضش تیک نخورده باشه»)
دکمه‌ها:
[ارسال نهایی] (سبز، primary)
[لغو و دور انداختن] (قرمز، حذف کامل session)
[تولید مجدد پرامپت] (آبی) — اگر کاربر transcript را ویرایش کرد، می‌تواند AI را دوباره صدا بزند
۴. مشخصات دقیق حالت A — ضبط مانیتور iframe
۴.۱ منبع ویدئو — تصمیم قاطع
⚠️ چالش فنی: مانیتور بازرس ویژه یک <iframe> است که از یک دامنهٔ Render سرو می‌شود (cross-origin نسبت به دامنهٔ ai-creator-frontend). به این معنا:

❌ html2canvas روی iframe cross-origin → پنجرهٔ مشکی برمی‌گرداند (security)
❌ MediaRecorder روی iframe.contentDocument → دسترسی ممنوع (cross-origin)
❌ iframe.contentWindow.captureStream() → موجود نیست در هیچ مرورگری
✅ استفاده از screenshot endpoint موجود (POST /api/render/inspector/screenshot که از Playwright در backend استفاده می‌کند)
روش پیشنهادی (Definitive Recommendation): «screencast از طریق Playwright headless»

این روش بهترین کیفیت و کمترین پیچیدگی دارد:

در شروع، frontend یک endpoint جدید POST /api/recording/inspector/start-screencast صدا می‌زند با {project_id, url, fps=2}
backend یک Playwright session شروع می‌کند که صفحهٔ URL iframe را در headless mode باز می‌کند (روی همان viewport و resolution فرانت‌اند)
Playwright هر ۵۰۰ms (یا fps انتخاب‌شده) screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/frame_{seq:06d}.png ذخیره می‌کند
در پایان ضبط، با ffmpeg (اگر در dockerfile نصب است) frames به یک webm/mp4 تبدیل می‌شوند:
ffmpeg -framerate 2 -i frame_%06d.png -c:v libvpx-vp9 -b:v 600k output.webm
اگر ffmpeg در Dockerfile نیست، اضافه نکن (سایز image مهم است) — به‌جایش از pillow-based GIF یا webp animated استفاده کن (کیفیت پایین‌تر ولی بدون dependency).
جایگزین اگر Playwright session جدید سنگین است (fallback): استفاده از همان endpoint screenshot موجود POST /api/render/inspector/screenshot با polling هر ۵۰۰ms از frontend. سربار بیشتر روی شبکه ولی استفاده مجدد بدون تغییر backend.

انتخاب: گزینهٔ Playwright جدید (روش اصلی) را اول امتحان کن. اگر memory در Render محدود است (free tier 512MB)، به polling endpoint موجود سقوط کن.

۴.۲ صدا
صدا در سمت frontend ضبط می‌شود (چون Playwright headless میکروفون ندارد):

getUserMedia({ audio: { deviceId: selected_mic } }) برای میکروفون
اگر system_audio=true: getDisplayMedia({ audio: true, video: false }) ولی این یک پنجرهٔ پیکر باز می‌کند که در حالت A نخواستی. در حالت A پیشنهاد: صدای سیستم را غیرفعال بگذار، یا با هشدار:
«برای ضبط صدای سیستم در حالت A، مرورگر باید از شما اجازهٔ ضبط صفحه بخواهد. می‌خواهید ادامه دهید؟ (در غیر این صورت فقط میکروفون ضبط می‌شود.)»

ترکیب جریان‌های صوتی با Web Audio API → یک stream واحد → ضبط با MediaRecorder({mimeType: 'audio/webm'}) → ارسال chunks به backend هر ۱۰ ثانیه به POST /api/recording/inspector/{session_id}/audio-chunk.

۴.۳ لاگ‌های همگام
در طول ضبط:

هر ۱۰ ثانیه: POST /api/render/logs/fetch (با service_ids پروژهٔ منتخب) برای sync لاگ‌های جدید
در همان زمان: capture فعلی inspectorConsoleLogs (که قبلاً در state نگه‌داری می‌شود — خط 6259) به session تجمیع می‌شود
در لحظهٔ stop:

یک fetch نهایی + GET /api/render/logs?service_ids=...&minutes=<duration+2> برای اطمینان از داشتن همهٔ لاگ‌های دورهٔ ضبط
console logs کامل iframe از state موجود
۴.۴ تعاملات کاربر (با cross-origin handling)
چون iframe cross-origin است، event listening مستقیم ممکن نیست. دو راه ممکن:

روش ۱ (پیشنهادی): postMessage bridge

اگر کنترل کد سرور-سمت پروژه را داریم (Render preview)، یک قطعه کوچک JS در صفحات پروژه inject کنیم که این event ها را به parent بفرستد:

// در پروژهٔ مورد inspect (اگر possible)
['click','scroll','submit'].forEach(t => 
  window.addEventListener(t, e => 
    parent.postMessage({__inspector_event: true, type: t, ts: Date.now(), 
                        selector: e.target?.tagName, x: e.clientX, y: e.clientY}, '*')
  )
);
این روش ایده‌آل است ولی شرط دارد: کاربر باید این snippet را در پروژه‌های inspectable خودش inject کرده باشد. اگر نکرده، روش ۲.

روش ۲ (fallback - عمدتاً برای پروژه‌های third-party): استنتاج از فریم‌ها

از مقایسهٔ pixel-diff بین فریم‌های متوالی (با Pillow در backend) تشخیص بده:

آیا صفحه scroll شده (تغییر کل تصویر در محور Y) → ثبت scroll event
آیا یک تغییر موضعی رخ داده (تغییر در یک ناحیه) → احتمالاً click → ثبت inferred_click {area: bbox}
آیا URL تغییر کرده (از تغییر URL bar در صورت visible) → ثبت navigation
این روش approximate است ولی برای پرامپت کافی است.

تصمیم: روش ۱ را اول امتحان کن. اگر در ۲ ثانیه اول هیچ message دریافت نکردی، روش ۲ را فعال کن. هر دو روش رو در کد داشته باش.

ذخیرهٔ events: array از {ts: ms_since_start, type: string, details: object, source: 'postmessage'|'inferred'}.

۴.۵ سنتز پرامپت قوی — تعریف "قوی"
معیار «قوی» (که در آخر برای verify معتبر است):

✅ شامل یادداشت اولیه (raw_idea کاربر) به‌صورت دست‌نخورده در ابتدا
✅ شامل بلوک منشأ که می‌گوید این پرامپت از کجا آمده (ضبط بازرس ویژه، حالت A، تاریخ، مدت، لینک ویدئو در تلگرام)
✅ شامل Transcript تمیز صدا با timestamp (نه فقط متن خام)
✅ شامل خلاصهٔ بصری که توالی زمانی صفحات و تعاملات را توصیف می‌کند
✅ شامل استخراج intent: AI باید بفهمد «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه را رد می‌کند، کجاها منظورش است» — نه فقط transcribe خام
✅ شامل قطعات لاگ مرتبط (خطاها و warning های مرتبط با timestampهای فعالیت)
✅ شامل توصیف تعاملات (کلیک‌ها، اسکرول‌ها، navigationها) به زبان طبیعی
✅ شامل فهرستی از فایل‌ها/URLs/کامپوننت‌ها که AI تشخیص داده مرتبط هستند
✅ بدون truncation در هر اندازه‌ای — حتی اگر ۱۰۰هزار کاراکتر شد
✅ ساختاریافته با header markdown قابل خوانش توسط مدل AI بعدی
✅ actionable — یک agent دیگر باید بتواند با فقط این پرامپت کار را پیش ببرد

ساختار explicit پرامپت نهایی (که در OversightTask.prompt ذخیره می‌شود)
# {یادداشت اولیهٔ کاربر — عین متن textarea}

---

## 📍 منشأ این پرامپت

- **منبع**: ضبط در تب بازرس ویژه — حالت A (مانیتور iframe پروژه)
- **پروژه**: `{repo_full_name}`
- **تاریخ ضبط**: {ISO datetime}
- **مدت**: {minutes}m {seconds}s
- **ویدئو**: ارسال شد به تلگرام در پیام `{telegram_message_id or '—'}` (در سرور ذخیره نشد)
- **session_id**: `{session_id}` (موقت)

## 🎙 Transcript صدای کاربر (با timestamp)

[00:00] {…}
[00:12] {…}
...

## 🔍 خلاصهٔ بصری AI — توالی صفحات و رویدادها

[00:00-00:08] صفحهٔ X با عنصر Y دیده می‌شود. کاربر روی Z کلیک می‌کند.
[00:08-00:20] تغییر صفحه به URL='...'. اسکرول به وسط. کاربر در توضیح صوتی به دکمهٔ "..." اشاره می‌کند.
...

## 🎯 intent استخراج‌شده توسط AI

کاربر می‌خواهد: {یک خط واضح}
رد می‌کند: {اگر چیزی را رد کرد}
محل اشاره: {فایل/کامپوننت/صفحه‌ای که منظورش بوده}

## 🖱 تعاملات کاربر

| زمان | نوع | جزئیات |
|---|---|---|
| 00:03 | click | selector="button.submit" |
| 00:05 | scroll | y=420 |
...

## 🌐 [حالت B فقط] نقشهٔ زمانی فعالیت

| زمان | محل | مرتبط با پروژه؟ | فعالیت |
|---|---|---|---|
| 00:00-00:05 | تب کروم github.com/{repo} | ✓ بله | مرور کد |
| 00:05-00:10 | تب کروم imdb.com | ✗ خیر — خارج | (نامرتبط — حذف از بدنهٔ پرامپت) |
| 00:10-00:20 | پنجرهٔ VS Code | ✓ بله (دستی تشخیص داده شد) | ویرایش `auth.py` |
...

## 📋 لاگ‌های frontend (Console)
{relevant console errors/warnings from iframe}

## 📋 لاگ‌های backend (Render)
{relevant backend errors/warnings during recording period}

## 🔗 منابع شناسایی‌شده (URLs, فایل‌ها، کامپوننت‌ها)
- {url1}
- {file1.py}
- {component2.tsx}
## 💡 پرامپت قابل اجرای نهایی (برای agent دیگر)
{متن نهایی جامع که agent دیگر می‌تواند با آن کار را پیش ببرد — این بخش توسط AI با system prompt مخصوص تولید می‌شود}
۵. مشخصات دقیق حالت B — ضبط آزاد چندصفحه‌ای
۵.۱ منبع ویدئو
navigator.mediaDevices.getDisplayMedia({ video: {frameRate: 15}, audio: system_audio }) — کاربر در dialog مرورگر یک تب/پنجره/screen انتخاب می‌کند.

۵.۲ صدا
ترکیب میکروفون + system audio (اگر فعال) — همان روش حالت A بخش ۴.۲.

۵.۳ تشخیص محل کاربر (سه دسته جداگانه)
هر ۵ ثانیه یک screenshot از فریم استخراج می‌شود (از ویدئو blob با canvas) و به describe_screenshot_with_vision با system prompt اختصاصی:

این یک فریم از ضبط آزاد است. تشخیص بده در این فریم کاربر کجاست:
دسته‌ها:
1. تب کروم — اگر نوار آدرس مرورگر دیده می‌شود:
   - URL را از نوار آدرس استخراج کن
   - اگر domain یا path شامل '{repo_full_name}' یا '{render_preview_domain}' است → «مرتبط با پروژه»
   - وگرنه → «خارج از پروژه»
   
2. برنامهٔ دیگر دسکتاپ — اگر title bar یک برنامه (نه مرورگر) دیده می‌شود:
   - نام برنامه را تشخیص بده (VS Code, Slack, Terminal, …)
   - اگر نام پروژه یا فایل‌های آن visible است → «مرتبط (دستی)»
   - وگرنه → «خارج (دستی)»
3. دسکتاپ خام — اگر هیچ پنجرهٔ مشخصی روی صفحه نیست (icons + wallpaper):
   - دسته: «desktop»
   - فعالیت: کاربر در حال جابجایی پنجره‌ها یا navigation است
برای هر فریم خروجی بده:
{
  "category": "chrome_tab" | "other_app" | "desktop",
  "related_to_project": bool,
  "url_or_app_name": str,
  "activity_description": str (یک جمله),
  "user_action_inferred": str (مقایسه با فریم قبل: clicked/scrolled/typed/navigated)
}
۵.۴ تعاملات استنتاجی
چون event listening روی صفحات دیگر ممکن نیست:

AI vision (همان call بالا) خودش استنتاج می‌کند: «در این فاصلهٔ زمانی کاربر چه کاری انجام داد؟»
تغییرات بزرگ تصویر (با pixel-diff) → ثبت timestamp + توصیف AI
۵.۵ سنتز پرامپت
مشابه حالت A ولی با system prompt متفاوت برای text-generation نهایی:

کاربر در حال ضبط آزاد بین چند صفحه/برنامه بوده. ورودی‌های زیر را ترکیب کن و یک پرامپت قوی بساز که:
۱) ترتیب زمانی فعالیت‌های کاربر را به زبان طبیعی توصیف کند — کجا رفت، چه دید، چه کار کرد.
۲) اگر کاربر بین چند منبع جابجا شد، ارتباط بین آنها را در پرامپت توضیح بده (مثلاً «کاربر کد را در GitHub خواند، سپس به IDE رفت و فایل را باز کرد»).
۳) اگر کاربر به جایی خارج از پروژه رفت ولی در صدا به آن **اشاره نکرد** → از پرامپت نهایی حذف کن (نامرتبط).
۴) اگر کاربر به جایی خارج رفت **و در صدا به آن اشاره کرد** (مثلاً «این تو IMDB رو ببین، می‌خوام مثل این UI رو بسازم») → آن را در پرامپت به‌عنوان «مرجع/الهام» نگه دار.
۵) intent اصلی کاربر را در یک پاراگراف بالای پرامپت قرار بده.
۶. flow کامل — از کلیک تا تحویل
1. کاربر روی 🎬 کلیک
   ↓
2. Picker باز: انتخاب حالت A/B + تنظیمات صدا + منبع میکروفون
   ↓
3. کلیک [شروع ضبط]:
   - Frontend: POST /api/recording/inspector/start با {project_id, mode, audio_config, fps?}
   - Backend: ایجاد session، اگر mode='A' و روش Playwright انتخاب شد → شروع Playwright screencast
   - Frontend: شروع MediaRecorder برای صدا (mic + optional system audio)
   - Frontend: اگر mode='A' و postMessage bridge ممکن → شروع listener
   - Frontend: نمایش status bar
   ↓
4. در طول ضبط (background):
   - audio chunks هر 10s → POST /api/recording/inspector/{session_id}/audio-chunk
   - interactions هر 5s → POST /api/recording/inspector/{session_id}/interactions
   - backend logs sync هر 10s → POST /api/render/logs/fetch
   - frontend: نگه‌داری console logs در state (موجود)
   ↓
5. کاربر [⏹ پایان] (یا 60min auto-stop):
   - Frontend: MediaRecorder.stop()، ارسال آخرین chunks، POST /api/recording/inspector/{session_id}/stop
   ↓
6. Backend: نمایش modal "در حال آماده‌سازی" در frontend
   ↓
7. Backend processing (در سرور، با progress events که به frontend stream می‌شوند):
   a. مونتاژ video از frames (mode A) یا blob (mode B) با ffmpeg یا fallback
   b. مونتاژ audio chunks در یک فایل
   c. transcribe صدا (با audio_transcribe ماژول auto-select)
   d. استخراج 6-12 keyframe از ویدئو (هر دقیقه یکی، یا بر اساس scene-change)
   e. parallel: ارسال هر keyframe به describe_screenshot_with_vision()
   f. (mode B): پردازش frame analysis برای location tracking
   g. سنتز پرامپت قوی: ترکیب transcript + visual + interactions + logs → AI text model
   ↓
8. Frontend: modal پیش‌نمایش با همهٔ خروجی‌ها
   ↓
9. کاربر می‌بیند، یادداشت اضافه می‌کند، transcript را اصلاح می‌کند (اختیاری):
   - اگر transcript را اصلاح کرد → [تولید مجدد پرامپت] → step 7g دوباره
   ↓
10. کاربر [ارسال نهایی] می‌زند:
    - Frontend: POST /api/recording/inspector/{session_id}/finalize با {user_note, send_to_oversight, edited_transcript?}
    - Backend:
      i. ارسال ویدئو به تلگرام با send_document (caption شامل خلاصهٔ پرامپت)
      ii. اگر size > 45MB → split به chunks 45MB و چند call پشت سر هم با caption شماره‌گذاری‌شده
      iii. حذف کامل /tmp/inspector_recordings/{session_id} از disk
      iv. اگر send_to_oversight=true:
          POST /api/oversight/tasks/from-inspector-recording (endpoint جدید):
            - raw_idea = user_note
            - prompt = پرامپت قوی structure شده (طبق ۴.۵)
            - project_id = inspector's loaded project_id
          → برمی‌گرداند task_id
      v. اگر send_to_oversight=false:
          ارسال prompt به چت inspector (همان مسیر sendToOversight('visual_debug') با pre-built prompt)
    - Backend: برمی‌گرداند {success, task_id?, telegram_message_id?, chat_message_id?}
    ↓
11. Frontend: modal بسته می‌شود، toast «ارسال موفق» + لینک به تسک یا پیام چت
۷. ساختار endpoint‌های جدید
Path	Method	Body	Returns
/api/recording/inspector/start	POST	{project_id, mode: 'A'|'B', audio_config: {mic, system, mic_device_id?}, target_fps?}	{session_id, started_at, screencast_method?}
/api/recording/inspector/{sid}/audio-chunk	POST	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/video-chunk	POST (فقط mode B)	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/interactions	POST	{events: [{ts, type, details, source}]}	{ok}
/api/recording/inspector/{sid}/stop	POST	empty	{ok}
/api/recording/inspector/{sid}/process	POST	empty	SSE stream از پیشرفت پردازش
/api/recording/inspector/{sid}/preview	GET	—	{video_url_temp, transcript, visual_summary, interactions, logs, prompt}
/api/recording/inspector/{sid}/regenerate-prompt	POST	{edited_transcript}	{prompt}
/api/recording/inspector/{sid}/finalize	POST	{user_note, send_to_oversight, edited_transcript?}	{success, task_id?, telegram_message_id?, chat_message_id?}
/api/recording/inspector/{sid}/cancel	POST	empty	{ok} (cleanup کامل)
/api/oversight/tasks/from-inspector-recording	POST	{project_id, user_request, pre_built_prompt, recording_metadata}	{task_id, watched_id}
همگی در یک فایل جدید backend/app/api/routes/inspector_recording.py. در backend/app/main.py با prefix /api ثبت شود.

سرویس اصلی: backend/app/services/inspector_recording_service.py.

state جلسات: در RAM (یک dict در service singleton). cleanup خودکار: هر session که >۱۲۰ دقیقه active است auto-cancel + cleanup.

۸. مسیر فایل‌ها و حذف
ذخیرهٔ موقت: /tmp/inspector_recordings/{session_id}/
frames/ (mode A فقط)
audio_chunks/
video_chunks/ (mode B فقط)
final.webm (پس از مونتاژ)
transcript.txt
visual_summary.json
events.json
حذف کامل: پس از ارسال موفق به تلگرام یا پس از فراخوانی cancel
در صورت crash backend مید-process: cron-like job در scheduler_tick هر session با age > 120m را cleanup کند (اضافه به _sweep_pending_external_verifies یا یک sweeper جدید)
۹. مدیریت اندازهٔ فایل تلگرام
bitrate پیش‌فرض ویدئو: 600 kbps (برای 60min ≈ 270MB → بزرگ)
استراتژی: اگر فایل > 45MB → split
با ffmpeg -ss start -t duration -c copy chunk_N.webm برش بدون re-encode
هر chunk حداکثر 45MB
ارسال پشت سر هم با caption [1/N], [2/N], ..., [N/N] — caption نهایی شامل خلاصهٔ پرامپت
اگر تلگرام fail کرد:

۳ بار retry با backoff [2s, 5s, 10s]
اگر بازم fail:
نگه‌داری فایل برای ۲۴ ساعت در /tmp/inspector_recordings/{session_id}/
notification به کاربر: «ویدئو به تلگرام ارسال نشد. دانلود از: {temporary_download_url} (۲۴ ساعت معتبر)»
{temporary_download_url} در پرامپت تسک (بخش منشأ) درج می‌شود
پس از ۲۴ ساعت auto-cleanup
۱۰. transcribe صدا — استفاده از pattern موجود
⛔ هرگز موازی نساز. کاربر صریح:

«این از همون قابلیت دیباگ بصری باید برای استخراج استفاده کنه و مگه یادت نیست برای استخراج بصری مدل بصری خودکار انتخاب می‌شد یا لیست می‌اومد که انتخاب کنیم ... این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی‌اش کنی و لازم نیست چیز موازی انجام بدی»

روند بررسی پیش از ساخت:

در backend/app/services/oversight_inspector_bridge.py بگرد آیا تابعی برای transcribe صوت با همان pattern auto-select وجود دارد (search: audio, transcribe, whisper, speech)
در backend/app/services/ بگرد فایل‌هایی به نام audio_*.py, *_transcribe*.py, *_speech*.py
در مدل‌های موجود get_vision_models()/get_models() ببین آیا مدل‌هایی با capability=audio یا transcribe ثبت شده‌اند
اگر پیدا شد: همان را استفاده کن، حتی اگر نام transcribe_audio() ندارد و باید با adapter کوچک wrap شود.

اگر پیدا نشد: یک ماژول جدید backend/app/services/audio_transcribe.py با عین pattern describe_screenshot_with_vision بساز:

تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → {"text": str, "language": str, "duration_sec": float, "model_used": str}
داخل: شبیه oversight_inspector_bridge.py:61-89:
audio_models = get_audio_models() or [] (اگر get_audio_models موجود نیست، از env var check استفاده کن)
اولین provider در دسترس را انتخاب کن (OpenAI Whisper / Groq Whisper / …)
اگر هیچ‌کدام در دسترس نیست: log warning + برگردان {text: "", language: "", duration_sec: 0, model_used: ""} با هشدار در پرامپت نهایی «transcript صوت در دسترس نبود — هیچ provider برای transcribe پیکربندی نشده. می‌توانید OPENAI_API_KEY یا GROQ_API_KEY در Render env اضافه کنید»
هیچ کجا API call مستقیم به OpenAI/Anthropic/… نزن — همه از طریق ai_manager یا get_*_models() patterns.

۱۱. ساخت تسک و حفظ یادداشت اولیه (دقیق)
⛔ یادداشت اولیه را با پرامپت AI مخلوط نکن. کاربر صریح:

«اون یادداشت‌های اولیه که برای هر تسک میاد که باید باشه سر جاش ... البته اون یادداشت‌های اول پرامپت‌های تسک‌ها (نسخه به‌روزشده‌ش) باید ابتداش قرار بگیره و همچنین دقیق بگه از کجا ریشه گرفته»

ترجمه به ساختار:

اگر send_to_oversight=true:

یک تسک جدید با POST /api/oversight/tasks/from-inspector-recording
در OversightTask:
raw_idea = user_note (یادداشت اولیهٔ کاربر، عین متن textarea — نسخهٔ به‌روزشده‌اش که الان کاربر می‌نویسد، نه آنچه در ضبط گفت)
prompt = ساختار کامل طبق بخش ۴.۵ که با همان user_note در ابتدا شروع می‌شود (به عنوان heading اول)، بعد بلوک منشأ، بعد بقیه
type = "feature_request" (پیش‌فرض)
priority = "medium" (پیش‌فرض)
watched_id = پروژهٔ iframe
project_full_name = repo_full_name
فیلدهای موجود acceptance_criteria, task_steps = [] (تولید AC در این مرحله out of scope — اگر AI آنها را در پرامپت ذکر کرده، فقط در متن پرامپت می‌مانند)
target_files = [] (همان)
برای endpoint from-inspector-recording:

این یک endpoint جدید است. در backend/app/api/routes/oversight.py (همان فایلی که from-inspector در آن است) اضافه شود:

class FromInspectorRecordingRequest(BaseModel):
    project_id: str
    user_request: str          # یادداشت اولیه (raw_idea)
    pre_built_prompt: str      # پرامپت قوی آماده — هیچ پردازش جدید
    recording_metadata: Dict[str, Any]
    # شامل: {duration_sec, mode: 'A'|'B', telegram_message_id?, video_size_mb, session_id, completed_at}

@router.post("/tasks/from-inspector-recording")
async def create_task_from_inspector_recording(payload: FromInspectorRecordingRequest):
    service = get_oversight_service()
    # ⚠️ بر خلاف /from-inspector که خودش پرامپت می‌سازد، اینجا پرامپت آماده است
    # وظیفه: فقط ساخت تسک + حفظ raw_idea + نگه‌داری prompt
    new_task = await service.create_task(
        watched_id=resolve_watched_id_from_project_id(payload.project_id),
        title=extract_title_from_prompt(payload.pre_built_prompt) or "ضبط بازرس ویژه",
        raw_idea=payload.user_request,
        prompt=payload.pre_built_prompt,
        type="feature_request",
        priority="medium",
        execution_mode="manual",
    )
    return {"task_id": new_task.id, "watched_id": new_task.watched_id}
اگر send_to_oversight=false:

پرامپت کامل به چت inspector ارسال می‌شود (همان مسیر فعلی sendToOversight('visual_debug') با pre_built_prompt به‌جای regenerate)
یادداشت اولیه به‌عنوان bold heading در ابتدای پیام چت قرار می‌گیرد
۱۲. ارسال به تلگرام — جزئیات caption
caption تلگرام (حداکثر ۱۰۲۰ char، طبق محدودیت Telegram Bot API):

🎬 ضبط بازرس ویژه — حالت {🎯 A: iframe-focused | 🌐 B: free-multi-screen}
📁 پروژه: {repo_full_name}
⏱ مدت: {minutes}m {seconds}s
{اگر تسک ساخته شد: 🆔 task: {task_id_short} | 🔗 {frontend_url}/projects/{pid}/tasks/{task_id}}
{اگر به چت رفت: 💬 ارسال شد به چت inspector}
📝 یادداشت اولیه کاربر:
{user_note[:200]}{اگر بیشتر: ...}
🤖 خلاصهٔ پرامپت AI:
{first 500 chars of pre_built_prompt body section, after the source block}
اگر طول از 1020 char بیشتر شد، خلاصهٔ پرامپت کوتاه می‌شود (نه یادداشت اولیه).

۱۳. محدودیت‌های زمانی و امن
محدودیت	مقدار
حداکثر مدت ضبط	۶۰ دقیقه (سپس auto-stop + warning)
حداکثر bitrate ویدئو	1 Mbps (mode B) / 600 kbps پیشنهادی
target fps mode A	2 fps (Playwright screencast) — صرفه‌جویی
target fps mode B	15 fps (getDisplayMedia native)
حداکثر اندازه فایل قبل از split	45 MB
Idle session cleanup	۱۲۰ دقیقه بدون finalize → auto-cancel
Transcribe timeout	۹۰ ثانیه
Vision call timeout per screenshot	۳۰ ثانیه
تعداد screenshot استخراجی برای vision	min(۱۲, max(۶, duration_minutes))
Polling interval mode A (fallback)	۵۰۰ ms
۱۴. وابستگی‌های جدید
ffmpeg در Dockerfile: چک کن از قبل هست یا نه (با which ffmpeg در یک تست container). اگر هست استفاده کن. اگر نیست:
اولویت اول: اضافه به Dockerfile (یک خط apt install ffmpeg)
اگر سایز image دغدغه است: استفاده از pillow برای ساخت GIF animated (کیفیت پایین ولی بدون dependency)
pillow (PIL) برای pixel-diff تشخیص scroll/click — احتمالاً از قبل نصب است
frontend: بدون package جدید، فقط Web APIs (MediaRecorder, getDisplayMedia, getUserMedia, Web Audio API)
۱۵. منع‌های قطعی
❌ هرگز ویدئو را در DB یا persistent storage ذخیره نکن (هیچ مدل دیتابیس جدید برای ویدئو)
❌ هرگز ویدئو را پس از ارسال موفق به تلگرام نگه‌دار (حذف immediate)
❌ هرگز کد describe_screenshot_with_vision یا oversight_inspector_bridge.py را modify نکن — فقط استفاده کن
❌ هرگز from-inspector endpoint موجود را خراب نکن — فقط from-inspector-recording جدید اضافه کن
❌ هرگز پیش‌فرض چک‌باکس "ارسال به مرکز نظارت" را روشن نکن (کاربر صریح)
❌ هرگز پرامپت تولیدشده را در backend truncate نکن — بدون محدودیت توکن
❌ هرگز یادداشت اولیه (raw_idea) را با پرامپت AI overwrite کن — جدا نگه دار
❌ هرگز حالت B را حذف یا غیرفعال نکن — کاربر صریح: «حتی غیرفعالش هم نکن»
❌ هرگز بدون تأیید کاربر در preview modal چیزی ارسال نکن
❌ هرگز mode A را با getDisplayMedia پیاده نکن — کاربر صریح می‌خواهد iframe-only باشد، نه پنجرهٔ picker مرورگر
❌ هرگز ضبط را در حالت B بدون پیکر مرورگر شروع نکن — getDisplayMedia() ذاتاً پیکر را باز می‌کند، این طبیعی است
۱۶. AC رفتار-محور (برای verify)
not done
دکمهٔ 🎬 ضبط ویدئو در کنار دکمهٔ screenshot در تب بازرس ویژه قابل مشاهده است
not done
کلیک روی دکمه، Picker حالت A/B با تنظیمات صدا و انتخاب میکروفون باز می‌شود
not done
حالت A: ضبط ویدئو از مانیتور iframe بدون نمایش پیکر مرورگر شروع می‌شود
not done
حالت B: ضبط با getDisplayMedia() و پیکر مرورگر شروع می‌شود
not done
در هر دو حالت، نوار وضعیت با شمارش زمان و حالت نمایش داده می‌شود
not done
auto-stop در ۶۰ دقیقه با هشدار به کاربر
not done
پس از stop، modal "در حال آماده‌سازی" با progress mراحل (transcribe, vision, sync) نمایش داده می‌شود
not done
modal پیش‌نمایش شامل: ویدئو، transcript قابل ویرایش، خلاصهٔ بصری، لاگ‌ها، تعاملات، پرامپت قوی نهایی، چک‌باکس "ارسال به مرکز نظارت" که پیش‌فرض خاموش است
not done
در حالت A، پرامپت قوی شامل تعاملات کاربر داخل iframe (کلیک‌ها، اسکرول‌ها) با timestamp است
not done
در حالت B، پرامپت قوی شامل نقشهٔ زمانی فعالیت با دسته‌بندی (chrome_tab/other_app/desktop) و فلگ مرتبط/خارج از پروژه است
not done
پرامپت قوی بدون truncation ذخیره و نمایش داده می‌شود (می‌تواند هر طولی باشد)
not done
یادداشت اولیه کاربر (textarea) عیناً در OversightTask.raw_idea ذخیره می‌شود
not done
پرامپت قوی در OversightTask.prompt ذخیره می‌شود — با ساختار: یادداشت اولیه در heading اول → بلوک منشأ → بقیه
not done
با ارسال نهایی + چک‌باکس روشن: تسک جدید زیر پروژهٔ iframe ایجاد می‌شود (watched_id صحیح)
not done
با ارسال نهایی + چک‌باکس خاموش: پرامپت کامل به چت inspector ارسال می‌شود (دیده می‌شود مثل screenshot)
not done
ویدئو با caption شامل خلاصه به تلگرام ارسال می‌شود
not done
اگر ویدئو > 45MB، به chunks تقسیم و چندتا send_document پشت سر هم
not done
فایل ویدئو از /tmp/inspector_recordings/ پس از ارسال موفق به تلگرام حذف می‌شود (با os.path.exists تأیید شود)
not done
اگر کاربر "لغو و دور انداختن" را زد، فایل حذف و هیچ ارسالی صورت نمی‌گیرد
not done
AI vision (در حالت B) درست تشخیص می‌دهد کاربر در یک تب دیگر کروم خارج از پروژه است (تست با ضبط شامل تب IMDB یا مشابه)
not done
AI درست تشخیص می‌دهد اگر کاربر در صدا به چیزی خارج از iframe اشاره کرد (حالت A) و آن را در پرامپت لحاظ نمی‌کند
not done
AI درست intent را استخراج می‌کند: «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، کجاها منظورش است» (تست با ضبط حاوی صدای ابهام‌آمیز)
not done
مکانیزم visual debug (describe_screenshot_with_vision) unchanged باقی می‌ماند — کد آن لمس نشده
not done
linter بدون warning عبور می‌کند
not done
type-check موفق است (tsc --noEmit / mypy)
not done
هیچ migration دیتابیس مورد نیاز نیست (state در RAM، فایل‌ها در /tmp)
۱۷. وابستگی‌های upstream / downstream / cross-tier / side
upstream (متکی به):

describe_screenshot_with_vision (oversight_inspector_bridge.py:44) — استفاده، نه تغییر
TelegramChannel.send_document (notification_service.py:579) — استفاده
GET/POST /api/render/logs* (render_logs.py) — استفاده
OversightTask.raw_idea و OversightTask.prompt (oversight_service.py:328-407) — استفاده
inspectorIframeRef و _readIframeActualUrl() (page.tsx) — استفاده
inspectorConsoleLogs state (page.tsx) — خواندن
وجود Playwright در backend/app/services/browser_automation.py (یا معادل) — استفاده برای screencast
downstream (متکی این تسک می‌شوند):

چک‌باکس sendToOversightVisual (page.tsx:673-675) — همچنان همان معنا، فقط در modal جدید
تابع sendToOversight('visual_debug') (page.tsx:5198) — یک branch اضافه برای pre-built prompt
cross-tier (backend ↔ frontend):

۱۰ endpoint جدید REST
type definitions جدید: RecordingSession, FinalizeResult, FromInspectorRecordingRequest
state جدید frontend: videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession
side artifacts:

.env.example: اضافه شود (اگر transcribe نیاز به provider جدید داشت) — مثلاً # اختیاری برای transcribe سریع: GROQ_API_KEY=
README: یک پاراگراف کوتاه دربارهٔ این feature در بخش "بازرس ویژه"
Dockerfile: شاید ffmpeg (اگر نیست)
هیچ migration DB
هیچ تغییر i18n
هیچ تغییر در requirements.txt (تمام dependencies موجود کافی است)
۱۸. ترتیب پیاده‌سازی (foundation → core → integration → polish)
این تسک بزرگ است. به ۶ commit متوالی push شود (هر کدام به‌تنهایی verify-able):

Commit 1 — foundation

inspector_recording_service.py با stub functions
inspector_recording.py با endpoints stub (همگی برمی‌گردانند dummy data)
state in-memory + cleanup sweeper
ثبت در main.py
Commit 2 — audio transcribe

بررسی oversight_inspector_bridge.py و سایر فایل‌ها برای audio transcribe موجود
اگر هست: adapter ساده
اگر نیست: audio_transcribe.py با pattern auto-select از env vars (دقیقاً مثل describe_screenshot_with_vision)
تست: یک wav کوچک → text برگردد یا warning واضح
Commit 3 — recording mode A (iframe)

Playwright screencast: endpoint start-screencast + frame extraction
audio capture frontend + chunk upload
log sync (frontend console + backend Render logs)
postMessage bridge attempt + pixel-diff fallback برای interactions
مونتاژ نهایی frames به ویدئو
vision analysis از keyframes
prompt synthesis (متن نهایی بدون tructaion)
Commit 4 — recording mode B (free)

frontend MediaRecorder با getDisplayMedia
video chunk upload
vision-based location classifier (chrome_tab/other_app/desktop)
AI integration برای intent extraction در حالت B
prompt synthesis مخصوص حالت B
Commit 5 — finalize + Telegram + task creation

ارسال به Telegram با split برای فایل‌های بزرگ
حذف از disk
endpoint from-inspector-recording در oversight.py
اتصال به چت inspector (اگر چک‌باکس خاموش)
retry + fallback download URL
Commit 6 — UI کامل + polish

دکمهٔ ضبط در page.tsx
Picker modal
status bar
processing progress modal با SSE
preview modal با همهٔ بخش‌ها
regenerate prompt
error states + toasts
accessibility (keyboard navigation)
هر commit:

linter pass
type-check pass
syntax check (python -c "import ast; ast.parse(open(f).read())" برای py / tsc --noEmit برای ts)
push بلافاصله به default branch
بدون اجبار خلاصه‌سازی — اگر کامیت بزرگ شد، صبر کن تا تمام شود
۱۹. مدیریت TO-DO
این تسک هیچ بخش Manual-required ندارد — همه چیز قابل پیاده‌سازی خودکار است. کاربر تصمیمی سلیقه‌ای از ما نمی‌خواهد، credential خاصی لازم نیست (مگر اینکه provider transcribe در env نباشد، که آن یک TO-DO کوچک معتبر است در لیست سفید مورد ۱: «Credential که فقط کاربر دارد» — می‌توانی یک TO-DO سبک برای GROQ_API_KEY یا OPENAI_API_KEY بسازی اگر env در زمان اجرا خالی بود، ولی همان موقع باید پرامپت با هشدار transcript خالی تولید شود — نه stop کامل).

اگر هیچ TO-DO ساختی، در commit message صریح بنویس:

«بررسی شد — این تسک هیچ بخش Manual-required ندارد. ۰ فایل TO-DO ساخته شد.»

پایان پرامپت
```

## 📋 چک‌لیست مراحل (6 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [x] **مرحله 1: پیاده‌سازی زیرساخت ضبط دوحالته (سرویس، endpoint‌ها، state در RAM)** — ایجاد فایل‌های اصلی backend: inspector_recording_service.py با کلاس RecordingSession و state in-memory (dict)، inspector_recording.py با تمام endpoint‌های جدید (start, audio-chunk, video-chunk, interactions, stop, process, preview, regenerate-prompt, finalize, cancel) به‌صورت stub که dummy data برمی
- [x] **مرحله 2: پیاده‌سازی transcribe صدا با استفاده از pattern موجود (audio_transcribe.py)** — بررسی فایل‌های backend/app/services/ برای وجود ماژول transcribe صدا (جستجوی کلیدواژه‌های audio, transcribe, whisper, speech). اگر ماژول موجود است (مثلاً در oversight_inspector_bridge.py یا فایل جداگانه)، یک adapter ساده در inspector_recording_service.py بنویس که از آن استفاده کند. اگر ماژول موجود نی
- [~] **مرحله 3: پیاده‌سازی ضبط حالت A (iframe) — Playwright screencast، صدا، لاگ‌ها، تعاملات، مونتاژ ویدئو، vision analysis، prompt synthesis** — پیاده‌سازی کامل حالت A (ضبط مانیتور iframe) در inspector_recording_service.py و inspector_recording.py. شامل: endpoint start-screencast که یک Playwright session headless باز می‌کند و هر 500ms screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/ ذخیره می‌کند. audio capture در fronte
- [x] **مرحله 4: پیاده‌سازی ضبط حالت B (آزاد) — getDisplayMedia، video chunk upload، vision-based location classifier، prompt synthesis مخصوص** — پیاده‌سازی کامل حالت B (ضبط آزاد چندصفحه‌ای) در inspector_recording_service.py. شامل: frontend MediaRecorder با getDisplayMedia برای ویدئو و صدا. video chunk upload به backend هر 5 ثانیه. vision-based location classifier: هر 5 ثانیه یک screenshot از فریم ویدئو استخراج می‌شود و به describe_screenshot
- [x] **مرحله 5: پیاده‌سازی finalize — ارسال به تلگرام با split، حذف از disk، task creation، ارسال به چت inspector** — پیاده‌سازی کامل مرحله finalize در inspector_recording_service.py و inspector_recording.py. شامل: endpoint POST /api/recording/inspector/{sid}/finalize که user_note, send_to_oversight, edited_transcript را دریافت می‌کند. ارسال ویدئو به تلگرام با send_document از notification_service.py با caption شام
- [x] **مرحله 6: پیاده‌سازی کامل UI frontend — دکمه ضبط، picker حالت، status bar، processing modal، preview modal، regenerate** — پیاده‌سازی کامل UI در frontend/src/app/projects/[id]/page.tsx. شامل: دکمه 🎬 ضبط ویدئو در کنار دکمه screenshot (خط 13694-13705) با شرط شلوغ نشدن UI. Picker modal با دو کارت (حالت A و B) و تنظیمات صدا (چک‌باکس میکروفون، دراپ‌دان منبع میکروفون، چک‌باکس صدای سیستم). Status bar شناور پایین صفحه با شمارش 

---

# 🔹 مرحله 1: پیاده‌سازی زیرساخت ضبط دوحالته (سرویس، endpoint‌ها، state در RAM)

**Scope:** ایجاد فایل‌های اصلی backend: inspector_recording_service.py با کلاس RecordingSession و state in-memory (dict)، inspector_recording.py با تمام endpoint‌های جدید (start, audio-chunk, video-chunk, interactions, stop, process, preview, regenerate-prompt, finalize, cancel) به‌صورت stub که dummy data برمی‌گردانند. ثبت router در main.py با prefix /api. اضافه کردن sweeper برای cleanup session‌های older از 120 دقیقه. هیچ منطق واقعی ضبط یا پردازش در این مرحله پیاده‌سازی نمی‌شود — فقط ساختار و stub. endpoint‌ها: POST /api/recording/inspector/start, POST /api/recording/inspector/{sid}/audio-chunk, POST /api/recording/inspector/{sid}/video-chunk, POST /api/recording/inspector/{sid}/interactions, POST /api/recording/inspector/{sid}/stop, POST /api/recording/inspector/{sid}/process, GET /api/recording/inspector/{sid}/preview, POST /api/recording/inspector/{sid}/regenerate-prompt, POST /api/recording/inspector/{sid}/finalize, POST /api/recording/inspector/{sid}/cancel. همچنین endpoint جدید POST /api/oversight/tasks/from-inspector-recording در oversight.py با مدل FromInspectorRecordingRequest. هیچ تغییری در فایل‌های موجود (render_logs.py, oversight_inspector_bridge.py, notification_service.py) داده نمی‌شود.
**Key terms:** backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, backend/app/main.py, backend/app/api/routes/oversight.py, RecordingSession, FromInspectorRecordingRequest, POST /api/recording/inspector/start, POST /api/recording/inspector/{sid}/audio-chunk, POST /api/recording/inspector/{sid}/video-chunk, POST /api/recording/inspector/{sid}/interactions, POST /api/recording/inspector/{sid}/stop, POST /api/recording/inspector/{sid}/process, GET /api/recording/inspector/{sid}/preview, POST /api/recording/inspector/{sid}/regenerate-prompt, POST /api/recording/inspector/{sid}/finalize, POST /api/recording/inspector/{sid}/cancel, POST /api/oversight/tasks/from-inspector-recording

**بخش مربوط از متن کاربر:**
```
۷. ساختار endpoint‌های جدید
Path	Method	Body	Returns
/api/recording/inspector/start	POST	{project_id, mode: 'A'|'B', audio_config: {mic, system, mic_device_id?}, target_fps?}	{session_id, started_at, screencast_method?}
/api/recording/inspector/{sid}/audio-chunk	POST	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/video-chunk	POST (فقط mode B)	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/interactions	POST	{events: [{ts, type, details, source}]}	{ok}
/api/recording/inspector/{sid}/stop	POST	empty	{ok}
/api/recording/inspector/{sid}/process	POST	empty	SSE stream از پیشرفت پردازش
/api/recording/inspector/{sid}/preview	GET	—	{video_url_temp, transcript, visual_summary, interactions, logs, prompt}
/api/recording/inspector/{sid}/regenerate-prompt	POST	{edited_transcript}	{prompt}
/api/recording/inspector/{sid}/finalize	POST	{user_note, send_to_oversight, edited_transcript?}	{success, task_id?, telegram_message_id?, chat_message_id?}
/api/recording/inspector/{sid}/cancel	POST	empty	{ok} (cleanup کامل)
/api/oversight/tasks/from-inspector-recording	POST	{project_id, user_request, pre_built_prompt, recording_metadata}	{task_id, watched_id}
همگی در یک فایل جدید backend/app/api/routes/inspector_recording.py. در backend/app/main.py با prefix /api ثبت شود.

سرویس اصلی: backend/app/services/inspector_recording_service.py.

state جلسات: در RAM (یک dict در service singleton). cleanup خودکار: هر session که >۱۲۰ دقیقه active است auto-cancel + cleanup.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد زیرساخت ضبط دوحالته (سرویس، endpointها، state در RAM)

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی زیرساخت ضبط دوحالته (سرویس، endpointها، state در RAM) را دارد. این شامل ایجاد فایل‌های اصلی backend است: `backend/app/services/inspector_recording_service.py` با کلاس `RecordingSession` و state in-memory (dict)، و `backend/app/api/routes/inspector_recording.py` با تمام endpointهای جدید (start, audio-chunk, video-chunk, interactions, stop, process, preview, regenerate-prompt, finalize, cancel) به‌صورت stub که dummy data برمی‌گردانند. همچنین ثبت router در `backend/app/main.py` با prefix `/api` و اضافه کردن sweeper برای cleanup session‌های older از 120 دقیقه. هیچ منطق واقعی ضبط یا پردازش در این مرحله پیاده‌سازی نمی‌شود — فقط ساختار و stub. endpointها: `POST /api/recording/inspector/start`, `POST /api/recording/inspector/{sid}/audio-chunk`, `POST /api/recording/inspector/{sid}/video-chunk`, `POST /api/recording/inspector/{sid}/interactions`, `POST /api/recording/inspector/{sid}/stop`, `POST /api/recording/inspector/{sid}/process`, `GET /api/recording/inspector/{sid}/preview`, `POST /api/recording/inspector/{sid}/regenerate-prompt`, `POST /api/recording/inspector/{sid}/finalize`, `POST /api/recording/inspector/{sid}/cancel`. همچنین endpoint جدید `POST /api/oversight/tasks/from-inspector-recording` در `backend/app/api/routes/oversight.py` با مدل `FromInspectorRecordingRequest`. هیچ تغییری در فایل‌های موجود (`render_logs.py`, `oversight_inspector_bridge.py`, `notification_service.py`) داده نمی‌شود. بخش مربوط از درخواست اصلی کاربر شامل جدول endpointها با path، method، body و returns است. کلیدواژه‌ها: `backend/app/services/inspector_recording_service.py`, `backend/app/api/routes/inspector_recording.py`, `backend/app/main.py`, `backend/app/api/routes/oversight.py`, `RecordingSession`, `FromInspectorRecordingRequest`, `POST /api/recording/inspector/start`, `POST /api/recording/inspector/{sid}/audio-chunk`, `POST /api/recording/inspector/{sid}/video-chunk`, `POST /api/recording/inspector/{sid}/interactions`, `POST /api/recording/inspector/{sid}/stop`, `POST /api/recording/inspector/{sid}/process`, `GET /api/recording/inspector/{sid}/preview`, `POST /api/recording/inspector/{sid}/regenerate-prompt`, `POST /api/recording/inspector/{sid}/finalize`, `POST /api/recording/inspector/{sid}/cancel`, `POST /api/oversight/tasks/from-inspector-recording`. شواهد در کد واقعی: فایل `backend/app/main.py` (که routerها را ثبت می‌کند) و `backend/app/api/routes/oversight.py` (که endpoint جدید باید به آن اضافه شود) در deep context موجود هستند. فایل `backend/app/services/notification_service.py` (که نباید تغییر کند) و `backend/app/services/oversight_inspector_bridge.py` (که نباید تغییر کند) نیز در deep context هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل `backend/app/services/inspector_recording_service.py` با کلاس `RecordingSession` (شامل فیلدهای session_id, project_id, mode, audio_config, target_fps, started_at, status, chunks, interactions, transcript, prompt, metadata) و یک dict در سطح ماژول به نام `_sessions: Dict[str, RecordingSession]` برای نگهداری state در RAM. 2. پیاده‌سازی تابع `_sweep_stale_sessions()` که session‌های older از 120 دقیقه را auto-cancel و cleanup کند. 3. ایجاد فایل `backend/app/api/routes/inspector_recording.py` با تمام endpointهای ذکر شده (start, audio-chunk, video-chunk, interactions, stop, process,

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 2: پیاده‌سازی transcribe صدا با استفاده از pattern موجود (audio_transcribe.py)

**Scope:** بررسی فایل‌های backend/app/services/ برای وجود ماژول transcribe صدا (جستجوی کلیدواژه‌های audio, transcribe, whisper, speech). اگر ماژول موجود است (مثلاً در oversight_inspector_bridge.py یا فایل جداگانه)، یک adapter ساده در inspector_recording_service.py بنویس که از آن استفاده کند. اگر ماژول موجود نیست، فایل جدید backend/app/services/audio_transcribe.py با تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → dict بساز که از pattern auto-select مشابه describe_screenshot_with_vision استفاده کند: چک کردن env var برای provider (OPENAI_API_KEY, GROQ_API_KEY)، انتخاب اولین provider در دسترس، فراخوانی API، برگرداندن {text, language, duration_sec, model_used}. اگر هیچ provider در دسترس نیست، log warning و برگردان {text: '', language: '', duration_sec: 0, model_used: ''} با هشدار. هیچ API call مستقیم به OpenAI/Anthropic نزن — همه از طریق ai_manager یا get_*_models() patterns.
**Key terms:** backend/app/services/audio_transcribe.py, backend/app/services/oversight_inspector_bridge.py, transcribe_audio, get_audio_models, get_vision_models, OPENAI_API_KEY, GROQ_API_KEY, describe_screenshot_with_vision

**بخش مربوط از متن کاربر:**
```
۱۰. transcribe صدا — استفاده از pattern موجود
⛔ هرگز موازی نساز. کاربر صریح:

«این از همون قابلیت دیباگ بصری باید برای استخراج استفاده کنه و مگه یادت نیست برای استخراج بصری مدل بصری خودکار انتخاب می‌شد یا لیست می‌اومد که انتخاب کنیم ... این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی‌اش کنی و لازم نیست چیز موازی انجام بدی»

روند بررسی پیش از ساخت:

در backend/app/services/oversight_inspector_bridge.py بگرد آیا تابعی برای transcribe صوت با همان pattern auto-select وجود دارد (search: audio, transcribe, whisper, speech)
در backend/app/services/ بگرد فایل‌هایی به نام audio_*.py, *_transcribe*.py, *_speech*.py
در مدل‌های موجود get_vision_models()/get_models() ببین آیا مدل‌هایی با capability=audio یا transcribe ثبت شده‌اند
اگر پیدا شد: همان را استفاده کن، حتی اگر نام transcribe_audio() ندارد و باید با adapter کوچک wrap شود.

اگر پیدا نشد: یک ماژول جدید backend/app/services/audio_transcribe.py با عین pattern describe_screenshot_with_vision بساز:

تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → {"text": str, "language": str, "duration_sec": float, "model_used": str}
داخل: شبیه oversight_inspector_bridge.py:61-89:
audio_models = get_audio_models() or [] (اگر get_audio_models موجود نیست، از env var check استفاده کن)
اولین provider در دسترس را انتخاب کن (OpenAI Whisper / Groq Whisper / …)
اگر هیچ‌کدام در دسترس نیست: log warning + برگردان {text: "", language: "", duration_sec: 0, model_used: ""} با هشدار در پرامپت نهایی «transcript صوت در دسترس نبود — هیچ provider برای transcribe پیکربندی نشده. می‌توانید OPENAI_API_KEY یا GROQ_API_KEY در Render env اضافه کنید»
هیچ کجا API call مستقیم به OpenAI/Anthropic/… نزن — همه از طریق ai_manager یا get_*_models() patterns.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی transcribe صدا با pattern auto-select موجود

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی transcribe صدا با استفاده از pattern موجود در پروژه. کاربر درخواست داده است که فایل‌های backend/app/services/ برای وجود ماژول transcribe صدا بررسی شود (جستجوی کلیدواژه‌های audio, transcribe, whisper, speech). اگر ماژول موجود است (مثلاً در oversight_inspector_bridge.py یا فایل جداگانه)، یک adapter ساده در inspector_recording_service.py بنویس که از آن استفاده کند. اگر ماژول موجود نیست، فایل جدید backend/app/services/audio_transcribe.py با تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → dict بساز که از pattern auto-select مشابه describe_screenshot_with_vision استفاده کند: چک کردن env var برای provider (OPENAI_API_KEY, GROQ_API_KEY)، انتخاب اولین provider در دسترس، فراخوانی API، برگرداندن {text, language, duration_sec, model_used}. اگر هیچ provider در دسترس نیست، log warning و برگردان {text: '', language: '', duration_sec: 0, model_used: ''} با هشدار. هیچ API call مستقیم به OpenAI/Anthropic نزن — همه از طریق ai_manager یا get_*_models() patterns.

بخش مربوط از درخواست اصلی کاربر:
۱۰. transcribe صدا — استفاده از pattern موجود
⛔ هرگز موازی نساز. کاربر صریح:
«این از همون قابلیت دیباگ بصری باید برای استخراج استفاده کنه و مگه یادت نیست برای استخراج بصری مدل بصری خودکار انتخاب می‌شد یا لیست می‌اومد که انتخاب کنیم ... این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی‌اش کنی و لازم نیست چیز موازی انجام بدی»

روند بررسی پیش از ساخت:
در backend/app/services/oversight_inspector_bridge.py بگرد آیا تابعی برای transcribe صوت با همان pattern auto-select وجود دارد (search: audio, transcribe, whisper, speech)
در backend/app/services/ بگرد فایل‌هایی به نام audio_*.py, *_transcribe*.py, *_speech*.py
در مدل‌های موجود get_vision_models()/get_models() ببین آیا مدل‌هایی با capability=audio یا transcribe ثبت شده‌اند
اگر پیدا شد: همان را استفاده کن، حتی اگر نام transcribe_audio() ندارد و باید با adapter کوچک wrap شود.

اگر پیدا نشد: یک ماژول جدید backend/app/services/audio_transcribe.py با عین pattern describe_screenshot_with_vision بساز:
تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → {"text": str, "language": str, "duration_sec": float, "model_used": str}
داخل: شبیه oversight_inspector_bridge.py:61-89:
audio_models = get_audio_models() or [] (اگر get_audio_models موجود نیست، از env var check استفاده کن)
اولین provider در دسترس را انتخاب کن (OpenAI Whisper / Groq Whisper / …)
اگر هیچ‌کدام در دسترس نیست: log warning + برگردان {text: "", language: "", duration_sec: 0, model_used: ""} با هشدار در پرامپت نهایی «transcript صوت در دسترس نبود — هیچ provider برای transcribe پیکربندی نشده. می‌توانید OPENAI_API_KEY یا GROQ_API_KEY در Render env اضافه کنید»
هیچ کجا API call مستقیم به OpenAI/Anthropic/… نزن — همه از طریق ai_manager یا get_*_models() patterns.

کلیدواژه‌ها:
backend/app/services/audio_transcribe.py, backend/app/services/oversight_inspector_bridge.py, transcribe_audio, get_audio_models, get_vision_models, OPENAI_API_KEY, GROQ_API_KEY, describe_screenshot_with_vision

شواهد در کد واقعی:
- فایل backend/app/services/audio_transcription_service.py در ساختار پروژه موجود است (مسیر: backend/app/services/audio_transcription_service.py) — این فایل deep-read شده و باید

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 3: پیاده‌سازی ضبط حالت A (iframe) — Playwright screencast، صدا، لاگ‌ها، تعاملات، مونتاژ ویدئو، vision analysis، prompt synthesis

**Scope:** پیاده‌سازی کامل حالت A (ضبط مانیتور iframe) در inspector_recording_service.py و inspector_recording.py. شامل: endpoint start-screencast که یک Playwright session headless باز می‌کند و هر 500ms screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/ ذخیره می‌کند. audio capture در frontend با getUserMedia و ارسال chunks به backend هر 10 ثانیه. log sync: هر 10 ثانیه POST /api/render/logs/fetch برای backend logs + capture inspectorConsoleLogs از state frontend. تعاملات: تلاش برای postMessage bridge (listener در frontend)، اگر در 2 ثانیه اول هیچ message دریافت نشد، fallback به pixel-diff با Pillow برای تشخیص scroll/click. مونتاژ نهایی frames به ویدئو با ffmpeg (یا pillow-based GIF اگر ffmpeg نیست). vision analysis: استخراج 6-12 keyframe از ویدئو و ارسال به describe_screenshot_with_vision. prompt synthesis: ترکیب transcript + visual + interactions + logs در ساختار explicit طبق بخش 4.5 پرامپت. هیچ تغییری در describe_screenshot_with_vision داده نمی‌شود — فقط استفاده می‌شود.
**Key terms:** backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, POST /api/recording/inspector/start-screencast, POST /api/recording/inspector/{session_id}/audio-chunk, POST /api/recording/inspector/{session_id}/interactions, POST /api/render/logs/fetch, GET /api/render/logs, backend/app/services/oversight_inspector_bridge.py, describe_screenshot_with_vision, backend/app/services/browser_automation.py, backend/app/services/audio_transcribe.py, RecordingSession, inspectorConsoleLogs, postMessage, Pillow, ffmpeg, /tmp/inspector_recordings/{session_id}/frames/

**بخش مربوط از متن کاربر:**
```
۴. مشخصات دقیق حالت A — ضبط مانیتور iframe
۴.۱ منبع ویدئو — تصمیم قاطع
⚠️ چالش فنی: مانیتور بازرس ویژه یک <iframe> است که از یک دامنهٔ Render سرو می‌شود (cross-origin نسبت به دامنهٔ ai-creator-frontend). به این معنا:

❌ html2canvas روی iframe cross-origin → پنجرهٔ مشکی برمی‌گرداند (security)
❌ MediaRecorder روی iframe.contentDocument → دسترسی ممنوع (cross-origin)
❌ iframe.contentWindow.captureStream() → موجود نیست در هیچ مرورگری
✅ استفاده از screenshot endpoint موجود (POST /api/render/inspector/screenshot که از Playwright در backend استفاده می‌کند)
روش پیشنهادی (Definitive Recommendation): «screencast از طریق Playwright headless»

این روش بهترین کیفیت و کمترین پیچیدگی دارد:

در شروع، frontend یک endpoint جدید POST /api/recording/inspector/start-screencast صدا می‌زند با {project_id, url, fps=2}
backend یک Playwright session شروع می‌کند که صفحهٔ URL iframe را در headless mode باز می‌کند (روی همان viewport و resolution فرانت‌اند)
Playwright هر ۵۰۰ms (یا fps انتخاب‌شده) screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/frame_{seq:06d}.png ذخیره می‌کند
در پایان ضبط، با ffmpeg (اگر در dockerfile نصب است) frames به یک webm/mp4 تبدیل می‌شوند:
ffmpeg -framerate 2 -i frame_%06d.png -c:v libvpx-vp9 -b:v 600k output.webm
اگر ffmpeg در Dockerfile نیست، اضافه نکن (سایز image مهم است) — به‌جایش از pillow-based GIF یا webp animated استفاده کن (کیفیت پایین‌تر ولی بدون dependency).
جایگزین اگر Playwright session جدید سنگین است (fallback): استفاده از همان endpoint screenshot موجود POST /api/render/inspector/screenshot با polling هر ۵۰۰ms از frontend. سربار بیشتر روی شبکه ولی استفاده مجدد بدون تغییر backend.

انتخاب: گزینهٔ Playwright جدید (روش اصلی) را اول امتحان کن. اگر memory در Render محدود است (free tier 512MB)، به polling endpoint موجود سقوط کن.

۴.۲ صدا
صدا در سمت frontend ضبط می‌شود (چون Playwright headless میکروفون ندارد):

getUserMedia({ audio: { deviceId: selected_mic } }) برای میکروفون
اگر system_audio=true: getDisplayMedia({ audio: true, video: false }) ولی این یک پنجرهٔ پیکر باز می‌کند که در حالت A نخواستی. در حالت A پیشنهاد: صدای سیستم را غیرفعال بگذار، یا با هشدار:
«برای ضبط صدای سیستم در حالت A، مرورگر باید از شما اجازهٔ ضبط صفحه بخواهد. می‌خواهید ادامه دهید؟ (در غیر این صورت فقط میکروفون ضبط می‌شود.)»

ترکیب جریان‌های صوتی با Web Audio API → یک stream واحد → ضبط با MediaRecorder({mimeType: 'audio/webm'}) → ارسال chunks به backend هر ۱۰ ثانیه به POST /api/recording/inspector/{session_id}/audio-chunk.

۴.۳ لاگ‌های همگام
در طول ضبط:

هر ۱۰ ثانیه: POST /api/render/logs/fetch (با service_ids پروژهٔ منتخب) برای sync لاگ‌های جدید
در همان زمان: capture فعلی inspectorConsoleLogs (که قبلاً در state نگه‌داری می‌شود — خط 6259) به session تجمیع می‌شود
در لحظهٔ stop:

یک fetch نهایی + GET /api/render/logs?service_ids=...&minutes=<duration+2> برای اطمینان از داشتن همهٔ لاگ‌های دورهٔ ضبط
console logs کامل iframe از state موجود

۴.۴ تعاملات کاربر (با cross-origin handling)
چون iframe cross-origin است، event listening مستقیم ممکن نیست. دو راه ممکن:

روش ۱ (پیشنهادی): postMessage bridge

اگر کنترل کد سرور-سمت پروژه را داریم (Render preview)، یک قطعه کوچک JS در صفحات پروژه inject کنیم که این event ها را به parent بفرستد:

// در پروژهٔ مورد inspect (اگر possible)
['click','scroll','submit'].forEach(t => 
  window.addEventListener(t, e => 
    parent.postMessage({__inspector_event: true, type: t, ts: Date.now(), 
                        selector: e.target?.tagName, x: e.clientX, y: e.clientY}, '*')
  )
);
این روش ایده‌آل است ولی شرط دارد: کاربر باید این snippet را در پروژه‌های inspectable خودش inject کرده باشد. اگر نکرده، روش ۲.

روش ۲ (fallback - عمدتاً برای پروژه‌های third-party): استنتاج از فریم‌ها

از مقایسهٔ pixel-diff بین فریم‌های متوالی (با Pillow در backend) تشخیص بده:

آیا صفحه scroll شده (تغییر کل تصویر در محور Y) → ثبت scroll event
آیا یک تغییر موضعی رخ داده (تغییر در یک ناحیه) → احتمالاً click → ثبت inferred_click {area: bbox}
آیا URL تغییر کرده (از تغییر URL bar در صورت visible) → ثبت navigation
این روش approximate است ولی برای پرامپت کافی است.

تصمیم: روش ۱ را اول امتحان کن. اگر در ۲ ثانیه اول هیچ message دریافت نکردی، روش ۲ را فعال کن. هر دو روش رو در کد داشته باش.

ذخیرهٔ events: array از {ts: ms_since_start, type: string, details: object, source: 'postmessage'|'inferred'}.

۴.۵ سنتز پرامپت قوی — تعریف "قوی"
معیار «قوی» (که در آخر برای verify معتبر است):

✅ شامل یادداشت اولیه (raw_idea کاربر) به‌صورت دست‌نخورده در ابتدا
✅ شامل بلوک منشأ که می‌گوید این پرامپت از کجا آمده (ضبط بازرس ویژه، حالت A، تاریخ، مدت، لینک ویدئو در تلگرام)
✅ شامل Transcript تمیز صدا با timestamp (نه فقط متن خام)
✅ شامل خلاصهٔ بصری که توالی زمانی صفحات و تعاملات را توصیف می‌کند
✅ شامل استخراج intent: AI باید بفهمد «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه را رد می‌کند، کجاها منظورش است» — نه فقط transcribe خام
✅ شامل قطعات لاگ مرتبط (خطاها و warning های مرتبط با timestampهای فعالیت)
✅ شامل توصیف تعاملات (کلیک‌ها، اسکرول‌ها، navigationها) به زبان طبیعی
✅ شامل فهرستی از فایل‌ها/URLs/کامپوننت‌ها که AI تشخیص داده مرتبط هستند
✅ بدون truncation در هر اندازه‌ای — حتی اگر ۱۰۰هزار کاراکتر شد
✅ ساختاریافته با header markdown قابل خوانش توسط مدل AI بعدی
✅ actionable — یک agent دیگر باید بتواند با فقط این پرامپت کار را پیش ببرد
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ضبط حالت A (iframe) — Playwright screencast، صدا، لاگ‌ها، تعاملات، مونتاژ ویدئو، vision analysis، prompt synthesis

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی کامل حالت A (ضبط مانیتور iframe) در inspector_recording_service.py و inspector_recording.py. شامل: endpoint start-screencast که یک Playwright session headless باز می‌کند و هر 500ms screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/ ذخیره می‌کند. audio capture در frontend با getUserMedia و ارسال chunks به backend هر 10 ثانیه. log sync: هر 10 ثانیه POST /api/render/logs/fetch برای backend logs + capture inspectorConsoleLogs از state frontend. تعاملات: تلاش برای postMessage bridge (listener در frontend)، اگر در 2 ثانیه اول هیچ message دریافت نشد، fallback به pixel-diff با Pillow برای تشخیص scroll/click. مونتاژ نهایی frames به ویدئو با ffmpeg (یا pillow-based GIF اگر ffmpeg نیست). vision analysis: استخراج 6-12 keyframe از ویدئو و ارسال به describe_screenshot_with_vision. prompt synthesis: ترکیب transcript + visual + interactions + logs در ساختار explicit طبق بخش 4.5 پرامپت. هیچ تغییری در describe_screenshot_with_vision داده نمی‌شود — فقط استفاده می‌شود.

کلیدواژه‌ها: backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, POST /api/recording/inspector/start-screencast, POST /api/recording/inspector/{session_id}/audio-chunk, POST /api/recording/inspector/{session_id}/interactions, POST /api/render/logs/fetch, GET /api/render/logs, backend/app/services/oversight_inspector_bridge.py, describe_screenshot_with_vision, backend/app/services/browser_automation.py, backend/app/services/audio_transcribe.py, RecordingSession, inspectorConsoleLogs, postMessage, Pillow, ffmpeg, /tmp/inspector_recordings/{session_id}/frames/

شواهد در کد: در backend/app/services/browser_automation.py کلاس BrowserSession (خط 46) با متدهای start (خط 76)، take_screenshot (خط 88)، extract_interactive_elements (خط 133) و click_element_directly (خط 284) وجود دارد که می‌توان از آن برای screencast استفاده کرد. در backend/app/services/oversight_inspector_bridge.py تابع describe_screenshot_with_vision (خط 44) برای vision analysis موجود است. endpoint POST /api/render/inspector/screenshot در backend/app/api/routes/render_logs.py احتمالاً وجود دارد. backend/app/services/audio_transcription_service.py برای transcription صدا موجود است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/inspector_recording_service.py با کلاس InspectorRecordingService شامل:
   - متد start_screencast(project_id, url, fps=2) که یک BrowserSession از browser_automation.py ایجاد کند، صفحه را باز کند و هر 500ms screenshot بگیرد و در /tmp/inspector_recordings/{session_id}/frames/ ذخیره کند.
   - متد stop_screencast(session_id) که ضبط را متوقف کند و با ffmpeg (یا pillow-based GIF) frames را به ویدئو مونتاژ کند.
   - متد receive_audio_chunk(session_id, chunk) که chunks صوتی را ذخیره کند.
   - متد receive_interactions(session_id, interactions) که تعاملات postMessage یا inferred را ذخیره کند.
   - متد sync_logs(session_id) که هر 10 ثانیه POST /api/render/logs/fetch بزند.
   - متد extract_keyframes(video_path, count=6-12) برای استخراج keyframes.
   - متد synthesize_prompt(session_id) که transcript + visual + interactions + logs را ترکیب کند.

2. ایجاد فایل جدید backend/app/api/routes/inspector_recording.py با endpointهای:
   - POST /api/recording/inspector/start-screencast با body {project_id, url, fps=2}
   - POST /api/recording/inspector/{session

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی ضبط حالت B (آزاد) — getDisplayMedia، video chunk upload، vision-based location classifier، prompt synthesis مخصوص

**Scope:** پیاده‌سازی کامل حالت B (ضبط آزاد چندصفحه‌ای) در inspector_recording_service.py. شامل: frontend MediaRecorder با getDisplayMedia برای ویدئو و صدا. video chunk upload به backend هر 5 ثانیه. vision-based location classifier: هر 5 ثانیه یک screenshot از فریم ویدئو استخراج می‌شود و به describe_screenshot_with_vision با system prompt اختصاصی فرستاده می‌شود تا دسته‌بندی کند (chrome_tab/other_app/desktop) و تشخیص دهد آیا مرتبط با پروژه است یا خیر. تعاملات استنتاجی: AI vision خودش تشخیص می‌دهد کاربر در هر بازه چه کرده (clicked/scrolled/typed/navigated). prompt synthesis مخصوص حالت B: با system prompt متفاوت که نقشه زمانی فعالیت با دسته‌بندی و فلگ مرتبط/خارج از پروژه را شامل می‌شود. اگر کاربر به جایی خارج از پروژه رفت ولی در صدا اشاره نکرد → از پرامپت حذف شود. اگر اشاره کرد → به عنوان مرجع/الهام نگه داشته شود.
**Key terms:** backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, POST /api/recording/inspector/{sid}/video-chunk, getDisplayMedia, describe_screenshot_with_vision, backend/app/services/oversight_inspector_bridge.py, repo_full_name, render_preview_domain, chrome_tab, other_app, desktop, related_to_project, url_or_app_name, activity_description, user_action_inferred

**بخش مربوط از متن کاربر:**
```
۵. مشخصات دقیق حالت B — ضبط آزاد چندصفحه‌ای
۵.۱ منبع ویدئو
navigator.mediaDevices.getDisplayMedia({ video: {frameRate: 15}, audio: system_audio }) — کاربر در dialog مرورگر یک تب/پنجره/screen انتخاب می‌کند.

۵.۲ صدا
ترکیب میکروفون + system audio (اگر فعال) — همان روش حالت A بخش ۴.۲.

۵.۳ تشخیص محل کاربر (سه دسته جداگانه)
هر ۵ ثانیه یک screenshot از فریم استخراج می‌شود (از ویدئو blob با canvas) و به describe_screenshot_with_vision با system prompt اختصاصی:

این یک فریم از ضبط آزاد است. تشخیص بده در این فریم کاربر کجاست:
دسته‌ها:
1. تب کروم — اگر نوار آدرس مرورگر دیده می‌شود:
   - URL را از نوار آدرس استخراج کن
   - اگر domain یا path شامل '{repo_full_name}' یا '{render_preview_domain}' است → «مرتبط با پروژه»
   - وگرنه → «خارج از پروژه»
   
2. برنامهٔ دیگر دسکتاپ — اگر title bar یک برنامه (نه مرورگر) دیده می‌شود:
   - نام برنامه را تشخیص بده (VS Code, Slack, Terminal, …)
   - اگر نام پروژه یا فایل‌های آن visible است → «مرتبط (دستی)»
   - وگرنه → «خارج (دستی)»
3. دسکتاپ خام — اگر هیچ پنجرهٔ مشخصی روی صفحه نیست (icons + wallpaper):
   - دسته: «desktop»
   - فعالیت: کاربر در حال جابجایی پنجره‌ها یا navigation است
برای هر فریم خروجی بده:
{
  "category": "chrome_tab" | "other_app" | "desktop",
  "related_to_project": bool,
  "url_or_app_name": str,
  "activity_description": str (یک جمله),
  "user_action_inferred": str (مقایسه با فریم قبل: clicked/scrolled/typed/navigated)
}

۵.۴ تعاملات استنتاجی
چون event listening روی صفحات دیگر ممکن نیست:

AI vision (همان call بالا) خودش استنتاج می‌کند: «در این فاصلهٔ زمانی کاربر چه کاری انجام داد؟»
تغییرات بزرگ تصویر (با pixel-diff) → ثبت timestamp + توصیف AI

۵.۵ سنتز پرامپت
مشابه حالت A ولی با system prompt متفاوت برای text-generation نهایی:

کاربر در حال ضبط آزاد بین چند صفحه/برنامه بوده. ورودی‌های زیر را ترکیب کن و یک پرامپت قوی بساز که:
۱) ترتیب زمانی فعالیت‌های کاربر را به زبان طبیعی توصیف کند — کجا رفت، چه دید، چه کار کرد.
۲) اگر کاربر بین چند منبع جابجا شد، ارتباط بین آنها را در پرامپت توضیح بده (مثلاً «کاربر کد را در GitHub خواند، سپس به IDE رفت و فایل را باز کرد»).
۳) اگر کاربر به جایی خارج از پروژه رفت ولی در صدا به آن **اشاره نکرد** → از پرامپت نهایی حذف کن (نامرتبط).
۴) اگر کاربر به جایی خارج رفت **و در صدا به آن اشاره کرد** (مثلاً «این تو IMDB رو ببین، می‌خوام مثل این UI رو بسازم») → آن را در پرامپت به‌عنوان «مرجع/الهام» نگه دار.
۵) intent اصلی کاربر را در یک پاراگراف بالای پرامپت قرار بده.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی ضبط حالت B (آزاد) در inspector_recording_service

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی کامل حالت B (ضبط آزاد چندصفحه‌ای) در inspector_recording_service.py. شامل: frontend MediaRecorder با getDisplayMedia برای ویدئو و صدا. video chunk upload به backend هر 5 ثانیه. vision-based location classifier: هر 5 ثانیه یک screenshot از فریم ویدئو استخراج می‌شود و به describe_screenshot_with_vision (در backend/app/services/oversight_inspector_bridge.py خطوط 44-285) با system prompt اختصاصی فرستاده می‌شود تا دسته‌بندی کند (chrome_tab/other_app/desktop) و تشخیص دهد آیا مرتبط با پروژه است یا خیر. تعاملات استنتاجی: AI vision خودش تشخیص می‌دهد کاربر در هر بازه چه کرده (clicked/scrolled/typed/navigated). prompt synthesis مخصوص حالت B: با system prompt متفاوت که نقشه زمانی فعالیت با دسته‌بندی و فلگ مرتبط/خارج از پروژه را شامل می‌شود. اگر کاربر به جایی خارج از پروژه رفت ولی در صدا اشاره نکرد → از پرامپت حذف شود. اگر اشاره کرد → به عنوان مرجع/الهام نگه داشته شود. مشخصات دقیق حالت B: navigator.mediaDevices.getDisplayMedia({ video: {frameRate: 15}, audio: system_audio }) — کاربر در dialog مرورگر یک تب/پنجره/screen انتخاب می‌کند. صدا ترکیب میکروفون + system audio. تشخیص محل کاربر هر 5 ثانیه یک screenshot از فریم استخراج می‌شود (از ویدئو blob با canvas) و به describe_screenshot_with_vision با system prompt اختصاصی: دسته‌های chrome_tab (اگر نوار آدرس مرورگر دیده می‌شود، URL را استخراج کن، اگر domain یا path شامل repo_full_name یا render_preview_domain است → مرتبط با پروژه، وگرنه خارج از پروژه)، other_app (اگر title bar یک برنامه دیده می‌شود، نام برنامه را تشخیص بده، اگر نام پروژه یا فایل‌های آن visible است → مرتبط (دستی)، وگرنه خارج (دستی))، desktop (اگر هیچ پنجره‌ای روی صفحه نیست). برای هر فریم خروجی: category, related_to_project, url_or_app_name, activity_description, user_action_inferred. تعاملات استنتاجی: AI vision خودش استنتاج می‌کند کاربر چه کاری انجام داد، تغییرات بزرگ تصویر با pixel-diff → ثبت timestamp + توصیف AI. سنتز پرامپت: مشابه حالت A ولی با system prompt متفاوت برای text-generation نهایی که ترتیب زمانی فعالیت‌ها را توصیف کند، ارتباط بین منابع را توضیح دهد، بخش‌های خارج از پروژه بدون اشاره صوتی حذف شوند، بخش‌های خارج با اشاره صوتی به عنوان مرجع/الهام نگه داشته شوند، intent اصلی در یک پاراگراف بالای پرامپت قرار گیرد. کلیدواژه‌ها: backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, POST /api/recording/inspector/{sid}/video-chunk, getDisplayMedia, describe_screenshot_with_vision, backend/app/services/oversight_inspector_bridge.py, repo_full_name, render_preview_domain, chrome_tab, other_app, desktop, related_to_project, url_or_app_name, activity_description, user_action_inferred.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/inspector_recording_service.py با کلاس InspectorRecordingService شامل متدهای: start_recording (دریافت MediaStream از getDisplayMedia)، process_video_chunk (دریافت chunk ویدئو هر 5 ثانیه از endpoint POST /api/recording/inspector/{sid}/video-chunk)، extract_screenshot_from_frame (استخراج فریم از blob ویدئو با canvas)، classify_location (فراخوانی describe_screenshot_with_vision از backend/app/services/oversight_inspector_bridge.py خطوط 44-285 با system prompt اختصاصی برای دسته‌بندی chrome_tab/other_app/desktop و استخراج url_or_app_name, related_to_project, activity_description, user_action_inferred)، synthesize_prompt (ساخت پر

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 5: پیاده‌سازی finalize — ارسال به تلگرام با split، حذف از disk، task creation، ارسال به چت inspector

**Scope:** پیاده‌سازی کامل مرحله finalize در inspector_recording_service.py و inspector_recording.py. شامل: endpoint POST /api/recording/inspector/{sid}/finalize که user_note, send_to_oversight, edited_transcript را دریافت می‌کند. ارسال ویدئو به تلگرام با send_document از notification_service.py با caption شامل خلاصه پرامپت (max 1020 char). اگر ویدئو > 45MB، split با ffmpeg به chunks 45MB و ارسال پشت سر هم با caption [1/N], [2/N], ... حذف کامل /tmp/inspector_recordings/{session_id} از disk پس از ارسال موفق. اگر send_to_oversight=true: فراخوانی endpoint POST /api/oversight/tasks/from-inspector-recording که یک تسک جدید با raw_idea=user_note و prompt=پرامپت قوی می‌سازد. اگر send_to_oversight=false: ارسال پرامپت کامل به چت inspector از طریق مسیر sendToOversight('visual_debug') با pre-built prompt. retry logic: 3 بار retry با backoff [2s, 5s, 10s] برای تلگرام. اگر تلگرام fail کرد: نگه‌داری فایل برای 24 ساعت و notification به کاربر با temporary_download_url.
**Key terms:** backend/app/services/inspector_recording_service.py, backend/app/api/routes/inspector_recording.py, POST /api/recording/inspector/{sid}/finalize, POST /api/oversight/tasks/from-inspector-recording, backend/app/services/notification_service.py, TelegramChannel.send_document, backend/app/api/routes/oversight.py, FromInspectorRecordingRequest, OversightTask.raw_idea, OversightTask.prompt, sendToOversight('visual_debug'), /tmp/inspector_recordings/{session_id}/, ffmpeg

**بخش مربوط از متن کاربر:**
```
۶. flow کامل — از کلیک تا تحویل
...
10. کاربر [ارسال نهایی] می‌زند:
    - Frontend: POST /api/recording/inspector/{session_id}/finalize با {user_note, send_to_oversight, edited_transcript?}
    - Backend:
      i. ارسال ویدئو به تلگرام با send_document (caption شامل خلاصهٔ پرامپت)
      ii. اگر size > 45MB → split به chunks 45MB و چند call پشت سر هم با caption شماره‌گذاری‌شده
      iii. حذف کامل /tmp/inspector_recordings/{session_id} از disk
      iv. اگر send_to_oversight=true:
          POST /api/oversight/tasks/from-inspector-recording (endpoint جدید):
            - raw_idea = user_note
            - prompt = پرامپت قوی structure شده (طبق ۴.۵)
            - project_id = inspector's loaded project_id
          → برمی‌گرداند task_id
      v. اگر send_to_oversight=false:
          ارسال prompt به چت inspector (همان مسیر sendToOversight('visual_debug') با pre-built prompt)
    - Backend: برمی‌گرداند {success, task_id?, telegram_message_id?, chat_message_id?}
    ↓
11. Frontend: modal بسته می‌شود، toast «ارسال موفق» + لینک به تسک یا پیام چت

۹. مدیریت اندازهٔ فایل تلگرام
bitrate پیش‌فرض ویدئو: 600 kbps (برای 60min ≈ 270MB → بزرگ)
استراتژی: اگر فایل > 45MB → split
با ffmpeg -ss start -t duration -c copy chunk_N.webm برش بدون re-encode
هر chunk حداکثر 45MB
ارسال پشت سر هم با caption [1/N], [2/N], ..., [N/N] — caption نهایی شامل خلاصهٔ پرامپت
اگر تلگرام fail کرد:

۳ بار retry با backoff [2s, 5s, 10s]
اگر بازم fail:
نگه‌داری فایل برای ۲۴ ساعت در /tmp/inspector_recordings/{session_id}/
notification به کاربر: «ویدئو به تلگرام ارسال نشد. دانلود از: {temporary_download_url} (۲۴ ساعت معتبر)»
{temporary_download_url} در پرامپت تسک (بخش منشأ) درج می‌شود
پس از ۲۴ ساعت auto-cleanup

۱۱. ساخت تسک و حفظ یادداشت اولیه (دقیق)
⛔ یادداشت اولیه را با پرامپت AI مخلوط نکن. کاربر صریح:

«اون یادداشت‌های اولیه که برای هر تسک میاد که باید باشه سر جاش ... البته اون یادداشت‌های اول پرامپت‌های تسک‌ها (نسخه به‌روزشده‌ش) باید ابتداش قرار بگیره و همچنین دقیق بگه از کجا ریشه گرفته»

ترجمه به ساختار:

اگر send_to_oversight=true:

یک تسک جدید با POST /api/oversight/tasks/from-inspector-recording
در OversightTask:
raw_idea = user_note (یادداشت اولیهٔ کاربر، عین متن textarea — نسخهٔ به‌روزشده‌اش که الان کاربر می‌نویسد، نه آنچه در ضبط گفت)
prompt = ساختار کامل طبق بخش ۴.۵ که با همان user_note در ابتدا شروع می‌شود (به عنوان heading اول)، بعد بلوک منشأ، بعد بقیه
...
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی finalize در inspector_recording_service — ارسال به تلگرام با split، حذف از disk، task creation

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی کامل مرحله finalize در inspector_recording_service.py و inspector_recording.py. شامل: endpoint POST /api/recording/inspector/{sid}/finalize که user_note, send_to_oversight, edited_transcript را دریافت می‌کند. ارسال ویدئو به تلگرام با send_document از notification_service.py با caption شامل خلاصه پرامپت (max 1020 char). اگر ویدئو > 45MB، split با ffmpeg به chunks 45MB و ارسال پشت سر هم با caption [1/N], [2/N], ... حذف کامل /tmp/inspector_recordings/{session_id} از disk پس از ارسال موفق. اگر send_to_oversight=true: فراخوانی endpoint POST /api/oversight/tasks/from-inspector-recording که یک تسک جدید با raw_idea=user_note و prompt=پرامپت قوی می‌سازد. اگر send_to_oversight=false: ارسال پرامپت کامل به چت inspector از طریق مسیر sendToOversight('visual_debug') با pre-built prompt. retry logic: 3 بار retry با backoff [2s, 5s, 10s] برای تلگرام. اگر تلگرام fail کرد: نگه‌داری فایل برای 24 ساعت و notification به کاربر با temporary_download_url. بر اساس تحلیل کد واقعی: backend/app/services/notification_service.py خطوط 579-645 (TelegramChannel.send_document) و خطوط 532-577 (TelegramChannel.send) و خطوط 120-386 (EVENT_REGISTRY) و backend/app/api/routes/oversight.py (نیاز به endpoint جدید FromInspectorRecordingRequest). فایل‌های مرتبط: backend/app/services/inspector_recording_service.py (فعلاً deep-read نشده — باید ایجاد شود)، backend/app/api/routes/inspector_recording.py (فعلاً deep-read نشده — باید ایجاد شود). کلیدواژه‌های کاربر: POST /api/recording/inspector/{sid}/finalize, POST /api/oversight/tasks/from-inspector-recording, TelegramChannel.send_document, sendToOversight('visual_debug'), /tmp/inspector_recordings/{session_id}/, ffmpeg, 3 بار retry با backoff [2s, 5s, 10s], 24 ساعت نگه‌داری فایل, temporary_download_url.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/inspector_recording_service.py با کلاس InspectorRecordingService شامل متد finalize_session(session_id, user_note, send_to_oversight, edited_transcript). 2. در این متد: a) مسیر /tmp/inspector_recordings/{session_id}/ را برای یافتن فایل ویدئو اسکن کند. b) اگر فایل > 45MB، با subprocess ffmpeg -ss start -t duration -c copy chunk_N.webm split کند. c) برای هر chunk، TelegramChannel.send_document را با caption [1/N], [2/N], ... فراخوانی کند. d) اگر send_to_oversight=true، با httpx POST به /api/oversight/tasks/from-inspector-recording با body {raw_idea: user_note, prompt: پرامپت قوی} بزند. e) اگر send_to_oversight=false، از طریق sendToOversight('visual_debug') پرامپت را به چت inspector بفرستد. f) پس از موفقیت، shutil.rmtree('/tmp/inspector_recordings/{session_id}/') را اجرا کند. g) retry logic: 3 بار با backoff [2s, 5s, 10s] برای هر call تلگرام. h) اگر همه retryها fail شد، فایل را نگه دارد و temporary_download_url تولید کند. 3. ایجاد endpoint جدید در backend/app/api/routes/inspector_recording.py: POST /api/recording/inspector/{sid}/finalize با Pydantic model شامل user_note: str, send_to_oversight: bool, edited_transcript: Optional[str]. 4. ایجاد endpoint جدید در backend/app/api/routes/oversight.py: POST /api/oversight/tasks/from-inspector-recording با FromInspectorRecordingRequest شامل raw_idea, prompt, project_id.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

# 🔹 مرحله 6: پیاده‌سازی کامل UI frontend — دکمه ضبط، picker حالت، status bar، processing modal، preview modal، regenerate

**Scope:** پیاده‌سازی کامل UI در frontend/src/app/projects/[id]/page.tsx. شامل: دکمه 🎬 ضبط ویدئو در کنار دکمه screenshot (خط 13694-13705) با شرط شلوغ نشدن UI. Picker modal با دو کارت (حالت A و B) و تنظیمات صدا (چک‌باکس میکروفون، دراپ‌دان منبع میکروفون، چک‌باکس صدای سیستم). Status bar شناور پایین صفحه با شمارش زمان، حالت، وضعیت صدا، دکمه‌های مکث و پایان. Processing modal با progress bar برای مراحل (مونتاژ ویدئو، transcribe، vision، logs، prompt). Preview modal با: ویدئو پلیر، textarea یادداشت اولیه، transcript قابل ویرایش، خلاصه بصری read-only (expandable)، لاگ‌ها (accordion)، تعاملات (collapsed by default برای حالت A)، نقشه زمانی فعالیت (برای حالت B)، پرامپت قوی read-only با دکمه copy، چک‌باکس 'ارسال به مرکز نظارت' (پیش‌فرض خاموش). دکمه‌های: ارسال نهایی (سبز)، لغو و دور انداختن (قرمز)، تولید مجدد پرامپت (آبی). state management: videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession. auto-stop در 60 دقیقه با هشدار 5 دقیقه قبل.
**Key terms:** frontend/src/app/projects/[id]/page.tsx, videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession, sendToOversightVisual, sendToOversight('visual_debug'), inspectorConsoleLogs, navigator.mediaDevices.enumerateDevices, getUserMedia, getDisplayMedia, MediaRecorder, Web Audio API

**بخش مربوط از متن کاربر:**
```
۳. مشخصات دقیق UI
۳.۱ محل دکمه
در frontend/src/app/projects/[id]/page.tsx خط 13694-13705، دقیقاً بعد از دکمهٔ screenshot (همان flex container absolute top-7 left-2 z-50):

{/* 🎬 دکمهٔ ضبط ویدئو (دو حالته) */}
<button
  onClick={() => setVideoRecordPickerOpen(true)}
  disabled={videoRecordingActive}
  title="ضبط ویدئو از iframe بازرس ویژه یا صفحات آزاد — با لاگ‌ها، صدا، تعاملات و پرامپت AI خودکار"
  className="..."  /* استایل دقیقاً مشابه دکمهٔ screenshot — اندازه و رنگ مشابه برای یکپارچگی بصری */
>
  {videoRecordingActive ? '⏺' : '🎬'}
</button>
شرط شلوغ نشدن UI: اگر در توسعه دیدی این flex container تنگ می‌شود، دکمه را فشرده‌تر کن (فقط آیکون، بدون متن). همان‌طور که کاربر گفته «جایی که حالا شلوغم نشه».

۳.۲ Picker حالت
با کلیک، یک popover/modal کوچک باز می‌شود با دو کارت بزرگ کنار هم:

کارت ۱ — «🎯 ضبط بازرس ویژه» (پیش‌فرض highlighted):

توضیح: «فقط محتوای داخل iframe ضبط می‌شود. تمام لاگ‌ها، تعاملات، اسکرول‌ها و کلیک‌های شما داخل همین مانیتور رصد می‌شود. AI فقط حول همین پروژه پرامپت می‌سازد. اگر در توضیح صوتی به چیزی خارج از این مانیتور اشاره کنید، AI آن را بی‌ربط تشخیص می‌دهد.»
کارت ۲ — «🌐 ضبط آزاد (تب/پنجره/دسکتاپ)»:

توضیح: «مرورگر از شما می‌پرسد کدام تب یا پنجره یا کل صفحه را ضبط کند. می‌توانید آزادانه بین صفحات سیر کنید. AI با تحلیل بصری فریم‌ها متوجه می‌شود در هر لحظه کجا هستید (تب کروم / برنامهٔ دیگر / دسکتاپ) و چه می‌کنید. کلیک‌ها و اسکرول‌ها از مقایسهٔ فریم‌ها استنتاج می‌شوند.»
زیر کارت‌ها — تنظیمات مشترک:

چک‌باکس 🎤 ضبط صدای میکروفون (پیش‌فرض روشن)
درپ‌دان منبع میکروفون: «پیش‌فرض سیستم» / «هندزفری/Bluetooth» / لیست تمام دستگاه‌های صوتی از navigator.mediaDevices.enumerateDevices() (شامل headset، USB mic، Bluetooth devices)
چک‌باکس 🔊 ضبط صدای سیستم (پیش‌فرض خاموش — چون نیاز به اجازهٔ خاص دارد و در همه مرورگرها پشتیبانی نمی‌شود)
دکمه‌های Modal:

[شروع ضبط] (سبز، فعال فقط اگر یک حالت انتخاب شده)
[انصراف] (خاکستری)

۳.۳ نوار وضعیت ضبط
پس از شروع، یک bar شناور پایین صفحه (یا گوشهٔ بالا-راست):

⏺ در حال ضبط: 00:23  |  🎯 iframe  |  🎤 ✓ مایک پیش‌فرض  |  🔊 ✗  |  [⏸ مکث]  [⏹ پایان و آماده‌سازی]
شمارش ثانیه‌ای زمان
در ۵۵ دقیقه (۵ دقیقه به سقف ۶۰ دقیقه): تغییر رنگ به نارنجی + toast هشدار «۵ دقیقه به پایان خودکار مانده»
در ۶۰ دقیقه: auto-stop + toast هشدار + گذار به مرحلهٔ آماده‌سازی

۳.۴ مرحلهٔ آماده‌سازی (Processing)
بین stop و modal پیش‌نمایش، یک modal با progress bar نمایش داده می‌شود:

⏳ در حال آماده‌سازی...
  ⚙️ مونتاژ ویدئو          [✓]
  🎙 transcribe صدا         [⏳ 45%]
  🔍 تحلیل بصری ۸ فریم      [⏳ 3/8]
  📊 جمع‌آوری لاگ‌ها        [✓]
  🤖 سنتز پرامپت قوی        [—]
این برای شفافیت — کاربر بداند سیستم در حال چه‌کاری است و چقدر مانده.

۳.۵ Modal پیش‌نمایش و ارسال
پس از آماده‌سازی، modal بزرگی باز می‌شود با این بخش‌ها (به ترتیب، از بالا به پایین):

🎥 پیش‌نمایش ویدئو — با <video controls> قابل پخش
📝 یادداشت اولیه (raw_idea) — یک textarea با placeholder:
«یادداشت اولیه (اختیاری) — این متن عین به عین در ابتدای پرامپت تسک حفظ می‌شود و در فیلد raw_idea ذخیره می‌گردد. اگر چیزی هست که می‌خواهید AI آن را به‌عنوان قصد اصلی شما درک کند، اینجا بنویسید.»

🎙 Transcript صدا (قابل ویرایش) — کاربر می‌تواند خطاهای transcribe را اصلاح کند، چون این متن به AI داده می‌شود
🔍 خلاصهٔ بصری (read-only، تولیدشده توسط describe_screenshot_with_vision) — قابل expand
📋 لاگ‌ها (به‌صورت accordion، collapsed by default):
Console logs frontend iframe (تعداد + پنل قابل باز شدن)
Backend logs Render (تعداد + پنل قابل باز شدن)
🖱 تعاملات کاربر (در حالت A، collapsed by default): لیست رویدادها با timestamp
🌐 نقشهٔ زمانی فعالیت (در حالت B): جدول [زمان | محل | فعالیت | مرتبط با پروژه؟]
🎯 پرامپت قوی تولیدشده (read-only، قابل copy) — AI generated، نمایش کامل بدون truncation
چک‌باکس [ ] «ارسال به مرکز نظارت به‌جای چت» — پیش‌فرض: خاموش (هرگز تیک زده نشود؛ کاربر صریحاً گفته «این رو تغییر ندی و پیش‌فرضش تیک نخورده باشه»)
دکمه‌ها:
[ارسال نهایی] (سبز، primary)
[لغو و دور انداختن] (قرمز، حذف کامل session)
[تولید مجدد پرامپت] (آبی) — اگر کاربر transcript را ویرایش کرد، می‌تواند AI را دوباره صدا بزند
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی کامل UI ضبط ویدئو در صفحه پروژه

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی کامل UI frontend برای ضبط ویدئو در frontend/src/app/projects/[id]/page.tsx را دارد. این شامل: دکمه 🎬 ضبط ویدئو در کنار دکمه screenshot (خط 13694-13705) با شرط شلوغ نشدن UI. Picker modal با دو کارت (حالت A: ضبط بازرس ویژه و حالت B: ضبط آزاد) و تنظیمات صدا (چک‌باکس میکروفون، دراپ‌دان منبع میکروفون از navigator.mediaDevices.enumerateDevices()، چک‌باکس صدای سیستم). Status bar شناور پایین صفحه با شمارش زمان، حالت، وضعیت صدا، دکمه‌های مکث و پایان. Processing modal با progress bar برای مراحل (مونتاژ ویدئو، transcribe، vision، logs، prompt). Preview modal با: ویدئو پلیر، textarea یادداشت اولیه (raw_idea)، transcript قابل ویرایش، خلاصه بصری read-only (expandable)، لاگ‌ها (accordion)، تعاملات (collapsed by default برای حالت A)، نقشه زمانی فعالیت (برای حالت B)، پرامپت قوی read-only با دکمه copy، چک‌باکس 'ارسال به مرکز نظارت' (پیش‌فرض خاموش). دکمه‌های: ارسال نهایی (سبز)، لغو و دور انداختن (قرمز)، تولید مجدد پرامپت (آبی). state management: videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession. auto-stop در 60 دقیقه با هشدار 5 دقیقه قبل. کاربر به صراحت اشاره کرده که دکمه در frontend/src/app/projects/[id]/page.tsx خط 13694-13705، دقیقاً بعد از دکمهٔ screenshot (همان flex container absolute top-7 left-2 z-50) قرار گیرد. همچنین اشاره به استفاده از getUserMedia, getDisplayMedia, MediaRecorder, Web Audio API برای ضبط. در کد فعلی پروژه، فایل backend/app/api/routes/screen_recording.py وجود دارد که endpointهای مربوط به screen recording را شامل می‌شود و backend/app/services/audio_transcription_service.py برای transcribe صدا. همچنین backend/app/services/inspector_agent.py و backend/app/services/inspector_intent_resolver.py برای تحلیل بصری و سنتز پرامپت. فایل backend/app/api/routes/external_prompts.py شامل endpointهای /external/prompts است که برای ارسال تسک به مرکز نظارت استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در frontend/src/app/projects/[id]/page.tsx، دکمه ضبط ویدئو را بعد از دکمه screenshot در flex container با کلاس absolute top-7 left-2 z-50 اضافه کن. از stateهای videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession استفاده کن. 2. یک PickerModal کامپوننت بساز با دو کارت برای حالت A (ضبط بازرس ویژه) و حالت B (ضبط آزاد). تنظیمات صدا شامل چک‌باکس میکروفون، دراپ‌دان منبع میکروفون از navigator.mediaDevices.enumerateDevices()، چک‌باکس صدای سیستم. 3. یک StatusBar شناور پایین صفحه با نمایش زمان، حالت، وضعیت صدا، دکمه‌های مکث و پایان. 4. یک ProcessingModal با progress bar برای مراحل مونتاژ ویدئو، transcribe، vision، logs، prompt. 5. یک PreviewModal با ویدئو پلیر، textarea یادداشت اولیه (raw_idea)، transcript قابل ویرایش، خلاصه بصری read-only (expandable)، لاگ‌ها (accordion)، تعاملات (collapsed by default برای حالت A)، نقشه زمانی فعالیت (برای حالت B)، پرامپت قوی read-only با دکمه copy، چک‌باکس 'ارسال به مرکز نظارت' (پیش‌فرض خاموش). دکمه‌های ارسال نهایی (سبز)، لغو و دور انداختن (

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: idea
- اولویت: critical
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. هیچ تستی fail نمی‌شود (`npm run test` / `pytest`) _(verify: backend_test)_
2. linter بدون warning عبور می‌کند _(verify: manual_only)_
3. type-check موفق است (`tsc --noEmit` / `mypy`) _(verify: manual_only)_

## Task Steps

### Step 1: پیاده‌سازی زیرساخت ضبط دوحالته (سرویس، endpoint‌ها، state در RAM)
**Status:** `done` (100%)
**Scope:** ایجاد فایل‌های اصلی backend: inspector_recording_service.py با کلاس RecordingSession و state in-memory (dict)، inspector_recording.py با تمام endpoint‌های جدید (start, audio-chunk, video-chunk, interactions, stop, process, preview, regenerate-prompt, finalize, cancel) به‌صورت stub که dummy data برمی‌گردانند. ثبت router در main.py با prefix /api. اضافه کردن sweeper برای cleanup session‌های older از 120 دقیقه. هیچ منطق واقعی ضبط یا پردازش در این مرحله پیاده‌سازی نمی‌شود — فقط ساختار و stub. endpoint‌ها: POST /api/recording/inspector/start, POST /api/recording/inspector/{sid}/audio-chunk, POST /api/recording/inspector/{sid}/video-chunk, POST /api/recording/inspector/{sid}/interactions, POST /api/recording/inspector/{sid}/stop, POST /api/recording/inspector/{sid}/process, GET /api/recording/inspector/{sid}/preview, POST /api/recording/inspector/{sid}/regenerate-prompt, POST /api/recording/inspector/{sid}/finalize, POST /api/recording/inspector/{sid}/cancel. همچنین endpoint جدید POST /api/oversight/tasks/from-inspector-recording در oversight.py با مدل FromInspectorRecordingRequest. هیچ تغییری در فایل‌های موجود (render_logs.py, oversight_inspector_bridge.py, notification_service.py) داده نمی‌شود.
**Excerpt:**
```
۷. ساختار endpoint‌های جدید
Path	Method	Body	Returns
/api/recording/inspector/start	POST	{project_id, mode: 'A'|'B', audio_config: {mic, system, mic_device_id?}, target_fps?}	{session_id, started_at, screencast_method?}
/api/recording/inspector/{sid}/audio-chunk	POST	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/video-chunk	POST (فقط mode B)	multipart با {seq: int}	{ok}
/api/recording/inspector/{sid}/interactions	POST	{events: [{ts, type, details, source}]}	{ok}
/api/recording/inspector/{sid}/stop	POST	empty	{ok}
/api/recording/inspector/{sid}/process	POST	empty	SSE stream از پیشرفت پردازش
/api/recording/inspector/{sid}/preview	GET	—	{video_url_temp, transcript, visual_summary, interactions, logs, prompt}
/api/recording/inspector/{sid}/regenerate-prompt	POST	{edited_transcript}	{prompt}
/api/recording/inspector/{sid}/finalize	POST	{user_note, send_to_oversight, edited_transcript?}	{success, task_id?, telegram_message_id?, chat_message_id?}
/api/recording/inspector/{sid}/cancel	POST	empty	{ok} (cleanup کامل)
/api/oversight/tasks/from-inspector-recording	POST	{project_id, user_request, pre_built_prompt, recording_metadata}	{task_id, watched_id}
همگی در یک فایل جدید backend/app/api/routes/inspector_recording.py. در backend/app/main.py با prefix /api ثبت شود.

سرویس اصلی: backend/app/services/inspector_recording_service.py.

state جلسات: در RAM (یک dict در service singleton). cleanup خودکار: هر session که >۱۲۰ دقیقه active است auto-cancel + cleanup.
```

### Step 2: پیاده‌سازی transcribe صدا با استفاده از pattern موجود (audio_transcribe.py)
**Status:** `done` (100%)
**Scope:** بررسی فایل‌های backend/app/services/ برای وجود ماژول transcribe صدا (جستجوی کلیدواژه‌های audio, transcribe, whisper, speech). اگر ماژول موجود است (مثلاً در oversight_inspector_bridge.py یا فایل جداگانه)، یک adapter ساده در inspector_recording_service.py بنویس که از آن استفاده کند. اگر ماژول موجود نیست، فایل جدید backend/app/services/audio_transcribe.py با تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → dict بساز که از pattern auto-select مشابه describe_screenshot_with_vision استفاده کند: چک کردن env var برای provider (OPENAI_API_KEY, GROQ_API_KEY)، انتخاب اولین provider در دسترس، فراخوانی API، برگرداندن {text, language, duration_sec, model_used}. اگر هیچ provider در دسترس نیست، log warning و برگردان {text: '', language: '', duration_sec: 0, model_used: ''} با هشدار. هیچ API call مستقیم به OpenAI/Anthropic نزن — همه از طریق ai_manager یا get_*_models() patterns.
**Excerpt:**
```
۱۰. transcribe صدا — استفاده از pattern موجود
⛔ هرگز موازی نساز. کاربر صریح:

«این از همون قابلیت دیباگ بصری باید برای استخراج استفاده کنه و مگه یادت نیست برای استخراج بصری مدل بصری خودکار انتخاب می‌شد یا لیست می‌اومد که انتخاب کنیم ... این مکانیزم قبلاً مفصل برای دیباگ بصری تو این قسمت پیاده‌سازی شده ... کافیه بررسی کنی و بتونی به نحو احسن پیاده‌سازی‌اش کنی و لازم نیست چیز موازی انجام بدی»

روند بررسی پیش از ساخت:

در backend/app/services/oversight_inspector_bridge.py بگرد آیا تابعی برای transcribe صوت با همان pattern auto-select وجود دارد (search: audio, transcribe, whisper, speech)
در backend/app/services/ بگرد فایل‌هایی به نام audio_*.py, *_transcribe*.py, *_speech*.py
در مدل‌های موجود get_vision_models()/get_models() ببین آیا مدل‌هایی با capability=audio یا transcribe ثبت شده‌اند
اگر پیدا شد: همان را استفاده کن، حتی اگر نام transcribe_audio() ندارد و باید با adapter کوچک wrap شود.

اگر پیدا نشد: یک ماژول جدید backend/app/services/audio_transcribe.py با عین pattern describe_screenshot_with_vision بساز:

تابع async def transcribe_audio(audio_bytes, mime_type='audio/webm') → {"text": str, "language": str, "duration_sec": float, "model_used": str}
داخل: شبیه oversight_inspector_bridge.py:61-89:
audio_models = get_audio_models() or [] (اگر get_audio_models موجود نیست، از env var check استفاده کن)
اولین provider در دسترس را انتخاب کن (OpenAI Whisper / Groq Whisper / …)
اگر هیچ‌کدام در دسترس نیست: log warning + برگردان {text: "", language: "", duration_sec: 0, model_used: ""} با هشدار در پرامپت نهایی «transcript صوت در دسترس نبود — هیچ provider برای transcribe پیکربندی نشده. می‌توانید OPENAI_API_KEY یا GROQ_API_KEY در Render env اضافه کنید»
هیچ کجا API call مستقیم به OpenAI/Anthropic/… نزن — همه از طریق ai_manager یا get_*_models() patterns.
```

### Step 3: پیاده‌سازی ضبط حالت A (iframe) — Playwright screencast، صدا، لاگ‌ها، تعاملات، مونتاژ ویدئو، vision analysis، prompt synthesis
**Status:** `partial` (70%)
**Scope:** پیاده‌سازی کامل حالت A (ضبط مانیتور iframe) در inspector_recording_service.py و inspector_recording.py. شامل: endpoint start-screencast که یک Playwright session headless باز می‌کند و هر 500ms screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/ ذخیره می‌کند. audio capture در frontend با getUserMedia و ارسال chunks به backend هر 10 ثانیه. log sync: هر 10 ثانیه POST /api/render/logs/fetch برای backend logs + capture inspectorConsoleLogs از state frontend. تعاملات: تلاش برای postMessage bridge (listener در frontend)، اگر در 2 ثانیه اول هیچ message دریافت نشد، fallback به pixel-diff با Pillow برای تشخیص scroll/click. مونتاژ نهایی frames به ویدئو با ffmpeg (یا pillow-based GIF اگر ffmpeg نیست). vision analysis: استخراج 6-12 keyframe از ویدئو و ارسال به describe_screenshot_with_vision. prompt synthesis: ترکیب transcript + visual + interactions + logs در ساختار explicit طبق بخش 4.5 پرامپت. هیچ تغییری در describe_screenshot_with_vision داده نمی‌شود — فقط استفاده می‌شود.
**Excerpt:**
```
۴. مشخصات دقیق حالت A — ضبط مانیتور iframe
۴.۱ منبع ویدئو — تصمیم قاطع
⚠️ چالش فنی: مانیتور بازرس ویژه یک <iframe> است که از یک دامنهٔ Render سرو می‌شود (cross-origin نسبت به دامنهٔ ai-creator-frontend). به این معنا:

❌ html2canvas روی iframe cross-origin → پنجرهٔ مشکی برمی‌گرداند (security)
❌ MediaRecorder روی iframe.contentDocument → دسترسی ممنوع (cross-origin)
❌ iframe.contentWindow.captureStream() → موجود نیست در هیچ مرورگری
✅ استفاده از screenshot endpoint موجود (POST /api/render/inspector/screenshot که از Playwright در backend استفاده می‌کند)
روش پیشنهادی (Definitive Recommendation): «screencast از طریق Playwright headless»

این روش بهترین کیفیت و کمترین پیچیدگی دارد:

در شروع، frontend یک endpoint جدید POST /api/recording/inspector/start-screencast صدا می‌زند با {project_id, url, fps=2}
backend یک Playwright session شروع می‌کند که صفحهٔ URL iframe را در headless mode باز می‌کند (روی همان viewport و resolution فرانت‌اند)
Playwright هر ۵۰۰ms (یا fps انتخاب‌شده) screenshot می‌گیرد و در /tmp/inspector_recordings/{session_id}/frames/frame_{seq:06d}.png ذخیره می‌کند
در پایان ضبط، با ffmpeg (اگر در dockerfile نصب است) frames به یک webm/mp4 تبدیل می‌شوند:
ffmpeg -framerate 2 -i frame_%06d.png -c:v libvpx-vp9 -b:v 600k output.webm
اگر ffmpeg در Dockerfile نیست، اضافه نکن (سایز image مهم است) — به‌جایش از pillow-based GIF یا webp animated استفاده کن (کیفیت پایین‌تر ولی بدون dependency).
جایگزین اگر Playwright session جدید سنگین است (fallback): استفاده از همان endpoint screenshot موجود POST /api/render/inspector/screenshot با polling هر ۵۰۰ms از frontend. سربار بیشتر روی شبکه ولی استفاده مجدد بدون تغییر backend.

انتخاب: گزینهٔ Playwright جدید (روش اصلی) را اول امتحان کن. اگر memory در Render محدود است (free tier 512MB)، به polling endpoint موجود سقوط کن.

۴.۲ صدا
صدا در سمت frontend ضبط می‌شود (چون Playwright headless میکروفون ندارد):

getUserMedia({ audio: { deviceId: selected_mic } }) برای میکروفون
اگر system_audio=true: getDisplayMedia({ audio: true, video: false }) ولی این یک پنجرهٔ پیکر باز می‌کند که در حالت A نخواستی. در حالت A پیشنهاد: صدای سیستم را غیرفعال بگذار، یا با هشدار:
«برای ضبط صدای سیستم در حالت A، مرورگر باید از شما اجازهٔ ضبط صفحه بخواهد. می‌خواهید ادامه دهید؟ (در غیر این صورت فقط میکروفون ضبط می‌شود.)»

ترکیب جریان‌های صوتی با Web Audio API → یک stream واحد → ضبط با MediaRecorder({mimeType: 'audio/webm'}) → ارسال chunks به backend هر ۱۰ ثانیه به POST /api/recording/inspector/{session_id}/audio-chunk.

۴.۳ لاگ‌های همگام
در طول ضبط:

هر ۱۰ ثانیه: POST /api/render/logs/fetch (با service_ids پروژهٔ منتخب) برای sync لاگ‌های جدید
در همان زمان: capture فعلی inspectorConsoleLogs (که قبلاً در state نگه‌داری می‌شود — خط 6259) به session تجمیع می‌شود
در لحظهٔ stop:

یک fetch نهایی + GET /api/render/logs?service_ids=...&minutes=<duration+2> برای اطمینان از داشتن همهٔ لاگ‌های دورهٔ ضبط
console logs کامل iframe از state موجود

۴.۴ تعاملات کاربر (با cross-origin handling)
چون iframe cross-origin است، event listening مستقیم ممکن نیست. دو راه ممکن:

روش ۱ (پیشنهادی): postMessage bridge

اگر کنترل کد سرور-سمت پروژه را داریم (Render preview)، یک قطعه کوچک JS در صفحات پروژه inject کنیم که این event ها را به parent بفرستد:

// در پروژهٔ مورد inspect (اگر possible)
['click','scroll','submit'].forEach(t => 
  window.addEventListener(t, e => 
    parent.postMessage({__inspector_event: true, type: t, ts: Date.now(), 
                        selector: e.target?.tagName, x: e.clientX, y: e.clientY}, '*')
  )
);
این روش ایده‌آل است ولی شرط دارد: کاربر باید این snippet را در پروژه‌های inspectable خودش inject کرده باشد. اگر نکرده، روش ۲.

روش ۲ (fallback - عمدتاً برای پروژه‌های third-party): استنتاج از فریم‌ها

از مقایسهٔ pixel-diff بین فریم‌های متوالی (با Pillow در backend) تشخیص بده:

آیا صفحه scroll شده (تغییر کل تصویر در محور Y) → ثبت scroll event
آیا یک تغییر موضعی رخ داده (تغییر در یک ناحیه) → احتمالاً click → ثبت inferred_click {area: bbox}
آیا URL تغییر کرده (از تغییر URL bar در صورت visible) → ثبت navigation
این روش approximate است ولی برای پرامپت کافی است.

تصمیم: روش ۱ را اول امتحان کن. اگر در ۲ ثانیه اول هیچ message دریافت نکردی، روش ۲ را فعال کن. هر دو روش رو در کد داشته باش.

ذخیرهٔ events: array از {ts: ms_since_start, type: string, details: object, source: 'postmessage'|'inferred'}.

۴.۵ سنتز پرامپت قوی — تعریف "قوی"
معیار «قوی» (که در آخر برای verify معتبر است):

✅ شامل یادداشت اولیه (raw_idea کاربر) به‌صورت دست‌نخورده در ابتدا
✅ شامل بلوک منشأ که می‌گوید این پرامپت از کجا آمده (ضبط بازرس ویژه، حالت A، تاریخ، مدت، لینک ویدئو در تلگرام)
✅ شامل Transcript تمیز صدا با timestamp (نه فقط متن خام)
✅ شامل خلاصهٔ بصری که توالی زمانی صفحات و تعاملات را توصیف می‌کند
✅ شامل استخراج intent: AI باید بفهمد «دقیقاً کاربر چه می‌گوید، چه می‌خواهد، چه را رد می‌کند، کجاها منظورش است» — نه فقط transcribe خام
✅ شامل قطعات لاگ مرتبط (خطاها و warning های مرتبط با timestampهای فعالیت)
✅ شامل توصیف تعاملات (کلیک‌ها، اسکرول‌ها، navigationها) به زبان طبیعی
✅ شامل فهرستی از فایل‌ها/URLs/کامپوننت‌ها که AI تشخیص داده مرتبط هستند
✅ بدون truncation در هر اندازه‌ای — حتی اگر ۱۰۰هزار کاراکتر شد
✅ ساختاریافته با header markdown قابل خوانش توسط مدل AI بعدی
✅ actionable — یک agent دیگر باید بتواند با فقط این پرامپت کار را پیش ببرد
```

### Step 4: پیاده‌سازی ضبط حالت B (آزاد) — getDisplayMedia، video chunk upload، vision-based location classifier، prompt synthesis مخصوص
**Status:** `done` (100%)
**Scope:** پیاده‌سازی کامل حالت B (ضبط آزاد چندصفحه‌ای) در inspector_recording_service.py. شامل: frontend MediaRecorder با getDisplayMedia برای ویدئو و صدا. video chunk upload به backend هر 5 ثانیه. vision-based location classifier: هر 5 ثانیه یک screenshot از فریم ویدئو استخراج می‌شود و به describe_screenshot_with_vision با system prompt اختصاصی فرستاده می‌شود تا دسته‌بندی کند (chrome_tab/other_app/desktop) و تشخیص دهد آیا مرتبط با پروژه است یا خیر. تعاملات استنتاجی: AI vision خودش تشخیص می‌دهد کاربر در هر بازه چه کرده (clicked/scrolled/typed/navigated). prompt synthesis مخصوص حالت B: با system prompt متفاوت که نقشه زمانی فعالیت با دسته‌بندی و فلگ مرتبط/خارج از پروژه را شامل می‌شود. اگر کاربر به جایی خارج از پروژه رفت ولی در صدا اشاره نکرد → از پرامپت حذف شود. اگر اشاره کرد → به عنوان مرجع/الهام نگه داشته شود.
**Excerpt:**
```
۵. مشخصات دقیق حالت B — ضبط آزاد چندصفحه‌ای
۵.۱ منبع ویدئو
navigator.mediaDevices.getDisplayMedia({ video: {frameRate: 15}, audio: system_audio }) — کاربر در dialog مرورگر یک تب/پنجره/screen انتخاب می‌کند.

۵.۲ صدا
ترکیب میکروفون + system audio (اگر فعال) — همان روش حالت A بخش ۴.۲.

۵.۳ تشخیص محل کاربر (سه دسته جداگانه)
هر ۵ ثانیه یک screenshot از فریم استخراج می‌شود (از ویدئو blob با canvas) و به describe_screenshot_with_vision با system prompt اختصاصی:

این یک فریم از ضبط آزاد است. تشخیص بده در این فریم کاربر کجاست:
دسته‌ها:
1. تب کروم — اگر نوار آدرس مرورگر دیده می‌شود:
   - URL را از نوار آدرس استخراج کن
   - اگر domain یا path شامل '{repo_full_name}' یا '{render_preview_domain}' است → «مرتبط با پروژه»
   - وگرنه → «خارج از پروژه»
   
2. برنامهٔ دیگر دسکتاپ — اگر title bar یک برنامه (نه مرورگر) دیده می‌شود:
   - نام برنامه را تشخیص بده (VS Code, Slack, Terminal, …)
   - اگر نام پروژه یا فایل‌های آن visible است → «مرتبط (دستی)»
   - وگرنه → «خارج (دستی)»
3. دسکتاپ خام — اگر هیچ پنجرهٔ مشخصی روی صفحه نیست (icons + wallpaper):
   - دسته: «desktop»
   - فعالیت: کاربر در حال جابجایی پنجره‌ها یا navigation است
برای هر فریم خروجی بده:
{
  "category": "chrome_tab" | "other_app" | "desktop",
  "related_to_project": bool,
  "url_or_app_name": str,
  "activity_description": str (یک جمله),
  "user_action_inferred": str (مقایسه با فریم قبل: clicked/scrolled/typed/navigated)
}

۵.۴ تعاملات استنتاجی
چون event listening روی صفحات دیگر ممکن نیست:

AI vision (همان call بالا) خودش استنتاج می‌کند: «در این فاصلهٔ زمانی کاربر چه کاری انجام داد؟»
تغییرات بزرگ تصویر (با pixel-diff) → ثبت timestamp + توصیف AI

۵.۵ سنتز پرامپت
مشابه حالت A ولی با system prompt متفاوت برای text-generation نهایی:

کاربر در حال ضبط آزاد بین چند صفحه/برنامه بوده. ورودی‌های زیر را ترکیب کن و یک پرامپت قوی بساز که:
۱) ترتیب زمانی فعالیت‌های کاربر را به زبان طبیعی توصیف کند — کجا رفت، چه دید، چه کار کرد.
۲) اگر کاربر بین چند منبع جابجا شد، ارتباط بین آنها را در پرامپت توضیح بده (مثلاً «کاربر کد را در GitHub خواند، سپس به IDE رفت و فایل را باز کرد»).
۳) اگر کاربر به جایی خارج از پروژه رفت ولی در صدا به آن **اشاره نکرد** → از پرامپت نهایی حذف کن (نامرتبط).
۴) اگر کاربر به جایی خارج رفت **و در صدا به آن اشاره کرد** (مثلاً «این تو IMDB رو ببین، می‌خوام مثل این UI رو بسازم») → آن را در پرامپت به‌عنوان «مرجع/الهام» نگه دار.
۵) intent اصلی کاربر را در یک پاراگراف بالای پرامپت قرار بده.
```

### Step 5: پیاده‌سازی finalize — ارسال به تلگرام با split، حذف از disk، task creation، ارسال به چت inspector
**Status:** `done` (100%)
**Scope:** پیاده‌سازی کامل مرحله finalize در inspector_recording_service.py و inspector_recording.py. شامل: endpoint POST /api/recording/inspector/{sid}/finalize که user_note, send_to_oversight, edited_transcript را دریافت می‌کند. ارسال ویدئو به تلگرام با send_document از notification_service.py با caption شامل خلاصه پرامپت (max 1020 char). اگر ویدئو > 45MB، split با ffmpeg به chunks 45MB و ارسال پشت سر هم با caption [1/N], [2/N], ... حذف کامل /tmp/inspector_recordings/{session_id} از disk پس از ارسال موفق. اگر send_to_oversight=true: فراخوانی endpoint POST /api/oversight/tasks/from-inspector-recording که یک تسک جدید با raw_idea=user_note و prompt=پرامپت قوی می‌سازد. اگر send_to_oversight=false: ارسال پرامپت کامل به چت inspector از طریق مسیر sendToOversight('visual_debug') با pre-built prompt. retry logic: 3 بار retry با backoff [2s, 5s, 10s] برای تلگرام. اگر تلگرام fail کرد: نگه‌داری فایل برای 24 ساعت و notification به کاربر با temporary_download_url.
**Excerpt:**
```
۶. flow کامل — از کلیک تا تحویل
...
10. کاربر [ارسال نهایی] می‌زند:
    - Frontend: POST /api/recording/inspector/{session_id}/finalize با {user_note, send_to_oversight, edited_transcript?}
    - Backend:
      i. ارسال ویدئو به تلگرام با send_document (caption شامل خلاصهٔ پرامپت)
      ii. اگر size > 45MB → split به chunks 45MB و چند call پشت سر هم با caption شماره‌گذاری‌شده
      iii. حذف کامل /tmp/inspector_recordings/{session_id} از disk
      iv. اگر send_to_oversight=true:
          POST /api/oversight/tasks/from-inspector-recording (endpoint جدید):
            - raw_idea = user_note
            - prompt = پرامپت قوی structure شده (طبق ۴.۵)
            - project_id = inspector's loaded project_id
          → برمی‌گرداند task_id
      v. اگر send_to_oversight=false:
          ارسال prompt به چت inspector (همان مسیر sendToOversight('visual_debug') با pre-built prompt)
    - Backend: برمی‌گرداند {success, task_id?, telegram_message_id?, chat_message_id?}
    ↓
11. Frontend: modal بسته می‌شود، toast «ارسال موفق» + لینک به تسک یا پیام چت

۹. مدیریت اندازهٔ فایل تلگرام
bitrate پیش‌فرض ویدئو: 600 kbps (برای 60min ≈ 270MB → بزرگ)
استراتژی: اگر فایل > 45MB → split
با ffmpeg -ss start -t duration -c copy chunk_N.webm برش بدون re-encode
هر chunk حداکثر 45MB
ارسال پشت سر هم با caption [1/N], [2/N], ..., [N/N] — caption نهایی شامل خلاصهٔ پرامپت
اگر تلگرام fail کرد:

۳ بار retry با backoff [2s, 5s, 10s]
اگر بازم fail:
نگه‌داری فایل برای ۲۴ ساعت در /tmp/inspector_recordings/{session_id}/
notification به کاربر: «ویدئو به تلگرام ارسال نشد. دانلود از: {temporary_download_url} (۲۴ ساعت معتبر)»
{temporary_download_url} در پرامپت تسک (بخش منشأ) درج می‌شود
پس از ۲۴ ساعت auto-cleanup

۱۱. ساخت تسک و حفظ یادداشت اولیه (دقیق)
⛔ یادداشت اولیه را با پرامپت AI مخلوط نکن. کاربر صریح:

«اون یادداشت‌های اولیه که برای هر تسک میاد که باید باشه سر جاش ... البته اون یادداشت‌های اول پرامپت‌های تسک‌ها (نسخه به‌روزشده‌ش) باید ابتداش قرار بگیره و همچنین دقیق بگه از کجا ریشه گرفته»

ترجمه به ساختار:

اگر send_to_oversight=true:

یک تسک جدید با POST /api/oversight/tasks/from-inspector-recording
در OversightTask:
raw_idea = user_note (یادداشت اولیهٔ کاربر، عین متن textarea — نسخهٔ به‌روزشده‌اش که الان کاربر می‌نویسد، نه آنچه در ضبط گفت)
prompt = ساختار کامل طبق بخش ۴.۵ که با همان user_note در ابتدا شروع می‌شود (به عنوان heading اول)، بعد بلوک منشأ، بعد بقیه
...
```

### Step 6: پیاده‌سازی کامل UI frontend — دکمه ضبط، picker حالت، status bar، processing modal، preview modal، regenerate
**Status:** `done` (100%)
**Scope:** پیاده‌سازی کامل UI در frontend/src/app/projects/[id]/page.tsx. شامل: دکمه 🎬 ضبط ویدئو در کنار دکمه screenshot (خط 13694-13705) با شرط شلوغ نشدن UI. Picker modal با دو کارت (حالت A و B) و تنظیمات صدا (چک‌باکس میکروفون، دراپ‌دان منبع میکروفون، چک‌باکس صدای سیستم). Status bar شناور پایین صفحه با شمارش زمان، حالت، وضعیت صدا، دکمه‌های مکث و پایان. Processing modal با progress bar برای مراحل (مونتاژ ویدئو، transcribe، vision، logs، prompt). Preview modal با: ویدئو پلیر، textarea یادداشت اولیه، transcript قابل ویرایش، خلاصه بصری read-only (expandable)، لاگ‌ها (accordion)، تعاملات (collapsed by default برای حالت A)، نقشه زمانی فعالیت (برای حالت B)، پرامپت قوی read-only با دکمه copy، چک‌باکس 'ارسال به مرکز نظارت' (پیش‌فرض خاموش). دکمه‌های: ارسال نهایی (سبز)، لغو و دور انداختن (قرمز)، تولید مجدد پرامپت (آبی). state management: videoRecordPickerOpen, videoRecordingActive, videoRecordingMode, videoRecordingDuration, videoRecordingPreview, videoRecordSession. auto-stop در 60 دقیقه با هشدار 5 دقیقه قبل.
**Excerpt:**
```
۳. مشخصات دقیق UI
۳.۱ محل دکمه
در frontend/src/app/projects/[id]/page.tsx خط 13694-13705، دقیقاً بعد از دکمهٔ screenshot (همان flex container absolute top-7 left-2 z-50):

{/* 🎬 دکمهٔ ضبط ویدئو (دو حالته) */}
<button
  onClick={() => setVideoRecordPickerOpen(true)}
  disabled={videoRecordingActive}
  title="ضبط ویدئو از iframe بازرس ویژه یا صفحات آزاد — با لاگ‌ها، صدا، تعاملات و پرامپت AI خودکار"
  className="..."  /* استایل دقیقاً مشابه دکمهٔ screenshot — اندازه و رنگ مشابه برای یکپارچگی بصری */
>
  {videoRecordingActive ? '⏺' : '🎬'}
</button>
شرط شلوغ نشدن UI: اگر در توسعه دیدی این flex container تنگ می‌شود، دکمه را فشرده‌تر کن (فقط آیکون، بدون متن). همان‌طور که کاربر گفته «جایی که حالا شلوغم نشه».

۳.۲ Picker حالت
با کلیک، یک popover/modal کوچک باز می‌شود با دو کارت بزرگ کنار هم:

کارت ۱ — «🎯 ضبط بازرس ویژه» (پیش‌فرض highlighted):

توضیح: «فقط محتوای داخل iframe ضبط می‌شود. تمام لاگ‌ها، تعاملات، اسکرول‌ها و کلیک‌های شما داخل همین مانیتور رصد می‌شود. AI فقط حول همین پروژه پرامپت می‌سازد. اگر در توضیح صوتی به چیزی خارج از این مانیتور اشاره کنید، AI آن را بی‌ربط تشخیص می‌دهد.»
کارت ۲ — «🌐 ضبط آزاد (تب/پنجره/دسکتاپ)»:

توضیح: «مرورگر از شما می‌پرسد کدام تب یا پنجره یا کل صفحه را ضبط کند. می‌توانید آزادانه بین صفحات سیر کنید. AI با تحلیل بصری فریم‌ها متوجه می‌شود در هر لحظه کجا هستید (تب کروم / برنامهٔ دیگر / دسکتاپ) و چه می‌کنید. کلیک‌ها و اسکرول‌ها از مقایسهٔ فریم‌ها استنتاج می‌شوند.»
زیر کارت‌ها — تنظیمات مشترک:

چک‌باکس 🎤 ضبط صدای میکروفون (پیش‌فرض روشن)
درپ‌دان منبع میکروفون: «پیش‌فرض سیستم» / «هندزفری/Bluetooth» / لیست تمام دستگاه‌های صوتی از navigator.mediaDevices.enumerateDevices() (شامل headset، USB mic، Bluetooth devices)
چک‌باکس 🔊 ضبط صدای سیستم (پیش‌فرض خاموش — چون نیاز به اجازهٔ خاص دارد و در همه مرورگرها پشتیبانی نمی‌شود)
دکمه‌های Modal:

[شروع ضبط] (سبز، فعال فقط اگر یک حالت انتخاب شده)
[انصراف] (خاکستری)

۳.۳ نوار وضعیت ضبط
پس از شروع، یک bar شناور پایین صفحه (یا گوشهٔ بالا-راست):

⏺ در حال ضبط: 00:23  |  🎯 iframe  |  🎤 ✓ مایک پیش‌فرض  |  🔊 ✗  |  [⏸ مکث]  [⏹ پایان و آماده‌سازی]
شمارش ثانیه‌ای زمان
در ۵۵ دقیقه (۵ دقیقه به سقف ۶۰ دقیقه): تغییر رنگ به نارنجی + toast هشدار «۵ دقیقه به پایان خودکار مانده»
در ۶۰ دقیقه: auto-stop + toast هشدار + گذار به مرحلهٔ آماده‌سازی

۳.۴ مرحلهٔ آماده‌سازی (Processing)
بین stop و modal پیش‌نمایش، یک modal با progress bar نمایش داده می‌شود:

⏳ در حال آماده‌سازی...
  ⚙️ مونتاژ ویدئو          [✓]
  🎙 transcribe صدا         [⏳ 45%]
  🔍 تحلیل بصری ۸ فریم      [⏳ 3/8]
  📊 جمع‌آوری لاگ‌ها        [✓]
  🤖 سنتز پرامپت قوی        [—]
این برای شفافیت — کاربر بداند سیستم در حال چه‌کاری است و چقدر مانده.

۳.۵ Modal پیش‌نمایش و ارسال
پس از آماده‌سازی، modal بزرگی باز می‌شود با این بخش‌ها (به ترتیب، از بالا به پایین):

🎥 پیش‌نمایش ویدئو — با <video controls> قابل پخش
📝 یادداشت اولیه (raw_idea) — یک textarea با placeholder:
«یادداشت اولیه (اختیاری) — این متن عین به عین در ابتدای پرامپت تسک حفظ می‌شود و در فیلد raw_idea ذخیره می‌گردد. اگر چیزی هست که می‌خواهید AI آن را به‌عنوان قصد اصلی شما درک کند، اینجا بنویسید.»

🎙 Transcript صدا (قابل ویرایش) — کاربر می‌تواند خطاهای transcribe را اصلاح کند، چون این متن به AI داده می‌شود
🔍 خلاصهٔ بصری (read-only، تولیدشده توسط describe_screenshot_with_vision) — قابل expand
📋 لاگ‌ها (به‌صورت accordion، collapsed by default):
Console logs frontend iframe (تعداد + پنل قابل باز شدن)
Backend logs Render (تعداد + پنل قابل باز شدن)
🖱 تعاملات کاربر (در حالت A، collapsed by default): لیست رویدادها با timestamp
🌐 نقشهٔ زمانی فعالیت (در حالت B): جدول [زمان | محل | فعالیت | مرتبط با پروژه؟]
🎯 پرامپت قوی تولیدشده (read-only، قابل copy) — AI generated، نمایش کامل بدون truncation
چک‌باکس [ ] «ارسال به مرکز نظارت به‌جای چت» — پیش‌فرض: خاموش (هرگز تیک زده نشود؛ کاربر صریحاً گفته «این رو تغییر ندی و پیش‌فرضش تیک نخورده باشه»)
دکمه‌ها:
[ارسال نهایی] (سبز، primary)
[لغو و دور انداختن] (قرمز، حذف کامل session)
[تولید مجدد پرامپت] (آبی) — اگر کاربر transcript را ویرایش کرد، می‌تواند AI را دوباره صدا بزند
```
