---
task_id: 5ff5e14f-6da5-4338-a3f6-d45e8f832e9c
title: '[منطق] عدم پشتیبانی از streaming response در معماری فعلی'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T17:41:24.418625+00:00'
updated_at: '2026-05-29T20:14:53.417044+00:00'
---

# [منطق] عدم پشتیبانی از streaming response در معماری فعلی

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ‌کدام از کامپوننت‌ها به streaming response اشاره‌ای ندارند. ai_manager خروجی را به صورت کامل (لیست مدل‌ها) برمی‌گرداند، و oversight_strong_prompt یک پرامپت کامل تولید می‌کند. این با نیازمندی‌های real-time UI ناسازگار است.

## 💥 پیامد (impact)
برای کاربردهای interactive (مثل chat یا code review زنده)، کاربر باید منتظر بماند تا کل پاسخ تولید شود. این تجربه کاربری ضعیفی ایجاد می‌کند و برای taskهای طولانی غیرعملی است.

## 🛠 پیشنهاد رفع اولیه
یک interface streaming به ai_manager اضافه کنید که از async generators پشتیبانی کند. هر provider باید متد stream() داشته باشد. oversight_strong_prompt باید بتواند پرامپت را به صورت chunked تولید کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم پشتیبانی از streaming response در معماری فعلی

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

هیچ‌کدام از کامپوننت‌ها به streaming response اشاره‌ای ندارند. ai_manager خروجی را به صورت کامل (لیست مدل‌ها) برمی‌گرداند، و oversight_strong_prompt یک پرامپت کامل تولید می‌کند. این با نیازمندی‌های real-time UI ناسازگار است.

## 💥 پیامد (impact)
برای کاربردهای interactive (مثل chat یا code review زنده)، کاربر باید منتظر بماند تا کل پاسخ تولید شود. این تجربه کاربری ضعیفی ایجاد می‌کند و برای taskهای طولانی غیرعملی است.

## 🛠 پیشنهاد رفع اولیه
یک interface streaming به ai_manager اضافه کنید که از async generators پشتیبانی کند. هر provider باید متد stream() داشته باشد. oversight_strong_prompt باید بتواند پرامپت را به صورت chunked تولید کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی streaming در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن هرگونه پیاده‌سازی موجود از streaming response در pipeline ai_llm است. باید فایل‌های ai_manager، oversight_strong_prompt، و تمام providerهای موجود (مانند OpenAI، Anthropic و غیره) بررسی شوند. همچنین باید endpointهای API مربوطه (احتمالاً در FastAPI) و هرگونه async generator یا WebSocket موجود جستجو شود. خروجی این مرحله یک مستند کوتاه (در قالب کامیت message یا فایل markdown) است که مشخص می‌کند چه چیزی از قبل وجود دارد و چه چیزی کم است. این مرحله شامل هیچ تغییری در کد نمی‌شود.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد: هیچ‌کدام از کامپوننت‌ها به streaming response اشاره‌ای ندارند. ai_manager خروجی را به صورت کامل (لیست مدل‌ها) برمی‌گرداند، و oversight_strong_prompt یک پرامپت کامل تولید می‌کند. این با نیازمندی‌های real-time UI ناسازگار است. ... ♻️ **احتمال پیاده‌سازی قبلی (مهم):** ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
```

### Step 2: تعریف interface/abstract base class برای streaming در ai_manager
**Status:** `not_done` (0%)
**Scope:** ایجاد یک کلاس انتزاعی (ABC) یا Protocol در ماژول ai_manager که متد `stream()` را به عنوان یک async generator تعریف کند. این interface باید شامل امضای متد با پارامترهای ورودی (مانند prompt، context، model_name) و نوع خروجی (AsyncGenerator[str, None] یا AsyncGenerator[dict, None]) باشد. همچنین باید متد async `generate()` موجود را به‌عنوان fallback نگه دارد. این مرحله فقط interface را تعریف می‌کند و شامل پیاده‌سازی providerها نمی‌شود.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید که از async generators پشتیبانی کند. هر provider باید متد stream() داشته باشد.
```

### Step 3: پیاده‌سازی متد stream() در provider اول (مثلاً OpenAI)
**Status:** `not_done` (0%)
**Scope:** پیاده‌سازی متد `stream()` در کلاس provider مربوط به OpenAI (یا اولین provider موجود در پروژه). این متد باید از کتابخانه رسمی OpenAI (openai>=1.0) برای streaming استفاده کند و هر chunk را yield کند. باید از همان پارامترهای متد `generate()` موجود استفاده کند (مانند temperature، max_tokens). همچنین باید خطاهای API (rate limit، timeout) را به صورت مناسب مدیریت کند. این مرحله فقط یک provider را پوشش می‌دهد.
**Excerpt:**
```
هر provider باید متد stream() داشته باشد.
```

### Step 4: پیاده‌سازی متد stream() در سایر providerهای موجود
**Status:** `not_done` (0%)
**Scope:** پیاده‌سازی متد `stream()` در تمام providerهای دیگری که در پروژه وجود دارند (مانند Anthropic Claude، Google Gemini، یا هر provider سفارشی). هر provider باید از SDK مخصوص خود برای streaming استفاده کند. اگر providerای از streaming پشتیبانی نمی‌کند، متد stream() می‌تواند fallback کند به همان generate() و کل پاسخ را یکباره yield کند. این مرحله همه providerها را پوشش می‌دهد.
**Excerpt:**
```
هر provider باید متد stream() داشته باشد.
```

### Step 5: اصلاح oversight_strong_prompt برای تولید chunked prompt
**Status:** `not_done` (0%)
**Scope:** تغییر کلاس/تابع `oversight_strong_prompt` به طوری که بتواند پرامپت را به صورت chunked (قطعه‌قطعه) تولید کند. این یعنی متد `generate_prompt()` (یا معادل آن) باید به یک async generator تبدیل شود که بخش‌های مختلف پرامپت (system message، context، user input، instructions) را به ترتیب yield کند. همچنین باید یک متد جدید `stream_prompt()` اضافه شود که با `stream()` هماهنگ باشد. این مرحله شامل هماهنگی با ai_manager است.
**Excerpt:**
```
oversight_strong_prompt باید بتواند پرامپت را به صورت chunked تولید کند.
```

### Step 6: ایجاد endpoint جدید FastAPI برای streaming response
**Status:** `not_done` (0%)
**Scope:** اضافه کردن یک endpoint جدید (مثلاً `POST /api/ai/stream`) در فایل FastAPI مربوط به ai_llm pipeline. این endpoint باید از `StreamingResponse` از starlette/fastapi استفاده کند و async generator حاصل از زنجیره `oversight_strong_prompt.stream_prompt()` -> `provider.stream()` را به عنوان response برگرداند. باید از همان authentication/authorization موجود استفاده کند. این endpoint باید با معماری موجود (dependency injection, error handling) سازگار باشد.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید ... این با نیازمندی‌های real-time UI ناسازگار است.
```

### Step 7: به‌روزرسانی ai_manager برای استفاده از stream() به جای generate() در مسیر streaming
**Status:** `not_done` (0%)
**Scope:** تغییر کلاس `ai_manager` به طوری که یک متد جدید `async def stream_response(...)` داشته باشد که از زنجیره کامل streaming استفاده کند: دریافت درخواست، فراخوانی `oversight_strong_prompt.stream_prompt()`، سپس عبور دادن chunkها به `provider.stream()`، و در نهایت yield کردن پاسخ نهایی. متد `generate()` قدیمی باید untouched بماند برای backward compatibility. این مرحله شامل هماهنگی بین کامپوننت‌هاست.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید که از async generators پشتیبانی کند.
```

### Step 8: اتصال endpoint جدید به ai_manager.stream_response()
**Status:** `not_done` (0%)
**Scope:** تغییر endpoint ایجاد شده در مرحله 6 به طوری که به جای فراخوانی مستقیم provider، از `ai_manager.stream_response()` استفاده کند. این شامل عبور دادن پارامترهای درخواست (prompt, model, context) به ai_manager و سپس streaming خروجی آن به client است. همچنین باید error handling مناسب برای中断‌های connection (مثل disconnect client) اضافه شود.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید ... این با نیازمندی‌های real-time UI ناسازگار است.
```

### Step 9: نوشتن تست‌های واحد برای متد stream() در providerها
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های واحد (unit tests) برای متد `stream()` در هر provider. این تست‌ها باید شامل موارد زیر باشند: 1) بررسی اینکه stream() یک async generator است، 2) بررسی اینکه حداقل یک chunk yield می‌کند، 3) بررسی اینکه chunkها با پاسخ کامل مطابقت دارند (با mock کردن API)، 4) بررسی رفتار در صورت خطای API (rate limit, timeout), 5) بررسی fallback برای providerهایی که streaming ندارند. از pytest و pytest-asyncio استفاده شود.
**Excerpt:**
```
هر provider باید متد stream() داشته باشد. ... این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```

### Step 10: نوشتن تست‌های واحد برای oversight_strong_prompt.stream_prompt()
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های واحد برای متد `stream_prompt()` در oversight_strong_prompt. این تست‌ها باید شامل: 1) بررسی اینکه stream_prompt یک async generator است، 2) بررسی اینکه chunkها شامل بخش‌های مختلف پرامپت هستند (system, context, user), 3) بررسی ترتیب صحیح chunkها, 4) بررسی رفتار با ورودی‌های خالی یا بسیار طولانی, 5) بررسی هماهنگی با stream() در provider.
**Excerpt:**
```
oversight_strong_prompt باید بتواند پرامپت را به صورت chunked تولید کند.
```

### Step 11: نوشتن تست‌های integration برای endpoint /api/ai/stream
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های integration (با استفاده از TestClient FastAPI) که endpoint جدید streaming را تست می‌کنند. این تست‌ها باید شامل: 1) ارسال درخواست معتبر و دریافت chunkها, 2) بررسی اینکه response headers صحیح هستند (Content-Type), 3) بررسی رفتار با پارامترهای نامعتبر (model ناموجود), 4) بررسی disconnect client, 5) بررسی هماهنگی با ai_manager. از mock برای providerها استفاده شود تا وابستگی به API خارجی نباشد.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید ... این با نیازمندی‌های real-time UI ناسازگار است.
```

### Step 12: به‌روزرسانی مستندات API برای endpoint جدید streaming
**Status:** `not_done` (0%)
**Scope:** به‌روزرسانی مستندات API (احتمالاً در فایل‌های OpenAPI/Swagger یا مستندات جداگانه) برای شامل کردن endpoint جدید `/api/ai/stream`. این شامل توضیح پارامترها، نوع response (text/event-stream)، مثال درخواست و پاسخ، و نکات مربوط به handling streaming در client-side است. همچنین باید backward compatibility با endpoint قدیمی توضیح داده شود.
**Excerpt:**
```
یک interface streaming به ai_manager اضافه کنید ... این با نیازمندی‌های real-time UI ناسازگار است.
```

### Step 13: بررسی و رفع coherence issues در pipeline (audit نهایی)
**Status:** `not_done` (0%)
**Scope:** بررسی نهایی کل pipeline ai_llm برای اطمینان از اینکه streaming به صورت یکپارچه در همه کامپوننت‌ها کار می‌کند. این شامل: 1) بررسی اینکه ai_manager هم generate() و هم stream_response() را به درستی مدیریت می‌کند, 2) بررسی اینکه oversight_strong_prompt هم generate_prompt() و هم stream_prompt() را دارد, 3) بررسی اینکه endpoint قدیمی (غیر-streaming) هنوز کار می‌کند, 4) بررسی اینکه هیچ coherence issue جدیدی ایجاد نشده است. این مرحله شامل commit نهایی با خلاصه تغییرات است.
**Excerpt:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```
