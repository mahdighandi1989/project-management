---
task_id: ab8ec3aa-b6c0-4904-8756-79c22e50ce54
title: '[Effectiveness] فقدان تست‌های یکپارچگی برای Notification Service با 12+ نوع رویداد'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T16:46:56.468340+00:00'
updated_at: '2026-06-03T18:13:42.594641+00:00'
---

# [Effectiveness] فقدان تست‌های یکپارچگی برای Notification Service با 12+ نوع رویداد

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
پس از افزودن تست‌های یکپارچگی، 100% رویدادهای پشتیبانی‌شده باید بدون خطا ارسال شوند و soundهای اختصاصی به درستی پخش شوند

## 📊 وضعیت فعلی
با وجود 12+ نوع رویداد و per-event sound، هیچ تست خودکاری برای صحت عملکرد notification service وجود ندارد. صرفاً error_rate صفر است.

## 🛠 اقدام پیشنهادی
ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping، و تست webhook handler با mock تلگرام.

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] فقدان تست‌های یکپارچگی برای Notification Service با 12+ نوع رویداد

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
پس از افزودن تست‌های یکپارچگی، 100% رویدادهای پشتیبانی‌شده باید بدون خطا ارسال شوند و soundهای اختصاصی به درستی پخش شوند

## 📊 وضعیت فعلی
با وجود 12+ نوع رویداد و per-event sound، هیچ تست خودکاری برای صحت عملکرد notification service وجود ندارد. صرفاً error_rate صفر است.

## 🛠 اقدام پیشنهادی
ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping، و تست webhook handler با mock تلگرام.

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: static)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: static)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: static)_

## Task Steps

### Step 1: شناسایی و مستندسازی تمام 12+ نوع رویداد و sound mapping موجود در Notification Service
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی کامل کد منبع Notification Service برای یافتن تمام انواع رویدادهای پشتیبانی‌شده (مانند new_message, task_assigned, etc.) و mapping آن‌ها به soundهای اختصاصی است. باید یک لیست جامع از event types و sound files مربوطه تهیه شود. خارج از این مرحله: ایجاد تست، تغییر کد، یا اصلاح mapping. نکته حیاتی: باید فایل‌های کانفیگ، enumها، و هرگونه دیکشنری mapping در کد جستجو شود.
**Excerpt:**
```
با وجود 12+ نوع رویداد و per-event sound، هیچ تست خودکاری برای صحت عملکرد notification service وجود ندارد. ... ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping
```

### Step 2: ایجاد فیکسچرها و mock objects برای تست Notification Service
**Status:** `not_done` (0%)
**Scope:** ایجاد فیکسچرهای pytest و mock objects برای شبیه‌سازی وابستگی‌های Notification Service مانند دیتابیس، صف پیام، سرویس تلگرام، و سرویس صدا. این شامل mock کردن توابع ارسال notification، پخش sound، و webhook handlerهاست. خارج از این مرحله: نوشتن تست‌های واقعی. نکته حیاتی: mockها باید به اندازه کافی انعطاف‌پذیر باشند تا بتوانند سناریوهای مختلف را پوشش دهند.
**Excerpt:**
```
ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping، و تست webhook handler با mock تلگرام.
```

### Step 3: نوشتن تست‌های unit برای هر نوع رویداد با بررسی صحت sound mapping
**Status:** `not_done` (0%)
**Scope:** برای هر یک از 12+ نوع رویداد، یک تست unit بنویسید که بررسی کند Notification Service رویداد را به درستی پردازش می‌کند و sound مناسب را انتخاب می‌کند. تست‌ها باید mapping event type به sound file را تأیید کنند. خارج از این مرحله: تست integration با سرویس‌های خارجی. نکته حیاتی: هر تست باید یک event type خاص را تست کند و نتیجه sound mapping را بررسی نماید.
**Excerpt:**
```
ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping
```

### Step 4: نوشتن تست‌های unit برای webhook handler با mock تلگرام
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های unit برای webhook handler که رویدادها را از تلگرام دریافت می‌کند. از mock برای شبیه‌سازی درخواست‌های تلگرام استفاده کنید. تست‌ها باید بررسی کنند که handler رویداد را به درستی parse کرده و به Notification Service ارسال می‌کند. خارج از این مرحله: تست واقعی با API تلگرام. نکته حیاتی: mock باید انواع مختلف payloadهای تلگرام را پوشش دهد.
**Excerpt:**
```
تست webhook handler با mock تلگرام
```

### Step 5: نوشتن تست‌های integration برای Notification Service با mock سرویس‌های خارجی
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های integration که جریان کامل ارسال notification را از دریافت رویداد تا ارسال notification و پخش sound تست می‌کنند. از mock برای سرویس‌های خارجی (تلگرام، سرویس صدا) استفاده کنید. تست‌ها باید بررسی کنند که notification به درستی ارسال شده و sound پخش می‌شود. خارج از این مرحله: تست با سرویس‌های واقعی. نکته حیاتی: تست‌ها باید outcome نهایی را بررسی کنند، نه فقط اینکه تابع صدا زده شده است.
**Excerpt:**
```
ایجاد تست‌های unit و integration برای هر نوع رویداد، بررسی صحت sound mapping
```

### Step 6: نوشتن تست‌های E2E که outcome مطلوب را اندازه‌گیری می‌کنند
**Status:** `not_done` (0%)
**Scope:** نوشتن یک یا چند تست end-to-end که سناریوی کامل را شبیه‌سازی می‌کنند: از ایجاد یک رویداد (مثلاً از طریق API) تا دریافت notification و پخش sound. این تست‌ها باید outcome مطلوب را اندازه‌گیری کنند: اینکه notification بدون خطا ارسال شده و sound به درستی پخش شده است. خارج از این مرحله: تست در محیط production. نکته حیاتی: این تست‌ها باید measurable outcome را بررسی کنند، نه فقط اینکه کد اجرا شده است.
**Excerpt:**
```
پس از افزودن تست‌های یکپارچگی، 100% رویدادهای پشتیبانی‌شده باید بدون خطا ارسال شوند و soundهای اختصاصی به درستی پخش شوند ... test E2E که outcome را اندازه می‌گیرد عبور می‌کند
```

### Step 7: اضافه کردن metric/log برای تشخیص outcome rate در production
**Status:** `not_done` (0%)
**Scope:** اضافه کردن metricها و logهای مناسب به Notification Service تا بتوان در production نرخ موفقیت ارسال notification و پخش sound را اندازه‌گیری کرد. این شامل logging هر رویداد، نتیجه ارسال notification، و نتیجه پخش sound است. خارج از این مرحله: راه‌اندازی سیستم monitoring. نکته حیاتی: metricها باید به اندازه کافی granular باشند تا بتوان مشکلات را تشخیص داد.
**Excerpt:**
```
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 8: بررسی و اصلاح outcome target به صورت measurable
**Status:** `not_done` (0%)
**Scope:** بررسی outcome target فعلی ('100% رویدادها بدون خطا ارسال شوند و soundها به درستی پخش شوند') و بازنویسی آن به صورت measurable و قابل اندازه‌گیری. مثلاً تبدیل به 'نرخ موفقیت ارسال notification > 99.9% و نرخ پخش صحیح sound > 99%'. خارج از این مرحله: تغییر کد. نکته حیاتی: outcome target باید قابل اندازه‌گیری با metricهای اضافه شده در مرحله قبل باشد.
**Excerpt:**
```
outcome target به‌صورت measurable بازنویسی شد
```

### Step 9: اجرای تست‌ها و رفع خطاهای احتمالی
**Status:** `not_done` (0%)
**Scope:** اجرای تمام تست‌های نوشته شده (unit, integration, E2E) و رفع هرگونه خطا یا شکست. اطمینان از اینکه `pytest` تمام تست‌ها را پاس می‌کند. خارج از این مرحله: تغییر معماری یا اضافه کردن قابلیت جدید. نکته حیاتی: اگر تست‌ها fail شوند، باید کد اصلی اصلاح شود تا تست‌ها پاس شوند.
**Excerpt:**
```
هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 10: اجرای linter و type-check و رفع warningها
**Status:** `not_done` (0%)
**Scope:** اجرای linter (مانند flake8, pylint) و type-checker (مانند mypy) روی کد تغییر یافته و رفع تمام warningها و errors. اطمینان از اینکه کد با استانداردهای پروژه مطابقت دارد. خارج از این مرحله: تغییر سبک کدنویسی. نکته حیاتی: linter و type-checker باید بدون هیچ warning پاس شوند.
**Excerpt:**
```
linter بدون warning عبور می‌کند ... type-check موفق
```
