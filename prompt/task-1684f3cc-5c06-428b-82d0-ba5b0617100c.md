---
task_id: 1684f3cc-5c06-428b-82d0-ba5b0617100c
title: '[منطق] عدم پشتیبانی از rollback در oversight_upload_session'
type: logic_audit
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T17:42:24.948934+00:00'
updated_at: '2026-05-20T04:25:11.399211+00:00'
---

# [منطق] عدم پشتیبانی از rollback در oversight_upload_session

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در صورت خطا در حین آپلود (مثلاً قطع connection)، سشن در state نامشخص باقی می‌ماند و فایل‌های temp پاکسازی نمی‌شوند. مکانیزم rollback یا abort تعریف نشده است.

## 💥 پیامد (impact)
انباشت فایل‌های temp روی دیسک که باعث مصرف فضای ذخیره‌سازی و کاهش performance می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک مکانیزم timeout و cleanup برای سشن‌های ناقص اضافه کنید. همچنین endpointهایی برای abort سشن و پاکسازی فایل‌های temp تعریف کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم پشتیبانی از rollback در oversight_upload_session

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline auth است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در صورت خطا در حین آپلود (مثلاً قطع connection)، سشن در state نامشخص باقی می‌ماند و فایل‌های temp پاکسازی نمی‌شوند. مکانیزم rollback یا abort تعریف نشده است.

## 💥 پیامد (impact)
انباشت فایل‌های temp روی دیسک که باعث مصرف فضای ذخیره‌سازی و کاهش performance می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک مکانیزم timeout و cleanup برای سشن‌های ناقص اضافه کنید. همچنین endpointهایی برای abort سشن و پاکسازی فایل‌های temp تعریف کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `auth` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: medium
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `auth` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی pipeline auth و شناسایی فرض‌های ناسازگار
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن تمام فایل‌های مرتبط با pipeline auth، به‌ویژه بخش‌های مربوط به oversight_upload_session، مدیریت سشن‌های آپلود، فایل‌های temp، و مکانیزم‌های rollback/abort است. باید فرض‌های ناسازگار بین بخش‌های مختلف کد شناسایی و مستند شوند. خارج از این مرحله: پیاده‌سازی هرگونه تغییر یا اصلاح کد. نکته حیاتی: این مرحله صرفاً تحلیلی و مستندسازی است و هیچ تغییری در کد ایجاد نمی‌کند.
**Excerpt:**
```
در pipeline `auth` یک ناسازگاری منطقی پیدا شد: در صورت خطا در حین آپلود (مثلاً قطع connection)، سشن در state نامشخص باقی می‌ماند و فایل‌های temp پاکسازی نمی‌شوند. مکانیزم rollback یا abort تعریف نشده است. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 2: تعیین ground truth و align کردن فرض‌های ناسازگار
**Status:** `not_done` (0%)
**Scope:** پس از شناسایی فرض‌های ناسازگار در مرحله قبل، این مرحله ground truth (حقیقت مرجع) را تعیین می‌کند: تصمیم‌گیری می‌کند که کدام رفتار صحیح است (مثلاً: هر سشن آپلود باید timeout داشته باشد و در صورت خطا، فایل‌های temp پاکسازی شوند). سپس فرض‌های طرف دیگر کد با این ground truth هماهنگ می‌شوند. خارج از این مرحله: پیاده‌سازی کد جدید. نکته حیاتی: این مرحله تصمیم‌گیری معماری است و باید در مستندات یا کامیت message ثبت شود.
**Excerpt:**
```
ground truth تعیین شد و طرف دیگر align شد. coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 3: اضافه کردن مکانیزم timeout برای سشن‌های آپلود ناقص
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم timeout برای oversight_upload_session است. باید تعیین شود که پس از چه مدتی (مثلاً 30 دقیقه) یک سشن آپلود ناقص به‌طور خودکار منقضی شود. این مکانیزم باید در سطح pipeline auth پیاده‌سازی شود و شامل یک تابع یا کلاس جدید برای مدیریت timeout باشد. خارج از این مرحله: endpointهای abort و پاکسازی دستی. نکته حیاتی: timeout باید قابل تنظیم باشد (مثلاً از طریق environment variable یا config).
**Excerpt:**
```
یک مکانیزم timeout و cleanup برای سشن‌های ناقص اضافه کنید. در صورت خطا در حین آپلود (مثلاً قطع connection)، سشن در state نامشخص باقی می‌ماند و فایل‌های temp پاکسازی نمی‌شوند.
```

### Step 4: پیاده‌سازی مکانیزم cleanup خودکار فایل‌های temp برای سشن‌های منقضی‌شده
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم cleanup است که پس از timeout یا abort یک سشن، فایل‌های temp مرتبط با آن سشن را از دیسک پاک کند. این مکانیزم باید به‌صورت خودکار (مثلاً با یک background task یا cron job) اجرا شود و تمام فایل‌های temp مربوط به سشن‌های منقضی‌شده را حذف کند. خارج از این مرحله: endpoint دستی برای پاکسازی. نکته حیاتی: cleanup باید atomic باشد و در صورت خطا در حذف یک فایل، بقیه فایل‌ها همچنان حذف شوند.
**Excerpt:**
```
فایل‌های temp پاکسازی نمی‌شوند. مکانیزم rollback یا abort تعریف نشده است. انباشت فایل‌های temp روی دیسک که باعث مصرف فضای ذخیره‌سازی و کاهش performance می‌شود.
```

### Step 5: تعریف endpoint برای abort سشن آپلود
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint جدید (مثلاً POST /auth/upload-session/{id}/abort) است که به کاربر اجازه می‌دهد یک سشن آپلود را به‌صورت دستی abort کند. این endpoint باید state سشن را به 'aborted' تغییر دهد و cleanup فایل‌های temp را آغاز کند. خارج از این مرحله: cleanup خودکار timeout (مرحله قبل). نکته حیاتی: این endpoint باید احراز هویت شود و فقط مالک سشن بتواند آن را abort کند.
**Excerpt:**
```
endpointهایی برای abort سشن و پاکسازی فایل‌های temp تعریف کنید.
```

### Step 6: تعریف endpoint برای پاکسازی دستی فایل‌های temp یک سشن خاص
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint دیگر (مثلاً POST /auth/upload-session/{id}/cleanup) است که صرفاً فایل‌های temp یک سشن خاص را پاک می‌کند بدون اینکه state سشن را تغییر دهد. این برای مواردی مفید است که سشن کامل شده اما فایل‌های temp به‌دلیلی باقی مانده‌اند. خارج از این مرحله: abort سشن. نکته حیاتی: این endpoint باید بررسی کند که سشن در state مناسب (مثلاً 'completed' یا 'failed') باشد.
**Excerpt:**
```
endpointهایی برای abort سشن و پاکسازی فایل‌های temp تعریف کنید.
```

### Step 7: اضافه کردن integration test برای pipeline auth با سناریوی timeout
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test است که سناریوی timeout سشن آپلود را شبیه‌سازی می‌کند: یک سشن آپلود شروع می‌شود، منتظر می‌مانیم تا timeout شود، و سپس بررسی می‌کنیم که state سشن به 'expired' تغییر کرده و فایل‌های temp پاکسازی شده‌اند. خارج از این مرحله: unit test برای توابع جداگانه. نکته حیاتی: این test باید در pipeline CI/CD اجرا شود و از دیتابیس واقعی یا mock استفاده کند.
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند.
```

### Step 8: اضافه کردن integration test برای endpoint abort سشن
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test برای endpoint abort است: یک سشن آپلود شروع می‌شود، درخواست abort ارسال می‌شود، و سپس بررسی می‌کنیم که state سشن به 'aborted' تغییر کرده و فایل‌های temp پاکسازی شده‌اند. خارج از این مرحله: test برای timeout. نکته حیاتی: این test باید احراز هویت را نیز test کند (فقط مالک سشن بتواند abort کند).
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند.
```

### Step 9: اضافه کردن integration test برای endpoint cleanup دستی فایل‌های temp
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک integration test برای endpoint cleanup دستی است: یک سشن آپلود کامل می‌شود، فایل‌های temp به‌صورت دستی باقی می‌مانند (با شبیه‌سازی)، درخواست cleanup ارسال می‌شود، و سپس بررسی می‌کنیم که فایل‌های temp پاک شده‌اند اما state سشن تغییر نکرده است. خارج از این مرحله: test برای abort. نکته حیاتی: این test باید بررسی کند که cleanup فقط برای سشن‌های با state مناسب مجاز است.
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند.
```

### Step 10: نوشتن PR description با توضیح کامل تصمیمات و checklist کامیت‌ها
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک PR description جامع است که شامل: توضیح مشکل (ناسازگاری منطقی)، ground truth انتخاب‌شده، توضیح هر کامیت (timeout, cleanup, endpointها, testها)، و یک checklist از همه کامیت‌ها. خارج از این مرحله: تغییر کد. نکته حیاتی: PR description باید شامل بخش 'Why this decision was made' باشد.
**Excerpt:**
```
PR description توضیح می‌دهد چرا این تصمیم گرفته شد. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد. ground truth تعیین شد و طرف دیگر align شد.
```
