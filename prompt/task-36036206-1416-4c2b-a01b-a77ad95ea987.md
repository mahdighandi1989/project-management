---
task_id: 36036206-1416-4c2b-a01b-a77ad95ea987
title: '[منطق] عدم وجود جدول User و Role در مدل‌ها'
type: logic_audit
priority: critical
execution_priority: 1000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T16:40:40.415121+00:00'
updated_at: '2026-05-29T20:13:47.076959+00:00'
---

# [منطق] عدم وجود جدول User و Role در مدل‌ها

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در components فقط inspector_session به عنوان مدل معرفی شده است. هیچ مدلی برای User یا Role وجود ندارد. این یعنی سیستم احراز هویت و مجوزدهی اساساً پیاده‌سازی نشده است.

## 💥 پیامد (impact)
بدون مدل User و Role، امکان ذخیره کاربران و نقش‌ها وجود ندارد. pipeline auth بدون این مدل‌ها ناقص است.

## 🛠 پیشنهاد رفع اولیه
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید. سپس inspector_session را به User متصل کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] عدم وجود جدول User و Role در مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline auth است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `auth` یک ناسازگاری منطقی پیدا شد:

در components فقط inspector_session به عنوان مدل معرفی شده است. هیچ مدلی برای User یا Role وجود ندارد. این یعنی سیستم احراز هویت و مجوزدهی اساساً پیاده‌سازی نشده است.

## 💥 پیامد (impact)
بدون مدل User و Role، امکان ذخیره کاربران و نقش‌ها وجود ندارد. pipeline auth بدون این مدل‌ها ناقص است.

## 🛠 پیشنهاد رفع اولیه
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید. سپس inspector_session را به User متصل کنید.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `auth` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: manual_only)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: manual_only)_
3. integration test برای pipeline `auth` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی pipeline auth و مدل‌های موجود
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repo برای یافتن تمام فایل‌های مرتبط با pipeline auth، مدل‌های موجود (inspector_session و هر مدل دیگر)، endpoint‌های مرتبط، و وابستگی‌ها است. باید مشخص شود که آیا مدل‌های User یا Role قبلاً (به صورت کامل یا ناقص) وجود دارند یا خیر. همچنین فرضیات ناسازگار بین بخش‌های مختلف کد شناسایی و مستند می‌شوند. خروجی این مرحله یک سند (یا کامیت) است که وضعیت فعلی را شرح می‌دهد. این مرحله شامل هیچ تغییری در کد نمی‌شود.
**Excerpt:**
```
در pipeline `auth` یک ناسازگاری منطقی پیدا شد: در components فقط inspector_session به عنوان مدل معرفی شده است. هیچ مدلی برای User یا Role وجود ندارد. این یعنی سیستم احراز هویت و مجوزدهی اساساً پیاده‌سازی نشده است. ... coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 2: ایجاد مدل User با فیلدهای اصلی (username, password_hash, role_id)
**Status:** `not_done` (0%)
**Scope:** ایجاد یک مدل دیتابیس (SQLAlchemy یا ORM مشابه) برای User در مسیر مناسب (مثلاً `app/models/user.py`). فیلدهای اجباری شامل `id` (primary key)، `username` (unique, not null)، `password_hash` (not null)، `role_id` (foreign key به Role) و `created_at`/`updated_at` است. این مدل باید با استانداردهای کدنویسی پروژه (مثل استفاده از Base, dataclass, pydantic) هماهنگ باشد. این مرحله شامل ایجاد جدول دیتابیس (از طریق migration) نیست.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید. سپس inspector_session را به User متصل کنید.
```

### Step 3: ایجاد مدل Role با فیلدهای اصلی (name, permissions)
**Status:** `not_done` (0%)
**Scope:** ایجاد یک مدل دیتابیس برای Role در مسیر مناسب (مثلاً `app/models/role.py`). فیلدهای اجباری شامل `id` (primary key)، `name` (unique, not null) و `permissions` (JSON یا Text برای ذخیره مجوزها) است. این مدل باید با استانداردهای پروژه هماهنگ باشد. این مرحله شامل ایجاد جدول دیتابیس نیست.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید.
```

### Step 4: ایجاد رابطه (relationship) بین User و Role
**Status:** `not_done` (0%)
**Scope:** اضافه کردن یک relationship در مدل User به Role (مثلاً `role = relationship('Role', back_populates='users')`) و در مدل Role (مثلاً `users = relationship('User', back_populates='role')`). همچنین اطمینان از اینکه foreign key `role_id` در مدل User به درستی تعریف شده است. این مرحله شامل ایجاد migration نیست.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید.
```

### Step 5: ایجاد رابطه (relationship) بین inspector_session و User
**Status:** `not_done` (0%)
**Scope:** اضافه کردن یک foreign key `user_id` در مدل `inspector_session` و ایجاد relationship بین `inspector_session` و `User`. همچنین در صورت نیاز، back_populates یا backref مناسب در مدل User اضافه شود. این مرحله شامل بررسی فایل موجود `inspector_session` و تطبیق آن با استانداردهای جدید است.
**Excerpt:**
```
سپس inspector_session را به User متصل کنید.
```

### Step 6: ایجاد migration دیتابیس برای جداول User, Role و تغییرات inspector_session
**Status:** `not_done` (0%)
**Scope:** استفاده از ابزار migration موجود در پروژه (احتمالاً Alembic) برای ایجاد یک migration جدید که جداول `users` و `roles` را ایجاد کرده و فیلد `user_id` را به جدول `inspector_sessions` اضافه کند. این مرحله شامل اجرای migration بر روی دیتابیس توسعه است.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید. سپس inspector_session را به User متصل کنید.
```

### Step 7: به‌روزرسانی یا ایجاد Pydantic schema برای User و Role
**Status:** `not_done` (0%)
**Scope:** ایجاد یا به‌روزرسانی schemaهای Pydantic (یا معادل) برای سریالایز/دیسریالایز کردن مدل‌های User و Role. این شامل schemaهای ورودی (برای ایجاد کاربر) و خروجی (برای نمایش اطلاعات کاربر) است. همچنین schema برای Role. این مرحله شامل endpoint‌ها نمی‌شود.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید.
```

### Step 8: ایجاد endpoint‌های CRUD پایه برای Role (ایجاد، خواندن، لیست)
**Status:** `not_done` (0%)
**Scope:** ایجاد endpoint‌های API برای مدیریت نقش‌ها: `POST /roles` (ایجاد نقش جدید)، `GET /roles/{id}` (دریافت یک نقش), `GET /roles` (لیست همه نقش‌ها). این endpointها باید از schemaهای Pydantic استفاده کنند و با دیتابیس تعامل داشته باشند. این مرحله شامل endpointهای به‌روزرسانی و حذف نمی‌شود.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید.
```

### Step 9: ایجاد endpoint‌های CRUD پایه برای User (ثبت‌نام، دریافت، لیست)
**Status:** `not_done` (0%)
**Scope:** ایجاد endpointهای API برای مدیریت کاربران: `POST /users/register` (ثبت‌نام کاربر جدید با username و password)، `GET /users/{id}` (دریافت اطلاعات یک کاربر), `GET /users` (لیست همه کاربران). در endpoint ثبت‌نام، password باید hash شود. این مرحله شامل endpointهای به‌روزرسانی و حذف نمی‌شود.
**Excerpt:**
```
مدل‌های User و Role را با فیلدهای مناسب (username, password_hash, role_id و ...) ایجاد کنید.
```

### Step 10: ایجاد endpoint لاگین (POST /auth/login) و تولید JWT token
**Status:** `not_done` (0%)
**Scope:** ایجاد یک endpoint برای لاگین کاربران: `POST /auth/login` که username و password را دریافت کرده، اعتبارسنجی می‌کند و در صورت موفقیت، یک JWT token (access token) برمی‌گرداند. این مرحله شامل پیاده‌سازی منطق احراز هویت (verify password) و تولید token است. همچنین باید یک تابع dependency برای استخراج کاربر فعلی از token ایجاد شود.
**Excerpt:**
```
در pipeline `auth` یک ناسازگاری منطقی پیدا شد: ... سیستم احراز هویت و مجوزدهی اساساً پیاده‌سازی نشده است.
```

### Step 11: پیاده‌سازی سیستم مجوزدهی (Role-Based Access Control) برای endpointها
**Status:** `not_done` (0%)
**Scope:** ایجاد یک dependency یا decorator (مثلاً `require_role('admin')`) که بررسی می‌کند آیا کاربر فعلی (که از JWT استخراج شده) نقش مورد نیاز را دارد یا خیر. این dependency باید در endpointهای حساس (مثل مدیریت کاربران و نقش‌ها) استفاده شود. این مرحله شامل اعمال این مجوزها بر روی endpointهای ساخته شده در مراحل قبل است.
**Excerpt:**
```
در pipeline `auth` یک ناسازگاری منطقی پیدا شد: ... هیچ مدلی برای User یا Role وجود ندارد. این یعنی سیستم احراز هویت و مجوزدهی اساساً پیاده‌سازی نشده است.
```

### Step 12: به‌روزرسانی endpoint inspector_session برای استفاده از کاربر احراز هویت‌شده
**Status:** `not_done` (0%)
**Scope:** بررسی و به‌روزرسانی endpointهای مرتبط با `inspector_session` (که احتمالاً از قبل وجود دارند) به گونه‌ای که به طور خودکار `user_id` را از کاربر احراز هویت‌شده (JWT) دریافت کرده و در session ذخیره کنند. این مرحله شامل تغییر منطق ایجاد session است.
**Excerpt:**
```
سپس inspector_session را به User متصل کنید.
```

### Step 13: نوشتن unit tests برای مدل‌های User و Role
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های واحد برای مدل‌های User و Role شامل: ایجاد نمونه، اعتبارسنجی فیلدهای اجباری، بررسی unique constraint (با mock دیتابیس)، و بررسی relationship‌ها. این تست‌ها باید با استفاده از فریمورک تست موجود (pytest) نوشته شوند.
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند
```

### Step 14: نوشتن unit tests برای endpointهای User و Role
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های واحد برای endpointهای ایجاد شده (با استفاده از TestClient فریمورک FastAPI). این تست‌ها باید شامل: ایجاد نقش، ایجاد کاربر، لاگین، و بررسی خطاهای معتبر (مثل username تکراری، رمز اشتباه) باشند. دیتابیس باید mock شود.
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند
```

### Step 15: نوشتن integration tests برای pipeline auth
**Status:** `not_done` (0%)
**Scope:** نوشتن تست‌های integration که کل pipeline auth را از ابتدا تا انتها تست می‌کنند: ایجاد نقش، ثبت‌نام کاربر، لاگین، دسترسی به endpoint محافظت‌شده، و ایجاد inspector_session. این تست‌ها باید با یک دیتابیس تست (مثلاً SQLite in-memory) اجرا شوند.
**Excerpt:**
```
integration test برای pipeline `auth` بدون شکست عبور می‌کند
```

### Step 16: مستندسازی نهایی و ایجاد PR description
**Status:** `not_done` (0%)
**Scope:** ایجاد یک PR description جامع که شامل: خلاصه تغییرات، لیست کامیت‌ها، توضیح ناسازگاری اولیه و نحوه رفع آن، و checklist از تمام مراحل انجام شده است. همچنین به‌روزرسانی هرگونه مستندات مرتبط (مثل README).
**Excerpt:**
```
PR description توضیح می‌دهد چرا این تص
```
