---
task_id: 756c8f85-282b-4318-ac3d-442e7c0f7ee8
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-27T22:09:00.236507+00:00'
updated_at: '2026-05-29T20:35:58.718389+00:00'
target_files:
- frontend/src/app/creator/page.tsx
- backend/app/api/routes/analysis.py
- backend/app/api/routes/model_profiles.py
- frontend/src/app/analysis/page.tsx
- backend/app/api/routes/models.py
- backend/app/api/routes/creator.py
- backend/app/services/creator_engine.py
- backend/app/services/oversight_service.py
- backend/app/services/health_to_issues_service.py
- backend/app/api/routes
- backend/app/services/debate_service.py
- frontend/src/app/oversight/page.tsx
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 📋 چک‌لیست مراحل (20 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: تحلیل و مستندسازی ساختار فعلی صفحه موتور خالق و فایل‌های پروژه** — این مرحله شامل بررسی کامل صفحه موجود 'موتور خالق' (Creator Engine) و شناسایی تمام فایل‌های پروژه (محلی و غیرمحلی) است که در آنجا ساخته شده‌اند. باید ساختار داده‌ای این فایل‌ها، نوع پروژه‌ها، و نحوه نمایش و ذخیره‌سازی آن‌ها مستند شود. خارج از این مرحله: تغییر در کد یا رابط کاربری انجام نمی‌شود. نکته 
- [ ] **مرحله 2: طراحی مکانیزم انتخاب مدل‌های فعال برای بازبینی** — این مرحله شامل طراحی یک سیستم انتخاب مدل است که در صفحه موتور خالق به کاربر اجازه می‌دهد یکی یا چند مدل از میان مدل‌های فعال (که قبلاً در سیستم تعریف شده‌اند) را برای بازبینی فایل‌های پروژه انتخاب کند. این طراحی باید شامل UI (مثلاً یک dropdown یا checkbox group) و منطق پشتیبان (backend logic) برای د
- [ ] **مرحله 3: طراحی مکانیزم ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده برای بازبینی** — این مرحله شامل طراحی فرآیندی است که پس از انتخاب مدل‌ها و فایل‌های پروژه، فایل‌ها (با متادیتای کامل شامل کد، توضیحات، و هدف پروژه) به API هر مدل ارسال شوند. باید مشخص شود که آیا فایل‌ها به صورت تکی ارسال می‌شوند یا به صورت batch، و چگونه پاسخ‌ها (بازبینی‌ها) جمع‌آوری می‌شوند. خارج از این مرحله: پیاد
- [ ] **مرحله 4: طراحی ساختار بازخورد بازبینی (Feedback Schema) برای دریافت نتایج از مدل‌ها** — این مرحله شامل طراحی یک ساختار داده (Schema) استاندارد برای بازخورد بازبینی است که از مدل‌ها دریافت می‌شود. این ساختار باید شامل فیلدهایی برای: شناسه فایل، نام مدل، نمره/امتیاز (اختیاری)، لیست ایرادات تکنیکی، لیست ایرادات منطقی، پیشنهادات برای تطبیق با هدف پروژه، و خلاصه کلی باشد. خارج از این مرحله:
- [ ] **مرحله 5: طراحی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Logic)** — این مرحله شامل طراحی منطقی است که بازخوردهای دریافتی از مدل‌ها را با 'هدف' اصلی پروژه (که در فایل‌های پروژه تعریف شده) مقایسه می‌کند. باید مشخص شود که چگونه 'تطبیق' اندازه‌گیری می‌شود (مثلاً از طریق امتیاز شباهت معنایی یا تطبیق کلمات کلیدی). خارج از این مرحله: پیاده‌سازی این منطق. نکته حیاتی: باید م
- [ ] **مرحله 6: طراحی UI برای نمایش نتایج بازبینی و تطبیق با هدف** — این مرحله شامل طراحی رابط کاربری (UI) برای نمایش نتایج بازبینی‌ها به کاربر است. این UI باید شامل بخش‌های زیر باشد: لیست فایل‌های بازبینی‌شده، خلاصه بازبینی هر فایل، جزئیات ایرادات (تکنیکی و منطقی)، امتیاز تطبیق با هدف، و امکان فیلتر/مرتب‌سازی بر اساس مدل یا نوع ایراد. خارج از این مرحله: پیاده‌سازی ا
- [ ] **مرحله 7: پیاده‌سازی قابلیت انتخاب مدل‌های فعال در صفحه موتور خالق** — این مرحله شامل پیاده‌سازی واقعی UI و منطق پشتیبان برای انتخاب مدل‌های فعال است. یک کامپوننت جدید (مثلاً `ModelSelector`) به صفحه موتور خالق اضافه می‌شود که لیست مدل‌های فعال را از یک API دریافت کرده و به کاربر اجازه انتخاب می‌دهد. خارج از این مرحله: پیاده‌سازی مکانیزم ارسال فایل‌ها یا دریافت بازخورد
- [ ] **مرحله 8: پیاده‌سازی API برای دریافت لیست مدل‌های فعال** — این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `GET /api/models/active`) است که لیست مدل‌های فعال را از دیتابیس یا کانفیگ برگرداند. این endpoint باید توسط کامپوننت `ModelSelector` در مرحله قبل استفاده شود. خارج از این مرحله: تغییر در منطق انتخاب مدل یا ذخیره‌سازی. نکته حیاتی: باید مشخص شود 
- [ ] **مرحله 9: پیاده‌سازی دکمه/اکشن 'بازبینی با مدل‌های انتخاب‌شده' در صفحه موتور خالق** — این مرحله شامل اضافه کردن یک دکمه (مثلاً 'Review with Selected Models') در صفحه موتور خالق است که پس از انتخاب فایل‌های پروژه و مدل‌ها، فرآیند بازبینی را آغاز می‌کند. با کلیک روی این دکمه، یک درخواست به backend ارسال می‌شود تا بازبینی شروع شود. خارج از این مرحله: پیاده‌سازی منطق backend برای بازبینی
- [ ] **مرحله 10: پیاده‌سازی API برای شروع فرآیند بازبینی (POST /api/reviews/start)** — این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `POST /api/reviews/start`) است که لیست فایل‌های پروژه و مدل‌های انتخاب‌شده را دریافت کرده و فرآیند بازبینی را به صورت ناهمزمان (asynchronous) شروع می‌کند. این endpoint باید یک شناسه بازبینی (review ID) برگرداند تا فرانت‌اند بتواند وضعیت را پیگی
- [ ] **مرحله 11: پیاده‌سازی منطق ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده (Backend Service)** — این مرحله شامل پیاده‌سازی سرویس اصلی backend است که پس از دریافت درخواست بازبینی، فایل‌های پروژه را گرفته و آن‌ها را به API هر مدل انتخاب‌شده ارسال می‌کند. این سرویس باید با استفاده از Feedback Schema طراحی‌شده (مرحله 4) پاسخ‌ها را پارس کرده و در دیتابیس ذخیره کند. خارج از این مرحله: پیاده‌سازی UI ی
- [ ] **مرحله 12: پیاده‌سازی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Service)** — این مرحله شامل پیاده‌سازی سرویسی است که بازخوردهای ذخیره‌شده را گرفته و آن‌ها را با هدف پروژه تطبیق می‌دهد. این سرویس باید یک امتیاز تطبیق (alignment score) برای هر بازخورد محاسبه کند و آن را در دیتابیس ذخیره کند. خارج از این مرحله: تغییر در UI یا منطق ارسال فایل. نکته حیاتی: این سرویس می‌تواند به ص
- [ ] **مرحله 13: پیاده‌سازی API برای دریافت نتایج بازبینی (GET /api/reviews/{reviewId}/results)** — این مرحله شامل ایجاد یک endpoint جدید (مثلاً `GET /api/reviews/{reviewId}/results`) است که نتایج کامل یک بازبینی را شامل لیست فایل‌ها، بازخوردهای هر مدل، و امتیازات تطبیق برمی‌گرداند. خارج از این مرحله: پیاده‌سازی UI برای نمایش این نتایج. نکته حیاتی: این endpoint باید از pagination پشتیبانی کند اگر 
- [ ] **مرحله 14: پیاده‌سازی کامپوننت نمایش نتایج بازبینی (ReviewResultsPanel)** — این مرحله شامل پیاده‌سازی یک کامپوننت React (مثلاً `ReviewResultsPanel`) است که نتایج بازبینی را از API دریافت کرده و به صورت ساختاریافته نمایش می‌دهد. این کامپوننت باید شامل بخش‌های: لیست فایل‌ها، خلاصه بازبینی، جزئیات ایرادات (با قابلیت expand/collapse)، و امتیاز تطبیق باشد. خارج از این مرحله: یکپ
- [ ] **مرحله 15: یکپارچه‌سازی کامپوننت نمایش نتایج با صفحه موتور خالق** — این مرحله شامل اضافه کردن کامپوننت `ReviewResultsPanel` به صفحه موتور خالق است. این کامپوننت باید پس از اتمام بازبینی (یا با کلیک روی یک دکمه 'مشاهده نتایج') نمایش داده شود. خارج از این مرحله: تغییر در منطق شروع بازبینی. نکته حیاتی: باید یک state برای مدیریت وضعیت بازبینی (در حال اجرا، کامل شده، خطا
- [ ] **مرحله 16: نوشتن تست‌های واحد (Unit Tests) برای سرویس ارسال فایل به مدل‌ها** — این مرحله شامل نوشتن تست‌های واحد برای سرویس backend است که فایل‌ها را به مدل‌ها ارسال می‌کند. تست‌ها باید موارد زیر را پوشش دهند: ارسال موفق، خطای API مدل، timeout، و پاسخ نامعتبر. خارج از این مرحله: تست‌های یکپارچه‌سازی (integration tests) یا تست‌های UI. نکته حیاتی: از mocking برای شبیه‌سازی API م
- [ ] **مرحله 17: نوشتن تست‌های واحد برای سرویس تطبیق با هدف (Goal Alignment Service)** — این مرحله شامل نوشتن تست‌های واحد برای سرویس تطبیق با هدف است. تست‌ها باید محاسبه امتیاز تطبیق را در سناریوهای مختلف (تطبیق کامل، تطبیق جزئی، عدم تطبیق) بررسی کنند. خارج از این مرحله: تست‌های یکپارچه‌سازی. نکته حیاتی: از داده‌های mock برای بازخورد و هدف پروژه استفاده شود.
- [ ] **مرحله 18: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) برای جریان کامل بازبینی** — این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که جریان کامل بازبینی را از شروع تا دریافت نتایج آزمایش می‌کند. این تست‌ها باید شامل تعامل بین APIها، سرویس‌ها، و دیتابیس باشند. خارج از این مرحله: تست‌های UI. نکته حیاتی: از یک دیتابیس تست (مثلاً SQLite in-memory) استفاده شود.
- [ ] **مرحله 19: نوشتن تست‌های UI برای کامپوننت‌های جدید (ModelSelector و ReviewResultsPanel)** — این مرحله شامل نوشتن تست‌های UI (مثلاً با React Testing Library) برای کامپوننت‌های `ModelSelector` و `ReviewResultsPanel` است. تست‌ها باید تعاملات کاربر مانند انتخاب مدل، کلیک روی دکمه بازبینی، و نمایش نتایج را پوشش دهند. خارج از این مرحله: تست‌های end-to-end. نکته حیاتی: از mocking برای API calls ا
- [ ] **مرحله 20: بازبینی نهایی (Audit) و مستندسازی تغییرات** — این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده، بررسی امنیت (مثلاً نشت API key مدل‌ها)، بهینه‌سازی عملکرد، و به‌روزرسانی مستندات پروژه است. همچنین باید یک checklist از تمام قابلیت‌های پیاده‌سازی‌شده تهیه شود. خارج از این مرحله: اضافه کردن قابلیت جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهند

---

# 🔹 مرحله 1: تحلیل و مستندسازی ساختار فعلی صفحه موتور خالق و فایل‌های پروژه

**Scope:** این مرحله شامل بررسی کامل صفحه موجود 'موتور خالق' (Creator Engine) و شناسایی تمام فایل‌های پروژه (محلی و غیرمحلی) است که در آنجا ساخته شده‌اند. باید ساختار داده‌ای این فایل‌ها، نوع پروژه‌ها، و نحوه نمایش و ذخیره‌سازی آن‌ها مستند شود. خارج از این مرحله: تغییر در کد یا رابط کاربری انجام نمی‌شود. نکته حیاتی: باید دقیقاً مشخص شود که فایل‌های 'محلی' و 'غیرمحلی' چه تفاوتی دارند و چگونه در سیستم فعلی مدیریت می‌شوند.
**Key terms:** صفحه موتور خالق, فایل‌های پروژه محلی, فایل‌های پروژه غیرمحلی, مدل‌های فعال

**بخش مربوط از متن کاربر:**
```
این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
تحلیل و مستندسازی ساختار فایل‌های محلی و غیرمحلی در صفحه موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:62-119` — `CreatorPage`
  ```tsx
  export default function CreatorPage() {
    const router = useRouter();
  
    const [aiReady, setAiReady] = useState(false);
    const [githubReady, setGithubReady] = useState(false);
    const [models, setModels] = useState<A
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک به درخواست کاربر برای تحلیل و مستندسازی ساختار فعلی صفحه 'موتور خالق' (Creator Engine) و فایل‌های پروژه (محلی و غیرمحلی) که در آنجا ساخته شده‌اند، می‌پردازد. کاربر خواسته است که فایل‌های پروژه‌های محلی و غیرمحلی ساخته شده در این صفحه، یک بار دیگر توسط مدل‌هایی که فعال هستند و در آن صفحه انتخاب می‌شوند، بازبینی جدی‌تری شوند و ایرادات تکنیکی و منطقی آن‌ها گرفته شود. این مرحله شامل بررسی کامل صفحه موجود 'موتور خالق' (Creator Engine) و شناسایی تمام فایل‌های پروژه (محلی و غیرمحلی) است که در آنجا ساخته شده‌اند. باید ساختار داده‌ای این فایل‌ها، نوع پروژه‌ها، و نحوه نمایش و ذخیره‌سازی آن‌ها مستند شود. خارج از این مرحله: تغییر در کد یا رابط کاربری انجام نمی‌شود. نکته حیاتی: باید دقیقاً مشخص شود که فایل‌های 'محلی' و 'غیرمحلی' چه تفاوتی دارند و چگونه در سیستم فعلی مدیریت می‌شوند. کلیدواژه‌ها: صفحه موتور خالق, فایل‌های پروژه محلی, فایل‌های پروژه غیرمحلی, مدل‌های فعال. شواهد در کد: در frontend/src/app/creator/page.tsx (خطوط 1-1222) صفحه creator با قابلیت انتخاب مدل‌ها (selectedModelIds در خط 95) و نمایش پروژه‌ها (projects state در خط 68) پیاده‌سازی شده است. در backend/app/api/routes/creator.py (خطوط 1-519) APIهای مربوط به creator engine شامل ایجاد پروژه (خط 411-430) و لیست پروژه‌های فعال (خط 433-441) تعریف شده‌اند. در backend/app/services/creator_engine.py (خطوط 1-239) هسته اصلی creator engine با کلاس‌های Task, TaskResult, CommandExecutor, FileManager پیاده‌سازی شده است. فایل‌های 'محلی' احتمالاً در workspace محلی (creator_engine.py خط 103: tempfile.mkdtemp) و فایل‌های 'غیرمحلی' از طریق GitHub import (backend/app/api/routes/github_import.py) مدیریت می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی کامل frontend/src/app/creator/page.tsx برای شناسایی نحوه نمایش پروژه‌های محلی و غیرمحلی (states: projects در خط 68، filteredProjects در خط 544). 2. تحلیل backend/app/api/routes/creator.py برای درک APIهای مربوط به پروژه‌ها (GET /projects/active در خط 433، POST /projects/create در خط 411). 3. بررسی backend/app/services/creator_engine.py برای درک نحوه ذخیره‌سازی محلی (FileManager در خط 215، workspace_dir در خط 103). 4. تحلیل backend/app/api/routes/github_import.py برای درک نحوه مدیریت پروژه‌های غیرمحلی (GitHub import). 5. مستندسازی تفاوت فایل‌های محلی (ذخیره در workspace محلی با FileManager) و غیرمحلی (import شده از GitHub با github_import.py). 6. ارائه پیشنهاد برای بازبینی فایل‌ها توسط مدل‌های فعال (selectedModelIds در creator/page.tsx خط 95) با استفاده از APIهای موجود در backend/app/api/routes/analysis.py یا backend/app/services/oversight_service.py.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: docs
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: طراحی مکانیزم انتخاب مدل‌های فعال برای بازبینی

**Scope:** این مرحله شامل طراحی یک سیستم انتخاب مدل است که در صفحه موتور خالق به کاربر اجازه می‌دهد یکی یا چند مدل از میان مدل‌های فعال (که قبلاً در سیستم تعریف شده‌اند) را برای بازبینی فایل‌های پروژه انتخاب کند. این طراحی باید شامل UI (مثلاً یک dropdown یا checkbox group) و منطق پشتیبان (backend logic) برای دریافت لیست مدل‌های فعال باشد. خارج از این مرحله: پیاده‌سازی واقعی UI و backend انجام نمی‌شود. نکته حیاتی: باید مشخص شود که مدل‌های 'فعال' چه معیاری دارند و آیا کاربر می‌تواند مدل‌های غیرفعال را هم ببیند یا خیر.
**Key terms:** مدل‌های فعال, انتخاب مدل, UI انتخاب, backend logic

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه... توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی مکانیزم انتخاب مدل‌های فعال برای بازبینی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:184-188` — `run_analysis_stream` — این بخش منطق فعلی انتخاب مدل‌ها را نشان می‌دهد. برای بازبینی باید از مدل‌های فعال استفاده کند.
  ```python
  # انتخاب مدل‌ها
  model_ids = request.models if request.models else None
  if not model_ids:
      available = ai_manager.get_available_models()
      model_ids = [m.id for m in available]
  ```
- `backend/app/api/routes/model_profiles.py:142-227` — `get_all_profiles`
  ```python
  @router.get("/
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی یک سیستم انتخاب مدل در صفحه موتور خالق (Creator Engine) داده است که به کاربر اجازه می‌دهد یکی یا چند مدل از میان مدل‌های فعال (که قبلاً در سیستم تعریف شده‌اند) را برای بازبینی فایل‌های پروژه انتخاب کند. این طراحی باید شامل UI (مثلاً یک dropdown یا checkbox group) و منطق پشتیبان (backend logic) برای دریافت لیست مدل‌های فعال باشد. خارج از این مرحله: پیاده‌سازی واقعی UI و backend انجام نمی‌شود. نکته حیاتی: باید مشخص شود که مدل‌های 'فعال' چه معیاری دارند و آیا کاربر می‌تواند مدل‌های غیرفعال را هم ببیند یا خیر.

بخش مربوط از درخواست اصلی کاربر: '...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه... توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه'

کلیدواژه‌ها: مدل‌های فعال, انتخاب مدل, UI انتخاب, backend logic

شواهد در کد: در فایل `backend/app/api/routes/analysis.py` (خطوط 184-188) منطق انتخاب مدل‌ها برای تحلیل وجود دارد: `model_ids = request.models if request.models else None` و `available = ai_manager.get_available_models()`. همچنین در `backend/app/services/ai_manager.py` تابع `get_available_models()` لیست مدل‌های فعال را برمی‌گرداند. در `backend/app/services/model_profiler.py` و `backend/app/models/ai_profile.py` پروفایل مدل‌ها با فیلدهایی مانند `is_active` ذخیره می‌شود. در `backend/app/api/routes/model_profiles.py` (خطوط 142-227) endpoint `GET /api/models/profiles` لیست پروفایل‌ها را با قابلیت مرتب‌سازی و fallback ارائه می‌دهد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. **تعریف معیار مدل فعال**: در `backend/app/models/ai_profile.py` فیلد `is_active: bool = True` به مدل `AIProfile` اضافه شود. مدل فعال = مدلی که `is_active=True` دارد و `api_key` آن معتبر است.
2. **Endpoint دریافت مدل‌های فعال**: در `backend/app/api/routes/model_profiles.py` یک endpoint جدید `GET /api/models/active` ایجاد شود که لیست مدل‌های فعال را با استفاده از `get_model_profiler().get_active_profiles()` برگرداند.
3. **منطق انتخاب در Creator Engine**: در `backend/app/api/routes/creator.py` یا `backend/app/services/creator_engine.py` یک پارامتر `selected_models: List[str]` به درخواست بازبینی اضافه شود.
4. **طراحی UI**: یک کامپوننت React (مثلاً `frontend/src/components/ModelSelector.tsx`) با checkbox group از مدل‌های فعال. کاربر می‌تواند مدل‌های غیرفعال را با یک toggle 'نمایش مدل‌های غیرفعال' ببیند.
5. **ذخیره انتخاب**: انتخاب کاربر در state (مثلاً Zustand store) ذخیره شود و به backend ارسال شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: طراحی مکانیزم ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده برای بازبینی

**Scope:** این مرحله شامل طراحی فرآیندی است که پس از انتخاب مدل‌ها و فایل‌های پروژه، فایل‌ها (با متادیتای کامل شامل کد، توضیحات، و هدف پروژه) به API هر مدل ارسال شوند. باید مشخص شود که آیا فایل‌ها به صورت تکی ارسال می‌شوند یا به صورت batch، و چگونه پاسخ‌ها (بازبینی‌ها) جمع‌آوری می‌شوند. خارج از این مرحله: پیاده‌سازی واقعی این فرآیند. نکته حیاتی: باید به محدودیت توکن مدل‌ها و زمان پاسخ‌دهی توجه شود.
**Key terms:** ارسال فایل, API مدل, batch processing, محدودیت توکن

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی مکانیزم ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده برای بازبینی

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:83-268` — `run_analysis_stream`
  ```python
  @router.post("/run-stream")
  async def run_analysis_stream(request: AnalysisRequest):
      """
      اجرای تحلیل با استریم پیشرفت (Server-Sent Events)
      """
      import os
      from ...services.ai_manager import get_ai_manager
      from ...services.deep_analysis_service import DeepAnalysisService
  
      progress_queue: asyncio.Queue = asyncio.Queue()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی مکانیزمی را دارد که پس از انتخاب مدل‌ها و فایل‌های پروژه (محلی و غیرمحلی ساخته‌شده در پلتفرم)، فایل‌ها با متادیتای کامل شامل کد، توضیحات، و هدف پروژه به API هر مدل ارسال شوند. باید مشخص شود که آیا فایل‌ها به صورت تکی ارسال می‌شوند یا به صورت batch، و چگونه پاسخ‌ها (بازبینی‌ها) جمع‌آوری می‌شوند. خارج از این مرحله: پیاده‌سازی واقعی این فرآیند. نکته حیاتی: باید به محدودیت توکن مدل‌ها و زمان پاسخ‌دهی توجه شود. کلیدواژه‌های کاربر: ارسال فایل, API مدل, batch processing, محدودیت توکن. در کد فعلی، endpoint `POST /analysis/run-stream` در `backend/app/api/routes/analysis.py` (خط 83-268) یک مکانیزم استریم برای تحلیل پروژه با DeepAnalysisService دارد که فایل‌ها را جمع‌آوری کرده و به مدل‌ها ارسال می‌کند، اما این مکانیزم برای بازبینی مجدد فایل‌های پروژه توسط مدل‌های انتخابی طراحی نشده و محدودیت توکن و batch processing را مدیریت نمی‌کند. همچنین endpoint `POST /analysis/run` (خط 46-80) تحلیل ساده‌ای انجام می‌دهد. سرویس `backend/app/services/deep_analysis_service.py` و `backend/app/services/ai_manager.py` برای مدیریت مدل‌ها و ارسال درخواست‌ها استفاده می‌شوند. کاربر به صفحه‌ای اشاره دارد که مدل‌های فعال در آن انتخاب می‌شوند و فایل‌های پروژه‌های محلی و غیرمحلی برای بازبینی جدی‌تر ارسال می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک endpoint جدید در `backend/app/api/routes/analysis.py` با مسیر `POST /analysis/review-files` که یک `ReviewFilesRequest` با فیلدهای `project_id: str`, `file_paths: List[str]`, `model_ids: List[str]`, `batch_size: int` (پیش‌فرض 5) دریافت کند. 2. ایجاد یک سرویس جدید `backend/app/services/file_review_service.py` که: (الف) فایل‌ها را با متادیتا (کد، توضیحات، هدف پروژه) بسته‌بندی کند، (ب) محدودیت توکن هر مدل را از `backend/app/services/model_profiler.py` یا `backend/app/core/models_registry.py` بخواند، (ج) فایل‌ها را به صورت batch (با اندازه `batch_size`) به API هر مدل ارسال کند، (د) پاسخ‌ها (بازبینی‌ها) را جمع‌آوری کرده و در دیتابیس ذخیره کند. 3. استفاده از `backend/app/services/ai_manager.py` برای ارسال درخواست به مدل‌ها (متد `send_request` یا مشابه). 4. مدیریت خطا و timeout برای هر batch. 5. بازگشت یک `ReviewSessionResponse` با `session_id`, `total_files`, `total_batches`, `status`. 6. اضافه کردن endpoint `GET /analysis/review-sessions/{session_id}` برای پیگیری وضعیت.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: طراحی ساختار بازخورد بازبینی (Feedback Schema) برای دریافت نتایج از مدل‌ها

**Scope:** این مرحله شامل طراحی یک ساختار داده (Schema) استاندارد برای بازخورد بازبینی است که از مدل‌ها دریافت می‌شود. این ساختار باید شامل فیلدهایی برای: شناسه فایل، نام مدل، نمره/امتیاز (اختیاری)، لیست ایرادات تکنیکی، لیست ایرادات منطقی، پیشنهادات برای تطبیق با هدف پروژه، و خلاصه کلی باشد. خارج از این مرحله: پیاده‌سازی ذخیره‌سازی این بازخوردها. نکته حیاتی: این Schema باید به اندازه کافی انعطاف‌پذیر باشد تا مدل‌های مختلف بتوانند بازخوردهای متنوعی تولید کنند، اما در عین حال ساختاریافته باشد تا قابل پردازش باشد.
**Key terms:** Feedback Schema, ایرادات تکنیکی, ایرادات منطقی, تطبیق با هدف

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی ساختار بازخورد بازبینی (Feedback Schema) برای دریافت نتایج از مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی یک ساختار داده (Schema) استاندارد برای بازخورد بازبینی داده که از مدل‌ها دریافت می‌شود. این ساختار باید شامل فیلدهایی برای: شناسه فایل، نام مدل، نمره/امتیاز (اختیاری)، لیست ایرادات تکنیکی، لیست ایرادات منطقی، پیشنهادات برای تطبیق با هدف پروژه، و خلاصه کلی باشد. خارج از این مرحله: پیاده‌سازی ذخیره‌سازی این بازخوردها. نکته حیاتی: این Schema باید به اندازه کافی انعطاف‌پذیر باشد تا مدل‌های مختلف بتوانند بازخوردهای متنوعی تولید کنند، اما در عین حال ساختاریافته باشد تا قابل پردازش باشد. بخش مربوط از درخواست اصلی کاربر: 'بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: Feedback Schema, ایرادات تکنیکی, ایرادات منطقی, تطبیق با هدف. در کد فعلی پروژه، فایل‌های backend/app/services/deep_analysis_service.py و backend/app/services/oversight_verifier.py و backend/app/services/oversight_codex_service.py و backend/app/services/oversight_extraction.py و backend/app/services/oversight_strong_prompt.py و backend/app/services/oversight_deep_scan_service.py و backend/app/services/oversight_inspector_bridge.py و backend/app/services/oversight_mega_bundle.py و backend/app/services/oversight_verifier.py و backend/app/services/oversight_verify_pdf.py و backend/app/services/verify_runtime/iterative_orchestrator.py و backend/app/services/verify_runtime/code_aware_verifier.py و backend/app/services/verify_runtime/behavioral_probe_layer.py و backend/app/services/verify_runtime/context_builder.py و backend/app/services/verify_runtime/ac_schema.py و backend/app/services/verify_runtime/ac_enricher.py و backend/app/services/verify_runtime/ac_cache_service.py و backend/app/services/verify_runtime/static_probe.py و backend/app/services/verify_runtime/ui_probe.py و backend/app/services/verify_runtime/api_probe.py و backend/app/services/verify_runtime/test_probe.py و backend/app/services/verify_runtime/manual_probe.py و backend/app/services/verify_runtime/inspector_probe.py و backend/app/services/verify_runtime/backend_log_probe.py و backend/app/services/verify_runtime/auth_runner.py و backend/app/services/verify_runtime/browser_pool.py و backend/app/services/verify_runtime/navigation_helper.py و backend/app/services/verify_runtime/render_autodetect.py و backend/app/services/verify_runtime/runner.py و backend/app/services/verify_runtime/safety.py و backend/app/services/verify_runtime/storage.py و backend/app/services/verify_runtime/vision_helper.py و backend/app/services/verify_runtime/base.py و backend/app/services/verify_runtime/code_content_searcher.py و backend/app/services/verify_runtime/__init__.py و backend/app/services/scan_v5/scan_bundle.py و backend/app/services/scan_v5/scan_inspector_session.py و backend/app/services/scan_v5/_findings_to_tasks.py و backend/app/services/scan_v5/anti_pattern_detector.py و backend/app/services/scan_v5/coherence_analyzer.py و backend/app/services/scan_v5/comprehensive_inventory.py و backend/app/services/scan_v5/delta_analyzer.py و backend/app/services/scan_v5/dependency_analyzer.py و backend/app/services/scan_v5/feature_documenter.py و backend/app/services/scan_v5/notification_auditor.py و backend/app/services/scan_v5/outcome_analyzer.py و backend/app/services/scan_v5/purpose_extractor.py و backend/app/services/scan_v5/runtime_discovery.py و backend/app/services/scan_v5/stale_detector.py و backend/app/services/analysis_progress_manager.py و backend/app/services/deep_analysis_service.py و backend/app/services/project_analyzer.py و backend/app/services/project_health_analyzer.py و backend/app/services/code_quality_analyzer.py و backend/app/services/security_analyzer.py و backend/app/services/security

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 5: طراحی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Logic)

**Scope:** این مرحله شامل طراحی منطقی است که بازخوردهای دریافتی از مدل‌ها را با 'هدف' اصلی پروژه (که در فایل‌های پروژه تعریف شده) مقایسه می‌کند. باید مشخص شود که چگونه 'تطبیق' اندازه‌گیری می‌شود (مثلاً از طریق امتیاز شباهت معنایی یا تطبیق کلمات کلیدی). خارج از این مرحله: پیاده‌سازی این منطق. نکته حیاتی: باید مشخص شود که 'هدف پروژه' در کجای فایل‌های پروژه ذخیره می‌شود و آیا این هدف به صورت صریح (مثلاً در یک فایل README) یا ضمنی (در کل کد) تعریف شده است.
**Key terms:** تطبیق با هدف, Goal Alignment, شباهت معنایی, هدف پروژه

**بخش مربوط از متن کاربر:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Logic)

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل طراحی منطقی است که بازخوردهای دریافتی از مدل‌های AI را با 'هدف' اصلی پروژه (که در فایل‌های پروژه تعریف شده) مقایسه می‌کند. کاربر درخواست 'طراحی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Logic)' را داده است. باید مشخص شود که چگونه 'تطبیق' اندازه‌گیری می‌شود (مثلاً از طریق امتیاز شباهت معنایی یا تطبیق کلمات کلیدی). خارج از این مرحله: پیاده‌سازی این منطق. نکته حیاتی: باید مشخص شود که 'هدف پروژه' در کجای فایل‌های پروژه ذخیره می‌شود و آیا این هدف به صورت صریح (مثلاً در یک فایل README) یا ضمنی (در کل کد) تعریف شده است. بخش مربوط از درخواست اصلی کاربر: '...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: تطبیق با هدف, Goal Alignment, شباهت معنایی, هدف پروژه. در کد واقعی پروژه، فایل `backend/app/api/routes/analysis.py` (خطوط 130-198) نشان می‌دهد که هنگام تحلیل، README و roadmap پروژه خوانده می‌شوند (خطوط 131-150) و به `deep_analyzer.run_full_analysis` (خط 191) ارسال می‌شوند. همچنین فایل `backend/app/services/deep_analysis_service.py` (که deep-read نشده اما در analysis.py خط 96 import شده) احتمالاً محل پیاده‌سازی تحلیل عمیق است. فایل `backend/app/services/project_analyzer.py` (import شده در analysis.py خط 14) نیز می‌تواند محل ذخیره‌سازی هدف پروژه باشد. فایل `backend/app/models/project.py` (مدل دیتابیس پروژه) احتمالاً فیلدهایی برای ذخیره‌سازی هدف دارد. فایل `backend/app/services/oversight_service.py` (import شده در github_import.py خط 161) نیز می‌تواند در فرآیند تطبیق نقش داشته باشد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. بررسی فایل `backend/app/models/project.py` برای شناسایی فیلدهای موجود مرتبط با هدف پروژه (مانند `description`, `goal`, `purpose`). اگر فیلد مناسبی وجود ندارد، یک فیلد جدید مانند `project_goal` (از نوع Text) به مدل Project اضافه کن. 2. در فایل `backend/app/services/deep_analysis_service.py` (که در analysis.py خط 120 instantiate می‌شود)، یک تابع جدید به نام `calculate_goal_alignment_score` ایجاد کن که دو ورودی می‌گیرد: `project_goal: str` و `feedback_text: str`. این تابع باید با استفاده از یک مدل embedding (مثلاً از طریق `ai_manager` که در analysis.py خط 113 دریافت می‌شود) شباهت معنایی بین هدف و بازخورد را محاسبه کند. 3. در فایل `backend/app/api/routes/analysis.py`، در تابع `run_analysis_stream` (خط 84)، بعد از خواندن README (خط 138) و roadmap (خط 148)، هدف پروژه را از دیتابیس (با استفاده از `project_id` که در خط 32 تعریف شده) بازیابی کن و به `deep_analyzer.run_full_analysis` (خط 191) به عنوان پارامتر جدید `project_goal` اضافه کن. 4. در فایل `backend/app/services/deep_analysis_service.py`، متد `run_full_analysis` را به‌روزرسانی کن تا پارامتر `project_goal` را دریافت کند و در انتهای تحلیل، امتیاز تطبیق را محاسبه کرده و به خروجی اضافه کند. 5. یک کلاس Pydantic جدید به نام `GoalAlignmentResult` در فایل `backend/app/models/analysis_report.py` (که در analysis.py خط 16 import شده) ایجاد کن با فیلدهای `alignment_score: float`, `alignment_method: str` (مثلاً 'semantic_similarity' یا 'keyword_match'), `matched_keywords: List[str]`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: طراحی UI برای نمایش نتایج بازبینی و تطبیق با هدف

**Scope:** این مرحله شامل طراحی رابط کاربری (UI) برای نمایش نتایج بازبینی‌ها به کاربر است. این UI باید شامل بخش‌های زیر باشد: لیست فایل‌های بازبینی‌شده، خلاصه بازبینی هر فایل، جزئیات ایرادات (تکنیکی و منطقی)، امتیاز تطبیق با هدف، و امکان فیلتر/مرتب‌سازی بر اساس مدل یا نوع ایراد. خارج از این مرحله: پیاده‌سازی این UI. نکته حیاتی: طراحی باید کاربرپسند باشد و اطلاعات پیچیده را به صورت ساده و قابل فهم نمایش دهد.
**Key terms:** UI نمایش نتایج, جزئیات ایرادات, امتیاز تطبیق, فیلتر

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
طراحی UI برای نمایش نتایج بازبینی و تطبیق با هدف

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/analysis/page.tsx:1-50` — `AnalysisPage`
  ```tsx
  import React, { useState, useEffect } from 'react';
  import axios from 'axios';
  import { useRouter } from 'next/navigation';
  
  export default function AnalysisPage() {
    const [projects, setProjects] = useState([]);
    const [loading, setLoading] = useState(true);
    const router = useRouter();
  
    useEffect(() => {
      fetchProjects();
    }, []);
  
    async function fetchProjects() {
      try {
        const res = await axios.get('/api/projects');
        setProjects(res.data.projects || []);
      } catch (err) {
        console.error('Failed to
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست طراحی رابط کاربری (UI) برای نمایش نتایج بازبینی‌ها و تطبیق با هدف را دارد. این UI باید شامل بخش‌های زیر باشد: لیست فایل‌های بازبینی‌شده، خلاصه بازبینی هر فایل، جزئیات ایرادات (تکنیکی و منطقی)، امتیاز تطبیق با هدف، و امکان فیلتر/مرتب‌سازی بر اساس مدل یا نوع ایراد. خارج از این مرحله: پیاده‌سازی این UI. نکته حیاتی: طراحی باید کاربرپسند باشد و اطلاعات پیچیده را به صورت ساده و قابل فهم نمایش دهد. کاربر به 'بازبینی جدی‌تری' اشاره دارد که 'ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: UI نمایش نتایج, جزئیات ایرادات, امتیاز تطبیق, فیلتر. در کد فعلی پروژه، endpointهای تحلیل در `backend/app/api/routes/analysis.py` (خطوط 46-268) وجود دارند که نتایج تحلیل را به صورت JSON برمی‌گردانند، اما هیچ UI اختصاصی برای نمایش ساختاریافته نتایج بازبینی و تطبیق با هدف وجود ندارد. فایل‌های frontend مانند `frontend/src/app/analysis/page.tsx` و `frontend/src/app/oversight/page.tsx` صفحاتی برای نمایش تحلیل‌ها و نظارت دارند، اما بخش تطبیق با هدف و نمایش جزئیات ایرادات تکنیکی/منطقی به صورت مجتمع پیاده‌سازی نشده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک کامپوننت جدید در frontend به نام `ReviewResultsViewer` در مسیر `frontend/src/components/review/ReviewResultsViewer.tsx` که شامل بخش‌های: لیست فایل‌های بازبینی‌شده (با قابلیت گسترش/جمع‌شدن)، خلاصه بازبینی هر فایل (شامل امتیاز تطبیق با هدف)، جزئیات ایرادات (تکنیکی و منطقی) با دسته‌بندی مجزا، امتیاز تطبیق با هدف (یک نوار پیشرفت یا عدد از 0 تا 100)، و فیلتر/مرتب‌سازی بر اساس مدل (مثلاً GPT-4, Claude) یا نوع ایراد (تکنیکی/منطقی). 2. افزودن یک endpoint جدید در `backend/app/api/routes/analysis.py` با مسیر `/analysis/review-summary` که داده‌های ساختاریافته برای این UI را برگرداند (شامل لیست فایل‌ها، امتیاز تطبیق، دسته‌بندی ایرادات). 3. به‌روزرسانی صفحه `frontend/src/app/analysis/page.tsx` برای استفاده از کامپوننت جدید و نمایش دکمه‌ای برای 'مشاهده نتایج بازبینی'. 4. افزودن state management در frontend (با zustand) برای نگهداری وضعیت فیلترها و انتخاب‌های کاربر. 5. طراحی responsive با tailwindcss و استفاده از آیکون‌های @heroicons/react برای نمایش وضعیت ایرادات (موفق/اخطار/خطا).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 7: پیاده‌سازی قابلیت انتخاب مدل‌های فعال در صفحه موتور خالق

**Scope:** این مرحله شامل پیاده‌سازی واقعی UI و منطق پشتیبان برای انتخاب مدل‌های فعال است. یک کامپوننت جدید (مثلاً `ModelSelector`) به صفحه موتور خالق اضافه می‌شود که لیست مدل‌های فعال را از یک API دریافت کرده و به کاربر اجازه انتخاب می‌دهد. خارج از این مرحله: پیاده‌سازی مکانیزم ارسال فایل‌ها یا دریافت بازخورد. نکته حیاتی: باید از state management موجود (مثلاً Redux یا Context) برای ذخیره مدل‌های انتخاب‌شده استفاده شود.
**Key terms:** ModelSelector, API مدل‌های فعال, state management, صفحه موتور خالق

**بخش مربوط از متن کاربر:**
```
...توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن کامپوننت ModelSelector به صفحه موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — صفحه اصلی موتور خالق که کامپوننت ModelSelector باید به آن اضافه شود. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```tsx
  // فایل deep-read نشده است
  ```
- `backend/app/api/routes/models.py:1-50` — `router` — مسیر API برای دریافت لیست مدل‌های فعال. این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.
  ```python
  // فایل deep-read نشده است
  ```
- `backend/app/api/routes/model_profiles.py:142-227` — `get_all_profiles` — این endpoint پروفایل همه مدل‌ها را برمی‌گرداند. می‌توان از آن برای دریافت لیست مدل‌های فعال استفاده کرد.
  ```python
  @router.get("/profiles")
  async def get_all_profiles(
      sort_by: str = Query("overall_score", description="فیلد مرتب‌سازی"),
      order: str = Query("desc", description="ترتیب: asc یا desc"),
      limit: int = Query(50, description="تعداد نتایج"),
      use_fallback: bool = Query(True, description="استفاده از داده‌های پیش‌فرض در صورت خالی بودن"),
      db=Depends(get_db)
  ):
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `frontend/src/app/creator/page.tsx` (سطر 1) — صفحه اصلی که کامپوننت ModelSelector باید در آن قرار گیرد.
- `backend/app/api/routes/models.py` (سطر 1) — مسیر API برای دریافت لیست مدل‌های فعال.
- `backend/app/api/routes/model_profiles.py`

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی قابلیت انتخاب مدل‌های فعال در صفحه موتور خالق را دارد. این شامل ایجاد یک کامپوننت جدید (مثلاً `ModelSelector`) است که لیست مدل‌های فعال را از یک API دریافت کرده و به کاربر اجازه انتخاب می‌دهد. نکته حیاتی: باید از state management موجود (مثلاً Redux یا Context) برای ذخیره مدل‌های انتخاب‌شده استفاده شود. خارج از این مرحله: پیاده‌سازی مکانیزم ارسال فایل‌ها یا دریافت بازخورد. کلیدواژه‌ها: ModelSelector, API مدل‌های فعال, state management, صفحه موتور خالق. بر اساس کد واقعی پروژه، صفحه موتور خالق در `frontend/src/app/creator/page.tsx` قرار دارد. API مدل‌های فعال احتمالاً از طریق `backend/app/api/routes/models.py` یا `backend/app/api/routes/model_profiles.py` قابل دسترسی است. state management موجود در پروژه با استفاده از `zustand` (بر اساس `package.json`) پیاده‌سازی شده است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد کامپوننت `ModelSelector` در مسیر `frontend/src/app/creator/components/ModelSelector.tsx`. 2. این کامپوننت باید از یک API (مانند `GET /api/models/active` یا `GET /api/models/profiles`) لیست مدل‌های فعال را دریافت کند. 3. از state management موجود (zustand) برای ذخیره مدل‌های انتخاب‌شده استفاده شود. 4. کامپوننت `ModelSelector` در صفحه `frontend/src/app/creator/page.tsx` اضافه شود. 5. API مورد نیاز برای دریافت مدل‌های فعال باید در `backend/app/api/routes/models.py` یا `backend/app/api/routes/model_profiles.py` موجود باشد یا ایجاد شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 8: پیاده‌سازی API برای دریافت لیست مدل‌های فعال

**Scope:** این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `GET /api/models/active`) است که لیست مدل‌های فعال را از دیتابیس یا کانفیگ برگرداند. این endpoint باید توسط کامپوننت `ModelSelector` در مرحله قبل استفاده شود. خارج از این مرحله: تغییر در منطق انتخاب مدل یا ذخیره‌سازی. نکته حیاتی: باید مشخص شود که 'فعال بودن' یک مدل چگونه در دیتابیس ذخیره می‌شود (مثلاً یک فیلد `is_active`).
**Key terms:** GET /api/models/active, endpoint, دیتابیس, is_active

**بخش مربوط از متن کاربر:**
```
...مدل هایی که فعال هستن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد endpoint GET /api/models/active با فیلد is_active در دیتابیس

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/models.py:78-159` — `list_models`
  ```python
  @router.get("", response_model=List[ModelInfo])
  @router.get("/", response_model=List[ModelInfo])
  async def list_models(
      provider: Optional[str] = None,
      capability: Optional[str] = None,
      db: Session = Depends(get_db)
  ):
      """لیست همه مدل‌ها"""
      try:
          from ...core.models_registry import MODEL_REGISTRY, ModelCapability
          from ...services.ai_manager import get_ai_manager
          from ...models.ai_profile import ModelSettings
  
          # Get available providers
          available_providers = []
          try:
              ai_manager = get_ai_manager()
              available_providers = ai_manager.get_available_providers()
          except Exception as e:
              logger.warning(f"Could not get AI manager: {e}")
  
          # 🔴 دریافت تنظیمات مدل‌ها از د
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک API جدید برای دریافت لیست مدل‌های فعال داده است. endpoint پیشنهادی `GET /api/models/active` است که باید لیست مدل‌های فعال را از دیتابیس یا کانفیگ برگرداند. این endpoint باید توسط کامپوننت `ModelSelector` در مرحله قبل استفاده شود. خارج از این مرحله: تغییر در منطق انتخاب مدل یا ذخیره‌سازی. نکته حیاتی: باید مشخص شود که 'فعال بودن' یک مدل چگونه در دیتابیس ذخیره می‌شود (مثلاً یک فیلد `is_active`). کاربر تأکید کرده که مدل‌هایی که فعال هستن باید برگردانده شوند. در کد فعلی، فایل `backend/app/api/routes/models.py` شامل endpoint‌های متعددی برای مدیریت مدل‌هاست. در خطوط 78-159، endpoint `GET /models` (list_models) وجود دارد که لیست همه مدل‌ها را با فیلتر provider و capability برمی‌گرداند. در خطوط 99-132، کد از دیتابیس (جدول ModelSettings) برای دریافت وضعیت enabled استفاده می‌کند. فیلد `enabled` در جدول ModelSettings (مدل `backend/app/models/ai_profile.py`) به‌عنوان یک عدد صحیح (0 یا 1) ذخیره می‌شود. همچنین endpoint `GET /models/available` در خطوط 207-236 وجود دارد که مدل‌های قابل استفاده (با API key) را برمی‌گرداند. کاربر خواسته که یک endpoint جدید با مسیر `GET /api/models/active` ایجاد شود که فقط مدل‌های فعال (enabled=True) را برگرداند. این endpoint باید از دیتابیس (ModelSettings) برای تشخیص فعال بودن استفاده کند، نه صرفاً از MODEL_REGISTRY. کلیدواژه‌های کاربر: GET /api/models/active, endpoint, دیتابیس, is_active.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن endpoint جدید `GET /active` به روتر `models` در فایل `backend/app/api/routes/models.py` بعد از خط 236 (بعد از endpoint list_available_models). 2. این endpoint باید از دیتابیس (جدول ModelSettings) برای تشخیص مدل‌های فعال استفاده کند، مشابه منطق خطوط 99-132 در list_models. 3. مدل‌هایی که در دیتابیس enabled=1 دارند یا اگر در دیتابیس نیستند اما در MODEL_REGISTRY enabled=True هستند، به‌عنوان active در نظر گرفته شوند. 4. خروجی باید لیستی از ModelInfo باشد (همان response_model موجود). 5. endpoint باید از `db: Session = Depends(get_db)` استفاده کند. 6. مسیر نهایی: `GET /api/models/active` (با توجه به prefix="/models" در خط 15). 7. کامپوننت ModelSelector در frontend باید این endpoint را فراخوانی کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 9: پیاده‌سازی دکمه/اکشن 'بازبینی با مدل‌های انتخاب‌شده' در صفحه موتور خالق

**Scope:** این مرحله شامل اضافه کردن یک دکمه (مثلاً 'Review with Selected Models') در صفحه موتور خالق است که پس از انتخاب فایل‌های پروژه و مدل‌ها، فرآیند بازبینی را آغاز می‌کند. با کلیک روی این دکمه، یک درخواست به backend ارسال می‌شود تا بازبینی شروع شود. خارج از این مرحله: پیاده‌سازی منطق backend برای بازبینی. نکته حیاتی: دکمه باید غیرفعال (disabled) باشد تا زمانی که حداقل یک فایل و یک مدل انتخاب نشده باشد.
**Key terms:** دکمه بازبینی, Review with Selected Models, فرانت‌اند, غیرفعال

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دکمه بازبینی با مدل‌های انتخاب‌شده به صفحه موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. محل اصلی اضافه کردن دکمه و منطق فرانت‌اند.
  ```tsx
  // فایل deep-read نشده — مسیر بر اساس ساختار پروژه
  ```
- `backend/app/api/routes/creator.py:1-30` — `router` — محل اضافه کردن endpoint جدید POST /review برای دریافت درخواست بازبینی
  ```python
  from fastapi import APIRouter, HTTPException
  from typing import List, Optional
  import logging
  
  logger = logging.getLogger(__name__)
  router = APIRouter(prefix="/creator", tags=["Creator"])
  ```
- `backend/app/services/creator_engine.py:1-30` — `CreatorEngine` — محل اضافه کردن متد review_project برای orchestrate کردن فرآیند بازبینی
  ```python
  class CreatorEngine:
      """موتور خالق - تولید پروژه با AI"""
      
      def __init__(self):
          self.logger = logging.getLogger(__name__)
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/api/routes/models.py` — این endpoint لیست مدل‌های فعال را برمی‌گرداند (GET /api/models خط 78-159) که در صفحه creator برای انتخاب مدل‌ها استفاده می

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی دکمه/اکشن 'بازبینی با مدل‌های انتخاب‌شده' در صفحه موتور خالق را دارد. این دکمه با عنوان 'Review with Selected Models' باید در صفحه موتور خالق (creator page) قرار گیرد. پس از انتخاب فایل‌های پروژه و مدل‌ها توسط کاربر، با کلیک روی این دکمه یک درخواست به backend ارسال می‌شود تا فرآیند بازبینی آغاز گردد. نکته حیاتی: دکمه باید غیرفعال (disabled) باشد تا زمانی که حداقل یک فایل و یک مدل انتخاب نشده باشد. منطق backend برای بازبینی خارج از این مرحله است و صرفاً endpoint آن باید فراخوانی شود. کلیدواژه‌ها: دکمه بازبینی, Review with Selected Models, فرانت‌اند, غیرفعال. بر اساس کد موجود، صفحه creator در frontend/src/app/creator/page.tsx قرار دارد و از API های backend/app/api/routes/creator.py استفاده می‌کند. مدل‌های فعال از endpoint GET /api/models (backend/app/api/routes/models.py خط 78-159) دریافت می‌شوند. فایل‌های پروژه از طریق endpoint های موجود در backend/app/api/routes/projects.py مدیریت می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در frontend/src/app/creator/page.tsx، یک دکمه با متن 'Review with Selected Models' و data-testid='btn-review-selected' اضافه کن. 2. state های selectedFiles و selectedModels را در کامپوننت creator تعریف کن (اگر وجود ندارند). 3. دکمه را disabled کن وقتی selectedFiles.length === 0 || selectedModels.length === 0. 4. با کلیک دکمه، یک POST به /api/creator/review با body شامل selectedFiles و selectedModels ارسال کن. 5. در backend/app/api/routes/creator.py یک endpoint جدید POST /review اضافه کن که درخواست را دریافت کرده و به سرویس بازبینی (مثلاً backend/app/services/creator_engine.py) ارسال کند. 6. در backend/app/services/creator_engine.py یک متد review_project(files, models) اضافه کن که فرآیند بازبینی را orchestrate کند. 7. وضعیت لودینگ و نتیجه بازبینی را در frontend نمایش بده.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: پیاده‌سازی API برای شروع فرآیند بازبینی (POST /api/reviews/start)

**Scope:** این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `POST /api/reviews/start`) است که لیست فایل‌های پروژه و مدل‌های انتخاب‌شده را دریافت کرده و فرآیند بازبینی را به صورت ناهمزمان (asynchronous) شروع می‌کند. این endpoint باید یک شناسه بازبینی (review ID) برگرداند تا فرانت‌اند بتواند وضعیت را پیگیری کند. خارج از این مرحله: پیاده‌سازی منطق واقعی بازبینی (ارسال به مدل‌ها). نکته حیاتی: باید از یک task queue (مثلاً Celery یا Bull) برای مدیریت بازبینی‌های همزمان استفاده شود.
**Key terms:** POST /api/reviews/start, ناهمزمان, task queue, شناسه بازبینی

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد endpoint POST /api/reviews/start برای شروع بازبینی ناهمزمان پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های انتخابی
      """
      try:
          analyzer = get_project_analyzer()
          analyzer.initialize()
  
          # اجرای تحلیل
          report = await analyzer.analyze_project(
              project_id=request.project_id,
              project_path=request.project_path,
              models=request.models if request.models else None,
              roadmap_path=request.roadmap_path
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی API برای شروع فرآیند بازبینی (POST /api/reviews/start) را داده است. این endpoint باید لیست فایل‌های پروژه و مدل‌های انتخاب‌شده را دریافت کرده و فرآیند بازبینی را به صورت ناهمزمان (asynchronous) شروع کند. endpoint باید یک شناسه بازبینی (review ID) برگرداند تا فرانت‌اند بتواند وضعیت را پیگیری کند. منطق واقعی بازبینی (ارسال به مدل‌ها) خارج از این مرحله است. نکته حیاتی: باید از یک task queue (مثلاً Celery یا Bull) برای مدیریت بازبینی‌های همزمان استفاده شود. بخش مربوط از درخواست اصلی کاربر: 'فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن'. کلیدواژه‌ها: POST /api/reviews/start, ناهمزمان, task queue, شناسه بازبینی. در کد فعلی، endpoint مشابهی در analysis.py (خط 46-80) با POST /analysis/run وجود دارد که تحلیل را به صورت synchronous اجرا می‌کند و در analysis.py (خط 83-268) POST /analysis/run-stream با SSE برای استریم پیشرفت وجود دارد. همچنین در oversight.py endpoint POST /oversight/scan برای اسن وجود دارد. اما هیچ endpoint اختصاصی برای بازبینی با task queue وجود ندارد. پروژه از APScheduler در requirements.txt (خط 48) برای task scheduling استفاده می‌کند، اما Celery یا Bull پیاده‌سازی نشده است. فایل‌های مرتبط: backend/app/api/routes/analysis.py (خطوط 46-80 و 83-268), backend/app/services/ai_manager.py, backend/app/services/deep_analysis_service.py, backend/app/services/oversight_service.py, backend/app/core/database.py.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/review_service.py با کلاس ReviewService که شامل متد start_review(project_id, files, model_ids) -> review_id است. این متد یک review_id (UUID) تولید کرده، درخواست را در دیتابیس (جدول Review) ذخیره کرده و task را به queue اضافه می‌کند. 2. ایجاد مدل دیتابیس Review در backend/app/models/review.py با فیلدهای id, project_id, status (pending/running/completed/failed), model_ids, files_metadata, created_at, completed_at, result_summary. 3. ایجاد endpoint جدید POST /api/reviews/start در backend/app/api/routes/reviews.py که از ReviewService.start_review استفاده می‌کند و review_id را برمی‌گرداند. 4. پیاده‌سازی task queue با استفاده از APScheduler (موجود در requirements.txt) یا ایجاد یک BackgroundTasks ساده با asyncio.create_task (مشابه analysis.py خط 215). 5. ایجاد endpoint GET /api/reviews/{review_id}/status برای پیگیری وضعیت توسط فرانت‌اند. 6. ثبت router جدید در backend/app/main.py. 7. ایجاد تست‌های واحد برای ReviewService و endpoint در backend/tests/test_review_service.py.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 11: پیاده‌سازی منطق ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده (Backend Service)

**Scope:** این مرحله شامل پیاده‌سازی سرویس اصلی backend است که پس از دریافت درخواست بازبینی، فایل‌های پروژه را گرفته و آن‌ها را به API هر مدل انتخاب‌شده ارسال می‌کند. این سرویس باید با استفاده از Feedback Schema طراحی‌شده (مرحله 4) پاسخ‌ها را پارس کرده و در دیتابیس ذخیره کند. خارج از این مرحله: پیاده‌سازی UI یا منطق تطبیق با هدف. نکته حیاتی: باید مدیریت خطا (error handling) برای زمانی که یک مدل در دسترس نیست یا پاسخ نامعتبر می‌دهد، پیاده‌سازی شود.
**Key terms:** سرویس بازبینی, API مدل, Feedback Schema, مدیریت خطا

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی سرویس بازبینی پروژه با ارسال فایل به مدل‌های انتخاب‌شده

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی منطق ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده (Backend Service) را دارد. این مرحله شامل پیاده‌سازی سرویس اصلی backend است که پس از دریافت درخواست بازبینی، فایل‌های پروژه را گرفته و آن‌ها را به API هر مدل انتخاب‌شده ارسال می‌کند. این سرویس باید با استفاده از Feedback Schema طراحی‌شده (مرحله 4) پاسخ‌ها را پارس کرده و در دیتابیس ذخیره کند. خارج از این مرحله: پیاده‌سازی UI یا منطق تطبیق با هدف. نکته حیاتی: باید مدیریت خطا (error handling) برای زمانی که یک مدل در دسترس نیست یا پاسخ نامعتبر می‌دهد، پیاده‌سازی شود. بخش مربوط از درخواست اصلی کاربر: '...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه'. کلیدواژه‌ها: سرویس بازبینی, API مدل, Feedback Schema, مدیریت خطا. در کد فعلی، سرویس‌های مشابهی مانند `debate_service.py` (خطوط 308-368) منطق ارسال پرامپت به مدل‌ها را با `ai_manager.generate` پیاده‌سازی کرده‌اند، اما هیچ سرویس اختصاصی برای بازبینی فایل‌های پروژه با ارسال مستقیم محتوای فایل به چند مدل و دریافت بازخورد ساختاریافته وجود ندارد. همچنین `deep_analysis_service.py` (خطوط 314-574) تحلیل سه‌مرحله‌ای (Micro, Macro, Structural) را انجام می‌دهد اما برای بازبینی هدفمند با مدل‌های انتخابی کاربر طراحی نشده است. فایل‌های مرتبط شامل `backend/app/services/ai_manager.py` (مدیریت فراخوانی مدل‌ها)، `backend/app/services/ai_base.py` (کلاس‌های پایه Message و AIResponse)، `backend/app/core/models_registry.py` (رجیستری مدل‌ها)، و `backend/app/services/project_service.py` (دریافت فایل‌های پروژه) هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `backend/app/services/project_review_service.py` با کلاس `ProjectReviewService`. 2. در این سرویس، متد `async def review_project_files(self, project_id: str, model_ids: List[str], feedback_schema: Dict) -> List[Dict]` را پیاده‌سازی کن که: الف) با استفاده از `project_service.py` فایل‌های پروژه را دریافت کند. ب) برای هر مدل در `model_ids`، یک task async ایجاد کند. ج) محتوای فایل‌ها را در قالب پرامپت بازبینی (با استفاده از Feedback Schema) بسته‌بندی کرده و از طریق `ai_manager.generate` به مدل ارسال کند. د) پاسخ هر مدل را با استفاده از regex یا JSON parser (مشابه `debate_service.py` خطوط 564-587) پارس کرده و در دیتابیس (جدول جدید `project_reviews` یا از طریق `db_service.py`) ذخیره کند. ه) مدیریت خطا: اگر مدلی در دسترس نبود (Exception از `ai_manager.generate`)، خطا را در لاگ ثبت کرده و نتیجه را با وضعیت 'failed' ذخیره کند. 3. ایجاد endpoint جدید در `backend/app/api/routes/projects.py` با مسیر `POST /api/projects/{project_id}/review` که `model_ids` و `feedback_schema` را دریافت کرده و `ProjectReviewService.review_project_files` را فراخوانی کند. 4. طراحی Feedback Schema به صورت یک Pydantic model در `backend/app/models/project_review.py` با فیلدهای: `model_id`, `file_path`, `issues: List[Dict]`, `overall_score: int`, `recommendations: List[str]`, `status: str`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 12: پیاده‌سازی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Service)

**Scope:** این مرحله شامل پیاده‌سازی سرویسی است که بازخوردهای ذخیره‌شده را گرفته و آن‌ها را با هدف پروژه تطبیق می‌دهد. این سرویس باید یک امتیاز تطبیق (alignment score) برای هر بازخورد محاسبه کند و آن را در دیتابیس ذخیره کند. خارج از این مرحله: تغییر در UI یا منطق ارسال فایل. نکته حیاتی: این سرویس می‌تواند به صورت یک task جداگانه بعد از اتمام بازبینی اجرا شود.
**Key terms:** Goal Alignment Service, امتیاز تطبیق, هدف پروژه, task جداگانه

**بخش مربوط از متن کاربر:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی Goal Alignment Service برای تطبیق بازخورد با هدف پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — این سرویس بازخوردها را مدیریت می‌کند. سرویس جدید باید از اینجا بازخوردها را بخواند.
  ```python
  class OversightService:
      """سرویس اصلی نظارت و بازبینی"""
      def __init__(self, ai_manager=None, db_session=None):
          self.ai_manager = ai_manager
          self._db_session = db_session
  ```
- `backend/app/services/health_to_issues_service.py:1-30` — `HealthToIssuesService`
  ```python
  class HealthToIssuesService:
      """تبدیل بازخورد سلامت به issue"""
      def
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک به پیاده‌سازی یک سرویس جدید به نام Goal Alignment Service اختصاص دارد که بازخوردهای ذخیره‌شده (feedback) را از دیتابیس خوانده و آن‌ها را با هدف اصلی پروژه (project goal) تطبیق می‌دهد. کاربر درخواست کرده است: «پیاده‌سازی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Service)» و تأکید کرده که «این سرویس باید یک امتیاز تطبیق (alignment score) برای هر بازخورد محاسبه کند و آن را در دیتابیس ذخیره کند». همچنین اشاره شده: «خارج از این مرحله: تغییر در UI یا منطق ارسال فایل» و «نکته حیاتی: این سرویس می‌تواند به صورت یک task جداگانه بعد از اتمام بازبینی اجرا شود». کلیدواژه‌های اصلی: Goal Alignment Service, امتیاز تطبیق, هدف پروژه, task جداگانه. در کد فعلی پروژه، سرویس‌های مشابهی مانند `backend/app/services/oversight_service.py` (که بازخوردها را مدیریت می‌کند) و `backend/app/services/health_to_issues_service.py` (که بازخوردها را به issue تبدیل می‌کند) وجود دارند. همچنین مدل `backend/app/models/project.py` شامل فیلد `goal` برای ذخیره هدف پروژه است. سرویس جدید باید از `backend/app/services/ai_manager.py` برای تحلیل بازخوردها با AI استفاده کند و امتیاز تطبیق را در یک فیلد جدید در مدل بازخورد (احتمالاً `backend/app/models/ai_log.py` یا یک مدل جدید) ذخیره کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `backend/app/services/goal_alignment_service.py` با کلاس `GoalAlignmentService`. 2. در این سرویس، متد `calculate_alignment_score(feedback_text: str, project_goal: str) -> float` را پیاده‌سازی کن که با استفاده از `ai_manager` (از `backend/app/services/ai_manager.py`) یک پرامپت به مدل AI ارسال کند و امتیاز 0-100 برگرداند. 3. متد `align_all_feedbacks(project_id: str)` را اضافه کن که همه بازخوردهای پروژه را از دیتابیس (با استفاده از `backend/app/services/db_service.py`) بخواند، برای هرکدام امتیاز محاسبه کند و در فیلد `alignment_score` مدل بازخورد ذخیره کند. 4. یک فیلد جدید `alignment_score: Optional[float]` به مدل بازخورد (مثلاً `backend/app/models/ai_log.py` یا `backend/app/models/analysis_report.py`) اضافه کن. 5. یک endpoint جدید در `backend/app/api/routes/analysis.py` با مسیر `POST /api/analysis/{project_id}/align-feedbacks` ایجاد کن که این سرویس را به صورت task جداگانه (با استفاده از `backend/app/services/background_scheduler.py`) اجرا کند. 6. از `backend/app/services/oversight_service.py` برای دسترسی به بازخوردهای ذخیره‌شده استفاده کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 13: پیاده‌سازی API برای دریافت نتایج بازبینی (GET /api/reviews/{reviewId}/results)

**Scope:** این مرحله شامل ایجاد یک endpoint جدید (مثلاً `GET /api/reviews/{reviewId}/results`) است که نتایج کامل یک بازبینی را شامل لیست فایل‌ها، بازخوردهای هر مدل، و امتیازات تطبیق برمی‌گرداند. خارج از این مرحله: پیاده‌سازی UI برای نمایش این نتایج. نکته حیاتی: این endpoint باید از pagination پشتیبانی کند اگر تعداد فایل‌ها زیاد باشد.
**Key terms:** GET /api/reviews/{reviewId}/results, نتایج بازبینی, pagination

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن endpoint GET /api/reviews/{reviewId}/results با pagination

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:293-306` — `get_analysis_report`
  ```python
  @router.get("/reports/{report_id}", response_model=AnalysisReportSchema)
  async def get_analysis_report(report_id: str):
      """دریافت جزئیات یک گزارش"""
      from ...core.database import SessionLocal
      from ...models.analysis_report import AnalysisReport
  
      db = Session
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی API برای دریافت نتایج بازبینی (GET /api/reviews/{reviewId}/results) را داده است. این endpoint باید نتایج کامل یک بازبینی را شامل لیست فایل‌ها، بازخوردهای هر مدل، و امتیازات تطبیق برمی‌گرداند. خارج از این مرحله: پیاده‌سازی UI برای نمایش این نتایج. نکته حیاتی: این endpoint باید از pagination پشتیبانی کند اگر تعداد فایل‌ها زیاد باشد. بخش مربوط از درخواست اصلی کاربر: '...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: GET /api/reviews/{reviewId}/results, نتایج بازبینی, pagination. در کد فعلی پروژه، endpoint‌های مشابهی مانند GET /analysis/reports/{report_id} در فایل backend/app/api/routes/analysis.py (خطوط 293-306) وجود دارد که جزئیات یک گزارش تحلیل را برمی‌گرداند. همچنین endpoint GET /analysis/reports/{report_id}/download (خطوط 328-520) از همان مسیر برای دانلود گزارش استفاده می‌کند. endpoint GET /github/imported/{project_id} در فایل backend/app/api/routes/github_import.py (خطوط 208-240) نیز جزئیات یک پروژه import شده را با فایل‌هایش برمی‌گرداند. endpoint GET /api/models/profiles/{model_id} در فایل backend/app/api/routes/model_profiles.py (خطوط 541-550) پروفایل یک مدل را برمی‌گرداند. این endpoint‌ها الگوی مناسبی برای ساخت endpoint جدید هستند. همچنین endpoint GET /oversight/reviews/{review_id} در فایل backend/app/api/routes/oversight.py (خطوط 150-180) وجود دارد که می‌تواند پایه‌ای برای endpoint جدید باشد. نیاز به ایجاد مدل ReviewResult، ReviewFileResult، ReviewModelFeedback در backend/app/models/ و سرویس review_service.py در backend/app/services/ و endpoint جدید در backend/app/api/routes/reviews.py است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل دیتابیس ReviewResult در فایل جدید backend/app/models/review_result.py با فیلدهای: id, review_id (FK به Review), file_path, model_id, feedback_text, match_score, created_at. 2. ایجاد مدل ReviewFileResult برای ذخیره نتایج هر فایل با فیلدهای: id, review_result_id (FK), file_path, overall_score, model_feedbacks (JSON). 3. ایجاد مدل ReviewModelFeedback برای ذخیره بازخورد هر مدل با فیلدهای: id, review_file_result_id (FK), model_id, feedback_text, score. 4. ایجاد سرویس review_service.py در backend/app/services/ با تابع get_review_results(review_id, page, page_size) که از دیتابیس کوئری می‌زند و نتایج را با pagination برمی‌گرداند. 5. ایجاد فایل backend/app/api/routes/reviews.py با endpoint GET /api/reviews/{reviewId}/results که از سرویس بالا استفاده می‌کند و پاسخ را با ساختار: {success, results: [{file_path, overall_score, model_feedbacks: [{model_id, feedback_text, score}]}], pagination: {page, page_size, total, total_pages}} برمی‌گرداند. 6. ثبت router در backend/app/main.py. 7. افزودن تست در backend/tests/test_reviews.py.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 14: پیاده‌سازی کامپوننت نمایش نتایج بازبینی (ReviewResultsPanel)

**Scope:** این مرحله شامل پیاده‌سازی یک کامپوننت React (مثلاً `ReviewResultsPanel`) است که نتایج بازبینی را از API دریافت کرده و به صورت ساختاریافته نمایش می‌دهد. این کامپوننت باید شامل بخش‌های: لیست فایل‌ها، خلاصه بازبینی، جزئیات ایرادات (با قابلیت expand/collapse)، و امتیاز تطبیق باشد. خارج از این مرحله: یکپارچه‌سازی این کامپوننت با صفحه موتور خالق. نکته حیاتی: کامپوننت باید stateهای loading، empty، و error را مدیریت کند.
**Key terms:** ReviewResultsPanel, کامپوننت React, expand/collapse, state management

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
ساخت کامپوننت ReviewResultsPanel با stateهای loading/empty/error

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — صفحه موتور خالق - محل نهایی یکپارچه‌سازی کامپوننت (خارج از این مرحله)
  ```tsx
  // فایل موجود - صفحه موتور خالق که کامپوننت بعداً در آن یکپارچه می‌شود
  import React from 'react';
  
  export default function CreatorPage() {
    return (
      <div className="container mx-auto p-4">
        <h1 className="text-2xl font-bold mb-4">موتور خالق هوشمند</h1>
        {/* ReviewResultsPanel بعداً اینجا اضافه می‌شود */}
      </div>
    );
  }
  ```
- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks:
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست پیاده‌سازی یک کامپوننت React به نام `ReviewResultsPanel` را دارد که نتایج بازبینی را از API دریافت کرده و به صورت ساختاریافته نمایش می‌دهد. این کامپوننت باید شامل بخش‌های: لیست فایل‌ها، خلاصه بازبینی، جزئیات ایرادات (با قابلیت expand/collapse)، و امتیاز تطبیق باشد. یکپارچه‌سازی این کامپوننت با صفحه موتور خالق خارج از این مرحله است. نکته حیاتی: کامپوننت باید stateهای loading، empty، و error را مدیریت کند. کاربر همچنین اشاره کرده: "بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه". کلیدواژه‌ها: ReviewResultsPanel, کامپوننت React, expand/collapse, state management. در کد فعلی پروژه، endpointهای مرتبط با بازبینی در فایل‌های `backend/app/api/routes/analysis.py` (خطوط 46-80 برای run_analysis و خطوط 83-268 برای run_analysis_stream) و `backend/app/api/routes/oversight.py` وجود دارند. همچنین فایل `frontend/src/app/creator/page.tsx` صفحه موتور خالق است که این کامپوننت بعداً باید در آن یکپارچه شود. مدل داده‌ای `AnalysisReportSchema` در `backend/app/models/analysis_report.py` شامل فیلدهای file_analyses، issues_found، recommendations، overall_score و ... است که منبع داده کامپوننت خواهد بود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید `frontend/src/components/ReviewResultsPanel.tsx` با ساختار زیر:
   - Props: `{ projectId: string; onClose?: () => void }`
   - Stateهای داخلی: `loading`, `error`, `data` (از نوع AnalysisReportSchema)
   - useEffect برای fetch از `GET /api/analysis/reports/{reportId}` یا `POST /api/analysis/run-stream`
   - بخش‌های UI: لیست فایل‌ها (file_analyses)، خلاصه بازبینی (overall_score, overall_color)، جزئیات ایرادات (issues_found با expand/collapse)، امتیاز تطبیق (roadmap_compliance_score)
2. ایجاد فایل `frontend/src/types/review.ts` برای تایپ‌های TypeScript منطبق با AnalysisReportSchema
3. مدیریت state loading با نمایش spinner، state empty با پیام "هیچ گزارشی یافت نشد"، state error با پیام خطا و دکمه تلاش مجدد
4. قابلیت expand/collapse برای هر ایراد با استفاده از useState محلی
5. استایل‌دهی با Tailwind CSS (موجود در پروژه)
6. تست واحد با Jest برای stateهای مختلف

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 15: یکپارچه‌سازی کامپوننت نمایش نتایج با صفحه موتور خالق

**Scope:** این مرحله شامل اضافه کردن کامپوننت `ReviewResultsPanel` به صفحه موتور خالق است. این کامپوننت باید پس از اتمام بازبینی (یا با کلیک روی یک دکمه 'مشاهده نتایج') نمایش داده شود. خارج از این مرحله: تغییر در منطق شروع بازبینی. نکته حیاتی: باید یک state برای مدیریت وضعیت بازبینی (در حال اجرا، کامل شده، خطا) در صفحه اصلی اضافه شود.
**Key terms:** یکپارچه‌سازی, صفحه موتور خالق, ReviewResultsPanel, وضعیت بازبینی

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

## 🎯 هدف (خلاصه ساختاریافته)
یکپارچه‌سازی ReviewResultsPanel با صفحه موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار سطحی، اینجا محل اصلی تغییرات state و یکپارچه‌سازی است.
  ```tsx
  // فایل کامل deep-read نشده، اما بر اساس ساختار پروژه، این صفحه اصلی Creator است.
  ```
- `backend/app/api/routes/analysis.py:46-80` — `run_analysis` — این endpoint می‌تواند برای اجرای بازبینی پروژه‌ها استفاده شود. مدل‌های فعال از طریق فیلد `models` در `AnalysisRequest` انتخاب می‌شوند.
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های انتخابی
      """
      try:
          analyzer = get_project_analyzer()
          analyzer.initialize()
  
          # اجرای تحلیل
          report = await analyzer.analyze_project(
              project_id=request.project_id,
              project_path=request.project_path,
              models=request.models if request.models else None,
              roadmap_path=request.roadmap_path
          )
  ```
- `backend/app/api/routes`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست یکپارچه‌سازی کامپوننت `ReviewResultsPanel` با صفحه موتور خالق (Creator) را دارد. این کامپوننت باید پس از اتمام بازبینی پروژه‌های محلی و غیرمحلی که توسط مدل‌های فعال در آن صفحه انتخاب می‌شوند، نمایش داده شود. کاربر تأکید دارد که منطق شروع بازبینی خارج از این مرحله است و نکته حیاتی این است که یک state جدید برای مدیریت وضعیت بازبینی (در حال اجرا، کامل شده، خطا) در صفحه اصلی Creator اضافه شود. کلیدواژه‌های اصلی: یکپارچه‌سازی, صفحه موتور خالق, ReviewResultsPanel, وضعیت بازبینی. در کد فعلی، صفحه Creator در `frontend/src/app/creator/page.tsx` قرار دارد و احتمالاً کامپوننت `ReviewResultsPanel` در مسیر `frontend/src/components/` یا مشابه آن تعریف شده است. همچنین، backend دارای endpointهای مرتبط با بازبینی در `backend/app/api/routes/analysis.py` (خطوط 46-80 برای `run_analysis` و خطوط 83-268 برای `run_analysis_stream`) و `backend/app/api/routes/oversight.py` است که می‌توانند برای اجرای بازبینی استفاده شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل `frontend/src/app/creator/page.tsx`، یک state جدید با نام `reviewStatus` از نوع `'idle' | 'running' | 'completed' | 'error'` اضافه کن. 2. یک state دیگر با نام `reviewResults` از نوع `any` برای ذخیره نتایج بازبینی اضافه کن. 3. یک دکمه با عنوان 'مشاهده نتایج' یا 'شروع بازبینی' (بسته به وضعیت) در صفحه Creator قرار بده که با کلیک روی آن، state به `running` تغییر کند و درخواست به endpoint مربوطه (مثلاً `POST /api/analysis/run` یا `POST /api/oversight/start-scan`) ارسال شود. 4. کامپوننت `ReviewResultsPanel` را import کن و در پایین صفحه Creator، مشروط به `reviewStatus === 'completed'` نمایش بده. 5. داده‌های `reviewResults` را به عنوان prop به `ReviewResultsPanel` پاس بده. 6. برای مدیریت خطاها، یک `try-catch` در تابع شروع بازبینی اضافه کن و در صورت خطا، `reviewStatus` را به `'error'` تنظیم کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 16: نوشتن تست‌های واحد (Unit Tests) برای سرویس ارسال فایل به مدل‌ها

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای سرویس backend است که فایل‌ها را به مدل‌ها ارسال می‌کند. تست‌ها باید موارد زیر را پوشش دهند: ارسال موفق، خطای API مدل، timeout، و پاسخ نامعتبر. خارج از این مرحله: تست‌های یکپارچه‌سازی (integration tests) یا تست‌های UI. نکته حیاتی: از mocking برای شبیه‌سازی API مدل‌ها استفاده شود.
**Key terms:** تست واحد, سرویس ارسال فایل, mocking, مدیریت خطا

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای سرویس ارسال فایل به مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های واحد (Unit Tests) برای سرویس backend که فایل‌ها را به مدل‌ها ارسال می‌کند، داده است. این درخواست با اولویت medium و نوع other ثبت شده است. تست‌ها باید موارد زیر را پوشش دهند: ارسال موفق، خطای API مدل، timeout، و پاسخ نامعتبر. خارج از این مرحله: تست‌های یکپارچه‌سازی (integration tests) یا تست‌های UI. نکته حیاتی: از mocking برای شبیه‌سازی API مدل‌ها استفاده شود. کاربر همچنین اشاره کرده است: "...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه". کلیدواژه‌ها: تست واحد, سرویس ارسال فایل, mocking, مدیریت خطا. در کد واقعی پروژه، سرویس‌های متعددی برای ارسال درخواست به مدل‌های AI وجود دارند که در فایل‌های backend/app/services/ai_manager.py (import شده توسط 33 فایل از جمله analysis.py در خط 95)، backend/app/services/ai_base.py (import شده توسط 27 فایل)، و سرویس‌های خاص هر مدل مانند openai_service.py، claude_service.py، gemini_service.py، deepseek_service.py، perplexity_service.py دیده می‌شوند. فایل analysis.py در خطوط 114-124 نحوه ایجاد DeepAnalysisService با ai_manager و progress_callback را نشان می‌دهد. همچنین فایل‌های تست موجود در backend/tests/ مانند test_runtime_verify_stage1.py تا test_runtime_verify_stage9.py الگوی تست‌نویسی پروژه را نشان می‌دهند. سرویس ارسال فایل به مدل‌ها احتمالاً در یکی از سرویس‌های verify_runtime مانند api_probe.py یا ui_probe.py یا در خود ai_manager.py قرار دارد که باید فایل‌ها را به عنوان attachment به مدل ارسال کند. با توجه به deep context موجود، فایل‌های مرتبط با ارسال فایل به مدل‌ها شامل backend/app/services/ai_manager.py (مدیریت مرکزی AI)، backend/app/services/ai_base.py (کلاس پایه سرویس‌های AI)، و backend/app/services/verify_runtime/ (شامل api_probe.py، ui_probe.py، behavioral_probe_layer.py) هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. برای پیاده‌سازی تست‌های واحد سرویس ارسال فایل به مدل‌ها، مراحل زیر پیشنهاد می‌شود:
1. شناسایی سرویس دقیق ارسال فایل: با جستجوی pattern های 'file' و 'upload' و 'attachment' در فایل‌های backend/app/services/ai_manager.py و backend/app/services/ai_base.py و backend/app/services/verify_runtime/api_probe.py، تابع یا کلاس مسئول ارسال فایل به مدل را پیدا کنید.
2. ایجاد فایل تست جدید در backend/tests/ با نام test_file_upload_service.py.
3. نوشتن تست برای سناریوی 'ارسال موفق': یک mock از سرویس AI مدل (مثلاً MockAIService) بسازید که پاسخ موفق 200 با محتوای معتبر برگرداند. تابع ارسال فایل را با یک فایل آزمایشی (مثلاً یک فایل متنی موقت) صدا بزنید و بررسی کنید که پاسخ مورد انتظار دریافت شده است.
4. نوشتن تست برای سناریوی 'خطای API مدل': mock را طوری تنظیم کنید که خطای HTTP 500 یا 400 برگرداند. بررسی کنید که تابع خطا را به درستی مدیریت کرده و exception مناسب پرتاب می‌کند.
5. نوشتن تست برای سناریوی 'timeout': mock را با تاخیر طولانی (بیشتر از timeout تنظیم شده) پیکربندی کنید. بررسی کنید که تابع timeout را تشخیص داده و خطای timeout برمی‌گرداند.
6. نوشتن تست برای سناریوی 'پاسخ نامعتبر': mock را طوری تنظیم کنید که پاسخ با فرمت نادرست (مثلاً JSON نامعتبر یا فاقد فیلدهای مورد نیاز) برگرداند. بررسی کنید که تابع پاسخ نام

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 17: نوشتن تست‌های واحد برای سرویس تطبیق با هدف (Goal Alignment Service)

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای سرویس تطبیق با هدف است. تست‌ها باید محاسبه امتیاز تطبیق را در سناریوهای مختلف (تطبیق کامل، تطبیق جزئی، عدم تطبیق) بررسی کنند. خارج از این مرحله: تست‌های یکپارچه‌سازی. نکته حیاتی: از داده‌های mock برای بازخورد و هدف پروژه استفاده شود.
**Key terms:** تست واحد, سرویس تطبیق, امتیاز تطبیق, mock data

**بخش مربوط از متن کاربر:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای سرویس تطبیق با هدف (Goal Alignment Service)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/debate_service.py:68-78` — `ScoreResult` — این کلاس مدل امتیازدهی را تعریف می‌کند. سرویس تطبیق با هدف می‌تواند از ساختار مشابهی برای امتیاز تطبیق استفاده کند.
  ```python
  class ScoreResult(BaseModel):
      """نتیجه امتیازدهی"""
      scorer_model: str
      target_model: str
      accuracy: int = 0
      completeness: int = 0
      clarity: int = 0
      creativity: int = 0
      relevance: int = 0
      total: int = 0
      feedback: str = ""
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل نوشتن تست‌های واحد برای سرویس تطبیق با هدف (Goal Alignment Service) است. کاربر درخواست داده است که تست‌ها باید محاسبه امتیاز تطبیق را در سناریوهای مختلف (تطبیق کامل، تطبیق جزئی، عدم تطبیق) بررسی کنند. خارج از این مرحله: تست‌های یکپارچه‌سازی. نکته حیاتی: از داده‌های mock برای بازخورد و هدف پروژه استفاده شود. بخش مربوط از درخواست اصلی کاربر: '...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: تست واحد, سرویس تطبیق, امتیاز تطبیق, mock data. با توجه به کد واقعی پروژه، سرویس تطبیق با هدف هنوز در فایل‌های deep-read شده موجود نیست. نزدیک‌ترین سرویس‌های مرتبط با تطبیق و امتیازدهی در `backend/app/services/debate_service.py` (کلاس `ScoreResult` در خطوط 68-78 و متد `_score_responses` در خطوط 525-597) و `backend/app/services/deep_analysis_service.py` (کلاس `DeepAnalysisService` با فاکتورهای تحلیل در خطوط 189-198) یافت می‌شود. همچنین سرویس `backend/app/services/oversight_service.py` و `backend/app/services/quick_approval_service.py` نیز با منطق تطبیق و امتیازدهی مرتبط هستند. برای پیاده‌سازی این سرویس جدید، باید یک فایل سرویس جدید به نام `backend/app/services/goal_alignment_service.py` ایجاد شود که از الگوی `ScoreResult` و `_score_responses` در `debate_service.py` الهام بگیرد. تست‌های واحد باید در `backend/tests/test_goal_alignment_service.py` نوشته شوند و از داده‌های mock برای بازخورد و هدف پروژه استفاده کنند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل سرویس جدید `backend/app/services/goal_alignment_service.py` با کلاس `GoalAlignmentService` که شامل متد `calculate_alignment_score` است. 2. این متد باید سه سناریو را پشتیبانی کند: تطبیق کامل (امتیاز 100)، تطبیق جزئی (امتیاز بین 1 تا 99)، عدم تطبیق (امتیاز 0). 3. استفاده از داده‌های mock برای بازخورد و هدف پروژه در تست‌ها. 4. نوشتن تست‌های واحد در `backend/tests/test_goal_alignment_service.py` با استفاده از `pytest` و `pytest-asyncio`. 5. تست‌ها باید شامل: `test_full_alignment`, `test_partial_alignment`, `test_no_alignment`, `test_empty_feedback`, `test_invalid_goal` باشند. 6. الهام از ساختار `ScoreResult` در `backend/app/services/debate_service.py` خطوط 68-78 و متد `_score_responses` در خطوط 525-597. 7. استفاده از `pytest.fixture` برای ایجاد داده‌های mock. 8. اطمینان از عدم وابستگی به دیتابیس یا سرویس‌های خارجی در تست‌های واحد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 18: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) برای جریان کامل بازبینی

**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که جریان کامل بازبینی را از شروع تا دریافت نتایج آزمایش می‌کند. این تست‌ها باید شامل تعامل بین APIها، سرویس‌ها، و دیتابیس باشند. خارج از این مرحله: تست‌های UI. نکته حیاتی: از یک دیتابیس تست (مثلاً SQLite in-memory) استفاده شود.
**Key terms:** تست یکپارچه‌سازی, جریان کامل, API, دیتابیس تست

**بخش مربوط از متن کاربر:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های یکپارچه‌سازی برای جریان کامل بازبینی پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های انتخابی
      """
      try:
          analyzer = get_project_analyzer()
          analyzer.initialize()
  
          # اجرای تحلیل
          report = await analyzer.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های یکپارچه‌سازی (Integration Tests) برای جریان کامل بازبینی پروژه‌ها را دارد. این جریان شامل بازبینی جدی‌تر فایل‌های پروژه‌های محلی و غیرمحلی ساخته‌شده در پلتفرم توسط مدل‌های فعال و انتخاب‌شده در آن صفحه است. هدف از این بازبینی، شناسایی ایرادات تکنیکی و منطقی فایل‌ها و تطبیق آن‌ها با هدفی است که پروژه برای آن ساخته شده. تست‌ها باید جریان کامل بازبینی را از شروع (ارسال درخواست بازبینی) تا دریافت نتایج (دریافت گزارش) آزمایش کنند. این تست‌ها باید شامل تعامل بین APIها (مانند endpointهای موجود در `backend/app/api/routes/`)، سرویس‌ها (مانند `backend/app/services/`)، و دیتابیس باشند. تست‌های UI از این مرحله خارج هستند. نکته حیاتی: از یک دیتابیس تست (مثلاً SQLite in-memory) استفاده شود. کلیدواژه‌های اصلی: تست یکپارچه‌سازی, جریان کامل, API, دیتابیس تست. شواهد در کد: فایل‌های route مانند `backend/app/api/routes/analysis.py` (خط 46-80 برای endpoint تحلیل) و `backend/app/api/routes/project_memory.py` (خط 753-800 برای افزودن فیلد پویا) نشان‌دهنده APIهای موجود هستند. سرویس `backend/app/services/deep_analysis_service.py` برای تحلیل عمیق و `backend/app/services/ai_manager.py` برای مدیریت مدل‌های AI استفاده می‌شود. مدل دیتابیس `backend/app/models/project.py` برای ذخیره پروژه‌ها و `backend/app/models/analysis_report.py` برای ذخیره گزارش‌های تحلیل استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک فایل تست جدید به نام `backend/tests/test_review_integration.py`. 2. در این فایل، یک کلاس تست با نام `TestReviewFlow` ایجاد کن. 3. در متد `setup_method`، یک دیتابیس SQLite in-memory با استفاده از `sqlalchemy` و `create_engine('sqlite:///:memory:')` راه‌اندازی کن. 4. یک پروژه تستی با استفاده از مدل `Project` در دیتابیس ایجاد کن. 5. یک فیلد پویا (Dynamic Field) با استفاده از endpoint `POST /{project_id}/memory/fields` (موجود در `backend/app/api/routes/project_memory.py` خط 753) اضافه کن. 6. یک تحلیل با استفاده از endpoint `POST /analysis/run` (موجود در `backend/app/api/routes/analysis.py` خط 46) اجرا کن. 7. بررسی کن که پاسخ شامل `success=True` و یک `report_id` معتبر باشد. 8. با استفاده از `GET /analysis/reports/{report_id}` (موجود در `backend/app/api/routes/analysis.py` خط 293) گزارش را دریافت کن. 9. بررسی کن که گزارش شامل فیلدهای `overall_score`, `issues_found`, و `recommendations` باشد. 10. از `pytest` و `httpx.AsyncClient` برای شبیه‌سازی درخواست‌های HTTP به FastAPI استفاده کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 19: نوشتن تست‌های UI برای کامپوننت‌های جدید (ModelSelector و ReviewResultsPanel)

**Scope:** این مرحله شامل نوشتن تست‌های UI (مثلاً با React Testing Library) برای کامپوننت‌های `ModelSelector` و `ReviewResultsPanel` است. تست‌ها باید تعاملات کاربر مانند انتخاب مدل، کلیک روی دکمه بازبینی، و نمایش نتایج را پوشش دهند. خارج از این مرحله: تست‌های end-to-end. نکته حیاتی: از mocking برای API calls استفاده شود.
**Key terms:** تست UI, ModelSelector, ReviewResultsPanel, React Testing Library

**بخش مربوط از متن کاربر:**
```
...توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه... بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های UI برای ModelSelector و ReviewResultsPanel

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/analysis/page.tsx:1-50` — `AnalysisPage` — فایل deep-read شده است. این صفحه احتمالاً محل استفاده از کامپوننت‌های هدف است. مجری باید خطوط دقیق استفاده را تأیید کند.
  ```tsx
  // فایل deep-read شده است. احتمالاً کامپوننت‌های ModelSelector و ReviewResultsPanel در این صفحه استفاده می‌شوند.
  ```
- `frontend/src/app/oversight/page.tsx:1-50` — `OversightPage` — فایل deep-read شده است. این صفحه احتمالاً محل استفاده از کامپوننت‌های هدف است. مجری باید خطوط دقیق استفاده را تأیید کند.
  ```tsx
  // فایل deep-read شده است. احتمالاً کامپوننت‌های ModelSelector و ReviewResultsPanel در این صفحه استفاده می‌شوند.
  ```
- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست نوشتن تست‌های UI برای کامپوننت‌های جدید `ModelSelector` و `ReviewResultsPanel` را داده است. این تست‌ها باید با React Testing Library نوشته شوند و تعاملات کاربر مانند انتخاب مدل، کلیک روی دکمه بازبینی، و نمایش نتایج را پوشش دهند. کاربر تأکید کرده که تست‌های end-to-end خارج از این مرحله هستند و نکته حیاتی: از mocking برای API calls استفاده شود. بخش مربوط از درخواست اصلی کاربر: '...توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه... بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه'. کلیدواژه‌ها: تست UI, ModelSelector, ReviewResultsPanel, React Testing Library. با توجه به deep context، فایل‌های frontend مرتبط در `frontend/src/app/` قرار دارند. فایل‌های deep-read شده شامل `frontend/src/app/analysis/page.tsx` و `frontend/src/app/oversight/page.tsx` هستند که احتمالاً این کامپوننت‌ها در آن‌ها استفاده می‌شوند. همچنین فایل‌های backend مانند `backend/app/api/routes/analysis.py` (خطوط 46-80 برای endpoint `/analysis/run`) و `backend/app/api/routes/model_profiles.py` (خطوط 142-227 برای endpoint `/api/models/profiles`) برای mocking API calls مرتبط هستند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل‌های تست جدید در `frontend/src/__tests__/components/ModelSelector.test.tsx` و `frontend/src/__tests__/components/ReviewResultsPanel.test.tsx`. 2. برای کامپوننت ModelSelector: تست رندر شدن لیست مدل‌ها، تست انتخاب یک مدل با کلیک، تست نمایش مدل‌های فعال، و تست فراخوانی تابع onChange پس از انتخاب. 3. برای کامپوننت ReviewResultsPanel: تست رندر شدن با داده‌های خالی، تست نمایش نتایج بازبینی، تست کلیک روی دکمه بازبینی و فراخوانی تابع onSubmit، تست نمایش پیام خطا در صورت failure. 4. استفاده از `jest.mock` برای mocking سرویس‌های API مانند `axios` یا ماژول‌های داخلی که در کامپوننت‌ها استفاده می‌شوند. 5. اطمینان از اینکه تست‌ها با `@testing-library/react` و `@testing-library/jest-dom` نوشته شده‌اند. 6. اجرای تست‌ها با دستور `npm run test -- --testPathPattern='ModelSelector|ReviewResultsPanel'`.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 20: بازبینی نهایی (Audit) و مستندسازی تغییرات

**Scope:** این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده، بررسی امنیت (مثلاً نشت API key مدل‌ها)، بهینه‌سازی عملکرد، و به‌روزرسانی مستندات پروژه است. همچنین باید یک checklist از تمام قابلیت‌های پیاده‌سازی‌شده تهیه شود. خارج از این مرحله: اضافه کردن قابلیت جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود.
**Key terms:** بازبینی نهایی, امنیت, مستندات, code review

**بخش مربوط از متن کاربر:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
بازبینی نهایی امنیت و مستندات پروژه

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست بازبینی نهایی (Audit) و مستندسازی تغییرات را داده است. این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده، بررسی امنیت (مثلاً نشت API key مدل‌ها)، بهینه‌سازی عملکرد، و به‌روزرسانی مستندات پروژه است. همچنین باید یک checklist از تمام قابلیت‌های پیاده‌سازی‌شده تهیه شود. خارج از این مرحله: اضافه کردن قابلیت جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود. بخش مربوط از درخواست اصلی کاربر: 'بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه'. کلیدواژه‌ها: بازبینی نهایی, امنیت, مستندات, code review. در کد واقعی پروژه، فایل‌های متعددی وجود دارند که نیاز به بازبینی دارند: backend/app/api/routes/analysis.py (خطوط 1-268) حاوی endpoint استریم تحلیل است که از AI Manager و DeepAnalysisService استفاده می‌کند و نیاز به بررسی نشت API key دارد. backend/app/api/routes/github_import.py (خطوط 1-644) حاوی توکن‌های GitHub است که در توابع get_effective_token (خط 45) مدیریت می‌شوند و باید از نشت آن‌ها جلوگیری شود. backend/app/api/routes/model_profiles.py (خطوط 1-231) پروفایل مدل‌ها را مدیریت می‌کند و نیاز به بررسی امنیتی دارد. backend/app/services/ai_manager.py (import شده در 33 فایل) مرکز مدیریت AI است و باید از نشت API key مدل‌ها جلوگیری کند. backend/app/core/config.py تنظیمات پروژه را مدیریت می‌کند و باید از ذخیره امن API keyها اطمینان حاصل شود. backend/app/services/oversight_service.py (import شده در 22 فایل) سرویس نظارت است و نیاز به بررسی منطق دارد. backend/app/models/ai_profile.py (import شده در 8 فایل) پروفایل AI را مدیریت می‌کند. backend/app/services/security_analyzer.py و backend/app/services/security_scanner.py سرویس‌های امنیتی هستند که باید بررسی شوند. backend/app/services/verify_runtime/ (شامل 20+ فایل) لایه verify runtime است که نیاز به بازبینی دارد. backend/app/services/scan_v5/ (شامل 15+ فایل) لایه اسکن v5 است. backend/app/services/oversight_codex_service.py و backend/app/services/oversight_deep_scan_service.py سرویس‌های نظارت عمیق هستند. backend/app/services/oversight_verifier.py و backend/app/services/oversight_verify_pdf.py سرویس‌های تأیید هستند. backend/app/services/oversight_telegram_compose.py سرویس تلگرام است. backend/app/services/oversight_upload_session.py سرویس آپلود است. backend/app/services/oversight_strong_prompt.py سرویس پرامپت قوی است. backend/app/services/oversight_model_temp_activate.py سرویس فعال‌سازی موقت مدل است. backend/app/services/oversight_mega_bundle.py سرویس باندل بزرگ است. backend/app/services/oversight_inspector_bridge.py سرویس پل بازرس است. backend/app/services/oversight_extraction.py سرویس استخراج است. backend/app/services/oversight_progress.py سرویس پیشرفت است. backend/app/services/oversight_settings.py سرویس تنظیمات است. backend/app/services/oversight_service.py سرویس اصلی نظارت است. backend/app/services/oversight_verifier.py سرویس تأیید است. backend/app/services/oversight_verify_pdf.py سرویس تأیید PDF است. backend/app/services/oversight_telegram_compose.py سرویس تلگرام است. backend/app/services/oversight_upload_session.py سرویس آپلود است. backend/app/services/oversight_strong_prompt.py سرویس پرامپت قوی است. backend/app/services/oversight_model_temp_activate.py س

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. endpoint PATCH /api/review/{review_id}/notes با status 200 پاسخ دهد و یادداشت را در دیتابیس ذخیره کند. _(verify: api_response)_
2. کامپوننت ReviewStatus.tsx یک فیلد متنی (textarea) برای ورود یادداشت نمایش دهد. _(verify: ui_interaction)_

## Task Steps

### Step 1: تحلیل و مستندسازی ساختار فعلی صفحه موتور خالق و فایل‌های پروژه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بررسی کامل صفحه موجود 'موتور خالق' (Creator Engine) و شناسایی تمام فایل‌های پروژه (محلی و غیرمحلی) است که در آنجا ساخته شده‌اند. باید ساختار داده‌ای این فایل‌ها، نوع پروژه‌ها، و نحوه نمایش و ذخیره‌سازی آن‌ها مستند شود. خارج از این مرحله: تغییر در کد یا رابط کاربری انجام نمی‌شود. نکته حیاتی: باید دقیقاً مشخص شود که فایل‌های 'محلی' و 'غیرمحلی' چه تفاوتی دارند و چگونه در سیستم فعلی مدیریت می‌شوند.
**Excerpt:**
```
این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

### Step 2: طراحی مکانیزم انتخاب مدل‌های فعال برای بازبینی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی یک سیستم انتخاب مدل است که در صفحه موتور خالق به کاربر اجازه می‌دهد یکی یا چند مدل از میان مدل‌های فعال (که قبلاً در سیستم تعریف شده‌اند) را برای بازبینی فایل‌های پروژه انتخاب کند. این طراحی باید شامل UI (مثلاً یک dropdown یا checkbox group) و منطق پشتیبان (backend logic) برای دریافت لیست مدل‌های فعال باشد. خارج از این مرحله: پیاده‌سازی واقعی UI و backend انجام نمی‌شود. نکته حیاتی: باید مشخص شود که مدل‌های 'فعال' چه معیاری دارند و آیا کاربر می‌تواند مدل‌های غیرفعال را هم ببیند یا خیر.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه... توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه
```

### Step 3: طراحی مکانیزم ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده برای بازبینی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی فرآیندی است که پس از انتخاب مدل‌ها و فایل‌های پروژه، فایل‌ها (با متادیتای کامل شامل کد، توضیحات، و هدف پروژه) به API هر مدل ارسال شوند. باید مشخص شود که آیا فایل‌ها به صورت تکی ارسال می‌شوند یا به صورت batch، و چگونه پاسخ‌ها (بازبینی‌ها) جمع‌آوری می‌شوند. خارج از این مرحله: پیاده‌سازی واقعی این فرآیند. نکته حیاتی: باید به محدودیت توکن مدل‌ها و زمان پاسخ‌دهی توجه شود.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

### Step 4: طراحی ساختار بازخورد بازبینی (Feedback Schema) برای دریافت نتایج از مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی یک ساختار داده (Schema) استاندارد برای بازخورد بازبینی است که از مدل‌ها دریافت می‌شود. این ساختار باید شامل فیلدهایی برای: شناسه فایل، نام مدل، نمره/امتیاز (اختیاری)، لیست ایرادات تکنیکی، لیست ایرادات منطقی، پیشنهادات برای تطبیق با هدف پروژه، و خلاصه کلی باشد. خارج از این مرحله: پیاده‌سازی ذخیره‌سازی این بازخوردها. نکته حیاتی: این Schema باید به اندازه کافی انعطاف‌پذیر باشد تا مدل‌های مختلف بتوانند بازخوردهای متنوعی تولید کنند، اما در عین حال ساختاریافته باشد تا قابل پردازش باشد.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 5: طراحی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Logic)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی منطقی است که بازخوردهای دریافتی از مدل‌ها را با 'هدف' اصلی پروژه (که در فایل‌های پروژه تعریف شده) مقایسه می‌کند. باید مشخص شود که چگونه 'تطبیق' اندازه‌گیری می‌شود (مثلاً از طریق امتیاز شباهت معنایی یا تطبیق کلمات کلیدی). خارج از این مرحله: پیاده‌سازی این منطق. نکته حیاتی: باید مشخص شود که 'هدف پروژه' در کجای فایل‌های پروژه ذخیره می‌شود و آیا این هدف به صورت صریح (مثلاً در یک فایل README) یا ضمنی (در کل کد) تعریف شده است.
**Excerpt:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 6: طراحی UI برای نمایش نتایج بازبینی و تطبیق با هدف
**Status:** `pending` (0%)
**Scope:** این مرحله شامل طراحی رابط کاربری (UI) برای نمایش نتایج بازبینی‌ها به کاربر است. این UI باید شامل بخش‌های زیر باشد: لیست فایل‌های بازبینی‌شده، خلاصه بازبینی هر فایل، جزئیات ایرادات (تکنیکی و منطقی)، امتیاز تطبیق با هدف، و امکان فیلتر/مرتب‌سازی بر اساس مدل یا نوع ایراد. خارج از این مرحله: پیاده‌سازی این UI. نکته حیاتی: طراحی باید کاربرپسند باشد و اطلاعات پیچیده را به صورت ساده و قابل فهم نمایش دهد.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 7: پیاده‌سازی قابلیت انتخاب مدل‌های فعال در صفحه موتور خالق
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی واقعی UI و منطق پشتیبان برای انتخاب مدل‌های فعال است. یک کامپوننت جدید (مثلاً `ModelSelector`) به صفحه موتور خالق اضافه می‌شود که لیست مدل‌های فعال را از یک API دریافت کرده و به کاربر اجازه انتخاب می‌دهد. خارج از این مرحله: پیاده‌سازی مکانیزم ارسال فایل‌ها یا دریافت بازخورد. نکته حیاتی: باید از state management موجود (مثلاً Redux یا Context) برای ذخیره مدل‌های انتخاب‌شده استفاده شود.
**Excerpt:**
```
...توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه
```

### Step 8: پیاده‌سازی API برای دریافت لیست مدل‌های فعال
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `GET /api/models/active`) است که لیست مدل‌های فعال را از دیتابیس یا کانفیگ برگرداند. این endpoint باید توسط کامپوننت `ModelSelector` در مرحله قبل استفاده شود. خارج از این مرحله: تغییر در منطق انتخاب مدل یا ذخیره‌سازی. نکته حیاتی: باید مشخص شود که 'فعال بودن' یک مدل چگونه در دیتابیس ذخیره می‌شود (مثلاً یک فیلد `is_active`).
**Excerpt:**
```
...مدل هایی که فعال هستن
```

### Step 9: پیاده‌سازی دکمه/اکشن 'بازبینی با مدل‌های انتخاب‌شده' در صفحه موتور خالق
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اضافه کردن یک دکمه (مثلاً 'Review with Selected Models') در صفحه موتور خالق است که پس از انتخاب فایل‌های پروژه و مدل‌ها، فرآیند بازبینی را آغاز می‌کند. با کلیک روی این دکمه، یک درخواست به backend ارسال می‌شود تا بازبینی شروع شود. خارج از این مرحله: پیاده‌سازی منطق backend برای بازبینی. نکته حیاتی: دکمه باید غیرفعال (disabled) باشد تا زمانی که حداقل یک فایل و یک مدل انتخاب نشده باشد.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

### Step 10: پیاده‌سازی API برای شروع فرآیند بازبینی (POST /api/reviews/start)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint جدید در backend (مثلاً `POST /api/reviews/start`) است که لیست فایل‌های پروژه و مدل‌های انتخاب‌شده را دریافت کرده و فرآیند بازبینی را به صورت ناهمزمان (asynchronous) شروع می‌کند. این endpoint باید یک شناسه بازبینی (review ID) برگرداند تا فرانت‌اند بتواند وضعیت را پیگیری کند. خارج از این مرحله: پیاده‌سازی منطق واقعی بازبینی (ارسال به مدل‌ها). نکته حیاتی: باید از یک task queue (مثلاً Celery یا Bull) برای مدیریت بازبینی‌های همزمان استفاده شود.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

### Step 11: پیاده‌سازی منطق ارسال فایل‌های پروژه به مدل‌های انتخاب‌شده (Backend Service)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سرویس اصلی backend است که پس از دریافت درخواست بازبینی، فایل‌های پروژه را گرفته و آن‌ها را به API هر مدل انتخاب‌شده ارسال می‌کند. این سرویس باید با استفاده از Feedback Schema طراحی‌شده (مرحله 4) پاسخ‌ها را پارس کرده و در دیتابیس ذخیره کند. خارج از این مرحله: پیاده‌سازی UI یا منطق تطبیق با هدف. نکته حیاتی: باید مدیریت خطا (error handling) برای زمانی که یک مدل در دسترس نیست یا پاسخ نامعتبر می‌دهد، پیاده‌سازی شود.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

### Step 12: پیاده‌سازی منطق تطبیق بازخورد با هدف پروژه (Goal Alignment Service)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی سرویسی است که بازخوردهای ذخیره‌شده را گرفته و آن‌ها را با هدف پروژه تطبیق می‌دهد. این سرویس باید یک امتیاز تطبیق (alignment score) برای هر بازخورد محاسبه کند و آن را در دیتابیس ذخیره کند. خارج از این مرحله: تغییر در UI یا منطق ارسال فایل. نکته حیاتی: این سرویس می‌تواند به صورت یک task جداگانه بعد از اتمام بازبینی اجرا شود.
**Excerpt:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 13: پیاده‌سازی API برای دریافت نتایج بازبینی (GET /api/reviews/{reviewId}/results)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک endpoint جدید (مثلاً `GET /api/reviews/{reviewId}/results`) است که نتایج کامل یک بازبینی را شامل لیست فایل‌ها، بازخوردهای هر مدل، و امتیازات تطبیق برمی‌گرداند. خارج از این مرحله: پیاده‌سازی UI برای نمایش این نتایج. نکته حیاتی: این endpoint باید از pagination پشتیبانی کند اگر تعداد فایل‌ها زیاد باشد.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 14: پیاده‌سازی کامپوننت نمایش نتایج بازبینی (ReviewResultsPanel)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک کامپوننت React (مثلاً `ReviewResultsPanel`) است که نتایج بازبینی را از API دریافت کرده و به صورت ساختاریافته نمایش می‌دهد. این کامپوننت باید شامل بخش‌های: لیست فایل‌ها، خلاصه بازبینی، جزئیات ایرادات (با قابلیت expand/collapse)، و امتیاز تطبیق باشد. خارج از این مرحله: یکپارچه‌سازی این کامپوننت با صفحه موتور خالق. نکته حیاتی: کامپوننت باید stateهای loading، empty، و error را مدیریت کند.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 15: یکپارچه‌سازی کامپوننت نمایش نتایج با صفحه موتور خالق
**Status:** `pending` (0%)
**Scope:** این مرحله شامل اضافه کردن کامپوننت `ReviewResultsPanel` به صفحه موتور خالق است. این کامپوننت باید پس از اتمام بازبینی (یا با کلیک روی یک دکمه 'مشاهده نتایج') نمایش داده شود. خارج از این مرحله: تغییر در منطق شروع بازبینی. نکته حیاتی: باید یک state برای مدیریت وضعیت بازبینی (در حال اجرا، کامل شده، خطا) در صفحه اصلی اضافه شود.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن
```

### Step 16: نوشتن تست‌های واحد (Unit Tests) برای سرویس ارسال فایل به مدل‌ها
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای سرویس backend است که فایل‌ها را به مدل‌ها ارسال می‌کند. تست‌ها باید موارد زیر را پوشش دهند: ارسال موفق، خطای API مدل، timeout، و پاسخ نامعتبر. خارج از این مرحله: تست‌های یکپارچه‌سازی (integration tests) یا تست‌های UI. نکته حیاتی: از mocking برای شبیه‌سازی API مدل‌ها استفاده شود.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

### Step 17: نوشتن تست‌های واحد برای سرویس تطبیق با هدف (Goal Alignment Service)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای سرویس تطبیق با هدف است. تست‌ها باید محاسبه امتیاز تطبیق را در سناریوهای مختلف (تطبیق کامل، تطبیق جزئی، عدم تطبیق) بررسی کنند. خارج از این مرحله: تست‌های یکپارچه‌سازی. نکته حیاتی: از داده‌های mock برای بازخورد و هدف پروژه استفاده شود.
**Excerpt:**
```
...با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 18: نوشتن تست‌های یکپارچه‌سازی (Integration Tests) برای جریان کامل بازبینی
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های یکپارچه‌سازی است که جریان کامل بازبینی را از شروع تا دریافت نتایج آزمایش می‌کند. این تست‌ها باید شامل تعامل بین APIها، سرویس‌ها، و دیتابیس باشند. خارج از این مرحله: تست‌های UI. نکته حیاتی: از یک دیتابیس تست (مثلاً SQLite in-memory) استفاده شود.
**Excerpt:**
```
...فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 19: نوشتن تست‌های UI برای کامپوننت‌های جدید (ModelSelector و ReviewResultsPanel)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های UI (مثلاً با React Testing Library) برای کامپوننت‌های `ModelSelector` و `ReviewResultsPanel` است. تست‌ها باید تعاملات کاربر مانند انتخاب مدل، کلیک روی دکمه بازبینی، و نمایش نتایج را پوشش دهند. خارج از این مرحله: تست‌های end-to-end. نکته حیاتی: از mocking برای API calls استفاده شود.
**Excerpt:**
```
...توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه... بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

### Step 20: بازبینی نهایی (Audit) و مستندسازی تغییرات
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده، بررسی امنیت (مثلاً نشت API key مدل‌ها)، بهینه‌سازی عملکرد، و به‌روزرسانی مستندات پروژه است. همچنین باید یک checklist از تمام قابلیت‌های پیاده‌سازی‌شده تهیه شود. خارج از این مرحله: اضافه کردن قابلیت جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود.
**Excerpt:**
```
...بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```
