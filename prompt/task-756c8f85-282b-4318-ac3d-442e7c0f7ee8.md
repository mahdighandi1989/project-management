---
task_id: 756c8f85-282b-4318-ac3d-442e7c0f7ee8
title: '[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)'
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-27T22:09:00.236507+00:00'
updated_at: '2026-06-03T18:31:13.424768+00:00'
target_files:
- frontend/src/app/creator/page.tsx
- backend/app/api/routes/analysis.py
- backend/app/api/routes/creator.py
- backend/app/services/creator_engine.py
- backend/app/services/cloud_code_executor.py
---

# [ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست (12 مرحله)

## Raw Idea

این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
ضمن اینکه الان که توکن کلاد رو دارم و داره تو جاهای مثل اجرای خودکار تسک ها و اجرای تکی تسک ها کار میکنه و از اشتراک ماهانه م ستفاده میکنه و نیاز به هزینه دا نداره، میخوام این گزینه استافده از کلاد هم در این قسمت برای ساخت و همینطور نظارت بر پروژه ها در این قسمت موتور خالق قایل انتخبا و استفاده باشه و دسترسی های مناسبو وسیعی داشته باشه

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

🔗 **وابستگی‌ها و همگام‌سازی (بسیار حیاتی — هرگز skip نکن):**

این بخش از همهٔ بخش‌های دیگرِ این یادداشت **مهم‌تر** است. اگر نقض شود،
نتیجهٔ کار ممکن است مشروع به‌نظر برسد ولی در عمل بخش‌های دیگر سیستم را عقب
بیندازد، broken reference تولید کند، یا منجر به data corruption شود.

پیش از و حین تغییر، تمام وابستگی‌ها را در **چهار جهت** به‌طور **کامل و
بدون هیچ خلاصه‌سازی** شناسایی و همگام کن:

**۱. وابستگی‌های upstream (این تسک به چه چیزهایی متکی است):**
- چه فایل‌ها، توابع، کلاس‌ها، API endpoint ها، schema های دیتابیس،
  env vars، یا config هایی که این تسک نیاز دارد؟
- آیا قرار است چیزی را ویرایش/حذف کنی که جای دیگر (signature، رفتار،
  return type، side effect) از آن انتظار خاصی می‌رود؟
- اگر dependency جدیدی اضافه می‌کنی، آیا با dependencyهای موجود تداخل
  دارد (نسخه، compat، lock file)؟

**۲. وابستگی‌های downstream (چه چیزهایی به این تسک متکی‌اند):**
- چه فایل‌ها، توابع، تست‌ها، migrations، docs، یا UI component هایی از
  کدی که داری ویرایش/اضافه/حذف می‌کنی **استفاده می‌کنند**؟
- با grep و reference search **همه‌ی** call sites، importها، subclassها،
  reference های مستقیم و غیرمستقیم را پیدا کن — نه فقط چند مورد اصلی.
- خصوصاً برای حذف یا rename: هیچ broken reference نباید باقی بماند.

**۳. وابستگی‌های cross-tier (بسیار مهم — هرگز فقط یک لایه را نبین):**

تسک شما ممکن است از backend، frontend، database، worker، یا هر tier
دیگری شروع شده باشد. ولی تغییرات تقریباً همیشه روی tier های دیگر هم
اثر می‌گذارند. **مستقل از اینکه تسک از کدام tier است**، این چک‌های دو
طرفه را همیشه انجام بده:

🔁 **اگر backend را تغییر دادی** (API، service، model، route):
  → frontend: کدام component/page/hook این endpoint یا data shape را
    مصرف می‌کند؟ type definition، state shape، error handling، loading
    state، form validation، URL routing همگی باید همگام شوند.
  → mobile/SDK/client library (اگر پروژه دارد): همان داستان frontend.
  → database: آیا migration لازم است؟ آیا rollback امن است؟
  → background workers: آیا event producer/consumer ها تحت تأثیرند؟
  → rate limit، auth، CORS، CSP: آیا رفتار جدید پشتیبانی می‌شود؟

🔁 **اگر frontend را تغییر دادی** (component، form، state، route):
  → backend: آیا endpoint جدید/تغییریافته لازم است؟ آیا data shape ای
    که ارسال می‌شود با schema سرور سازگار است؟
  → backend validation: آیا برای ورودی‌های جدید UI کافی است؟
  → permissions/RBAC: آیا feature جدید نیاز به role check جدید دارد؟
  → analytics/tracking: آیا event های جدید باید در backend log شوند؟
  → SEO/SSR: آیا تغییر route نیاز به sitemap/meta tags جدید دارد؟

🔁 **اگر database/migration را تغییر دادی**:
  → backend models (ORM، Pydantic، dataclasses) همگی به‌روزند؟
  → query های raw SQL یا ORM queries با schema جدید سازگارند؟
  → seed data، fixtures، factory functions تست‌ها به‌روزند؟
  → frontend: آیا data shape جدید در UI به‌درستی render می‌شود؟
  → rollback migration نوشته شده و امن است؟

🔁 **اگر API contract یا event schema را تغییر دادی** (REST، GraphQL،
   WebSocket، gRPC، Kafka، …):
  → OpenAPI/GraphQL schema/proto file آپدیت شد؟
  → همه‌ی consumer ها (client، subscriber، webhook، external API
    user) با version جدید سازگارند؟
  → backward compatibility حفظ شده یا migration path روشن است؟
  → versioning header/path اگر breaking change است؟

🔁 **اگر infrastructure یا config را تغییر دادی** (Dockerfile، CI، Render
   config، env، secrets):
  → README setup/installation section به‌روزه؟
  → `.env.example` با env vars جدید آپدیت شد؟
  → deploy script یا CI workflow هم تغییر کرد؟
  → docs/architecture یا diagram های infrastructure به‌روزند؟

⚠️ **هرگز فقط یک tier را تغییر نده و فرض کنی بقیه خودکار همگام می‌شوند.**
   حتی برای تغییرات به‌ظاهر «کوچک»، چک کن.

**۴. وابستگی‌های جانبی (artifacts که همیشه چک شوند):**

تغییرات کد همیشه روی این artifact ها اثر دارند. **همه را** بررسی و
به‌روز کن — مستندات اولویت **بالا** دارد چون فراموش‌شدنی‌ترین است.

  📝 **مستندات** (همیشه چک کن — حتی برای تغییر کوچک کد):
    - README.md (شرح، setup، نمونه‌های استفاده، badge ها)
    - CHANGELOG.md / RELEASE_NOTES.md
    - docs/ folder (architecture، API reference، user guides، runbooks)
    - inline docstrings/کامنت‌های توابع و کلاس‌های تغییریافته
    - OpenAPI/Swagger annotations، JSDoc/TSDoc
    - architecture diagrams (اگر component اضافه/حذف شد)
    - migration guides (اگر breaking change است)

  🌍 **مستندات کاربر**:
    - i18n files و translation keys
    - UI labels، tooltip ها، help text، error messages
    - in-app onboarding (اگر flow جدید است)

  🧪 **تست‌ها**:
    - unit tests (همه‌ی فایل‌های مرتبط — حتی اگر «بی‌ربط» به‌نظر می‌رسد)
    - integration tests
    - e2e tests (Playwright/Cypress/Selenium)
    - snapshot tests (اگر UI تغییر کرد)
    - contract tests (Pact یا مشابه)
    - performance benchmarks (اگر behavior performance-sensitive تغییر کرد)

  🧬 **type definitions و contracts**:
    - .d.ts files
    - Pydantic models، dataclasses
    - Protobuf/Avro/Thrift schemas
    - GraphQL schema definitions
    - JSON Schemas

  🏗 **infrastructure و config**:
    - Dockerfile، docker-compose.yml
    - Kubernetes manifests
    - Render/Vercel/Netlify config
    - GitHub Actions / GitLab CI workflows
    - environment templates (.env.example، .env.sample)
    - feature flags (LaunchDarkly، GrowthBook، config)

  📊 **monitoring و observability**:
    - logging keys (اگر اضافه/حذف شد، log parser ها هم به‌روز شوند)
    - metric names (Prometheus، Datadog)
    - tracing spans
    - alert rules و dashboards
    - error tracking (Sentry rules، groupings)

  🔐 **security**:
    - auth rules (rate limit، CORS، CSP، HSTS)
    - permissions/RBAC config
    - secrets rotation policies
    - audit log events (اگر action جدید اضافه شد)

  💾 **caches و serialization**:
    - cache keys و TTL (اگر data shape یا lifecycle تغییر کرد)
    - serializer formats (Redis، session storage)
    - browser storage (localStorage، IndexedDB schemas)

**قانون مطلق همگام‌سازی:**
- هر چیزی که در (۱)، (۲)، (۳)، یا (۴) شناسایی شد، در **همان workflow
  این تسک** همگام و به‌روز شود. هرگز برای بعد رها نکن.
- اگر یک فایل/تست/docs نسبت به تغییر شما عقب بماند، در بهترین حالت bug،
  در بدترین حالت مشکل امنیتی یا data corruption تولید می‌کند.
- تغییرات همگام‌سازی می‌توانند در commit جداگانه باشند (در همان task)،
  ولی نباید skip شوند یا به «refactor آینده» سپرده شوند.

**هرگز این جمله‌ها قابل قبول نیست:**
- ❌ «بعداً پیداش می‌کنم»
- ❌ «احتمالاً جای دیگه‌ای استفاده نمی‌شه»
- ❌ «این یه refactor جداگانه‌ست — out of scope»
- ❌ «فقط فایل‌های اصلی رو بررسی کردم»
- ❌ «حدس می‌زنم چیزی بهش وابسته نیست»
- ❌ «دامنه‌ی وابستگی‌ها رو خلاصه کردم» — هرگز خلاصه نکن
- ❌ «این task فقط backend است؛ frontend مشکل خودش» — هرگز
- ❌ «این task فقط frontend است؛ backend از قبل کار می‌کند» — هرگز ثابت نکرده
- ❌ «مستندات بعداً به‌روز می‌شن» — همیشه same-task همگام شوند
- ❌ «testها رو نگاه نکردم چون فقط یه تغییر کوچیک بود»

**در commit message یا PR description**، دامنهٔ وابستگی‌های شناسایی‌شده و
همگام‌شده را به‌طور explicit و **per-tier** بنویس. مثال:
```
Dependencies synced:
- upstream: User model schema, auth middleware
- downstream: 3 API endpoints, 5 frontend components, 12 tests
- cross-tier (backend → frontend): UserProfile.tsx, useUser.ts hook,
  api-types.ts (TS definitions)
- cross-tier (backend → infra): .env.example added NEW_AUTH_SCOPES
- side artifacts: OpenAPI spec, README API section, i18n keys for
  new errors, Sentry alert rule for new error code
```
اگر هیچ وابستگی پیدا نکردی در هر کدام از چهار جهت، صریحاً بنویس:
«بررسی شد — هیچ وابستگی upstream / downstream / cross-tier (backend↔
frontend↔db↔infra) / side شناسایی نشد» تا مشخص باشد بررسی **انجام شده**
نه اینکه فراموش شده.

📋 **مدیریت TO-DO برای اقدامات دستی کاربر (همیشه چک کن):**

⚠️ **هشدار بحرانی — قاعدهٔ ضد-فرار:** TO-DO فقط برای کارهایی است که
**واقعاً غیرممکن** برای agent است (نیاز به انسان مطلق)، نه برای کارهایی
که «بزرگ‌اند»، «وقت می‌برند»، یا «نیازمند fixture/setup» هستند. اگر یک
agent در یک سشن بیش از **۲۰٪ از تسک‌ها** را با TO-DO ببندد، یعنی از کار
فرار می‌کند — این الگو در سشن‌های قبلی **مشاهده** شده و الان ممنوع است.

✅ **فقط برای این موارد TO-DO بساز** (لیست بسته — هرچه خارج این لیست
ممنوع است):

  ۱. **Credential/secret که فقط کاربر دارد**:
     - تنظیم API key واقعی در پنل ادمین خارجی (Render، AWS، Stripe، …)
     - تأیید OAuth client روی console آن سرویس
     - paste کردن webhook secret که فقط بعد از ساخت در dashboard ظاهر می‌شود

  ۲. **Account/billing روی سرویس خارجی که کاربر باید عضو شود**:
     - ساخت account جدید روی Stripe/SendGrid/Twilio/Google Cloud
     - تأیید verification شماره یا ID
     - فعال‌سازی subscription پولی

  ۳. **داده/asset خصوصی که فقط کاربر دارد**:
     - آپلود لوگو/تصویر/فونت برند
     - paste کردن داده‌ای که در محل کار کاربر است
     - import داده‌ای که فقط روی device کاربر است

  ۴. **تصمیم سلیقه‌ای/حقوقی/کسب‌وکار**:
     - انتخاب رنگ‌بندی نهایی یا تم
     - متن دقیق Terms of Service / Privacy Policy
     - تعرفهٔ قیمت‌گذاری
     - نام نهایی برند یا دامنه

⛔ **هرگز TO-DO نکن برای** (لیست سیاه — هر چیزی که در این لیست است
**قابل اجرا** توسط agent است، حتی اگر بزرگ یا چندبخشی باشد):

  ❌ UI component / page / dashboard (هر فریم‌ورک: React, Vue, Angular,
     Svelte، حتی اگر معماری بزرگ دارد) — می‌توانی stub اولیه + state
     management + layout + استایل بسازی
  ❌ "نیازمند Google Drive / Stripe / Twilio API" — می‌توانی **client
     stub** با abstraction layer بسازی که با env var واقعی plug-in شود؛
     کد integration یعنی پیاده‌سازی، نه TO-DO
  ❌ "feature بزرگ، چند روز کار می‌برد" — اندازه دلیل defer نیست؛ کوچک
     شروع کن، iterate کن، در همین سشن کامل کن
  ❌ Celery / background worker / scheduler — یک task ساده + register
     می‌توانی بسازی
  ❌ Migration / model / schema — حتی اگر فیلد جدید نیاز دارد، اضافه کن
  ❌ REST endpoint / GraphQL resolver / WebSocket route — هرگز TO-DO
  ❌ test (unit/integration/e2e) — همیشه قابل نوشتن
  ❌ Documentation / README / API docs — همیشه قابل نوشتن
  ❌ Config file / .env.example / Dockerfile / CI workflow — همیشه قابل
     نوشتن
  ❌ "می‌توانستی .tsx ولی repo .jsx است" — از .jsx استفاده کن، TO-DO نکن
  ❌ "نیازمند فیلد X در مدل دیگر" — اضافه کن فیلد را، TO-DO نکن
  ❌ "تصمیم admin-vs-user-scoped" — پرامپت اولیه scope را معلوم کرده،
     یا با محتاطانه‌ترین تفسیر پیش برو
  ❌ "credential در production هنوز ست نیست" — این TO-DO ساده برای
     تنظیم env var است (مورد ۱ بالا)، نه دلیل برای defer کردن کد
  ❌ "نیازمند verification از کاربر" — اگر اقدام واقعی غیرممکن نیست،
     پیش برو
  ❌ هر چیزی که در یک کامنت `# TODO` معمولی نوشته می‌شد — این فایل
     TO-DO نیست، کامنت inline است

🔬 **قاعدهٔ «حداقل تلاش» قبل از TO-DO**: قبل از TO-DO کردن یک AC، **اثبات
کن** که قابل انجام نیست:

  ۱. آیا می‌توانم یک stub/placeholder بسازم که با env واقعی plug-in شود؟
     → اگر بله، بساز و TO-DO نکن
  ۲. آیا می‌توانم برای این بخش یک test (حتی mock-based) بنویسم؟
     → اگر بله، بنویس و TO-DO نکن
  ۳. آیا می‌توانم abstraction/interface را تعریف کنم، حتی اگر backend
     واقعی نیست؟ → اگر بله، تعریف کن و TO-DO نکن
  ۴. آیا فقط یک حالت سلیقه‌ای/decision کاربر در میان است؟
     → فقط آن یک decision را TO-DO کن، نه کل feature را

اگر یکی از این چهار راه‌حل ممکن بود ولی به TO-DO رفتی، **اعتبار شما از
بین می‌رود**.

📊 **آستانهٔ TO-DO per session**: در یک حلقهٔ اجرای N تسک، اگر بیشتر از
**۲۰٪** تسک‌ها فایل TO-DO ساختی، خودت در گزارش پایانی صریحاً اعلام کن:

  "⚠️ نسبت TO-DO من {K}/{N} = {%} است که از آستانهٔ ۲۰٪ بالاتر است.
   احتمالاً برخی از این TO-DO ها قابل اجرا بودند ولی من فرار کردم.
   لیست TO-DO ها را کاربر باید بازبینی کند که آیا واقعاً Manual-required
   بودند یا agent ضعیف کار کرده."

**یادآوری همیشگی:** اگر در آینده قابلیت‌های شما گسترش پیدا کرد و توانستید
یکی از موارد لیست سفید را خودکار انجام دهید (مثلاً managed credential
injection، یا integration پولی automate شود)، انجام دهید و TO-DO نسازید.
لیست سفید بسته است ولی **بسته از پایین** (می‌تواند کوچک‌تر شود اگر
قابلیت‌ها رشد کنند، ولی هرگز بزرگ‌تر نشود برای فرار).

**اگر هیچ بخش Manual-required نبود (تمام تسک Auto-capable است)**:
  → فایل TO-DO **نساز**. فولدر TO-DO/ باید پاک و معنادار بماند.
  → اگر برای این task از قبل `TO-DO/todo-task-{task_id_first_8}.md` بود
     (یعنی در run قبلی نیاز به دخالت کاربر بود ولی الان نه): فایل قدیمی
     را پاک کن و entry را از `TO-DO/_index.json` حذف کن.

**اگر بخش Manual-required دارد** (همه‌جانبه یا hybrid):
  1. فولدر TO-DO/ را در ریشه ریپو ایجاد کن اگر نیست
  2. فایل `TO-DO/todo-task-{task_id_first_8}.md` بساز با front-matter
     شامل: task_id, task_title, execution_priority, created_at,
     updated_at, status: "pending"
     و در بدنه: «چرا این فایل ساخته شد»، «وضعیت بخش‌های خودکار»
     (commit ها reference)، «کارهایی که باید انجام دهی» با اولویت
     بالا/متوسط/پایین به ترتیب، «وقتی این کارها را تمام کردی»
  3. `TO-DO/_index.json` را با **merge** آپدیت کن (نه overwrite):
     - فایل موجود را بخوان
     - entry های orphan (فایلشان پاک شده) را حذف کن
     - entry این task را اضافه/replace کن
     - بر اساس execution_priority صعودی مرتب کن
     - ساختار: `{"version":1, "generated_at": ISO, "total": N, "items": [...]}`
  4. این تغییرات TO-DO را در **همان commit کد** شامل کن (نه commit جداگانه)

⛔ **ممنوعات مطلق TO-DO**:
  ❌ ساختن TO-DO برای کاری که می‌توانستی خودت انجام دهی (شلوغی فولدر)
  ❌ overwrite کردن `TO-DO/_index.json` بدون merge (data loss)
  ❌ نگه‌داشتن entry هایی که فایل‌شان پاک شده (broken reference)
  ❌ فراموش کردن نوشتن «خروجی مورد انتظار» در هر آیتم TO-DO

این بخش الزامی است. حتی اگر فکر می‌کنی "این تسک کاملاً auto است و نیازی
به TO-DO نیست"، صریحاً در commit message یا report بنویس:
"بررسی شد — این تسک هیچ بخش Manual-required ندارد، TO-DO ساخته نشد."

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

🔁 **Commit + Push فوری per-task (بسیار مهم برای جریان کار صحیح):**

پس از اتمام پیاده‌سازی این تسک، **بلافاصله** commit کن و **همان موقع**
به default branch (main/master) push کن. سپس به تسک بعدی برو.

✓ چرا این قانون حیاتی است:
  - تسک‌های بعدی ممکن است به فایل‌ها/تغییراتی که این تسک ایجاد کرده
    نیاز داشته باشند. اگر push نکنی، `git pull` بعدی آن‌ها را نمی‌بیند.
  - جمع‌کردن تغییرات چند تسک منجر به conflict های بزرگ می‌شود.
  - اگر در میانه fail کنی، task های push شده ضایع نمی‌شوند.

⛔ ممنوع: "همه task ها را تمام می‌کنم بعد یک‌جا push می‌زنم"
⛔ ممنوع: branch جدا برای task — مستقیم به default branch
⛔ ممنوع: task بعدی بدون push کامل task قبلی

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
این ایده برای صفحه موتور خالق هست که از قبل وجود داره و میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
ضمن اینکه الان که توکن کلاد رو دارم و داره تو جاهای مثل اجرای خودکار تسک ها و اجرای تکی تسک ها کار میکنه و از اشتراک ماهانه م ستفاده میکنه و نیاز به هزینه دا نداره، میخوام این گزینه استافده از کلاد هم در این قسمت برای ساخت و همینطور نظارت بر پروژه ها در این قسمت موتور خالق قایل انتخبا و استفاده باشه و دسترسی های مناسبو وسیعی داشته باشه
```

## 📋 چک‌لیست مراحل (4 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: اضافه کردن قابلیت بازبینی مجدد فایل‌های پروژه در صفحه موتور خالق** — این مرحله شامل افزودن یک دکمه یا گزینه جدید در صفحه موتور خالق است که به کاربر اجازه می‌دهد فایل‌های پروژه‌های محلی و غیرمحلی ساخته شده را برای بازبینی مجدد انتخاب کند. این بازبینی باید توسط مدل‌های فعال و انتخاب‌شده در همان صفحه انجام شود. خارج از این مرحله: تغییر در منطق اصلی پروژه‌ها، اضافه کردن 
- [ ] **مرحله 2: تطبیق فایل‌های پروژه با هدف اصلی پروژه در بازبینی مجدد** — این مرحله شامل افزودن منطق تطبیق فایل‌های بازبینی‌شده با هدف اصلی پروژه است. یعنی مدل باید بررسی کند که آیا فایل‌ها با هدف تعریف‌شده برای پروژه همخوانی دارند یا خیر. خارج از این مرحله: تغییر در نحوه تعریف هدف پروژه یا ذخیره‌سازی آن. نکته حیاتی: هدف پروژه باید از metadata پروژه استخراج شود.
- [ ] **مرحله 3: اضافه کردن گزینه استفاده از Cloud در صفحه موتور خالق برای ساخت پروژه** — این مرحله شامل افزودن یک گزینه یا سوئیچ در صفحه موتور خالق است که به کاربر اجازه می‌دهد هنگام ساخت پروژه‌های جدید از Cloud استفاده کند. این گزینه باید از اشتراک ماهانه Cloud (Token Cloud) استفاده کند و نیازی به هزینه اضافی نداشته باشد. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌های موجود یا بازبینی
- [ ] **مرحله 4: اعطای دسترسی‌های مناسب و وسیع به Cloud در صفحه موتور خالق** — این مرحله شامل تنظیم دسترسی‌های Cloud در صفحه موتور خالق است تا کاربر بتواند از تمام قابلیت‌های Cloud (مانند اجرای تسک‌ها، بازبینی، و ساخت) استفاده کند. این دسترسی‌ها باید بر اساس اشتراک ماهانه کاربر محدود شوند. خارج از این مرحله: تغییر در نحوه اشتراک‌گذاری پروژه‌ها. نکته حیاتی: دسترسی‌ها باید با سط

---

# 🔹 مرحله 1: اضافه کردن قابلیت بازبینی مجدد فایل‌های پروژه در صفحه موتور خالق

**Scope:** این مرحله شامل افزودن یک دکمه یا گزینه جدید در صفحه موتور خالق است که به کاربر اجازه می‌دهد فایل‌های پروژه‌های محلی و غیرمحلی ساخته شده را برای بازبینی مجدد انتخاب کند. این بازبینی باید توسط مدل‌های فعال و انتخاب‌شده در همان صفحه انجام شود. خارج از این مرحله: تغییر در منطق اصلی پروژه‌ها، اضافه کردن قابلیت Cloud، یا تغییر در نحوه ذخیره‌سازی فایل‌ها. نکته حیاتی: این قابلیت باید فقط برای پروژه‌هایی فعال باشد که قبلاً در صفحه موتور خالق ساخته شده‌اند.
**Key terms:** صفحه موتور خالق, فایل‌های پروژه, پروژه‌های محلی, پروژه‌های غیرمحلی, مدل‌های فعال

**بخش مربوط از متن کاربر:**
```
میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن دکمه بازبینی مجدد فایل‌های پروژه در صفحه موتور خالق

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — صفحه اصلی موتور خالق — اینجا باید دکمه بازبینی مجدد اضافه شود
  ```tsx
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/api/routes/analysis.py:46-80` — `run_analysis` — این endpoint موجود برای تحلیل پروژه قابل استفاده است — فقط نیاز به فراخوانی از frontend دارد
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های انتخابی
      """
      try:
          analyzer = get_project_analyzer()
          analyzer.initialize()
  
          # اجرای تحلیل
          report = await analyzer.analyze_project(
              project_id=request.project_id,
              project_path=request.project_path,
              models=request.models if request.models else None,
              roadmap_path=request.roadmap_path
          )
  ```
- `backend/app/api/routes/creator.py:1-30` — `creator_router` — نیاز به اضافه کردن endpoint GET
  ```python
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست اضافه کردن قابلیت بازبینی مجدد فایل‌های پروژه‌های ساخته شده در صفحه موتور خالق را دارد. این قابلیت باید به کاربر اجازه دهد فایل‌های پروژه‌های محلی و غیرمحلی ساخته شده در آن صفحه را برای بازبینی مجدد انتخاب کند. بازبینی باید توسط مدل‌های فعال و انتخاب‌شده در همان صفحه انجام شود. خارج از این مرحله: تغییر در منطق اصلی پروژه‌ها، اضافه کردن قابلیت Cloud، یا تغییر در نحوه ذخیره‌سازی فایل‌ها. نکته حیاتی: این قابلیت باید فقط برای پروژه‌هایی فعال باشد که قبلاً در صفحه موتور خالق ساخته شده‌اند. کلیدواژه‌ها: صفحه موتور خالق, فایل‌های پروژه, پروژه‌های محلی, پروژه‌های غیرمحلی, مدل‌های فعال. بر اساس کد موجود، صفحه creator در frontend/src/app/creator/page.tsx قرار دارد و از backend/app/api/routes/creator.py برای API استفاده می‌کند. همچنین backend/app/services/creator_engine.py و backend/app/services/simple_creator.py سرویس‌های اصلی هستند. برای بازبینی مجدد، می‌توان از endpoint موجود /analysis/run در backend/app/api/routes/analysis.py (خط 46-80) استفاده کرد که قابلیت تحلیل پروژه با مدل‌های انتخابی را دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در frontend/src/app/creator/page.tsx، یک دکمه 'بازبینی مجدد' به کارت هر پروژه ساخته شده اضافه کن. 2. این دکمه فقط برای پروژه‌هایی نمایش داده شود که creator_id یا source='creator' دارند. 3. با کلیک، یک modal باز شود که کاربر بتواند مدل‌های فعال را انتخاب کند. 4. سپس یک POST به /api/analysis/run با project_id و project_path و models انتخابی ارسال شود. 5. نتیجه تحلیل در یک modal یا بخش جدید نمایش داده شود. 6. در backend/app/api/routes/creator.py یک endpoint جدید GET /creator/projects اضافه کن که پروژه‌های ساخته شده توسط creator را برگرداند. 7. در backend/app/services/creator_engine.py متد get_created_projects() اضافه کن که پروژه‌های با source='creator' را از دیتابیس فیلتر کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: تطبیق فایل‌های پروژه با هدف اصلی پروژه در بازبینی مجدد

**Scope:** این مرحله شامل افزودن منطق تطبیق فایل‌های بازبینی‌شده با هدف اصلی پروژه است. یعنی مدل باید بررسی کند که آیا فایل‌ها با هدف تعریف‌شده برای پروژه همخوانی دارند یا خیر. خارج از این مرحله: تغییر در نحوه تعریف هدف پروژه یا ذخیره‌سازی آن. نکته حیاتی: هدف پروژه باید از metadata پروژه استخراج شود.
**Key terms:** هدف پروژه, تطبیق فایل‌ها, metadata پروژه

**بخش مربوط از متن کاربر:**
```
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن منطق تطبیق فایل‌های بازبینی‌شده با هدف اصلی پروژه در تحلیل

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:46-80` — `run_analysis` — این
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest,
      background_tasks: BackgroundTasks
  ):
      """
      اجرای تحلیل جدید
  
      تحلیل کامل پروژه توسط مدل‌های انتخابی
      """
      try:
          analyzer = get_project_analyzer()
          analyzer.initialize()
  
          # اجرای تحلیل
          report = await analyzer.analyze_project(
              project_id=request.project_id,
              project_path=request.project_path,
              models=request.models if request.models else None,
              roadmap_path=request.roadmap_path
          )
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن منطق تطبیق فایل‌های بازبینی‌شده با هدف اصلی پروژه را دارد. این مرحله شامل بررسی همخوانی فایل‌ها با هدف تعریف‌شده برای پروژه است. نکته حیاتی: هدف پروژه باید از metadata پروژه استخراج شود، نه از تعریف مجدد یا ذخیره‌سازی جدید. خارج از این مرحله: تغییر در نحوه تعریف هدف پروژه یا ذخیره‌سازی آن. کلیدواژه‌ها: هدف پروژه, تطبیق فایل‌ها, metadata پروژه. شواهد در کد: در فایل `backend/app/api/routes/analysis.py` خطوط 46-80 endpoint `POST /analysis/run` تحلیل پروژه را اجرا می‌کند اما هیچ منطقی برای تطبیق فایل‌ها با هدف پروژه وجود ندارد. در خطوط 83-268 endpoint `POST /analysis/run-stream` تحلیل استریم را انجام می‌دهد که فایل‌ها را جمع‌آوری کرده و به `DeepAnalysisService.run_full_analysis` می‌دهد (خط 191-198) اما هدف پروژه از metadata استخراج نمی‌شود. در `backend/app/services/deep_analysis_service.py` (که در خط 120 analysis.py import شده) تابع `run_full_analysis` فایل‌ها و readme_content را دریافت می‌کند اما هدف پروژه را از metadata پروژه نمی‌خواند. مدل `backend/app/models/project.py` (که در 23 فایل import شده) احتمالاً فیلد `goal` یا `purpose` دارد که باید استخراج شود. همچنین در `backend/app/services/project_analyzer.py` (import شده در analysis.py خط 14) تابع `analyze_project` باید اصلاح شود تا هدف پروژه را از metadata بخواند و به تحلیل‌گر ارسال کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در `backend/app/services/project_analyzer.py` تابع `analyze_project` را اصلاح کن تا قبل از تحلیل، هدف پروژه را از metadata پروژه (احتمالاً از مدل `Project` در `backend/app/models/project.py`) استخراج کند. 2. در `backend/app/services/deep_analysis_service.py` تابع `run_full_analysis` را اصلاح کن تا پارامتر `project_goal` را دریافت کند و آن را به مدل AI برای تطبیق فایل‌ها ارسال کند. 3. در `backend/app/api/routes/analysis.py` endpoint `POST /analysis/run` (خط 46) و `POST /analysis/run-stream` (خط 83) را اصلاح کن تا `project_goal` را از metadata پروژه (با استفاده از `project_id`) بخوانند و به سرویس تحلیل پاس دهند. 4. در `backend/app/services/ai_manager.py` (import شده در analysis.py خط 95) یا `backend/app/services/ai_base.py` مطمئن شو که پرامپت ارسالی به مدل شامل هدف پروژه است. 5. در `backend/app/models/project.py` بررسی کن که فیلد `goal` یا `purpose` وجود دارد؛ اگر نه، آن را اضافه کن (اما خارج از scope این تسک است).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 3: اضافه کردن گزینه استفاده از Cloud در صفحه موتور خالق برای ساخت پروژه

**Scope:** این مرحله شامل افزودن یک گزینه یا سوئیچ در صفحه موتور خالق است که به کاربر اجازه می‌دهد هنگام ساخت پروژه‌های جدید از Cloud استفاده کند. این گزینه باید از اشتراک ماهانه Cloud (Token Cloud) استفاده کند و نیازی به هزینه اضافی نداشته باشد. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌های موجود یا بازبینی مجدد. نکته حیاتی: این گزینه باید فقط برای کاربرانی که اشتراک Cloud فعال دارند قابل مشاهده باشد.
— [merged] این مرحله شامل افزودن یک گزینه یا سوئیچ در صفحه موتور خالق است که به کاربر اجازه می‌دهد هنگام نظارت بر پروژه‌های موجود (بازبینی مجدد) از Cloud استفاده کند. این گزینه باید از اشتراک ماهانه Cloud استفاده کند. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌ها. نکته حیاتی: این گزینه باید مستقل از گزینه ساخت پروژه باشد.
**Key terms:** صفحه موتور خالق, ساخت پروژه, بازبینی مجدد, Token Cloud, نظارت بر پروژه‌ها, اشتراک ماهانه

**بخش مربوط از متن کاربر:**
```
الان که توکن کلاد رو دارم و داره تو جاهای مثل اجرای خودکار تسک ها و اجرای تکی تسک ها کار میکنه و از اشتراک ماهانه م ستفاده میکنه و نیاز به هزینه دا نداره، میخوام این گزینه استافده از کلاد هم در این قسمت برای ساخت و همینطور نظارت بر پروژه ها در این قسمت موتور خالق قایل انتخا و استفاده باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن گزینه Cloud به صفحه موتور خالق برای ساخت و نظارت پروژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. صفحه اصلی موتور خالق که فرم ساخت پروژه در آن قرار دارد.
  ```tsx
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/api/routes/creator.py:1-30` — `create_project` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. endpoint ساخت پروژه که باید پارامتر use_cloud را بپذیرد.
  ```python
  // فایل deep-read نشده — بر اساس ساختار سطحی
  ```
- `backend/app/services/creator_engine.py:1-50` — `create_project`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست افزودن یک گزینه یا سوئیچ در صفحه موتور خالق (Creator Page) داده است که به کاربر اجازه می‌دهد هنگام ساخت پروژه‌های جدید و همچنین هنگام نظارت/بازبینی مجدد پروژه‌های موجود از Cloud (Token Cloud) استفاده کند. این گزینه باید از اشتراک ماهانه Cloud (Token Cloud) استفاده کند و نیازی به هزینه اضافی نداشته باشد. نکته حیاتی: این گزینه باید فقط برای کاربرانی که اشتراک Cloud فعال دارند قابل مشاهده باشد و مستقل از گزینه ساخت پروژه باشد. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌های موجود یا بازبینی مجدد. کاربر اشاره کرده که Token Cloud در جاهای دیگر مثل اجرای خودکار تسک‌ها و اجرای تکی تسک‌ها کار می‌کند و از اشتراک ماهانه استفاده می‌کند. کلیدواژه‌ها: صفحه موتور خالق, ساخت پروژه, بازبینی مجدد, Token Cloud, نظارت بر پروژه‌ها, اشتراک ماهانه. در کد فعلی، صفحه creator در frontend/src/app/creator/page.tsx قرار دارد و backend مسیرهای مربوط به creator در backend/app/api/routes/creator.py است. همچنین backend/app/services/creator_engine.py و backend/app/services/creator_idea_to_prompt.py سرویس‌های اصلی ساخت پروژه هستند. برای Cloud Code، endpoint‌هایی مثل GET /api/render/inspector/cloud-code/status و POST /api/render/inspector/chat-cloud-code در README ذکر شده‌اند. فایل backend/app/services/cloud_code_service.py و backend/app/services/cloud_code_executor.py نیز مرتبط هستند. همچنین backend/app/services/claude_service.py برای ارتباط با Claude API استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در frontend/src/app/creator/page.tsx، یک سوئیچ/radio-button با عنوان 'استفاده از Cloud' به فرم ساخت پروژه اضافه کن. این سوئیچ باید فقط زمانی نمایش داده شود که کاربر اشتراک Cloud فعال داشته باشد (با فراخوانی GET /api/render/inspector/cloud-code/status). 2. در backend/app/api/routes/creator.py، endpoint ساخت پروژه (POST /api/creator/create) را به‌روز کن تا پارامتر use_cloud را بپذیرد. 3. در backend/app/services/creator_engine.py، تابع create_project را تغییر بده تا اگر use_cloud=True بود، به جای مدل‌های معمولی از cloud_code_service برای تولید پروژه استفاده کند. 4. برای نظارت/بازبینی مجدد، یک سوئیچ مشابه در صفحه پروژه (frontend/src/app/projects/[id]/page.tsx) اضافه کن که هنگام بازبینی مجدد از Cloud استفاده کند. 5. در backend/app/api/routes/projects.py، endpoint بازبینی مجدد را به‌روز کن تا use_cloud را بپذیرد و به cloud_code_service هدایت کند. 6. اطمینان حاصل کن که cloud_code_service (backend/app/services/cloud_code_service.py) متد جدیدی برای ساخت پروژه و بازبینی مجدد دارد.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 4: اعطای دسترسی‌های مناسب و وسیع به Cloud در صفحه موتور خالق

**Scope:** این مرحله شامل تنظیم دسترسی‌های Cloud در صفحه موتور خالق است تا کاربر بتواند از تمام قابلیت‌های Cloud (مانند اجرای تسک‌ها، بازبینی، و ساخت) استفاده کند. این دسترسی‌ها باید بر اساس اشتراک ماهانه کاربر محدود شوند. خارج از این مرحله: تغییر در نحوه اشتراک‌گذاری پروژه‌ها. نکته حیاتی: دسترسی‌ها باید با سطح اشتراک کاربر هماهنگ باشد.
**Key terms:** دسترسی‌های Cloud, اشتراک ماهانه, صفحه موتور خالق

**بخش مربوط از متن کاربر:**
```
و دسترسی های مناسبو وسیعی داشته باشه
```

## 🎯 هدف (خلاصه ساختاریافته)
اعطای دسترسی‌های Cloud در صفحه موتور خالق بر اساس اشتراک ماهانه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/cloud_code_executor.py:1-50` — `CloudCodeExecutor` — این سرویس اجرای کد در Cloud را مدیریت می‌کند. باید تابع check_cloud_access به آن اضافه شود تا قبل از اجرا، دسترسی کاربر بررسی شود.
  ```python
  class CloudCodeExecutor:
      def __init__(self):
          self.logger = logging.getLogger(__name__)
          self.active_sessions = {}
  
      async def execute_code(self, code: str, language: str, timeout: int = 30):
          ...
  ```
- `backend/app/api/routes/creator.py:1-30` — `router` — این فایل endpointهای مربوط به موتور خالق را تعریف می‌کند. باید یک endpoint جدید برای بررسی دسترسی‌های Cloud اضافه شود.
  ```python
  from fastapi import APIRouter, HTTPException, Depends
  from ...services.creator_engine import CreatorEngine
  from ...services.cloud_code_executor import CloudCodeExecutor
  
  router = APIRouter(prefix="/creator", tags=["Creator"])
  ```
- `frontend/src/app/creator/page.tsx:1-50` — `CreatorPage`
  ```tsx
  export default function CreatorPage() {
    return (
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست 'اعطای دسترسی‌های مناسب و وسیع به Cloud در صفحه موتور خالق' را دارد. این شامل تنظیم دسترسی‌های Cloud در صفحه موتور خالق است تا کاربر بتواند از تمام قابلیت‌های Cloud (مانند اجرای تسک‌ها، بازبینی، و ساخت) استفاده کند. این دسترسی‌ها باید بر اساس اشتراک ماهانه کاربر محدود شوند. خارج از این مرحله: تغییر در نحوه اشتراک‌گذاری پروژه‌ها. نکته حیاتی: دسترسی‌ها باید با سطح اشتراک کاربر هماهنگ باشد. کلیدواژه‌ها: دسترسی‌های Cloud, اشتراک ماهانه, صفحه موتور خالق. در کد فعلی، صفحه موتور خالق در frontend/src/app/creator/page.tsx قرار دارد و از backend/app/api/routes/creator.py برای API استفاده می‌کند. همچنین backend/app/services/creator_engine.py سرویس اصلی تولید پروژه است. backend/app/services/cloud_code_executor.py و backend/app/services/cloud_code_service.py برای اجرای Cloud Code وجود دارند. backend/app/services/notification_service.py برای اطلاع‌رسانی استفاده می‌شود. backend/app/models/setting.py برای ذخیره تنظیمات کاربر و اشتراک استفاده می‌شود. backend/app/core/config.py تنظیمات اصلی را نگه می‌دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در backend/app/models/setting.py یک مدل جدید یا فیلدهای جدید برای ذخیره سطح اشتراک کاربر (مثلاً 'free', 'pro', 'enterprise') و محدودیت‌های دسترسی Cloud (مثلاً max_tasks_per_month, max_reviews_per_month, max_builds_per_month) اضافه کن. 2. در backend/app/services/cloud_code_executor.py یک تابع جدید check_cloud_access(user_id: str, feature: str) -> bool ایجاد کن که سطح اشتراک کاربر را از دیتابیس بخواند و بر اساس آن دسترسی به feature خاص (مثلاً 'run_task', 'review', 'build') را بررسی کند. 3. در backend/app/api/routes/creator.py یک endpoint جدید GET /creator/cloud-access/{user_id} اضافه کن که وضعیت دسترسی‌های Cloud کاربر را برگرداند. 4. در frontend/src/app/creator/page.tsx یک کامپوننت جدید CloudAccessPanel اضافه کن که از endpoint بالا استفاده کند و دسترسی‌های موجود را به کاربر نمایش دهد. 5. در backend/app/services/notification_service.py یک تابع notify_cloud_access_change(user_id, new_level) اضافه کن تا تغییر سطح دسترسی به کاربر اطلاع داده شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. endpoint PATCH /api/review/{review_id}/notes با status 200 پاسخ دهد و یادداشت را در دیتابیس ذخیره کند. _(verify: api_response)_
2. کامپوننت ReviewStatus.tsx یک فیلد متنی (textarea) برای ورود یادداشت نمایش دهد. _(verify: ui_interaction)_

## Task Steps

### Step 1: اضافه کردن قابلیت بازبینی مجدد فایل‌های پروژه در صفحه موتور خالق
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک دکمه یا گزینه جدید در صفحه موتور خالق است که به کاربر اجازه می‌دهد فایل‌های پروژه‌های محلی و غیرمحلی ساخته شده را برای بازبینی مجدد انتخاب کند. این بازبینی باید توسط مدل‌های فعال و انتخاب‌شده در همان صفحه انجام شود. خارج از این مرحله: تغییر در منطق اصلی پروژه‌ها، اضافه کردن قابلیت Cloud، یا تغییر در نحوه ذخیره‌سازی فایل‌ها. نکته حیاتی: این قابلیت باید فقط برای پروژه‌هایی فعال باشد که قبلاً در صفحه موتور خالق ساخته شده‌اند.
**Excerpt:**
```
میخوام جوری بشه که فایل های پروژه های محلی و غیر محلی ساخته شده در اونجا؛ یک بار دیگه بشه توسط مدل هایی که فعال هستن و انتخاب میشن تو اون صفحه؛ بازبینی جدی تری بشن و ایرادات تکنیکی و منطقیشون گرفته بشه
```

### Step 2: تطبیق فایل‌های پروژه با هدف اصلی پروژه در بازبینی مجدد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن منطق تطبیق فایل‌های بازبینی‌شده با هدف اصلی پروژه است. یعنی مدل باید بررسی کند که آیا فایل‌ها با هدف تعریف‌شده برای پروژه همخوانی دارند یا خیر. خارج از این مرحله: تغییر در نحوه تعریف هدف پروژه یا ذخیره‌سازی آن. نکته حیاتی: هدف پروژه باید از metadata پروژه استخراج شود.
**Excerpt:**
```
و با هدفی که براش این فایلها و پروژه ساخته شده تطبیق داده بشه
```

### Step 3: اضافه کردن گزینه استفاده از Cloud در صفحه موتور خالق برای ساخت پروژه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل افزودن یک گزینه یا سوئیچ در صفحه موتور خالق است که به کاربر اجازه می‌دهد هنگام ساخت پروژه‌های جدید از Cloud استفاده کند. این گزینه باید از اشتراک ماهانه Cloud (Token Cloud) استفاده کند و نیازی به هزینه اضافی نداشته باشد. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌های موجود یا بازبینی مجدد. نکته حیاتی: این گزینه باید فقط برای کاربرانی که اشتراک Cloud فعال دارند قابل مشاهده باشد.
— [merged] این مرحله شامل افزودن یک گزینه یا سوئیچ در صفحه موتور خالق است که به کاربر اجازه می‌دهد هنگام نظارت بر پروژه‌های موجود (بازبینی مجدد) از Cloud استفاده کند. این گزینه باید از اشتراک ماهانه Cloud استفاده کند. خارج از این مرحله: تغییر در نحوه ساخت پروژه‌ها. نکته حیاتی: این گزینه باید مستقل از گزینه ساخت پروژه باشد.
**Excerpt:**
```
الان که توکن کلاد رو دارم و داره تو جاهای مثل اجرای خودکار تسک ها و اجرای تکی تسک ها کار میکنه و از اشتراک ماهانه م ستفاده میکنه و نیاز به هزینه دا نداره، میخوام این گزینه استافده از کلاد هم در این قسمت برای ساخت و همینطور نظارت بر پروژه ها در این قسمت موتور خالق قایل انتخا و استفاده باشه
```

### Step 4: اعطای دسترسی‌های مناسب و وسیع به Cloud در صفحه موتور خالق
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تنظیم دسترسی‌های Cloud در صفحه موتور خالق است تا کاربر بتواند از تمام قابلیت‌های Cloud (مانند اجرای تسک‌ها، بازبینی، و ساخت) استفاده کند. این دسترسی‌ها باید بر اساس اشتراک ماهانه کاربر محدود شوند. خارج از این مرحله: تغییر در نحوه اشتراک‌گذاری پروژه‌ها. نکته حیاتی: دسترسی‌ها باید با سطح اشتراک کاربر هماهنگ باشد.
**Excerpt:**
```
و دسترسی های مناسبو وسیعی داشته باشه
```
