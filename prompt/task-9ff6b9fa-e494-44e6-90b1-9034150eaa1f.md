---
task_id: 9ff6b9fa-e494-44e6-90b1-9034150eaa1f
title: ایجاد قابلیتی شبیه به 'Cloud Dreaming' برای بازرس ویژه
type: other
priority: medium
execution_priority: 3000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-20T07:48:40.944221+00:00'
updated_at: '2026-06-03T18:24:27.634921+00:00'
target_files:
- backend/app/models/ai_log.py
- backend/app/models/analysis_report.py
- backend/app/services/ai_manager.py
- backend/app/models/inspector_session.py
- backend/app/services/oversight_upload_session.py
- frontend/src/app/InspectorBridge.tsx
- backend/app/api/routes/oversight.py
- backend/app/api/routes/analysis.py
- backend/app/api/routes/chat.py
- backend/app/services/verify_runtime/context_builder.py
- backend/tests/test_dreaming_agent_models.py
- backend/app/services/oversight_service.py
- backend/app/services/oversight_deep_scan_service.py
---

# ایجاد قابلیتی شبیه به 'Cloud Dreaming' برای بازرس ویژه

## Raw Idea

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_23712_AgADVx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد_

## درخواست اصلی کاربر  _(at: audio/ogg)_

درخواست اصلی کاربر:

[ 0m1s654ms - 0m8s644ms ] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[ 0m8s644ms - 0m27s594ms ] و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت بکنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[ 0m27s594ms - 0m32s474ms ] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو برداشت بکنن.
[ 0m33s744ms - 0m48s544ms ] می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[ 0m48s544ms - 0m58s944ms ] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[ 0m58s944ms - 1m11s444ms ] که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[ 1m11s444ms - 1m23s694ms ] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه
[ 1m23s694ms - 1m31s944ms ] و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[ 1m32s294ms - 1m38s844ms ] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[ 1m39s944ms - 1m42s694ms ] حالا امیدوارم متوجه شده باشید.
[ 1m43s294ms - 1m49s294ms ] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[ 1m49s294ms - 1m54s594ms ] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کار رو پیدا بکنه.
[ 1m54s594ms - 2m0s794ms ] یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کار رو اشتباه انجام داده.
[ 2m1s494ms - 2m3s894ms ] یا چیزای دیگه که می‌تونسته بهتر باشه.
[ 2m3s894ms - 2m18s794ms ] و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[ 2m19s294ms - 2m21s194ms ] یعنی مراجعه داشته باشن.
[ 2m21s194ms - 2m25s144ms ] البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## مراحل مورد نظر برای پردازش  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal مرتبط با heading "مراحل مورد نظر برای پردازش" ارائه شده است:

[00:01.600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ
[00:08.700] و کارش اینه که
[00:10.700] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه، ایجنت‌هاش برن توی سیشن‌ها
[00:17.100] و چت‌ها رو بررسی بکنن و
[00:19.800] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[00:26.500] بهبود بدن.
[00:27.600] از اون سیشن بتونن و چت‌هاش بتونن
[00:30.200] نکات مهمی رو برداشت بکنن.
[00:33.700] می‌خواستم همچین چیزی
[00:36.100] برای قسمت بازرس ویژه
[00:38.900] یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه
[00:43.300] تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48.900] اونجا مثلاً یه
[00:50.400] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54.600] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00.900] که بایگانی شدن
[01:03.400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08.500] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[01:12.300] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[01:16.500] پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[01:22.600] اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه
[01:28.700] بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32.400] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[01:36.000] چت کار بکنن از اون استفاده بکنن.
[01:39.900] حالا امیدوارم متوجه شده باشید.
[01:43.300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49.600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره
[01:52.900] ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که
[01:56.800] مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01.400] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04.200] و اینها رو به عنوان نکات آموزشی یا نکات
[02:07.900] برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[02:13.300] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19.200] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## اقلام اطلاعاتی مورد نیاز از تحلیل  _(at: audio/ogg)_

در ادامه رونویسی کامل و literal از فایل صوتی ارائه شده است:

[00:01.600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[00:08.700] و کارش اینه که
[00:10.800] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها
[00:17.000] و چت‌ها رو بررسی بکنن و
[00:19.800] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[00:26.700] بهبود بدن. از اون سیشن بتونن و چت‌هاش بتونن
[00:30.200] نکات مهمی رو برداشت بکنن.
[00:33.800] می‌خواستم همچین چیزی
[00:36.200] برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه
[00:43.300] تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48.900] اونجا مثلاً یه
[00:50.500] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54.700] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00.800] که بایگانی شدن
[01:03.400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08.500] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[01:12.300] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[01:16.600] پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[01:22.400] اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه
[01:28.600] بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32.400] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[01:36.000] چت کار بکنن از اون استفاده بکنن.
[01:39.900] حالا امیدوارم متوجه شده باشی.
[01:43.300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49.600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که
[01:57.000] مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01.400] یا چیزای دیگه که می‌تونسته بهتر باشه. و اینها رو به عنوان نکات آموزشی یا نکات
[02:08.200] برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[02:13.300] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19.300] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## خروجی‌های نهایی درخواستی  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

[ 0m1s506ms ] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد
[ 0m4s466ms ] یه قابلیتی داره تحت عنوان کلاد دریمینگ
[ 0m8s466ms ] و کارش اینه که
[ 0m10s766ms ] بتونه
[ 0m11s766ms ] مثلاً زمان‌هایی که حالا
[ 0m13s166ms ] کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها
[ 0m17s16ms ] و چت‌ها رو بررسی بکنن و
[ 0m19s666ms ] ایرادها رو بگیرن یا یه سری نکات برداشت بکنن نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[ 0m26s466ms ] بهبود بدن
[ 0m27s466ms ] از اون سیشن بتونن و چت‌هاش بتونن
[ 0m30s266ms ] نکات مهمی رو قابل برداشت بکنن
[ 0m33s766ms ] می‌خواستم همچین چیزی
[ 0m36s266ms ] برای قسمت بازرس ویژه
[ 0m38s866ms ] یعنی توی
[ 0m40s166ms ] صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[ 0m48s866ms ] اونجا مثلاً یه
[ 0m50s366ms ] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[ 0m54s566ms ] که این کارش همین باشه مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[ 1m0s966ms ] که بایگانی شدن
[ 1m3s466ms ] یا حتی سیشن فعالی که
[ 1m5s466ms ] چت‌هاش مثلاً تا اون لحظه انجام شده
[ 1m8s366ms ] بتونه نکات رو یادداشت بکنه نکات رو برداشت بکنه حافظه رو
[ 1m11s966ms ] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[ 1m16s566ms ] پیدا بکنه چیزایی که
[ 1m18s166ms ] می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[ 1m22s266ms ] اونها رو پیدا بکنه
[ 1m23s866ms ] و اینها رو ذیل تب
[ 1m25s666ms ] ذیل اون
[ 1m27s166ms ] خود صفحه
[ 1m28s466ms ] بازرس ویژه یه جا هست به اسم آموزشی و حافظه
[ 1m32s166ms ] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[ 1m36s166ms ] چت کار بکنن از اون استفاده بکنن
[ 1m39s966ms ] حالا امیدوارم متوجه شده باشید
[ 1m43s366ms ] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه
[ 1m49s166ms ] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره
[ 1m52s866ms ] ریشه کارو پیدا بکنه یعنی ریشه مثلاً فلان مشکل چی بوده که
[ 1m56s866ms ] مدل اینجا تو این سیشن
[ 1m58s466ms ] کارو اشتباه انجام داده
[ 2m1s466ms ] یا چیزای دیگه که می‌تونسته بهتر باشه
[ 2m3s866ms ] و اینها رو به عنوان نکات آموزشی یا نکات
[ 2m7s666ms ] برای حافظه
[ 2m9s666ms ] تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[ 2m13s366ms ] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون
[ 2m17s466ms ] استفاده بکنن
[ 2m19s166ms ] یعنی مراجعه داشته باشن
[ 2m21s16ms ] البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم

## تأکیدات کیفی  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

**HEADING: تأکیدات کیفی**

[00:01:600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ و کارش اینه که
[00:10:600] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و
[00:19:500] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[00:27:600] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو قابل برداشت بکنن.
[00:33:800] می‌خواستم همچین چیزی
[00:36:200] برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48:900] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54:500] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00:900] که بایگانی شدن
[01:03:400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32:200] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[01:39:900] حالا امیدوارم متوجه شده باشی.
[01:43:300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49:600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01:200] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04:100] و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19:300] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## هدف نهایی از درخواست  _(at: audio/ogg)_

**HEADING: هدف نهایی از درخواست**

[00:00] (صدای محیط)
[00:01] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[00:08] و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه، ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[00:27] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو برداشت بکنن.
[00:33] می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌هایی که هست و چت‌هایی که انجام میشه
[00:48] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[00:59] که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه.
[01:17] چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اون‌ها رو پیدا بکنه
[01:23] و این‌ها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[01:39] حالا امیدوارم متوجه شده باشی.
[01:43] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه.
[01:54] یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04] و این‌ها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.
--------------
نکته مهم اینکه موارد استخراج شده باید طوری عمومیت داشتهباشن که همیشه و مورد استفاده باشن ... یعنی برای یه مشکل به خصوص کا گاهی در سیستم ها ممکن ایجاد بشه ، ردیف های استخراج شده  توسط مدل ها از سیشن های قبلی بتونه یه چراغ راه باشه که اون اشتباه هات قبلی رو در قبل انجام شده تکرار نشه و یا روش هایی که انجام شده و مشکل حل شده رو که اونها هم عمومیت دارن و بت شدن باید قرا داده بشه تا مورد مراجعه در زمان مناسبش باشه
اینکه هر سیشن و پیام هایی که استخراج لازم ازشون انجام شده یا به هر نحوی بررسی شده باید طوری علامت بخورن که دیگه هر بار مراجه انجام نشه ... مگر برای سیشن باز موجود که ممکن بررسی شده باشه پیامهاش ولی چون هنوز ارشیو نشده ممکن لازم باشه بازم بعدا بررسی بشه و فقط  وقتی ارشیو شدن و بررسی هم شده باشن همه چیز دفعه بعدیش بررسی نمیشه

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


---

## 📥 درخواست خام کاربر (verbatim — همان متنی که کاربر نوشت)
_(همهٔ URL ها، آدرس‌ها، نام‌ها، و کلمات کلیدی در این متن دست‌نخورده هستند.)_

```
[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_23712_AgADVx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 6 segment استخراج شد_

## درخواست اصلی کاربر  _(at: audio/ogg)_

درخواست اصلی کاربر:

[ 0m1s654ms - 0m8s644ms ] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[ 0m8s644ms - 0m27s594ms ] و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت بکنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[ 0m27s594ms - 0m32s474ms ] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو برداشت بکنن.
[ 0m33s744ms - 0m48s544ms ] می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[ 0m48s544ms - 0m58s944ms ] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[ 0m58s944ms - 1m11s444ms ] که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[ 1m11s444ms - 1m23s694ms ] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه
[ 1m23s694ms - 1m31s944ms ] و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[ 1m32s294ms - 1m38s844ms ] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[ 1m39s944ms - 1m42s694ms ] حالا امیدوارم متوجه شده باشید.
[ 1m43s294ms - 1m49s294ms ] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[ 1m49s294ms - 1m54s594ms ] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کار رو پیدا بکنه.
[ 1m54s594ms - 2m0s794ms ] یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کار رو اشتباه انجام داده.
[ 2m1s494ms - 2m3s894ms ] یا چیزای دیگه که می‌تونسته بهتر باشه.
[ 2m3s894ms - 2m18s794ms ] و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[ 2m19s294ms - 2m21s194ms ] یعنی مراجعه داشته باشن.
[ 2m21s194ms - 2m25s144ms ] البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## مراحل مورد نظر برای پردازش  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal مرتبط با heading "مراحل مورد نظر برای پردازش" ارائه شده است:

[00:01.600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ
[00:08.700] و کارش اینه که
[00:10.700] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه، ایجنت‌هاش برن توی سیشن‌ها
[00:17.100] و چت‌ها رو بررسی بکنن و
[00:19.800] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[00:26.500] بهبود بدن.
[00:27.600] از اون سیشن بتونن و چت‌هاش بتونن
[00:30.200] نکات مهمی رو برداشت بکنن.
[00:33.700] می‌خواستم همچین چیزی
[00:36.100] برای قسمت بازرس ویژه
[00:38.900] یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه
[00:43.300] تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48.900] اونجا مثلاً یه
[00:50.400] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54.600] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00.900] که بایگانی شدن
[01:03.400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08.500] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[01:12.300] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[01:16.500] پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[01:22.600] اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه
[01:28.700] بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32.400] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[01:36.000] چت کار بکنن از اون استفاده بکنن.
[01:39.900] حالا امیدوارم متوجه شده باشید.
[01:43.300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49.600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره
[01:52.900] ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که
[01:56.800] مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01.400] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04.200] و اینها رو به عنوان نکات آموزشی یا نکات
[02:07.900] برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[02:13.300] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19.200] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## اقلام اطلاعاتی مورد نیاز از تحلیل  _(at: audio/ogg)_

در ادامه رونویسی کامل و literal از فایل صوتی ارائه شده است:

[00:01.600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[00:08.700] و کارش اینه که
[00:10.800] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها
[00:17.000] و چت‌ها رو بررسی بکنن و
[00:19.800] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[00:26.700] بهبود بدن. از اون سیشن بتونن و چت‌هاش بتونن
[00:30.200] نکات مهمی رو برداشت بکنن.
[00:33.800] می‌خواستم همچین چیزی
[00:36.200] برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه
[00:43.300] تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48.900] اونجا مثلاً یه
[00:50.500] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54.700] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00.800] که بایگانی شدن
[01:03.400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08.500] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو
[01:12.300] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[01:16.600] پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[01:22.400] اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه
[01:28.600] بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32.400] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[01:36.000] چت کار بکنن از اون استفاده بکنن.
[01:39.900] حالا امیدوارم متوجه شده باشی.
[01:43.300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49.600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که
[01:57.000] مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01.400] یا چیزای دیگه که می‌تونسته بهتر باشه. و اینها رو به عنوان نکات آموزشی یا نکات
[02:08.200] برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[02:13.300] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19.300] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## خروجی‌های نهایی درخواستی  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

[ 0m1s506ms ] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد
[ 0m4s466ms ] یه قابلیتی داره تحت عنوان کلاد دریمینگ
[ 0m8s466ms ] و کارش اینه که
[ 0m10s766ms ] بتونه
[ 0m11s766ms ] مثلاً زمان‌هایی که حالا
[ 0m13s166ms ] کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها
[ 0m17s16ms ] و چت‌ها رو بررسی بکنن و
[ 0m19s666ms ] ایرادها رو بگیرن یا یه سری نکات برداشت بکنن نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً
[ 0m26s466ms ] بهبود بدن
[ 0m27s466ms ] از اون سیشن بتونن و چت‌هاش بتونن
[ 0m30s266ms ] نکات مهمی رو قابل برداشت بکنن
[ 0m33s766ms ] می‌خواستم همچین چیزی
[ 0m36s266ms ] برای قسمت بازرس ویژه
[ 0m38s866ms ] یعنی توی
[ 0m40s166ms ] صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[ 0m48s866ms ] اونجا مثلاً یه
[ 0m50s366ms ] یکی از مدل‌های فعال موجود رو انتخاب بکنم
[ 0m54s566ms ] که این کارش همین باشه مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[ 1m0s966ms ] که بایگانی شدن
[ 1m3s466ms ] یا حتی سیشن فعالی که
[ 1m5s466ms ] چت‌هاش مثلاً تا اون لحظه انجام شده
[ 1m8s366ms ] بتونه نکات رو یادداشت بکنه نکات رو برداشت بکنه حافظه رو
[ 1m11s966ms ] چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو
[ 1m16s566ms ] پیدا بکنه چیزایی که
[ 1m18s166ms ] می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه
[ 1m22s266ms ] اونها رو پیدا بکنه
[ 1m23s866ms ] و اینها رو ذیل تب
[ 1m25s666ms ] ذیل اون
[ 1m27s166ms ] خود صفحه
[ 1m28s466ms ] بازرس ویژه یه جا هست به اسم آموزشی و حافظه
[ 1m32s166ms ] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون
[ 1m36s166ms ] چت کار بکنن از اون استفاده بکنن
[ 1m39s966ms ] حالا امیدوارم متوجه شده باشید
[ 1m43s366ms ] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه
[ 1m49s166ms ] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره
[ 1m52s866ms ] ریشه کارو پیدا بکنه یعنی ریشه مثلاً فلان مشکل چی بوده که
[ 1m56s866ms ] مدل اینجا تو این سیشن
[ 1m58s466ms ] کارو اشتباه انجام داده
[ 2m1s466ms ] یا چیزای دیگه که می‌تونسته بهتر باشه
[ 2m3s866ms ] و اینها رو به عنوان نکات آموزشی یا نکات
[ 2m7s666ms ] برای حافظه
[ 2m9s666ms ] تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون
[ 2m13s366ms ] سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون
[ 2m17s466ms ] استفاده بکنن
[ 2m19s166ms ] یعنی مراجعه داشته باشن
[ 2m21s16ms ] البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم

## تأکیدات کیفی  _(at: audio/ogg)_

در ادامه رونویسی/توصیف کامل و literal از فایل صوتی ارائه شده است:

**HEADING: تأکیدات کیفی**

[00:01:600] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ و کارش اینه که
[00:10:600] بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و
[00:19:500] ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[00:27:600] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو قابل برداشت بکنن.
[00:33:800] می‌خواستم همچین چیزی
[00:36:200] برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه
[00:48:900] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم
[00:54:500] که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[01:00:900] که بایگانی شدن
[01:03:400] یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32:200] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[01:39:900] حالا امیدوارم متوجه شده باشی.
[01:43:300] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49:600] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01:200] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04:100] و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19:300] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.

## هدف نهایی از درخواست  _(at: audio/ogg)_

**HEADING: هدف نهایی از درخواست**

[00:00] (صدای محیط)
[00:01] یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیت تحت عنوان کلاد دریمینگ
[00:08] و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه، ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت کنن، نکات مثلاً یادداشت بکنن توی حافظه رو مثلاً بهبود بدن.
[00:27] از اون سیشن بتونن و چت‌هاش بتونن نکات مهمی رو برداشت بکنن.
[00:33] می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌هایی که هست و چت‌هایی که انجام میشه
[00:48] اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
[00:59] که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
[01:08] بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه.
[01:17] چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اون‌ها رو پیدا بکنه
[01:23] و این‌ها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه.
[01:32] اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
[01:39] حالا امیدوارم متوجه شده باشی.
[01:43] یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه.
[01:49] یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه.
[01:54] یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده.
[02:01] یا چیزای دیگه که می‌تونسته بهتر باشه.
[02:04] و این‌ها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن.
[02:19] یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.
--------------
نکته مهم اینکه موارد استخراج شده باید طوری عمومیت داشتهباشن که همیشه و مورد استفاده باشن ... یعنی برای یه مشکل به خصوص کا گاهی در سیستم ها ممکن ایجاد بشه ، ردیف های استخراج شده  توسط مدل ها از سیشن های قبلی بتونه یه چراغ راه باشه که اون اشتباه هات قبلی رو در قبل انجام شده تکرار نشه و یا روش هایی که انجام شده و مشکل حل شده رو که اونها هم عمومیت دارن و بت شدن باید قرا داده بشه تا مورد مراجعه در زمان مناسبش باشه
اینکه هر سیشن و پیام هایی که استخراج لازم ازشون انجام شده یا به هر نحوی بررسی شده باید طوری علامت بخورن که دیگه هر بار مراجه انجام نشه ... مگر برای سیشن باز موجود که ممکن بررسی شده باشه پیامهاش ولی چون هنوز ارشیو نشده ممکن لازم باشه بازم بعدا بررسی بشه و فقط  وقتی ارشیو شدن و بررسی هم شده باشن همه چیز دفعه بعدیش بررسی نمیشه
```

## 📋 چک‌لیست مراحل (15 مرحله)

این تسک به مراحل کوچک‌تر تقسیم شده. **در هر verify خودکار، وضعیت هر مرحله به‌صورت `[ ]` (انجام نشده)، `[~]` (ناقص)، یا `[x]` (انجام شده) به‌روز می‌شود.**
وقتی تمام مراحل `[x]` شدند، تسک به‌طور خودکار به «انجام شده» منتقل می‌شود.

- [ ] **مرحله 1: ایجاد دیتاکلاس و ساختار داده برای Dreaming Agent Task و نتایج تحلیل** — این مرحله شامل تعریف دیتاکلاس‌های اصلی برای نمایش یک Task تحلیل (Dreaming Agent Task) و نتایج آن (AnalysisResult) است. این دیتاکلاس‌ها باید شامل فیلدهایی برای وضعیت (pending/running/completed/failed)، نوع تحلیل (educational/memory)، session_id، chat_id، لیست نکات استخراج‌شده، و timestamp باشند. خارج
- [ ] **مرحله 2: ایجاد جدول دیتابیس برای ذخیره Task‌ها و نتایج تحلیل Dreaming Agent** — این مرحله شامل ایجاد یک مدل دیتابیس (مثلاً با SQLAlchemy یا Prisma) برای ذخیره‌سازی پایدار Task‌های Dreaming Agent و نتایج تحلیل آنهاست. فیلدها باید شامل: id, session_id, chat_id, task_type (educational/memory), status, result (JSON), created_at, updated_at, analyzed_at باشند. خارج از این مرحله: مها
- [ ] **مرحله 3: ایجاد سرویس اصلی Dreaming Agent با قابلیت انتخاب مدل فعال** — این مرحله شامل ایجاد یک سرویس (کلاس یا ماژول) به نام DreamingAgentService است که وظیفه اصلی آن مدیریت فرآیند تحلیل است. این سرویس باید قابلیت دریافت یک مدل فعال (از بین مدل‌های موجود در سیستم) را داشته باشد و بتواند یک Task تحلیل جدید ایجاد کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌ب
- [ ] **مرحله 4: پیاده‌سازی منطق تحلیل session‌های بایگانی‌شده برای استخراج نکات آموزشی و حافظه** — این مرحله شامل پیاده‌سازی منطق اصلی تحلیل است. این منطق باید بتواند session‌های بایگانی‌شده (archived) را دریافت کرده، پیام‌های آنها را بررسی کند، و نکات آموزشی (educational) و نکات مرتبط با حافظه (memory) را استخراج کند. نکات آموزشی باید ریشه مشکلات (root cause) را شناسایی کنند و نکات حافظه باید اط
- [ ] **مرحله 5: پیاده‌سازی منطق تحلیل session‌های فعال برای استخراج نکات تا لحظه تحلیل** — این مرحله شامل پیاده‌سازی منطق تحلیل برای session‌های فعال (active) است. این منطق باید بتواند session فعال را دریافت کرده، پیام‌های موجود تا لحظه تحلیل را بررسی کند، و نکات آموزشی و حافظه را استخراج کند. خارج از این مرحله: تحلیل session‌های بایگانی‌شده، UI، و ذخیره‌سازی نتایج. نکته حیاتی: این تحلیل 
- [ ] **مرحله 6: ایجاد مکانیزم علامت‌گذاری session‌ها و پیام‌های تحلیل‌شده برای جلوگیری از تحلیل مجدد** — این مرحله شامل پیاده‌سازی یک مکانیزم علامت‌گذاری (flagging) برای session‌ها و پیام‌هایی است که قبلاً تحلیل شده‌اند. این مکانیزم باید از تحلیل مجدد session‌های بایگانی‌شده و کاملاً تحلیل‌شده جلوگیری کند. برای session‌های فعال، باید پیام‌های جدید (تحلیل‌نشده) را شناسایی کرده و فقط آنها را تحلیل کند. خ
- [ ] **مرحله 7: ایجاد زیرساخت زمان‌بندی (Scheduler) برای اجرای خودکار Dreaming Agent در زمان‌های بیکاری** — این مرحله شامل ایجاد یک زمان‌بند (scheduler) است که به صورت خودکار Dreaming Agent را در زمان‌های مشخص (مثلاً وقتی کاربر آفلاین است یا سیستم بیکار است) اجرا کند. این زمان‌بند باید session‌های بایگانی‌شده و فعال را برای تحلیل انتخاب کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و انتخاب مدل. نکته
- [ ] **مرحله 8: ایجاد تب 'آموزشی و حافظه' در صفحه بازرس ویژه (UI)** — این مرحله شامل ایجاد یک تب جدید به نام 'آموزشی و حافظه' (Educational & Memory) در صفحه بازرس ویژه (Inspector Page) است. این تب باید نتایج تحلیل‌های Dreaming Agent را به صورت دسته‌بندی‌شده (educational و memory) نمایش دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، انتخاب مدل، و زمان‌بندی. نکته حیاتی:
- [ ] **مرحله 9: ایجاد UI برای انتخاب مدل فعال و تنظیمات Dreaming Agent در تب بازرس ویژه** — این مرحله شامل ایجاد یک بخش UI در تب بازرس ویژه (یا در تنظیمات پروژه) است که به کاربر اجازه می‌دهد یک مدل فعال را از بین مدل‌های موجود انتخاب کند و تنظیمات مربوط به Dreaming Agent (مانند نوع تحلیل، session‌های هدف) را پیکربندی کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و نمایش نتایج. 
- [ ] **مرحله 10: اتصال UI انتخاب مدل و تنظیمات به سرویس Dreaming Agent (Backend Integration)** — این مرحله شامل ایجاد API endpoints برای ذخیره و بازیابی تنظیمات Dreaming Agent (مدل انتخاب‌شده، نوع تحلیل، session‌های هدف) و اتصال UI به این endpoints است. همچنین شامل ایجاد یک endpoint برای شروع دستی یک Task تحلیل است. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و UI. نکته حیاتی: تنظیمات 
- [ ] **مرحله 11: ایجاد مکانیزم مراجعه مدل‌ها به نتایج تحلیل (Memory Retrieval) در حین چت** — این مرحله شامل پیاده‌سازی یک مکانیزم است که به مدل‌های فعال در یک session اجازه می‌دهد در حین چت به نتایج تحلیل‌های قبلی (educational و memory) مراجعه کنند. این مکانیزم باید نتایج مرتبط با session فعلی یا session‌های مشابه را بازیابی کرده و در context چت قرار دهد. خارج از این مرحله: پیاده‌سازی منطق 
- [ ] **مرحله 12: نوشتن تست‌های واحد برای دیتاکلاس‌ها و مدل دیتابیس Dreaming Agent** — این مرحله شامل نوشتن تست‌های واحد برای دیتاکلاس‌های ایجادشده (DreamingAgentTask, AnalysisResult) و مدل دیتابیس است. تست‌ها باید ایجاد، به‌روزرسانی، و بازیابی اشیا را پوشش دهند. خارج از این مرحله: تست‌های integration، تست‌های UI، و تست‌های منطق تحلیل. نکته حیاتی: تست‌ها باید مستقل از دیتابیس واقعی با
- [ ] **مرحله 13: نوشتن تست‌های واحد برای سرویس Dreaming Agent** — این مرحله شامل نوشتن تست‌های واحد برای DreamingAgentService است. تست‌ها باید ایجاد Task، انتخاب مدل، و فراخوانی متدهای تحلیل را با استفاده از mock پوشش دهند. خارج از این مرحله: تست‌های integration با مدل واقعی، تست‌های UI، و تست‌های زمان‌بندی. نکته حیاتی: از mock برای شبیه‌سازی مدل و session‌ها استف
- [ ] **مرحله 14: نوشتن تست‌های integration برای فرآیند کامل Dreaming Agent** — این مرحله شامل نوشتن تست‌های integration است که فرآیند کامل Dreaming Agent را از ایجاد Task تا ذخیره نتایج و نمایش در UI پوشش می‌دهد. این تست‌ها باید با دیتابیس واقعی (یا test database) و مدل واقعی (یا mock نزدیک به واقعیت) اجرا شوند. خارج از این مرحله: تست‌های واحد و تست‌های UI. نکته حیاتی: این تست
- [ ] **مرحله 15: بازبینی نهایی (Audit) و مستندسازی Dreaming Agent** — این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده برای Dreaming Agent، بررسی امنیت، performance، و مستندسازی است. همچنین شامل به‌روزرسانی مستندات پروژه و اضافه کردن توضیحات برای استفاده از این قابلیت است. خارج از این مرحله: نوشتن کد جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code rev

---

# 🔹 مرحله 1: ایجاد دیتاکلاس و ساختار داده برای Dreaming Agent Task و نتایج تحلیل

**Scope:** این مرحله شامل تعریف دیتاکلاس‌های اصلی برای نمایش یک Task تحلیل (Dreaming Agent Task) و نتایج آن (AnalysisResult) است. این دیتاکلاس‌ها باید شامل فیلدهایی برای وضعیت (pending/running/completed/failed)، نوع تحلیل (educational/memory)، session_id، chat_id، لیست نکات استخراج‌شده، و timestamp باشند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی در دیتابیس. نکته حیاتی: این دیتاکلاس‌ها باید به‌گونه‌ای طراحی شوند که هم برای تحلیل session‌های بایگانی‌شده و هم session‌های فعال قابل استفاده باشند.
**Key terms:** DreamingAgentTask, AnalysisResult, session_id, chat_id, task_status, analysis_type, educational, memory

**بخش مربوط از متن کاربر:**
```
می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه
```

## 🎯 هدف (خلاصه ساختاریافته)
تعریف دیتاکلاس‌های DreamingAgentTask و AnalysisResult برای تحلیل session‌های بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد دیتاکلاس و ساختار داده برای Dreaming Agent Task و نتایج تحلیل. این مرحله شامل تعریف دیتاکلاس‌های اصلی برای نمایش یک Task تحلیل (Dreaming Agent Task) و نتایج آن (AnalysisResult) است. این دیتاکلاس‌ها باید شامل فیلدهایی برای وضعیت (pending/running/completed/failed)، نوع تحلیل (educational/memory)، session_id، chat_id، لیست نکات استخراج‌شده، و timestamp باشند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی در دیتابیس. نکته حیاتی: این دیتاکلاس‌ها باید به‌گونه‌ای طراحی شوند که هم برای تحلیل session‌های بایگانی‌شده و هم session‌های فعال قابل استفاده باشند. کاربر در بخش مربوط از درخواست اصلی خود اشاره کرده: «می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه». کلیدواژه‌های اصلی: DreamingAgentTask, AnalysisResult, session_id, chat_id, task_status, analysis_type, educational, memory. در کد فعلی پروژه، فایل‌های backend/app/api/routes/oversight.py و backend/app/services/oversight_service.py و backend/app/services/oversight_deep_scan_service.py و backend/app/services/oversight_inspector_bridge.py و backend/app/services/oversight_extraction.py و backend/app/services/oversight_codex_service.py و backend/app/services/oversight_verifier.py و backend/app/services/oversight_strong_prompt.py و backend/app/services/oversight_progress.py و backend/app/services/oversight_settings.py و backend/app/services/oversight_telegram_compose.py و backend/app/services/oversight_upload_session.py و backend/app/services/oversight_verify_pdf.py و backend/app/services/oversight_model_temp_activate.py و backend/app/services/oversight_mega_bundle.py و backend/app/models/inspector_session.py و backend/app/models/inspector_prompt_field.py و backend/app/models/ai_log.py و backend/app/models/ai_profile.py و backend/app/models/project.py و backend/app/models/debate.py و backend/app/models/analysis_report.py و backend/app/models/render_log.py و backend/app/models/setting.py و backend/app/models/system_prompt.py و backend/app/core/database.py و backend/app/core/config.py و backend/app/core/models_registry.py و backend/app/core/roles.py و backend/app/services/ai_manager.py و backend/app/services/ai_base.py و backend/app/services/ai_balance_service.py و backend/app/services/capability_detector.py و backend/app/services/model_capability_tester.py و backend/app/services/model_profiler.py و backend/app/services/scan_sections.py و backend/app/services/scan_v5/scan_bundle.py و backend/app/services/scan_v5/scan_inspector_session.py و backend/app/services/scan_v5/comprehensive_inventory.py و backend/app/services/scan_v5/dependency_analyzer.py و backend/app/services/scan_v5/coherence_analyzer.py و backend/app/services/scan_v5/anti_pattern_detector.py و backend/app/services/scan_v5/feature_documenter.py و backend/app/services/scan_v5/outcome_analyzer.py و backend/app/services/scan_v5/purpose_extractor.py و backend/app/services/scan_v5/runtime_discovery.py و backend/app/services/scan_v

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
_(مجری بر اساس Context و معیارهای پذیرش، مراحل را تعیین کند)_

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 2: ایجاد جدول دیتابیس برای ذخیره Task‌ها و نتایج تحلیل Dreaming Agent

**Scope:** این مرحله شامل ایجاد یک مدل دیتابیس (مثلاً با SQLAlchemy یا Prisma) برای ذخیره‌سازی پایدار Task‌های Dreaming Agent و نتایج تحلیل آنهاست. فیلدها باید شامل: id, session_id, chat_id, task_type (educational/memory), status, result (JSON), created_at, updated_at, analyzed_at باشند. خارج از این مرحله: مهاجرت دیتابیس، پیاده‌سازی منطق تحلیل، و UI. نکته حیاتی: باید قابلیت ذخیره نتایج به صورت JSON را داشته باشد تا نکات استخراج‌شده به صورت ساختاریافته ذخیره شوند.
**Key terms:** DreamingAgentTask, database_model, session_id, chat_id, task_type, status, result, JSON

**بخش مربوط از متن کاربر:**
```
و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن. یعنی مراجعه داشته باشن.
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد مدل دیتابیس DreamingAgentTask برای ذخیره Task و نتایج تحلیل

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/ai_log.py:1-50` — `AILog` — این فایل الگوی مناسبی برای مدل DreamingAgentTask است. فیلدهای session_id, chat_id, result (JSON) دقیقاً مشابه نیاز کاربر است.
  ```python
  class AILog(Base):
      __tablename__ = 'ai_logs'
      id = Column(String, primary_key=True, default=lambda: str(uuid4()))
      session_id = Column(String, index=True)
      chat_id = Column(String, index=True)
      result = Column(JSON, nullable=True)
      created_at = Column(DateTime, default=datetime.utcnow)
  ```
- `backend/app/models/analysis_report.py:1-80` — `AnalysisReport`
  ```python
  class AnalysisReport(Base):
      __tablename__ = 'analysis_reports'
      id = Column(String, primary_key=True, default=lambda: str(uuid4()))
      status = Column(String, default='pending')
      result = Column(
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد یک جدول دیتابیس برای ذخیره Task‌ها و نتایج تحلیل Dreaming Agent را دارد. این مرحله شامل ایجاد یک مدل دیتابیس (مثلاً با SQLAlchemy) برای ذخیره‌سازی پایدار Task‌های Dreaming Agent و نتایج تحلیل آنهاست. فیلدها باید شامل: id, session_id, chat_id, task_type (educational/memory), status, result (JSON), created_at, updated_at, analyzed_at باشند. خارج از این مرحله: مهاجرت دیتابیس، پیاده‌سازی منطق تحلیل، و UI. نکته حیاتی: باید قابلیت ذخیره نتایج به صورت JSON را داشته باشد تا نکات استخراج‌شده به صورت ساختاریافته ذخیره شوند. کاربر همچنین اشاره کرده: "و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن. یعنی مراجعه داشته باشن." کلیدواژه‌ها: DreamingAgentTask, database_model, session_id, chat_id, task_type, status, result, JSON. در کد فعلی پروژه، مدل‌های دیتابیس مشابهی در backend/app/models/ وجود دارند مانند ai_log.py (خط 1-50) که دارای فیلدهای session_id, chat_id, result (JSON) هستند و می‌توان از آن‌ها به عنوان الگو استفاده کرد. همچنین در backend/app/models/analysis_report.py (خط 1-80) مدل AnalysisReport دارای فیلدهای result به صورت JSON است. در backend/app/core/database.py (خط 1-40) SessionLocal و get_db تعریف شده‌اند که برای ایجاد session دیتابیس استفاده می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/models/dreaming_agent_task.py با مدل SQLAlchemy DreamingAgentTask شامل فیلدهای: id (UUID primary key), session_id (String, indexed), chat_id (String, indexed), task_type (String, enum: 'educational'/'memory'), status (String, enum: 'pending'/'processing'/'completed'/'failed'), result (JSON/Text, nullable), created_at (DateTime, default=utcnow), updated_at (DateTime, onupdate=utcnow), analyzed_at (DateTime, nullable). 2. به‌روزرسانی backend/app/models/__init__.py برای import مدل جدید. 3. اضافه کردن enum کلاس TaskType و TaskStatus در همان فایل. 4. استفاده از SQLAlchemy's Column(JSON) یا Text برای ذخیره result به صورت JSON. 5. اطمینان از compatibility با دیتابیس SQLite فعلی (که JSON را به صورت Text ذخیره می‌کند). 6. افزودن relationship به chat/session در صورت نیاز (اختیاری).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: small

---

# 🔹 مرحله 3: ایجاد سرویس اصلی Dreaming Agent با قابلیت انتخاب مدل فعال

**Scope:** این مرحله شامل ایجاد یک سرویس (کلاس یا ماژول) به نام DreamingAgentService است که وظیفه اصلی آن مدیریت فرآیند تحلیل است. این سرویس باید قابلیت دریافت یک مدل فعال (از بین مدل‌های موجود در سیستم) را داشته باشد و بتواند یک Task تحلیل جدید ایجاد کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: این سرویس باید به‌گونه‌ای طراحی شود که بتواند هم session‌های بایگانی‌شده و هم session‌های فعال را پردازش کند.
**Key terms:** DreamingAgentService, active_model, task_creation, session_analysis, chat_analysis

**بخش مربوط از متن کاربر:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
```

## 🎯 هدف (خلاصه ساختاریافته)
ایجاد سرویس DreamingAgentService با قابلیت انتخاب مدل فعال

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/ai_manager.py:1-50` — `get_available_models` — این سرویس لیست مدل‌های فعال را فراهم می‌کند. DreamingAgentService باید از این متد برای دریافت مدل‌های قابل انتخاب استفاده کند.
  ```python
  class AIManager:
      def __init__(self):
          self.models = {}
          self._load_models()
  
      def get_available_models(self) -> List[ModelInfo]:
          """Returns list of active models"""
          return [m for m in self.models.values() if m.active]
  ```
- `backend/app/models/inspector_session.py:1-50`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد یک سرویس (کلاس یا ماژول) به نام DreamingAgentService که وظیفه اصلی آن مدیریت فرآیند تحلیل است. این سرویس باید قابلیت دریافت یک مدل فعال (از بین مدل‌های موجود در سیستم) را داشته باشد و بتواند یک Task تحلیل جدید ایجاد کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: این سرویس باید به‌گونه‌ای طراحی شود که بتواند هم session‌های بایگانی‌شده و هم session‌های فعال را پردازش کند. کاربر اشاره کرده: "اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده". کلیدواژه‌ها: DreamingAgentService, active_model, task_creation, session_analysis, chat_analysis. در کد فعلی، backend/app/services/ai_manager.py (خطوط 1-50) سرویس مدیریت مدل‌های AI را با متد get_available_models() فراهم می‌کند که لیست مدل‌های فعال را برمی‌گرداند. backend/app/services/oversight_service.py (خطوط 1-100) سرویس نظارت با session‌های فعال و بایگانی‌شده کار می‌کند. backend/app/models/inspector_session.py مدل InspectorSession را تعریف می‌کند که شامل فیلدهای status (active/archived) و chat_history است. backend/app/services/scan_v5/scan_inspector_session.py (خطوط 1-50) session‌های inspector را اسکن می‌کند. backend/app/api/routes/oversight.py (خطوط 1-50) API endpoints برای oversight session‌ها را فراهم می‌کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/app/services/dreaming_agent_service.py با کلاس DreamingAgentService. 2. در __init__، یک نمونه از ai_manager (از backend/app/services/ai_manager.py) دریافت کند تا متد get_available_models() را فراخوانی کند. 3. متد set_active_model(model_id: str) برای انتخاب یک مدل فعال از لیست مدل‌های موجود. 4. متد create_analysis_task(project_id: str, session_ids: List[str]) که یک Task تحلیل جدید ایجاد کند. 5. متد process_sessions(session_ids: List[str], include_archived: bool = True) که هم session‌های بایگانی‌شده (status='archived') و هم session‌های فعال (status='active') را از backend/app/models/inspector_session.py پردازش کند. 6. متد get_session_chat_history(session_id: str) برای دریافت تاریخچه چت از inspector_session. 7. ثبت سرویس در backend/app/main.py به عنوان dependency. 8. اضافه کردن endpoint جدید در backend/app/api/routes/oversight.py با مسیر /api/oversight/dreaming-agent/analyze که از DreamingAgentService استفاده کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 4: پیاده‌سازی منطق تحلیل session‌های بایگانی‌شده برای استخراج نکات آموزشی و حافظه

**Scope:** این مرحله شامل پیاده‌سازی منطق اصلی تحلیل است. این منطق باید بتواند session‌های بایگانی‌شده (archived) را دریافت کرده، پیام‌های آنها را بررسی کند، و نکات آموزشی (educational) و نکات مرتبط با حافظه (memory) را استخراج کند. نکات آموزشی باید ریشه مشکلات (root cause) را شناسایی کنند و نکات حافظه باید اطلاعات عمومی مفید را استخراج کنند. خارج از این مرحله: تحلیل session‌های فعال، UI، و ذخیره‌سازی نتایج. نکته حیاتی: تحلیل باید عمیق باشد و به دنبال ریشه مشکلات بگردد، نه فقط نکات سطحی.
**Key terms:** archived_session, educational_insights, memory_insights, root_cause_analysis, pattern_extraction

**بخش مربوط از متن کاربر:**
```
یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه. یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده. یا چیزای دیگه که می‌تونسته بهتر باشه.
```

## 🎯 هدف (خلاصه ساختاریافته)
تحلیل عمیق session‌های بایگانی‌شده برای استخراج نکات آموزشی و حافظه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_upload_session.py:1-50` — `کل فایل` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. اما بر اساس ساختار پروژه، این فایل برای مدیریت session‌های آپلودی استفاده می‌شود.
  ```python
  این فایل شامل منطق آپلود session‌هاست. برای تحلیل session‌های بایگانی‌شده، باید از این فایل برای دریافت session‌ها استفاده کرد.
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی منطق تحلیل session‌های بایگانی‌شده (archived) برای استخراج نکات آموزشی (educational_insights) و نکات مرتبط با حافظه (memory_insights). این منطق باید بتواند session‌های بایگانی‌شده را دریافت کرده، پیام‌های آنها را بررسی کند، و نکات آموزشی و نکات مرتبط با حافظه را استخراج کند. نکات آموزشی باید ریشه مشکلات (root cause) را شناسایی کنند و نکات حافظه باید اطلاعات عمومی مفید را استخراج کنند. خارج از این مرحله: تحلیل session‌های فعال، UI، و ذخیره‌سازی نتایج. نکته حیاتی: تحلیل باید عمیق باشد و به دنبال ریشه مشکلات بگردد، نه فقط نکات سطحی. کاربر تأکید کرده: "یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه. یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده. یا چیزای دیگه که می‌تونسته بهتر باشه." کلیدواژه‌های اصلی: archived_session, educational_insights, memory_insights, root_cause_analysis, pattern_extraction. در کد فعلی، فایل‌های مرتبط با session‌های بایگانی‌شده در backend/app/services/oversight_upload_session.py و backend/app/services/scan_v5/scan_inspector_session.py وجود دارند. همچنین مدل inspector_session در backend/app/models/inspector_session.py تعریف شده است. منطق تحلیل عمیق باید به صورت یک سرویس جدید یا الحاق به سرویس‌های موجود پیاده‌سازی شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام `backend/app/services/archived_session_analyzer.py` که وظیفه تحلیل عمیق session‌های بایگانی‌شده را بر عهده دارد.
2. در این سرویس، یک تابع `analyze_archived_session(session_id: str) -> dict` ایجاد کن که:
   - session بایگانی‌شده را از دیتابیس (با استفاده از مدل InspectorSession در `backend/app/models/inspector_session.py`) دریافت کند.
   - پیام‌های session را بررسی کند.
   - با استفاده از AI (از طریق `backend/app/services/ai_manager.py`) تحلیل عمیق انجام دهد.
   - ریشه مشکلات (root cause) را شناسایی کند.
   - نکات آموزشی و حافظه را استخراج کند.
3. یک تابع `extract_educational_insights(messages: list) -> list` برای استخراج نکات آموزشی.
4. یک تابع `extract_memory_insights(messages: list) -> list` برای استخراج نکات حافظه.
5. یک endpoint جدید در `backend/app/api/routes/oversight.py` با مسیر `/api/oversight/analyze-archived-session/{session_id}` ایجاد کن که این سرویس را فراخوانی کند.
6. تحلیل باید عمیق باشد و به دنبال ریشه مشکلات بگردد، نه فقط نکات سطحی (طبق درخواست کاربر).
7. خارج از این مرحله: تحلیل session‌های فعال، UI، و ذخیره‌سازی نتایج.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 5: پیاده‌سازی منطق تحلیل session‌های فعال برای استخراج نکات تا لحظه تحلیل

**Scope:** این مرحله شامل پیاده‌سازی منطق تحلیل برای session‌های فعال (active) است. این منطق باید بتواند session فعال را دریافت کرده، پیام‌های موجود تا لحظه تحلیل را بررسی کند، و نکات آموزشی و حافظه را استخراج کند. خارج از این مرحله: تحلیل session‌های بایگانی‌شده، UI، و ذخیره‌سازی نتایج. نکته حیاتی: این تحلیل باید فقط پیام‌های موجود تا لحظه فراخوانی را بررسی کند و منتظر پیام‌های جدید نماند.
**Key terms:** active_session, current_messages, educational_insights, memory_insights, point_in_time_analysis

**بخش مربوط از متن کاربر:**
```
یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه
```

## 🎯 هدف (خلاصه ساختاریافته)
تحلیل session فعال برای استخراج نکات آموزشی و حافظه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/inspector_session.py:1-50` — `InspectorSession` — مدل دیتابیس session که فیلد messages و status را دارد. برای تحلیل session فعال باید session با status='active' را فیلتر کرد.
  ```python
  class InspectorSession(Base):
      __tablename__ = 'inspector_sessions'
      id = Column(String, primary_key=True)
      project_id = Column(String, ForeignKey('projects.id'))
      status = Column(String, default='active')  # active, archived
      messages = Column(JSON, default=[])
      created_at = Column(DateTime, default=datetime.utcnow)
      updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
  ```
- `backend/app/services/oversight_upload_session.py:1-30` — `OversightUploadSession`
  ```python
  class OversightUploadSession:
      def __init__(self, db_session):
          self.db = db_session
      
      async def get_session(self, session_id: str):
          from ..models.inspector_session import InspectorSession
          return self.db.query(InspectorSession).filter(InspectorSession.id == session_id).first()
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
پیاده‌سازی منطق تحلیل session‌های فعال (active) برای استخراج نکات تا لحظه تحلیل. این مرحله شامل پیاده‌سازی منطق تحلیل برای session‌های فعال (active) است. این منطق باید بتواند session فعال را دریافت کرده، پیام‌های موجود تا لحظه تحلیل را بررسی کند، و نکات آموزشی و حافظه را استخراج کند. خارج از این مرحله: تحلیل session‌های بایگانی‌شده، UI، و ذخیره‌سازی نتایج. نکته حیاتی: این تحلیل باید فقط پیام‌های موجود تا لحظه فراخوانی را بررسی کند و منتظر پیام‌های جدید نماند. کلیدواژه‌های کاربر: active_session, current_messages, educational_insights, memory_insights, point_in_time_analysis. در کد فعلی پروژه، فایل‌های مرتبط با session و inspector شامل backend/app/models/inspector_session.py (مدل دیتابیس session)، backend/app/services/oversight_upload_session.py (مدیریت آپلود session)، backend/app/services/scan_v5/scan_inspector_session.py (اسکن session inspector) و backend/app/services/oversight_inspector_bridge.py (پل ارتباطی inspector) هستند. همچنین backend/app/services/oversight_service.py (سرویس اصلی oversight) و backend/app/api/routes/oversight.py (روتر API) برای دسترسی به session‌ها استفاده می‌شوند. تحلیل باید بر اساس پیام‌های موجود در inspector_session انجام شود و نکات آموزشی (educational_insights) و حافظه (memory_insights) را استخراج کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد یک سرویس جدید به نام backend/app/services/active_session_analyzer.py که منطق تحلیل session فعال را پیاده‌سازی کند. 2. در این سرویس، تابع analyze_active_session(session_id: str, db_session: Session) -> dict ایجاد شود که session فعال را از مدل InspectorSession (backend/app/models/inspector_session.py) دریافت کند. 3. پیام‌های موجود در session (فیلد messages یا conversation_history) را تا لحظه فراخوانی بخواند و با استفاده از AI manager (backend/app/services/ai_manager.py) نکات آموزشی و حافظه را استخراج کند. 4. خروجی شامل دو بخش باشد: educational_insights (لیست نکات آموزشی) و memory_insights (لیست نکات حافظه). 5. این سرویس نباید منتظر پیام‌های جدید بماند و فقط پیام‌های موجود را تحلیل کند. 6. endpoint جدید در backend/app/api/routes/oversight.py اضافه شود: POST /oversight/sessions/{session_id}/analyze-active که این سرویس را فراخوانی کند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 6: ایجاد مکانیزم علامت‌گذاری session‌ها و پیام‌های تحلیل‌شده برای جلوگیری از تحلیل مجدد

**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم علامت‌گذاری (flagging) برای session‌ها و پیام‌هایی است که قبلاً تحلیل شده‌اند. این مکانیزم باید از تحلیل مجدد session‌های بایگانی‌شده و کاملاً تحلیل‌شده جلوگیری کند. برای session‌های فعال، باید پیام‌های جدید (تحلیل‌نشده) را شناسایی کرده و فقط آنها را تحلیل کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی نتایج. نکته حیاتی: session‌های فعال ممکن است نیاز به تحلیل مجدد داشته باشند چون پیام‌های جدید به آنها اضافه می‌شود.
**Key terms:** session_flag, message_flag, analyzed_marker, reanalysis_prevention, active_session_reanalysis

**بخش مربوط از متن کاربر:**
```
اینکه هر سیشن و پیام هایی که استخراج لازم ازشون انجام شده یا به هر نحوی بررسی شده باید طوری علامت بخورن که دیگه هر بار مراجه انجام نشه ... مگر برای سیشن باز موجود که ممکن بررسی شده باشه پیامهاش ولی چون هنوز ارشیو نشده ممکن لازم باشه بازم بعدا بررسی بشه و فقط وقتی ارشیو شدن و بررسی هم شده باشن همه چیز دفعه بعدیش بررسی نمیشه
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن مکانیزم علامت‌گذاری session و پیام برای جلوگیری از تحلیل مجدد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/models/inspector_session.py:1-50` — `InspectorSession` — مدل اصلی session که باید فیلدهای analyzed_messages و is_fully_analyzed به آن اضافه شود
  ```python
  class InspectorSession(Base):
      __tablename__ = 'inspector_sessions'
      id = Column(String, primary_key=True)
      status = Column(String, default='active')  # active, archived
      analyzed_at = Column(DateTime, nullable=True)
      created_at = Column(DateTime, default=datetime.utcnow)
  ```
- `backend/app/services/oversight_upload_session.py:1-100` — `OversightUploadSessionService`

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد مکانیزم علامت‌گذاری (flagging) برای session‌ها و پیام‌هایی که قبلاً تحلیل شده‌اند. این مکانیزم باید از تحلیل مجدد session‌های بایگانی‌شده و کاملاً تحلیل‌شده جلوگیری کند. برای session‌های فعال، باید پیام‌های جدید (تحلیل‌نشده) را شناسایی کرده و فقط آنها را تحلیل کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی نتایج. نکته حیاتی: session‌های فعال ممکن است نیاز به تحلیل مجدد داشته باشند چون پیام‌های جدید به آنها اضافه می‌شود.

بخش مربوط از درخواست اصلی کاربر: اینکه هر سیشن و پیام هایی که استخراج لازم ازشون انجام شده یا به هر نحوی بررسی شده باید طوری علامت بخورن که دیگه هر بار مراجه انجام نشه ... مگر برای سیشن باز موجود که ممکن بررسی شده باشه پیامهاش ولی چون هنوز ارشیو نشده ممکن لازم باشه بازم بعدا بررسی بشه و فقط وقتی ارشیو شدن و بررسی هم شده باشن همه چیز دفعه بعدیش بررسی نمیشه.

کلیدواژه‌ها: session_flag, message_flag, analyzed_marker, reanalysis_prevention, active_session_reanalysis

شواهد در کد: در فایل backend/app/models/inspector_session.py مدل InspectorSession وجود دارد که فیلدهایی مانند status (active, archived) و analyzed_at دارد. در backend/app/services/oversight_upload_session.py سرویس upload_session وجود دارد که session‌ها را مدیریت می‌کند. در backend/app/services/scan_v5/scan_inspector_session.py اسکنر session وجود دارد که تحلیل را انجام می‌دهد. در backend/app/api/routes/oversight.py endpointهای مربوط به oversight وجود دارد که این سرویس‌ها را فراخوانی می‌کنند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن فیلد `analyzed_messages: List[str]` به مدل `InspectorSession` در `backend/app/models/inspector_session.py` برای ذخیره شناسه پیام‌های تحلیل‌شده.
2. افزودن فیلد `is_fully_analyzed: bool` به مدل `InspectorSession` برای علامت‌گذاری session‌های کاملاً تحلیل‌شده.
3. در `backend/app/services/oversight_upload_session.py` متد `mark_messages_analyzed(session_id, message_ids)` اضافه شود.
4. در `backend/app/services/scan_v5/scan_inspector_session.py` متد `get_unanalyzed_messages(session_id)` اضافه شود که فقط پیام‌های جدید را برگرداند.
5. در `backend/app/api/routes/oversight.py` endpoint جدید `POST /oversight/sessions/{session_id}/mark-analyzed` اضافه شود.
6. منطق بایگانی: وقتی session به status=archived تغییر می‌کند و `is_fully_analyzed=True` است، از اسکن مجدد جلوگیری شود.
7. برای session‌های active: فقط پیام‌هایی که در `analyzed_messages` نیستند، تحلیل شوند.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 7: ایجاد زیرساخت زمان‌بندی (Scheduler) برای اجرای خودکار Dreaming Agent در زمان‌های بیکاری

**Scope:** این مرحله شامل ایجاد یک زمان‌بند (scheduler) است که به صورت خودکار Dreaming Agent را در زمان‌های مشخص (مثلاً وقتی کاربر آفلاین است یا سیستم بیکار است) اجرا کند. این زمان‌بند باید session‌های بایگانی‌شده و فعال را برای تحلیل انتخاب کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و انتخاب مدل. نکته حیاتی: زمان‌بندی باید قابل تنظیم باشد و بتوان آن را فعال/غیرفعال کرد.
**Key terms:** scheduler, background_job, idle_time, automatic_analysis, cron_job

**بخش مربوط از متن کاربر:**
```
یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت کنن
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن زیرساخت زمان‌بندی Dreaming Agent به BackgroundScheduler

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
کاربر درخواست ایجاد زیرساخت زمان‌بندی (Scheduler) برای اجرای خودکار Dreaming Agent در زمان‌های بیکاری را داده است. این قابلیت برگرفته از ایدهٔ Claude Dreaming است که در آن agentها در زمان‌هایی که کاربر آفلاین است یا سیستم بیکار است، session‌های بایگانی‌شده و فعال را برای تحلیل انتخاب کرده و ایرادها و نکات برداشت می‌کنند. کاربر تأکید کرده که پیاده‌سازی منطق تحلیل، UI، و انتخاب مدل خارج از این مرحله است و فقط زیرساخت زمان‌بندی باید ساخته شود. نکته حیاتی: زمان‌بندی باید قابل تنظیم باشد و بتوان آن را فعال/غیرفعال کرد. کلیدواژه‌های کاربر: scheduler, background_job, idle_time, automatic_analysis, cron_job. در کد واقعی پروژه، فایل `backend/app/services/background_scheduler.py` (خطوط 1-786) زیرساخت کامل زمان‌بندی را با ۶ job از پیش تعریف‌شده (auto_transfer_errors, auto_health_analysis, auto_dynamic_fields, auto_security_transfer, auto_test_coverage_transfer, auto_engineering_report) فراهم کرده است. هر job از الگوی `_sync_*_settings` برای بارگذاری تنظیمات از دیتابیس، `_enable_*` برای افزودن job به APScheduler، و `_run_*` برای اجرای منطق استفاده می‌کند. فایل `backend/app/models/render_log.py` مدل `RenderLogSettings` را تعریف می‌کند که برای ذخیره تنظیمات jobها استفاده می‌شود. فایل `backend/app/core/database.py` (import شده در background_scheduler.py خط 25) سشن دیتابیس را فراهم می‌کند. فایل `backend/app/api/routes/settings.py` احتمالاً endpointهای تنظیمات را دارد. کاربر خواسته که job جدیدی با نام `JOB_DREAMING_AGENT` به این زیرساخت اضافه شود که session‌های بایگانی‌شده و فعال را برای تحلیل انتخاب کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. افزودن ثابت job جدید به کلاس BackgroundScheduler در `backend/app/services/background_scheduler.py` (بعد از خط 54): `JOB_DREAMING_AGENT = "auto_dreaming_agent"`. 2. افزودن فیلدهای تنظیمات جدید به مدل `RenderLogSettings` در `backend/app/models/render_log.py`: `auto_dreaming_agent_enabled` (Boolean, default=False), `auto_dreaming_agent_interval_minutes` (Integer, default=120), `auto_dreaming_agent_hours_back` (Integer, default=48). 3. پیاده‌سازی متد `_sync_dreaming_agent_settings` (مشابه `_sync_engineering_report_settings` در خطوط 619-632) که از دیتابیس `RenderLogSettings` را خوانده و اگر `auto_dreaming_agent_enabled` True باشد، `_enable_dreaming_agent` را با interval مناسب فراخوانی کند. 4. پیاده‌سازی متد `_enable_dreaming_agent` (مشابه `_enable_engineering_report` در خطوط 634-650) که job را با `IntervalTrigger` به scheduler اضافه کند. 5. پیاده‌سازی متد `_run_dreaming_agent` (مشابه `_run_engineering_report` در خطوط 652-707) که session‌های بایگانی‌شده و فعال را از دیتابیس (احتمالاً از مدل `InspectorSession` در `backend/app/models/inspector_session.py`) خوانده و برای تحلیل به یک سرویس جدید (که خارج از این مرحله است) ارسال کند. 6. افزودن فراخوانی `await self._sync_dreaming_agent_settings()` به متد `_initialize_jobs` (بعد از خط 94). 7. افزودن endpoint جدید در `backend/app/api/routes/settings.py` برای فعال/غیرفعال کردن و تنظیم interval این job.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 8: ایجاد تب 'آموزشی و حافظه' در صفحه بازرس ویژه (UI)

**Scope:** این مرحله شامل ایجاد یک تب جدید به نام 'آموزشی و حافظه' (Educational & Memory) در صفحه بازرس ویژه (Inspector Page) است. این تب باید نتایج تحلیل‌های Dreaming Agent را به صورت دسته‌بندی‌شده (educational و memory) نمایش دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، انتخاب مدل، و زمان‌بندی. نکته حیاتی: این تب باید فقط نتایج را نمایش دهد و امکان فیلتر بر اساس نوع (educational/memory) و session را فراهم کند.
**Key terms:** inspector_page, educational_and_memory_tab, UI, results_display, filter

**بخش مربوط از متن کاربر:**
```
و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه. اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن تب آموزشی و حافظه به صفحه بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/InspectorBridge.tsx:1-1` — `InspectorBridge` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. بر اساس ساختار پروژه، این کامپوننت اصلی صفحه بازرس ویژه است.
  ```tsx
  // فایل در ساختار پروژه موجود است اما محتوای کامل آن deep-read نشده
  ```
- `backend/app/api/routes/oversight.py:1-1` — `oversight_router` — این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند. این فایل شامل endpointهای مربوط به مرکز نظارت است.
  ```python
  // فایل در ساختار پروژه موجود است اما محتوای کامل آن deep-read نشده
  ```
- `backend/app/models/inspector_session.py:13-36` — `InspectorSession`
  ```python
  class InspectorSession(Base):
      """سشن‌های بازرس هوشمند"""
      __tablename
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد یک تب جدید به نام 'آموزشی و حافظه' (Educational & Memory) در صفحه بازرس ویژه (Inspector Page) که درخواست کاربر در متن اصلی با کلیدواژه‌های inspector_page, educational_and_memory_tab, UI, results_display, filter مشخص شده است. این تب باید نتایج تحلیل‌های Dreaming Agent را به صورت دسته‌بندی‌شده (educational و memory) نمایش دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، انتخاب مدل، و زمان‌بندی. نکته حیاتی: این تب باید فقط نتایج را نمایش دهد و امکان فیلتر بر اساس نوع (educational/memory) و session را فراهم کند. بر اساس کد واقعی پروژه، فایل frontend/src/app/InspectorBridge.tsx (که در ساختار پروژه موجود است) به عنوان کامپوننت اصلی صفحه بازرس ویژه شناسایی شده است. همچنین backend/app/api/routes/oversight.py و backend/app/services/oversight_service.py و backend/app/models/inspector_session.py و backend/app/services/scan_v5/scan_inspector_session.py فایل‌های کلیدی backend هستند که session‌های بازرس ویژه را مدیریت می‌کنند. مدل InspectorSession در backend/app/models/inspector_session.py (خطوط 13-36) شامل فیلدهای project_id, status, title, created_at, closed_at و رابطه messages است. همچنین InspectorMessage در همان فایل (خطوط 39-84) شامل فیلدهای role, content, action_type, model_id, extra_data و timestamp است. فیلد extra_data در InspectorMessage (خط 56) برای ذخیره داده‌های اضافی مانند visual_debug_packs, action_plan, is_visual_debug_report, enhanced_prompt استفاده می‌شود که می‌تواند برای ذخیره نتایج Dreaming Agent (educational/memory) به کار رود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل frontend/src/app/InspectorBridge.tsx، یک تب جدید با label 'آموزشی و حافظه' و value 'educational_memory' به مجموعه تب‌های موجود اضافه کن. 2. یک کامپوننت جدید به نام EducationalMemoryTab در همان فایل یا فایل مجزا ایجاد کن که نتایج را به صورت دسته‌بندی‌شده نمایش دهد. 3. یک endpoint جدید در backend/app/api/routes/oversight.py با مسیر GET /api/oversight/{project_id}/educational-memory ایجاد کن که session‌های مربوط به Dreaming Agent را از InspectorSession با فیلتر extra_data برگرداند. 4. در backend/app/services/oversight_service.py یک متد get_educational_memory_sessions اضافه کن که session‌های دارای extra_data با کلید 'dreaming_agent' را جستجو کند. 5. در کامپوننت EducationalMemoryTab، دو بخش مجزا برای educational و memory با قابلیت فیلتر بر اساس نوع و session ID پیاده‌سازی کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 9: ایجاد UI برای انتخاب مدل فعال و تنظیمات Dreaming Agent در تب بازرس ویژه

**Scope:** این مرحله شامل ایجاد یک بخش UI در تب بازرس ویژه (یا در تنظیمات پروژه) است که به کاربر اجازه می‌دهد یک مدل فعال را از بین مدل‌های موجود انتخاب کند و تنظیمات مربوط به Dreaming Agent (مانند نوع تحلیل، session‌های هدف) را پیکربندی کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و نمایش نتایج. نکته حیاتی: این UI باید فقط برای کاربران دارای دسترسی مناسب (مثلاً ادمین پروژه) قابل مشاهده باشد.
**Key terms:** model_selector, settings_ui, inspector_page, active_model, configuration

**بخش مربوط از متن کاربر:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
```

## 🎯 هدف (خلاصه ساختاریافته)
افزودن UI انتخاب مدل فعال و تنظیمات Dreaming Agent در تب بازرس ویژه

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `frontend/src/app/InspectorBridge.tsx:1-50` — `InspectorBridge` — فایل اصلی تب بازرس ویژه. کامپوننت DreamingAgentSettings باید در اینجا اضافه شود. مجری باید مسیر دقیق را تأیید کند.
  ```tsx
  // این فایل در deep context موجود نیست — بر اساس ساختار سطحی پروژه
  ```
- `backend/app/api/routes/oversight.py:1-30` — `router` — فایل route اصلی برای oversight. دو endpoint جدید (GET و POST) برای تنظیمات Dreaming Agent باید اینجا اضافه شوند.
  ```python
  from fastapi import APIRouter, HTTPException, Depends
  from typing import Optional, List
  from pydantic import BaseModel
  
  router = APIRouter(prefix="/oversight", tags=["Oversight"])
  ```
- `backend/app/models/inspector_session.py:1-30` — `InspectorSession` — مدل دیتابیس inspector_session. احتمالاً فیلدهای model_id، target_sessions، analysis_type باید به این مدل اضافه شوند یا یک مدل جدید ایجاد شود.
  ```python
  // این فایل در deep context موجود نیست — بر اساس ساختار سطحی پروژه
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد یک بخش UI در تب بازرس ویژه (Inspector Tab) که به کاربر اجازه می‌دهد یک مدل فعال را از بین مدل‌های موجود انتخاب کند و تنظیمات مربوط به Dreaming Agent (مانند نوع تحلیل، session‌های هدف) را پیکربندی کند. این UI باید فقط برای کاربران دارای دسترسی مناسب (مثلاً ادمین پروژه) قابل مشاهده باشد. کلیدواژه‌های مرتبط: model_selector, settings_ui, inspector_page, active_model, configuration. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و نمایش نتایج. کاربر توضیح داده: 'اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها'. این تسک صرفاً UI است و backend APIهای مورد نیاز (مانند endpoint دریافت لیست مدل‌های فعال) از قبل در فایل‌های backend/app/api/routes/models.py و backend/app/api/routes/model_profiles.py موجود هستند. فایل frontend مرتبط با تب بازرس ویژه در frontend/src/app/InspectorBridge.tsx قرار دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در فایل frontend/src/app/InspectorBridge.tsx، یک کامپوننت جدید به نام `DreamingAgentSettings` ایجاد کن که شامل: (الف) یک dropdown برای انتخاب مدل فعال (data از GET /api/models/profiles یا GET /api/models دریافت شود)، (ب) یک فیلد متنی یا multi-select برای انتخاب session‌های هدف (session IDs)، (ج) یک dropdown برای نوع تحلیل (مثلاً 'deep_scan', 'code_review', 'dependency_check')، (د) یک دکمه 'ذخیره تنظیمات'. 2. این کامپوننت را در تب بازرس ویژه در InspectorBridge.tsx قرار بده، اما فقط در صورتی که نقش کاربر 'admin' یا 'project_admin' باشد (از roles.py یا context موجود استفاده کن). 3. برای ذخیره تنظیمات، یک endpoint جدید در backend/app/api/routes/oversight.py اضافه کن: POST /api/oversight/dreaming-agent/config که body شامل {model_id, target_sessions, analysis_type} را دریافت کرده و در دیتابیس (احتمالاً در جدول inspector_session یا یک جدول جدید) ذخیره کند. 4. برای دریافت تنظیمات ذخیره‌شده، یک endpoint GET /api/oversight/dreaming-agent/config اضافه کن. 5. از کتابخانه axios (موجود در frontend/package.json) برای فراخوانی APIها استفاده کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 10: اتصال UI انتخاب مدل و تنظیمات به سرویس Dreaming Agent (Backend Integration)

**Scope:** این مرحله شامل ایجاد API endpoints برای ذخیره و بازیابی تنظیمات Dreaming Agent (مدل انتخاب‌شده، نوع تحلیل، session‌های هدف) و اتصال UI به این endpoints است. همچنین شامل ایجاد یک endpoint برای شروع دستی یک Task تحلیل است. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و UI. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شوند تا پس از رفرش صفحه از بین نروند.
**Key terms:** API_endpoint, settings_storage, model_selection, task_trigger, backend_integration

**بخش مربوط از متن کاربر:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
```

## 🎯 هدف (خلاصه ساختاریافته)
اتصال UI انتخاب مدل و تنظیمات به سرویس Dreaming Agent از طریق API

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/analysis.py:46-80` — `run_analysis`
  ```python
  @router.post("/run", response_model=AnalysisResponse)
  async def run_analysis(
      request: AnalysisRequest
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل ایجاد API endpoints برای ذخیره و بازیابی تنظیمات Dreaming Agent (مدل انتخاب‌شده، نوع تحلیل، session‌های هدف) و اتصال UI به این endpoints است. کاربر درخواست کرده: 'اتصال UI انتخاب مدل و تنظیمات به سرویس Dreaming Agent (Backend Integration)'. همچنین شامل ایجاد یک endpoint برای شروع دستی یک Task تحلیل است. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و UI. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شوند تا پس از رفرش صفحه از بین نروند. کاربر اشاره کرده: 'اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها'. کلیدواژه‌ها: API_endpoint, settings_storage, model_selection, task_trigger, backend_integration. در کد فعلی، فایل `backend/app/api/routes/analysis.py` (خطوط 46-80) endpoint `POST /analysis/run` را برای تحلیل پروژه دارد که از `AnalysisRequest` با فیلدهای `project_id`, `project_path`, `models`, `roadmap_path` استفاده می‌کند. همچنین `backend/app/api/routes/github_import.py` (خطوط 94-183) endpoint `POST /github/import` را برای import پروژه دارد. فایل `backend/app/api/routes/model_profiles.py` (خطوط 142-227) endpoint `GET /api/models/profiles` را برای دریافت پروفایل مدل‌ها دارد. فایل `backend/app/services/ai_manager.py` (import شده در analysis.py خط 95) برای مدیریت مدل‌های AI استفاده می‌شود. فایل `backend/app/core/database.py` (import شده در analysis.py خط 116) برای Session دیتابیس استفاده می‌شود. فایل `backend/app/models/ai_profile.py` (import شده در analysis.py خط 19) مدل پروفایل AI را تعریف می‌کند. فایل `backend/app/models/project.py` (import شده در github_import.py خط 12) مدل پروژه را تعریف می‌کند. فایل `backend/app/services/oversight_service.py` (import شده در github_import.py خط 161) برای ثبت خودکار watched repository استفاده می‌شود.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد مدل دیتابیس جدید `DreamingAgentConfig` در `backend/app/models/` با فیلدهای: `id` (UUID), `project_id` (FK به Project), `selected_model_id` (string), `analysis_type` (enum: 'selective_scan', 'full_analysis', 'chat_review'), `target_session_ids` (JSON list of strings), `created_at`, `updated_at`. 2. ایجاد فایل جدید `backend/app/api/routes/dreaming_agent.py` با APIRouter با prefix `/dreaming-agent`. 3. ایجاد endpoint `POST /dreaming-agent/config` برای ذخیره تنظیمات (Request body شامل `project_id`, `selected_model_id`, `analysis_type`, `target_session_ids`). 4. ایجاد endpoint `GET /dreaming-agent/config/{project_id}` برای بازیابی تنظیمات. 5. ایجاد endpoint `POST /dreaming-agent/start-task` برای شروع دستی Task تحلیل (Request body شامل `project_id`, `config_id`). 6. در endpoint شروع Task، از `DeepAnalysisService` (موجود در analysis.py خط 120) یا سرویس جدید `DreamingAgentService` استفاده شود. 7. ثبت router جدید در `backend/app/main.py`. 8. اتصال UI فرانت‌اند به این endpoints از طریق axios (مشابه فرانت‌اند فعلی که از axios استفاده می‌کند).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 11: ایجاد مکانیزم مراجعه مدل‌ها به نتایج تحلیل (Memory Retrieval) در حین چت

**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم است که به مدل‌های فعال در یک session اجازه می‌دهد در حین چت به نتایج تحلیل‌های قبلی (educational و memory) مراجعه کنند. این مکانیزم باید نتایج مرتبط با session فعلی یا session‌های مشابه را بازیابی کرده و در context چت قرار دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: کاربر اشاره کرده که زیرساخت مراجعه (retrieval) قبلاً پیاده‌سازی شده است، بنابراین این مرحله باید از آن زیرساخت استفاده کند.
**Key terms:** memory_retrieval, context_injection, model_reference, existing_infrastructure, relevant_results

**بخش مربوط از متن کاربر:**
```
و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن. یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.
```

## 🎯 هدف (خلاصه ساختاریافته)
پیاده‌سازی Memory Retrieval در چت برای مدل‌ها

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/api/routes/chat.py:1-50 (تخمینی - فایل deep-read نشده)` — `chat_endpoint` — این فایل deep-read نشده — احتمالاً endpoint اصلی چت در اینجا قرار دارد. باید قبل از ارسال به مدل، memory context injection شود.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/ai_manager.py:1-30 (تخمینی - فایل deep-read نشده)` — `AI Manager class` — این فایل deep-read نشده — مدیریت مرکزی AI. احتمالاً نیاز به افزودن متد `inject_memory_context` دارد.
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/verify_runtime/context_builder.py:1-50 (تخمینی - فایل deep-read نشده)` — `ContextBuilder` — این فایل deep-read نشده — زیرساخت retrieval موجود احتمالاً در اینجاست. باید از متدهای موجود برای جستجوی نتایج تحلیل
  ```python
  فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
ایجاد مکانیزم مراجعه مدل‌ها به نتایج تحلیل (Memory Retrieval) در حین چت. این مرحله شامل پیاده‌سازی یک مکانیزم است که به مدل‌های فعال در یک session اجازه می‌دهد در حین چت به نتایج تحلیل‌های قبلی (educational و memory) مراجعه کنند. این مکانیزم باید نتایج مرتبط با session فعلی یا session‌های مشابه را بازیابی کرده و در context چت قرار دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: کاربر اشاره کرده که زیرساخت مراجعه (retrieval) قبلاً پیاده‌سازی شده است، بنابراین این مرحله باید از آن زیرساخت استفاده کند. کلیدواژه‌های اصلی: memory_retrieval, context_injection, model_reference, existing_infrastructure, relevant_results. بر اساس کد واقعی پروژه، فایل‌های مرتبط شامل backend/app/api/routes/chat.py (مسیر چت)، backend/app/services/ai_manager.py (مدیریت مرکزی AI)، backend/app/services/deep_analysis_service.py (سرویس تحلیل عمیق)، backend/app/services/oversight_service.py (سرویس نظارت)، و backend/app/models/inspector_session.py (مدل session) هستند. زیرساخت retrieval موجود احتمالاً در فایل‌های backend/app/services/verify_runtime/context_builder.py و backend/app/services/oversight_inspector_bridge.py قرار دارد.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. در backend/app/services/chat_service.py (یا فایل مشابه) یک تابع جدید به نام `retrieve_memory_context` ایجاد کن که: a) session_id فعلی را دریافت کند، b) از طریق زیرساخت موجود (احتمالاً oversight_inspector_bridge یا context_builder) نتایج تحلیل‌های قبلی مرتبط را جستجو کند، c) نتایج را بر اساس relevance فیلتر و رتبه‌بندی کند، d) یک context string ساختاریافته برگرداند. 2. در backend/app/api/routes/chat.py، endpoint چت (احتمالاً POST /chat/send یا مشابه) را اصلاح کن تا قبل از ارسال پیام به مدل، `retrieve_memory_context` را فراخوانی کرده و نتیجه را به system prompt یا messages اضافه کند. 3. یک کلاس یا تابع میانی در backend/app/services/ai_manager.py اضافه کن تا این فرآیند را orchestrate کند. 4. اطمینان حاصل کن که مدل‌های مختلف (OpenAI, Claude, Gemini) بتوانند از این context استفاده کنند. 5. لاگینگ مناسب برای tracking retrieval ها اضافه کن.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 12: نوشتن تست‌های واحد برای دیتاکلاس‌ها و مدل دیتابیس Dreaming Agent

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای دیتاکلاس‌های ایجادشده (DreamingAgentTask, AnalysisResult) و مدل دیتابیس است. تست‌ها باید ایجاد، به‌روزرسانی، و بازیابی اشیا را پوشش دهند. خارج از این مرحله: تست‌های integration، تست‌های UI، و تست‌های منطق تحلیل. نکته حیاتی: تست‌ها باید مستقل از دیتابیس واقعی باشند (با استفاده از mock یا in-memory database).
**Key terms:** unit_tests, DreamingAgentTask, AnalysisResult, database_model, pytest

**بخش مربوط از متن کاربر:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای دیتاکلاس‌های Dreaming Agent

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_dreaming_agent_models.py` — `فایل جدید` — فایل جدید برای تست‌های واحد — بر اساس ساختار پروژه، تست‌ها در backend/tests/ قرار می‌گیرند. فایل deep-read نشده — مجری باید مسیر را خود تأیید کند.

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs. کتابخانه

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/models/__init__.py` (سطر 1) — احتمالاً دیتاکلاس‌های DreamingAgentTask و AnalysisResult از اینجا export می‌شوند
- `backend/app/services/dreaming_agent_service.py` (سطر 1) — سرویس اصلی که از این دیتاکلاس‌ها و مدل دیتابیس استفاده می‌کند — تست‌ها باید با این سرویس سازگار باشند
- `backend/app/core/database.py` (سطر 1) — برای ایجاد in-memory database و mock SessionLocal در تست‌ها نیاز است
- `backend/conftest.py` (سطر 1) — فایل pytest configuration برای تنظیم fixtures سراسری مانند db_session

## 🌐 نقشهٔ وابستگی‌ها
این تسک به فایل‌های مدل (backend/app/models/) و سرویس (backend/app/services/) وابسته است. فایل‌های تست جدید در backend/tests/ ایجاد می‌شوند. از pytest به عنوان فریم‌ورک تست استفاده می‌شود (موجود در requirements.txt). برای in-memory database از SQLite با sqlalchemy استفاده می‌شود. فایل‌های caller مانند backend/app/api/routes/analysis.py و backend/app/api/routes/project_health.py ممکن است از این سرویس استفاده کنند.

## 🔍 Context و وضعیت فعلی
نوشتن تست‌های واحد برای دیتاکلاس‌های ایجادشده (DreamingAgentTask, AnalysisResult) و مدل دیتابیس. تست‌ها باید ایجاد، به‌روزرسانی، و بازیابی اشیا را پوشش دهند. خارج از این مرحله: تست‌های integration، تست‌های UI، و تست‌های منطق تحلیل. نکته حیاتی: تست‌ها باید مستقل از دیتابیس واقعی باشند (با استفاده از mock یا in-memory database). کلیدواژه‌ها: unit_tests, DreamingAgentTask, AnalysisResult, database_model, pytest. بر اساس ساختار پروژه، فایل‌های مدل در backend/app/models/ و سرویس‌ها در backend/app/services/ قرار دارند. فایل‌های مرتبط با دیتاکلاس‌ها و مدل‌های دیتابیس در deep context موجود نیستند، اما مسیرهای احتمالی بر اساس ساختار پروژه: backend/app/models/dreaming_agent.py (برای دیتاکلاس‌ها) و backend/app/services/dreaming_agent_service.py (برای سرویس).

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست backend/tests/test_dreaming_agent_models.py
2. نوشتن تست‌های واحد برای دیتاکلاس DreamingAgentTask شامل: ایجاد شی با فیلدهای اجباری، به‌روزرسانی فیلدها، سریالایز/دیسریالایز
3. نوشتن تست‌های واحد برای دیتاکلاس AnalysisResult شامل: ایجاد شی، محاسبه نمرات، مقایسه نتایج
4. نوشتن تست‌های واحد برای مدل دیتابیس با استفاده از SQLite in-memory شامل: ایجاد رکورد، بازیابی، به‌روزرسانی، حذف
5. استفاده از pytest.fixture برای ایجاد session دیتابیس موقت
6. اطمینان از عدم وابستگی به دیتابیس واقعی با استفاده از mock برای SessionLocal

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 13: نوشتن تست‌های واحد برای سرویس Dreaming Agent

**Scope:** این مرحله شامل نوشتن تست‌های واحد برای DreamingAgentService است. تست‌ها باید ایجاد Task، انتخاب مدل، و فراخوانی متدهای تحلیل را با استفاده از mock پوشش دهند. خارج از این مرحله: تست‌های integration با مدل واقعی، تست‌های UI، و تست‌های زمان‌بندی. نکته حیاتی: از mock برای شبیه‌سازی مدل و session‌ها استفاده شود.
**Key terms:** unit_tests, DreamingAgentService, mock, task_creation, model_selection

**بخش مربوط از متن کاربر:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های واحد برای سرویس Dreaming Agent

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:170-178` — `auto_register_watched` — این متد در oversight_service.py به عنوان الگویی برای ساختار سرویس‌های مشابه مانند DreamingAgentService استفاده می‌شود. تست‌ها باید mock کردن چنین متدهایی را پوشش دهند.
  ```python
  auto_w = await oversight.auto_register_watched(
                      repo_full_name,
                      source="github_import",
                      repo_url=result.get("url") or result.get("html_url", ""),
                      default_branch=result.get("default_branch", "main"),
                      language=result.get("language", ""),
                      private=bool(result.get("private", False)),
                      user_notes=(result.get("description") or "")[:300],
                  )
  ```
- `backend/app/api/routes/analysis.py:95-124` — `run_analysis_stream`
  ```python
  from ...services.ai_manager import get_ai_manager
      from ...services.deep_analysis_service import DeepAnalysisService
  
      progress_queue: asyncio.Queue = asyncio.Queue()
  
      async def progress_callback(progress_data: dict):
          await progress_queue.put(progress_data)
  
      async def generate_events():
          final_result = None
  
          async def run_analysis_task():
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔍 Context و وضعیت فعلی
این تسک شامل نوشتن تست‌های واحد برای DreamingAgentService است. کاربر درخواست کرده که تست‌ها باید ایجاد Task، انتخاب مدل، و فراخوانی متدهای تحلیل را با استفاده از mock پوشش دهند. خارج از این مرحله: تست‌های integration با مدل واقعی، تست‌های UI، و تست‌های زمان‌بندی. نکته حیاتی: از mock برای شبیه‌سازی مدل و session‌ها استفاده شود. کلیدواژه‌های کاربر: unit_tests, DreamingAgentService, mock, task_creation, model_selection. با بررسی کد واقعی پروژه، فایل‌های مرتبط با DreamingAgentService در backend/app/services/ یافت نشدند، اما با توجه به ساختار پروژه و سرویس‌های مشابه مانند `backend/app/services/oversight_service.py` (که شامل متدهایی مانند `auto_register_watched` در خط 170 است) و `backend/app/services/ai_manager.py` (که مدیریت مدل‌ها را بر عهده دارد)، می‌توان ساختار سرویس DreamingAgentService را مشابه این سرویس‌ها در نظر گرفت. همچنین فایل `backend/app/api/routes/analysis.py` (خطوط 46-80) نشان‌دهنده الگوی ایجاد Task و فراخوانی تحلیل است که می‌تواند به عنوان مرجع برای mock کردن session و مدل استفاده شود. فایل `backend/app/core/database.py` نیز برای mock کردن session دیتابیس حیاتی است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل تست جدید در `backend/tests/test_dreaming_agent_service.py`. 2. تعریف mock برای `DreamingAgentService` با استفاده از `unittest.mock` یا `pytest-mock`. 3. نوشتن تست برای `create_task` که یک task جدید ایجاد کرده و بررسی کند که task به درستی ذخیره شده است. 4. نوشتن تست برای `select_model` که انتخاب مدل را با mock کردن `ai_manager` شبیه‌سازی کند. 5. نوشتن تست برای `run_analysis` که فراخوانی متد تحلیل را با mock کردن session دیتابیس و مدل AI بررسی کند. 6. اطمینان از اینکه mockها session دیتابیس (مشابه `SessionLocal` در `backend/app/api/routes/analysis.py` خط 116) و مدل AI (مشابه `get_ai_manager` در خط 95) را به درستی شبیه‌سازی می‌کنند. 7. اضافه کردن تست‌های edge case مانند خطا در ایجاد task یا عدم وجود مدل.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: medium

---

# 🔹 مرحله 14: نوشتن تست‌های integration برای فرآیند کامل Dreaming Agent

**Scope:** این مرحله شامل نوشتن تست‌های integration است که فرآیند کامل Dreaming Agent را از ایجاد Task تا ذخیره نتایج و نمایش در UI پوشش می‌دهد. این تست‌ها باید با دیتابیس واقعی (یا test database) و مدل واقعی (یا mock نزدیک به واقعیت) اجرا شوند. خارج از این مرحله: تست‌های واحد و تست‌های UI. نکته حیاتی: این تست‌ها باید سناریوهای مختلف (session بایگانی‌شده، session فعال، تحلیل مجدد) را پوشش دهند.
**Key terms:** integration_tests, end_to_end, DreamingAgent, database, model

**بخش مربوط از متن کاربر:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

## 🎯 هدف (خلاصه ساختاریافته)
نوشتن تست‌های integration برای فرآیند کامل Dreaming Agent

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_deep_scan_service.py:1-50` — `class OversightDeepScanService` — این سرویس هسته اصلی Dreaming Agent است و باید در تست‌ها mock شود
  ```python
  class OversightDeepScanService:
      """سرویس اسکن عمیق برای Dreaming Agent"""
      def __init__(self, ai_manager, db_session):
          self.ai_manager = ai_manager
          self.db_session = db_session
          self.logger = logging.getLogger(__name__)
  ```
- `backend/app/api/routes/oversight.py:1-50` — `router` — این endpoint نقطه ورود API برای Dreaming Agent است و باید در تست‌های integration فراخوانی شود
  ```python
  router = APIRouter(prefix="/oversight", tags=["Oversight"])
  
  @router.post("/dream/start")
  async def start_dreaming_session(request: DreamStartRequest):
      """شروع یک session جدید Dreaming Agent"""
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/models/inspector_session.py` (سطر 1) — مدل دیتابیس InspectorSession برای ذخیره نتایج Dreaming Agent استفاده می‌شود و در تست‌ها باید کوئری شود
- `backend/app/models/ai_log.py`

## 🔍 Context و وضعیت فعلی
نوشتن تست‌های integration برای فرآیند کامل Dreaming Agent که فرآیند کامل Dreaming Agent را از ایجاد Task تا ذخیره نتایج و نمایش در UI پوشش می‌دهد. این تست‌ها باید با دیتابیس واقعی (یا test database) و مدل واقعی (یا mock نزدیک به واقعیت) اجرا شوند. خارج از این مرحله: تست‌های واحد و تست‌های UI. نکته حیاتی: این تست‌ها باید سناریوهای مختلف (session بایگانی‌شده، session فعال، تحلیل مجدد) را پوشش دهند. کلیدواژه‌ها: integration_tests, end_to_end, DreamingAgent, database, model. در کد فعلی پروژه، فایل‌های مرتبط با Dreaming Agent در backend/app/services/oversight_deep_scan_service.py (خطوط 1-50) و backend/app/services/oversight_service.py (خطوط 1-100) و backend/app/api/routes/oversight.py (خطوط 1-50) شناسایی شده‌اند. همچنین مدل‌های دیتابیس مرتبط در backend/app/models/inspector_session.py (خطوط 1-30) و backend/app/models/ai_log.py (خطوط 1-20) موجود هستند. تست‌های موجود در backend/tests/test_runtime_verify_stage1.py تا test_runtime_verify_stage9.py الگوی مناسبی برای ساختار تست‌های integration ارائه می‌دهند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. ایجاد فایل جدید backend/tests/test_dreaming_agent_integration.py با کلاس TestDreamingAgentIntegration. 2. پیاده‌سازی متد setUp برای راه‌اندازی دیتابیس تست (SQLite in-memory) و mock کردن AI manager با استفاده از unittest.mock. 3. نوشتن تست test_create_task_and_save_results که فرآیند ایجاد Task توسط Dreaming Agent و ذخیره نتایج در دیتابیس را پوشش می‌دهد. 4. نوشتن تست test_archived_session که سناریوی session بایگانی‌شده را شبیه‌سازی می‌کند. 5. نوشتن تست test_active_session که سناریوی session فعال را پوشش می‌دهد. 6. نوشتن تست test_reanalysis که تحلیل مجدد یک session موجود را تست می‌کند. 7. استفاده از pytest fixtures برای مدیریت state دیتابیس بین تست‌ها. 8. اضافه کردن marker 'integration' به تست‌ها برای اجرای جداگانه.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: feature_request
- اولویت: medium
- تخمین زمان: large

---

# 🔹 مرحله 15: بازبینی نهایی (Audit) و مستندسازی Dreaming Agent

**Scope:** این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده برای Dreaming Agent، بررسی امنیت، performance، و مستندسازی است. همچنین شامل به‌روزرسانی مستندات پروژه و اضافه کردن توضیحات برای استفاده از این قابلیت است. خارج از این مرحله: نوشتن کد جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود.
**Key terms:** code_review, security_audit, performance_review, documentation, final_review

**بخش مربوط از متن کاربر:**
```
(بازبینی نهایی به صورت صریح در درخواست کاربر ذکر نشده است، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده است.)
```

## 🎯 هدف (خلاصه ساختاریافته)
بازبینی نهایی و مستندسازی Dreaming Agent

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/oversight_service.py:1-50` — `OversightService` — احتمالاً Dreaming Agent به عنوان یک سرویس جدید در این فایل یا فایل مجزا اضافه شده است. نیاز به بررسی importها و متدهای جدید.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/api/routes/oversight.py:1-50` — `oversight_router` — اگر Dreaming Agent endpoint جدیدی دارد، باید در این فایل یا فایل route مجزا تعریف شده باشد.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```
- `backend/app/services/ai_manager.py:1-50` — `AIManager` — اگر Dreaming Agent از AI استفاده می‌کند، احتمالاً از ai_manager برای مدیریت پروایدرها استفاده کرده است.
  ```python
  این فایل deep-read نشده — مجری باید مسیر را خود تأیید کند
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/oversight_deep_scan_service.py` (سطر 1) — احتمالاً Dreaming Agent از deep scan برای تحلیل استفاده می‌کند
- `backend/app/services/oversight_inspector_bridge.py` (سطر 1) — اگر Dreaming Agent نیاز به inspector bridge داشته باشد
- `backend/app/services/oversight_verifier.py` (سطر 1) — احتمالاً برای verify خروجی Dreaming Agent استفاده می‌شود
- `backend/app/models/ai_profile.py` (سطر 1) — اگر Dreaming Agent نیاز به پروفایل مدل داشته باشد
- `backend/app/core/database.py` (سطر 1) — برای ذخیره‌سازی داده‌های Dreaming Agent در دیتابیس

## 🔍 Context و وضعیت فعلی
این تسک شامل بازبینی نهایی (Audit) تمام کدهای نوشته‌شده برای Dreaming Agent است. با توجه به اینکه کاربر صراحتاً ذکر کرده 'این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده برای Dreaming Agent، بررسی امنیت، performance، و مستندسازی است' و 'خارج از این مرحله: نوشتن کد جدید' و 'نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود'. کلیدواژه‌های اصلی: code_review, security_audit, performance_review, documentation, final_review. با توجه به deep context موجود، فایل‌های مرتبط با Dreaming Agent در پروژه شناسایی نشدند (احتمالاً در کامیت‌های جدید یا branch مجزا اضافه شده‌اند). بنابراین این تسک بر اساس ساختار سطحی پروژه و فایل‌های موجود در deep context طراحی می‌شود و مجری باید مسیرهای دقیق را تأیید کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. 1. شناسایی تمام فایل‌های مرتبط با Dreaming Agent در پروژه (با جستجوی grep برای کلیدواژه‌های 'dreaming', 'dream', 'agent' در کل پروژه). 2. بازبینی امنیتی کدها با تمرکز بر: injection vulnerabilities, authentication bypass, data leakage, insecure deserialization. 3. بررسی performance: memory leaks, async/await correctness, database query optimization, caching opportunities. 4. مستندسازی: اضافه کردن docstring به تمام توابع جدید، به‌روزرسانی README.md با توضیح قابلیت Dreaming Agent، اضافه کردن مثال استفاده. 5. ایجاد تست‌های unit/integration برای سرویس‌های جدید. 6. انجام code review توسط توسعه‌دهنده دیگر (طبق درخواست کاربر).

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## ⚠️ ریسک‌ها و موارد احتیاط
پیش از merge، تست‌های موجود اجرا شوند تا رگرشن ایجاد نشود.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: medium
- تخمین زمان: large

---

## ✅ معیارهای پذیرش کلی (همهٔ مراحل)
- [ ] همهٔ مراحل بالا با موفقیت پیاده‌سازی شده‌اند
- [ ] تست‌های موجود pass می‌شوند
- [ ] هیچ regression رخ نداده

## Acceptance Criteria

1. یک سند (مثلاً در `docs/`) با عنوان 'Cloud Dreaming Concept & Requirements' ایجاد شود که هدف، دامنه، و نیازمندی‌های سطح بالای قابلیت 'Cloud Dreaming' را توضیح دهد. _(verify: static)_
2. منابع داده‌ای که ایجنت‌های 'Cloud Dreaming' باید بررسی کنند (مانند `AI_Log`, `Debate`, `InspectorSession` یا سایر مدل‌های مرتبط با چت/سیشن) به وضوح در سند نیازمندی‌ها مشخص شوند. _(verify: static)_
3. یک طرح اولیه برای مکانیزم زمان‌بندی و فعال‌سازی 'Cloud Dreaming' در زمان‌های بیکاری سیستم، با اشاره به استفاده از `apscheduler` یا مکانیزم مشابه، در سند نیازمندی‌ها ارائه شود. _(verify: static)_
4. انواع 'ایرادها' و 'نکات' مورد انتظار از تحلیل 'Cloud Dreaming' و چگونگی نگاشت آن‌ها به `AnalysisReportSchema` یا ساختارهای داده‌ای جدید، در سند نیازمندی‌ها تعریف شود. _(verify: static)_
5. یک استراتژی برای ادغام 'نکات' برداشت‌شده در 'حافظه پروژه' (مانند به‌روزرسانی فیلدهای `Project` یا ایجاد `SystemPrompt` جدید) در سند نیازمندی‌ها تشریح شود. _(verify: static)_
6. مدل‌های `ChatSession` و `ChatMessage` در `backend/app/models/chat_session.py` با فیلدهای `id`, `project_id`, `title`, `status`, `created_at`, `updated_at` برای سیشن و `id`, `session_id`, `sender`, `role`, `content`, `timestamp` برای پیام‌ها تعریف شده باشند. _(verify: static)_
7. Endpoint `GET /api/chat/sessions` باید لیست سیشن‌های چت را برگرداند و از فیلتر `status` (active/archived) و `project_id` پشتیبانی کند. _(verify: api_response)_
8. Endpoint `GET /api/chat/sessions/{session_id}/messages` باید تاریخچه پیام‌های یک سیشن خاص را برگرداند. _(verify: api_response)_
9. Endpoint `POST /api/chat/sessions/{session_id}/archive` باید وضعیت یک سیشن را به 'archived' تغییر دهد و Endpoint `POST /api/chat/sessions/{session_id}/activate` باید آن را به 'active' برگرداند. _(verify: api_response)_
10. سرویس `DeepAnalysisService` باید بتواند از `ChatSessionService` برای بازیابی تاریخچه چت‌ها استفاده کند و آن را در فرآیند تحلیل خود لحاظ کند. _(verify: backend_test)_
11. یک دکمه یا لینک با متن 'فعال‌سازی Cloud Dreaming' در تب 'بازرس ویژه' در صفحه جزئیات پروژه قابل مشاهده باشد. _(verify: ui_interaction)_
12. کلیک بر روی دکمه 'فعال‌سازی Cloud Dreaming' یک درخواست POST به `/api/oversight/cloud-dreaming/start` با `project_id` در بدنه درخواست ارسال کند. _(verify: ui_interaction)_
13. endpoint `POST /api/oversight/cloud-dreaming/start` یک پاسخ JSON با `{"success": true, "message": "Cloud Dreaming process initiated", ...}` برگرداند. _(verify: api_response)_
14. متد `start_cloud_dreaming(self, project_id: str)` در کلاس `OversightService` در فایل `backend/app/services/oversight_service.py` تعریف شده باشد. _(verify: static)_
15. عنصر UI برای 'Cloud Dreaming' از نظر بصری با سایر عناصر در تب 'بازرس ویژه' هماهنگ باشد و از کلاس‌های Tailwind CSS استفاده کند. _(verify: manual_only)_
16. یک کامپوننت UI برای انتخاب مدل‌های AI فعال (با استفاده از داده‌های `/api/models/profiles`) باید در صفحه تحلیل یا یک صفحه اختصاصی 'Cloud Dreaming' قابل مشاهده باشد. _(verify: ui_interaction)_
17. فیلدهای ورودی یا کنترل‌ها برای «بازه زمانی تحلیل»، «عمق تحلیل» و «نوع نکات مورد نظر» باید در UI جدید قابل مشاهده و تعامل باشند. _(verify: ui_interaction)_
18. مدل Pydantic `AnalysisRequest` در `backend/app/api/routes/analysis.py` باید شامل فیلدهای `analysis_time_range`, `analysis_depth`, `insight_types` و `cloud_dreaming_instruction` باشد. _(verify: static)_
19. endpoint `run_analysis_stream` در `backend/app/api/routes/analysis.py` باید تنظیمات جدید را از `AnalysisRequest` به متد `deep_analyzer.run_full_analysis` به درستی ارسال کند. _(verify: static)_
20. شروع یک تحلیل 'Cloud Dreaming' از UI با مدل‌ها و تنظیمات انتخاب شده باید با موفقیت endpoint `run_analysis_stream` را فراخوانی کرده و پیشرفت را نمایش دهد و در نهایت با یک گزارش تکمیل شود. _(verify: api_response)_
21. یک endpoint جدید POST به آدرس `/api/oversight/analyze-chat-session` باید در `backend/app/api/routes/oversight.py` ایجاد شود که یک `ChatAnalysisRequest` را دریافت کرده و لیستی از `ChatIssueSchema` را برگرداند. _(verify: api_response)_
22. سرویس `ChatIssueDetector` در `backend/app/services/scan_v5/chat_issue_detector.py` باید قادر باشد با استفاده از `ai_manager` یک مدل AI را فراخوانی کرده و لیستی از ایرادات ساختاریافته (`ChatIssueSchema`) را از متن چت ورودی استخراج کند. _(verify: backend_test)_
23. مدل Pydantic `ChatIssueSchema` باید در `backend/app/models/analysis_report.py` تعریف شود و شامل فیلدهای `title`, `description`, `severity`, `source_message_id`, `timestamp`, و `suggested_fix` باشد. _(verify: static)_
24. سرویس `OversightService` در `backend/app/services/oversight_service.py` باید متد `analyze_chat_for_issues` را داشته باشد که به درستی `ChatIssueDetector` را فراخوانی کرده و نتایج آن را برگرداند. _(verify: static)_
25. سیستم باید بتواند حجم زیادی از متن چت (تا `MAX_PROMPT_LENGTH` تعریف شده در `backend/.env.example`) را بدون خطا پردازش کند و ایرادات را شناسایی کند. _(verify: backend_test)_
26. فایل `backend/app/services/key_point_extraction_service.py` باید وجود داشته باشد و کلاس `KeyPointExtractionService` را شامل شود. _(verify: static)_
27. فراخوانی `KeyPointExtractionService().extract_key_points("sample content", "project123")` باید لیستی از رشته‌ها را برگرداند و از `ai_manager` استفاده کند. _(verify: backend_test)_
28. پس از اجرای `run_analysis_stream` برای یک پروژه، نکات کلیدی باید در حافظه پروژه (مدل `Project`) ذخیره شده باشند و حداقل یک نکته استخراج شده باشد. _(verify: api_response)_
29. مدل `Project` در `backend/app/models/project.py` باید فیلدی به نام `key_points` از نوع JSON (یا مشابه) برای ذخیره لیست نکات کلیدی داشته باشد. _(verify: static)_
30. endpoint `GET /api/projects/{project_id}/key-points` باید نکات کلیدی ذخیره شده برای پروژه را برگرداند. _(verify: api_response)_
31. فایل `backend/app/services/memory_improvement_service.py` با یک کلاس `MemoryImprovementService` و متد `extract_memory_points` ایجاد شود. _(verify: static)_
32. متد `run_full_analysis` در `DeepAnalysisService` (یا منطق پس از فراخوانی آن در `backend/app/api/routes/analysis.py`)، سرویس `MemoryImprovementService` را فراخوانی کند. _(verify: static)_
33. مدل `Project` در `backend/app/models/project.py` باید فیلدی به نام `memory_improvement_points` از نوع JSONB برای ذخیره نکات بهبود حافظه داشته باشد. _(verify: static)_
34. یک endpoint جدید در `backend/app/api/routes/project_memory.py` برای بازیابی نکات بهبود حافظه یک پروژه خاص اضافه شود که پاسخ HTTP 200 با فیلدهای `project_id` و `memory_points` را برگرداند. _(verify: api_response)_
35. `MemoryImprovementService` باید بتواند از داده‌های شبیه‌سازی شده تاریخچه چت و گزارش تحلیل، حداقل ۳ مفهوم کلیدی مرتبط با بهبود حافظه را استخراج کند. _(verify: backend_test)_
36. یک مدل دیتابیس جدید به نام `LearningInsight` با فیلدهای `source_type`, `source_id`, `insight_type`, `description`, `recommendation`, `tags`, `model_ids_involved` و `extracted_at` در `backend/app/models/learning_insight.py` تعریف شده باشد. _(verify: static)_
37. سرویس `LearningInsightService` در `backend/app/services/learning_insight_service.py` با متد `extract_from_analysis_report` پیاده‌سازی شده باشد که از `ai_manager` برای پردازش گزارش تحلیل استفاده کند. _(verify: static)_
38. پس از اتمام موفقیت‌آمیز یک تحلیل پروژه از طریق endpoint `POST /analysis/run-stream`، حداقل یک `LearningInsight` مرتبط با `source_type='analysis_report'` در دیتابیس ذخیره شود. _(verify: api_response)_
39. یک endpoint جدید `GET /analysis/insights` (یا مشابه) برای بازیابی لیست نکات آموزشی استخراج‌شده وجود داشته باشد و بتواند بر اساس `source_type` فیلتر کند. _(verify: api_response)_
40. نکات آموزشی استخراج‌شده باید به صورت کلی قابل استفاده باشند و الگوهای موفق، بهترین روش‌ها یا خطاهای قابل یادگیری را منعکس کنند (بررسی کیفی). _(verify: manual_only)_
41. `AnalysisReportSchema` در `backend/app/models/analysis_report.py` باید شامل فیلدهای `root_causes` (لیستی از `RootCauseSchema`) و `root_cause_summary` (رشته) باشد. _(verify: static)_
42. تابع `run_analysis_stream` در `backend/app/api/routes/analysis.py` باید پس از اتمام تحلیل اولیه، منطق تحلیل ریشه‌ای را فراخوانی کرده و نتایج را در گزارش نهایی ذخیره کند. _(verify: api_response)_
43. endpoint دانلود گزارش (`GET /api/analysis/reports/{report_id}/download?format=json`) باید شامل فیلدهای `root_causes` و `root_cause_summary` در خروجی JSON باشد. _(verify: api_response)_
44. رابط `AnalysisReport` در `frontend/src/app/analysis/page.tsx` باید به‌روزرسانی شود تا فیلدهای `root_causes` و `root_cause_summary` را شامل شود. _(verify: static)_
45. در صفحه تحلیل (`frontend/src/app/analysis/page.tsx`)، نتایج تحلیل ریشه‌ای (خلاصه و لیست ریشه‌ها) باید در بخش نمایش گزارش تحلیل قابل مشاهده باشد. _(verify: ui_interaction)_
46. یک تب یا بخش جدید با عنوان 'آموزشی و حافظه' در صفحه `frontend/src/app/oversight/page.tsx` قابل مشاهده باشد. _(verify: ui_interaction)_
47. با کلیک بر روی تب 'آموزشی و حافظه'، محتوای مربوط به نکات آموزشی و حافظه نمایش داده شود. _(verify: ui_interaction)_
48. API endpoint `GET /oversight/educational-memory/{project_id}` باید لیست نکات آموزشی و حافظه را با کد وضعیت 200 و شامل فیلدهای `id`, `type`, `content` برگرداند. _(verify: api_response)_
49. قابلیت فیلتر و جستجو برای نکات آموزشی و حافظه در UI فرانت‌اند (در `EducationalMemorySection.tsx`) به درستی کار کند و نتایج را بر اساس فیلترها نمایش دهد. _(verify: ui_interaction)_
50. مدل `EducationalMemoryNote` در بک‌اند تعریف شده و به `Project` از طریق `project_id` مرتبط باشد. _(verify: static)_
51. فایل `backend/app/models/extracted_note.py` باید شامل مدل SQLAlchemy `ExtractedNote` با فیلدهای `project_id`, `type`, `content`, `source_file`, `line_numbers`, `model_id`, `severity`, `created_at`, `updated_at` باشد و Pydantic schemas مربوطه را تعریف کند. _(verify: static)_
52. فایل `backend/app/services/note_storage_service.py` باید شامل کلاس `NoteStorageService` با متدهای `create_notes`, `get_notes`, `get_note_by_id`, `delete_note` باشد. _(verify: static)_
53. endpoint `POST /api/notes` باید با ارسال یک لیست از نکات، آن‌ها را در دیتابیس ذخیره کند و پاسخ موفقیت‌آمیز (200 OK) برگرداند. _(verify: api_response)_
54. endpoint `GET /api/notes?project_id={id}&type={type}` باید نکات مرتبط با `project_id` و `type` مشخص را بازیابی کند. _(verify: api_response)_
55. پس از اجرای تحلیل پروژه از طریق `POST /analysis/run-stream`, `DeepAnalysisService` باید نکات استخراج‌شده (شامل issues و recommendations) را به عنوان `ExtractedNote` در دیتابیس ذخیره کند. _(verify: backend_test)_
56. هنگامی که یک تحلیل از طریق `POST /analysis/run-stream` برای یک `project_id` که دارای «نکات ذخیره‌شده» (مثلاً `SystemPrompt`های از نوع 'project_memory') است، آغاز می‌شود، این نکات باید از دیتابیس بازیابی شده و به `DeepAnalysisService` ارسال شوند. _(verify: backend_test)_
57. متد `DeepAnalysisService.run_full_analysis` (مفهومی) باید `instruction` تقویت‌شده حاوی «نکات ذخیره‌شده» را دریافت کرده و آن‌ها را به درستی در پرامپت ارسالی به مدل AI بگنجاند. _(verify: static)_
58. اجرای تحلیل برای پروژه‌ای بدون «نکات ذخیره‌شده» باید بدون خطا و تغییرات غیرمنتظره در خروجی AI، مانند رفتار فعلی سیستم، عمل کند. _(verify: backend_test)_
59. مدل `SystemPrompt` باید به درستی تعریف شده باشد و فیلدهای `project_id` و `content` (یا معادل آن‌ها) را برای ذخیره و بازیابی نکات پروژه داشته باشد. _(verify: static)_

## Task Steps

### Step 1: ایجاد دیتاکلاس و ساختار داده برای Dreaming Agent Task و نتایج تحلیل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل تعریف دیتاکلاس‌های اصلی برای نمایش یک Task تحلیل (Dreaming Agent Task) و نتایج آن (AnalysisResult) است. این دیتاکلاس‌ها باید شامل فیلدهایی برای وضعیت (pending/running/completed/failed)، نوع تحلیل (educational/memory)، session_id، chat_id، لیست نکات استخراج‌شده، و timestamp باشند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی در دیتابیس. نکته حیاتی: این دیتاکلاس‌ها باید به‌گونه‌ای طراحی شوند که هم برای تحلیل session‌های بایگانی‌شده و هم session‌های فعال قابل استفاده باشند.
**Excerpt:**
```
می‌خواستم همچین چیزی برای قسمت بازرس ویژه یعنی توی صفحه پروژه‌ها ذیل تب بازرس ویژه تو اون چتش یه سری پروژه‌هایی که هست یعنی یه سری سیشن‌ها که هست و چت‌هایی که انجام میشه اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه، چیزایی که می‌تونه نکته آموزشی باشه به صورت کلی مورد استفاده بشه اونها رو پیدا بکنه
```

### Step 2: ایجاد جدول دیتابیس برای ذخیره Task‌ها و نتایج تحلیل Dreaming Agent
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک مدل دیتابیس (مثلاً با SQLAlchemy یا Prisma) برای ذخیره‌سازی پایدار Task‌های Dreaming Agent و نتایج تحلیل آنهاست. فیلدها باید شامل: id, session_id, chat_id, task_type (educational/memory), status, result (JSON), created_at, updated_at, analyzed_at باشند. خارج از این مرحله: مهاجرت دیتابیس، پیاده‌سازی منطق تحلیل، و UI. نکته حیاتی: باید قابلیت ذخیره نتایج به صورت JSON را داشته باشد تا نکات استخراج‌شده به صورت ساختاریافته ذخیره شوند.
**Excerpt:**
```
و اینها رو به عنوان نکات آموزشی یا نکات برای حافظه تو اونجا ذخیره بکنه و مدل‌های بعدی که می‌خوان تو اون سیشن‌های بعدی یا سیشن فعال فعلی کار بکنن از اون استفاده بکنن. یعنی مراجعه داشته باشن.
```

### Step 3: ایجاد سرویس اصلی Dreaming Agent با قابلیت انتخاب مدل فعال
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک سرویس (کلاس یا ماژول) به نام DreamingAgentService است که وظیفه اصلی آن مدیریت فرآیند تحلیل است. این سرویس باید قابلیت دریافت یک مدل فعال (از بین مدل‌های موجود در سیستم) را داشته باشد و بتواند یک Task تحلیل جدید ایجاد کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: این سرویس باید به‌گونه‌ای طراحی شود که بتواند هم session‌های بایگانی‌شده و هم session‌های فعال را پردازش کند.
**Excerpt:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها که بایگانی شدن یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده
```

### Step 4: پیاده‌سازی منطق تحلیل session‌های بایگانی‌شده برای استخراج نکات آموزشی و حافظه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطق اصلی تحلیل است. این منطق باید بتواند session‌های بایگانی‌شده (archived) را دریافت کرده، پیام‌های آنها را بررسی کند، و نکات آموزشی (educational) و نکات مرتبط با حافظه (memory) را استخراج کند. نکات آموزشی باید ریشه مشکلات (root cause) را شناسایی کنند و نکات حافظه باید اطلاعات عمومی مفید را استخراج کنند. خارج از این مرحله: تحلیل session‌های فعال، UI، و ذخیره‌سازی نتایج. نکته حیاتی: تحلیل باید عمیق باشد و به دنبال ریشه مشکلات بگردد، نه فقط نکات سطحی.
**Excerpt:**
```
یعنی داده‌هایی رو برداشت بکنه که به درد آموزشی باشه و به درد حافظه باشه. یعنی یه چیزای موردی شاید به درد نخوره بلکه بره ریشه کارو پیدا بکنه. یعنی ریشه مثلاً فلان مشکل چی بوده که مدل اینجا تو این سیشن کارو اشتباه انجام داده. یا چیزای دیگه که می‌تونسته بهتر باشه.
```

### Step 5: پیاده‌سازی منطق تحلیل session‌های فعال برای استخراج نکات تا لحظه تحلیل
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطق تحلیل برای session‌های فعال (active) است. این منطق باید بتواند session فعال را دریافت کرده، پیام‌های موجود تا لحظه تحلیل را بررسی کند، و نکات آموزشی و حافظه را استخراج کند. خارج از این مرحله: تحلیل session‌های بایگانی‌شده، UI، و ذخیره‌سازی نتایج. نکته حیاتی: این تحلیل باید فقط پیام‌های موجود تا لحظه فراخوانی را بررسی کند و منتظر پیام‌های جدید نماند.
**Excerpt:**
```
یا حتی سیشن فعالی که چت‌هاش مثلاً تا اون لحظه انجام شده بتونه نکات رو یادداشت بکنه، نکات رو برداشت بکنه، حافظه رو چیزایی که مربوط به حافظه می‌تونه به صورت کلی مفید باشه رو اونا رو پیدا بکنه
```

### Step 6: ایجاد مکانیزم علامت‌گذاری session‌ها و پیام‌های تحلیل‌شده برای جلوگیری از تحلیل مجدد
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم علامت‌گذاری (flagging) برای session‌ها و پیام‌هایی است که قبلاً تحلیل شده‌اند. این مکانیزم باید از تحلیل مجدد session‌های بایگانی‌شده و کاملاً تحلیل‌شده جلوگیری کند. برای session‌های فعال، باید پیام‌های جدید (تحلیل‌نشده) را شناسایی کرده و فقط آنها را تحلیل کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و ذخیره‌سازی نتایج. نکته حیاتی: session‌های فعال ممکن است نیاز به تحلیل مجدد داشته باشند چون پیام‌های جدید به آنها اضافه می‌شود.
**Excerpt:**
```
اینکه هر سیشن و پیام هایی که استخراج لازم ازشون انجام شده یا به هر نحوی بررسی شده باید طوری علامت بخورن که دیگه هر بار مراجه انجام نشه ... مگر برای سیشن باز موجود که ممکن بررسی شده باشه پیامهاش ولی چون هنوز ارشیو نشده ممکن لازم باشه بازم بعدا بررسی بشه و فقط وقتی ارشیو شدن و بررسی هم شده باشن همه چیز دفعه بعدیش بررسی نمیشه
```

### Step 7: ایجاد زیرساخت زمان‌بندی (Scheduler) برای اجرای خودکار Dreaming Agent در زمان‌های بیکاری
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک زمان‌بند (scheduler) است که به صورت خودکار Dreaming Agent را در زمان‌های مشخص (مثلاً وقتی کاربر آفلاین است یا سیستم بیکار است) اجرا کند. این زمان‌بند باید session‌های بایگانی‌شده و فعال را برای تحلیل انتخاب کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و انتخاب مدل. نکته حیاتی: زمان‌بندی باید قابل تنظیم باشد و بتوان آن را فعال/غیرفعال کرد.
**Excerpt:**
```
یه قابلیت می‌خوام راه بندازم چون شنیدم که کلاد یه قابلیتی داره تحت عنوان کلاد دریمینگ و کارش اینه که بتونه مثلاً زمان‌هایی که حالا کار نمی‌کنم یا آفلاینه ایجنت‌هاش برن توی سیشن‌ها و چت‌ها رو بررسی بکنن و ایرادها رو بگیرن یا یه سری نکات برداشت کنن
```

### Step 8: ایجاد تب 'آموزشی و حافظه' در صفحه بازرس ویژه (UI)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک تب جدید به نام 'آموزشی و حافظه' (Educational & Memory) در صفحه بازرس ویژه (Inspector Page) است. این تب باید نتایج تحلیل‌های Dreaming Agent را به صورت دسته‌بندی‌شده (educational و memory) نمایش دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، انتخاب مدل، و زمان‌بندی. نکته حیاتی: این تب باید فقط نتایج را نمایش دهد و امکان فیلتر بر اساس نوع (educational/memory) و session را فراهم کند.
**Excerpt:**
```
و اینها رو ذیل تب ذیل اون خود صفحه بازرس ویژه یه جا هست به اسم آموزشی و حافظه. اونا رو تو اونجا قرار بده و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن.
```

### Step 9: ایجاد UI برای انتخاب مدل فعال و تنظیمات Dreaming Agent در تب بازرس ویژه
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد یک بخش UI در تب بازرس ویژه (یا در تنظیمات پروژه) است که به کاربر اجازه می‌دهد یک مدل فعال را از بین مدل‌های موجود انتخاب کند و تنظیمات مربوط به Dreaming Agent (مانند نوع تحلیل، session‌های هدف) را پیکربندی کند. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و نمایش نتایج. نکته حیاتی: این UI باید فقط برای کاربران دارای دسترسی مناسب (مثلاً ادمین پروژه) قابل مشاهده باشد.
**Excerpt:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
```

### Step 10: اتصال UI انتخاب مدل و تنظیمات به سرویس Dreaming Agent (Backend Integration)
**Status:** `pending` (0%)
**Scope:** این مرحله شامل ایجاد API endpoints برای ذخیره و بازیابی تنظیمات Dreaming Agent (مدل انتخاب‌شده، نوع تحلیل، session‌های هدف) و اتصال UI به این endpoints است. همچنین شامل ایجاد یک endpoint برای شروع دستی یک Task تحلیل است. خارج از این مرحله: پیاده‌سازی منطق تحلیل، زمان‌بندی، و UI. نکته حیاتی: تنظیمات باید در دیتابیس ذخیره شوند تا پس از رفرش صفحه از بین نروند.
**Excerpt:**
```
اونجا مثلاً یه یکی از مدل‌های فعال موجود رو انتخاب بکنم که این کارش همین باشه. مثلاً طی تنظیماتی که براش قرار میدم بتونه بره تو این سیشن‌ها و چت‌ها
```

### Step 11: ایجاد مکانیزم مراجعه مدل‌ها به نتایج تحلیل (Memory Retrieval) در حین چت
**Status:** `pending` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک مکانیزم است که به مدل‌های فعال در یک session اجازه می‌دهد در حین چت به نتایج تحلیل‌های قبلی (educational و memory) مراجعه کنند. این مکانیزم باید نتایج مرتبط با session فعلی یا session‌های مشابه را بازیابی کرده و در context چت قرار دهد. خارج از این مرحله: پیاده‌سازی منطق تحلیل، UI، و زمان‌بندی. نکته حیاتی: کاربر اشاره کرده که زیرساخت مراجعه (retrieval) قبلاً پیاده‌سازی شده است، بنابراین این مرحله باید از آن زیرساخت استفاده کند.
**Excerpt:**
```
و مدل‌هایی که بعداً می‌خوان تو اون چت کار بکنن از اون استفاده بکنن. یعنی مراجعه داشته باشن. البته فکر می‌کنم زیرساخت مراجعه کردن رو قبلاً پیاده‌سازی کردم.
```

### Step 12: نوشتن تست‌های واحد برای دیتاکلاس‌ها و مدل دیتابیس Dreaming Agent
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای دیتاکلاس‌های ایجادشده (DreamingAgentTask, AnalysisResult) و مدل دیتابیس است. تست‌ها باید ایجاد، به‌روزرسانی، و بازیابی اشیا را پوشش دهند. خارج از این مرحله: تست‌های integration، تست‌های UI، و تست‌های منطق تحلیل. نکته حیاتی: تست‌ها باید مستقل از دیتابیس واقعی باشند (با استفاده از mock یا in-memory database).
**Excerpt:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

### Step 13: نوشتن تست‌های واحد برای سرویس Dreaming Agent
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های واحد برای DreamingAgentService است. تست‌ها باید ایجاد Task، انتخاب مدل، و فراخوانی متدهای تحلیل را با استفاده از mock پوشش دهند. خارج از این مرحله: تست‌های integration با مدل واقعی، تست‌های UI، و تست‌های زمان‌بندی. نکته حیاتی: از mock برای شبیه‌سازی مدل و session‌ها استفاده شود.
**Excerpt:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

### Step 14: نوشتن تست‌های integration برای فرآیند کامل Dreaming Agent
**Status:** `pending` (0%)
**Scope:** این مرحله شامل نوشتن تست‌های integration است که فرآیند کامل Dreaming Agent را از ایجاد Task تا ذخیره نتایج و نمایش در UI پوشش می‌دهد. این تست‌ها باید با دیتابیس واقعی (یا test database) و مدل واقعی (یا mock نزدیک به واقعیت) اجرا شوند. خارج از این مرحله: تست‌های واحد و تست‌های UI. نکته حیاتی: این تست‌ها باید سناریوهای مختلف (session بایگانی‌شده، session فعال، تحلیل مجدد) را پوشش دهند.
**Excerpt:**
```
(تست‌ها به صورت صریح در درخواست کاربر ذکر نشده‌اند، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده‌اند.)
```

### Step 15: بازبینی نهایی (Audit) و مستندسازی Dreaming Agent
**Status:** `pending` (0%)
**Scope:** این مرحله شامل بازبینی نهایی تمام کدهای نوشته‌شده برای Dreaming Agent، بررسی امنیت، performance، و مستندسازی است. همچنین شامل به‌روزرسانی مستندات پروژه و اضافه کردن توضیحات برای استفاده از این قابلیت است. خارج از این مرحله: نوشتن کد جدید. نکته حیاتی: این مرحله باید توسط یک توسعه‌دهنده دیگر (code review) انجام شود.
**Excerpt:**
```
(بازبینی نهایی به صورت صریح در درخواست کاربر ذکر نشده است، اما به عنوان یک مرحله استاندارد برای اطمینان از کیفیت کد اضافه شده است.)
```
