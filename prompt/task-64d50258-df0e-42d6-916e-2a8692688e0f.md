---
task_id: 64d50258-df0e-42d6-916e-2a8692688e0f
title: سرویس‌های AI (ai_manager, openai_service, claude_service) بدون تست واحد
type: bug
priority: critical
execution_priority: 1000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T19:15:01.688718+00:00'
updated_at: '2026-05-20T04:25:14.801576+00:00'
target_files:
- backend/app/services/ai_manager.py
---

# سرویس‌های AI (ai_manager, openai_service, claude_service) بدون تست واحد

## Raw Idea

فایل‌های `backend/app/services/ai_manager.py` (هاب اصلی با 40 import کننده)، `backend/app/services/openai_service.py`، `backend/app/services/claude_service.py` و `backend/app/services/gemini_service.py` هیچ فایل تست متناظری ندارند. این سرویس‌ها هسته اصلی تعامل با مدل‌های AI هستند و هرگونه باگ در آن‌ها می‌تواند کل سیستم را از کار بیندازد. با توجه به اینکه 40 فایل مختلف از ai_manager استفاده می‌کنند، این یک critical path است.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
سرویس‌های AI (ai_manager, openai_service, claude_service) بدون تست واحد

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/app/services/ai_manager.py:1-50` — `AIProviderManager` — کلاس اصلی مدیریت AI که باید تست شود
  ```python
  class AIProviderManager:
      def __init__(self):
          self._providers = {}
          self._init_errors = {}
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + OpenAI SDK + Anthropic SDK + Google AI SDK

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/openai_service.py` (سطر 1) — یکی از providerهای اصلی
- `backend/app/services/claude_service.py` (سطر 1) — دومین provider اصلی
- `backend/app/services/ai_base.py` (سطر 1) — کلاس پایه که 39 فایل import می‌کنند
- `backend/app/services/gemini_service.py` — `ai_manager.py` این فایل را import می‌کند
- `backend/app/api/routes/analysis.py` — این فایل `ai_manager.py` را import می‌کند (caller)
- `backend/app/api/routes/models.py` — این فایل `ai_manager.py` را import می‌کند (caller)

## 🌐 نقشهٔ وابستگی‌ها
ai_manager توسط 40 فایل مختلف import می‌شود؛ هر تغییری در آن روی کل سیستم اثر می‌گذارد.

## 🔍 Context و وضعیت فعلی
فایل‌های `backend/app/services/ai_manager.py` (هاب اصلی با 40 import کننده)، `backend/app/services/openai_service.py`، `backend/app/services/claude_service.py` و `backend/app/services/gemini_service.py` هیچ فایل تست متناظری ندارند. این سرویس‌ها هسته اصلی تعامل با مدل‌های AI هستند و هرگونه باگ در آن‌ها می‌تواند کل سیستم را از کار بیندازد. با توجه به اینکه 40 فایل مختلف از ai_manager استفاده می‌کنند، این یک critical path است.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] تست fallback chain: اگر OpenAI خطا دهد، Claude جایگزین شود
- [ ] تست timeout: اگر مدلی timeout خورد، مدل بعدی امتحان شود
- [ ] تست rate limit handling: خطای 429 به درستی مدیریت شود
- [ ] تست initialization: providerهای ناقص (بدون API key) نادیده گرفته شوند
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد تست‌های واحد برای ai_manager با mock کردن providerها. تست‌های integration برای fallback chain بین مدل‌ها.

## 💡 نمونه‌های قبل/بعد
**تست fallback chain**

_قبل:_
```
هیچ تستی وجود ندارد
```

_بعد:_
```
def test_fallback_when_primary_fails():
    manager = AIProviderManager()
    manager._providers = {'openai': MockOpenAI(), 'claude': MockClaude()}
    manager._providers['openai'].generate.side_effect = Exception('API Error')
    result = await manager.generate('test', messages=[...])
    assert result.content == 'fallback response'
    assert manager._providers['claude'].generate.called
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_ai_manager.py -v`
- `pytest backend/tests/test_ai_fallback.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
نیاز به mock کردن SDKهای خارجی؛ ممکن است برخی رفتارهای واقعی در mock قابل شبیه

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: bug
- اولویت: critical
- تخمین زمان: medium

## Acceptance Criteria

1. تست fallback chain: اگر OpenAI خطا دهد، Claude جایگزین شود _(verify: backend_test)_
2. تست timeout: اگر مدلی timeout خورد، مدل بعدی امتحان شود _(verify: backend_test)_
3. تست rate limit handling: خطای 429 به درستی مدیریت شود _(verify: backend_test)_
4. تست initialization: providerهای ناقص (بدون API key) نادیده گرفته شوند _(verify: backend_test)_
