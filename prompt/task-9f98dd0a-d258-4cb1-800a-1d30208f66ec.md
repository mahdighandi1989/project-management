---
task_id: 9f98dd0a-d258-4cb1-800a-1d30208f66ec
title: فقدان تست واحد برای سرویس‌های اصلی AI (ai_manager, ai_base, model_profiler)
type: other
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-17T07:09:43.942197+00:00'
updated_at: '2026-06-03T18:16:12.969492+00:00'
target_files:
- backend/tests/test_ai_services.py
---

# فقدان تست واحد برای سرویس‌های اصلی AI (ai_manager, ai_base, model_profiler)

## Raw Idea

سرویس‌های `ai_manager.py`، `ai_base.py` و `model_profiler.py` هسته اصلی تعامل با مدل‌های AI هستند و توسط بیش از ۳۰ فایل دیگر import می‌شوند. با این حال، هیچ فایل تست مستقیمی برای این سرویس‌ها در پوشه `backend/tests/` وجود ندارد. این یک ریسک جدی است زیرا هر تغییری در منطق انتخاب مدل، fallback، یا امتیازدهی می‌تواند بدون هشدار، کل سیستم را مختل کند.

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
فقدان تست واحد برای سرویس‌های اصلی AI (ai_manager, ai_base, model_profiler)

## 📍 موقعیت دقیق در پروژه
_(file:line — symbol — snippet)_

- `backend/tests/test_ai_services.py:1-1` — `N/A` — فایل تست جدید برای سرویس‌های AI
  ```python
  فایل جدید
  ```

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
FastAPI + Pytest + asyncio

## 🔗 فایل‌های مرتبط (Cross-references)
_(فایل‌هایی که با موقعیت‌های هدف در ارتباط هستند — import، caller، shared state)_

- `backend/app/services/ai_manager.py` (سطر 1) — سرویس اصلی مدیریت مدل‌ها و fallback
- `backend/app/services/ai_base.py` (سطر 1) — کلاس پایه برای تمام سرویس‌های AI
- `backend/app/services/model_profiler.py` (سطر 1) — سرویس امتیازدهی و پروفایل مدل‌ها

## 🌐 نقشهٔ وابستگی‌ها
این سه سرویس توسط ۴۰+ فایل دیگر (از جمله تمام route‌های اصلی) استفاده می‌شوند. عدم تست آن‌ها به معنای تست نکردن قلب سیستم است.

## 🔍 Context و وضعیت فعلی
سرویس‌های `ai_manager.py`، `ai_base.py` و `model_profiler.py` هسته اصلی تعامل با مدل‌های AI هستند و توسط بیش از ۳۰ فایل دیگر import می‌شوند. با این حال، هیچ فایل تست مستقیمی برای این سرویس‌ها در پوشه `backend/tests/` وجود ندارد. این یک ریسک جدی است زیرا هر تغییری در منطق انتخاب مدل، fallback، یا امتیازدهی می‌تواند بدون هشدار، کل سیستم را مختل کند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] فایل `backend/tests/test_ai_services.py` ایجاد شود.
- [ ] حداقل ۳ تست واحد برای `ai_manager.generate_with_fallback` وجود داشته باشد.
- [ ] حداقل ۲ تست واحد برای `model_profiler.update_profile` وجود داشته باشد.
- [ ] همه تست‌ها با دستور `pytest backend/tests/test_ai_services.py -v` پاس شوند.
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. ایجاد یک فایل تست جدید `backend/tests/test_ai_services.py` که شامل تست‌های واحد برای `ai_manager` (مدیریت fallback)، `ai_base` (پایه تعامل با API) و `model_profiler` (محاسبه امتیازات) باشد.

## 💡 نمونه‌های قبل/بعد
**تست fallback در ai_manager**

_قبل:_
```
هیچ تستی وجود ندارد.
```

_بعد:_
```
```python
@pytest.mark.asyncio
async def test_ai_manager_fallback():
    manager = get_ai_manager()
    # شبیه‌سازی شکست مدل اول
    result = await manager.generate_with_fallback(
        model_ids=['invalid-model', 'gpt-4o-mini'],
        messages=[Message(role="user", content="test")]
    )
    assert result is not None
    assert result.model_id == 'gpt-4o-mini'
```
```

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest backend/tests/test_ai_services.py -v`

## ⚠️ ریسک‌ها و موارد احتیاط
کم. این یک فایل تست جدید است و خطری برای کد فعلی ندارد.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: other
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. فایل `backend/tests/test_ai_services.py` ایجاد شود. _(verify: static)_
2. حداقل ۳ تست واحد برای `ai_manager.generate_with_fallback` وجود داشته باشد. _(verify: backend_test)_
3. حداقل ۲ تست واحد برای `model_profiler.update_profile` وجود داشته باشد. _(verify: backend_test)_
4. همه تست‌ها با دستور `pytest backend/tests/test_ai_services.py -v` پاس شوند. _(verify: backend_test)_
