---
task_id: 38c6eb2b-6fb4-4274-afb2-62a9b81c32db
title: '[Effectiveness] فقدان تست‌های یکپارچگی بین AI Manager و Oversight Service'
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T10:32:30.798115+00:00'
updated_at: '2026-06-02T17:47:12.147064+00:00'
---

# [Effectiveness] فقدان تست‌های یکپارچگی بین AI Manager و Oversight Service

## Raw Idea

## 🎯 هدف مطلوب (outcome target)
پس از افزودن تست‌ها، 100% از سناریوهای fallback و load balancing باید بدون خطا کار کنند و زمان بازیابی از خطا کمتر از 5 ثانیه باشد

## 📊 وضعیت فعلی
error_rate 0% نشان‌دهنده عدم وجود خطا در 30 روز است، اما این می‌تواند به دلیل عدم پوشش سناریوهای واقعی (مثلاً قطعی سرویس OpenAI) باشد. بدون تست یکپارچگی، ریسک خرابی در production بالاست.

## 🛠 اقدام پیشنهادی
افزودن integration tests برای شبیه‌سازی قطعی سرویس‌های AI و بررسی عملکرد fallback و load balancing

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.
---
[scan #2 at 2026-05-15T17:39:49.605641+00:00]
## 🎯 هدف مطلوب (outcome target)
پس از افزودن تست‌های یکپارچگی، 100% سناریوهای fallback بین سرویس نظارت گیت‌هاب و AI Manager باید بدون خطا کار کنند.

## 📊 وضعیت فعلی
Oversight Service با AI Manager یکپارچه است اما هیچ داده‌ای از عملکرد این یکپارچگی در outcome data دیده نمی‌شود. ممکن است در سناریوهای 
---
[scan #3 at 2026-05-17T07:20:34.097596+00:00]
## 🎯 هدف مطلوب (outcome target)
پوشش 90% سناریوهای تعامل بین AI Manager و Oversight Service با تست‌های خودکار تا 2 هفته آینده

## 📊 وضعیت فعلی
با وجود یکپارچگی طراحی شده بین این دو سرویس، هیچ داده‌ای از تست‌های موفق یا شکست‌خورده در outcome data وجود ندارد

## 🛠 اقدام پیشنهادی
پیاده‌سازی integration

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[Effectiveness] فقدان تست‌های یکپارچگی بین AI Manager و Oversight Service

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🔍 Context و وضعیت فعلی
## 🎯 هدف مطلوب (outcome target)
پس از افزودن تست‌ها، 100% از سناریوهای fallback و load balancing باید بدون خطا کار کنند و زمان بازیابی از خطا کمتر از 5 ثانیه باشد

## 📊 وضعیت فعلی
error_rate 0% نشان‌دهنده عدم وجود خطا در 30 روز است، اما این می‌تواند به دلیل عدم پوشش سناریوهای واقعی (مثلاً قطعی سرویس OpenAI) باشد. بدون تست یکپارچگی، ریسک خرابی در production بالاست.

## 🛠 اقدام پیشنهادی
افزودن integration tests برای شبیه‌سازی قطعی سرویس‌های AI و بررسی عملکرد fallback و load balancing

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] outcome target به‌صورت measurable بازنویسی شد
- [ ] کد تغییر کرد تا outcome target محقق شود
- [ ] test E2E که outcome را اندازه می‌گیرد عبور می‌کند
- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: outcome target را به‌صورت قابل اندازه‌گیری بازنویسی کن (مثلاً: «email send rate > 95% در ۱۰۰ تلاش»).
گام ۲: کد را تغییر بده تا outcome محقق شود.
گام ۳: یک end-to-end test که outcome را اندازه می‌گیرد بنویس.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest -k 'outcome or e2e'`

## ⚠️ ریسک‌ها و موارد احتیاط
بهبود outcome ممکن است latency یا cost را افزایش دهد — قبل/بعد metric ها را compare کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. outcome target به‌صورت measurable بازنویسی شد _(verify: static)_
2. کد تغییر کرد تا outcome target محقق شود _(verify: backend_test)_
3. test E2E که outcome را اندازه می‌گیرد عبور می‌کند _(verify: backend_test)_
4. metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد _(verify: static)_

## Task Steps

### Step 1: بررسی و مستندسازی وضعیت فعلی fallback و load balancing در AI Manager
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی کامل کد موجود در repository برای یافتن پیاده‌سازی فعلی fallback و load balancing در AI Manager است. باید مشخص شود که آیا این قابلیت‌ها از قبل وجود دارند، چگونه کار می‌کنند، و چه فایل‌هایی درگیر هستند. همچنین باید بررسی شود که آیا تست‌های یکپارچگی یا unit test مرتبطی وجود دارد. خروجی این مرحله یک مستند کوتاه (در قالب کامیت message یا فایل markdown) است که وضعیت فعلی را شرح می‌دهد. این مرحله شامل هیچ تغییری در کد نمی‌شود.
**Excerpt:**
```
📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.
```

### Step 2: طراحی و پیاده‌سازی mock service برای شبیه‌سازی قطعی سرویس‌های AI
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل ایجاد یک mock service (احتمالاً با استفاده از unittest.mock یا کتابخانه‌ای مشابه) است که بتواند قطعی سرویس‌های AI مانند OpenAI را شبیه‌سازی کند. این mock باید بتواند خطاهای مختلف (HTTP 500, timeout, connection error) را برگرداند. این mock در فایل tests/ یا tests/integration/ قرار می‌گیرد. این مرحله شامل نوشتن تست‌های واقعی نمی‌شود.
**Excerpt:**
```
افزودن integration tests برای شبیه‌سازی قطعی سرویس‌های AI و بررسی عملکرد fallback و load balancing

## ⚙️ ماهیت این finding
این یک effectiveness issue است — کد ممکن است syntactically کار کند ولی **outcome مطلوب** (مثل: «فرم باید ایمیل ارسال کند») حاصل نمی‌شود. verify باید outcome را اندازه بگیرد، نه فقط وجود فایل/خط.
```

### Step 3: پیاده‌سازی integration test برای fallback به سرویس جایگزین هنگام قطعی سرویس اصلی
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست یکپارچگی است که سناریوی قطعی سرویس اصلی AI (مثلاً OpenAI) را شبیه‌سازی می‌کند و بررسی می‌کند که AI Manager به‌درستی به سرویس جایگزین (fallback) سوئیچ می‌کند. تست باید از mock ساخته شده در مرحله قبل استفاده کند. معیار موفقیت: درخواست با خطا مواجه نشود و پاسخ معتبر از سرویس جایگزین برگردد. این تست در فایل tests/integration/test_fallback.py قرار می‌گیرد.
**Excerpt:**
```
پس از افزودن تست‌ها، 100% از سناریوهای fallback و load balancing باید بدون خطا کار کنند و زمان بازیابی از خطا کمتر از 5 ثانیه باشد

## 📊 وضعیت فعلی
error_rate 0% نشان‌دهنده عدم وجود خطا در 30 روز است، اما این می‌تواند به دلیل عدم پوشش سناریوهای واقعی (مثلاً قطعی سرویس OpenAI) باشد. بدون تست یکپارچگی، ریسک خرابی در production بالاست.
```

### Step 4: پیاده‌سازی integration test برای load balancing بین چند سرویس AI
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست یکپارچگی است که بررسی می‌کند AI Manager درخواست‌ها را به‌طور متوازن بین چند سرویس AI توزیع می‌کند. تست باید با شبیه‌سازی چند سرویس (با استفاده از mock) و ارسال چندین درخواست، توزیع تقریباً یکنواخت را تأیید کند. معیار موفقیت: تعداد درخواست‌های ارسال شده به هر سرویس در یک محدوده قابل قبول (مثلاً 40-60% از کل) باشد. این تست در فایل tests/integration/test_load_balancing.py قرار می‌گیرد.
**Excerpt:**
```
پس از افزودن تست‌ها، 100% از سناریوهای fallback و load balancing باید بدون خطا کار کنند و زمان بازیابی از خطا کمتر از 5 ثانیه باشد

## 🛠 اقدام پیشنهادی
افزودن integration tests برای شبیه‌سازی قطعی سرویس‌های AI و بررسی عملکرد fallback و load balancing
```

### Step 5: پیاده‌سازی integration test برای زمان بازیابی از خطا (recovery time) کمتر از 5 ثانیه
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن یک تست یکپارچگی است که سناریوی قطعی ناگهانی سرویس اصلی و سپس بازیابی آن را شبیه‌سازی می‌کند. تست باید زمان بین قطعی سرویس و بازیابی کامل (زمانی که سیستم دوباره به سرویس اصلی سوئیچ می‌کند) را اندازه‌گیری کند و اطمینان حاصل کند که این زمان کمتر از 5 ثانیه است. این تست در فایل tests/integration/test_recovery_time.py قرار می‌گیرد.
**Excerpt:**
```
پس از افزودن تست‌ها، 100% از سناریوهای fallback و load balancing باید بدون خطا کار کنند و زمان بازیابی از خطا کمتر از 5 ثانیه باشد
```

### Step 6: افزودن metric/log برای تشخیص نرخ موفقیت fallback و load balancing در production
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل افزودن logging و metrics به AI Manager است تا در production بتوان نرخ موفقیت fallback و load balancing را اندازه‌گیری کرد. باید یک metric (مثلاً با استفاده از Prometheus یا یک logger ساده) اضافه شود که تعداد موفقیت‌ها و شکست‌های fallback و تعداد درخواست‌های توزیع شده به هر سرویس را ثبت کند. این metric باید در endpoint/metrics یا در logهای ساختاریافته قابل مشاهده باشد.
**Excerpt:**
```
metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد

- [ ] metric/log اضافه شد تا در production outcome rate قابل تشخیص باشد
```

### Step 7: اجرای تمام تست‌ها و اطمینان از عدم شکست (pytest)
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای کامل مجموعه تست‌ها (شامل تست‌های جدید و قدیمی) با دستور `pytest` است. باید اطمینان حاصل شود که هیچ تستی fail نمی‌شود. اگر تستی fail شد، باید رفع شود (ممکن است نیاز به اصلاح mock یا تنظیمات داشته باشد). این مرحله شامل نوشتن تست جدید نیست.
**Excerpt:**
```
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
```

### Step 8: اجرای linter و رفع هشدارها
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل اجرای linter (مثلاً flake8, pylint, یا ruff) بر روی کل پروژه است. باید اطمینان حاصل شود که هیچ warning یا خطایی وجود ندارد. اگر warning وجود دارد، باید رفع شود (مثلاً با اصلاح فرمت کد یا اضافه کردن docstring). این مرحله شامل تغییر در logic نیست.
**Excerpt:**
```
- [ ] linter بدون warn
```
