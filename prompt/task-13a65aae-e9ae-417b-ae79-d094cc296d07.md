---
task_id: 13a65aae-e9ae-417b-ae79-d094cc296d07
title: '[منطق] ناسازگاری بین oversight_strong_prompt و prompt_helper'
type: logic_audit
priority: high
execution_priority: 2000
status: pending
external_status: pending
verification_status: pending
watched_id: 3f34a2b1-2a8d-4ad2-904a-9835a8a5b7c9
project: mahdighandi1989/project-management
created_at: '2026-05-15T10:29:58.038175+00:00'
updated_at: '2026-05-24T09:10:31.691353+00:00'
---

# [منطق] ناسازگاری بین oversight_strong_prompt و prompt_helper

## Raw Idea

## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

oversight_strong_prompt یک template سخت‌کد شده با فرمت ثابت تولید می‌کند، در حالی که prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند. این دو رویکرد موازی می‌توانند باعث inconsistency در فرمت پرامپت‌های نهایی شوند.

## 💥 پیامد (impact)
بسته به اینکه کدام مسیر استفاده شود، AI ممکن است پرامپت‌هایی با ساختار متفاوت دریافت کند که منجر به خروجی‌های غیرقابل پیش‌بینی می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
---
[scan #2 at 2026-05-24T09:10:31.691344+00:00]
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

oversight_strong_prompt یک template پرامپت بسیار دقیق می‌سازد، اما هیچ مکانیزمی برای جلوگیری از hallucination (مانند grounding به source files, fact-checking, یا self-consistency checks) در خود پرامپت یا pipeline تعبیه نشده است. ای

## Prompt

## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  باشد ("خودت گفتی" قابل قبول نیست).
- اگر معیارهای پذیرش (AC) مبهم/ناقص بودند، بهترین تفسیر را انتخاب کن و در
  commit message توضیح بده.

📦 **اگر کار طولانی است:**
- **خلاصه‌اش نکن.** همه را به‌طور کامل انجام بده.
- اگر یک کامیت گنجایش ندارد، در **چندین کامیت متوالی** انجام بده — ولی
  هیچ بخشی را skip نکن.
- ترتیب کامیت‌ها را منطقی نگه‌دار (foundation → core → integration → tests).
- در آخر یک checklist از همه‌ی کامیت‌ها در PR description بنویس.

---


## 🎯 هدف (خلاصه ساختاریافته)
[منطق] ناسازگاری بین oversight_strong_prompt و prompt_helper

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)_

## 🧭 هدف اصلی پروژه (از یادداشت کاربر)
(کاربر یادداشتی ثبت نکرده است)

## 🧱 پشتهٔ فناوری و معماری
Stack: fastapi, nextjs.

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.

## 🔍 Context و وضعیت فعلی
## 📋 شرح ناسازگاری
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

oversight_strong_prompt یک template سخت‌کد شده با فرمت ثابت تولید می‌کند، در حالی که prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند. این دو رویکرد موازی می‌توانند باعث inconsistency در فرمت پرامپت‌های نهایی شوند.

## 💥 پیامد (impact)
بسته به اینکه کدام مسیر استفاده شود، AI ممکن است پرامپت‌هایی با ساختار متفاوت دریافت کند که منجر به خروجی‌های غیرقابل پیش‌بینی می‌شود.

## 🛠 پیشنهاد رفع اولیه
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.

## 🤔 چرا مهم است
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.

## ✅ معیار پذیرش (Acceptance Criteria) — رفتار-محور
**مهم:** هر AC رفتار قابل مشاهده را تعریف می‌کند، نه نام فایل/کلاس.
verify می‌تواند پیاده‌سازی متفاوت ولی هم‌ارز را قبول کند.

- [ ] هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد
- [ ] ground truth تعیین شد و طرف دیگر align شد
- [ ] integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند
- [ ] PR description توضیح می‌دهد چرا این تصمیم گرفته شد
- [ ] هیچ تستی fail نمی‌شود (`npm run test` / `pytest`)
- [ ] linter بدون warning عبور می‌کند
- [ ] type-check موفق است (`tsc --noEmit` / `mypy`)

## 🪜 مراحل اجرایی پیشنهادی
1. گام ۱: هر دو طرف ناسازگاری را بخوان و فرض‌هایشان را لیست کن.
گام ۲: تصمیم بگیر کدام طرف ground truth است — معمولاً business logic مهم‌تر است.
گام ۳: طرف دیگر را با ground truth align کن.
گام ۴: integration test برای این pipeline بنویس تا regression جلوگیری شود.

## 📤 خروجی مورد انتظار
تغییر کد در فایل‌های مرتبط، commit یا PR جدید با پیام واضح، و عبور تمام معیارهای پذیرش.

## 🧪 دستورات اعتبارسنجی
- `pytest`
- `npm run test`

## ⚠️ ریسک‌ها و موارد احتیاط
تغییر یک طرف ممکن است downstream consumers را break کند. حتماً قبل از merge، همه caller های هر دو طرف را بررسی کن.

## 🔗 وابستگی‌های تسکی
_(مستقل)_

## 🏷 دسته‌بندی
- نوع: logic_audit
- اولویت: high
- تخمین زمان: medium

## Acceptance Criteria

1. هر دو طرف ناسازگاری شناسایی + فرض‌هایشان مستند شد _(verify: static)_
2. ground truth تعیین شد و طرف دیگر align شد _(verify: static)_
3. integration test برای pipeline `ai_llm` بدون شکست عبور می‌کند _(verify: backend_test)_
4. PR description توضیح می‌دهد چرا این تصمیم گرفته شد _(verify: manual_only)_

## Task Steps

### Step 1: بررسی و شناسایی فایل‌های مرتبط با oversight_strong_prompt و prompt_helper در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل جستجوی کامل در repository برای یافتن تمام فایل‌های مرتبط با oversight_strong_prompt و prompt_helper در pipeline ai_llm است. باید مشخص شود که oversight_strong_prompt در کدام فایل‌ها تعریف شده، چگونه template سخت‌کد شده را تولید می‌کند، و prompt_helper چگونه از دیتابیس می‌خواند. همچنین باید وابستگی‌های بین این دو مؤلفه و سایر بخش‌های pipeline شناسایی شود. خارج از این مرحله: تغییر کد یا طراحی راه‌حل.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

oversight_strong_prompt یک template سخت‌کد شده با فرمت ثابت تولید می‌کند، در حالی که prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند. این دو رویکرد موازی می‌توانند باعث inconsistency در فرمت پرامپت‌های نهایی شوند.

---

## 📍 موقعیت دقیق در پروژه
_(فایل‌های دقیق توسط مجری شناسایی شوند — هیچ موقعیت مشخصی استخراج نشد)

## 🌐 نقشهٔ وابستگی‌ها
این مورد در pipeline ai_llm است — همه فایل‌های این pipeline مرتبط هستند.
```

### Step 2: تحلیل ساختار و فرمت خروجی oversight_strong_prompt
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی دقیق کد oversight_strong_prompt برای درک ساختار template سخت‌کد شده، متغیرهای ورودی، و فرمت خروجی آن است. باید مشخص شود که این تابع چه نوع پرامپتی تولید می‌کند، چه بخش‌هایی ثابت و چه بخش‌هایی پویا هستند. همچنین باید بررسی شود که آیا این تابع از دیتابیس استفاده می‌کند یا کاملاً مستقل است. خارج از این مرحله: تغییر کد یا تحلیل prompt_helper.
**Excerpt:**
```
oversight_strong_prompt یک template سخت‌کد شده با فرمت ثابت تولید می‌کند، در حالی که prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند.
```

### Step 3: تحلیل ساختار و نحوه کار prompt_helper با دیتابیس
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی دقیق کد prompt_helper برای درک نحوه خواندن template‌ها از دیتابیس، ترکیب با متغیرها، و فرمت خروجی نهایی است. باید مشخص شود که prompt_helper از چه ساختار دیتابیسی استفاده می‌کند، چه نوع query‌هایی می‌زند، و خروجی نهایی چه فرمتی دارد. خارج از این مرحله: تغییر کد یا تحلیل oversight_strong_prompt.
**Excerpt:**
```
prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند.
```

### Step 4: مستندسازی تفاوت‌های فرمت خروجی بین oversight_strong_prompt و prompt_helper
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل مقایسه دقیق فرمت خروجی oversight_strong_prompt و prompt_helper و مستندسازی تفاوت‌های ساختاری، نحوه استفاده از متغیرها، و بخش‌های ثابت و پویا است. باید مشخص شود که چه بخش‌هایی از خروجی باعث inconsistency می‌شوند. خارج از این مرحله: تغییر کد یا طراحی راه‌حل.
**Excerpt:**
```
این دو رویکرد موازی می‌توانند باعث inconsistency در فرمت پرامپت‌های نهایی شوند.

بسته به اینکه کدام مسیر استفاده شود، AI ممکن است پرامپت‌هایی با ساختار متفاوت دریافت کند که منجر به خروجی‌های غیرقابل پیش‌بینی می‌شود.
```

### Step 5: طراحی معماری unified prompt builder با پشتیبانی از هر دو منبع template
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل طراحی معماری یک unified prompt builder است که بتواند هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند. معماری باید شامل یک interface مشترک برای دریافت template، یک سیستم تبدیل فرمت به استاندارد واحد، و یک مکانیزم برای انتخاب منبع template بر اساس نیاز باشد. خارج از این مرحله: پیاده‌سازی کد.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 6: پیاده‌سازی interface مشترک برای دریافت template در unified prompt builder
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک interface یا abstract class در Python است که متدهای مشترک برای دریافت template از منابع مختلف (دیتابیس و سخت‌کد شده) را تعریف می‌کند. این interface باید شامل متدهایی مانند get_template(template_id, variables) و get_available_templates() باشد. خارج از این مرحله: پیاده‌سازی پیاده‌سازی‌های concrete یا منطق تبدیل فرمت.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 7: پیاده‌سازی کلاس DatabaseTemplateProvider که از prompt_helper برای خواندن template از دیتابیس استفاده می‌کند
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک کلاس concrete به نام DatabaseTemplateProvider است که interface تعریف‌شده در مرحله قبل را پیاده‌سازی می‌کند. این کلاس باید از prompt_helper موجود برای خواندن template‌ها از دیتابیس استفاده کند و آن‌ها را به فرمت استاندارد تبدیل کند. خارج از این مرحله: تغییر کد prompt_helper موجود یا پیاده‌سازی HardcodedTemplateProvider.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 8: پیاده‌سازی کلاس HardcodedTemplateProvider که از oversight_strong_prompt برای تولید template سخت‌کد شده استفاده می‌کند
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی یک کلاس concrete به نام HardcodedTemplateProvider است که interface تعریف‌شده در مرحله 6 را پیاده‌سازی می‌کند. این کلاس باید از oversight_strong_prompt موجود برای تولید template سخت‌کد شده استفاده کند و خروجی آن را به فرمت استاندارد تبدیل کند. خارج از این مرحله: تغییر کد oversight_strong_prompt موجود یا پیاده‌سازی DatabaseTemplateProvider.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 9: پیاده‌سازی کلاس UnifiedPromptBuilder که از TemplateProviderها برای ساخت پرامپت نهایی استفاده می‌کند
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی کلاس اصلی UnifiedPromptBuilder است که از TemplateProviderهای مختلف (DatabaseTemplateProvider و HardcodedTemplateProvider) برای دریافت template استفاده می‌کند و آن‌ها را به یک فرمت استاندارد نهایی تبدیل می‌کند. این کلاس باید یک متد build_prompt(template_id, variables, source) داشته باشد که منبع template را مشخص می‌کند. خارج از این مرحله: پیاده‌سازی TemplateProviderها یا تست.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 10: تعریف فرمت استاندارد خروجی برای پرامپت‌ها در unified prompt builder
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تعریف یک dataclass یا struct در Python است که فرمت استاندارد خروجی پرامپت را مشخص می‌کند. این فرمت باید شامل فیلدهایی مانند system_prompt, user_prompt, context, variables_used, metadata باشد. همچنین باید یک متد to_dict() برای تبدیل به دیکشنری و یک متد validate() برای اعتبارسنجی داشته باشد. خارج از این مرحله: پیاده‌سازی منطق تبدیل یا TemplateProviderها.
**Excerpt:**
```
خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 11: پیاده‌سازی منطق تبدیل فرمت خروجی oversight_strong_prompt به فرمت استاندارد در HardcodedTemplateProvider
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطق تبدیل در HardcodedTemplateProvider است که خروجی oversight_strong_prompt را به فرمت استاندارد تعریف‌شده در مرحله قبل تبدیل می‌کند. باید بخش‌های مختلف پرامپت (system, user, context) استخراج و در فیلدهای مناسب قرار داده شوند. خارج از این مرحله: تغییر کد oversight_strong_prompt یا پیاده‌سازی منطق تبدیل برای DatabaseTemplateProvider.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 12: پیاده‌سازی منطق تبدیل فرمت خروجی prompt_helper به فرمت استاندارد در DatabaseTemplateProvider
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل پیاده‌سازی منطق تبدیل در DatabaseTemplateProvider است که خروجی prompt_helper را به فرمت استاندارد تعریف‌شده در مرحله 10 تبدیل می‌کند. باید بخش‌های مختلف پرامپت (system, user, context) استخراج و در فیلدهای مناسب قرار داده شوند. خارج از این مرحله: تغییر کد prompt_helper یا پیاده‌سازی منطق تبدیل برای HardcodedTemplateProvider.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 13: یکپارچه‌سازی unified prompt builder با pipeline ai_llm و جایگزینی فراخوانی‌های مستقیم oversight_strong_prompt و prompt_helper
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل تغییر کدهای موجود در pipeline ai_llm است که به طور مستقیم از oversight_strong_prompt یا prompt_helper استفاده می‌کنند تا به جای آن از UnifiedPromptBuilder استفاده کنند. باید تمام نقاط استفاده شناسایی و با فراخوانی UnifiedPromptBuilder.build_prompt() جایگزین شوند. خارج از این مرحله: تست یا تغییر سایر pipelineها.
**Excerpt:**
```
در pipeline `ai_llm` یک ناسازگاری منطقی پیدا شد:

oversight_strong_prompt یک template سخت‌کد شده با فرمت ثابت تولید می‌کند، در حالی که prompt_helper پرامپت‌ها را از دیتابیس می‌خواند و با متغیرها ترکیب می‌کند.
```

### Step 14: نوشتن unit tests برای کلاس UnifiedPromptBuilder
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای کلاس UnifiedPromptBuilder است که رفتار آن را در سناریوهای مختلف تست می‌کند: استفاده از DatabaseTemplateProvider، استفاده از HardcodedTemplateProvider، استفاده از template نامعتبر، و استفاده از متغیرهای مختلف. تست‌ها باید با pytest نوشته شوند و coverage حداقل 90% داشته باشند. خارج از این مرحله: integration tests یا tests برای TemplateProviderها.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 15: نوشتن unit tests برای DatabaseTemplateProvider
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای کلاس DatabaseTemplateProvider است که رفتار آن را در سناریوهای مختلف تست می‌کند: خواندن template معتبر از دیتابیس، خواندن template نامعتبر، و تبدیل به فرمت استاندارد. تست‌ها باید با pytest و با استفاده از mock برای دیتابیس نوشته شوند. خارج از این مرحله: tests برای HardcodedTemplateProvider یا integration tests.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 16: نوشتن unit tests برای HardcodedTemplateProvider
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن unit tests برای کلاس HardcodedTemplateProvider است که رفتار آن را در سناریوهای مختلف تست می‌کند: استفاده از oversight_strong_prompt معتبر، استفاده از template نامعتبر، و تبدیل به فرمت استاندارد. تست‌ها باید با pytest و با استفاده از mock برای oversight_strong_prompt نوشته شوند. خارج از این مرحله: tests برای DatabaseTemplateProvider یا integration tests.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 17: نوشتن integration tests برای unified prompt builder در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل نوشتن integration tests است که کل pipeline ai_llm را با unified prompt builder تست می‌کند. تست‌ها باید سناریوهای واقعی استفاده از هر دو منبع template را پوشش دهند و اطمینان حاصل کنند که خروجی نهایی همیشه با فرمت استاندارد مطابقت دارد. خارج از این مرحله: unit tests یا tests برای سایر pipelineها.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 18: بررسی و حذف کدهای تکراری یا قدیمی مرتبط با تولید پرامپت پس از یکپارچه‌سازی
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل بررسی کامل کدهای pipeline ai_llm برای یافتن هرگونه کد تکراری، قدیمی، یا unused مرتبط با تولید پرامپت است که پس از یکپارچه‌سازی unified prompt builder باقی مانده است. این کدها باید حذف یا با فراخوانی unified prompt builder جایگزین شوند. خارج از این مرحله: تغییر کدهای خارج از pipeline ai_llm.
**Excerpt:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است.
```

### Step 19: به‌روزرسانی مستندات فنی مرتبط با تولید پرامپت در pipeline ai_llm
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل به‌روزرسانی مستندات فنی موجود (مانند README، docstrings، یا فایل‌های docs) برای منعکس‌سازی تغییرات ایجاد شده است. باید نحوه استفاده از UnifiedPromptBuilder، TemplateProviderها، و فرمت استاندارد خروجی مستند شود. خارج از این مرحله: مستندات کاربری یا آموزش.
**Excerpt:**
```
یک unified prompt builder ایجاد کنید که هم از template‌های دیتابیس (از طریق prompt_helper) و هم از template‌های سخت‌کد شده (مانند oversight_strong_prompt) پشتیبانی کند و خروجی نهایی را به یک فرمت استاندارد تبدیل کند.
```

### Step 20: انجام audit نهایی برای اطمینان از رفع کامل ناسازگاری
**Status:** `not_done` (0%)
**Scope:** این مرحله شامل یک audit نهایی است که بررسی می‌کند آیا ناسازگاری بین oversight_strong_prompt و prompt_helper به طور کامل رفع شده است. باید تمام مسیرهای کد که پرامپت تولید می‌کنند بررسی شوند تا اطمینان حاصل شود که همه از UnifiedPromptBuilder استفاده می‌کنند و خروجی نهایی همیشه با فرمت استاندارد مطابقت دارد. خارج از این مرحله: تغییر کد یا اضافه کردن feature جدید.
**Excerpt:**
```
coherence issue یعنی دو بخش کد فرض‌های ناسازگار دارند — معمولاً نشانه‌ی refactor ناتمام یا feature flag rot است. این کلاس bug ها در test معمولی پیدا نمی‌شوند چون unit test ها در silo اجرا می‌شوند.
```
