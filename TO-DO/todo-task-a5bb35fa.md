# TODO — Task a5bb35fa (نیاز به تکمیل دستی)

> **پیاده‌سازی مرکز دانش**

## 🔎 خلاصه وضعیت

- **task_id**: `a5bb35fa-85bc-4879-a9bf-f20d8d85e061`
- **repo**: `mahdighandi1989/project-management`
- **verification_status**: `partial`
- **archived_reason**: `max_retries` — Claude به سقف retry رسید بدون اینکه verify=done شود
- **retries_done**: 1
- **verifier confidence**: 0.95
- **verifier model**: `—`
- **report_id**: `efccf589-bdc9-42b5-8d03-174bf2c8c19b`
- **created_at**: 2026-06-05T09:56:39.149900+00:00

## 🚧 چه چیزی باقی مانده (مهم‌ترین بخش)

- [ ] منوی سایدبار لینک مرکز دانش را ندارد (data-testid='nav-knowledge-center' یافت نشد)
- [ ] یکپارچه‌سازی Cloud Code برای ثبت دستی تجربیات پیاده‌سازی نشده است

## 👉 قدم‌های بعدی پیشنهادی (از verifier)

1. افزودن لینک مرکز دانش به کامپوننت سایدبار با data-testid='nav-knowledge-center'
2. پیاده‌سازی API برای Cloud Code جهت ثبت دستی تجربیات در مخزن داده مرکزی

## ✅ چه چیزی Claude انجام داد

- [x] صفحه مرکز دانش در frontend/src/app/knowledge-center/page.tsx ایجاد شده است
- [x] لینک مرکز دانش در داشبورد اصلی (page.tsx) اضافه شده است
- [x] endpoint GET /api/knowledge-center/entries با status 200 کار می‌کند
- [x] سرویس KnowledgeCenterService با متد ensure_experiences_folder تعریف شده است
- [x] endpoint ایمپورت چت فرمت‌های txt/md/html/pdf را می‌پذیرد و chunking دارد
- [x] منطق merge/dedup با ثبت reference منبع پیاده‌سازی شده است
- [x] نمایش دانشنامه‌ای شامل TOC و دسته‌بندی در خروجی endpoint موجود است
- [x] پوشه تجربیات به صورت خودکار برای پروژه‌های تحت نظارت ساخته می‌شود
- [x] همگام‌سازی پوشه‌های تجربه (sync_experiences) پیاده‌سازی شده است
- [x] رابط کاربری آپلود لاگ چت در صفحه مرکز دانش وجود دارد
- [x] استخراج چالش‌ها و راه‌حل‌ها با AI از چت‌ها پیاده‌سازی شده است
- [x] منطق ادغام هوشمند و ارجاع‌دهی برای تجربیات تکراری پیاده‌سازی شده است

## 📝 خلاصهٔ verifier

مرکز دانش تا حد زیادی پیاده‌سازی شده: صفحه، endpoint، سرویس، ایمپورت چت، merge/dedup، همگام‌سازی پوشه و نمایش TOC همگی موجودند. دو مشکل باقی‌مانده: (1) لینک مرکز دانش در منوی سایدبار وجود ندارد (فقط در داشبورد اصلی است) و (2) یکپارچه‌سازی Cloud Code برای ثبت دستی تجربیات انجام نشده است.

## 📋 Acceptance Criteria (مرجع کامل)

این لیست معیار done شدن تسک است — هر آیتمی که هنوز satisfy نیست
باید توسط انسان تکمیل شود.

- صفحهٔ جدید 'مرکز دانش' در منوی sidebar نمایش داده می‌شود و قابل ناوبری است
- لینک/کارت مرکز دانش در داشبورد اصلی موجود است
- endpoint GET /api/knowledge-center/entries لیست فهرست‌بندی‌شدهٔ تجربیات را با status 200 برمی‌گرداند
- سرویس KnowledgeCenterService با متد ensure_experiences_folder تعریف شده است
- endpoint ایمپورت چت فرمت‌های txt/md/html/pdf را می‌پذیرد و chunking برای فایل حجیم دارد
- منطق merge/dedup هنگام افزودن تجربهٔ تکراری، داده‌های جدید را ادغام و reference منبع را ثبت می‌کند
- نمایش دانشنامه‌ای صفحه شامل فهرست (TOC) و دسته‌بندی مناسب باشد

## 🔬 Evidence که verifier پیدا کرد

**Commits:**
- `647d6a3`
- `97770b7`
- `da2959b`

**Files lams شده:**
- `backend/app/services/knowledge_center_service.py`
- `backend/app/api/routes/knowledge_center.py`
- `frontend/src/app/knowledge-center/page.tsx`
- `frontend/src/app/page.tsx`
- `backend/tests/test_knowledge_center.py`

## 💡 ایدهٔ اصلی تسک

[ایدهٔ متنی همراه نیست — دستورالعمل/درخواست کاربر **داخل** محتوای فایل‌های پیوست است. لطفاً متن استخراج‌شدهٔ فایل‌ها را بخوان، دستورالعمل را از آنجا برداشت کن، و یک پرامپت کامل بساز.]

---
## 📎 فایل‌های پیوست (به ترتیب آپلود = ترتیب بخش‌ها)

## 📎 فایل پیوست #1: voice_499968_AgADrx0A.ogg
_mime=audio/ogg • model=gemini-2.5-flash • 5 segment استخراج شد • 12,151 char متن_

## درخواست اصلی کاربر  _(at: audio/ogg)_

`--- صفحه 1 ---`
[00:00] می‌خوام یه صفحه ایجاد بشه تحت عنوان مرکز دانش.
[00:05] یه صفحه‌ای که حالا جدا از صفحات دیگه توی منو هم باید باشه، منوی سایدبار کنارش باید این صفحه هم موجود دیده بشه.
[00:14] و همین‌طور توی حالا داشبورد اصلی.
[00:17] که این صفحه کارش اینه، من مثلاً به محض اینکه این صفحه ایجاد می‌شه،
[00:24] باید در ابتدا تمام پروژه‌هایی که توی مرکز نظارت تحت نظارت هستند رو
[00:30] بره توی پوشه گیت‌هابشون، حالا اگر اون پروژه مثلاً توی گیت‌هابه که توی گیت‌هابه، اگه به صورت محلیه، یه پوشه مثلاً تو اون قسمتی که هستش
[00:40] ایجاد می‌کنه تحت عنوان اکسپرینس، اکسپرینسز یا تجربیات، همچین چیزی.
[00:47] تو اون پوشه مخزن، حالا گیت‌هاب یا محلی،
[00:52] ایجاد می‌کنه و حالا فعلاً خالیه این.
[00:57] و سینک می‌شه به اون صفحه.
[01:00] یه چیز، یعنی این پوشه هرچی داخلش قرار می‌گیره،
[01:04] به صورت منظم سینک می‌شه داخل اون صفحه
[01:08] مرکز دانش، حالا یا تجربیات از این چیزا.
[01:12] من چیکار می‌کنم؟ من مثلاً زمانی که حالا یه تسکی رو به صورت دستی
[01:17] می‌رم توی کلادکود یا جایی اجرا می‌کنم، چون کلادکود دسترسی داره به
[01:22] پوشه‌های گیت‌هاب و اینا. یه مشکلی رو مثلاً یه تسکی رو حا

## 📜 پرامپت اصلی (excerpt)

```
## ⚠️ یادداشت مهم برای مدل اجراکننده — قبل از شروع بخوان

این پرامپت بر اساس یک **بررسی اولیهٔ خودکار** از repo ساخته شده — ممکن است
حاوی اشتباه، تشخیص نادرست، یا حذف موارد مهم باشد. به‌عنوان منبع نهایی به
آن استناد نکن.

📖 **خواندن کامل + اجرای مو-به-مو (بسیار مهم):**

این پرامپت — از این یادداشت تا انتها — یک سند واحد است که هر بخشش
حاوی الزام یا context منحصربه‌فرد است. خواندن سطحی یا skim کردن **ممنوع**
است.

- پرامپت را **سطر به سطر** بخوان، نه head/tail/فقط-بخش-اصلی.
- اگر بخشی به‌نظر طولانی یا تکراری آمد، **حتماً** بخوان — تفاوت‌های
  ریز ممکن است در آن جا اساسی باشند.
- هر جمله، URL، نام فایل، نام تابع، یا مقدار عددی که در پرامپت آمده،
  دقیقاً همان است که کاربر می‌خواهد — تغییرش نده، رندش نکن، خلاصه‌اش
  نکن.
- اگر پرامپت چندین درخواست/مرحله/زیرتسک دارد، **همه** را پیاده کن. حتی
  یکی را نه به‌عنوان "خارج از scope" حذف کن.

❌ ممنوعات صریح:
- خلاصه‌سازی متن کاربر در commit message یا response
- "این بخش اصلی نیست، رد می‌کنم"
- "کاربر احتمالاً منظورش این بود..." — منظورش همان است که نوشته
- "این URL/نام به نظر قدیمی است، آپدیتش کردم" — تغییر بدون درخواست ممنوع
- پیاده‌سازی فقط بخشی از پرامپت و تظاهر به کامل بودن
- "همه آیتم‌های لیست A را بررسی کردم، B و C مشابه بودند" — نه؛
  هرکدام را جداگانه

♻️ **احتمال پیاده‌سازی قبلی (مهم):**
- ممکن است **بخشی یا تمامِ** این درخواست قبلاً (به صورت کامل یا ناقص) در
  repo پیاده‌سازی شده باشد. پیش از شروع، با grep/search و خواندن فایل‌های
  مرتبط بررسی کن که چه چیزی **از قبل وجود دارد**.
- اگر یک قابلیت/فایل/تابع از قبل موجود است: آن را **دوباره نساز**؛ فقط
  موارد ناقص یا اشتباه را اصلاح/تکمیل کن.
- اگر همه چیز از قبل به‌درستی انجام شده: یک کامیت توضیحی (no-op) ثبت کن که
  چرا تغییری لازم نبود و دقیقاً کدام فایل‌ها این درخواست را پوشش می‌دهند.

🔍 **مسئولیت تو (مدل اجراکننده):**
- پیش از هر تغییر، خودت ساختار repo، فایل‌های ذکرشده، و وابستگی‌های آن‌ها را
  مستقل بررسی کن.
- اگر تشخیص دادی موقعیت ذکرشده در پرامپت اشتباه است یا فایل دیگری مناسب‌تر
  است، بر اساس قضاوت خودت عمل کن — این پرامپت نمی‌تواند بهانهٔ کار اشتباه
  با

_[truncated — full prompt در پنل]_
```

---

_این فایل توسط Claude Auto-Runner تولید شده است. تسک با حالت_ `max_retries` _آرشیو شده و دیگر به‌صورت خودکار pickup نمی‌شود._